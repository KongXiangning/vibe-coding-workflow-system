# 面向大型 Vibe Coding 项目的质量控制体系

这份文档基于对 `gstack` 的分析，提炼出一套更适合个人开发者、`Codex CLI + Copilot CLI` 工作方式的实践体系。

目标不是降低开发速度，而是在继续保持高速度的前提下，尽量避免以下问题：

- 新需求开发时误改已经稳定的功能、方法、接口
- 局部改动扩散到无关模块，导致隐性回归
- 功能增加、移除、重构后，项目状态逐渐失控
- AI 连续尝试修复，反而引入更多新问题

---

## 核心目标

这套体系解决 3 类失控：

| 层次 | 常见症状 | 对应控制手段 |
| --- | --- | --- |
| 代码层 | 旧接口、旧函数被顺手改掉 | 契约锁定 |
| 变更层 | 一次需求修改波及无关文件 | 范围锁定 |
| 项目层 | 不知道哪些做完了、哪些稳定、哪些该停掉 | 状态追踪 |

核心思想很简单：

1. 先声明哪些东西不能乱动
2. 每次只允许 AI 在明确边界内工作
3. 每次完成后检查实际改动是否超出任务范围
4. 用持续更新的项目状态文件防止上下文漂移
5. 当修复开始失控时，强制停下来重新诊断

---

## 五层防线

### 第一层：契约锁定

目的：防止 AI 修改已经稳定的接口、核心函数、数据库结构，**以及系统结构本身**。

契约分为两层：

#### 层 A：接口契约

记录已经稳定的、对外暴露的接口边界：

- 已上线 API 的路径、入参、返回结构
- 核心函数签名
- 已上线表结构和关键字段
- 已确认稳定的工具模块导出接口

#### 层 B：架构契约

记录系统内部的结构规则——这些往往比函数签名更容易被 AI 悄悄破坏：

- 依赖方向：哪些模块只能被哪些模块依赖，反向依赖禁止
- 状态流：全局状态从哪里产生、经过哪些层、在哪里消费
- 目录职责：每个目录的职责边界和可访问范围
- DTO/事件语义：关键 payload 的字段名、类型、含义不可改
- 分层规则：页面层不能直连 DB，服务层不能直接操作 DOM 等

推荐分为三类：

- `🔒 已锁定`：修改前必须停下来征求确认
- `🟡 可扩展不可破坏`：可以新增，不能直接改已有契约
- `🟢 自由修改`：新功能实现、页面样式、测试文件等

示例：

```md
# CONTRACTS.md

## 一、接口契约

### 🔒 已锁定接口
- POST /api/auth/login -> { token, user }
- GET /api/users/:id -> { id, name, email, avatar }

### 🔒 已锁定核心函数
- src/lib/auth.ts: verifyToken(token: string) => Promise<User | null>
- src/lib/db.ts: query(sql: string, params: any[]) => Promise<Row[]>

### 🔒 已锁定表结构
- users(id, email, name, avatar, created_at)
- orders(id, user_id, status, total, created_at)

### 🟡 可扩展不可破坏
- 可以新增 API 路由，不能改已有路由的入参和返回
- 可以新增数据库表，不能改已有表结构
- 可以新增工具函数，不能改已有导出签名

### 🟢 自由修改
- 页面 UI / 样式
- 新功能目录
- 测试文件

## 二、架构契约

### 🔒 依赖方向
- pages/ → services/ → lib/ → utils/（单向依赖，禁止反向）
- components/ 不能直接 import services/ 或 lib/
- lib/ 不能 import pages/ 或 components/

### 🔒 状态流
- 全局认证状态只从 src/store/auth.ts 产生
- 订单状态只从 src/store/orders.ts 产生
- 页面组件通过 hooks 消费状态，禁止直接操作 store 内部

### 🔒 目录职责
- src/lib/：纯逻辑层，无 UI 依赖，无副作用
- src/api/：路由定义和请求处理，不含业务逻辑
- src/services/：业务逻辑层，可调用 lib/，不能直接返回 HTTP 响应

### 🔒 DTO/事件语义
- OrderCreatedEvent: { orderId: string, userId: string, items: Item[], total: number }
- 字段名和类型已固定，不可改名或改类型

### 🔒 分层规则
- 页面层禁止直连数据库
- API 路由层禁止包含业务判断逻辑
- 工具函数禁止依赖业务实体类型
```

配套指令建议：

```md
修改任何代码前，先读 CONTRACTS.md。
如果要修改 🔒 内容（包括接口契约和架构契约），必须先停下来说明原因并等待确认。
如果要修改 🟡 内容，允许操作，但需要在完成后明确告知变更点。
特别注意：不要改变模块间的依赖方向、不要跨层直接调用、不要改变已锁定的事件字段名。
```

---

### 第二层：范围锁定

目的：防止 AI 为了完成一个需求，把无关模块也顺手改掉。

每次提需求时，都要明确：

- 本次任务是什么
- 允许修改哪些文件或目录
- 不允许修改哪些文件或目录
- 如果必须越界修改，必须先说明原因

推荐任务描述模板：

```md
这次任务：给订单列表添加按状态筛选功能

允许修改：
- src/pages/orders/
- src/api/orders.ts
- src/components/FilterBar.tsx

不允许修改：
- src/lib/
- src/api/auth.ts
- src/api/users.ts
- 数据库 migration

如果必须修改范围外文件，先停下来告诉我：
1. 要改哪个文件
2. 为什么不改无法完成
3. 打算怎么改
```

配套指令建议：

```md
任务完成后，运行 git diff --stat。
列出所有实际改动文件，并标注：
- 是否在允许范围内
- 哪些属于范围外改动
- 范围外改动是否经过确认
```

这一步对应 gstack 的 `freeze + scope drift detection` 思路。

---

### 第三层：项目状态追踪

目的：让 AI 和你自己都能持续知道：

- 哪些功能已经稳定，不该随便碰
- 哪些功能正在开发
- 哪些需求待做
- 哪些需求已经取消或推迟

建议维护 `STATUS.md`：

```md
# STATUS.md

## ✅ 已完成且稳定
- [x] 用户注册/登录
- [x] 用户资料管理
- [x] 商品展示

## 🔨 正在开发
- [ ] 订单系统
  - [x] 创建订单
  - [x] 订单列表
  - [ ] 订单状态筛选
  - [ ] 订单详情页

## 📋 待开发
- [ ] 支付集成
- [ ] 消息通知

## ❌ 已移除/推迟
- 积分系统
- 社交分享
```

配套规则：

- 每次任务开始前先读 `STATUS.md`
- `✅` 的功能默认视为稳定功能，禁止为了新需求顺手重构
- 任务完成后更新状态
- 如果需求被取消，也要记录到 `❌ 已移除/推迟`

这一步对应 gstack 的 `TODOS + Plan Completion Audit` 思路。

---

### 第四层：变更验证

目的：在每轮开发结束时确认：

- 改动是否超出了计划
- 是否误伤了已锁定接口
- 是否影响稳定功能

推荐固定执行 4 个检查：

#### 1. 范围检查

```bash
git --no-pager diff --stat
```

检查：

- 修改文件数是否明显超出预期
- 是否出现未授权目录
- 是否出现“顺手重构”痕迹

#### 2. 契约检查

对照 `CONTRACTS.md`：

- 已锁定 API 是否改了路径、入参、返回结构
- 已锁定函数是否改了签名
- 已锁定表结构是否改了字段或语义

只要命中，就必须视为高优先级风险。

#### 3. 回归检查

如果有测试：

- 运行相关测试
- 至少跑与本次改动直接相关的测试

如果没有测试：

- 对 `STATUS.md` 中已稳定功能做最小 smoke check
- 检查关键入口、关键 import、关键页面或接口是否仍可工作

#### 4. 结果总结

每次任务结束都输出固定摘要：

```md
本次修改摘要：
- 修改文件：N 个
- 范围内修改：N 个
- 范围外修改：N 个
- 已锁定契约是否变化：是 / 否
- 测试结果：通过 / 失败 / 无测试
- 是否建议进入下一步：是 / 否
```

这一步对应 gstack 的 `review + ship + qa` 组合思路。

---

### 第五层：失控停止机制

目的：当 AI 修 bug 或改需求时，如果开始反复试错、引入额外破坏，就强制停下来。

推荐规则：

1. 先诊断，再修复
2. 只做最小修复，不顺手重构
3. 如果连续 3 次尝试仍未解决，停止继续猜测
4. 停止时输出根因分析、尝试记录、下一步建议

推荐指令：

```md
修 bug 时先确认根因，再动手修改。
只允许做最小修复，不要顺手优化或重构。
如果连续 3 次尝试没有解决问题，停止并汇报：
1. 你确认过的现象
2. 你认为的根因
3. 你已经尝试过什么
4. 你建议的下一步
```

这一步对应 gstack 的 `investigate` 和 QA 中的自我停止逻辑。

---

## 推荐的六个配套文件

如果要把这套体系真正落地到项目里，建议固定维护以下文件：

### 1. `CLAUDE.md` 或项目级 AI 指令文件

作用：定义 AI 必须遵守的工作规则。

至少应包含：

- 先读哪些文件（CONTRACTS.md、STATUS.md、DECISIONS.md、LESSONS.md）
- 哪些契约不能改（接口契约 + 架构契约）
- 越界修改时要停下来
- 完成后要做哪些检查
- 修 bug 的停止条件

### 2. `CONTRACTS.md`

作用：定义稳定边界，分两层。

- **接口契约**：API 路径、函数签名、表结构、模块导出
- **架构契约**：依赖方向、状态流、目录职责、DTO 语义、分层规则

接口契约保护的是"外部可见的稳定性"；架构契约保护的是"内部结构的一致性"。
对于大型项目，架构契约往往比接口契约更容易被 AI 悄悄破坏。

### 3. `STATUS.md`

作用：定义项目真实状态。

适合记录：

- 已稳定功能
- 正在开发功能
- 待开发功能
- 已取消功能

### 4. `DECISIONS.md`

作用：记录已确认的决策、口味选择、暂缓项、废弃项。

这是从"流程系统"升级为"治理系统"的关键文件。大项目失控往往不是代码先坏，
而是需求解释权被 AI 偷走了——AI 在你没注意的时候，用它自己的理解替换了你的决策。

DECISIONS.md 要记录的不是"做了什么"，而是"为什么这么做"和"哪些不做"。

推荐分类：

```md
# DECISIONS.md

## 🏗️ 架构决策

### AD-001: 使用 Supabase 而不是自建后端
- 原因：单人项目，运维成本是核心瓶颈
- 约束：所有数据访问必须通过 Supabase client，不能直连 PostgreSQL
- 不可逆性：高（迁移成本大）

### AD-002: 前端状态管理用 Zustand
- 原因：API 简单，适合中小规模状态
- 约束：全局状态集中在 src/store/，组件内部状态用 useState
- 替代方案：Redux（被否决，太重）

## 🎨 口味决策

### TD-001: 表单验证用前端即时验证，不等后端
- 这是口味选择，不是技术必然
- AI 不可以自行改为"提交后再验证"

### TD-002: 错误提示用 toast，不用 inline error
- 这是风格统一的选择
- AI 不可以在某些页面自行切换为 inline

## ⏸️ 暂缓决策

### DEFER-001: 是否引入 i18n
- 目前不做，v2 再评估
- AI 不可以自行添加 i18n 相关代码或依赖

### DEFER-002: 是否拆分微服务
- 目前保持单体
- AI 不可以自行引入服务间通信

## ❌ 已否决

### REJECTED-001: GraphQL
- 已评估，不采用，原因：单人维护 schema 成本高
- 如果 AI 建议切换到 GraphQL，应直接拒绝
```

### 5. `LESSONS.md`

作用：记录跨会话经验，避免反复踩同样的坑。

示例：

```md
# LESSONS.md

## 数据库
- 新表创建后要立即确认索引和约束
- 迁移脚本命名需要稳定，否则容易重复执行

## 前端
- 某页面依赖服务端返回特定字段，字段名不可随意调整
- 某组件状态逻辑复杂，新增功能前先看现有条件分支

## 部署
- 环境变量变更后需要重新部署才会生效
```

### 6. `CURRENT_TASK.md`

作用：把"当前变更"从散落在对话里的上下文，变成一个可追溯的标准任务包。

每次开始一个新任务时，创建或覆盖这个文件。任务完成后归档（可选移入 TASKS/ 目录）。

模板：

```md
# CURRENT_TASK.md

## 任务信息
- 任务 ID：TASK-012
- 目标：给订单列表添加按状态筛选功能
- 创建时间：2025-01-15

## 验收标准
- 订单列表页有状态下拉筛选器
- 选择状态后列表实时过滤
- 不选时显示全部订单

## 允许修改范围
- src/pages/orders/
- src/api/orders.ts
- src/components/FilterBar.tsx（新建）

## 禁止修改范围
- src/lib/
- src/api/auth.ts
- src/api/users.ts
- 数据库 migration

## 受影响的契约
- GET /api/orders 新增可选参数 ?status=xxx（🟡 扩展，不改已有行为）
- 不影响其他 🔒 契约

## 回归检查项
- 不带 status 参数时，列表行为与原来完全一致
- 订单创建功能不受影响
- 用户登录/认证流程不受影响

## 回滚点
- 如果筛选功能导致列表性能问题，回退方案是移除 FilterBar 组件
- git stash 或 git revert 到任务开始前的 commit

## 决策分类
- 筛选 UI 放在列表上方（口味决策，AI 不可自行改位置）
- 使用前端过滤而非后端分页（技术决策，数据量 < 1000 条时成立）

## 执行记录
- [ ] 步骤 1：新增 API 参数解析
- [ ] 步骤 2：后端查询逻辑
- [ ] 步骤 3：前端 FilterBar 组件
- [ ] 步骤 4：集成联调 + 回归验证
```

CURRENT_TASK.md 的核心价值是：**让每次改动都有来源、有边界、有证据、有回滚、有后续状态同步**。
它不是给人看的文档，而是 AI 每轮开始工作前必须读取的"工单"。

---

## 推荐工作流

把上面的五层防线串起来后，一个比较稳的日常循环如下：

### 1. 接需求

- 先更新 `STATUS.md`
- 写清楚这次任务的目标和边界
- 明确哪些目录能改，哪些不能改

### 2. 开工前

- 让 AI 先读 `CLAUDE.md`、`CONTRACTS.md`、`STATUS.md`、`LESSONS.md`
- 告诉 AI：本次只允许在指定范围内修改

### 3. 实现中

- 如果 AI 想改范围外内容，必须先报告
- 如果触碰稳定契约，必须停下来确认
- 如果修复 3 次仍失败，立即停止继续猜

### 4. 完成后

- 跑 `git diff --stat`
- 检查契约是否被破坏
- 跑测试或做 smoke check
- 更新 `STATUS.md`
- 把新坑写进 `LESSONS.md`

---

## 适合你的最小落地版本

如果不想一开始就维护太多文件，可以先只做最小集合：

1. `CLAUDE.md`
2. `CONTRACTS.md`（至少写好接口契约层）
3. `STATUS.md`

只靠这三个文件，就已经能显著降低下面这些风险：

- 新功能误改旧接口
- 改动范围失控
- 项目状态混乱

当项目继续变大、出现"决策被 AI 悄悄覆盖"的情况时，补上 `DECISIONS.md`。

当你发现"每次任务边界总是散落在对话里、下一轮就丢了"时，补上 `CURRENT_TASK.md`。

当你发现"同样的坑反复踩"时，补上 `LESSONS.md`。

推荐的升级路径：

```text
最小集合    CLAUDE.md + CONTRACTS.md + STATUS.md
    ↓
中级集合    + DECISIONS.md + LESSONS.md
    ↓
完整集合    + CURRENT_TASK.md（或 TASKS/ 目录归档历史任务）
```

---

## 与 gstack 的对应关系

这套轻量体系不是照搬 gstack，而是把它最有价值的部分翻译成更适合个人 vibe coding 的用法：

| gstack 机制 | 对应简化实现 |
| --- | --- |
| `freeze` | 任务级允许修改范围（CURRENT_TASK.md） |
| `careful` | 危险操作需确认 |
| `review scope drift` | 任务后对比 `git diff --stat` + 范围复核 |
| `plan completion audit` | 用 `STATUS.md` 追踪完成与变更 |
| `autoplan decision types` | 用 `DECISIONS.md` 区分架构/口味/暂缓/否决 |
| `SPEC_LOCKED / plan items` | 用 `CURRENT_TASK.md` 的验收标准和执行记录 |
| `learnings` | 用 `LESSONS.md` 记录项目经验 |
| `investigate` | 3 次失败自动停机 |
| `qa regression thinking` | 每次完成后做最小回归检查 |
| `review-army / adversarial` | Codex CLI 做第二视角审查 |
| `architecture diagrams` | `CONTRACTS.md` 架构契约层 |
| `document-release` | 每轮结束同步 STATUS/CONTRACTS/DECISIONS |

---

## 最终结论

对于大型 vibe coding 项目，最重要的不是让 AI 更聪明，而是建立一套让 AI **不容易失控** 的边界系统。

真正有效的体系通常不是“更复杂”，而是把这几件事固定下来：

- 哪些东西不能改
- 这次允许改哪里
- 项目现在处于什么状态
- 每次改完后如何验证没有越界
- 什么时候必须停下来，不再继续猜

如果这些规则是显式的、持久的、每轮开发都会被读取的，那么即使项目变大、需求频繁变化，整体也会稳很多。
