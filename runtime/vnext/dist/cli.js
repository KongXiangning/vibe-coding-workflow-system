// runtime/vnext/src/project-documents.ts
var heading = "### Project documents";
function invalid(message) {
  throw new Error(`PROJECT_DOCUMENTS_INVALID: ${message}`);
}
function normalizeProjectDocuments(value) {
  if (!Array.isArray(value) || value.length > 64)
    invalid("expected at most 64 document references");
  const seen = new Set;
  return value.map((entry, index) => {
    if (!entry || typeof entry !== "object" || Array.isArray(entry) || Object.keys(entry).sort().join(",") !== "path,purpose,revision,section")
      invalid(`reference ${index} fields mismatch`);
    const result = {};
    for (const key of ["path", "section", "revision", "purpose"]) {
      const text = entry[key];
      if (typeof text !== "string" || !text.trim() || text.length > 1024 || /[\r\n\x00-\x1f]/u.test(text))
        invalid(`reference ${index}.${key} must be bounded single-line text`);
      result[key] = text.trim();
    }
    if (result.path.includes("\\") || result.path.includes(":") || result.path.startsWith("/") || result.path.split("/").some((part) => !part || part === "." || part === ".."))
      invalid("path must be repository-relative with forward slashes");
    const identity = JSON.stringify([result.path, result.section]);
    if (seen.has(identity))
      invalid("duplicate path and section");
    seen.add(identity);
    return result;
  });
}
function renderProjectDocuments(value) {
  return value === undefined ? "" : `

${heading}

\`\`\`json
${JSON.stringify({ version: 1, sources: normalizeProjectDocuments(value) })}
\`\`\``;
}
function readProjectDocuments(background) {
  const normalized = background.replace(/\r\n?/gu, `
`);
  const sections = normalized.split(/^### Project documents\s*$/mu);
  if (sections.length === 1)
    return null;
  if (sections.length !== 2)
    invalid("duplicate section");
  const content = sections[1].split(/^#{1,3} /mu)[0].trim();
  const match = /^```json\n([^]*?)\n```$/u.exec(content);
  if (!match)
    invalid("expected versioned JSON block");
  let value;
  try {
    value = JSON.parse(match[1]);
  } catch {
    invalid("malformed JSON");
  }
  if (!value || Object.keys(value).sort().join(",") !== "sources,version" || value.version !== 1)
    invalid("unsupported record version or fields");
  return normalizeProjectDocuments(value.sources);
}

// runtime/vnext/src/task-storage-metrics.ts
import * as fs5 from "node:fs";
import * as path5 from "node:path";
import { parse as parse4, stringify as stringify3 } from "yaml";

// runtime/vnext/src/task-projection.ts
import * as crypto from "node:crypto";
import * as fs from "node:fs";
import * as path from "node:path";
import { parse, stringify } from "yaml";
import { isDeepStrictEqual } from "node:util";

// runtime/vnext/src/task-projection-v3.ts
var V3_HOT_FIELDS = ["schema_version", "task_id", "task_slug", "workflow_status", "lifecycle_state", "active_step_id", "active_step_status", "finding_queue_revision", "resume_requires_review", "resume_review_reasons"];
function bodyParts(body) {
  const headings = [...body.matchAll(/^##[ \t]+(.+?)[ \t]*\r?$/gmu)];
  const parts = [{ title: "", text: body.slice(0, headings[0]?.index ?? body.length) }];
  headings.forEach((h, i) => parts.push({ title: h[1].trim(), text: body.slice(h.index, headings[i + 1]?.index ?? body.length) }));
  return parts;
}
function excerpt(value, max = 180) {
  return typeof value === "string" ? value.replace(/[\r\n|`]/gu, " ").slice(0, max) : "";
}
function projectionV3Body(body, runtime) {
  const active = String(runtime.active_step_id ?? "");
  const parts = bodyParts(body);
  const steps = parts.find((p) => ["实施步骤", "Implementation Steps"].includes(p.title))?.text ?? "";
  const step = steps.split(/(?=^###\s)/mu).find((s) => new RegExp(`^###\\s+${active.replace(/[.*+?^${}()|[\]\\]/gu, "\\$&")}(?:[\\s:：.、—-]|$)`, "mu").test(s));
  const basis = parts.find((p) => ["任务输入依据", "Task Basis"].includes(p.title));
  const due = (Array.isArray(runtime.claim_evidence) ? runtime.claim_evidence : []).flatMap((claim) => (Array.isArray(claim.slots) ? claim.slots : []).filter((slot) => slot.due_step_id === active).map((slot) => `- ${excerpt(claim.claim_id, 64)} / ${excerpt(slot.slot_id, 64)} / ${excerpt(slot.check?.check_id, 64)}: ${excerpt(slot.report?.status ?? slot.disposition ?? "missing", 64)}${slot.user_decision ? `; decision=${excerpt(slot.user_decision.kind ?? slot.user_decision.decision_id, 64)}` : ""}`));
  return [
    "# CURRENT_TASK",
    "",
    "> Runtime-generated active projection. References select the complete immutable definition and state.",
    "> This summary is not an execution grant. Use task-context/task-read for exact obligations, authority and evidence.",
    "",
    "## 当前步骤",
    `- ${excerpt(active)}${step ? ` — ${excerpt(step.split(/\r?\n/u)[0].replace(/^###\s*/u, ""))}` : ""}`,
    `- workflow: ${excerpt(runtime.workflow_status)}; lifecycle: ${excerpt(runtime.lifecycle_state)}; step: ${excerpt(runtime.active_step_status)}`,
    "",
    "## 当前到期证据",
    ...due.length ? due : ["- none; inspect task-context for prerequisites and remaining obligations"],
    "",
    ...basis ? [basis.text.trimEnd(), ""] : [],
    "## 精确读取",
    "- `task-context`: current definition, step, gates and dependencies (paged).",
    "- `task-read`: exact definition, state, report, review and historical material.",
    "- `task-export`: complete retained aggregate; no audit history is discarded.",
    ""
  ].join(`
`);
}
function projectionV3Frontmatter(frontmatter, runtime, binding) {
  return {
    schema_version: frontmatter.schema_version,
    kind: frontmatter.kind,
    document_id: frontmatter.document_id,
    task_store: binding,
    runtime_state: Object.fromEntries(V3_HOT_FIELDS.filter((k) => runtime[k] !== undefined).map((k) => [k, runtime[k]]))
  };
}

// runtime/vnext/src/task-projection.ts
var ACTIVE_TASK_FORMAT = "compact-v3";
var DYNAMIC_SECTIONS = new Set(["任务信息", "Task Information", "执行记录", "Execution Log", "审查问题队列", "Review Queue", "传播治理记录", "Propagation Governance"]);
var SLOT_STATE = ["disposition", "evidence_refs", "report", "prerequisite_receipt", "user_decision"];
var HASH = /^[a-f0-9]{64}$/u;
var isRecord = (value) => !!value && typeof value === "object" && !Array.isArray(value);
var without = (value, keys) => Object.fromEntries(Object.entries(value).filter(([key]) => !keys.includes(key)));
function projectionJson(value) {
  if (Array.isArray(value))
    return `[${value.map(projectionJson).join(",")}]`;
  if (isRecord(value))
    return `{${Object.keys(value).sort().filter((k) => value[k] !== undefined).map((k) => `${JSON.stringify(k)}:${projectionJson(value[k])}`).join(",")}}`;
  return JSON.stringify(value);
}
var projectionDigest = (value) => crypto.createHash("sha256").update(projectionJson(value)).digest("hex");
function invalid2(detail) {
  throw new Error(`TASK_PROJECTION_INVALID: ${detail}`);
}
function frontmatterOf(raw) {
  const match = /^---\r?\n([\s\S]*?)\r?\n---(?:\r?\n|$)/u.exec(raw);
  if (!match)
    return invalid2("missing frontmatter");
  const frontmatter = parse(match[1]);
  if (!isRecord(frontmatter))
    return invalid2("invalid frontmatter");
  return { frontmatter, body: raw.slice(match[0].length) };
}
function bodyParts2(body) {
  const headings = [...body.matchAll(/^##[ \t]+(.+?)[ \t]*\r?$/gmu)];
  const parts = [{ title: "", text: body.slice(0, headings[0]?.index ?? body.length) }];
  headings.forEach((h, i) => parts.push({ title: h[1].trim(), text: body.slice(h.index, headings[i + 1]?.index ?? body.length) }));
  return parts;
}
function prepareTaskProjection(expandedContent) {
  const { frontmatter: fm, body } = frontmatterOf(expandedContent);
  if (!isRecord(fm.task_store) || !isRecord(fm.runtime_state) || !/^doc-[a-f0-9]{24}$/u.test(fm.document_id))
    return invalid2("projection requires a store-bound task");
  const runtime = without(fm.runtime_state, ["execution_log", "applied_proposals"]);
  const objects = new Map;
  const add = (object_type, payload) => {
    const object = { schema_version: 1, kind: "vnext-task-object", object_type, document_id: fm.document_id, payload };
    const sha256 = projectionDigest(object);
    objects.set(sha256, object);
    return { object_type, sha256 };
  };
  const storeValue = (value, objectType = "other", force = false) => {
    if (value === undefined)
      return invalid2("undefined stored value");
    if (!force && Buffer.byteLength(projectionJson(value)) < 512)
      return { value };
    return { ref: add(objectType, objectType === "other" ? { kind: "vnext-task-material/v1", value } : value) };
  };
  const dynamicBody = {};
  const sections = bodyParts2(body).map((part, index) => {
    if (DYNAMIC_SECTIONS.has(part.title)) {
      dynamicBody[index] = storeValue(part.text, "other", true);
      return { dynamic: index };
    }
    return { text: storeValue(part.text, "other", true) };
  });
  const claims = runtime.claim_evidence;
  const claimStates = [];
  const claimDefinitions = Array.isArray(claims) ? claims.map((claim) => {
    claimStates.push({ claim_id: claim.claim_id, slots: claim.slots.map((slot) => ({
      slot_id: slot.slot_id,
      fields: Object.fromEntries(SLOT_STATE.filter((k) => slot[k] !== undefined).map((k) => [
        k,
        storeValue(slot[k], k === "report" ? "evidence-report" : k === "prerequisite_receipt" ? "review-receipt" : "other", slot[k] !== null && ["report", "prerequisite_receipt", "user_decision"].includes(k))
      ]))
    })) });
    return storeValue({ ...without(claim, ["slots"]), slots: claim.slots.map((slot) => without(slot, SLOT_STATE)) }, "other", true);
  }) : null;
  const definition = add("definition", {
    kind: "vnext-active-definition/v1",
    frontmatter: without(fm, ["runtime_state", "task_store"]),
    sections,
    claim_definitions: claimDefinitions
  });
  const state = add("state", {
    kind: "vnext-active-state/v1",
    definition,
    runtime: Object.fromEntries(Object.entries(without(runtime, Array.isArray(claims) ? ["claim_evidence"] : [])).map(([k, v]) => [k, storeValue(v)])),
    claim_states: claimStates,
    dynamic_body: dynamicBody
  });
  const binding = { ...without(fm.task_store, ["projection"]), format: ACTIVE_TASK_FORMAT, projection: { definition, state } };
  const content = `---
${stringify(projectionV3Frontmatter(fm, runtime, binding)).trimEnd()}
---
${projectionV3Body(body, runtime)}`;
  const expanded = `---
${stringify({ ...fm, task_store: binding, runtime_state: runtime }).trimEnd()}
---
${body}`;
  return { content, expandedContent: expanded, objects: [...objects.values()] };
}
function location(filePath, relativePath, documentId) {
  if (path.isAbsolute(relativePath) || relativePath.split("/").some((p) => !p || p === "." || p === "..") || !/^doc-[a-f0-9]{24}$/u.test(documentId))
    return invalid2("unsafe task location");
  let root2 = path.resolve(filePath);
  for (const _part of relativePath.split("/"))
    root2 = path.dirname(root2);
  if (path.resolve(root2, relativePath) !== path.resolve(filePath))
    return invalid2("task path mismatch");
  return { root: root2, directory: path.join(root2, path.posix.dirname(relativePath), "task-data", documentId, "objects") };
}
function safePath(root2, target) {
  const relative2 = path.relative(root2, target);
  if (relative2.startsWith("..") || path.isAbsolute(relative2))
    return invalid2("material escapes project");
  let cursor = root2;
  for (const part of relative2.split(path.sep)) {
    cursor = path.join(cursor, part);
    if (fs.lstatSync(cursor, { throwIfNoEntry: false })?.isSymbolicLink())
      return invalid2("material traverses a symbolic link");
  }
}
function persistTaskProjection(prepared, filePath, relativePath) {
  if (!prepared.objects.length)
    return;
  const fm = frontmatterOf(prepared.content).frontmatter;
  const { root: root2, directory } = location(filePath, relativePath, fm.document_id);
  safePath(root2, directory);
  fs.mkdirSync(directory, { recursive: true });
  for (const object of prepared.objects) {
    const file = path.join(directory, `${projectionDigest(object)}.json`);
    safePath(root2, file);
    const bytes = `${projectionJson(object)}
`;
    if (fs.existsSync(file)) {
      if (!fs.lstatSync(file).isFile() || fs.readFileSync(file, "utf8") !== bytes)
        return invalid2("immutable material conflicts");
      continue;
    }
    const temporary = `${file}.${crypto.randomUUID()}.tmp`;
    try {
      const fd = fs.openSync(temporary, "wx");
      try {
        fs.writeFileSync(fd, bytes, "utf8");
        fs.fsyncSync(fd);
      } finally {
        fs.closeSync(fd);
      }
      fs.renameSync(temporary, file);
    } finally {
      if (fs.existsSync(temporary))
        fs.rmSync(temporary);
    }
  }
}
function expandTaskProjection(raw, filePath, relativePath, prepared) {
  const { frontmatter: fm, body: navigation } = frontmatterOf(raw);
  if (fm.task_store?.format !== ACTIVE_TASK_FORMAT)
    return { content: raw, expandedContent: raw, objects: [] };
  const { root: root2, directory } = location(filePath, relativePath, fm.document_id);
  if (fm.task_store.manifest_path !== `${path.posix.dirname(relativePath)}/task-data/${fm.document_id}/manifest.json`)
    return invalid2("manifest identity mismatch");
  const supplied = new Map((prepared?.objects ?? []).map((obj) => [projectionDigest(obj), obj]));
  const objects = new Map;
  const get = (ref, type) => {
    if (!isRecord(ref) || Object.keys(ref).sort().join(",") !== "object_type,sha256" || !HASH.test(ref.sha256) || type && ref.object_type !== type)
      return invalid2("invalid object reference");
    let object = objects.get(ref.sha256) ?? supplied.get(ref.sha256);
    if (!object) {
      const file = path.join(directory, `${ref.sha256}.json`);
      safePath(root2, file);
      if (!fs.existsSync(file) || !fs.lstatSync(file).isFile())
        return invalid2(`missing material ${ref.sha256}`);
      object = JSON.parse(fs.readFileSync(file, "utf8"));
    }
    if (!isRecord(object) || object.kind !== "vnext-task-object" || object.schema_version !== 1 || object.document_id !== fm.document_id || object.object_type !== ref.object_type || projectionDigest(object) !== ref.sha256)
      return invalid2("material content/identity mismatch");
    objects.set(ref.sha256, object);
    return object.payload;
  };
  const value = (stored) => {
    if (!isRecord(stored) || Object.keys(stored).length !== 1)
      return invalid2("invalid stored value");
    if ("value" in stored)
      return stored.value;
    if (!("ref" in stored))
      return invalid2("invalid stored reference");
    const payload = get(stored.ref);
    if (stored.ref.object_type !== "other")
      return payload;
    if (!isRecord(payload) || payload.kind !== "vnext-task-material/v1" || !("value" in payload))
      return invalid2("invalid material payload");
    return payload.value;
  };
  const projection = fm.task_store.projection;
  if (!isRecord(projection) || Object.keys(projection).sort().join(",") !== "definition,state")
    return invalid2("projection roots missing");
  const definition = get(projection.definition, "definition");
  const state = get(projection.state, "state");
  if (definition?.kind !== "vnext-active-definition/v1" || state?.kind !== "vnext-active-state/v1" || projectionJson(state.definition) !== projectionJson(projection.definition))
    return invalid2("definition/state roots disagree");
  if (!Array.isArray(definition.sections) || !Array.isArray(state.claim_states) || !isRecord(state.runtime) || !isRecord(state.dynamic_body) || !isRecord(definition.frontmatter))
    return invalid2("invalid material structure");
  const runtime = Object.fromEntries(Object.entries(state.runtime).map(([k, v]) => [k, value(v)]));
  if (Array.isArray(definition.claim_definitions)) {
    if (state.claim_states.length !== definition.claim_definitions.length)
      return invalid2("claim state count mismatch");
    runtime.claim_evidence = definition.claim_definitions.map((stored, index) => {
      const claim = value(stored);
      const progress = state.claim_states[index];
      if (claim.claim_id !== progress?.claim_id || !Array.isArray(claim.slots) || !Array.isArray(progress.slots) || claim.slots.length !== progress.slots.length)
        return invalid2("claim identity mismatch");
      return { ...claim, slots: claim.slots.map((slot, i) => {
        const next = progress.slots[i];
        if (slot.slot_id !== next?.slot_id || !isRecord(next.fields) || Object.keys(next.fields).some((k) => !SLOT_STATE.includes(k)))
          return invalid2("slot identity mismatch");
        return { ...slot, ...Object.fromEntries(Object.entries(next.fields).map(([k, v]) => [k, value(v)])) };
      }) };
    });
  } else if (definition.claim_definitions !== null)
    return invalid2("invalid claim definitions");
  const body = definition.sections.map((section) => {
    const text = "dynamic" in section ? value(state.dynamic_body[section.dynamic]) : value(section.text);
    if (typeof text !== "string")
      return invalid2("invalid section text");
    return text;
  }).join("");
  const expandedContent = `---
${stringify({ ...definition.frontmatter, task_store: fm.task_store, runtime_state: runtime }).trimEnd()}
---
${body}`;
  const binding = {
    schema_version: 1,
    kind: "vnext-current-task-store-binding",
    format: "compact-v3",
    manifest_path: fm.task_store.manifest_path,
    history: { execution_log: "task-store", applied_proposals: "task-store" },
    projection
  };
  if (!isDeepStrictEqual(fm, projectionV3Frontmatter(definition.frontmatter, runtime, binding)) || navigation.replace(/\r\n/gu, `
`) !== projectionV3Body(body, runtime).replace(/\r\n/gu, `
`)) {
    return invalid2("projection differs from its selected material");
  }
  return { content: raw, expandedContent, objects: [...objects.values()] };
}

// runtime/vnext/src/task-store.ts
import * as crypto3 from "node:crypto";
import * as fs4 from "node:fs";
import * as path4 from "node:path";
import { parse as parse3, stringify as stringify2 } from "yaml";

// runtime/vnext/src/review-preimage-store.ts
import * as crypto2 from "node:crypto";
import * as fs3 from "node:fs";
import * as path3 from "node:path";

// runtime/vnext/src/runtime-io.ts
import * as fs2 from "fs";
import * as path2 from "path";
import { parse as parse2 } from "yaml";
var WORKFLOW_PROFILE_RELATIVE_PATH = ".workflow-system/PROJECT_PROFILE.yaml";
var heldGovernanceLocks = new Set;
function assertGovernanceReadable(currentPath) {
  const lock = path2.join(path2.dirname(currentPath), ".vnext-governance-write.lock");
  if (fs2.existsSync(lock) && !heldGovernanceLocks.has(lock))
    throw new Error("GOVERNANCE_WRITE_LOCKED: publication is running or was interrupted; fail closed.");
}
function governanceWriteLockIsHeld(root2) {
  const profile = loadProfile(getWorkflowProfilePath(root2));
  const lock = getWorkflowDocPath(root2, profile, ".vnext-governance-write.lock");
  return heldGovernanceLocks.has(lock);
}
function withGovernanceWriteLock(root2, operation) {
  const profile = loadProfile(getWorkflowProfilePath(root2));
  const lock = getWorkflowDocPath(root2, profile, ".vnext-governance-write.lock");
  fs2.mkdirSync(path2.dirname(lock), { recursive: true });
  let fd;
  try {
    fd = fs2.openSync(lock, "wx");
  } catch {
    throw new Error("GOVERNANCE_WRITE_LOCKED: concurrent or interrupted governance publication requires recovery; no writes admitted.");
  }
  let release = true;
  try {
    fs2.writeFileSync(fd, JSON.stringify({ kind: "governance-write-lock/v1", pid: process.pid }) + `
`);
    fs2.fsyncSync(fd);
    heldGovernanceLocks.add(lock);
    const result = operation();
    if (result && typeof result === "object" && "code" in result && result.code === "ROLLBACK_FAILED")
      release = false;
    return result;
  } finally {
    heldGovernanceLocks.delete(lock);
    fs2.closeSync(fd);
    if (release)
      fs2.rmSync(lock);
  }
}
function readText(filePath) {
  if (!fs2.existsSync(filePath))
    throw new Error("Required file not found: " + filePath);
  return fs2.readFileSync(filePath, "utf8");
}
function getWorkflowProfilePath(root2) {
  return path2.join(path2.resolve(root2), ...WORKFLOW_PROFILE_RELATIVE_PATH.split("/"));
}
function loadProfile(profilePath) {
  const profile = parse2(readText(profilePath));
  if (!profile || typeof profile !== "object" || Array.isArray(profile)) {
    throw new Error(WORKFLOW_PROFILE_RELATIVE_PATH + " must parse to a mapping");
  }
  return profile;
}
function normalizeWorkflowHome(value) {
  if (value == null)
    return "";
  if (typeof value !== "string")
    throw new Error("PROJECT_PROFILE.yaml.paths.workflow_home must be a string");
  const normalized = value.trim().replace(/\\/g, "/").replace(/\/+/g, "/").replace(/^\.\//, "").replace(/\/$/, "");
  if (normalized === ".")
    return "";
  if (!normalized || normalized.startsWith("/") || /^[A-Za-z]:\//.test(normalized) || normalized.includes("..") || normalized.includes("*") || /[\0-\x1F\x7F]/.test(normalized)) {
    throw new Error("PROJECT_PROFILE.yaml.paths.workflow_home is not a safe repository-relative path");
  }
  return normalized;
}
function getWorkflowDocPath(root2, profile, file) {
  const paths = profile.paths;
  const workflowHome = paths && typeof paths === "object" && !Array.isArray(paths) ? normalizeWorkflowHome(paths.workflow_home) : "";
  return path2.join(path2.resolve(root2), ...[workflowHome, file].filter(Boolean).join("/").split("/"));
}
function executeWrites(operations, dryRun, _summary, _prepare, fileSystem = fs2) {
  if (dryRun || operations.length === 0)
    return;
  const stagingRoot = fs2.mkdtempSync(path2.join(path2.dirname(path2.resolve(operations[0].path)), ".vnext-runtime-"));
  const stagedWrites = [];
  const rollbackEntries = [];
  try {
    for (const [index, operation] of operations.entries()) {
      const tempPath = path2.join(stagingRoot, "staged", index + ".tmp");
      fileSystem.mkdirSync(path2.dirname(tempPath), { recursive: true });
      fileSystem.writeFileSync(tempPath, operation.content, "utf8");
      stagedWrites.push({ tempPath, targetPath: operation.path });
    }
    for (const [index, staged] of stagedWrites.entries()) {
      let backupPath;
      if (fileSystem.existsSync(staged.targetPath)) {
        backupPath = path2.join(stagingRoot, "backup", index + ".bak");
        fileSystem.mkdirSync(path2.dirname(backupPath), { recursive: true });
        fileSystem.renameSync(staged.targetPath, backupPath);
      }
      rollbackEntries.push({ targetPath: staged.targetPath, backupPath });
      fileSystem.mkdirSync(path2.dirname(staged.targetPath), { recursive: true });
      fileSystem.renameSync(staged.tempPath, staged.targetPath);
    }
    fileSystem.rmSync(stagingRoot, { recursive: true, force: true });
  } catch (error) {
    for (const staged of stagedWrites) {
      if (fileSystem.existsSync(staged.tempPath))
        fileSystem.rmSync(staged.tempPath, { force: true });
    }
    for (const entry of rollbackEntries.reverse()) {
      if (fileSystem.existsSync(entry.targetPath))
        fileSystem.rmSync(entry.targetPath, { force: true });
      if (entry.backupPath && fileSystem.existsSync(entry.backupPath)) {
        fileSystem.mkdirSync(path2.dirname(entry.targetPath), { recursive: true });
        fileSystem.renameSync(entry.backupPath, entry.targetPath);
      }
    }
    fileSystem.rmSync(stagingRoot, { recursive: true, force: true });
    throw error;
  }
}

// runtime/vnext/src/review-preimage-store.ts
var SHA256 = /^[a-f0-9]{64}$/u;
var REVIEW_PREIMAGE_DIRECTORY = "review-preimages";

class ReviewPreimageStoreError extends Error {
  code;
  constructor(code, message) {
    super(`${code}: ${message}`);
    this.name = "ReviewPreimageStoreError";
    this.code = code;
  }
}
function assertDigest(value) {
  if (!SHA256.test(value))
    throw new ReviewPreimageStoreError("REVIEW_BASELINE_PATH_INVALID", "review preimage identity must be a lowercase SHA-256 digest.");
  return value;
}
function relativePath(root2, target) {
  const resolvedRoot = path3.resolve(root2);
  const resolvedTarget = path3.resolve(target);
  const relative = path3.relative(resolvedRoot, resolvedTarget).replace(/\\/gu, "/");
  if (!relative || relative === ".." || relative.startsWith("../") || path3.isAbsolute(relative)) {
    throw new ReviewPreimageStoreError("REVIEW_BASELINE_PATH_INVALID", "review preimage storage path escapes the project root.");
  }
  return relative;
}
function assertNoSymlink(root2, target) {
  const relative = relativePath(root2, target);
  let cursor = path3.resolve(root2);
  for (const component of relative.split("/")) {
    cursor = path3.join(cursor, component);
    try {
      if (fs3.lstatSync(cursor).isSymbolicLink()) {
        throw new ReviewPreimageStoreError("REVIEW_BASELINE_PATH_INVALID", `review preimage storage traverses a symbolic link: ${relative}`);
      }
    } catch (error) {
      if (error.code === "ENOENT")
        break;
      throw error;
    }
  }
}
function reviewPreimageDirectory(root2) {
  const profile = loadProfile(getWorkflowProfilePath(root2));
  const directory = getWorkflowDocPath(root2, profile, REVIEW_PREIMAGE_DIRECTORY);
  assertNoSymlink(root2, directory);
  return directory;
}
function reviewPreimageBlobPath(root2, sha256) {
  const digest = assertDigest(sha256);
  const directory = reviewPreimageDirectory(root2);
  const file = path3.join(directory, `${digest}.blob`);
  assertNoSymlink(root2, file);
  return file;
}
function verifyContent(digest, content) {
  if (sha256(content) !== digest) {
    throw new ReviewPreimageStoreError("REVIEW_BASELINE_HASH_MISMATCH", `review preimage bytes do not match ${digest}.`);
  }
}
function sha256(content) {
  return crypto2.createHash("sha256").update(content).digest("hex");
}
function verifyExistingBlob(file, digest, expected) {
  let stat;
  try {
    stat = fs3.lstatSync(file);
  } catch (error) {
    if (error.code === "ENOENT")
      throw new ReviewPreimageStoreError("REVIEW_BASELINE_MISSING", `review preimage blob is missing: ${digest}.blob`);
    throw new ReviewPreimageStoreError("REVIEW_BASELINE_CORRUPT", `review preimage blob cannot be inspected: ${digest}.blob`);
  }
  if (stat.isSymbolicLink() || !stat.isFile())
    throw new ReviewPreimageStoreError("REVIEW_BASELINE_CORRUPT", `review preimage blob is not a regular file: ${digest}.blob`);
  let content;
  try {
    content = fs3.readFileSync(file);
  } catch {
    throw new ReviewPreimageStoreError("REVIEW_BASELINE_CORRUPT", `review preimage blob cannot be read: ${digest}.blob`);
  }
  verifyContent(digest, content);
  if (!content.equals(expected))
    throw new ReviewPreimageStoreError("REVIEW_BASELINE_CORRUPT", `immutable review preimage blob conflicts with ${digest}.blob`);
}
function persistReviewPreimage(root2, digest, content) {
  const address = assertDigest(digest);
  verifyContent(address, content);
  const directory = reviewPreimageDirectory(root2);
  fs3.mkdirSync(directory, { recursive: true });
  assertNoSymlink(root2, directory);
  const file = reviewPreimageBlobPath(root2, address);
  if (fs3.existsSync(file)) {
    verifyExistingBlob(file, address, content);
    return;
  }
  const temporary = path3.join(directory, `.${address}.${crypto2.randomUUID()}.tmp`);
  let descriptor;
  try {
    descriptor = fs3.openSync(temporary, "wx");
    fs3.writeFileSync(descriptor, content);
    fs3.fsyncSync(descriptor);
    fs3.closeSync(descriptor);
    descriptor = undefined;
    try {
      fs3.renameSync(temporary, file);
    } catch (error) {
      if (!fs3.existsSync(file))
        throw error;
      verifyExistingBlob(file, address, content);
    }
    if (!fs3.existsSync(file))
      throw new ReviewPreimageStoreError("REVIEW_BASELINE_PERSISTENCE_FAILED", `review preimage blob was not published: ${address}.blob`);
    verifyExistingBlob(file, address, content);
  } catch (error) {
    if (error instanceof ReviewPreimageStoreError)
      throw error;
    throw new ReviewPreimageStoreError("REVIEW_BASELINE_PERSISTENCE_FAILED", `review preimage blob could not be persisted: ${address}.blob`);
  } finally {
    if (descriptor !== undefined) {
      try {
        fs3.closeSync(descriptor);
      } catch {}
    }
    if (fs3.existsSync(temporary)) {
      try {
        fs3.unlinkSync(temporary);
      } catch {}
    }
  }
}
function persistReviewPreimageWrites(root2, writes) {
  const unique = new Map;
  for (const write of writes) {
    const digest = assertDigest(write.sha256);
    const prior = unique.get(digest);
    if (prior && !prior.equals(write.content)) {
      throw new ReviewPreimageStoreError("REVIEW_BASELINE_CORRUPT", `one execution produced conflicting bytes for ${digest}.`);
    }
    unique.set(digest, Buffer.from(write.content));
  }
  for (const [digest, content] of unique)
    persistReviewPreimage(root2, digest, content);
}
function readReviewPreimageBlob(root2, digest) {
  const address = assertDigest(digest);
  const file = reviewPreimageBlobPath(root2, address);
  let stat;
  try {
    stat = fs3.lstatSync(file);
  } catch (error) {
    if (error.code === "ENOENT")
      throw new ReviewPreimageStoreError("REVIEW_BASELINE_MISSING", `review preimage blob is missing: ${address}.blob`);
    throw new ReviewPreimageStoreError("REVIEW_BASELINE_CORRUPT", `review preimage blob cannot be inspected: ${address}.blob`);
  }
  if (stat.isSymbolicLink() || !stat.isFile())
    throw new ReviewPreimageStoreError("REVIEW_BASELINE_CORRUPT", `review preimage blob is not a regular file: ${address}.blob`);
  let content;
  try {
    content = fs3.readFileSync(file);
  } catch {
    throw new ReviewPreimageStoreError("REVIEW_BASELINE_CORRUPT", `review preimage blob cannot be read: ${address}.blob`);
  }
  if (sha256(content) !== address)
    throw new ReviewPreimageStoreError("REVIEW_BASELINE_HASH_MISMATCH", `review preimage blob hash does not match ${address}.blob`);
  return content;
}
function record(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value) ? value : null;
}
function decodeBase64(value, pathLabel) {
  if (typeof value !== "string" || Buffer.from(value, "base64").toString("base64") !== value) {
    throw new ReviewPreimageStoreError("REVIEW_BASELINE_INLINE_INVALID", `legacy inline review preimage is not canonical base64: ${pathLabel}.`);
  }
  return Buffer.from(value, "base64");
}
function decodeLegacyReviewPreimage(value) {
  const item = record(value);
  if (!item || item.state !== "file" && item.state !== "symlink" && item.state !== "absent" || typeof item.path !== "string") {
    throw new ReviewPreimageStoreError("REVIEW_BASELINE_INLINE_INVALID", "legacy inline review preimage has an invalid identity.");
  }
  if (!Object.prototype.hasOwnProperty.call(item, "content_base64"))
    return null;
  const state = item.state;
  if (state === "absent") {
    if (item.content_base64 !== null || item.sha256 !== null)
      throw new ReviewPreimageStoreError("REVIEW_BASELINE_INLINE_INVALID", `absent legacy review preimage is invalid: ${item.path}.`);
    return null;
  }
  if (typeof item.sha256 !== "string" || !SHA256.test(item.sha256))
    throw new ReviewPreimageStoreError("REVIEW_BASELINE_INLINE_INVALID", `legacy review preimage hash is invalid: ${item.path}.`);
  const content = decodeBase64(item.content_base64, item.path);
  verifyContent(item.sha256, content);
  return { sha256: item.sha256, content };
}
function persistLegacyReviewPreimages(root2, preimages) {
  const writes = [];
  for (const preimage of preimages) {
    const write = decodeLegacyReviewPreimage(preimage);
    if (write)
      writes.push(write);
  }
  persistReviewPreimageWrites(root2, writes);
}

// runtime/vnext/src/task-store.ts
var TASK_DEFINITION_REVISION_V1 = "task-definition/v1";
var TASK_DEFINITION_REVISION_V2 = "task-definition/v2";
var TASK_STATE_REVISION_V1 = "task-state/v1";
var TASK_STORE_KIND = "vnext-task-store-manifest";
var TASK_OBJECT_KIND = "vnext-task-object";
var TASK_EVENT_KIND = "vnext-task-event";
var TASK_STORE_MANIFEST_FILE = "manifest.json";
var TASK_STORE_DEFAULT_PAGE_BYTES = 16 * 1024;
var TASK_STORE_MAX_PAGE_BYTES = 64 * 1024;
var DOCUMENT_ID = /^doc-[a-f0-9]{24}$/u;
var SHA2562 = /^[a-f0-9]{64}$/u;
var EVENT_FILE = /^(\d+)-([a-f0-9]{64})\.json$/u;
var TASK_STORE_OBJECT_TYPES = new Set([
  "definition",
  "state",
  "current-snapshot",
  "legacy-current-task",
  "task-basis",
  "proposal",
  "result",
  "semantic-delta",
  "transaction",
  "execution-log-entry",
  "evidence-report",
  "review-receipt",
  "history-material",
  "other"
]);

class TaskStoreError extends Error {
  code;
  constructor(code, message) {
    super(`${code}: ${message}`);
    this.name = "TaskStoreError";
    this.code = code;
  }
}
function record2(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function stableValue(value) {
  if (Array.isArray(value))
    return value.map(stableValue);
  if (record2(value))
    return Object.fromEntries(Object.keys(value).sort().map((key) => [key, stableValue(value[key])]));
  return value;
}
function stableJson(value) {
  return JSON.stringify(stableValue(value));
}
function sha2562(value) {
  return crypto3.createHash("sha256").update(value).digest("hex");
}
function digest(value) {
  return sha2562(stableJson(value));
}
function assertDocumentId(value) {
  if (!DOCUMENT_ID.test(value))
    throw new TaskStoreError("TASK_STORE_PATH_INVALID", `document_id is invalid: ${value}`);
}
function normalizeRelative(value, label) {
  const normalized = value.replace(/\\/gu, "/").replace(/^\.\//u, "");
  if (!normalized || normalized.startsWith("/") || /^[A-Za-z]:/u.test(normalized) || normalized.split("/").includes("..") || /[\0\r\n]/u.test(normalized)) {
    throw new TaskStoreError("TASK_STORE_PATH_INVALID", `${label} must be a safe repository-relative path.`);
  }
  return normalized;
}
function workflowHomeForRoot(root2) {
  const profilePath = path4.join(root2, ".workflow-system", "PROJECT_PROFILE.yaml");
  if (!fs4.existsSync(profilePath))
    return "docs/workflow";
  try {
    const parsed = parse3(fs4.readFileSync(profilePath, "utf8"));
    const paths = record2(parsed?.paths) ? parsed.paths : {};
    const value = typeof paths.workflow_home === "string" ? paths.workflow_home.trim() : "docs/workflow";
    return value ? normalizeRelative(value, "workflow_home").replace(/\/$/u, "") : "";
  } catch (error) {
    throw new TaskStoreError("TASK_STORE_PATH_INVALID", `PROJECT_PROFILE.yaml cannot be read: ${error instanceof Error ? error.message : String(error)}`);
  }
}
function taskStorePaths(rootInput, documentId) {
  const root2 = path4.resolve(rootInput);
  assertDocumentId(documentId);
  const workflowHome = workflowHomeForRoot(root2);
  const relativeRoot = normalizeRelative(path4.posix.join(workflowHome, "task-data", documentId), "task store root");
  assertNoSymlink2(root2, relativeRoot);
  const directory = path4.join(root2, ...relativeRoot.split("/"));
  const objects = path4.join(directory, "objects");
  const events = path4.join(directory, "events");
  const indexes = path4.join(directory, "indexes");
  return {
    root: root2,
    workflowHome,
    documentId,
    relativeRoot,
    directory,
    objects,
    events,
    indexes,
    manifest: path4.join(directory, TASK_STORE_MANIFEST_FILE),
    pending: path4.join(indexes, "pending.json"),
    idempotencyIndex: path4.join(indexes, "idempotency.jsonl"),
    legacyIdempotencyIndex: path4.join(indexes, "idempotency.json"),
    eventIndex: path4.join(indexes, "events.jsonl"),
    legacyEventIndex: path4.join(indexes, "events.json")
  };
}
function relativePath2(root2, value) {
  const relative = path4.relative(root2, value).replace(/\\/gu, "/");
  if (!relative || relative.startsWith("../") || path4.isAbsolute(relative)) {
    throw new TaskStoreError("TASK_STORE_PATH_INVALID", `path escapes project root: ${value}`);
  }
  return relative;
}
function assertNoSymlink2(root2, relative) {
  let cursor = path4.resolve(root2);
  for (const part of relative.split("/").filter(Boolean)) {
    cursor = path4.join(cursor, part);
    try {
      if (fs4.lstatSync(cursor).isSymbolicLink())
        throw new TaskStoreError("TASK_STORE_PATH_INVALID", `task store path traverses a symbolic link: ${relative}`);
    } catch (error) {
      if (error.code !== "ENOENT")
        throw error;
    }
  }
}
function atomicWrite(filePath, content, sync = true) {
  fs4.mkdirSync(path4.dirname(filePath), { recursive: true });
  const temporary = path4.join(path4.dirname(filePath), `.${path4.basename(filePath)}.${crypto3.randomUUID()}.tmp`);
  let fd;
  try {
    fd = fs4.openSync(temporary, "wx");
    fs4.writeFileSync(fd, content, "utf8");
    if (sync)
      fs4.fsyncSync(fd);
    fs4.closeSync(fd);
    fd = undefined;
    fs4.renameSync(temporary, filePath);
  } finally {
    if (fd !== undefined)
      fs4.closeSync(fd);
    if (fs4.existsSync(temporary))
      fs4.rmSync(temporary, { force: true });
  }
}
function writeJson(filePath, value, sync = true) {
  atomicWrite(filePath, `${stableJson(value)}
`, sync);
}
function writePrecommitFile(filePath, content, committed, conflictCode, label) {
  fs4.mkdirSync(path4.dirname(filePath), { recursive: true });
  if (fs4.existsSync(filePath)) {
    const existing = fs4.readFileSync(filePath, "utf8");
    if (existing === content)
      return;
    if (committed())
      throw new TaskStoreError(conflictCode, `${label} has different bytes after it was committed.`);
    fs4.rmSync(filePath, { force: true });
  }
  fs4.writeFileSync(filePath, content, "utf8");
}
function readJson(filePath) {
  try {
    return JSON.parse(fs4.readFileSync(filePath, "utf8"));
  } catch (error) {
    throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${filePath} is not readable JSON: ${error instanceof Error ? error.message : String(error)}`);
  }
}
function copyWithout(value, keys) {
  return Object.fromEntries(Object.entries(value).filter(([key]) => !keys.includes(key)));
}
function withoutInlineReviewPreimageContent(value) {
  const normalized = structuredClone(value);
  const coverage = record2(normalized.review_coverage) ? normalized.review_coverage : null;
  if (coverage && Array.isArray(coverage.preimages)) {
    coverage.preimages = coverage.preimages.map((item) => record2(item) ? copyWithout(item, ["content_base64"]) : item);
  }
  return normalized;
}
function markdownSections(body) {
  const normalized = body.replace(/\r\n?/gu, `
`);
  const headings = [...normalized.matchAll(/^##\s+(.+?)\s*$/gmu)];
  return headings.map((match, index) => {
    const start = (match.index ?? 0) + match[0].length;
    const end = index + 1 < headings.length ? headings[index + 1].index ?? normalized.length : normalized.length;
    return { title: match[1].trim(), text: normalized.slice(start, end).replace(/^\n/u, "").trimEnd() };
  });
}
function slotDefinitionV1(slot) {
  return copyWithout(slot, ["report", "prerequisite_receipt"]);
}
function slotDefinitionV2(slot) {
  return copyWithout(slot, ["disposition", "evidence_refs", "report", "prerequisite_receipt", "user_decision"]);
}
function definitionSections(current, includeDynamicSections) {
  const dynamicTitles = new Set([
    "任务信息",
    "Task Information",
    "执行记录",
    "Execution Log",
    "审查问题队列",
    "Review Queue",
    "传播治理记录",
    "Propagation Governance"
  ]);
  return markdownSections(current.body).filter((section) => includeDynamicSections || !dynamicTitles.has(section.title)).map((section) => ({ title: section.title, text: section.text }));
}
function claimDefinitionPayload(current, version) {
  const state = current.runtimeState;
  return Array.isArray(state.claim_evidence) ? state.claim_evidence.map((item) => {
    if (!record2(item))
      return item;
    return {
      ...copyWithout(item, ["slots"]),
      slots: Array.isArray(item.slots) ? item.slots.filter(record2).map((slot) => version === 1 ? slotDefinitionV1(slot) : slotDefinitionV2(slot)) : []
    };
  }) : [];
}
function definitionPayloadV1(current) {
  const sections = markdownSections(current.body).filter((section) => !["任务信息", "Task Information", "执行记录", "Execution Log"].includes(section.title)).map((section) => ({ title: section.title, text: section.text }));
  const claims = claimDefinitionPayload(current, 1);
  return {
    schema_version: 1,
    kind: TASK_DEFINITION_REVISION_V1,
    document_id: current.sourceTuple.document_id,
    task_id: current.sourceTuple.task_id,
    source_path: current.relativePath,
    sections,
    claim_evidence_plan: claims,
    evidence_plan_revision: current.runtimeState.evidence_plan_revision ?? null
  };
}
function definitionPayloadV2(current) {
  const frontmatter = current.frontmatter ?? {};
  return {
    schema_version: 2,
    kind: TASK_DEFINITION_REVISION_V2,
    revision_algorithm: TASK_DEFINITION_REVISION_V2,
    document_id: current.sourceTuple.document_id,
    task_id: current.sourceTuple.task_id,
    source_path: current.relativePath,
    ...frontmatter.mutation_authority_version === 2 && frontmatter.mutation_authority !== undefined ? {
      mutation_authority_version: 2,
      mutation_authority: frontmatter.mutation_authority
    } : {},
    sections: definitionSections(current, false),
    claim_evidence_plan: claimDefinitionPayload(current, 2),
    evidence_plan_revision: current.runtimeState.evidence_plan_revision ?? null
  };
}
function definitionPayload(current, algorithm = TASK_DEFINITION_REVISION_V2) {
  return algorithm === TASK_DEFINITION_REVISION_V1 ? definitionPayloadV1(current) : definitionPayloadV2(current);
}
function compactCurrent(current) {
  const binding = record2(current.frontmatter?.task_store) ? current.frontmatter?.task_store : null;
  return binding !== null && ["compact-v2", ACTIVE_TASK_FORMAT].includes(binding.format);
}
function currentRepresentation(current) {
  return current.frontmatter?.task_store && record2(current.frontmatter.task_store) && current.frontmatter.task_store.format === ACTIVE_TASK_FORMAT ? ACTIVE_TASK_FORMAT : compactCurrent(current) ? "compact-v2" : "legacy-inline";
}
function projectionMaterial(current) {
  return currentRepresentation(current) === ACTIVE_TASK_FORMAT ? expandTaskProjection(current.raw, current.filePath, current.relativePath) : null;
}
function storedCurrentPayload(current, type, fallback) {
  const material = projectionMaterial(current);
  if (!material)
    return fallback;
  const binding = current.frontmatter.task_store;
  const sha = binding.projection[type].sha256;
  const object = material.objects.find((object) => projectionDigest(object) === sha && object.object_type === type);
  if (!object)
    throw new TaskStoreError("TASK_STORE_OBJECT_MISSING", "Projection root is missing.");
  return object.payload;
}
function stateSnapshotPayload(current) {
  const state = withoutInlineReviewPreimageContent(current.runtimeState);
  const snapshot = copyWithout(state, ["execution_log", "applied_proposals", "claim_evidence"]);
  snapshot.claim_evidence = Array.isArray(state.claim_evidence) ? state.claim_evidence.map((item) => {
    if (!record2(item))
      return item;
    const slots = Array.isArray(item.slots) ? item.slots.filter(record2).map((slot) => ({
      ...copyWithout(slot, ["report", "prerequisite_receipt"]),
      report_digest: slot.report === undefined ? null : digest(slot.report),
      prerequisite_receipt_digest: slot.prerequisite_receipt === undefined ? null : digest(slot.prerequisite_receipt)
    })) : [];
    return { ...copyWithout(item, ["slots"]), slots };
  }) : [];
  return {
    schema_version: 1,
    kind: "task-state-snapshot/v1",
    document_id: current.sourceTuple.document_id,
    source_revision: current.sourceTuple.revision,
    runtime_state: snapshot
  };
}
function objectRef(objectType, sha) {
  return { sha256: sha, object_type: objectType };
}
function refSha(value) {
  if (typeof value === "string")
    return SHA2562.test(value) ? value : null;
  return value && SHA2562.test(value.sha256) ? value.sha256 : null;
}
function isObjectReference(value) {
  return record2(value) && typeof value.sha256 === "string" && SHA2562.test(value.sha256) && typeof value.object_type === "string" && TASK_STORE_OBJECT_TYPES.has(value.object_type);
}
function storedProposalPayload(proposal, claimEvidenceReference, executionResultReference) {
  if (!record2(proposal) || !record2(proposal.semantic_delta) || claimEvidenceReference === undefined && executionResultReference === undefined) {
    return proposal;
  }
  return {
    schema_version: 2,
    kind: "vnext-proposal/v2",
    proposal: copyWithout(proposal, ["semantic_delta"]),
    semantic_delta: copyWithout(proposal.semantic_delta, ["claim_evidence", "execution_result"]),
    ...claimEvidenceReference === undefined ? {} : { claim_evidence_ref: claimEvidenceReference },
    ...executionResultReference === undefined ? {} : { execution_result_ref: executionResultReference }
  };
}
var committedObjectCaches = new Map;
var committedEventCaches = new Map;
var executionHistoryCaches = new Map;
var appliedProposalCaches = new Map;
var indexFileCaches = new Map;
var idempotencyEventCaches = new Map;
function committedObjectCacheKey(paths) {
  return path4.resolve(paths.directory);
}
function addObjectReferences(hashes, references) {
  for (const reference of references) {
    const hash = refSha(reference);
    if (hash)
      hashes.add(hash);
  }
}
function loadCommittedObjectCache(paths, manifest) {
  const key = committedObjectCacheKey(paths);
  const cached = committedObjectCaches.get(key);
  if (cached && cached.eventSequence === manifest.head.event_sequence && cached.eventHash === manifest.head.event_hash)
    return cached;
  const hashes = new Set;
  addObjectReferences(hashes, [
    manifest.object_refs.definition,
    manifest.object_refs.state,
    manifest.object_refs.current_snapshot,
    manifest.object_refs.legacy_source
  ]);
  for (const event of committedEventFiles(paths, manifest, true)) {
    addObjectReferences(hashes, Object.values(event.object_refs));
  }
  const next = { eventSequence: manifest.head.event_sequence, eventHash: manifest.head.event_hash, hashes };
  committedObjectCaches.set(key, next);
  return next;
}
function rememberCommittedObjectReferences(paths, manifest, references) {
  const key = committedObjectCacheKey(paths);
  const cached = committedObjectCaches.get(key);
  const next = cached && cached.eventSequence <= manifest.head.event_sequence ? { eventSequence: manifest.head.event_sequence, eventHash: manifest.head.event_hash, hashes: new Set(cached.hashes) } : loadCommittedObjectCache(paths, manifest);
  addObjectReferences(next.hashes, [
    manifest.object_refs.definition,
    manifest.object_refs.state,
    manifest.object_refs.current_snapshot,
    manifest.object_refs.legacy_source
  ]);
  addObjectReferences(next.hashes, references);
  committedObjectCaches.set(key, next);
}
function idempotencyEventCacheKey(paths) {
  return path4.resolve(paths.directory);
}
function loadIdempotencyEventCache(paths, manifest) {
  const key = idempotencyEventCacheKey(paths);
  const cached = idempotencyEventCaches.get(key);
  if (cached && cached.eventSequence === manifest.head.event_sequence && cached.eventHash === manifest.head.event_hash)
    return cached;
  const byKey = new Map;
  for (const event of committedEventFiles(paths, manifest, true)) {
    if (!event.idempotency_key || !event.proposal_digest)
      continue;
    const relative = path4.posix.join(paths.relativeRoot, "events", `${String(event.sequence).padStart(12, "0")}-${event.event_hash}.json`);
    const indexEntry = {
      idempotency_key: event.idempotency_key,
      operation_kind: event.operation_kind,
      proposal_digest: event.proposal_digest,
      source_revision: event.source_revision,
      event_path: relative,
      event_id: event.event_id,
      sequence: event.sequence,
      legacy: event.event_type === "legacy-import"
    };
    const values = byKey.get(indexEntry.idempotency_key) ?? [];
    values.push({ ...indexEntry, event });
    byKey.set(indexEntry.idempotency_key, values);
  }
  const next = { eventSequence: manifest.head.event_sequence, eventHash: manifest.head.event_hash, byKey };
  idempotencyEventCaches.set(key, next);
  return next;
}
function rememberIdempotencyMatches(paths, manifest, matches) {
  const key = idempotencyEventCacheKey(paths);
  const cached = idempotencyEventCaches.get(key);
  const next = cached && cached.eventSequence <= manifest.head.event_sequence ? { eventSequence: manifest.head.event_sequence, eventHash: manifest.head.event_hash, byKey: new Map([...cached.byKey].map(([itemKey, values]) => [itemKey, [...values]])) } : loadIdempotencyEventCache(paths, manifest);
  for (const match of matches) {
    const values = next.byKey.get(match.idempotency_key) ?? [];
    if (!values.some((value) => value.event.event_id === match.event.event_id))
      values.push(match);
    next.byKey.set(match.idempotency_key, values);
  }
  idempotencyEventCaches.set(key, next);
}
function objectFile(paths, sha) {
  if (!SHA2562.test(sha))
    throw new TaskStoreError("TASK_STORE_PATH_INVALID", "object reference is not a SHA-256 digest.");
  return path4.join(paths.objects, `${sha}.json`);
}
function eventFile(paths, sequence, eventHash) {
  if (!Number.isSafeInteger(sequence) || sequence < 1 || !SHA2562.test(eventHash)) {
    throw new TaskStoreError("TASK_STORE_PATH_INVALID", "event reference is invalid.");
  }
  return path4.join(paths.events, `${String(sequence).padStart(12, "0")}-${eventHash}.json`);
}
function assertObjectReference(value, location) {
  if (!record2(value) || typeof value.sha256 !== "string" || !SHA2562.test(value.sha256) || typeof value.object_type !== "string" || !TASK_STORE_OBJECT_TYPES.has(value.object_type)) {
    throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${location} is not an exact object reference.`);
  }
  return { sha256: value.sha256, object_type: value.object_type };
}
function validateManifest(value, paths) {
  if (!record2(value) || value.schema_version !== 1 || value.kind !== TASK_STORE_KIND) {
    throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${paths.manifest} has an unsupported manifest header.`);
  }
  if (value.document_id !== paths.documentId || typeof value.task_id !== "string" || typeof value.task_slug !== "string") {
    throw new TaskStoreError("TASK_STORE_IDENTITY_CONFLICT", `${paths.manifest} identity does not match the task-data path.`);
  }
  const head = record2(value.head) ? value.head : null;
  if (!head || typeof head.source_revision !== "string" || !SHA2562.test(head.source_revision) || typeof head.definition_revision !== "string" || !SHA2562.test(head.definition_revision) || typeof head.state_revision !== "string" || !SHA2562.test(head.state_revision) || !Number.isSafeInteger(head.event_sequence) || head.event_sequence < 0) {
    throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${paths.manifest} has an invalid aggregate head.`);
  }
  const refs = record2(value.object_refs) ? value.object_refs : null;
  if (!refs)
    throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${paths.manifest} has no object references.`);
  const objectRefs = {
    definition: assertObjectReference(refs.definition, "manifest.object_refs.definition"),
    state: assertObjectReference(refs.state, "manifest.object_refs.state"),
    current_snapshot: assertObjectReference(refs.current_snapshot, "manifest.object_refs.current_snapshot"),
    ...refs.legacy_source === undefined ? {} : { legacy_source: assertObjectReference(refs.legacy_source, "manifest.object_refs.legacy_source") }
  };
  const counts = record2(value.counts) ? value.counts : null;
  if (!counts || !Number.isSafeInteger(counts.events) || counts.events < 0 || !Number.isSafeInteger(counts.objects) || counts.objects < 0 || !Number.isSafeInteger(counts.idempotency_entries) || counts.idempotency_entries < 0) {
    throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${paths.manifest} has invalid counts.`);
  }
  return {
    schema_version: 1,
    kind: TASK_STORE_KIND,
    document_id: paths.documentId,
    task_id: value.task_id,
    task_slug: value.task_slug,
    current_task_path: typeof value.current_task_path === "string" ? normalizeRelative(value.current_task_path, "manifest.current_task_path") : "",
    storage_root: paths.relativeRoot,
    created_at: typeof value.created_at === "string" ? value.created_at : "",
    updated_at: typeof value.updated_at === "string" ? value.updated_at : "",
    head: {
      source_revision: head.source_revision,
      definition_revision: head.definition_revision,
      state_revision: head.state_revision,
      event_sequence: head.event_sequence,
      event_id: typeof head.event_id === "string" ? head.event_id : null,
      event_hash: typeof head.event_hash === "string" ? head.event_hash : null,
      event_range: record2(head.event_range) && (head.event_range.first === null || Number.isSafeInteger(head.event_range.first)) && (head.event_range.last === null || Number.isSafeInteger(head.event_range.last)) ? { first: head.event_range.first, last: head.event_range.last } : { first: null, last: null }
    },
    object_refs: objectRefs,
    counts: { events: counts.events, objects: counts.objects, idempotency_entries: counts.idempotency_entries },
    compatibility: { legacy_current_task: true, hot_window_is_cache: true, full_history_persistent: true },
    storage_format: value.storage_format === "vnext-task-store/v2" ? "vnext-task-store/v2" : "vnext-task-store/v1",
    definition_revision_algorithm: value.definition_revision_algorithm === TASK_DEFINITION_REVISION_V2 ? TASK_DEFINITION_REVISION_V2 : TASK_DEFINITION_REVISION_V1,
    state_revision_algorithm: TASK_STATE_REVISION_V1,
    current_representation: value.current_representation === ACTIVE_TASK_FORMAT ? ACTIVE_TASK_FORMAT : value.current_representation === "compact-v2" ? "compact-v2" : "legacy-inline"
  };
}
function validateEvent(value, paths, expectedPath) {
  if (!record2(value) || value.schema_version !== 1 || value.kind !== TASK_EVENT_KIND) {
    throw new TaskStoreError("TASK_STORE_EVENT_INVALID", `${expectedPath ?? "event"} has an unsupported event header.`);
  }
  if (value.document_id !== paths.documentId || !Number.isSafeInteger(value.sequence) || value.sequence < 1 || typeof value.event_id !== "string") {
    throw new TaskStoreError("TASK_STORE_IDENTITY_CONFLICT", `${expectedPath ?? "event"} identity or sequence is invalid.`);
  }
  for (const key of ["source_revision", "resulting_source_revision", "definition_revision", "state_revision"]) {
    if (typeof value[key] !== "string" || !SHA2562.test(value[key]))
      throw new TaskStoreError("TASK_STORE_EVENT_INVALID", `${expectedPath ?? "event"}.${key} is not a SHA-256 digest.`);
  }
  if (typeof value.event_hash !== "string" || !SHA2562.test(value.event_hash))
    throw new TaskStoreError("TASK_STORE_EVENT_INVALID", `${expectedPath ?? "event"}.event_hash is invalid.`);
  const unsigned = { ...value };
  delete unsigned.event_hash;
  if (sha2562(stableJson(unsigned)) !== value.event_hash)
    throw new TaskStoreError("TASK_STORE_EVENT_INVALID", `${expectedPath ?? "event"} event_hash does not match its content.`);
  const eventType = value.event_type;
  if (eventType !== "legacy-import" && eventType !== "transaction" && eventType !== "external-current-sync" && eventType !== "storage-migration")
    throw new TaskStoreEventError("TASK_STORE_EVENT_INVALID", `${expectedPath ?? "event"} event_type is invalid.`);
  if (!record2(value.object_refs))
    throw new TaskStoreError("TASK_STORE_EVENT_INVALID", `${expectedPath ?? "event"}.object_refs is invalid.`);
  for (const [key, reference] of Object.entries(value.object_refs)) {
    if (reference !== null)
      assertObjectReference(reference, `${expectedPath ?? "event"}.object_refs.${key}`);
  }
  const metadata = record2(value.metadata) ? value.metadata : null;
  if (!metadata || typeof metadata.status !== "string" || typeof metadata.recorded_at !== "string" || typeof metadata.committed !== "boolean")
    throw new TaskStoreError("TASK_STORE_EVENT_INVALID", `${expectedPath ?? "event"} metadata is invalid.`);
  return value;
}
function readEventFile(paths, name) {
  const match = EVENT_FILE.exec(name);
  if (!match)
    throw new TaskStoreError("TASK_STORE_EVENT_INVALID", `event filename is invalid: ${name}`);
  const sequence = Number(match[1]);
  const eventHash = match[2];
  const relative = path4.posix.join(paths.relativeRoot, "events", name);
  const file = path4.join(paths.events, name);
  assertNoSymlink2(paths.root, relative);
  if (!fs4.existsSync(file))
    throw new TaskStoreError("TASK_STORE_EVENT_MISSING", `event is missing: ${relative}`);
  const event = validateEvent(readJson(file), paths, relative);
  if (event.sequence !== sequence || event.event_hash !== eventHash) {
    throw new TaskStoreError("TASK_STORE_EVENT_INVALID", `${relative} filename does not match the event identity.`);
  }
  return event;
}
function committedEventFiles(paths, manifest, useCache = false) {
  const headSequence = manifest.head.event_sequence;
  if (headSequence === 0)
    return [];
  const cacheKey = committedObjectCacheKey(paths);
  if (!fs4.existsSync(paths.events))
    throw new TaskStoreError("TASK_STORE_EVENT_MISSING", "the committed event directory is missing.");
  assertNoSymlink2(paths.root, relativePath2(paths.root, paths.events));
  const directoryStat = fs4.statSync(paths.events);
  const directorySignature = `${directoryStat.size}:${directoryStat.mtimeMs}:${directoryStat.ctimeMs}`;
  const cached = useCache ? committedEventCaches.get(cacheKey) : undefined;
  if (cached && cached.eventSequence === headSequence && cached.eventHash === manifest.head.event_hash && cached.directorySignature === directorySignature)
    return cached.events;
  const bySequence = new Map;
  for (const entry of fs4.readdirSync(paths.events, { withFileTypes: true })) {
    if (!entry.isFile())
      continue;
    const match = EVENT_FILE.exec(entry.name);
    if (!match)
      continue;
    const sequence = Number(match[1]);
    if (sequence > headSequence)
      continue;
    const file = path4.join(paths.events, entry.name);
    assertNoSymlink2(paths.root, relativePath2(paths.root, file));
    const stat = fs4.statSync(file);
    const names = bySequence.get(sequence) ?? [];
    names.push({ name: entry.name, signature: `${entry.name}:${stat.size}:${stat.mtimeMs}:${stat.ctimeMs}` });
    bySequence.set(sequence, names);
  }
  const signatureFor = (sequence) => (bySequence.get(sequence) ?? []).map((item) => item.signature).sort();
  const cachedPrefixMatches = cached !== undefined && cached.eventSequence <= headSequence && [...cached.fileSignatures.keys()].every((sequence) => {
    if (sequence > headSequence)
      return false;
    return JSON.stringify(cached.fileSignatures.get(sequence)) === JSON.stringify(signatureFor(sequence));
  });
  const events = cachedPrefixMatches ? [...cached.events] : [];
  let previous = cachedPrefixMatches ? cached.eventHash : null;
  const firstSequence = cachedPrefixMatches ? cached.eventSequence + 1 : 1;
  for (let sequence = firstSequence;sequence <= headSequence; sequence += 1) {
    const names = bySequence.get(sequence) ?? [];
    if (names.length === 0)
      throw new TaskStoreError("TASK_STORE_EVENT_MISSING", `committed event sequence ${sequence} is missing.`);
    const candidates = [];
    let firstError;
    for (const item of names) {
      try {
        candidates.push(readEventFile(paths, item.name));
      } catch (error) {
        firstError ??= error;
      }
    }
    const matching = candidates.filter((event) => event.previous_event_hash === previous);
    if (matching.length > 1)
      throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", `committed event sequence ${sequence} has multiple valid successors.`);
    if (matching.length === 0) {
      const detail = firstError instanceof Error ? ` ${firstError.message}` : "";
      throw new TaskStoreError("TASK_STORE_EVENT_INVALID", `no event at sequence ${sequence} extends the committed chain.${detail}`);
    }
    const event = matching[0];
    events.push(event);
    previous = event.event_hash;
  }
  if (previous !== manifest.head.event_hash)
    throw new TaskStoreError("TASK_STORE_EVENT_INVALID", "manifest head does not match the committed event chain.");
  if (useCache) {
    const fileSignatures = new Map;
    for (let sequence = 1;sequence <= headSequence; sequence += 1)
      fileSignatures.set(sequence, signatureFor(sequence));
    committedEventCaches.set(cacheKey, { eventSequence: headSequence, eventHash: manifest.head.event_hash, events, fileSignatures, directorySignature });
  }
  return events;
}

class TaskStoreEventError extends TaskStoreError {
}
function validateObject(value, paths, sha) {
  if (!record2(value) || value.schema_version !== 1 || value.kind !== TASK_OBJECT_KIND || value.document_id !== paths.documentId || typeof value.object_type !== "string" || !TASK_STORE_OBJECT_TYPES.has(value.object_type) || !("payload" in value)) {
    throw new TaskStoreError("TASK_STORE_OBJECT_INVALID", `object ${sha} has an invalid envelope.`);
  }
  if (digest(value) !== sha)
    throw new TaskStoreError("TASK_STORE_OBJECT_INVALID", `object ${sha} content hash does not match its filename.`);
  return value;
}
function historyRawFromObject(object, sourceRevision) {
  if (!["history-material", "legacy-current-task"].includes(object.object_type) || !record2(object.payload))
    return null;
  const payload = object.payload;
  if (payload.source_revision !== sourceRevision)
    return null;
  if (typeof payload.raw_base64 === "string") {
    const rawBytes = Buffer.from(payload.raw_base64, "base64");
    const expectedDigest = typeof payload.raw_sha256 === "string" ? payload.raw_sha256 : sourceRevision;
    if (sha2562(rawBytes) !== expectedDigest || expectedDigest !== sourceRevision) {
      throw new TaskStoreError("TASK_STORE_OBJECT_INVALID", `retained history preimage digest does not match ${sourceRevision}.`);
    }
    return rawBytes.toString("utf8");
  }
  if (typeof payload.package_base64 !== "string")
    return null;
  const packageBytes = Buffer.from(payload.package_base64, "base64");
  if (typeof payload.package_sha256 === "string" && sha2562(packageBytes) !== payload.package_sha256) {
    throw new TaskStoreError("TASK_STORE_OBJECT_INVALID", `history material package digest does not match ${sourceRevision}.`);
  }
  let packageValue;
  try {
    packageValue = JSON.parse(packageBytes.toString("utf8"));
  } catch (error) {
    throw new TaskStoreError("TASK_STORE_OBJECT_INVALID", `history material package ${sourceRevision} is not readable JSON: ${error instanceof Error ? error.message : String(error)}`);
  }
  if (packageValue.kind !== "vnext-task-definition-history" || packageValue.source_revision !== sourceRevision || typeof packageValue.current_task_base64 !== "string") {
    throw new TaskStoreError("TASK_STORE_OBJECT_INVALID", `history material package ${sourceRevision} has an invalid preimage envelope.`);
  }
  const currentBytes = Buffer.from(packageValue.current_task_base64, "base64");
  if (sha2562(currentBytes) !== sourceRevision)
    throw new TaskStoreError("TASK_STORE_OBJECT_INVALID", `history material preimage digest does not match ${sourceRevision}.`);
  return currentBytes.toString("utf8");
}
function legacyAppliedProposals(object) {
  if (object.object_type !== "legacy-current-task" || !record2(object.payload))
    return [];
  const payload = object.payload;
  if (Array.isArray(payload.applied_proposals))
    return payload.applied_proposals;
  if (typeof payload.raw_base64 !== "string")
    return [];
  let raw;
  try {
    raw = Buffer.from(payload.raw_base64, "base64").toString("utf8");
    const match = /^---\r?\n([\s\S]*?)\r?\n---(?:\r?\n|$)/u.exec(raw);
    if (!match)
      return [];
    const parsed = parse3(match[1]);
    const runtime = record2(parsed) && record2(parsed.runtime_state) ? parsed.runtime_state : null;
    return runtime && Array.isArray(runtime.applied_proposals) ? runtime.applied_proposals : [];
  } catch (error) {
    throw new TaskStoreError("TASK_STORE_OBJECT_INVALID", `legacy CURRENT_TASK preimage cannot be parsed for its idempotency ledger: ${error instanceof Error ? error.message : String(error)}`);
  }
}
function legacyExecutionLog(object) {
  if (object.object_type !== "legacy-current-task" || !record2(object.payload))
    return [];
  const payload = object.payload;
  if (Array.isArray(payload.execution_log))
    return payload.execution_log;
  if (typeof payload.raw_base64 !== "string")
    return [];
  try {
    const raw = Buffer.from(payload.raw_base64, "base64").toString("utf8");
    const match = /^---\r?\n([\s\S]*?)\r?\n---(?:\r?\n|$)/u.exec(raw);
    if (!match)
      return [];
    const parsed = parse3(match[1]);
    const runtime = record2(parsed) && record2(parsed.runtime_state) ? parsed.runtime_state : null;
    return runtime && Array.isArray(runtime.execution_log) ? runtime.execution_log : [];
  } catch (error) {
    throw new TaskStoreError("TASK_STORE_OBJECT_INVALID", `legacy CURRENT_TASK preimage cannot be parsed for its execution history: ${error instanceof Error ? error.message : String(error)}`);
  }
}
function nowIso(value) {
  const result = value ?? new Date().toISOString();
  if (Number.isNaN(Date.parse(result)))
    throw new TaskStoreError("TASK_STORE_EVENT_INVALID", `invalid timestamp: ${result}`);
  return result;
}
function proposalDigest(value) {
  return value === undefined ? null : digest(value);
}
function optionalDigest(value) {
  return value === undefined ? null : digest(value);
}
function linkedTaskBasisReference(body) {
  const match = /##\s+(?:任务输入依据|Task Basis)\s*\r?\n\s*\r?\n-\s*path:\s*`([^`]+)`\s*\r?\n-\s*revision:\s*`([a-f0-9]{64})`/mu.exec(body);
  if (!match)
    return null;
  return { path: normalizeRelative(match[1].replace(/\\/gu, "/"), "task basis path"), revision: match[2] };
}
function taskBasisPayload(root2, current) {
  const reference = linkedTaskBasisReference(current.body);
  if (!reference)
    return null;
  const file = path4.resolve(root2, ...reference.path.split("/"));
  const relative = relativePath2(path4.resolve(root2), file);
  assertNoSymlink2(path4.resolve(root2), relative);
  if (!fs4.existsSync(file) || !fs4.statSync(file).isFile()) {
    return {
      schema_version: 1,
      kind: "task-basis-material/v1",
      path: reference.path,
      basis_revision: reference.revision,
      available: false,
      raw_base64: null,
      raw_bytes: 0,
      actual_revision: null
    };
  }
  const bytes = fs4.readFileSync(file);
  const actualRevision = sha2562(bytes);
  return {
    schema_version: 1,
    kind: "task-basis-material/v1",
    path: reference.path,
    basis_revision: reference.revision,
    available: true,
    raw_base64: bytes.toString("base64"),
    raw_bytes: bytes.length,
    actual_revision: actualRevision,
    revision_matches: actualRevision === reference.revision
  };
}
function historyMaterialPayloads(root2, current) {
  const resolvedRoot = path4.resolve(root2);
  const directory = path4.join(path4.dirname(current.filePath), "task-history", current.sourceTuple.document_id);
  const directoryRelative = relativePath2(resolvedRoot, directory);
  assertNoSymlink2(resolvedRoot, directoryRelative);
  if (!fs4.existsSync(directory))
    return [];
  const payloads = [];
  for (const entry of fs4.readdirSync(directory, { withFileTypes: true })) {
    if (entry.isSymbolicLink())
      throw new TaskStoreError("TASK_STORE_PATH_INVALID", `history material cannot traverse a symbolic link: ${entry.name}`);
    if (!entry.isFile())
      continue;
    const match = /^([a-f0-9]{64})\.json$/u.exec(entry.name);
    if (!match)
      continue;
    const file = path4.join(directory, entry.name);
    const relative = relativePath2(resolvedRoot, file);
    assertNoSymlink2(resolvedRoot, relative);
    const bytes = fs4.readFileSync(file);
    payloads.push({
      schema_version: 1,
      kind: "task-history-material/v1",
      source_revision: match[1],
      package_path: relative,
      package_sha256: sha2562(bytes),
      package_bytes: bytes.length,
      package_base64: bytes.toString("base64")
    });
  }
  return payloads;
}
function claimSlotMap(current) {
  const result = new Map;
  const claims = current && Array.isArray(current.runtimeState.claim_evidence) ? current.runtimeState.claim_evidence : [];
  for (const claim of claims) {
    if (!record2(claim) || !Array.isArray(claim.slots))
      continue;
    const claimId = typeof claim.claim_id === "string" ? claim.claim_id : "";
    for (const slot of claim.slots) {
      if (!record2(slot))
        continue;
      const slotId = typeof slot.slot_id === "string" ? slot.slot_id : "";
      result.set(`${claimId}\x00${slotId}`, slot);
    }
  }
  return result;
}
function lineCount(raw) {
  return raw.split(/\r\n?|\n/u).length;
}
function legacyLineMap(raw, replacementSourceRevision = null) {
  const oldLineCount = lineCount(raw);
  return {
    kind: "vnext-current-task-line-map/v1",
    source_revision: sha2562(raw),
    old_line_count: oldLineCount,
    replacement_source_revision: replacementSourceRevision,
    replacement_line_count: null,
    ranges: [{ old_line_start: 1, old_line_end: oldLineCount, new_line_start: null, new_line_end: null, locator: "history-material" }]
  };
}
function legacyLocatorPayload(raw, sourceRevision, sourcePath, replacementSourceRevision) {
  return {
    schema_version: 1,
    kind: "vnext-current-task-locator-alias/v1",
    source_revision: sourceRevision,
    source_path: sourcePath,
    raw_base64: Buffer.from(raw, "utf8").toString("base64"),
    raw_sha256: sha2562(raw),
    raw_bytes: Buffer.byteLength(raw, "utf8"),
    line_map: legacyLineMap(raw, replacementSourceRevision)
  };
}
function collectObjectPayloads(current, includeExecutionLog = true, root2, compareTo) {
  const payloads = [];
  const seen = new Set;
  const add = (objectType, payload) => {
    const key = `${objectType}:${digest(payload)}`;
    if (seen.has(key))
      return;
    seen.add(key);
    payloads.push({ object_type: objectType, payload });
  };
  const state = current.runtimeState;
  const previousMaterial = new Set((compareTo ? projectionMaterial(compareTo)?.objects ?? [] : []).map(projectionDigest));
  for (const object of projectionMaterial(current)?.objects ?? []) {
    if (!previousMaterial.has(projectionDigest(object)))
      add(object.object_type, object.payload);
  }
  if (root2) {
    const basis = taskBasisPayload(root2, current);
    const previousBasis = compareTo ? taskBasisPayload(root2, compareTo) : null;
    if (basis !== null && (!compareTo || optionalDigest(basis) !== optionalDigest(previousBasis)))
      add("task-basis", basis);
    if (includeExecutionLog) {
      for (const history of historyMaterialPayloads(root2, current))
        add("history-material", history);
    }
  }
  const previousSlots = claimSlotMap(compareTo);
  if (Array.isArray(state.claim_evidence)) {
    for (const claim of state.claim_evidence) {
      if (!record2(claim) || !Array.isArray(claim.slots))
        continue;
      for (const slot of claim.slots) {
        if (!record2(slot))
          continue;
        const claimId = typeof claim.claim_id === "string" ? claim.claim_id : "";
        const slotId = typeof slot.slot_id === "string" ? slot.slot_id : "";
        const previous = previousSlots.get(`${claimId}\x00${slotId}`);
        if (slot.report !== undefined && (!compareTo || !previous || optionalDigest(slot.report) !== optionalDigest(previous.report)))
          add("evidence-report", slot.report);
        if (slot.prerequisite_receipt !== undefined && (!compareTo || !previous || optionalDigest(slot.prerequisite_receipt) !== optionalDigest(previous.prerequisite_receipt)))
          add("review-receipt", slot.prerequisite_receipt);
      }
    }
  }
  if (includeExecutionLog && Array.isArray(state.execution_log)) {
    for (const entry of state.execution_log) {
      if (record2(entry)) {
        add("execution-log-entry", entry);
        if (entry.review_receipt !== undefined)
          add("review-receipt", entry.review_receipt);
        if (record2(entry.execution_result)) {
          for (const report of Array.isArray(entry.execution_result.validation_results) ? entry.execution_result.validation_results : [])
            add("evidence-report", report);
        }
      }
    }
  }
  return payloads;
}
function newlyAppendedExecutionEntries(before, after) {
  const beforeEntries = Array.isArray(before.runtimeState.execution_log) ? before.runtimeState.execution_log : [];
  const afterEntries = Array.isArray(after.runtimeState.execution_log) ? after.runtimeState.execution_log : [];
  if (afterEntries.length === 0)
    return [];
  if (beforeEntries.length === 0)
    return afterEntries;
  const beforeByKey = new Map;
  for (const entry of beforeEntries) {
    if (!record2(entry) || typeof entry.idempotency_key !== "string")
      continue;
    const values = beforeByKey.get(entry.idempotency_key) ?? new Set;
    values.add(stableJson(entry));
    beforeByKey.set(entry.idempotency_key, values);
  }
  const delta = afterEntries.filter((entry) => {
    if (!record2(entry) || typeof entry.idempotency_key !== "string")
      return false;
    return !beforeByKey.get(entry.idempotency_key)?.has(stableJson(entry));
  });
  if (delta.length > 0)
    return delta;
  const beforeJson = beforeEntries.map(stableJson);
  const prefix = beforeJson.reduce((count, value, index) => value === stableJson(afterEntries[index]) ? count + 1 : count, 0);
  return afterEntries.length > prefix ? afterEntries.slice(prefix) : [];
}
function newlyAppendedAppliedProposals(before, after) {
  const beforeEntries = Array.isArray(before.runtimeState.applied_proposals) ? before.runtimeState.applied_proposals : [];
  const afterEntries = Array.isArray(after.runtimeState.applied_proposals) ? after.runtimeState.applied_proposals : [];
  if (afterEntries.length === 0)
    return [];
  if (beforeEntries.length === 0)
    return afterEntries;
  const beforeByKey = new Map;
  for (const entry of beforeEntries) {
    if (!record2(entry) || typeof entry.idempotency_key !== "string")
      continue;
    const values = beforeByKey.get(entry.idempotency_key) ?? new Set;
    values.add(stableJson(entry));
    beforeByKey.set(entry.idempotency_key, values);
  }
  const delta = afterEntries.filter((entry) => {
    if (!record2(entry) || typeof entry.idempotency_key !== "string")
      return false;
    return !beforeByKey.get(entry.idempotency_key)?.has(stableJson(entry));
  });
  if (delta.length > 0)
    return delta;
  const beforeJson = beforeEntries.map(stableJson);
  const prefix = beforeJson.reduce((count, value, index) => value === stableJson(afterEntries[index]) ? count + 1 : count, 0);
  return afterEntries.length > prefix ? afterEntries.slice(prefix) : [];
}
function storedExecutionEntryPayload(entry, claimEvidenceReference, executionResultReference) {
  if (!record2(entry)) {
    return {
      schema_version: 2,
      kind: "vnext-execution-log-entry/v2",
      entry
    };
  }
  return {
    schema_version: 2,
    kind: "vnext-execution-log-entry/v2",
    entry: copyWithout(entry, ["claim_evidence", "execution_result"]),
    ...claimEvidenceReference === undefined ? {} : { claim_evidence_ref: claimEvidenceReference },
    ...executionResultReference === undefined ? {} : { execution_result_ref: executionResultReference }
  };
}
function directoryBytes(directory) {
  if (!fs4.existsSync(directory))
    return { bytes: 0, files: 0 };
  let bytes = 0;
  let files = 0;
  const visit = (current) => {
    for (const entry of fs4.readdirSync(current, { withFileTypes: true })) {
      const full = path4.join(current, entry.name);
      if (entry.isSymbolicLink())
        throw new TaskStoreError("TASK_STORE_PATH_INVALID", `symbolic link is not allowed in task store: ${full}`);
      if (entry.isDirectory())
        visit(full);
      else if (entry.isFile()) {
        bytes += fs4.statSync(full).size;
        files++;
      }
    }
  };
  visit(directory);
  return { bytes, files };
}

class TaskStore {
  paths;
  newlyReferencedObjects = new Set;
  constructor(root2, documentId) {
    this.paths = taskStorePaths(root2, documentId);
  }
  static forCurrent(root2, current) {
    return new TaskStore(root2, current.sourceTuple.document_id);
  }
  get exists() {
    return fs4.existsSync(this.paths.manifest);
  }
  get hasPendingCommit() {
    return this.readPending() !== null;
  }
  get pendingCommit() {
    return this.readPending();
  }
  get manifest() {
    if (!fs4.existsSync(this.paths.manifest))
      return null;
    return validateManifest(readJson(this.paths.manifest), this.paths);
  }
  storeObject(documentId, objectType, payload, sourceRevision, recordedAt) {
    const object = {
      schema_version: 1,
      kind: TASK_OBJECT_KIND,
      object_type: objectType,
      document_id: documentId,
      payload
    };
    const hash = digest(object);
    const file = objectFile(this.paths, hash);
    assertNoSymlink2(this.paths.root, relativePath2(this.paths.root, file));
    const content = `${stableJson(object)}
`;
    const fileExists = fs4.existsSync(file);
    const committedBefore = fileExists ? this.objectIsCommitted(hash) : false;
    if (fileExists) {
      if (fs4.readFileSync(file, "utf8") !== content) {
        if (this.objectIsCommitted(hash))
          throw new TaskStoreError("TASK_STORE_OBJECT_CONFLICT", `existing object ${hash} has different bytes.`);
        fs4.rmSync(file, { force: true });
        writePrecommitFile(file, content, () => this.objectIsCommitted(hash), "TASK_STORE_OBJECT_CONFLICT", `object ${hash}`);
      }
    } else {
      writePrecommitFile(file, content, () => this.objectIsCommitted(hash), "TASK_STORE_OBJECT_CONFLICT", `object ${hash}`);
    }
    if (!committedBefore)
      this.newlyReferencedObjects.add(hash);
    return objectRef(objectType, hash);
  }
  readObject(reference, expectedObjectType) {
    const hash = refSha(reference);
    if (!hash)
      throw new TaskStoreError("TASK_STORE_PATH_INVALID", "object reference is invalid.");
    const manifest = this.manifest;
    if (!manifest || !loadCommittedObjectCache(this.paths, manifest).hashes.has(hash)) {
      throw new TaskStoreError("TASK_STORE_OBJECT_NOT_COMMITTED", `object ${hash} is not acknowledged by the aggregate head.`);
    }
    const file = objectFile(this.paths, hash);
    assertNoSymlink2(this.paths.root, relativePath2(this.paths.root, file));
    if (!fs4.existsSync(file))
      throw new TaskStoreError("TASK_STORE_OBJECT_MISSING", `object ${hash} is missing.`);
    const object = validateObject(readJson(file), this.paths, hash);
    if (expectedObjectType !== undefined && object.object_type !== expectedObjectType) {
      throw new TaskStoreError("TASK_STORE_OBJECT_INVALID", `object ${hash} has type ${object.object_type}; expected ${expectedObjectType}.`);
    }
    return object;
  }
  readHistoryMaterial(sourceRevision) {
    if (!SHA2562.test(sourceRevision))
      throw new TaskStoreError("TASK_STORE_PATH_INVALID", "history source revision is invalid.");
    const manifest = this.manifest;
    if (!manifest)
      return null;
    const references = new Map;
    const add = (value) => {
      if (!value || typeof value === "string" || !["history-material", "legacy-current-task"].includes(value.object_type))
        return;
      references.set(value.sha256, value);
    };
    add(manifest.object_refs.legacy_source);
    for (const event of this.listEvents()) {
      for (const value of Object.values(event.object_refs))
        add(value);
    }
    for (const reference of references.values()) {
      const raw = historyRawFromObject(this.readObject(reference), sourceRevision);
      if (raw !== null)
        return raw;
    }
    return null;
  }
  readHistoryLocator(sourceRevision, oldLine) {
    if (!Number.isSafeInteger(oldLine) || oldLine < 1)
      throw new TaskStoreError("TASK_STORE_PATH_INVALID", "history old_line must be a positive integer.");
    const raw = this.readHistoryMaterial(sourceRevision);
    if (raw === null)
      return null;
    const lines = raw.split(/\r\n?|\n/u);
    if (oldLine > lines.length)
      throw new TaskStoreError("TASK_STORE_PATH_INVALID", `history old_line ${oldLine} is outside the retained source preimage.`);
    return { source_revision: sourceRevision, old_line: oldLine, old_line_count: lines.length, text: lines[oldLine - 1] ?? "", locator: "exact-preimage" };
  }
  restoreStoredExecutionEntry(object) {
    const payload = object.payload;
    if (!record2(payload) || payload.kind !== "vnext-execution-log-entry/v2" || !("entry" in payload))
      return payload;
    const entry = record2(payload.entry) ? { ...payload.entry } : payload.entry;
    if (!record2(entry))
      return entry;
    if (payload.claim_evidence_ref !== undefined) {
      const claimObject = this.readObject(payload.claim_evidence_ref, "other");
      const claimPayload = claimObject.payload;
      entry.claim_evidence = record2(claimPayload) && claimPayload.kind === "vnext-execution-claim-evidence/v1" ? claimPayload.claim_evidence : claimPayload;
    }
    if (payload.execution_result_ref !== undefined) {
      const resultObject = this.readObject(payload.execution_result_ref, "result");
      entry.execution_result = resultObject.payload;
    }
    return entry;
  }
  restoreStoredProposal(object) {
    const payload = object.payload;
    if (!record2(payload) || payload.kind !== "vnext-proposal/v2" || !("proposal" in payload))
      return payload;
    if (!record2(payload.proposal)) {
      throw new TaskStoreError("TASK_STORE_OBJECT_INVALID", "vnext-proposal/v2 is missing its proposal record.");
    }
    const proposal = { ...payload.proposal };
    if (!("semantic_delta" in payload))
      return proposal;
    const semanticDelta = record2(payload.semantic_delta) ? { ...payload.semantic_delta } : payload.semantic_delta;
    if (record2(semanticDelta)) {
      if (payload.claim_evidence_ref !== undefined) {
        const claimObject = this.readObject(payload.claim_evidence_ref, "other");
        const claimPayload = claimObject.payload;
        semanticDelta.claim_evidence = record2(claimPayload) && claimPayload.kind === "vnext-execution-claim-evidence/v1" ? claimPayload.claim_evidence : claimPayload;
      }
      if (payload.execution_result_ref !== undefined) {
        const resultObject = this.readObject(payload.execution_result_ref, "result");
        semanticDelta.execution_result = resultObject.payload;
      }
    }
    proposal.semantic_delta = semanticDelta;
    return proposal;
  }
  readTransactionPayload(reference, expected) {
    const object = this.readObject(reference, expected);
    return expected === "proposal" ? this.restoreStoredProposal(object) : object.payload;
  }
  readExecutionLog() {
    return this.readExecutionLogThroughSourceRevision(null);
  }
  readExecutionLogAtSourceRevision(sourceRevision) {
    if (!SHA2562.test(sourceRevision))
      throw new TaskStoreError("TASK_STORE_PATH_INVALID", "historical source revision is invalid.");
    return this.readExecutionLogThroughSourceRevision(sourceRevision);
  }
  readExecutionLogThroughSourceRevision(sourceRevision) {
    const manifest = this.manifest;
    if (!manifest)
      return [];
    const events = committedEventFiles(this.paths, manifest, true);
    const cacheKey = committedObjectCacheKey(this.paths);
    const cached = executionHistoryCaches.get(cacheKey);
    const sharedPrefix = cached !== undefined && cached.events.length <= events.length && cached.events.every((event, index) => events[index] === event);
    let history = sharedPrefix ? [...cached.history] : [];
    const lengths = sharedPrefix ? [...cached.lengths] : [];
    const firstSequence = sharedPrefix ? cached.events.length : 0;
    for (let index = firstSequence;index < events.length; index += 1) {
      const event = events[index];
      if (event.event_type === "legacy-import") {
        const legacyReference = Object.values(event.object_refs).find((reference) => record2(reference) && reference.object_type === "legacy-current-task");
        const reference = legacyReference ?? this.manifest?.object_refs.legacy_source;
        if (reference)
          history.push(...legacyExecutionLog(this.readObject(reference)));
      } else {
        const entries = event.transaction?.execution_log_entries;
        if (Array.isArray(entries)) {
          history.push(...entries);
        } else {
          for (const [key, reference] of Object.entries(event.object_refs)) {
            if (!key.startsWith("execution-log-entry:") || !reference)
              continue;
            history.push(this.restoreStoredExecutionEntry(this.readObject(reference)));
          }
        }
      }
      lengths[index] = history.length;
    }
    executionHistoryCaches.set(cacheKey, { events, history, lengths });
    if (sourceRevision === null)
      return history.slice();
    const boundary = events.findIndex((event) => event.resulting_source_revision === sourceRevision);
    if (boundary < 0)
      throw new TaskStoreError("TASK_STORE_SOURCE_CONFLICT", `no committed event ends at source revision ${sourceRevision}.`);
    return history.slice(0, lengths[boundary] ?? 0);
  }
  readAppliedProposals() {
    const manifest = this.manifest;
    if (!manifest)
      return [];
    const events = committedEventFiles(this.paths, manifest, true);
    const cacheKey = committedObjectCacheKey(this.paths);
    const cached = appliedProposalCaches.get(cacheKey);
    const sharedPrefix = cached !== undefined && cached.events.length <= events.length && cached.events.every((event, index) => events[index] === event);
    const ledger = sharedPrefix ? [...cached.ledger] : [];
    const firstSequence = sharedPrefix ? cached.events.length : 0;
    for (let index = firstSequence;index < events.length; index += 1) {
      const event = events[index];
      if (event.event_type === "storage-migration")
        continue;
      if (event.event_type === "legacy-import") {
        const legacyReference = Object.values(event.object_refs).find((reference) => record2(reference) && reference.object_type === "legacy-current-task");
        const reference = legacyReference ?? this.manifest?.object_refs.legacy_source;
        if (reference)
          ledger.push(...legacyAppliedProposals(this.readObject(reference)));
        continue;
      }
      const explicit = event.transaction?.applied_proposals;
      if (Array.isArray(explicit)) {
        ledger.push(...explicit);
        continue;
      }
      if (event.idempotency_key && event.proposal_digest) {
        ledger.push({
          idempotency_key: event.idempotency_key,
          operation_kind: event.operation_kind,
          proposal_digest: event.proposal_digest,
          source_revision: event.source_revision
        });
      }
    }
    appliedProposalCaches.set(cacheKey, { events, ledger });
    return ledger.slice();
  }
  hydrateHistory(current) {
    this.assertCurrentIdentity(current);
    if (!this.manifest)
      throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", "compact CURRENT_TASK requires a committed task-store manifest.");
    return { execution_log: this.readExecutionLog(), applied_proposals: this.readAppliedProposals() };
  }
  readIndexFile(file) {
    if (!fs4.existsSync(file))
      return [];
    const stat = fs4.statSync(file);
    const cached = indexFileCaches.get(file);
    if (cached && cached.fileSize === stat.size && cached.modifiedAt === stat.mtimeMs && cached.changedAt === stat.ctimeMs)
      return cached.entries;
    if (cached && cached.format === "jsonl" && stat.size > cached.fileSize && cached.changedAt === stat.ctimeMs) {
      const fd = fs4.openSync(file, "r");
      let suffix = "";
      try {
        const bytes = Buffer.alloc(stat.size - cached.fileSize);
        fs4.readSync(fd, bytes, 0, bytes.length, cached.fileSize);
        suffix = bytes.toString("utf8");
      } finally {
        fs4.closeSync(fd);
      }
      const lines = suffix.split(/\r?\n/u).filter(Boolean);
      const additions = lines.map((line, index) => {
        let value;
        try {
          value = JSON.parse(line);
        } catch (error) {
          throw new TaskStoreError("TASK_STORE_INDEX_INVALID", `${file} journal line ${index} is invalid: ${error instanceof Error ? error.message : String(error)}`);
        }
        return value;
      });
      const entries = [...cached.entries, ...this.validateIndexEntries(additions, file, cached.entries.length)];
      indexFileCaches.set(file, { fileSize: stat.size, modifiedAt: stat.mtimeMs, changedAt: stat.ctimeMs, format: "jsonl", entries });
      return entries;
    }
    const raw = fs4.readFileSync(file, "utf8").trim();
    if (!raw)
      return [];
    let values;
    if (raw.startsWith("[")) {
      let value;
      try {
        value = JSON.parse(raw);
      } catch (error) {
        throw new TaskStoreError("TASK_STORE_INDEX_INVALID", `${file} is not valid JSON: ${error instanceof Error ? error.message : String(error)}`);
      }
      if (!Array.isArray(value))
        throw new TaskStoreError("TASK_STORE_INDEX_INVALID", `${file} is not an array.`);
      values = value;
    } else {
      const lines = raw.split(/\r?\n/u).filter(Boolean);
      values = [];
      for (let index = 0;index < lines.length; index += 1) {
        try {
          values.push(JSON.parse(lines[index]));
        } catch (error) {
          if (index === lines.length - 1)
            break;
          throw new TaskStoreError("TASK_STORE_INDEX_INVALID", `${file} journal line ${index} is invalid: ${error instanceof Error ? error.message : String(error)}`);
        }
      }
    }
    const entries = this.validateIndexEntries(values, file, 0);
    indexFileCaches.set(file, { fileSize: stat.size, modifiedAt: stat.mtimeMs, changedAt: stat.ctimeMs, format: raw.startsWith("[") ? "json" : "jsonl", entries });
    return entries;
  }
  validateIndexEntries(values, file, offset) {
    return values.map((item, index) => {
      if (!record2(item) || typeof item.idempotency_key !== "string" || typeof item.operation_kind !== "string" || typeof item.proposal_digest !== "string" || !SHA2562.test(item.proposal_digest) || typeof item.source_revision !== "string" || !SHA2562.test(item.source_revision) || typeof item.event_path !== "string" || typeof item.event_id !== "string" || !Number.isSafeInteger(item.sequence) || item.sequence < 1 || typeof item.legacy !== "boolean") {
        throw new TaskStoreError("TASK_STORE_INDEX_INVALID", `idempotency index entry ${offset + index} in ${file} is invalid.`);
      }
      return item;
    });
  }
  readIndex() {
    const entries = [this.paths.idempotencyIndex, this.paths.legacyIdempotencyIndex].flatMap((file) => this.readIndexFile(file));
    const unique = new Map;
    for (const entry of entries) {
      const identity = stableJson(entry);
      if (!unique.has(identity))
        unique.set(identity, entry);
    }
    return [...unique.values()];
  }
  readEventPath(relative) {
    const normalized = normalizeRelative(relative, "event reference");
    if (!normalized.startsWith(`${this.paths.relativeRoot}/events/`))
      throw new TaskStoreError("TASK_STORE_PATH_INVALID", "event reference is outside this task store.");
    const basename = path4.posix.basename(normalized);
    const match = EVENT_FILE.exec(basename);
    if (!match)
      throw new TaskStoreError("TASK_STORE_PATH_INVALID", `event filename is invalid: ${relative}`);
    const sequence = Number(match[1]);
    const eventHash = match[2];
    const manifest = this.manifest;
    if (!manifest)
      throw new TaskStoreError("TASK_STORE_EVENT_NOT_COMMITTED", `event is not acknowledged by an aggregate head: ${relative}`);
    if (sequence > manifest.head.event_sequence)
      throw new TaskStoreError("TASK_STORE_EVENT_NOT_COMMITTED", `event is not acknowledged by the aggregate head: ${relative}`);
    const event = readEventFile(this.paths, basename);
    const committed = committedEventFiles(this.paths, manifest, true).find((item) => item.sequence === sequence);
    if (!committed || committed.event_hash !== eventHash)
      throw new TaskStoreError("TASK_STORE_EVENT_NOT_COMMITTED", `event is not part of the committed aggregate chain: ${relative}`);
    return event;
  }
  readEvent(reference) {
    const relative = typeof reference === "string" ? reference : path4.posix.join(this.paths.relativeRoot, "events", `${String(reference.sequence).padStart(12, "0")}-${reference.event_hash}.json`);
    return this.readEventPath(relative);
  }
  listEvents() {
    const manifest = this.manifest;
    return manifest ? committedEventFiles(this.paths, manifest) : [];
  }
  writeIndexes(events, idempotency) {
    const eventIndex = events.map((event) => this.eventIndexEntry(event));
    atomicWrite(this.paths.eventIndex, eventIndex.map((item) => stableJson(item)).join(`
`) + (eventIndex.length ? `
` : ""), false);
    atomicWrite(this.paths.idempotencyIndex, idempotency.map((item) => stableJson(item)).join(`
`) + (idempotency.length ? `
` : ""), false);
  }
  eventIndexEntry(event) {
    return {
      sequence: event.sequence,
      event_id: event.event_id,
      event_hash: event.event_hash,
      path: path4.posix.join(this.paths.relativeRoot, "events", `${String(event.sequence).padStart(12, "0")}-${event.event_hash}.json`),
      operation_kind: event.operation_kind,
      idempotency_key: event.idempotency_key,
      source_revision: event.source_revision
    };
  }
  appendIndexes(event, idempotency) {
    const entry = `${stableJson(this.eventIndexEntry(event))}
`;
    fs4.mkdirSync(path4.dirname(this.paths.eventIndex), { recursive: true });
    fs4.appendFileSync(this.paths.eventIndex, entry, "utf8");
    const idempotencyEntry = idempotency.find((item) => item.sequence === event.sequence && item.event_id === event.event_id && item.event_path.endsWith(`${String(event.sequence).padStart(12, "0")}-${event.event_hash}.json`));
    if (idempotencyEntry)
      fs4.appendFileSync(this.paths.idempotencyIndex, `${stableJson(idempotencyEntry)}
`, "utf8");
  }
  makeEvent(eventType, current, manifest, refs, input) {
    const sequence = manifest.head.event_sequence + 1;
    const eventId = `${current.sourceTuple.document_id}:event:${sequence}`;
    const base = {
      schema_version: 1,
      kind: TASK_EVENT_KIND,
      document_id: current.sourceTuple.document_id,
      sequence,
      event_id: eventId,
      event_type: eventType,
      operation_kind: input.operationKind,
      idempotency_key: input.idempotencyKey ?? null,
      proposal_digest: input.proposalDigest ?? null,
      source_revision: manifest.head.source_revision,
      resulting_source_revision: current.sourceTuple.revision,
      definition_revision: manifest.head.definition_revision,
      state_revision: manifest.head.state_revision,
      previous_event_hash: manifest.head.event_hash,
      object_refs: refs,
      ...input.transaction === undefined ? {} : { transaction: input.transaction },
      metadata: {
        committed: input.committed,
        status: input.status,
        ...input.message === undefined ? {} : { message: input.message },
        ...input.code === undefined ? {} : { code: input.code },
        recorded_at: nowIso(input.recordedAt)
      }
    };
    return { ...base, event_hash: sha2562(stableJson(base)) };
  }
  writeEvent(event) {
    const file = eventFile(this.paths, event.sequence, event.event_hash);
    assertNoSymlink2(this.paths.root, relativePath2(this.paths.root, file));
    const content = `${stableJson(event)}
`;
    writePrecommitFile(file, content, () => this.eventIsCommitted(event.sequence), "TASK_STORE_EVENT_CONFLICT", `event ${event.event_id}`);
    return relativePath2(this.paths.root, file);
  }
  objectIsCommitted(hash) {
    const manifest = this.manifest;
    if (!manifest)
      return false;
    return loadCommittedObjectCache(this.paths, manifest).hashes.has(hash);
  }
  eventIsCommitted(sequence) {
    const manifest = this.manifest;
    return Boolean(manifest && sequence <= manifest.head.event_sequence);
  }
  readPending() {
    if (!fs4.existsSync(this.paths.pending))
      return null;
    try {
      const value = JSON.parse(fs4.readFileSync(this.paths.pending, "utf8"));
      if (value.schema_version !== 1 || value.kind !== "vnext-task-store-pending-commit" || value.document_id !== this.paths.documentId || !Number.isSafeInteger(value.sequence) || value.sequence < 1 || typeof value.source_revision !== "string" || !SHA2562.test(value.source_revision) || typeof value.resulting_source_revision !== "string" || !SHA2562.test(value.resulting_source_revision) || value.idempotency_key !== null && typeof value.idempotency_key !== "string" || value.proposal_digest !== null && (typeof value.proposal_digest !== "string" || !SHA2562.test(value.proposal_digest))) {
        throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${this.paths.pending} has an invalid pending transaction marker.`);
      }
      if (value.phase !== undefined && !["prepared", "current-published", "store-published"].includes(value.phase)) {
        throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${this.paths.pending}.phase is invalid.`);
      }
      if (value.write_targets !== undefined && (!Array.isArray(value.write_targets) || value.write_targets.some((item) => typeof item !== "string"))) {
        throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${this.paths.pending}.write_targets is invalid.`);
      }
      if (value.write_set !== undefined) {
        if (!Array.isArray(value.write_set) || value.write_set.length === 0) {
          throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${this.paths.pending}.write_set is invalid.`);
        }
        const paths = new Set;
        for (const [index, item] of value.write_set.entries()) {
          if (!record2(item) || typeof item.path !== "string" || item.before_content !== null && typeof item.before_content !== "string" || item.after_content !== null && typeof item.after_content !== "string") {
            throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${this.paths.pending}.write_set[${index}] is invalid.`);
          }
          const normalized = normalizeRelative(item.path, `${this.paths.pending}.write_set[${index}].path`);
          if (paths.has(normalized))
            throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${this.paths.pending}.write_set contains duplicate path ${normalized}.`);
          paths.add(normalized);
        }
      }
      for (const key of ["before_raw", "after_raw"]) {
        if (value[key] !== undefined && typeof value[key] !== "string") {
          throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${this.paths.pending}.${key} must be a string when present.`);
        }
      }
      for (const key of ["before_runtime_state", "after_runtime_state"]) {
        if (value[key] !== undefined && !record2(value[key])) {
          throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${this.paths.pending}.${key} must be an object when present.`);
        }
      }
      for (const key of ["execution_log_entries", "applied_proposals"]) {
        if (value[key] !== undefined && !Array.isArray(value[key])) {
          throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${this.paths.pending}.${key} must be an array when present.`);
        }
      }
      if (value.previous_manifest_head !== undefined) {
        const head = record2(value.previous_manifest_head) ? value.previous_manifest_head : null;
        if (!head || typeof head.source_revision !== "string" || !SHA2562.test(head.source_revision) || !Number.isSafeInteger(head.event_sequence) || head.event_sequence < 0 || head.event_hash !== null && (typeof head.event_hash !== "string" || !SHA2562.test(head.event_hash))) {
          throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${this.paths.pending}.previous_manifest_head is invalid.`);
        }
      }
      return value;
    } catch (error) {
      if (error instanceof TaskStoreError)
        throw error;
      throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", `${this.paths.pending} is not readable JSON: ${error instanceof Error ? error.message : String(error)}`);
    }
  }
  writePending(pending) {
    assertNoSymlink2(this.paths.root, relativePath2(this.paths.root, this.paths.indexes));
    fs4.mkdirSync(path4.dirname(this.paths.pending), { recursive: true });
    writeJson(this.paths.pending, pending);
  }
  clearPending() {
    try {
      fs4.rmSync(this.paths.pending, { force: true });
    } catch {}
  }
  stageCommit(input) {
    this.assertCurrentIdentity(input.before);
    if (!SHA2562.test(input.after_source_revision))
      throw new TaskStoreError("TASK_STORE_SOURCE_CONFLICT", "pending resulting source revision is invalid.");
    const manifest = this.ensureInitialized(input.before);
    const key = record2(input.proposal) && typeof input.proposal.idempotency_key === "string" ? input.proposal.idempotency_key : null;
    const proposalHash = proposalDigest(input.proposal);
    const normalizedTargets = input.write_targets.map((target) => normalizeRelative(target, "pending write target"));
    const writeSet = input.write_set === undefined ? input.after === undefined ? undefined : [{ path: input.before.relativePath, before_content: input.before.raw, after_content: input.after.raw }] : input.write_set.map((item, index) => ({
      path: normalizeRelative(item.path, `pending write set[${index}].path`),
      before_content: item.before_content,
      after_content: item.after_content
    }));
    if (writeSet !== undefined) {
      const paths = new Set;
      for (const item of writeSet) {
        if (paths.has(item.path))
          throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", `pending write set contains duplicate path ${item.path}.`);
        paths.add(item.path);
        const file = path4.join(this.paths.root, ...item.path.split("/"));
        assertNoSymlink2(this.paths.root, item.path);
        const actual = fs4.existsSync(file) ? fs4.statSync(file).isFile() ? fs4.readFileSync(file, "utf8") : (() => {
          throw new TaskStoreError("TASK_STORE_PATH_INVALID", `pending write target is not a regular file: ${item.path}`);
        })() : null;
        if (actual !== item.before_content)
          throw new TaskStoreError("TASK_STORE_SOURCE_CONFLICT", `pending write target changed before staging: ${item.path}`);
      }
    }
    const compactHistory = compactCurrent(input.before) || input.after !== undefined && compactCurrent(input.after);
    const beforeRuntimeState = compactHistory && input.after !== undefined ? copyWithout(withoutInlineReviewPreimageContent(input.before.runtimeState), ["execution_log", "applied_proposals"]) : withoutInlineReviewPreimageContent(input.before.runtimeState);
    const afterRuntimeState = compactHistory && input.after !== undefined ? copyWithout(withoutInlineReviewPreimageContent(input.after.runtimeState), ["execution_log", "applied_proposals"]) : input.after === undefined ? undefined : withoutInlineReviewPreimageContent(input.after.runtimeState);
    const executionLogEntries = compactHistory && input.after !== undefined ? newlyAppendedExecutionEntries(input.before, input.after) : undefined;
    const appliedProposals = compactHistory && input.after !== undefined ? newlyAppendedAppliedProposals(input.before, input.after) : undefined;
    const existing = this.readPending();
    if (existing) {
      if (existing.sequence !== manifest.head.event_sequence + 1 || existing.source_revision !== input.before.sourceTuple.revision || existing.resulting_source_revision !== input.after_source_revision || existing.idempotency_key !== key || existing.proposal_digest !== proposalHash) {
        throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", "an unfinished task-store intent does not match the requested write.");
      }
      return;
    }
    this.writePending({
      schema_version: 1,
      kind: "vnext-task-store-pending-commit",
      document_id: input.before.sourceTuple.document_id,
      sequence: manifest.head.event_sequence + 1,
      source_revision: input.before.sourceTuple.revision,
      resulting_source_revision: input.after_source_revision,
      idempotency_key: key,
      proposal_digest: proposalHash,
      phase: "prepared",
      proposal: input.proposal,
      ...input.result === undefined ? {} : { result: input.result },
      write_targets: normalizedTargets,
      ...writeSet === undefined ? {} : { write_set: writeSet },
      ...input.after === undefined ? {} : {
        before_raw: input.before.raw,
        after_raw: input.after.raw,
        before_runtime_state: beforeRuntimeState,
        after_runtime_state: afterRuntimeState,
        ...executionLogEntries === undefined ? {} : { execution_log_entries: executionLogEntries },
        ...appliedProposals === undefined ? {} : { applied_proposals: appliedProposals }
      },
      previous_manifest_head: {
        source_revision: manifest.head.source_revision,
        event_sequence: manifest.head.event_sequence,
        event_hash: manifest.head.event_hash
      }
    });
  }
  markCurrentPublished(resultingSourceRevision) {
    if (!SHA2562.test(resultingSourceRevision))
      throw new TaskStoreError("TASK_STORE_SOURCE_CONFLICT", "published source revision is invalid.");
    const pending = this.readPending();
    if (!pending || pending.resulting_source_revision !== resultingSourceRevision) {
      throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", "cannot mark a task-store commit published without its exact pending intent.");
    }
    this.writePending({ ...pending, phase: "current-published" });
  }
  clearPendingForNoCommit() {
    this.clearPending();
  }
  findOrphanTransaction(previous, after, definition, state, idempotencyKey, proposalHash, proposal, proposalReference, operationKind) {
    if (!idempotencyKey || !proposalHash || !fs4.existsSync(this.paths.events))
      return null;
    const sequence = previous.head.event_sequence + 1;
    for (const entry of fs4.readdirSync(this.paths.events, { withFileTypes: true })) {
      if (!entry.isFile())
        continue;
      const match = EVENT_FILE.exec(entry.name);
      if (!match || Number(match[1]) !== sequence)
        continue;
      try {
        const event = readEventFile(this.paths, entry.name);
        const eventProposal = event.transaction?.proposal;
        const proposalMatches = proposalReference && isObjectReference(eventProposal) ? eventProposal.object_type === "proposal" && eventProposal.sha256 === proposalReference.sha256 : eventProposal !== undefined && digest(eventProposal) === digest(proposal);
        if (event.event_type !== (operationKind === "task-storage-migration" ? "storage-migration" : "transaction") || event.previous_event_hash !== previous.head.event_hash || event.idempotency_key !== idempotencyKey || event.proposal_digest !== proposalHash || event.resulting_source_revision !== after.sourceTuple.revision || refSha(event.object_refs.definition) !== definition.sha256 || refSha(event.object_refs.state) !== state.sha256 || !event.transaction || !proposalMatches)
          continue;
        return event;
      } catch {}
    }
    return null;
  }
  buildManifest(current, previous, refs, event, objectCount, idempotencyCount, recordedAt, algorithms) {
    const definitionAlgorithm = algorithms?.definition ?? previous?.definition_revision_algorithm ?? (compactCurrent(current) ? TASK_DEFINITION_REVISION_V2 : TASK_DEFINITION_REVISION_V1);
    const representation = algorithms?.representation ?? previous?.current_representation ?? currentRepresentation(current);
    const first = event ? previous?.head.event_range.first ?? event.sequence : previous?.head.event_range.first ?? null;
    return {
      schema_version: 1,
      kind: TASK_STORE_KIND,
      document_id: current.sourceTuple.document_id,
      task_id: current.sourceTuple.task_id,
      task_slug: current.sourceTuple.task_slug,
      current_task_path: current.relativePath,
      storage_root: this.paths.relativeRoot,
      created_at: previous?.created_at ?? nowIso(recordedAt),
      updated_at: nowIso(recordedAt),
      head: {
        source_revision: current.sourceTuple.revision,
        definition_revision: taskStoreDefinitionRevision(current, definitionAlgorithm),
        state_revision: taskStoreStateRevision(current),
        event_sequence: event?.sequence ?? previous?.head.event_sequence ?? 0,
        event_id: event?.event_id ?? previous?.head.event_id ?? null,
        event_hash: event?.event_hash ?? previous?.head.event_hash ?? null,
        event_range: { first, last: event?.sequence ?? previous?.head.event_range.last ?? null }
      },
      object_refs: {
        definition: refs.definition,
        state: refs.state,
        current_snapshot: refs.currentSnapshot,
        ...refs.legacySource === undefined ? {} : { legacy_source: refs.legacySource }
      },
      counts: {
        events: event ? (previous?.counts.events ?? 0) + 1 : previous?.counts.events ?? 0,
        objects: objectCount,
        idempotency_entries: idempotencyCount
      },
      compatibility: { legacy_current_task: true, hot_window_is_cache: true, full_history_persistent: true },
      storage_format: representation !== "legacy-inline" ? "vnext-task-store/v2" : "vnext-task-store/v1",
      definition_revision_algorithm: definitionAlgorithm,
      state_revision_algorithm: TASK_STATE_REVISION_V1,
      current_representation: representation
    };
  }
  writeManifest(manifest) {
    assertNoSymlink2(this.paths.root, relativePath2(this.paths.root, this.paths.manifest));
    writeJson(this.paths.manifest, manifest, false);
  }
  assertCurrentIdentity(current) {
    if (current.sourceTuple.document_id !== this.paths.documentId)
      throw new TaskStoreError("TASK_STORE_IDENTITY_CONFLICT", "current task document_id does not match task-data path.");
  }
  ensureInitialized(current, recordedAt) {
    this.assertCurrentIdentity(current);
    const existing = this.manifest;
    if (existing) {
      if (existing.task_id !== current.sourceTuple.task_id || existing.task_slug !== current.sourceTuple.task_slug || existing.current_task_path !== current.relativePath) {
        throw new TaskStoreError("TASK_STORE_IDENTITY_CONFLICT", "existing task store identity conflicts with CURRENT_TASK.");
      }
      if (existing.head.source_revision !== current.sourceTuple.revision) {
        return this.reconcileExternalCurrent(current, recordedAt);
      }
      return existing;
    }
    this.newlyReferencedObjects.clear();
    fs4.mkdirSync(this.paths.objects, { recursive: true });
    fs4.mkdirSync(this.paths.events, { recursive: true });
    fs4.mkdirSync(this.paths.indexes, { recursive: true });
    const definitionAlgorithm = TASK_DEFINITION_REVISION_V2;
    const representation = currentRepresentation(current);
    const definition = this.storeObject(current.sourceTuple.document_id, "definition", storedCurrentPayload(current, "definition", definitionPayload(current, definitionAlgorithm)), current.sourceTuple.revision, recordedAt);
    const state = this.storeObject(current.sourceTuple.document_id, "state", storedCurrentPayload(current, "state", stateSnapshotPayload(current)), current.sourceTuple.revision, recordedAt);
    const legacySource = this.storeObject(current.sourceTuple.document_id, "legacy-current-task", {
      source_revision: current.sourceTuple.revision,
      source_path: current.relativePath,
      raw_base64: Buffer.from(current.raw, "utf8").toString("base64"),
      raw_sha256: current.sourceTuple.revision,
      raw_bytes: Buffer.byteLength(current.raw),
      line_map: legacyLineMap(current.raw),
      ...compactCurrent(current) ? {
        execution_log: current.runtimeState.execution_log,
        applied_proposals: current.runtimeState.applied_proposals
      } : {}
    }, current.sourceTuple.revision, recordedAt);
    const empty = this.buildManifest(current, null, { definition, state, currentSnapshot: state, legacySource }, null, 0, 0, recordedAt, {
      definition: definitionAlgorithm,
      representation
    });
    const initialPayloadRefs = {};
    for (const payload of collectObjectPayloads(current, true, this.paths.root)) {
      const reference = this.storeObject(current.sourceTuple.document_id, payload.object_type, payload.payload, current.sourceTuple.revision, recordedAt);
      initialPayloadRefs[`${payload.object_type}:${reference.sha256}`] = reference;
    }
    const legacyEvent = this.makeEvent("legacy-import", current, empty, {
      definition,
      state,
      current_snapshot: state,
      legacy_source: legacySource,
      ...initialPayloadRefs
    }, {
      operationKind: "legacy-import",
      status: "imported",
      committed: true,
      message: "Legacy CURRENT_TASK was imported without rewriting its bytes.",
      recordedAt
    });
    const eventPath = this.writeEvent(legacyEvent);
    const applied = Array.isArray(current.runtimeState.applied_proposals) ? current.runtimeState.applied_proposals : [];
    const index = applied.filter(record2).map((item) => ({
      idempotency_key: typeof item.idempotency_key === "string" ? item.idempotency_key : "",
      operation_kind: typeof item.operation_kind === "string" ? item.operation_kind : "task-state-transaction",
      proposal_digest: typeof item.proposal_digest === "string" && SHA2562.test(item.proposal_digest) ? item.proposal_digest : digest(item),
      source_revision: typeof item.source_revision === "string" && SHA2562.test(item.source_revision) ? item.source_revision : current.sourceTuple.revision,
      event_path: eventPath,
      event_id: legacyEvent.event_id,
      sequence: legacyEvent.sequence,
      legacy: true
    })).filter((item) => item.idempotency_key.length > 0);
    this.writeIndexes([legacyEvent], index);
    const manifest = this.buildManifest(current, empty, { definition, state, currentSnapshot: state, legacySource }, legacyEvent, this.newlyReferencedObjects.size, index.length, recordedAt, {
      definition: definitionAlgorithm,
      representation
    });
    this.writeManifest(manifest);
    rememberCommittedObjectReferences(this.paths, manifest, Object.values(legacyEvent.object_refs));
    rememberIdempotencyMatches(this.paths, manifest, index.map((entry) => ({ ...entry, event: legacyEvent })));
    return manifest;
  }
  reconcileExternalCurrent(current, recordedAt) {
    const previous = this.manifest;
    if (!previous)
      return this.ensureInitialized(current, recordedAt);
    if (previous.current_representation !== "legacy-inline") {
      throw new TaskStoreError("TASK_STORE_SOURCE_CONFLICT", "compact CURRENT_TASK changed without a matching governed task-store journal; recovery is required.");
    }
    this.newlyReferencedObjects.clear();
    const definitionAlgorithm = previous.definition_revision_algorithm;
    const definition = this.storeObject(current.sourceTuple.document_id, "definition", storedCurrentPayload(current, "definition", definitionPayload(current, definitionAlgorithm)), current.sourceTuple.revision, recordedAt);
    const state = this.storeObject(current.sourceTuple.document_id, "state", storedCurrentPayload(current, "state", stateSnapshotPayload(current)), current.sourceTuple.revision, recordedAt);
    const stagedManifest = {
      ...previous,
      head: {
        ...previous.head,
        source_revision: current.sourceTuple.revision,
        definition_revision: taskStoreDefinitionRevision(current, definitionAlgorithm),
        state_revision: taskStoreStateRevision(current)
      }
    };
    const refs = { definition, state, current_snapshot: state };
    for (const payload of collectObjectPayloads(current, true, this.paths.root)) {
      const reference = this.storeObject(current.sourceTuple.document_id, payload.object_type, payload.payload, current.sourceTuple.revision, recordedAt);
      refs[`${payload.object_type}:${reference.sha256}`] = reference;
    }
    const event = this.makeEvent("external-current-sync", current, stagedManifest, refs, {
      operationKind: "external-current-sync",
      status: "reconciled",
      committed: true,
      message: "A legacy/current writer advanced CURRENT_TASK before the sidecar commit was observed.",
      recordedAt
    });
    const eventPath = this.writeEvent(event);
    const index = this.readIndex();
    this.appendIndexes(event, index);
    const manifest = this.buildManifest(current, previous, { definition, state, currentSnapshot: state, ...previous.object_refs.legacy_source ? { legacySource: previous.object_refs.legacy_source } : {} }, event, previous.counts.objects + this.newlyReferencedObjects.size, index.length, recordedAt);
    this.writeManifest(manifest);
    rememberCommittedObjectReferences(this.paths, manifest, Object.values(event.object_refs));
    return manifest;
  }
  recordCommit(input) {
    this.assertCurrentIdentity(input.before);
    this.assertCurrentIdentity(input.after);
    if (!input.result.committed)
      return this.manifest;
    this.newlyReferencedObjects.clear();
    const previous = this.ensureInitialized(input.before, input.recorded_at);
    this.newlyReferencedObjects.clear();
    const key = input.result.idempotency_key ?? (record2(input.proposal) && typeof input.proposal.idempotency_key === "string" ? input.proposal.idempotency_key : null);
    const proposalHash = proposalDigest(input.proposal);
    let recoveryPending = false;
    const pending = this.readPending();
    if (pending) {
      if (pending.sequence <= previous.head.event_sequence) {
        this.clearPending();
      } else if (pending.sequence !== previous.head.event_sequence + 1 || pending.source_revision !== input.before.sourceTuple.revision || pending.resulting_source_revision !== input.after.sourceTuple.revision || pending.idempotency_key !== key || pending.proposal_digest !== proposalHash) {
        throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", "an unfinished task-store transaction does not match this commit request.");
      } else {
        if (pending.proposal !== undefined && digest(pending.proposal) !== digest(input.proposal)) {
          throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", "pending task-store intent contains different proposal bytes.");
        }
        recoveryPending = true;
      }
    }
    if (key && proposalHash) {
      const prior = this.lookupIdempotency(key);
      if (prior) {
        if (prior.proposal_digest !== proposalHash)
          throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", `idempotency key ${key} is bound to different proposal bytes.`);
        return previous;
      }
    }
    this.writePending({
      schema_version: 1,
      kind: "vnext-task-store-pending-commit",
      document_id: input.after.sourceTuple.document_id,
      sequence: previous.head.event_sequence + 1,
      source_revision: input.before.sourceTuple.revision,
      resulting_source_revision: input.after.sourceTuple.revision,
      idempotency_key: key,
      proposal_digest: proposalHash,
      phase: "current-published",
      proposal: input.proposal,
      result: input.result,
      write_targets: [input.after.relativePath],
      ...pending?.before_raw === undefined ? {} : { before_raw: pending.before_raw },
      ...pending?.after_raw === undefined ? {} : { after_raw: pending.after_raw },
      ...pending?.before_runtime_state === undefined ? {} : { before_runtime_state: pending.before_runtime_state },
      ...pending?.after_runtime_state === undefined ? {} : { after_runtime_state: pending.after_runtime_state },
      ...pending?.execution_log_entries === undefined ? {} : { execution_log_entries: pending.execution_log_entries },
      ...pending?.applied_proposals === undefined ? {} : { applied_proposals: pending.applied_proposals },
      ...pending?.write_set === undefined ? {} : { write_set: pending.write_set },
      previous_manifest_head: {
        source_revision: previous.head.source_revision,
        event_sequence: previous.head.event_sequence,
        event_hash: previous.head.event_hash
      }
    });
    const definitionAlgorithm = compactCurrent(input.after) ? TASK_DEFINITION_REVISION_V2 : previous.definition_revision_algorithm;
    const definition = this.storeObject(input.after.sourceTuple.document_id, "definition", storedCurrentPayload(input.after, "definition", definitionPayload(input.after, definitionAlgorithm)), input.after.sourceTuple.revision, input.recorded_at);
    const state = this.storeObject(input.after.sourceTuple.document_id, "state", storedCurrentPayload(input.after, "state", stateSnapshotPayload(input.after)), input.after.sourceTuple.revision, input.recorded_at);
    const refs = {
      definition,
      state
    };
    const proposalRecord = record2(input.proposal) ? input.proposal : null;
    const proposalSemanticDelta = proposalRecord && record2(proposalRecord.semantic_delta) ? proposalRecord.semantic_delta : null;
    const proposalClaimEvidenceReference = proposalSemanticDelta && proposalSemanticDelta.claim_evidence !== undefined ? this.storeObject(input.after.sourceTuple.document_id, "other", {
      schema_version: 1,
      kind: "vnext-execution-claim-evidence/v1",
      claim_evidence: proposalSemanticDelta.claim_evidence
    }, input.after.sourceTuple.revision, input.recorded_at) : undefined;
    const proposalExecutionResultReference = proposalSemanticDelta && proposalSemanticDelta.execution_result !== undefined ? this.storeObject(input.after.sourceTuple.document_id, "result", proposalSemanticDelta.execution_result, input.after.sourceTuple.revision, input.recorded_at) : undefined;
    const proposalReference = this.storeObject(input.after.sourceTuple.document_id, "proposal", storedProposalPayload(input.proposal, proposalClaimEvidenceReference, proposalExecutionResultReference), input.after.sourceTuple.revision, input.recorded_at);
    const resultReference = this.storeObject(input.after.sourceTuple.document_id, "result", input.result, input.after.sourceTuple.revision, input.recorded_at);
    refs.proposal = proposalReference;
    refs.result = resultReference;
    if (proposalClaimEvidenceReference)
      refs["proposal-claim-evidence"] = proposalClaimEvidenceReference;
    if (proposalExecutionResultReference)
      refs["proposal-execution-result"] = proposalExecutionResultReference;
    if (input.result.operation_kind === "task-storage-migration" || previous.current_representation !== currentRepresentation(input.after) && compactCurrent(input.after)) {
      const locatorAlias = this.storeObject(input.after.sourceTuple.document_id, "history-material", legacyLocatorPayload(input.before.raw, input.before.sourceTuple.revision, input.before.relativePath, input.after.sourceTuple.revision), input.after.sourceTuple.revision, input.recorded_at);
      refs["legacy-locator-alias"] = locatorAlias;
    }
    for (const payload of collectObjectPayloads(input.after, false, this.paths.root, input.before)) {
      const reference = this.storeObject(input.after.sourceTuple.document_id, payload.object_type, payload.payload, input.after.sourceTuple.revision, input.recorded_at);
      const keyName = `${payload.object_type}:${reference.sha256}`;
      refs[keyName] = reference;
    }
    refs.current_snapshot = state;
    const executionEntries = newlyAppendedExecutionEntries(input.before, input.after);
    executionEntries.forEach((entry, index) => {
      const entryRecord = record2(entry) ? entry : null;
      const claimEvidenceReference = entryRecord && entryRecord.claim_evidence !== undefined ? this.storeObject(input.after.sourceTuple.document_id, "other", {
        schema_version: 1,
        kind: "vnext-execution-claim-evidence/v1",
        claim_evidence: entryRecord.claim_evidence
      }, input.after.sourceTuple.revision, input.recorded_at) : undefined;
      const executionResultReference = entryRecord && entryRecord.execution_result !== undefined ? this.storeObject(input.after.sourceTuple.document_id, "result", entryRecord.execution_result, input.after.sourceTuple.revision, input.recorded_at) : undefined;
      const executionEntryReference = this.storeObject(input.after.sourceTuple.document_id, "execution-log-entry", storedExecutionEntryPayload(entry, claimEvidenceReference, executionResultReference), input.after.sourceTuple.revision, input.recorded_at);
      refs[`execution-log-entry:${String(index).padStart(6, "0")}:${executionEntryReference.sha256}`] = executionEntryReference;
      if (claimEvidenceReference)
        refs[`execution-claim-evidence:${String(index).padStart(6, "0")}:${claimEvidenceReference.sha256}`] = claimEvidenceReference;
      if (executionResultReference)
        refs[`execution-result:${String(index).padStart(6, "0")}:${executionResultReference.sha256}`] = executionResultReference;
    });
    const stagedManifest = {
      ...previous,
      head: {
        ...previous.head,
        source_revision: input.before.sourceTuple.revision,
        definition_revision: taskStoreDefinitionRevision(input.after, definitionAlgorithm),
        state_revision: taskStoreStateRevision(input.after)
      }
    };
    const operationKind = input.result.operation_kind ?? (proposalRecord && typeof proposalRecord.operation_kind === "string" ? proposalRecord.operation_kind : "unknown");
    const recoveredEvent = recoveryPending ? this.findOrphanTransaction(previous, input.after, definition, state, key, proposalHash, input.proposal, proposalReference, operationKind) : null;
    const eventType = input.result.operation_kind === "task-storage-migration" ? "storage-migration" : "transaction";
    const event = recoveredEvent ?? this.makeEvent(eventType, input.after, stagedManifest, refs, {
      operationKind,
      idempotencyKey: key,
      proposalDigest: proposalHash,
      status: input.result.status,
      committed: input.result.committed,
      message: input.result.message,
      code: input.result.code,
      recordedAt: input.recorded_at,
      transaction: { proposal: proposalReference, result: resultReference }
    });
    const eventPath = recoveredEvent ? relativePath2(this.paths.root, eventFile(this.paths, recoveredEvent.sequence, recoveredEvent.event_hash)) : this.writeEvent(event);
    const index = this.readIndex();
    if (key && proposalHash)
      index.push({
        idempotency_key: key,
        operation_kind: event.operation_kind,
        proposal_digest: proposalHash,
        source_revision: input.before.sourceTuple.revision,
        event_path: eventPath,
        event_id: event.event_id,
        sequence: event.sequence,
        legacy: false
      });
    const uniqueIndex = [...new Map(index.map((item) => [stableJson(item), item])).values()];
    this.appendIndexes(event, uniqueIndex);
    const manifest = this.buildManifest(input.after, previous, {
      definition,
      state,
      currentSnapshot: state,
      ...previous.object_refs.legacy_source ? { legacySource: previous.object_refs.legacy_source } : {}
    }, event, previous.counts.objects + this.newlyReferencedObjects.size, uniqueIndex.length, input.recorded_at, {
      definition: definitionAlgorithm,
      representation: compactCurrent(input.after) ? currentRepresentation(input.after) : previous.current_representation
    });
    this.writeManifest(manifest);
    rememberCommittedObjectReferences(this.paths, manifest, Object.values(event.object_refs));
    if (key && proposalHash) {
      rememberIdempotencyMatches(this.paths, manifest, [{
        idempotency_key: key,
        operation_kind: event.operation_kind,
        proposal_digest: proposalHash,
        source_revision: input.before.sourceTuple.revision,
        event_path: eventPath,
        event_id: event.event_id,
        sequence: event.sequence,
        legacy: false,
        event
      }]);
    }
    this.clearPending();
    return manifest;
  }
  lookupIdempotency(idempotencyKey) {
    const manifest = this.manifest;
    if (!manifest)
      return null;
    let entries = this.readIndex();
    let matches = entries.filter((item) => item.idempotency_key === idempotencyKey && item.sequence <= manifest.head.event_sequence);
    if (matches.length === 0) {
      const cachedMatches = loadIdempotencyEventCache(this.paths, manifest).byKey.get(idempotencyKey) ?? [];
      matches = cachedMatches.map((match) => ({
        idempotency_key: match.idempotency_key,
        operation_kind: match.operation_kind,
        proposal_digest: match.proposal_digest,
        source_revision: match.source_revision,
        event_path: match.event_path,
        event_id: match.event_id,
        sequence: match.sequence,
        legacy: match.legacy
      }));
    }
    if (matches.length === 0)
      return null;
    const proposalDigests = new Set(matches.map((item) => item.proposal_digest));
    if (proposalDigests.size > 1) {
      throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", `idempotency key ${idempotencyKey} has multiple committed proposal payloads; the historical query is ambiguous.`);
    }
    const entry = matches[0];
    const event = this.readEvent(entry.event_path);
    let legacyEntryVerified = false;
    if (entry.legacy && event.event_type === "legacy-import" && event.proposal_digest === null) {
      const legacyRef = manifest.object_refs.legacy_source;
      if (legacyRef) {
        const legacy = this.readObject(legacyRef);
        const applied = legacyAppliedProposals(legacy);
        legacyEntryVerified = applied.some((item) => record2(item) && item.idempotency_key === entry.idempotency_key && item.proposal_digest === entry.proposal_digest && item.operation_kind === entry.operation_kind);
      }
    }
    if (event.document_id !== this.paths.documentId || event.event_id !== entry.event_id || event.sequence !== entry.sequence || event.idempotency_key !== entry.idempotency_key && !legacyEntryVerified || event.proposal_digest !== entry.proposal_digest && !legacyEntryVerified) {
      throw new TaskStoreError("TASK_STORE_INDEX_INVALID", `idempotency index entry ${idempotencyKey} does not point to the committed event.`);
    }
    return { ...entry, event };
  }
  latestEvents(limit = 16) {
    if (!Number.isSafeInteger(limit) || limit < 1 || limit > 256)
      throw new TaskStoreError("TASK_STORE_INDEX_INVALID", "latest event limit must be 1..256.");
    return this.listEvents().slice(-limit);
  }
  measure() {
    const objects = directoryBytes(this.paths.objects);
    const events = directoryBytes(this.paths.events);
    const indexes = directoryBytes(this.paths.indexes);
    const manifestBytes = fs4.existsSync(this.paths.manifest) ? fs4.statSync(this.paths.manifest).size : 0;
    return {
      document_id: this.paths.documentId,
      root: this.paths.relativeRoot,
      total_bytes: objects.bytes + events.bytes + indexes.bytes + manifestBytes,
      files: objects.files + events.files + indexes.files + (manifestBytes > 0 ? 1 : 0),
      objects_bytes: objects.bytes,
      events_bytes: events.bytes,
      indexes_bytes: indexes.bytes,
      manifest_bytes: manifestBytes
    };
  }
  validateCurrentAggregate(current) {
    const errors = [];
    const manifestPath = this.paths.relativeRoot + "/" + TASK_STORE_MANIFEST_FILE;
    let manifest = null;
    try {
      manifest = this.manifest;
    } catch (error) {
      errors.push(error instanceof Error ? error.message : String(error));
    }
    if (!manifest) {
      return {
        status: errors.length > 0 ? "invalid" : "unavailable",
        validation_scope: "current-aggregate",
        document_id: this.paths.documentId,
        manifest_path: manifestPath,
        verified_objects: 0,
        verified_head_event: false,
        errors
      };
    }
    try {
      this.assertCurrentIdentity(current);
      if (manifest.current_task_path !== current.relativePath)
        errors.push("manifest current_task_path does not match CURRENT_TASK.");
      if (manifest.task_id !== current.sourceTuple.task_id || manifest.task_slug !== current.sourceTuple.task_slug)
        errors.push("manifest task identity does not match CURRENT_TASK.");
      if (manifest.head.source_revision !== current.sourceTuple.revision)
        errors.push("manifest head source_revision does not match CURRENT_TASK.");
      if (currentRepresentation(current) === ACTIVE_TASK_FORMAT) {
        const binding = current.frontmatter.task_store;
        if (stableJson(binding.projection.definition) !== stableJson(manifest.object_refs.definition) || stableJson(binding.projection.state) !== stableJson(manifest.object_refs.state))
          errors.push("projection roots differ from the committed aggregate.");
        for (const object of projectionMaterial(current).objects)
          this.readObject({ object_type: object.object_type, sha256: projectionDigest(object) });
      }
      const expectedDefinitionRevision = taskStoreDefinitionRevision(current, manifest.definition_revision_algorithm);
      const expectedStateRevision = taskStoreStateRevision(current);
      if (manifest.head.definition_revision !== expectedDefinitionRevision)
        errors.push("manifest definition revision does not match the governed CURRENT_TASK definition.");
      if (manifest.head.state_revision !== expectedStateRevision || manifest.object_refs.current_snapshot.sha256 !== manifest.object_refs.state.sha256)
        errors.push("manifest state revision does not match the governed CURRENT_TASK state.");
      const currentReferences = new Map;
      for (const ref of [manifest.object_refs.definition, manifest.object_refs.state, manifest.object_refs.current_snapshot, manifest.object_refs.legacy_source].filter(Boolean)) {
        this.readObject(ref);
        currentReferences.set(ref.sha256, ref);
      }
      let verifiedHeadEvent = false;
      if (manifest.head.event_sequence === 0 || manifest.head.event_hash === null) {
        if (manifest.head.event_sequence !== 0 || manifest.head.event_hash !== null || manifest.head.event_id !== null)
          errors.push("manifest has an incomplete empty event head.");
      } else {
        if (manifest.head.event_id === null)
          errors.push("manifest head event_id is missing.");
        const event = this.readEvent({ sequence: manifest.head.event_sequence, event_hash: manifest.head.event_hash });
        if (event.event_id !== manifest.head.event_id || event.resulting_source_revision !== current.sourceTuple.revision)
          errors.push("manifest head event does not result in the current source revision.");
        verifiedHeadEvent = true;
      }
      return {
        status: errors.length === 0 ? "valid" : "invalid",
        validation_scope: "current-aggregate",
        document_id: this.paths.documentId,
        manifest_path: manifestPath,
        verified_objects: errors.length === 0 ? currentReferences.size : 0,
        verified_head_event: verifiedHeadEvent,
        errors
      };
    } catch (error) {
      errors.push(error instanceof Error ? error.message : String(error));
      return {
        status: "invalid",
        validation_scope: "current-aggregate",
        document_id: this.paths.documentId,
        manifest_path: manifestPath,
        verified_objects: 0,
        verified_head_event: false,
        errors
      };
    }
  }
  deepValidate() {
    const errors = [];
    let manifest = null;
    try {
      manifest = this.manifest;
    } catch (error) {
      errors.push(error instanceof Error ? error.message : String(error));
    }
    let objects = 0;
    let events = 0;
    let idempotency = 0;
    if (manifest) {
      const verifiedObjectHashes = new Set;
      const verifyObject = (ref) => {
        if (verifiedObjectHashes.has(ref.sha256))
          return;
        try {
          this.readObject(ref);
          verifiedObjectHashes.add(ref.sha256);
        } catch (error) {
          errors.push(error instanceof Error ? error.message : String(error));
        }
      };
      for (const ref of [manifest.object_refs.definition, manifest.object_refs.state, manifest.object_refs.current_snapshot, manifest.object_refs.legacy_source].filter(Boolean)) {
        verifyObject(ref);
      }
      let previous = null;
      try {
        const allEvents = this.listEvents();
        for (const event of allEvents) {
          if (event.sequence !== events + 1)
            errors.push(`event sequence gap at ${event.sequence}`);
          if (event.previous_event_hash !== previous)
            errors.push(`event chain mismatch at ${event.event_id}`);
          previous = event.event_hash;
          events++;
          for (const ref of Object.values(event.object_refs)) {
            if (ref === null)
              continue;
            verifyObject(ref);
          }
        }
        if (manifest.head.event_hash !== previous || manifest.head.event_sequence !== (allEvents.at(-1)?.sequence ?? 0))
          errors.push("manifest head does not match the full event chain.");
      } catch (error) {
        errors.push(error instanceof Error ? error.message : String(error));
      }
      try {
        const entries = this.readIndex();
        idempotency = entries.length;
        for (const entry of entries)
          this.lookupIdempotency(entry.idempotency_key);
        if (entries.length !== manifest.counts.idempotency_entries)
          errors.push("manifest idempotency count differs from the index.");
      } catch (error) {
        errors.push(error instanceof Error ? error.message : String(error));
      }
      if (manifest.counts.events !== events)
        errors.push("manifest event count differs from the event directory.");
      objects = verifiedObjectHashes.size;
      if (manifest.counts.objects !== objects)
        errors.push(`manifest object count ${manifest.counts.objects} differs from committed references ${objects}.`);
    }
    return {
      status: errors.length === 0 && manifest !== null ? "valid" : "invalid",
      validation_scope: "aggregate-and-full-history",
      document_id: this.paths.documentId,
      manifest_path: this.paths.relativeRoot + "/" + TASK_STORE_MANIFEST_FILE,
      verified_objects: objects,
      verified_events: events,
      verified_idempotency_entries: idempotency,
      errors
    };
  }
  exportAggregate() {
    const manifest = this.manifest;
    if (!manifest)
      throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", "task store is not initialized.");
    const references = new Map;
    const add = (value) => {
      if (!value || typeof value === "string")
        return;
      references.set(value.sha256, value);
    };
    add(manifest.object_refs.definition);
    add(manifest.object_refs.state);
    add(manifest.object_refs.current_snapshot);
    add(manifest.object_refs.legacy_source);
    const events = this.listEvents();
    for (const event of events)
      for (const value of Object.values(event.object_refs))
        add(value);
    const objects = [...references.values()].map((reference) => ({ sha256: reference.sha256, object: this.readObject(reference) }));
    return { manifest, objects: objects.sort((a, b) => a.sha256.localeCompare(b.sha256)), events, measure: this.measure() };
  }
}
function taskStoreDefinitionPayload(current, algorithm = TASK_DEFINITION_REVISION_V2) {
  return definitionPayload(current, algorithm);
}
function taskStoreStateSnapshotPayload(current) {
  return stateSnapshotPayload(current);
}
function taskStoreDefinitionRevision(current, algorithm = TASK_DEFINITION_REVISION_V2) {
  return digest(definitionPayload(current, algorithm));
}
function taskStoreDefinitionRevisionForManifest(current, manifest) {
  return taskStoreDefinitionRevision(current, manifest?.definition_revision_algorithm ?? TASK_DEFINITION_REVISION_V2);
}
function taskStoreStateRevision(current) {
  return digest(stateSnapshotPayload(current));
}
function taskStoreHistoryPath(root2, current, sourceRevision) {
  if (!SHA2562.test(sourceRevision))
    throw new TaskStoreError("TASK_STORE_PATH_INVALID", "history source revision is invalid.");
  const directory = path4.join(path4.dirname(current.filePath), "task-history", current.sourceTuple.document_id);
  const rootResolved = path4.resolve(root2);
  const normalized = relativePath2(rootResolved, directory);
  return path4.join(rootResolved, ...path4.posix.join(normalized, `${sourceRevision}.json`).split("/"));
}
function compactHistoryBody(body, executionLog) {
  const normalized = body.replace(/\r\n?/gu, `
`);
  const headings = [...normalized.matchAll(/^##\s+(.+?)\s*$/gmu)];
  const index = headings.findIndex((match) => ["执行记录", "Execution Log"].includes(match[1].trim()));
  if (index < 0)
    throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", "CURRENT_TASK is missing ## 执行记录 for compact migration.");
  const heading = headings[index];
  const contentStart = (heading.index ?? 0) + heading[0].length;
  const nextHeading = headings.slice(index + 1).find((match) => (match[0].match(/^#/u)?.[0].length ?? 2) <= 2);
  const contentEnd = nextHeading?.index ?? normalized.length;
  const previews = executionLog.filter(record2).slice(-8).map((item) => {
    const action = typeof item.action === "string" ? item.action : "step-execution";
    const key = typeof item.idempotency_key === "string" ? item.idempotency_key : "unknown";
    const step = typeof item.step_id === "string" ? ` | step=${item.step_id}` : "";
    const status = typeof item.status === "string" ? ` | status=${item.status}` : "";
    return `- ${action} | key=${key}${step}${status}`;
  });
  let preview = [
    "- Store-backed history is retained under task-data; use task-read for exact historical reads.",
    "- This section is a bounded navigation preview and is not the Runtime fact source.",
    ...previews.length > 0 ? ["", ...previews] : []
  ].join(`
`);
  if (Buffer.byteLength(preview, "utf8") > 8192)
    preview = Buffer.from(preview, "utf8").subarray(0, 8192).toString("utf8");
  return normalized.slice(0, contentStart) + `
${preview}

` + normalized.slice(contentEnd);
}
function compactCurrentRaw(current, store) {
  const frontmatter = structuredClone(current.frontmatter ?? {});
  const runtime = withoutInlineReviewPreimageContent(current.runtimeState);
  delete runtime.execution_log;
  delete runtime.applied_proposals;
  frontmatter.runtime_state = runtime;
  frontmatter.task_store = {
    schema_version: 1,
    kind: "vnext-current-task-store-binding",
    format: ACTIVE_TASK_FORMAT,
    manifest_path: `${store.paths.relativeRoot}/manifest.json`,
    history: { execution_log: "task-store", applied_proposals: "task-store" }
  };
  const body = compactHistoryBody(current.body, Array.isArray(current.runtimeState.execution_log) ? current.runtimeState.execution_log : []);
  return prepareTaskProjection(`---
${stringify2(frontmatter).trimEnd()}
---
${body}`);
}
function compactAfter(current, prepared) {
  const match = /^---\n([\s\S]*?)\n---\n/u.exec(prepared.expandedContent);
  const frontmatter = parse3(match[1]);
  return {
    ...current,
    raw: prepared.content,
    body: prepared.expandedContent.slice(match[0].length),
    frontmatter,
    runtimeState: withoutInlineReviewPreimageContent(current.runtimeState),
    sourceTuple: { ...current.sourceTuple, revision: sha2562(prepared.content) }
  };
}
function migrationSemanticModel(current) {
  return {
    identity: {
      path: current.relativePath,
      document_id: current.sourceTuple.document_id,
      task_id: current.sourceTuple.task_id,
      task_slug: current.sourceTuple.task_slug
    },
    frontmatter: copyWithout(current.frontmatter ?? {}, ["task_store", "runtime_state"]),
    runtime_state: copyWithout(withoutInlineReviewPreimageContent(current.runtimeState), ["execution_log", "applied_proposals"]),
    definition_sections: definitionSections(current, true).filter((section) => !["执行记录", "Execution Log"].includes(section.title))
  };
}
function hasLegacyInlineReviewPreimages(current) {
  const coverage = record2(current.runtimeState.review_coverage) ? current.runtimeState.review_coverage : null;
  return Array.isArray(coverage?.preimages) && coverage.preimages.some((item) => record2(item) && Object.prototype.hasOwnProperty.call(item, "content_base64"));
}
function readStorageMigrationLineage(root2, current, sourceRevision) {
  if (!SHA2562.test(sourceRevision))
    return null;
  const store = TaskStore.forCurrent(root2, current);
  const manifest = store.manifest;
  if (!manifest || store.hasPendingCommit || manifest.head.source_revision !== current.sourceTuple.revision || sha2562(current.raw) !== current.sourceTuple.revision)
    return null;
  let revision = current.sourceTuple.revision;
  const preimages = [];
  const events = store.listEvents();
  for (let i = events.length - 1;i >= 0; i--) {
    const event = events[i];
    if (event.resulting_source_revision !== revision || event.event_type !== "storage-migration" || event.operation_kind !== "task-storage-migration" || !event.metadata.committed || event.metadata.status !== "success")
      return null;
    const raw = store.readHistoryMaterial(event.source_revision);
    if (raw === null || sha2562(raw) !== event.source_revision)
      return null;
    preimages.push(raw);
    if (event.source_revision === sourceRevision)
      return preimages;
    revision = event.source_revision;
  }
  return null;
}
function previewTaskStorageMigration(root2, current) {
  const store = TaskStore.forCurrent(root2, current);
  const manifest = store.manifest;
  const legacyInlineReviewPreimages = hasLegacyInlineReviewPreimages(current);
  const definitionRevision = taskStoreDefinitionRevision(current);
  const stateRevision = taskStoreStateRevision(current);
  return {
    status: manifest?.current_representation === ACTIVE_TASK_FORMAT && !legacyInlineReviewPreimages ? "already-migrated" : "preview",
    operation_kind: "task-storage-migration",
    document_id: current.sourceTuple.document_id,
    source_revision: current.sourceTuple.revision,
    current_task_path: current.relativePath,
    manifest_path: `${store.paths.relativeRoot}/${TASK_STORE_MANIFEST_FILE}`,
    legacy_bytes: Buffer.byteLength(current.raw),
    projected_bytes: Buffer.byteLength(compactCurrentRaw(current, store).content),
    definition_revision: definitionRevision,
    state_revision: stateRevision,
    existing_store: manifest !== null,
    semantic_model_digest: digest({ task_id: current.sourceTuple.task_id, task_slug: current.sourceTuple.task_slug, workflow_status: current.sourceTuple.workflow_status, lifecycle_state: current.sourceTuple.lifecycle_state, active_step_id: current.sourceTuple.active_step_id, definition_revision: definitionRevision, state_revision: stateRevision }),
    planned_objects: ["definition", "state", "other", "evidence-report", "review-receipt", "legacy-current-task"],
    planned_events: manifest?.current_representation === ACTIVE_TASK_FORMAT && !legacyInlineReviewPreimages ? [] : [manifest ? "storage-migration" : "legacy-import", "storage-migration"]
  };
}
function commitTaskStorageMigration(root2, current, sourceRevision) {
  const store = TaskStore.forCurrent(root2, current);
  const existing = store.manifest;
  if (sourceRevision !== current.sourceTuple.revision) {
    const previous = SHA2562.test(sourceRevision) ? store.lookupIdempotency(`task-storage-migration-v3-${current.sourceTuple.document_id}-${sourceRevision.slice(0, 16)}`) : null;
    if (existing?.current_representation === ACTIVE_TASK_FORMAT && existing.head.source_revision === current.sourceTuple.revision && previous?.source_revision === sourceRevision && previous.event.event_type === "storage-migration" && previous.event.resulting_source_revision === current.sourceTuple.revision) {
      return { status: "no-op", operation_kind: "task-storage-migration", source_revision: sourceRevision, manifest: existing };
    }
    throw new TaskStoreError("TASK_STORE_MIGRATION_SOURCE_STALE", "migration source_revision does not match the exact current CURRENT_TASK bytes.");
  }
  const legacyInlineReviewPreimages = hasLegacyInlineReviewPreimages(current);
  if (existing?.current_representation === ACTIVE_TASK_FORMAT && !legacyInlineReviewPreimages) {
    if (existing.head.source_revision !== current.sourceTuple.revision)
      throw new TaskStoreError("TASK_STORE_SOURCE_CONFLICT", "compact task-store manifest does not match the requested migration source.");
    return { status: "no-op", operation_kind: "task-storage-migration", source_revision: sourceRevision, manifest: existing };
  }
  const coverage = record2(current.runtimeState.review_coverage) ? current.runtimeState.review_coverage : null;
  if (coverage && Array.isArray(coverage.preimages))
    persistLegacyReviewPreimages(root2, coverage.preimages);
  store.ensureInitialized(current);
  const prepared = compactCurrentRaw(current, store);
  const compactRaw = prepared.content;
  const after = compactAfter(current, prepared);
  if (digest(migrationSemanticModel(current)) !== digest(migrationSemanticModel(after))) {
    throw new TaskStoreError("TASK_STORE_SOURCE_CONFLICT", "storage migration changed the normalized task model outside its representation boundary.");
  }
  const proposal = {
    schema_version: 1,
    kind: "vnext-storage-migration-proposal",
    operation_kind: "task-storage-migration",
    idempotency_key: `task-storage-migration-v3-${current.sourceTuple.document_id}-${sourceRevision.slice(0, 16)}`,
    source_revision: sourceRevision
  };
  const result = { status: "success", committed: true, operation_kind: "task-storage-migration", idempotency_key: proposal.idempotency_key };
  persistTaskProjection(prepared, current.filePath, current.relativePath);
  store.stageCommit({ before: current, after, after_source_revision: after.sourceTuple.revision, proposal, result, write_targets: [current.relativePath] });
  atomicWrite(current.filePath, compactRaw);
  store.markCurrentPublished(after.sourceTuple.revision);
  const manifest = store.recordCommit({ before: current, after, proposal, result });
  if (!manifest || manifest.current_representation !== ACTIVE_TASK_FORMAT || manifest.head.source_revision !== after.sourceTuple.revision) {
    throw new TaskStoreError("TASK_STORE_SOURCE_CONFLICT", "compact migration did not publish a matching aggregate head.");
  }
  return { status: "committed", operation_kind: "task-storage-migration", source_revision: sourceRevision, manifest };
}

// runtime/vnext/src/task-storage-metrics.ts
var isRecord2 = (v) => !!v && typeof v === "object" && !Array.isArray(v);
var omitted = (v, keys) => Object.fromEntries(Object.entries(v).filter(([k]) => !keys.includes(k)));
var bytes = (v) => v == null ? 0 : Buffer.byteLength(stableJson(v), "utf8");
var LIMITS = { files: 4096, events: 1024, objects: 512, read_bytes: 8 * 1024 * 1024 };
var LOGICAL_KEYS = ["definition_bytes", "claim_evidence_bytes", "pending_review_bytes", "execution_hot_state_bytes", "logical_state_bytes"];
var nullLogical = () => Object.fromEntries(LOGICAL_KEYS.map((k) => [k, null]));
function unavailable(code) {
  throw new Error(code);
}
function codeOf(error) {
  const code = error instanceof Error ? error.message.split(":", 1)[0] : "";
  return /^[A-Z][A-Z0-9_]+$/u.test(code) ? code : "MEASUREMENT_UNAVAILABLE";
}
function logicalSizes(current) {
  const runtimeState = validateVNextRuntimeState(omitted(current.runtimeState, ["execution_log", "applied_proposals"]), { storeBackedHistory: true });
  const normalizedCurrent = { ...current, runtimeState };
  const normalized = migrationSemanticModel(normalizedCurrent);
  const runtime = normalized.runtime_state;
  return {
    definition_bytes: bytes(taskStoreDefinitionPayload(normalizedCurrent, "task-definition/v2")),
    claim_evidence_bytes: bytes(runtime.claim_evidence),
    pending_review_bytes: bytes(runtime.pending_review_result),
    execution_hot_state_bytes: bytes(omitted(runtime, ["claim_evidence", "pending_review_result"])),
    logical_state_bytes: bytes(runtime)
  };
}
function taskStorageMetrics(rootInput, current) {
  const result = {
    kind: "task-storage-metrics/v1",
    read_only: true,
    diagnostic_only: true,
    status: "complete",
    unit: "byte",
    source_revision: current.sourceTuple.revision,
    document_id: current.sourceTuple.document_id,
    representation: String(current.frontmatter?.task_store?.format ?? "legacy-inline"),
    logical_encoding: "stable-json-utf8/v1",
    logical_fields_overlap: true,
    active_projection_bytes: Buffer.byteLength(current.raw, "utf8"),
    ...nullLogical(),
    task_basis_bytes: null,
    aggregate_total_bytes: null,
    external_history_bytes: null,
    committed_material_bytes: null,
    physical_breakdown: null,
    previous_transaction_delta: {
      status: "unavailable",
      reason: "NO_PREVIOUS_EVENT",
      event_sequence: null,
      previous_event_sequence: null,
      event_type: null,
      operation_kind: null,
      action: null,
      recorded_at: null,
      from_source_revision: null,
      to_source_revision: current.sourceTuple.revision,
      active_projection_bytes: null,
      task_basis_bytes: null,
      ...nullLogical(),
      committed_material_bytes: null,
      aggregate_total_bytes: null
    },
    coverage: {
      event_history: "unavailable",
      physical_inventory: "unavailable",
      limits: { ...LIMITS },
      notes: ["LOGICAL_FIELDS_OVERLAP_DO_NOT_SUM", "HISTORICAL_FILESYSTEM_TOTAL_NOT_RETAINED"]
    }
  };
  const note = (message) => {
    if (!result.coverage.notes.includes(message))
      result.coverage.notes.push(message);
    result.status = "partial";
  };
  const attempt = (read) => {
    try {
      return read();
    } catch (error) {
      note(codeOf(error));
      return null;
    }
  };
  const logical = attempt(() => logicalSizes(current));
  if (logical)
    Object.assign(result, logical);
  const root2 = path5.resolve(rootInput);
  let readBytes = 0;
  let objectReads = 0;
  const safeStat = (file) => {
    const relative = path5.relative(root2, file);
    if (relative.split(path5.sep).includes("..") || path5.isAbsolute(relative))
      return unavailable("METRICS_UNSAFE_PATH");
    let cursor = root2;
    for (const part of relative.split(path5.sep).filter(Boolean)) {
      cursor = path5.join(cursor, part);
      const stat = fs5.lstatSync(cursor);
      if (stat.isSymbolicLink())
        return unavailable("METRICS_SYMLINK");
    }
    const stat = fs5.lstatSync(file);
    if (!stat.isFile())
      return unavailable("METRICS_NOT_REGULAR_FILE");
    return stat;
  };
  const chargeRead = (file) => {
    readBytes += safeStat(file).size;
    if (readBytes > LIMITS.read_bytes)
      unavailable("METRICS_READ_BUDGET");
  };
  const basis = attempt(() => ({ reference: readTaskBasisReferenceFromBody(current.body) }))?.reference;
  result.task_basis_bytes = basis === undefined ? null : basis === null ? 0 : attempt(() => {
    const file = path5.resolve(root2, basis.path.replace(/\\/gu, "/"));
    chargeRead(file);
    const content = fs5.readFileSync(file);
    if (sha2562(content) !== basis.revision)
      return unavailable("METRICS_BASIS_REVISION_MISMATCH");
    return content.length;
  });
  let verifySample;
  attempt(() => {
    const store = TaskStore.forCurrent(root2, current);
    const readManifest = () => {
      if (!fs5.existsSync(store.paths.manifest))
        return null;
      safeStat(store.paths.manifest);
      return fs5.readFileSync(store.paths.manifest, "utf8");
    };
    const manifestBytes = readManifest();
    const sourceStat = safeStat(current.filePath);
    const signature = (s) => `${s.dev}:${s.ino}:${s.size}:${s.mtimeMs}:${s.ctimeMs}`;
    verifySample = () => {
      if (store.hasPendingCommit || readManifest() !== manifestBytes || signature(safeStat(current.filePath)) !== signature(sourceStat))
        unavailable("METRICS_SAMPLE_CHANGED");
    };
    chargeRead(current.filePath);
    if (sha2562(fs5.readFileSync(current.filePath)) !== current.sourceTuple.revision)
      return unavailable("METRICS_SAMPLE_CHANGED");
    const manifest = store.manifest;
    verifySample();
    if (!manifest) {
      note("NO_COMMITTED_AGGREGATE");
      return;
    }
    if (manifest.head.source_revision !== current.sourceTuple.revision)
      return unavailable("METRICS_SAMPLE_CHANGED");
    const objects = new Map;
    const getObject = (ref) => {
      if (!ref || !/^[a-f0-9]{64}$/u.test(ref.sha256))
        return unavailable("METRICS_REFERENCE_INVALID");
      let object = objects.get(ref.sha256);
      if (!object) {
        if (++objectReads > LIMITS.objects)
          return unavailable("METRICS_OBJECT_BUDGET");
        chargeRead(path5.join(store.paths.objects, `${ref.sha256}.json`));
        object = store.readObject(ref, ref.object_type);
        objects.set(ref.sha256, object);
      }
      return object;
    };
    const eventFile = (event) => path5.join(store.paths.events, `${String(event.sequence).padStart(12, "0")}-${event.event_hash}.json`);
    const getEvent = (sequence, event_hash) => {
      chargeRead(eventFile({ sequence, event_hash }));
      return store.readEvent({ sequence, event_hash });
    };
    const inventory = attempt(() => {
      const sizes = new Map;
      let visits = 0;
      const walk = (directory) => {
        const dir = fs5.opendirSync(directory);
        try {
          for (let entry = dir.readSync();entry !== null; entry = dir.readSync()) {
            if (++visits > LIMITS.files)
              return unavailable("METRICS_INVENTORY_BUDGET");
            const file = path5.join(directory, entry.name);
            if (entry.isSymbolicLink())
              return unavailable("METRICS_SYMLINK");
            if (entry.isDirectory())
              walk(file);
            else
              sizes.set(path5.relative(store.paths.directory, file).split(path5.sep).join("/"), safeStat(file).size);
          }
        } finally {
          dir.closeSync();
        }
      };
      walk(store.paths.directory);
      const group = { objects_bytes: 0, events_bytes: 0, indexes_bytes: 0, manifest_bytes: 0, other_bytes: 0, files: sizes.size };
      for (const [name, size] of sizes) {
        const key = name.startsWith("objects/") ? "objects_bytes" : name.startsWith("events/") ? "events_bytes" : name.startsWith("indexes/") ? "indexes_bytes" : name === "manifest.json" ? "manifest_bytes" : "other_bytes";
        group[key] += size;
      }
      result.physical_breakdown = group;
      result.aggregate_total_bytes = group.objects_bytes + group.events_bytes + group.indexes_bytes + group.manifest_bytes + group.other_bytes;
      result.coverage.physical_inventory = "complete";
      return sizes;
    });
    const events = [];
    attempt(() => {
      if (manifest.head.event_sequence && manifest.head.event_hash) {
        const head = getEvent(manifest.head.event_sequence, manifest.head.event_hash);
        events.push(head);
        if (head.sequence > 1 && head.previous_event_hash)
          events.push(getEvent(head.sequence - 1, head.previous_event_hash));
      }
    });
    const finishHistory = () => {
      if (result.coverage.event_history === "complete")
        return;
      let sequence = events.length ? events[events.length - 1].sequence - 1 : manifest.head.event_sequence;
      let hash = events.length ? events[events.length - 1].previous_event_hash : manifest.head.event_hash;
      while (sequence > 0 && hash) {
        if (events.length >= LIMITS.events)
          return unavailable("METRICS_EVENT_BUDGET");
        const event = getEvent(sequence, hash);
        events.push(event);
        sequence--;
        hash = event.previous_event_hash;
      }
      if (sequence !== 0 || hash !== null)
        return unavailable("METRICS_EVENT_CHAIN_INCOMPLETE");
      result.coverage.event_history = "complete";
    };
    const head = events[0];
    const previousEvent = events[1];
    const delta = result.previous_transaction_delta;
    if (!head && manifest.head.event_sequence > 0)
      delta.reason = "HEAD_EVENT_UNAVAILABLE";
    else if (head && head.sequence > 1 && !previousEvent)
      delta.reason = "PREVIOUS_EVENT_UNAVAILABLE";
    if (head) {
      Object.assign(delta, {
        event_sequence: head.sequence,
        previous_event_sequence: previousEvent?.sequence ?? null,
        event_type: head.event_type,
        operation_kind: head.operation_kind,
        recorded_at: head.metadata.recorded_at,
        from_source_revision: head.source_revision
      });
      attempt(() => {
        const ref = head.object_refs.proposal;
        const value = ref && typeof ref !== "string" ? getObject(ref).payload : head.transaction?.proposal;
        delta.action = typeof value?.semantic_delta?.action === "string" ? value.semantic_delta.action : null;
      });
    }
    const currentRefs = new Set;
    const resolveV3 = (definitionRef, stateRef, sourceRevision, visited) => {
      const payload = (ref) => {
        visited.add(ref.sha256);
        return getObject(ref).payload;
      };
      const value = (v) => {
        if (!isRecord2(v))
          return unavailable("METRICS_HISTORICAL_SHAPE_UNSUPPORTED");
        if ("value" in v)
          return v.value;
        const resolved = payload(v.ref);
        return v.ref.object_type === "other" ? resolved.value : resolved;
      };
      const definition = payload(definitionRef);
      const state = payload(stateRef);
      if (definition.kind !== "vnext-active-definition/v1" || state.kind !== "vnext-active-state/v1" || stableJson(state.definition) !== stableJson(definitionRef))
        return unavailable("METRICS_HISTORICAL_SHAPE_UNSUPPORTED");
      const runtime = Object.fromEntries(Object.entries(state.runtime).map(([k, v]) => [k, value(v)]));
      if (Array.isArray(definition.claim_definitions))
        runtime.claim_evidence = definition.claim_definitions.map((v, i) => {
          const claim = value(v);
          const progress = state.claim_states[i];
          if (claim.claim_id !== progress?.claim_id || claim.slots.length !== progress.slots.length)
            return unavailable("METRICS_HISTORICAL_SHAPE_UNSUPPORTED");
          return { ...claim, slots: claim.slots.map((slot, j) => {
            if (slot.slot_id !== progress.slots[j]?.slot_id)
              return unavailable("METRICS_HISTORICAL_SHAPE_UNSUPPORTED");
            return { ...slot, ...Object.fromEntries(Object.entries(progress.slots[j].fields).map(([k, v]) => [k, value(v)])) };
          }) };
        });
      const body = definition.sections.map((section) => ("dynamic" in section) ? value(state.dynamic_body[section.dynamic]) : value(section.text)).join("");
      const binding = {
        schema_version: 1,
        kind: "vnext-current-task-store-binding",
        format: "compact-v3",
        manifest_path: `${store.paths.relativeRoot}/manifest.json`,
        history: { execution_log: "task-store", applied_proposals: "task-store" },
        projection: { definition: definitionRef, state: stateRef }
      };
      const frontmatter = { ...definition.frontmatter, task_store: binding, runtime_state: runtime };
      return {
        ...current,
        body,
        frontmatter,
        runtimeState: runtime,
        raw: "",
        sourceTuple: { ...current.sourceTuple, revision: sourceRevision, task_id: runtime.task_id, task_slug: runtime.task_slug }
      };
    };
    const currentClosure = result.representation === "compact-v3" ? attempt(() => resolveV3(manifest.object_refs.definition, manifest.object_refs.state, current.sourceTuple.revision, currentRefs)) : null;
    if (head && previousEvent && previousEvent.resulting_source_revision === head.source_revision) {
      const before = attempt(() => {
        const ref = head.object_refs["legacy-locator-alias"] ?? (previousEvent.sequence === 1 ? manifest.object_refs.legacy_source : undefined);
        if (ref && typeof ref !== "string") {
          const p = getObject(ref).payload;
          if (p.source_revision === head.source_revision && typeof p.raw_base64 === "string") {
            const raw = Buffer.from(p.raw_base64, "base64").toString("utf8");
            if (sha2562(raw) !== head.source_revision)
              return unavailable("METRICS_HISTORY_HASH_MISMATCH");
            const m = /^---\r?\n([\s\S]*?)\r?\n---(?:\r?\n|$)/u.exec(raw);
            if (!m)
              return unavailable("METRICS_HISTORICAL_SHAPE_UNSUPPORTED");
            const fm = parse4(m[1]);
            if (fm.document_id !== current.sourceTuple.document_id)
              return unavailable("METRICS_HISTORY_IDENTITY_MISMATCH");
            if (fm.task_store?.format !== "compact-v3")
              return {
                ...current,
                raw,
                frontmatter: fm,
                body: raw.slice(m[0].length),
                runtimeState: fm.runtime_state,
                sourceTuple: { ...current.sourceTuple, revision: head.source_revision }
              };
            const restored = resolveV3(previousEvent.object_refs.definition, previousEvent.object_refs.state, head.source_revision, new Set);
            if (stableJson(fm.task_store.projection) !== stableJson(restored.frontmatter.task_store.projection))
              return unavailable("METRICS_HISTORY_IDENTITY_MISMATCH");
            return { ...restored, raw };
          }
        }
        return resolveV3(previousEvent.object_refs.definition, previousEvent.object_refs.state, head.source_revision, new Set);
      });
      if (before && logical) {
        const old = logicalSizes(before);
        for (const key of LOGICAL_KEYS)
          delta[key] = logical[key] - old[key];
        let oldRaw = before.raw;
        if (!oldRaw)
          attempt(() => {
            const expanded = `---
${stringify3(before.frontmatter).trimEnd()}
---
${before.body}`;
            const candidate = prepareTaskProjection(expanded).content;
            if (sha2562(candidate) === head.source_revision)
              oldRaw = candidate;
          });
        if (oldRaw)
          delta.active_projection_bytes = result.active_projection_bytes - Buffer.byteLength(oldRaw, "utf8");
        const priorBasis = readTaskBasisReferenceFromBody(before.body);
        if (basis !== undefined && stableJson(priorBasis) === stableJson(basis) && result.task_basis_bytes !== null)
          delta.task_basis_bytes = 0;
        else if (priorBasis === null && result.task_basis_bytes !== null)
          delta.task_basis_bytes = result.task_basis_bytes;
        else if (priorBasis && result.task_basis_bytes !== null)
          attempt(() => {
            attempt(finishHistory);
            for (const event of events.slice(1))
              for (const ref of Object.values(event.object_refs)) {
                if (!ref || typeof ref === "string" || ref.object_type !== "task-basis")
                  continue;
                const p = getObject(ref).payload;
                if (p.path !== priorBasis.path || p.basis_revision !== priorBasis.revision || typeof p.raw_base64 !== "string")
                  continue;
                const raw = Buffer.from(p.raw_base64, "base64");
                if (sha2562(raw) === priorBasis.revision) {
                  delta.task_basis_bytes = result.task_basis_bytes - raw.length;
                  return;
                }
              }
          });
        delta.status = delta.active_projection_bytes !== null && delta.task_basis_bytes !== null ? "available" : "partial";
        delta.reason = delta.status === "available" ? null : "HISTORICAL_PHYSICAL_BYTES_UNAVAILABLE";
      } else
        delta.reason = "HISTORICAL_LOGICAL_MATERIAL_UNAVAILABLE";
    } else if (head && previousEvent)
      delta.reason = "EVENT_SOURCE_DISCONTINUITY";
    attempt(finishHistory);
    if (result.coverage.event_history !== "complete" && events.length)
      result.coverage.event_history = "partial";
    if (inventory && result.coverage.event_history === "complete")
      attempt(() => {
        const known = new Set;
        let committed = 0;
        let beforeHead = 0;
        const sizeOf = (name) => inventory.get(name) ?? unavailable("METRICS_COMMITTED_FILE_MISSING");
        for (const event of [...events].reverse()) {
          beforeHead = committed;
          for (const ref of Object.values(event.object_refs)) {
            const hash = typeof ref === "string" ? ref : ref?.sha256;
            if (hash && !known.has(hash)) {
              known.add(hash);
              committed += sizeOf(`objects/${hash}.json`);
            }
          }
          const length = sizeOf(`events/${path5.basename(eventFile(event))}`);
          committed += length;
        }
        result.committed_material_bytes = committed;
        if (previousEvent)
          delta.committed_material_bytes = committed - beforeHead;
        if (currentClosure && currentRefs.size) {
          let active = 0;
          for (const hash of currentRefs) {
            if (!known.has(hash))
              return unavailable("METRICS_CURRENT_MATERIAL_NOT_COMMITTED");
            active += sizeOf(`objects/${hash}.json`);
          }
          result.external_history_bytes = committed - active;
        } else
          note("LEGACY_CURRENT_MATERIAL_CLOSURE_UNAVAILABLE");
      });
  });
  if (verifySample)
    try {
      verifySample();
    } catch (error) {
      note(codeOf(error));
      note("METRICS_SAMPLE_CHANGED");
    }
  const delta = result.previous_transaction_delta;
  if (delta.previous_event_sequence !== null) {
    const fields = [...LOGICAL_KEYS, "active_projection_bytes", "task_basis_bytes", "committed_material_bytes"];
    const available = fields.filter((key) => delta[key] !== null).length;
    delta.status = available === fields.length ? "available" : available ? "partial" : "unavailable";
    if (delta.status === "available")
      delta.reason = null;
    else if (delta.reason === null)
      delta.reason = "COMMITTED_MATERIAL_COVERAGE_INCOMPLETE";
  }
  if (result.coverage.event_history !== "complete" || result.coverage.physical_inventory !== "complete" || result.previous_transaction_delta.reason !== "NO_PREVIOUS_EVENT" && result.previous_transaction_delta.status !== "available")
    result.status = "partial";
  if (result.coverage.notes.includes("METRICS_SAMPLE_CHANGED")) {
    result.status = "unavailable";
    Object.assign(result, nullLogical());
    result.task_basis_bytes = result.aggregate_total_bytes = result.external_history_bytes = result.committed_material_bytes = null;
    result.coverage.event_history = result.coverage.physical_inventory = "unavailable";
    result.physical_breakdown = null;
    result.previous_transaction_delta = {
      ...result.previous_transaction_delta,
      status: "unavailable",
      reason: "METRICS_SAMPLE_CHANGED",
      ...nullLogical(),
      active_projection_bytes: null,
      task_basis_bytes: null,
      committed_material_bytes: null
    };
  }
  return result;
}

// runtime/vnext/src/task-recovery.ts
var TASK_RECOVERY_PROTOCOL = {
  candidate: "correction-replan-candidate/v2",
  confirmation: "correction-replan-candidate-receipt/v2",
  modes: ["conclusion-correction", "execution-recovery"],
  strategies: ["forward-fix", "artifact-restore", "mixed"],
  routing: ["unrelated", "conclusion-counterevidence", "execution-error", "authority-change"],
  target_limit: 16,
  recovery_step_limit: 16,
  source: "runtime-task-document-source-tuple-basis-and-plan",
  obligations: "all-old-claim-slots-and-replaced-step-obligations",
  history: "immutable-definitions-executions-and-content-addressed-evidence",
  evidence: "evidence-carry-forward/v2-original-anchor-and-current-reception",
  artifact_checkpoint: "artifact-checkpoint/v1",
  artifact_completion: "artifact-restore-completion/v1-execution-v2-revalidation-and-exact-targets",
  artifact_publication: "exact-write-set-journal-rollback-or-fail-closed",
  concurrency: "shared-governance-write-lock-and-source-cas",
  authority: "caller-reported-no-permission-expansion",
  legacy: "readable-explicit-preservation-old-receipts-rejected"
};

// runtime/vnext/src/evidence-lineage.ts
import * as crypto4 from "crypto";
import * as fs6 from "fs";
import * as path6 from "path";
var MAX_OBJECT_BYTES = 1024 * 1024;
var MAX_SNAPSHOT_BYTES = 8 * MAX_OBJECT_BYTES;
function hash(bytes) {
  return crypto4.createHash("sha256").update(bytes).digest("hex");
}
function safeRepositoryFile(root2, relative) {
  if (!relative || relative.includes("\\") || relative.startsWith("/") || relative.includes(":") || relative.split("/").some((part) => !part || part === "." || part === ".." || part.toLowerCase() === ".git" || /[. ]$/.test(part))) {
    throw new Error("EVIDENCE_OBJECT_PATH_INVALID: expected a bounded repository-relative file.");
  }
  let cursor = path6.resolve(root2);
  if (fs6.lstatSync(cursor).isSymbolicLink())
    throw new Error("EVIDENCE_OBJECT_PATH_INVALID: symbolic root.");
  for (const part of relative.split("/")) {
    cursor = path6.join(cursor, part);
    try {
      if (fs6.lstatSync(cursor).isSymbolicLink())
        throw new Error("EVIDENCE_OBJECT_PATH_INVALID: symbolic paths are unsupported.");
    } catch (error) {
      if (error.code !== "ENOENT")
        throw error;
    }
  }
  return cursor;
}
function readBounded(file) {
  const stat = fs6.lstatSync(file);
  if (!stat.isFile() || stat.size > MAX_OBJECT_BYTES)
    throw new Error("EVIDENCE_OBJECT_UNSUPPORTED: requires a regular file of at most 1 MiB.");
  const bytes = fs6.readFileSync(file);
  if (bytes.length > MAX_OBJECT_BYTES)
    throw new Error("EVIDENCE_OBJECT_UNSUPPORTED: file exceeded the limit while reading.");
  return bytes;
}
function describeEvidenceObjects(root2, paths) {
  const unique = [...new Set(paths.map((item) => item.split("#")[0]))].sort();
  if (unique.length > 128)
    throw new Error("EVIDENCE_OBJECT_BUDGET_EXHAUSTED: too many snapshot files.");
  let total = 0;
  return unique.map((relative) => {
    const bytes = readBounded(safeRepositoryFile(root2, relative));
    total += bytes.length;
    if (total > MAX_SNAPSHOT_BYTES)
      throw new Error("EVIDENCE_OBJECT_BUDGET_EXHAUSTED: snapshot exceeds 8 MiB.");
    return { path: relative, sha256: hash(bytes), size: bytes.length };
  });
}
function objectFile2(root2, currentPath, sha) {
  if (!/^[a-f0-9]{64}$/.test(sha))
    throw new Error("EVIDENCE_OBJECT_INVALID: invalid digest.");
  const relative = path6.relative(root2, path6.join(path6.dirname(currentPath), "evidence-objects", `${sha}.blob`)).replace(/\\/g, "/");
  return safeRepositoryFile(root2, relative);
}
function readEvidenceObject(root2, currentPath, object) {
  verifyEvidenceObject(root2, currentPath, object);
  return readBounded(objectFile2(root2, currentPath, object.sha256));
}
function preserveEvidenceObjects(root2, currentPath, objects) {
  for (const object of objects) {
    const bytes = readBounded(safeRepositoryFile(root2, object.path));
    if (hash(bytes) !== object.sha256 || bytes.length !== object.size)
      throw new Error("EVIDENCE_OBJECT_STALE: source changed before preservation.");
    const file = objectFile2(root2, currentPath, object.sha256);
    fs6.mkdirSync(path6.dirname(file), { recursive: true });
    if (!fs6.existsSync(file)) {
      const fd = fs6.openSync(file, "wx");
      try {
        fs6.writeFileSync(fd, bytes);
        fs6.fsyncSync(fd);
      } finally {
        fs6.closeSync(fd);
      }
    }
    verifyEvidenceObject(root2, currentPath, object);
  }
}
function verifyEvidenceObject(root2, currentPath, object) {
  const bytes = readBounded(objectFile2(root2, currentPath, object.sha256));
  if (hash(bytes) !== object.sha256 || bytes.length !== object.size)
    throw new Error("EVIDENCE_OBJECT_CORRUPT: immutable evidence object changed.");
}
function ingestEvidenceText(root2, currentPath, input, dryRun) {
  const bytes = Buffer.from(input.body, "utf8");
  if (!bytes.length || bytes.length > MAX_OBJECT_BYTES || input.source_locator.length > 2048)
    throw new Error("EVIDENCE_OBJECT_UNSUPPORTED: bounded evidence body and source locator required.");
  const sha = hash(bytes);
  const file = objectFile2(root2, currentPath, sha);
  const provenance = JSON.stringify({ kind: "ingested-evidence/v1", ...input, body: undefined, evidence_sha256: sha, assurance: "caller-reported" }) + `
`;
  const metadata = objectFile2(root2, currentPath, hash(Buffer.from(provenance)));
  if (!dryRun) {
    fs6.mkdirSync(path6.dirname(file), { recursive: true });
    for (const [target, content] of [[file, bytes], [metadata, Buffer.from(provenance)]]) {
      if (!fs6.existsSync(target)) {
        const fd = fs6.openSync(target, "wx");
        try {
          fs6.writeFileSync(fd, content);
          fs6.fsyncSync(fd);
        } finally {
          fs6.closeSync(fd);
        }
      }
      if (!fs6.readFileSync(target).equals(content))
        throw new Error("EVIDENCE_OBJECT_CORRUPT: stored evidence differs.");
    }
  }
  return { evidence_ref: path6.relative(root2, file).replace(/\\/g, "/"), evidence_sha256: sha, provenance_ref: path6.relative(root2, metadata).replace(/\\/g, "/") };
}

// runtime/vnext/src/artifact-checkpoints.ts
import * as crypto5 from "crypto";
import * as fs7 from "fs";
import * as path7 from "path";
function digest2(value) {
  function canonical(item) {
    if (Array.isArray(item))
      return item.map(canonical);
    if (item && typeof item === "object")
      return Object.fromEntries(Object.entries(item).sort(([a], [b]) => a < b ? -1 : a > b ? 1 : 0).map(([key, value]) => [key, canonical(value)]));
    return item;
  }
  return crypto5.createHash("sha256").update(JSON.stringify(canonical(value))).digest("hex");
}
function assertProductPath(root2, currentPath, relative) {
  const absolute = safeRepositoryFile(root2, relative);
  const home = path7.dirname(currentPath);
  const local = path7.relative(home, absolute).replace(/\\/g, "/");
  if (relative.includes("#") || relative.toLowerCase() === ".gitmodules" || /^(TASKS|\.agents\/skills|\.claude\/skills|\.codex\/skills)(\/|$)/i.test(relative) || /^\.workflow-system(\/|$)/i.test(relative) || path7.resolve(home) !== path7.resolve(root2) && local !== ".." && !local.startsWith("../") || !local.startsWith("../") && (/^(CURRENT_TASK|TASK_BASIS|STATUS|CONTRACTS|DECISIONS|LESSONS|PROJECT_PROFILE)([._/]|$)/i.test(local) || /^(task-history|task-candidates|evidence-objects|archive|archives|\.vnext)/i.test(local))) {
    throw new Error("ARTIFACT_RESTORE_FORBIDDEN: governance and Runtime installation files cannot be restored.");
  }
  let parent = path7.dirname(absolute);
  while (parent !== path7.resolve(root2)) {
    if (fs7.existsSync(path7.join(parent, ".git")))
      throw new Error("ARTIFACT_UNSUPPORTED: submodule files cannot be restored.");
    parent = path7.dirname(parent);
  }
  for (const registry of ["FREEZE_REGISTRY.md", ".workflow-system/FREEZE_REGISTRY.md"]) {
    const file = path7.join(root2, registry);
    if (fs7.existsSync(file) && fs7.readFileSync(file, "utf8").split(/\r?\n/).some((line) => line.includes(relative) && !/^\s*[-#]*\s*(unfreeze|not frozen)/i.test(line)))
      throw new Error("ARTIFACT_FROZEN: path is in the freeze registry.");
  }
  if (fs7.existsSync(absolute)) {
    const stat = fs7.lstatSync(absolute);
    if (!stat.isFile() || stat.size > 1048576)
      throw new Error("ARTIFACT_UNSUPPORTED: only bounded regular files can be snapshotted.");
    const content = fs7.readFileSync(absolute);
    if (content.includes(0) || !Buffer.from(content.toString("utf8")).equals(content))
      throw new Error("ARTIFACT_UNSUPPORTED: binary baseline cannot be verified.");
    if (/@frozen|DO NOT MODIFY/i.test(content.subarray(0, 8192).toString("utf8")))
      throw new Error("ARTIFACT_FROZEN: file has a freeze marker.");
  }
  return absolute;
}
function captureArtifactImages(root2, currentPath, paths) {
  const unique = [...new Set(paths)].sort();
  if (unique.length > 128)
    throw new Error("ARTIFACT_BUDGET_EXHAUSTED: at most 128 paths.");
  const present = unique.filter((p) => fs7.existsSync(assertProductPath(root2, currentPath, p)));
  const objects = describeEvidenceObjects(root2, present);
  return unique.map((p) => ({ path: p, object: objects.find((object) => object.path === p) ?? null }));
}
function checkpointFile(root2, currentPath, documentId, id) {
  if (!/^doc-[a-f0-9]+$/.test(documentId) || !/^[a-f0-9]{64}$/.test(id))
    throw new Error("ARTIFACT_CHECKPOINT_INVALID: invalid identity.");
  return safeRepositoryFile(root2, path7.relative(root2, path7.join(path7.dirname(currentPath), "task-history", documentId, "artifact-checkpoints", `${id}.json`)).replace(/\\/g, "/"));
}
function saveArtifactCheckpoint(root2, currentPath, checkpoint, paths, dryRun = false) {
  const images = captureArtifactImages(root2, currentPath, paths);
  const payload = { kind: "artifact-checkpoint/v1", ...checkpoint, images };
  const id = digest2(payload);
  if (dryRun)
    return id;
  preserveEvidenceObjects(root2, currentPath, images.flatMap((image) => image.object ? [image.object] : []));
  const file = checkpointFile(root2, currentPath, checkpoint.document_id, id);
  fs7.mkdirSync(path7.dirname(file), { recursive: true });
  const bytes = JSON.stringify(payload) + `
`;
  if (!fs7.existsSync(file)) {
    const fd = fs7.openSync(file, "wx");
    try {
      fs7.writeFileSync(fd, bytes);
      fs7.fsyncSync(fd);
    } finally {
      fs7.closeSync(fd);
    }
  }
  if (fs7.readFileSync(file, "utf8") !== bytes)
    throw new Error("ARTIFACT_CHECKPOINT_CORRUPT: immutable checkpoint changed.");
  return id;
}
function prepareArtifactRestore(root2, currentPath, taskId, documentId, checkpointId, paths) {
  const file = checkpointFile(root2, currentPath, documentId, checkpointId);
  const checkpoint = JSON.parse(fs7.readFileSync(file, "utf8"));
  if (digest2(checkpoint) !== checkpointId || checkpoint.kind !== "artifact-checkpoint/v1" || checkpoint.task_id !== taskId || checkpoint.document_id !== documentId || !["before", "after"].includes(checkpoint.phase))
    throw new Error("ARTIFACT_CHECKPOINT_INVALID: checkpoint identity or bytes differ.");
  if (!paths.length || new Set(paths).size !== paths.length)
    throw new Error("ARTIFACT_RESTORE_INVALID: exact nonempty unique paths required.");
  const targets = [...paths].sort().map((p) => {
    const target = checkpoint.images.find((image) => image.path === p);
    if (!target)
      throw new Error("ARTIFACT_BASELINE_MISSING: path was not captured at this checkpoint.");
    assertProductPath(root2, currentPath, p);
    if (target.object)
      readEvidenceObject(root2, currentPath, target.object);
    return target;
  });
  return { checkpoint_id: checkpointId, checkpoint, expected: captureArtifactImages(root2, currentPath, paths), targets };
}
function assertNoArtifactPublication(currentPath) {
  if (fs7.existsSync(path7.join(path7.dirname(currentPath), ".vnext-artifact-restore.lock")))
    throw new Error("ARTIFACT_RECOVERY_REQUIRED: interrupted restore is retained; ordinary execution is blocked.");
}
function publish(root2, currentPath, image) {
  const file = assertProductPath(root2, currentPath, image.path);
  if (!image.object) {
    if (fs7.existsSync(file))
      fs7.unlinkSync(file);
    return;
  }
  const bytes = readEvidenceObject(root2, currentPath, image.object);
  fs7.mkdirSync(path7.dirname(file), { recursive: true });
  const stage = `${file}.vnext-${crypto5.randomUUID()}.tmp`;
  const fd = fs7.openSync(stage, "wx");
  try {
    fs7.writeFileSync(fd, bytes);
    fs7.fsyncSync(fd);
  } finally {
    fs7.closeSync(fd);
  }
  try {
    fs7.renameSync(stage, file);
  } finally {
    if (fs7.existsSync(stage))
      fs7.unlinkSync(stage);
  }
}
function applyArtifactRestore(root2, currentPath, plan, afterPublish, recordCompletion) {
  assertNoArtifactPublication(currentPath);
  if (digest2(captureArtifactImages(root2, currentPath, plan.expected.map((image) => image.path))) !== digest2(plan.expected))
    throw new Error("ARTIFACT_RESTORE_STALE: current files differ; user changes were preserved.");
  preserveEvidenceObjects(root2, currentPath, plan.expected.flatMap((image) => image.object ? [image.object] : []));
  const lock = path7.join(path7.dirname(currentPath), ".vnext-artifact-restore.lock");
  const fd = fs7.openSync(lock, "wx");
  try {
    fs7.writeFileSync(fd, JSON.stringify({ kind: "artifact-restore-journal/v1", pid: process.pid, plan }) + `
`);
    fs7.fsyncSync(fd);
  } finally {
    fs7.closeSync(fd);
  }
  const applied = [];
  try {
    for (const target of plan.targets) {
      const before = plan.expected.find((image) => image.path === target.path);
      if (digest2(captureArtifactImages(root2, currentPath, [target.path])[0]) !== digest2(before))
        throw new Error("ARTIFACT_RESTORE_STALE: path changed during publication.");
      publish(root2, currentPath, target);
      applied.push(target);
      afterPublish?.();
    }
    if (digest2(captureArtifactImages(root2, currentPath, plan.targets.map((image) => image.path))) !== digest2(plan.targets))
      throw new Error("ARTIFACT_RESTORE_VERIFY_FAILED: restored files differ.");
    recordCompletion?.();
    fs7.unlinkSync(lock);
  } catch (error) {
    for (const target of applied.reverse()) {
      if (digest2(captureArtifactImages(root2, currentPath, [target.path])[0]) !== digest2(target))
        throw new Error("ARTIFACT_RECOVERY_REQUIRED: drift during rollback; journal retained.");
      publish(root2, currentPath, plan.expected.find((image) => image.path === target.path));
    }
    if (digest2(captureArtifactImages(root2, currentPath, plan.expected.map((image) => image.path))) === digest2(plan.expected))
      fs7.unlinkSync(lock);
    throw error;
  }
}

// runtime/vnext/src/kernel.ts
import * as crypto10 from "crypto";
import * as fs11 from "fs";
import * as path12 from "path";
import { parseDocument, stringify as stringify4 } from "yaml";

// runtime/vnext/src/task-evolution-io.ts
import * as crypto6 from "crypto";
import * as fs8 from "fs";
import * as path8 from "path";
function sha2563(value) {
  return crypto6.createHash("sha256").update(value, "utf8").digest("hex");
}
function lockPathFor(currentPath) {
  return path8.join(path8.dirname(currentPath), ".vnext-task-evolution.lock");
}
function assertSafeHistoryDirectory(currentPath, directory) {
  const workflowDirectory = path8.dirname(currentPath);
  const relative = path8.relative(workflowDirectory, directory);
  if (!relative || relative.startsWith("..") || path8.isAbsolute(relative)) {
    throw new Error("TASK_HISTORY_PATH_INVALID: history must stay below the canonical workflow directory.");
  }
  let cursor = workflowDirectory;
  for (const part of relative.split(path8.sep)) {
    cursor = path8.join(cursor, part);
    if (fs8.existsSync(cursor) && fs8.lstatSync(cursor).isSymbolicLink()) {
      throw new Error("TASK_HISTORY_PATH_INVALID: history path cannot traverse a symbolic link.");
    }
  }
}
function taskHistoryLocation(input) {
  if (!/^doc-[a-f0-9]+$/u.test(input.documentId))
    throw new Error("TASK_HISTORY_IDENTITY_INVALID: document ID is invalid.");
  const revision = sha2563(input.previousContent);
  if (input.basisPath === undefined !== (input.basisContent === undefined)) {
    throw new Error("TASK_HISTORY_BASIS_INVALID: Task Basis path and bytes must be provided together.");
  }
  const basisRelativePath = input.basisPath === undefined ? null : path8.relative(path8.dirname(input.currentPath), input.basisPath);
  if (basisRelativePath !== null && (!basisRelativePath || basisRelativePath.startsWith("..") || path8.isAbsolute(basisRelativePath))) {
    throw new Error("TASK_HISTORY_BASIS_INVALID: linked Task Basis must stay inside the canonical workflow directory.");
  }
  const basisRevision = input.basisContent === undefined ? null : sha2563(input.basisContent);
  const directory = path8.join(path8.dirname(input.currentPath), "task-history", input.documentId);
  assertSafeHistoryDirectory(input.currentPath, directory);
  const historyPath = path8.join(directory, `${revision}.json`);
  const packageContent = JSON.stringify({
    schema_version: 1,
    kind: "vnext-task-definition-history",
    operation: input.operation ?? "supersede",
    task_id: input.taskId,
    document_id: input.documentId,
    source_path: path8.basename(input.currentPath),
    source_revision: revision,
    task_basis_path: basisRelativePath?.replace(/\\/gu, "/") ?? null,
    task_basis_revision: basisRevision,
    evidence_plan_revision: input.evidencePlanRevision ?? null,
    referenced_evidence: [...new Set(input.referencedEvidence ?? [])].sort().map((locator) => ({ locator, preservation: "reference-only; content not snapshotted" })),
    current_task_base64: Buffer.from(input.previousContent, "utf8").toString("base64"),
    task_basis_base64: input.basisContent === undefined ? null : Buffer.from(input.basisContent, "utf8").toString("base64")
  }, null, 2) + `
`;
  return {
    path: historyPath,
    relativePath: path8.relative(path8.dirname(input.currentPath), historyPath).replace(/\\/gu, "/"),
    content: packageContent
  };
}
function assertTaskHistoryForRevision(currentPath, documentId, taskId, sourceRevision, operation = "supersede") {
  if (!/^doc-[a-f0-9]+$/u.test(documentId) || !/^[a-f0-9]{64}$/u.test(sourceRevision)) {
    throw new Error("TASK_HISTORY_INVALID: replay identity or revision is invalid.");
  }
  const directory = path8.join(path8.dirname(currentPath), "task-history", documentId);
  assertSafeHistoryDirectory(currentPath, directory);
  const historyPath = path8.join(directory, `${sourceRevision}.json`);
  if (!fs8.existsSync(historyPath) || !fs8.lstatSync(historyPath).isFile()) {
    throw new Error("TASK_HISTORY_MISSING: task evolution requires the immutable preimage.");
  }
  let history;
  try {
    history = JSON.parse(fs8.readFileSync(historyPath, "utf8"));
  } catch {
    throw new Error("TASK_HISTORY_INVALID: preimage package is unreadable.");
  }
  if (history.schema_version !== 1 || history.kind !== "vnext-task-definition-history" || history.operation !== operation || history.document_id !== documentId || history.task_id !== taskId || history.source_path !== path8.basename(currentPath) || history.source_revision !== sourceRevision || typeof history.current_task_base64 !== "string" || sha2563(Buffer.from(history.current_task_base64, "base64").toString("utf8")) !== sourceRevision || history.task_basis_revision === null && (history.task_basis_base64 !== null || history.task_basis_path !== null) || history.task_basis_revision !== null && (typeof history.task_basis_revision !== "string" || typeof history.task_basis_base64 !== "string" || typeof history.task_basis_path !== "string" || sha2563(Buffer.from(history.task_basis_base64, "base64").toString("utf8")) !== history.task_basis_revision)) {
    throw new Error("TASK_HISTORY_INVALID: preimage identity or content digest changed.");
  }
}
function atomicReplace(filePath, content) {
  const temporaryPath = path8.join(path8.dirname(filePath), `.${path8.basename(filePath)}.${crypto6.randomUUID()}.tmp`);
  let descriptor;
  try {
    descriptor = fs8.openSync(temporaryPath, "wx");
    fs8.writeFileSync(descriptor, content, "utf8");
    fs8.fsyncSync(descriptor);
    fs8.closeSync(descriptor);
    descriptor = undefined;
    fs8.renameSync(temporaryPath, filePath);
  } finally {
    if (descriptor !== undefined)
      fs8.closeSync(descriptor);
    if (fs8.existsSync(temporaryPath))
      fs8.rmSync(temporaryPath, { force: true });
  }
}
function processIsAlive(pid) {
  try {
    process.kill(pid, 0);
    return true;
  } catch (error) {
    return error.code !== "ESRCH";
  }
}
function parseLock(content, currentPath) {
  let value;
  try {
    value = JSON.parse(content);
  } catch {
    throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: evolution lock is unreadable.");
  }
  if (value.schema_version !== 1 || !["prepared", "history-written", "current-published"].includes(value.phase ?? "") || !Number.isSafeInteger(value.pid) || !value.pid || value.current_path !== currentPath || ![value.previous_revision, value.next_revision, value.history_revision].every((item) => typeof item === "string" && /^[a-f0-9]{64}$/u.test(item)) || typeof value.history_path !== "string" || path8.basename(value.history_path) !== `${value.previous_revision}.json`) {
    throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: evolution lock is invalid.");
  }
  if (value.basis_path !== undefined) {
    if (typeof value.basis_path !== "string" || !value.basis_path || ![value.previous_basis_revision, value.next_basis_revision].every((item) => typeof item === "string" && /^[a-f0-9]{64}$/u.test(item))) {
      throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: evolution lock basis binding is invalid.");
    }
    const relative = path8.relative(path8.dirname(currentPath), value.basis_path);
    if (!relative || relative.startsWith("..") || path8.isAbsolute(relative))
      throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: Task Basis path escapes workflow directory.");
    assertSafeHistoryDirectory(currentPath, path8.dirname(value.basis_path));
  } else if (value.previous_basis_revision !== undefined || value.next_basis_revision !== undefined) {
    throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: evolution lock has incomplete Task Basis binding.");
  }
  const historyDirectory = path8.dirname(value.history_path);
  assertSafeHistoryDirectory(currentPath, historyDirectory);
  if (path8.dirname(historyDirectory) !== path8.join(path8.dirname(currentPath), "task-history")) {
    throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: evolution lock history path is not canonical.");
  }
  return value;
}
function recoverTaskEvolution(currentPath) {
  const lockPath = lockPathFor(currentPath);
  if (!fs8.existsSync(lockPath))
    return;
  const lock = parseLock(fs8.readFileSync(lockPath, "utf8"), currentPath);
  if (processIsAlive(lock.pid)) {
    throw new Error("TASK_EVOLUTION_IN_PROGRESS: another process owns the task evolution lock.");
  }
  if (!fs8.existsSync(currentPath)) {
    throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: CURRENT_TASK is missing after an interrupted evolution.");
  }
  const currentRevision = sha2563(fs8.readFileSync(currentPath, "utf8"));
  if (currentRevision !== lock.previous_revision && currentRevision !== lock.next_revision) {
    throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: CURRENT_TASK is neither the old nor the new recorded revision.");
  }
  if (fs8.existsSync(lock.history_path)) {
    if (sha2563(fs8.readFileSync(lock.history_path, "utf8")) !== lock.history_revision) {
      throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: history bytes changed after interruption.");
    }
  } else if (currentRevision === lock.next_revision) {
    throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: new CURRENT_TASK is visible without its required history.");
  }
  if (lock.basis_path) {
    if (!fs8.existsSync(lock.basis_path))
      throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: linked Task Basis is missing.");
    const basisRevision = sha2563(fs8.readFileSync(lock.basis_path, "utf8"));
    if (basisRevision !== lock.previous_basis_revision && basisRevision !== lock.next_basis_revision) {
      throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: linked Task Basis is neither old nor new revision.");
    }
    if (currentRevision !== lock.next_revision || basisRevision !== lock.next_basis_revision) {
      if (basisRevision === lock.next_basis_revision) {
        if (!fs8.existsSync(lock.history_path))
          throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: cannot restore Task Basis without history.");
        const history = JSON.parse(fs8.readFileSync(lock.history_path, "utf8"));
        if (!history.task_basis_base64)
          throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: history lacks old Task Basis bytes.");
        const oldBasis = Buffer.from(history.task_basis_base64, "base64").toString("utf8");
        if (sha2563(oldBasis) !== lock.previous_basis_revision || history.task_basis_revision !== lock.previous_basis_revision) {
          throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: history Task Basis preimage is invalid.");
        }
        atomicReplace(lock.basis_path, oldBasis);
      }
      if (currentRevision === lock.next_revision) {
        const history = JSON.parse(fs8.readFileSync(lock.history_path, "utf8"));
        if (!history.current_task_base64)
          throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: history lacks old CURRENT_TASK bytes.");
        const oldCurrent = Buffer.from(history.current_task_base64, "base64").toString("utf8");
        if (sha2563(oldCurrent) !== lock.previous_revision)
          throw new Error("TASK_EVOLUTION_RECOVERY_REQUIRED: history CURRENT_TASK preimage is invalid.");
        atomicReplace(currentPath, oldCurrent);
      }
    }
  }
  fs8.rmSync(lockPath);
}
function commitTaskEvolutionWithHistory(input, verifyNext, afterBasisPublished) {
  recoverTaskEvolution(input.currentPath);
  const history = taskHistoryLocation(input);
  const previousRevision = sha2563(input.previousContent);
  const nextRevision = sha2563(input.nextContent);
  const lock = {
    schema_version: 1,
    phase: "prepared",
    pid: process.pid,
    current_path: input.currentPath,
    previous_revision: previousRevision,
    next_revision: nextRevision,
    history_path: history.path,
    history_revision: sha2563(history.content),
    ...input.nextBasisContent === undefined ? {} : {
      basis_path: input.basisPath,
      previous_basis_revision: sha2563(input.basisContent),
      next_basis_revision: sha2563(input.nextBasisContent)
    }
  };
  const lockPath = lockPathFor(input.currentPath);
  let descriptor;
  let ownsLock = false;
  try {
    descriptor = fs8.openSync(lockPath, "wx");
    ownsLock = true;
    fs8.writeFileSync(descriptor, JSON.stringify(lock) + `
`, "utf8");
    fs8.fsyncSync(descriptor);
    fs8.closeSync(descriptor);
    descriptor = undefined;
    if (!fs8.existsSync(input.currentPath) || sha2563(fs8.readFileSync(input.currentPath, "utf8")) !== previousRevision) {
      throw new Error("TASK_EVOLUTION_SOURCE_STALE: CURRENT_TASK changed before the evolution lock was acquired.");
    }
    if (input.basisPath !== undefined && input.basisContent !== undefined && (!fs8.existsSync(input.basisPath) || sha2563(fs8.readFileSync(input.basisPath, "utf8")) !== sha2563(input.basisContent))) {
      throw new Error("TASK_EVOLUTION_BASIS_STALE: Task Basis changed before the evolution lock was acquired.");
    }
    fs8.mkdirSync(path8.dirname(history.path), { recursive: true });
    assertSafeHistoryDirectory(input.currentPath, path8.dirname(history.path));
    if (fs8.existsSync(history.path)) {
      if (fs8.readFileSync(history.path, "utf8") !== history.content) {
        throw new Error("TASK_HISTORY_CONFLICT: existing source revision has different bytes.");
      }
    } else {
      atomicReplace(history.path, history.content);
    }
    if (fs8.readFileSync(history.path, "utf8") !== history.content) {
      throw new Error("TASK_HISTORY_READ_BACK_FAILED: history bytes differ after write.");
    }
    atomicReplace(lockPath, JSON.stringify({ ...lock, phase: "history-written" }) + `
`);
    if (input.nextBasisContent !== undefined) {
      if (!input.basisPath || input.basisContent === undefined)
        throw new Error("TASK_EVOLUTION_BASIS_INVALID: new Task Basis requires its exact old binding.");
      atomicReplace(input.basisPath, input.nextBasisContent);
      if (fs8.readFileSync(input.basisPath, "utf8") !== input.nextBasisContent)
        throw new Error("TASK_EVOLUTION_BASIS_READ_BACK_FAILED: Task Basis bytes differ after write.");
      afterBasisPublished?.();
    }
    atomicReplace(input.currentPath, input.nextContent);
    const readBack = fs8.readFileSync(input.currentPath, "utf8");
    if (sha2563(readBack) !== nextRevision) {
      throw new Error("TASK_EVOLUTION_READ_BACK_FAILED: CURRENT_TASK bytes differ after write.");
    }
    atomicReplace(lockPath, JSON.stringify({ ...lock, phase: "current-published" }) + `
`);
    verifyNext(readBack);
    fs8.rmSync(lockPath);
    return history;
  } catch (error) {
    if (descriptor !== undefined)
      fs8.closeSync(descriptor);
    if (ownsLock && fs8.existsSync(input.currentPath) && sha2563(fs8.readFileSync(input.currentPath, "utf8")) === nextRevision) {
      try {
        atomicReplace(input.currentPath, input.previousContent);
      } catch {}
    }
    if (ownsLock && input.basisPath && input.basisContent !== undefined && input.nextBasisContent !== undefined && fs8.existsSync(input.basisPath) && sha2563(fs8.readFileSync(input.basisPath, "utf8")) === sha2563(input.nextBasisContent)) {
      try {
        atomicReplace(input.basisPath, input.basisContent);
      } catch {}
    }
    if (ownsLock && fs8.existsSync(input.currentPath) && sha2563(fs8.readFileSync(input.currentPath, "utf8")) === previousRevision && (input.nextBasisContent === undefined || input.basisPath && fs8.existsSync(input.basisPath) && sha2563(fs8.readFileSync(input.basisPath, "utf8")) === sha2563(input.basisContent)) && fs8.existsSync(lockPath)) {
      fs8.rmSync(lockPath);
    }
    throw error;
  }
}
function commitSupersedeWithHistory(input, verifyNext) {
  return commitTaskEvolutionWithHistory({ ...input, operation: "supersede" }, verifyNext);
}
function publishPreparedSuccessor(currentPath, previousContent, nextContent, artifacts, prepareAggregate) {
  if (fs8.readFileSync(currentPath, "utf8") !== previousContent) {
    throw new Error("SUCCESSOR_SOURCE_STALE: predecessor changed before publication.");
  }
  for (const artifact of artifacts) {
    if (fs8.existsSync(artifact.path)) {
      if (!fs8.lstatSync(artifact.path).isFile() || fs8.readFileSync(artifact.path, "utf8") !== artifact.content) {
        throw new Error("SUCCESSOR_HISTORY_CONFLICT: immutable prerequisite has different bytes.");
      }
      continue;
    }
    fs8.mkdirSync(path8.dirname(artifact.path), { recursive: true });
    atomicReplace(artifact.path, artifact.content);
    if (fs8.readFileSync(artifact.path, "utf8") !== artifact.content)
      throw new Error("SUCCESSOR_READ_BACK_FAILED: prerequisite differs.");
  }
  prepareAggregate();
  if (fs8.readFileSync(currentPath, "utf8") !== previousContent)
    throw new Error("SUCCESSOR_SOURCE_STALE: predecessor changed while preparing dependencies.");
  atomicReplace(currentPath, nextContent);
  if (fs8.readFileSync(currentPath, "utf8") !== nextContent)
    throw new Error("SUCCESSOR_READ_BACK_FAILED: published draft differs.");
}

// runtime/vnext/src/task-identity.ts
import * as path9 from "path";
var CURRENT_TASK_WORKFLOW_STATUSES = [
  "draft",
  "active",
  "suspended",
  "closed",
  "superseded",
  "replaced",
  "blocked_by_replan"
];
var TASK_LIFECYCLE_STATES = [
  "active",
  "paused_pending_closure",
  "paused_blocked",
  "interrupted",
  "archived"
];
var RESUME_REVIEW_REASON_ORDER = [
  "base_drift",
  "checkpoint_drift",
  "diff_review_target_changed",
  "environment_recovery_pending",
  "assumption_changed",
  "validation_pending",
  "manual_review_pending",
  "remaining_acceptance_pending",
  "blocker_recheck_required",
  "dirty_attribution_pending",
  "recovery_strategy_review_required"
];
var CURRENT_TASK_WORKFLOW_STATUS_SET = new Set(CURRENT_TASK_WORKFLOW_STATUSES);
var TASK_LIFECYCLE_STATE_SET = new Set(TASK_LIFECYCLE_STATES);
var TASK_ARTIFACT_KIND_SET = new Set(["archive", "paused", "interrupted"]);
var RESUME_REVIEW_REASON_SET = new Set(RESUME_REVIEW_REASON_ORDER);
var PAUSED_PENDING_CLOSURE_REASONS = [
  "validation_pending",
  "manual_review_pending",
  "remaining_acceptance_pending"
];
var INTERRUPTED_REQUIRED_REASONS = [
  "checkpoint_drift",
  "diff_review_target_changed",
  "dirty_attribution_pending",
  "environment_recovery_pending",
  "recovery_strategy_review_required"
];
var CURRENT_TASK_STATUS_TUPLES = new Map([
  ["draft|active", "active_owner"],
  ["active|active", "active_owner"],
  ["suspended|paused_pending_closure", "non_active_owner"],
  ["suspended|paused_blocked", "non_active_owner"],
  ["suspended|interrupted", "non_active_owner"],
  ["superseded|active", "non_active_owner"],
  ["replaced|active", "non_active_owner"],
  ["blocked_by_replan|active", "non_active_owner"],
  ["closed|archived", "non_active_owner"]
]);
var TASK_ID_PATTERN = /^[0-9]{3,}$/;
var TASK_SLUG_PATTERN = /^[a-z0-9]+(?:-[a-z0-9]+)*$/;
function normalizeValue(value) {
  const normalized = String(value ?? "").trim();
  return normalized.length > 0 ? normalized : null;
}
function escapeRegExp(value) {
  return value.replace(/[.*+?^()|[\]\\]/g, "\\$&").replace(/\$/g, "\\$&");
}
function extractTaskInfoSection(currentTaskContent) {
  const headingMatch = /^## 任务信息\s*$/m.exec(currentTaskContent);
  if (!headingMatch || headingMatch.index === undefined)
    return "";
  const afterHeading = currentTaskContent.slice(headingMatch.index + headingMatch[0].length);
  const nextHeadingMatch = /\r?\n##\s/.exec(afterHeading);
  const sectionEnd = nextHeadingMatch?.index ?? afterHeading.length;
  return afterHeading.slice(0, sectionEnd).trim();
}
function extractTaskInfoField(section, label) {
  const match = new RegExp("^-\\s*" + escapeRegExp(label) + "：\\s*(.+?)\\s*$", "m").exec(section);
  return normalizeValue(match?.[1]);
}
function normalizeDelimitedValues(values) {
  const sourceValues = Array.isArray(values) ? values : [values];
  return sourceValues.flatMap((value) => String(value ?? "").split(",").map((item) => item.trim()).filter(Boolean));
}
function extractTaskIdentityFromCurrentTask(currentTaskContent) {
  const section = extractTaskInfoSection(currentTaskContent);
  return {
    id: extractTaskInfoField(section, "任务 ID"),
    title: extractTaskInfoField(section, "任务标题"),
    slug: extractTaskInfoField(section, "任务 slug")
  };
}
function extractCurrentTaskStateFromCurrentTask(currentTaskContent) {
  const section = extractTaskInfoSection(currentTaskContent);
  const rawResume = extractTaskInfoField(section, "恢复需审查");
  let resumeRequiresReview = null;
  if (rawResume !== null) {
    if (rawResume === "true")
      resumeRequiresReview = true;
    else if (rawResume === "false")
      resumeRequiresReview = false;
    else
      throw new Error('恢复需审查 must be "true" or "false".');
  }
  return {
    workflowStatus: extractTaskInfoField(section, "当前状态"),
    lifecycleState: extractTaskInfoField(section, "生命周期状态"),
    resumeRequiresReview,
    resumeReviewReasons: extractTaskInfoField(section, "恢复审查原因")
  };
}
function parseBooleanField(value, label) {
  const normalized = normalizeValue(value);
  if (normalized === "true")
    return true;
  if (normalized === "false")
    return false;
  throw new Error(`${label} must be "true" or "false".`);
}
function parseCurrentTaskWorkflowStatus(value) {
  const normalized = normalizeValue(value);
  if (!normalized || !CURRENT_TASK_WORKFLOW_STATUS_SET.has(normalized)) {
    throw new Error(`当前状态 must use one of: ${CURRENT_TASK_WORKFLOW_STATUSES.join(", ")}.`);
  }
  return normalized;
}
function parseTaskLifecycleState(value) {
  const normalized = normalizeValue(value);
  if (!normalized || !TASK_LIFECYCLE_STATE_SET.has(normalized)) {
    throw new Error(`生命周期状态 must use one of: ${TASK_LIFECYCLE_STATES.join(", ")}.`);
  }
  return normalized;
}
function classifyCurrentTaskOwnershipStatus(workflowStatus, lifecycleState) {
  const normalizedWorkflowStatus = normalizeValue(workflowStatus);
  const normalizedLifecycleState = normalizeValue(lifecycleState);
  if (!normalizedWorkflowStatus || !normalizedLifecycleState)
    return "invalid_unknown";
  return CURRENT_TASK_STATUS_TUPLES.get(`${normalizedWorkflowStatus}|${normalizedLifecycleState}`) ?? "invalid_unknown";
}
function validateCurrentTaskStatusTuple(workflowStatus, lifecycleState) {
  const parsedWorkflowStatus = parseCurrentTaskWorkflowStatus(workflowStatus);
  const parsedLifecycleState = parseTaskLifecycleState(lifecycleState);
  const ownershipStatus = classifyCurrentTaskOwnershipStatus(parsedWorkflowStatus, parsedLifecycleState);
  if (ownershipStatus === "invalid_unknown") {
    throw new Error(`当前状态 × 生命周期状态 tuple "${parsedWorkflowStatus} + ${parsedLifecycleState}" is not allowed by the v1 lifecycle matrix.`);
  }
  return { workflowStatus: parsedWorkflowStatus, lifecycleState: parsedLifecycleState, ownershipStatus };
}
function normalizeResumeReviewReasons(resumeReviewReasons) {
  const providedReasons = normalizeDelimitedValues(resumeReviewReasons);
  const invalidReasons = [...new Set(providedReasons)].filter((reason) => !RESUME_REVIEW_REASON_SET.has(reason));
  if (invalidReasons.length > 0) {
    throw new Error(`resume_review_reasons must use the closed v1 set. Invalid values: ${invalidReasons.join(", ")}.`);
  }
  const provided = new Set(providedReasons);
  return RESUME_REVIEW_REASON_ORDER.filter((reason) => provided.has(reason));
}
function validateCurrentTaskResumeGate(lifecycleState, resumeRequiresReview, resumeReviewReasons) {
  const normalizedReasons = normalizeResumeReviewReasons(resumeReviewReasons);
  if (!resumeRequiresReview) {
    if (normalizedReasons.length > 0)
      throw new Error("恢复需审查 = false 时，恢复审查原因必须为空。");
    return { resumeRequiresReview: false, resumeReviewReasons: [] };
  }
  if (normalizedReasons.length === 0)
    throw new Error("恢复需审查 = true 时，恢复审查原因必须为非空闭合集合。");
  if (lifecycleState === "paused_pending_closure" && !PAUSED_PENDING_CLOSURE_REASONS.some((reason) => normalizedReasons.includes(reason))) {
    throw new Error("paused_pending_closure requires validation_pending, manual_review_pending, or remaining_acceptance_pending.");
  }
  if (lifecycleState === "paused_blocked" && !normalizedReasons.includes("blocker_recheck_required")) {
    throw new Error("paused_blocked requires blocker_recheck_required in resume_review_reasons.");
  }
  if (lifecycleState === "interrupted" && !INTERRUPTED_REQUIRED_REASONS.some((reason) => normalizedReasons.includes(reason))) {
    throw new Error("interrupted requires an interrupt recovery reason in resume_review_reasons.");
  }
  return { resumeRequiresReview: true, resumeReviewReasons: normalizedReasons };
}
function getTaskArtifactPath(taskId, taskSlug, kind) {
  validateTaskId(taskId);
  validateTaskSlug(taskSlug);
  if (!TASK_ARTIFACT_KIND_SET.has(kind))
    throw new Error(`Invalid TaskArtifactKind "${kind}".`);
  const fileName = `TASK-${taskId}-${taskSlug}.md`;
  if (kind === "archive")
    return path9.posix.join("TASKS", fileName);
  return path9.posix.join("TASKS", kind, fileName);
}
function validateTaskId(taskId) {
  const normalized = normalizeValue(taskId);
  if (!normalized || !TASK_ID_PATTERN.test(normalized)) {
    throw new Error('Invalid TASK_ID "' + taskId + '". Expected a zero-padded decimal string with at least 3 digits.');
  }
}
function validateTaskSlug(taskSlug) {
  const normalized = normalizeValue(taskSlug);
  if (!normalized || !TASK_SLUG_PATTERN.test(normalized)) {
    throw new Error('Invalid TASK_SLUG "' + taskSlug + '". Expected lowercase ASCII kebab-case.');
  }
}

// runtime/vnext/src/mutation-scope.ts
import * as crypto7 from "crypto";

class MutationScopeError extends Error {
  code;
  constructor(code, message) {
    super(message);
    this.name = "MutationScopeError";
    this.code = code;
  }
}
var ALLOWED_SCOPE_HEADINGS = new Set(["允许修改范围", "mutation scope", "scope"]);
var ALLOWED_BUCKET_HEADINGS = new Set(["allowed files", "allowed targets", "允许文件", "允许目标"]);
var CONDITIONAL_BUCKET_HEADINGS = new Set(["条件修改范围", "条件允许修改范围", "conditional files", "conditional targets"]);
var FORBIDDEN_SCOPE_HEADINGS = new Set(["禁止修改范围", "forbidden files", "forbidden targets"]);
var READ_DISCOVERY_HEADINGS = new Set([
  "read / discovery context",
  "read/discovery context",
  "read context",
  "discovery context",
  "读取 / 发现上下文",
  "读取/发现上下文",
  "发现上下文"
]);
var PERSISTENT_TEST_HEADINGS = new Set(["persistent tests", "持久测试"]);
var EMPTY_SCOPE_MARKER = /^(?:none|n\/a|na|nil|empty|no\s+(?:files?|targets?|scope)|无|暂无|不适用)[.!。]?$/iu;
var CONDITIONAL_LANGUAGE = /(?:when|if|after|once|upon|provided|only|condition|evidence|authority|approval|authorized|confirmed|满足|条件|证据|依据|授权|审批|确认|批准)/iu;
var PATH_PREFIX = /^(?:file|files|path|paths|target|targets|文件|路径|目标)\s*[:：]\s*/iu;
var UNSUPPORTED_GLOB_SYNTAX = /[\[\]{}!]/u;
function failInvalid(message) {
  throw new MutationScopeError("MUTATION_SCOPE_INVALID", message);
}
function normalizeHeading(title) {
  return title.trim().replace(/[：:]/gu, "").replace(/\s+/gu, " ").toLocaleLowerCase();
}
function scanMarkdownSections(body) {
  const headings = [];
  const headingPattern = /^(#{2,6})[ \t]+(.+?)[ \t]*$/gmu;
  for (const match of body.matchAll(headingPattern)) {
    const start = match.index ?? 0;
    headings.push({
      title: match[2].trim(),
      level: match[1].length,
      start,
      end: start + match[0].length
    });
  }
  return headings.map((heading, index) => {
    const contentStart = body.startsWith(`\r
`, heading.end) ? heading.end + 2 : getLineContentStart(body, heading.end);
    const next = headings.slice(index + 1).find((candidate) => candidate.level <= heading.level);
    return {
      title: heading.title,
      level: heading.level,
      heading_start: heading.start,
      content_start: contentStart,
      content_end: next?.start ?? body.length
    };
  });
}
function getLineContentStart(body, headingEnd) {
  return body.startsWith(`
`, headingEnd) ? headingEnd + 1 : headingEnd;
}
function findOneSection(sections, aliases, level, range) {
  const matches = sections.filter((section) => {
    if (section.level !== level || !aliases.has(normalizeHeading(section.title)))
      return false;
    if (!range)
      return true;
    return section.heading_start >= range.start && section.heading_start < range.end;
  });
  if (matches.length > 1)
    failInvalid(`CURRENT_TASK contains duplicate ${[...aliases].join(" / ")} scope sections.`);
  return matches[0] ?? null;
}
function sectionRange(section) {
  return { start: section.content_start, end: section.content_end };
}
function resolveBucketSection(sections, bucket) {
  const bucketAliases = aliasesForBucket(bucket);
  const containerAliases = containerAliasesForBucket(bucket);
  const container = containerAliases ? findOneSection(sections, containerAliases, 2) : null;
  const direct = findOneSection(sections, bucketAliases, 2);
  if (container && direct && direct.heading_start !== container.heading_start) {
    failInvalid(`CURRENT_TASK declares both a scope container and a direct ${bucket} section.`);
  }
  if (container) {
    const range = sectionRange(container);
    const nested = findOneSection(sections, bucketAliases, 3, range);
    if (bucket === "allowed") {
      const nestedConditional = findOneSection(sections, CONDITIONAL_BUCKET_HEADINGS, 3, range);
      const nestedRead = findOneSection(sections, READ_DISCOVERY_HEADINGS, 3, range);
      if ((nestedConditional || nestedRead) && !nested) {
        failInvalid("Nested Conditional Files or Read / discovery context requires a distinct nested Allowed Files section.");
      }
    }
    const nestedHeadings = sections.filter((section) => section.level === 3 && section.heading_start >= range.start && section.heading_start < range.end);
    if (nestedHeadings.length > 0 && !nested) {
      failInvalid(`${bucket} scope container has nested headings but no matching ${[...bucketAliases].join(" / ")} bucket.`);
    }
    if (nested)
      return nested;
    return container;
  }
  if (bucket === "conditional" || bucket === "read_discovery") {
    const allowedContainer = findOneSection(sections, ALLOWED_SCOPE_HEADINGS, 2);
    const nested = allowedContainer ? findOneSection(sections, bucketAliases, 3, sectionRange(allowedContainer)) : null;
    if (nested && direct)
      failInvalid(`CURRENT_TASK declares both nested and direct ${bucket} scope sections.`);
    if (nested)
      return nested;
  }
  if (direct)
    return direct;
  if (bucket === "read_discovery")
    return null;
  return null;
}
function aliasesForBucket(bucket) {
  if (bucket === "allowed")
    return ALLOWED_BUCKET_HEADINGS;
  if (bucket === "conditional")
    return CONDITIONAL_BUCKET_HEADINGS;
  if (bucket === "forbidden")
    return FORBIDDEN_SCOPE_HEADINGS;
  return READ_DISCOVERY_HEADINGS;
}
function containerAliasesForBucket(bucket) {
  if (bucket === "allowed")
    return ALLOWED_SCOPE_HEADINGS;
  if (bucket === "forbidden")
    return FORBIDDEN_SCOPE_HEADINGS;
  return null;
}
function extractDeclarationPath(text) {
  const code = /`([^`\r\n]+)`/u.exec(text);
  if (code) {
    return {
      pattern: code[1].trim(),
      remainder: `${text.slice(0, code.index)} ${text.slice(code.index + code[0].length)}`.trim()
    };
  }
  const prefixed = text.replace(PATH_PREFIX, "");
  const token = /^([^\s,;，；()]+)([\s\S]*)$/u.exec(prefixed.trim());
  if (!token)
    failInvalid(`Scope declaration does not identify a repository-relative path: ${text}`);
  return { pattern: token[1], remainder: token[2].trim() };
}
function normalizeScopePattern(value, location) {
  const normalized = value.trim().replace(/\\/gu, "/").replace(/^\.\//u, "").replace(/\/+/gu, "/");
  if (!normalized || normalized === ".")
    failInvalid(`${location} must identify a non-empty repository-relative path pattern.`);
  if (/^[A-Za-z]:\//u.test(normalized) || normalized.startsWith("/"))
    failInvalid(`${location} must not be absolute.`);
  if (/[\0-\x1F\x7F]/u.test(normalized))
    failInvalid(`${location} contains a control character.`);
  if (UNSUPPORTED_GLOB_SYNTAX.test(normalized))
    failInvalid(`${location} uses unsupported glob syntax; only * and ** are supported.`);
  if (normalized.split("/").some((segment) => segment === ".." || segment.length === 0))
    failInvalid(`${location} must not contain parent traversal or empty path segments.`);
  if (normalized.includes("://") || normalized.includes("$"))
    failInvalid(`${location} is not a repository-relative path pattern.`);
  if (/\s/u.test(normalized))
    failInvalid(`${location} must not contain unquoted whitespace.`);
  return normalized;
}
function isEmptyMarker(text) {
  return EMPTY_SCOPE_MARKER.test(text.trim());
}
function parseScopeBucket(body, section, bucket) {
  const content = body.slice(section.content_start, section.content_end);
  const entries = [];
  let sawMarker = false;
  let bulletCount = 0;
  for (const rawLine of content.split(/\r?\n/u)) {
    const line = rawLine.trim();
    if (!line || /^<!--.*-->$/u.test(line))
      continue;
    const bullet = /^(?:[-*+]\s+|\d+[.)]\s+)(.*)$/u.exec(line);
    if (!bullet)
      failInvalid(`${section.title} contains a non-list scope declaration: ${line}`);
    bulletCount += 1;
    const declaration = bullet[1].replace(/^\[[ xX]\]\s*/u, "").trim();
    if (isEmptyMarker(declaration)) {
      if (entries.length > 0 || sawMarker)
        failInvalid(`${section.title} mixes an empty marker with path declarations.`);
      sawMarker = true;
      continue;
    }
    if (sawMarker)
      failInvalid(`${section.title} mixes an empty marker with path declarations.`);
    const extracted = extractDeclarationPath(declaration);
    const pattern = normalizeScopePattern(extracted.pattern, `${section.title} declaration`);
    if (bucket === "conditional" && !CONDITIONAL_LANGUAGE.test(extracted.remainder)) {
      failInvalid(`Conditional Files declaration ${pattern} must state its condition, evidence, or authority.`);
    }
    entries.push({ pattern, broad: pattern.includes("*"), declaration });
  }
  if (bulletCount === 0)
    failInvalid(`${section.title} must explicitly list paths or declare none.`);
  if (new Set(entries.map((entry) => entry.pattern)).size !== entries.length)
    failInvalid(`${section.title} contains duplicate path patterns.`);
  return entries;
}
function parsePersistentTests(body, sections) {
  const matches = sections.filter((section) => PERSISTENT_TEST_HEADINGS.has(normalizeHeading(section.title)));
  if (matches.length > 1)
    failInvalid("CURRENT_TASK contains duplicate Persistent Tests sections.");
  const section = matches[0];
  if (!section)
    return null;
  const entries = [];
  let sawMarker = false;
  let bulletCount = 0;
  for (const rawLine of body.slice(section.content_start, section.content_end).split(/\r?\n/u)) {
    if (!rawLine.trim() || /^\s*<!--.*-->\s*$/u.test(rawLine))
      continue;
    if (/^\s+(?:[-*+]\s+|\d+[.)]\s+)/u.test(rawLine))
      continue;
    const bullet = /^(?:[-*+]\s+|\d+[.)]\s+)(.*)$/u.exec(rawLine);
    if (!bullet)
      failInvalid(`Persistent Tests contains a non-list declaration: ${rawLine.trim()}`);
    bulletCount += 1;
    const declaration = bullet[1].replace(/^\[[ xX]\]\s*/u, "").trim();
    if (isEmptyMarker(declaration)) {
      if (entries.length > 0 || sawMarker)
        failInvalid("Persistent Tests mixes an empty marker with path declarations.");
      sawMarker = true;
      continue;
    }
    if (sawMarker)
      failInvalid("Persistent Tests mixes an empty marker with path declarations.");
    const path = normalizeScopePattern(extractDeclarationPath(declaration).pattern, "Persistent Tests declaration");
    if (path.includes("*"))
      failInvalid(`Persistent Tests declaration ${path} must be an exact path.`);
    entries.push(path);
  }
  if (bulletCount === 0)
    failInvalid("Persistent Tests must explicitly list exact paths or declare none.");
  if (new Set(entries).size !== entries.length)
    failInvalid("Persistent Tests contains duplicate paths.");
  return entries;
}
function hash2(value) {
  return crypto7.createHash("sha256").update(value).digest("hex");
}
function scopeSummary(scope) {
  return {
    allowed: scope.allowed.map((entry) => entry.pattern),
    conditional: scope.conditional.map((entry) => entry.pattern),
    forbidden: scope.forbidden.map((entry) => entry.pattern),
    read_discovery: scope.read_discovery.map((entry) => entry.pattern),
    persistent_tests: scope.persistent_tests === null ? null : [...scope.persistent_tests]
  };
}
function parseMutationScope(body, sourceRevision = hash2(body)) {
  if (typeof body !== "string" || body.length === 0)
    failInvalid("CURRENT_TASK body is empty; mutation scope cannot be established.");
  const sections = scanMarkdownSections(body);
  const allowedSection = resolveBucketSection(sections, "allowed");
  const conditionalSection = resolveBucketSection(sections, "conditional");
  const forbiddenSection = resolveBucketSection(sections, "forbidden");
  const readSection = resolveBucketSection(sections, "read_discovery");
  if (!allowedSection)
    failInvalid("CURRENT_TASK is missing the required Allowed Files scope bucket.");
  if (!conditionalSection)
    failInvalid("CURRENT_TASK is missing the required Conditional Files scope bucket.");
  if (!forbiddenSection)
    failInvalid("CURRENT_TASK is missing the required Forbidden Files scope bucket.");
  if (!/^[a-f0-9]{64}$/u.test(sourceRevision))
    failInvalid("Mutation scope source_revision must be a SHA-256 digest.");
  return {
    source_revision: sourceRevision,
    allowed: parseScopeBucket(body, allowedSection, "allowed"),
    conditional: parseScopeBucket(body, conditionalSection, "conditional"),
    forbidden: parseScopeBucket(body, forbiddenSection, "forbidden"),
    read_discovery: readSection ? parseScopeBucket(body, readSection, "read_discovery") : [],
    persistent_tests: parsePersistentTests(body, sections)
  };
}
function isLikelyPersistentTestPath(value) {
  const normalized = value.trim().replace(/\\/gu, "/").replace(/^\.\//u, "").toLocaleLowerCase();
  return /(?:^|\/)(?:test|tests|__tests__|spec|specs|e2e)(?:\/|$)/u.test(normalized) || /(?:^|\/)[^/]+\.(?:test|spec)\.[^/]+$/u.test(normalized) || /\.snap$/u.test(normalized);
}
function escapeRegex(text) {
  return text.replace(/[|\\{}()[\]^$+?.]/gu, "\\$&");
}
function mutationScopePatternMatchesPath(file, pattern) {
  const normalizedFile = normalizeScopePattern(file, "changed path");
  const normalizedPattern = normalizeScopePattern(pattern, "scope pattern");
  const doubleStarSlashToken = "\x00MUTATION_SCOPE_DOUBLE_STAR_SLASH\x00";
  const doubleStarToken = "\x00MUTATION_SCOPE_DOUBLE_STAR\x00";
  const regex = escapeRegex(normalizedPattern).replace(/\*\*\//gu, doubleStarSlashToken).replace(/\*\*/gu, doubleStarToken).replace(/\*/gu, "[^/]*").replace(/\u0000MUTATION_SCOPE_DOUBLE_STAR_SLASH\u0000/gu, "(?:.*/)?").replace(/\u0000MUTATION_SCOPE_DOUBLE_STAR\u0000/gu, ".*");
  return new RegExp(`^${regex}$`, "u").test(normalizedFile);
}
function nonExecutableChangePatternIsBounded(pattern) {
  const normalized = normalizeScopePattern(pattern, "non-executable change pattern");
  if (!normalized.includes("*"))
    return true;
  if (!normalized.endsWith("/**"))
    return false;
  const prefix = normalized.slice(0, -3);
  return Boolean(prefix) && !prefix.includes("*");
}
function mutationScopePatternIsSubset(candidate, boundary) {
  const normalizedCandidate = normalizeScopePattern(candidate, "candidate scope pattern");
  const normalizedBoundary = normalizeScopePattern(boundary, "boundary scope pattern");
  if (normalizedCandidate === normalizedBoundary)
    return true;
  if (!normalizedCandidate.includes("*")) {
    return mutationScopePatternMatchesPath(normalizedCandidate, normalizedBoundary);
  }
  if (!normalizedBoundary.includes("*"))
    return false;
  if (normalizedBoundary.endsWith("/**")) {
    const boundaryPrefix = normalizedBoundary.slice(0, -3);
    return Boolean(boundaryPrefix) && normalizedCandidate.startsWith(`${boundaryPrefix}/`);
  }
  return false;
}
function normalizeChangedPath(value, index) {
  if (typeof value !== "string" || value.trim().length === 0)
    return null;
  try {
    const normalized = normalizeScopePattern(value, `changed_paths[${index}]`);
    if (normalized.includes("*"))
      return null;
    return normalized;
  } catch {
    return null;
  }
}
function validateConditionalAuthorizations(value) {
  if (value === undefined)
    return { authorizations: [], blockers: [] };
  if (!Array.isArray(value))
    return { authorizations: [], blockers: ["conditional_authorizations must be an array."] };
  const authorizations = [];
  const blockers = [];
  for (const [index, raw] of value.entries()) {
    if (!raw || typeof raw !== "object" || Array.isArray(raw)) {
      blockers.push(`conditional_authorizations[${index}] must be a mapping.`);
      continue;
    }
    const record = raw;
    const keys = Object.keys(record).sort();
    if (keys.join("|") !== ["authority", "evidence_refs", "pattern"].join("|")) {
      blockers.push(`conditional_authorizations[${index}] must contain exactly pattern, evidence_refs, and authority.`);
      continue;
    }
    let pattern;
    try {
      pattern = normalizeScopePattern(String(record.pattern ?? ""), `conditional_authorizations[${index}].pattern`);
    } catch (error) {
      blockers.push(error instanceof Error ? error.message : String(error));
      continue;
    }
    if (pattern.includes("*")) {
      blockers.push(`conditional_authorizations[${index}].pattern must narrow to an exact target.`);
      continue;
    }
    const evidenceRefs = record.evidence_refs;
    if (!Array.isArray(evidenceRefs) || evidenceRefs.length === 0 || evidenceRefs.some((item) => typeof item !== "string" || item.trim().length === 0)) {
      blockers.push(`conditional_authorizations[${index}].evidence_refs must be a non-empty list of references.`);
      continue;
    }
    const authority = record.authority;
    if (typeof authority !== "string" || authority.trim().length === 0) {
      blockers.push(`conditional_authorizations[${index}].authority must be non-empty.`);
      continue;
    }
    authorizations.push({
      pattern,
      evidence_refs: [...new Set(evidenceRefs.map((item) => item.trim()))],
      authority: authority.trim()
    });
  }
  return { authorizations, blockers };
}
function blockedResult(scope, inputPaths, transformationKind, blockers) {
  const normalizedPaths = inputPaths.filter((path) => typeof path === "string").map((path) => path.trim()).filter(Boolean);
  return {
    status: "blocked",
    source_revision: scope.source_revision,
    transformation_kind: transformationKind,
    scope: scopeSummary(scope),
    changed_paths: normalizedPaths,
    decisions: normalizedPaths.map((path) => ({
      path,
      classification: "invalid",
      mutation_admitted: false,
      matched_scope: [],
      read_discovery_matches: [],
      reason: "mutation scope input is invalid or incomplete"
    })),
    admitted_paths: [],
    blocked_paths: normalizedPaths,
    blockers: [...new Set(blockers)]
  };
}
function evaluateMutationScope(scope, input) {
  const transformationKind = input.transformation_kind ?? "localized";
  if (transformationKind !== "localized" && transformationKind !== "inherently-broad") {
    return blockedResult(scope, Array.isArray(input.changed_paths) ? input.changed_paths : [], "localized", ["transformation_kind must be localized or inherently-broad."]);
  }
  if (!Array.isArray(input.changed_paths) || input.changed_paths.length === 0) {
    return blockedResult(scope, [], transformationKind, ["an explicit non-empty changed_paths diff target is required; an empty diff cannot be admitted."]);
  }
  const authorizationResult = validateConditionalAuthorizations(input.conditional_authorizations);
  if (authorizationResult.blockers.length > 0) {
    return blockedResult(scope, input.changed_paths, transformationKind, authorizationResult.blockers);
  }
  const explicitPersistentTests = new Set;
  if (input.persistent_test_paths !== undefined) {
    if (!Array.isArray(input.persistent_test_paths)) {
      return blockedResult(scope, input.changed_paths, transformationKind, ["persistent_test_paths must be an array."]);
    }
    for (const [index, rawPath] of input.persistent_test_paths.entries()) {
      const pathValue = normalizeChangedPath(rawPath, index);
      if (!pathValue) {
        return blockedResult(scope, input.changed_paths, transformationKind, [`persistent_test_paths[${index}] must be an exact repository-relative path.`]);
      }
      explicitPersistentTests.add(pathValue);
    }
  }
  const decisions = [];
  const seen = new Set;
  const inputBlockers = [];
  for (const [index, rawPath] of input.changed_paths.entries()) {
    const pathValue = normalizeChangedPath(rawPath, index);
    if (!pathValue) {
      decisions.push({
        path: typeof rawPath === "string" ? rawPath.trim() : String(rawPath),
        classification: "invalid",
        mutation_admitted: false,
        matched_scope: [],
        read_discovery_matches: [],
        reason: "changed path must be a unique repository-relative file path without glob syntax or traversal."
      });
      inputBlockers.push(`changed_paths[${index}] is invalid.`);
      continue;
    }
    if (seen.has(pathValue)) {
      decisions.push({
        path: pathValue,
        classification: "invalid",
        mutation_admitted: false,
        matched_scope: [],
        read_discovery_matches: [],
        reason: "changed path is duplicated; the diff target must be explicit and unambiguous."
      });
      inputBlockers.push(`changed path ${pathValue} is duplicated.`);
      continue;
    }
    seen.add(pathValue);
    const forbidden = scope.forbidden.filter((entry) => mutationScopePatternMatchesPath(pathValue, entry.pattern));
    const allowed = scope.allowed.filter((entry) => mutationScopePatternMatchesPath(pathValue, entry.pattern));
    const conditional = scope.conditional.filter((entry) => mutationScopePatternMatchesPath(pathValue, entry.pattern));
    const readMatches = scope.read_discovery.filter((entry) => mutationScopePatternMatchesPath(pathValue, entry.pattern));
    const matchedScope = [
      ...forbidden.map((entry) => `Forbidden:${entry.pattern}`),
      ...allowed.map((entry) => `Allowed:${entry.pattern}`),
      ...conditional.map((entry) => `Conditional:${entry.pattern}`)
    ];
    const readDiscoveryMatches = readMatches.map((entry) => entry.pattern);
    if (forbidden.length > 0) {
      decisions.push({ path: pathValue, classification: "forbidden", mutation_admitted: false, matched_scope: matchedScope, read_discovery_matches: readDiscoveryMatches, reason: "Forbidden Files takes precedence over every other bucket." });
      continue;
    }
    const frozenPersistentTests = scope.persistent_tests;
    const isPersistentTest = explicitPersistentTests.has(pathValue) || isLikelyPersistentTestPath(pathValue);
    if (frozenPersistentTests !== null && isPersistentTest && !frozenPersistentTests.includes(pathValue)) {
      decisions.push({
        path: pathValue,
        classification: "persistent-test-unadmitted",
        mutation_admitted: false,
        matched_scope: matchedScope,
        read_discovery_matches: readDiscoveryMatches,
        reason: "the path is a persistent test but is absent from the frozen Persistent Tests allowlist."
      });
      continue;
    }
    if (allowed.length > 0 && conditional.length > 0) {
      decisions.push({ path: pathValue, classification: "ambiguous-overlap", mutation_admitted: false, matched_scope: matchedScope, read_discovery_matches: readDiscoveryMatches, reason: "the path matches both Allowed Files and Conditional Files; ambiguous authority is denied." });
      continue;
    }
    if (allowed.length > 0) {
      const exactAllowed = allowed.filter((entry) => !entry.broad);
      if (exactAllowed.length > 0) {
        decisions.push({ path: pathValue, classification: "allowed-exact", mutation_admitted: true, matched_scope: matchedScope, read_discovery_matches: readDiscoveryMatches, reason: "exact Allowed Files entry admits this localized mutation." });
        continue;
      }
      if (transformationKind === "inherently-broad") {
        decisions.push({ path: pathValue, classification: "allowed-broad", mutation_admitted: true, matched_scope: matchedScope, read_discovery_matches: readDiscoveryMatches, reason: "the declared broad Allowed Files pattern is admitted for an explicitly inherently-broad transformation." });
      } else {
        decisions.push({ path: pathValue, classification: "broad-scope-unqualified", mutation_admitted: false, matched_scope: matchedScope, read_discovery_matches: readDiscoveryMatches, reason: "a broad Allowed Files pattern requires transformation_kind: inherently-broad." });
      }
      continue;
    }
    if (conditional.length > 0) {
      const authorization = authorizationResult.authorizations.find((candidate) => candidate.pattern === pathValue && conditional.some((entry) => mutationScopePatternMatchesPath(pathValue, entry.pattern)));
      if (authorization) {
        decisions.push({ path: pathValue, classification: "conditional-admitted", mutation_admitted: true, matched_scope: matchedScope, read_discovery_matches: readDiscoveryMatches, reason: `Conditional Files admitted by exact target authorization with ${authorization.evidence_refs.length} evidence reference(s) and explicit authority.` });
      } else {
        decisions.push({ path: pathValue, classification: "conditional-unapproved", mutation_admitted: false, matched_scope: matchedScope, read_discovery_matches: readDiscoveryMatches, reason: "Conditional Files is not pre-authorized; an exact target authorization with evidence_refs and authority is required." });
      }
      continue;
    }
    if (readMatches.length > 0) {
      decisions.push({ path: pathValue, classification: "read-context-only", mutation_admitted: false, matched_scope: [], read_discovery_matches: readDiscoveryMatches, reason: "Read / discovery context is intentionally broader but never grants write authority." });
      continue;
    }
    decisions.push({ path: pathValue, classification: "unowned", mutation_admitted: false, matched_scope: [], read_discovery_matches: [], reason: "the path is not listed in Allowed Files or an authorized Conditional Files entry." });
  }
  const admittedPaths = decisions.filter((decision) => decision.mutation_admitted).map((decision) => decision.path);
  const blockedPaths = decisions.filter((decision) => !decision.mutation_admitted).map((decision) => decision.path);
  const decisionBlockers = decisions.filter((decision) => !decision.mutation_admitted).map((decision) => `${decision.path}: ${decision.reason}`);
  const blockers = [...new Set([...inputBlockers, ...decisionBlockers])];
  return {
    status: blockers.length === 0 && admittedPaths.length === decisions.length ? "pass" : "blocked",
    source_revision: scope.source_revision,
    transformation_kind: transformationKind,
    scope: scopeSummary(scope),
    changed_paths: decisions.map((decision) => decision.path),
    decisions,
    admitted_paths: admittedPaths,
    blocked_paths: blockedPaths,
    blockers
  };
}
function commandFootprintRepresentatives(pattern) {
  if (!pattern.includes("*"))
    return [pattern];
  const shallow = pattern.replace(/\*\*/gu, "workflow-command-output").replace(/\*/gu, "workflow-command-output");
  if (!pattern.includes("**"))
    return [shallow];
  const nested = pattern.replace(/\*\*/gu, "workflow-command-output/nested-output").replace(/\*/gu, "workflow-command-output");
  return shallow === nested ? [shallow] : [shallow, nested];
}
function commandFootprintPatternPrefix(pattern) {
  const wildcardIndex = pattern.search(/\*/u);
  return wildcardIndex < 0 ? pattern : pattern.slice(0, wildcardIndex);
}
function commandFootprintPatternsMayOverlap(left, right) {
  const leftPrefix = commandFootprintPatternPrefix(left);
  const rightPrefix = commandFootprintPatternPrefix(right);
  if (!leftPrefix || !rightPrefix)
    return true;
  return leftPrefix.startsWith(rightPrefix) || rightPrefix.startsWith(leftPrefix);
}
function commandFootprintSafetyBlockers(scope, target) {
  if (!target.includes("*"))
    return [];
  const blockers = [];
  if (!scope.allowed.some((entry) => entry.pattern === target)) {
    blockers.push("a glob expected_write_footprint must match an explicit Allowed Files pattern exactly; otherwise its full output set cannot be safely bounded.");
  }
  const conflictingPatterns = [
    ...scope.forbidden.map((entry) => `Forbidden:${entry.pattern}`),
    ...scope.conditional.map((entry) => `Conditional:${entry.pattern}`)
  ].filter((entry) => commandFootprintPatternsMayOverlap(target, entry.slice(entry.indexOf(":") + 1)));
  if (conflictingPatterns.length > 0) {
    blockers.push(`the glob footprint may overlap non-admitted scope entries: ${conflictingPatterns.join(", ")}.`);
  }
  return blockers;
}
function normalizeCommandWriteFootprint(value) {
  const blockers = [];
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    return { footprint: null, blockers: ["expected_write_footprint must be a mapping."] };
  }
  const kind = value.kind;
  if (kind !== "bounded" && kind !== "unbounded")
    blockers.push("expected_write_footprint.kind must be bounded or unbounded.");
  const rawTargets = value.targets;
  if (!Array.isArray(rawTargets)) {
    blockers.push("expected_write_footprint.targets must be an array.");
  }
  const rawEvidenceRefs = value.evidence_refs;
  if (!Array.isArray(rawEvidenceRefs) || rawEvidenceRefs.some((item) => typeof item !== "string" || item.trim().length === 0)) {
    blockers.push("expected_write_footprint.evidence_refs must be a list of evidence references.");
  } else if (kind === "bounded" && rawEvidenceRefs.length === 0) {
    blockers.push("a bounded expected_write_footprint must include at least one evidence reference.");
  }
  const reason = value.reason;
  if (kind === "unbounded" && (typeof reason !== "string" || reason.trim().length === 0)) {
    blockers.push("an unbounded expected_write_footprint must explain why the command cannot be safely bounded.");
  }
  const targets = [];
  if (Array.isArray(rawTargets)) {
    for (const [index, rawTarget] of rawTargets.entries()) {
      if (typeof rawTarget !== "string") {
        blockers.push(`expected_write_footprint.targets[${index}] must be a repository-relative path pattern.`);
        continue;
      }
      try {
        targets.push(normalizeScopePattern(rawTarget, `expected_write_footprint.targets[${index}]`));
      } catch (error) {
        blockers.push(error instanceof Error ? error.message : String(error));
      }
    }
  }
  if (new Set(targets).size !== targets.length)
    blockers.push("expected_write_footprint.targets must not contain duplicate path patterns.");
  if (kind === "bounded" && targets.length === 0)
    blockers.push("a bounded expected_write_footprint must list at least one target.");
  if (blockers.length > 0)
    return { footprint: null, blockers: [...new Set(blockers)] };
  return {
    footprint: {
      kind,
      targets,
      evidence_refs: [...new Set(rawEvidenceRefs.map((item) => item.trim()))],
      ...typeof reason === "string" && reason.trim().length > 0 ? { reason: reason.trim() } : {}
    },
    blockers: []
  };
}
function blockedCommandFootprintResult(scope, command, footprint, blockers) {
  return {
    status: "blocked",
    command,
    source_revision: scope.source_revision,
    expected_write_footprint: footprint,
    target_evaluations: [],
    admitted_targets: [],
    blocked_targets: Array.isArray(footprint?.targets) ? footprint.targets : [],
    blockers: [...new Set(blockers)]
  };
}
function evaluateCommandWriteFootprint(scope, input) {
  const command = typeof input?.command === "string" ? input.command.trim() : "";
  const rawFootprint = input?.expected_write_footprint;
  const fallbackFootprint = rawFootprint && typeof rawFootprint === "object" ? rawFootprint : { kind: "unbounded", targets: [], evidence_refs: [], reason: "missing expected write footprint" };
  const normalized = normalizeCommandWriteFootprint(fallbackFootprint);
  const normalizationBlockers = [...normalized.blockers];
  if (!command)
    normalizationBlockers.push("command must be a non-empty descriptor.");
  if (normalizationBlockers.length > 0 || !normalized.footprint) {
    return blockedCommandFootprintResult(scope, command, fallbackFootprint, normalizationBlockers);
  }
  const footprint = normalized.footprint;
  if (footprint.kind === "unbounded") {
    return blockedCommandFootprintResult(scope, command, footprint, [
      footprint.reason ?? "command write footprint is unbounded; the command must not execute.",
      "commands with repo-local writes require a bounded expected_write_footprint before execution."
    ]);
  }
  const targetEvaluations = [];
  const admittedTargets = [];
  const blockedTargets = [];
  const blockers = [];
  for (const target of footprint.targets) {
    const representativePaths = commandFootprintRepresentatives(target);
    const safetyBlockers = commandFootprintSafetyBlockers(scope, target);
    const results = representativePaths.map((representativePath) => evaluateMutationScope(scope, {
      changed_paths: [representativePath],
      ...input.conditional_authorizations ? { conditional_authorizations: input.conditional_authorizations } : {},
      ...input.transformation_kind ? { transformation_kind: input.transformation_kind } : {}
    }));
    const targetStatus = safetyBlockers.length === 0 && results.every((result) => result.status === "pass") ? "pass" : "blocked";
    targetEvaluations.push({ target, representative_paths: representativePaths, results, status: targetStatus });
    if (targetStatus === "pass") {
      admittedTargets.push(target);
    } else {
      blockedTargets.push(target);
      for (const blocker of safetyBlockers)
        blockers.push(`${target}: ${blocker}`);
      for (const result of results.filter((candidate) => candidate.status === "blocked")) {
        for (const blocker of result.blockers)
          blockers.push(`${target}: ${blocker}`);
      }
    }
  }
  return {
    status: blockers.length === 0 && blockedTargets.length === 0 ? "pass" : "blocked",
    command,
    source_revision: scope.source_revision,
    expected_write_footprint: footprint,
    target_evaluations: targetEvaluations,
    admitted_targets: admittedTargets,
    blocked_targets: blockedTargets,
    blockers: [...new Set(blockers)]
  };
}
function auditCommandMutation(scope, input) {
  const command = typeof input?.command === "string" ? input.command.trim() : "";
  const expected = evaluateCommandWriteFootprint(scope, input);
  const observedWritePaths = Array.isArray(input?.observed_write_paths) ? input.observed_write_paths : [];
  const cleanupPerformed = input?.cleanup?.performed === true;
  const cleanupPaths = Array.isArray(input?.cleanup?.paths) ? input.cleanup.paths.filter((path) => typeof path === "string").map((path) => path.trim()).filter(Boolean) : [];
  const auditedWritePaths = [...new Set([...observedWritePaths, ...cleanupPaths])];
  const cleanupBlockers = input?.cleanup !== undefined && typeof input.cleanup.performed !== "boolean" ? ["cleanup.performed must be a boolean when cleanup audit metadata is supplied."] : [];
  if (expected.status === "blocked") {
    return {
      status: "blocked",
      command,
      command_may_run: false,
      expected,
      observed: null,
      observed_status: "not-run",
      observed_write_paths: observedWritePaths,
      audited_write_paths: auditedWritePaths,
      unauthorized_observed_paths: [],
      cleanup_performed: cleanupPerformed,
      cleanup_paths: cleanupPaths,
      blockers: [...new Set([...expected.blockers, ...cleanupBlockers])],
      observation_limitations: ["Observed paths are caller-supplied; without OS-level filesystem instrumentation, transient create/delete history is not provable."]
    };
  }
  if (!Array.isArray(input?.observed_write_paths)) {
    return {
      status: "blocked",
      command,
      command_may_run: true,
      expected,
      observed: null,
      observed_status: "blocked",
      observed_write_paths: [],
      audited_write_paths: cleanupPaths,
      unauthorized_observed_paths: [],
      cleanup_performed: cleanupPerformed,
      cleanup_paths: cleanupPaths,
      blockers: ["observed_write_paths must be an array of repository-relative paths.", ...cleanupBlockers],
      observation_limitations: ["Observed paths are caller-supplied; without OS-level filesystem instrumentation, transient create/delete history is not provable."]
    };
  }
  const observed = auditedWritePaths.length > 0 ? evaluateMutationScope(scope, {
    changed_paths: auditedWritePaths,
    ...input.conditional_authorizations ? { conditional_authorizations: input.conditional_authorizations } : {},
    ...input.transformation_kind ? { transformation_kind: input.transformation_kind } : {}
  }) : null;
  const observedStatus = observed === null ? "no-writes-observed" : observed.status;
  const blockers = [...observed?.blockers ?? [], ...cleanupBlockers];
  return {
    status: blockers.length === 0 ? "pass" : "blocked",
    command,
    command_may_run: true,
    expected,
    observed,
    observed_status: observedStatus,
    observed_write_paths: observedWritePaths,
    audited_write_paths: auditedWritePaths,
    unauthorized_observed_paths: observed?.blocked_paths ?? [],
    cleanup_performed: cleanupPerformed,
    cleanup_paths: cleanupPaths,
    blockers: [...new Set(blockers)],
    observation_limitations: ["Observed paths are caller-supplied; without OS-level filesystem instrumentation, transient create/delete history is not provable."]
  };
}

// runtime/vnext/src/task-steps.ts
var STEP_ID_PATTERN = /^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/u;

class TaskStepDefinitionError extends Error {
  code;
  constructor(code, message) {
    super(`${code}: ${message}`);
    this.name = "TaskStepDefinitionError";
    this.code = code;
  }
}
var HEADING_ALIASES = new Set(["实施步骤", "implementation steps", "implementation_steps", "steps"]);
var METADATA_ALIASES = new Map([
  ["purpose", "purpose"],
  ["目标", "purpose"],
  ["目的", "purpose"],
  ["planned mutation targets", "planned_mutation_targets"],
  ["planned_mutation_targets", "planned_mutation_targets"],
  ["planned targets", "planned_mutation_targets"],
  ["计划修改目标", "planned_mutation_targets"],
  ["计划变更目标", "planned_mutation_targets"],
  ["mutation scope", "mutation_scope"],
  ["mutation_scope", "mutation_scope"],
  ["mutation boundary", "mutation_scope"],
  ["变更范围", "mutation_scope"],
  ["修改范围", "mutation_scope"],
  ["required evidence", "required_evidence"],
  ["required_evidence", "required_evidence"],
  ["必要证据", "required_evidence"],
  ["必需证据", "required_evidence"],
  ["review checkpoint", "review_checkpoint"],
  ["review_checkpoint", "review_checkpoint"],
  ["审查检查点", "review_checkpoint"],
  ["评审检查点", "review_checkpoint"]
]);
function normalizeLabel(value) {
  return value.replace(/`/gu, "").trim().toLocaleLowerCase().replace(/[\u00a0]/gu, " ").replace(/\s+/gu, " ");
}
function headingInfo(line) {
  const match = /^(#{1,6})\s+(.+?)\s*$/u.exec(line);
  if (!match)
    return null;
  return { level: match[1].length, title: normalizeLabel(match[2]) };
}
function stepSectionLines(body) {
  const lines = body.replace(/\r\n?/gu, `
`).split(`
`);
  const start = lines.findIndex((line) => {
    const heading = headingInfo(line);
    return heading?.level === 2 && HEADING_ALIASES.has(heading.title);
  });
  if (start < 0)
    throw new TaskStepDefinitionError("TASK_STEPS_INVALID", "CURRENT_TASK is missing the implementation steps section.");
  let end = lines.length;
  for (let index = start + 1;index < lines.length; index += 1) {
    const heading = headingInfo(lines[index]);
    if (heading && heading.level <= 2) {
      end = index;
      break;
    }
  }
  return lines.slice(start + 1, end);
}
function metadataKey(value) {
  const normalized = normalizeLabel(value);
  return METADATA_ALIASES.get(normalized) ?? METADATA_ALIASES.get(normalized.replace(/\s+/gu, "_")) ?? null;
}
function cleanMetadataValue(value) {
  return value.trim().replace(/^\s*[-–—:]\s*/u, "").trim();
}
function parseCheckpoint(value) {
  const normalized = cleanMetadataValue(value);
  const optional = /^(?:not[-\s]?required|optional|none|无需|不需要|非必需)(?=\s|[:：,，()（）[\]{}\-–—]|$)/iu.exec(normalized);
  if (optional)
    return { policy: "not-required", boundary: normalized.slice(optional[0].length).replace(/^[\s:：]+/u, "").trim() || null };
  const required = /^(?:required|mandatory|必需|必须|需要)(?=\s|[:：,，()（）[\]{}\-–—]|$)/iu.exec(normalized);
  if (!required)
    return { policy: null, boundary: null };
  const boundary = normalized.slice(required[0].length).replace(/^[\s:：,，()（）[\]{}\-–—]+/u, "").replace(/[\s,，()（）[\]{}\-–—]+$/u, "").trim();
  return { policy: "required", boundary: boundary || null };
}
function stepLine(line) {
  const match = /^(\s*)[-*]\s+(?:\[[ xX]\]\s*)?(?:(?:步骤|step)\s+([0-9]+)|([A-Za-z0-9][A-Za-z0-9._:-]{0,127}))\s*[:：]\s*(.*?)\s*$/iu.exec(line);
  if (!match)
    return null;
  const candidate = match[2] ? `step-${match[2]}` : match[3];
  if (!candidate || !STEP_ID_PATTERN.test(candidate) || metadataKey(candidate) !== null)
    return null;
  return { indent: match[1].length, id: candidate, description: match[4].trim() };
}
function metadataLine(line) {
  const match = /^\s*(?:[-*]\s+)?(?:\[[ xX]\]\s*)?([^:：]+?)\s*[:：]\s*(.*?)\s*$/u.exec(line);
  if (!match)
    return null;
  const key = metadataKey(match[1]);
  return key ? { key, value: cleanMetadataValue(match[2]) } : null;
}
function parseRawSteps(lines) {
  const parsed = [];
  let primaryIndent = null;
  let current = null;
  for (const line of lines) {
    const candidate = stepLine(line);
    if (candidate && (primaryIndent === null || candidate.indent <= primaryIndent)) {
      if (primaryIndent === null)
        primaryIndent = candidate.indent;
      if (candidate.indent === primaryIndent) {
        if (parsed.some((step) => step.id === candidate.id)) {
          throw new TaskStepDefinitionError("TASK_STEPS_INVALID", `implementation steps contain duplicate step ID ${candidate.id}.`);
        }
        current = { id: candidate.id, description: candidate.description, lines: [line], metadata: {} };
        parsed.push(current);
        continue;
      }
    }
    if (!current)
      continue;
    current.lines.push(line);
    const metadata = metadataLine(line);
    if (!metadata)
      continue;
    if (current.metadata[metadata.key] !== undefined) {
      throw new TaskStepDefinitionError("TASK_STEPS_INVALID", `step ${current.id} declares ${metadata.key} more than once.`);
    }
    current.metadata[metadata.key] = metadata.value;
  }
  if (parsed.length === 0) {
    throw new TaskStepDefinitionError("TASK_STEPS_INVALID", "implementation steps must contain at least one labelled step ID.");
  }
  return parsed;
}
function materializeStep(step) {
  const purpose = step.metadata.purpose || null;
  const plannedMutationTargets = step.metadata.planned_mutation_targets || step.metadata.mutation_scope || null;
  const mutationScope = step.metadata.mutation_scope || plannedMutationTargets;
  const requiredEvidence = step.metadata.required_evidence || null;
  const checkpoint = step.metadata.review_checkpoint === undefined ? { policy: null, boundary: null } : parseCheckpoint(step.metadata.review_checkpoint);
  const metadataComplete = Boolean(purpose && plannedMutationTargets && requiredEvidence && checkpoint.policy && (checkpoint.policy === "not-required" || checkpoint.boundary));
  return {
    id: step.id,
    description: step.description,
    plan_text: step.lines.join(`
`).trim() || null,
    purpose,
    planned_mutation_targets: plannedMutationTargets,
    mutation_scope: mutationScope,
    required_evidence: requiredEvidence,
    review_checkpoint: checkpoint.policy,
    checkpoint_boundary: checkpoint.boundary,
    metadata_complete: metadataComplete
  };
}
function parseImplementationSteps(implementationSteps) {
  const lines = implementationSteps.replace(/\r\n?/gu, `
`).split(`
`);
  return parseRawSteps(lines).map(materializeStep);
}
function parseTaskStepDefinitions(body) {
  return parseRawSteps(stepSectionLines(body)).map(materializeStep);
}
function resolveTaskStep(body, activeStepId) {
  const steps = parseTaskStepDefinitions(body);
  const index = steps.findIndex((step) => step.id === activeStepId);
  if (index < 0) {
    throw new TaskStepDefinitionError("TASK_STEP_NOT_FOUND", `active_step_id ${activeStepId} is not declared in implementation steps.`);
  }
  return {
    steps,
    current: steps[index],
    index,
    next: steps[index + 1] ?? null
  };
}

// runtime/vnext/src/mutation-authority.ts
import * as crypto8 from "crypto";
import * as fs9 from "fs";
import * as path10 from "path";
var MUTATION_AUTHORITY_VERSION = 2;

class MutationAuthorityError extends Error {
  code;
  constructor(code, message) {
    super(`${code}: ${message}`);
    this.name = "MutationAuthorityError";
    this.code = code;
  }
}
var DOMAIN_ID_PATTERN = /^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/u;
var MAX_DOMAINS = 128;
var MAX_ROOTS_PER_DOMAIN = 128;
var MAX_EXCEPTIONS = 256;
var MAX_FORBIDDEN = 256;
function isMutationAuthorityGovernanceBoundary(target) {
  return target === ".git" || target.startsWith(".git/") || target === ".workflow-system" || target.startsWith(".workflow-system/") || target === ".agents" || target.startsWith(".agents/") || target === ".claude" || target.startsWith(".claude/") || target === ".codex" || target.startsWith(".codex/") || target === "node_modules" || target.startsWith("node_modules/") || target === "docs/workflow/CURRENT_TASK.md" || target.startsWith("runtime/vnext/dist/") || target.startsWith("packages/vibe-governance/") || target.includes("/task-history/") || target.includes("/task-candidates/") || target.includes("/task-data/");
}
function fail(code, message) {
  throw new MutationAuthorityError(code, message);
}
function record3(value, location) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    fail("MUTATION_AUTHORITY_SCHEMA_INVALID", `${location} must be a mapping.`);
  }
  return value;
}
function exactKeys(value, expected, location) {
  const expectedSet = new Set(expected);
  const missing = expected.filter((key) => !(key in value));
  const extra = Object.keys(value).filter((key) => !expectedSet.has(key));
  if (missing.length > 0 || extra.length > 0) {
    fail("MUTATION_AUTHORITY_SCHEMA_INVALID", `${location} keys mismatch; missing=[${missing.join(", ")}], unexpected=[${extra.join(", ")}].`);
  }
}
function text(value, location, maxLength = 4096) {
  if (typeof value !== "string")
    fail("MUTATION_AUTHORITY_SCHEMA_INVALID", `${location} must be a string.`);
  const normalized = value.trim();
  if (!normalized || normalized.length > maxLength || /[\r\n\0]/u.test(normalized)) {
    fail("MUTATION_AUTHORITY_SCHEMA_INVALID", `${location} must be one non-empty line of at most ${maxLength} characters.`);
  }
  return normalized;
}
function normalizeAuthorityPath(value, location, allowBoundedGlob = false) {
  const original = text(value, location, 1024);
  const normalized = original.replace(/\\/gu, "/").replace(/^\.\//u, "").replace(/\/+$/u, "");
  const boundedGlob = normalized.endsWith("/**") && !normalized.slice(0, -3).includes("*");
  if (!normalized || normalized === "." || normalized.startsWith("/") || /^[A-Za-z]:\//u.test(original) || normalized.split("/").some((segment) => segment === "." || segment === ".." || segment.length === 0) || /[\0-\x1F\x7F]/u.test(normalized) || normalized.includes("**/") || normalized.includes("*") && (!allowBoundedGlob || !boundedGlob)) {
    fail("MUTATION_AUTHORITY_PATH_INVALID", `${location} must be a repository-relative exact path or literal /** prefix.`);
  }
  return normalized;
}
function stringList(value, location, options) {
  if (!Array.isArray(value) || value.length > options.max || !options.allowEmpty && value.length === 0) {
    fail("MUTATION_AUTHORITY_SCHEMA_INVALID", `${location} must be a bounded${options.allowEmpty ? "" : " non-empty"} array.`);
  }
  const values = value.map((item, index) => normalizeAuthorityPath(item, `${location}[${index}]`, options.allowBoundedGlob === true));
  if (new Set(values).size !== values.length)
    fail("MUTATION_AUTHORITY_SCHEMA_INVALID", `${location} contains duplicates.`);
  return values;
}
function boundaryOverlap(left, right) {
  const a = canonicalAuthorityRoot(left);
  const b = canonicalAuthorityRoot(right);
  return a === b || a.startsWith(`${b}/`) || b.startsWith(`${a}/`);
}
function canonicalAuthorityRoot(value) {
  return value.endsWith("/**") ? value.slice(0, -3) : value;
}
function profileAuthorityValue(profile) {
  return profile.mutation_authority;
}
function normalizeProjectMutationAuthority(value) {
  const authority = record3(value, "PROJECT_PROFILE.yaml.mutation_authority");
  exactKeys(authority, ["domains"], "PROJECT_PROFILE.yaml.mutation_authority");
  if (!Array.isArray(authority.domains) || authority.domains.length === 0 || authority.domains.length > MAX_DOMAINS) {
    fail("MUTATION_AUTHORITY_SCHEMA_INVALID", "PROJECT_PROFILE.yaml.mutation_authority.domains must be a bounded non-empty array.");
  }
  const domains = authority.domains.map((rawDomain, index) => {
    const domain = record3(rawDomain, `PROJECT_PROFILE.yaml.mutation_authority.domains[${index}]`);
    exactKeys(domain, ["id", "roots"], `PROJECT_PROFILE.yaml.mutation_authority.domains[${index}]`);
    const id = text(domain.id, `PROJECT_PROFILE.yaml.mutation_authority.domains[${index}].id`, 128);
    if (!DOMAIN_ID_PATTERN.test(id))
      fail("MUTATION_AUTHORITY_SCHEMA_INVALID", `authority domain id ${id} is invalid.`);
    const roots = stringList(domain.roots, `PROJECT_PROFILE.yaml.mutation_authority.domains[${index}].roots`, {
      allowEmpty: false,
      max: MAX_ROOTS_PER_DOMAIN,
      allowBoundedGlob: true
    });
    return { id, roots };
  });
  if (new Set(domains.map((domain) => domain.id)).size !== domains.length) {
    fail("MUTATION_AUTHORITY_SCHEMA_INVALID", "authority domain ids must be unique.");
  }
  for (let leftIndex = 0;leftIndex < domains.length; leftIndex += 1) {
    for (let rightIndex = leftIndex + 1;rightIndex < domains.length; rightIndex += 1) {
      for (const leftRoot of domains[leftIndex].roots) {
        for (const rightRoot of domains[rightIndex].roots) {
          if (boundaryOverlap(leftRoot, rightRoot)) {
            fail("MUTATION_AUTHORITY_DOMAIN_AMBIGUOUS", `authority roots overlap: ${domains[leftIndex].id}:${leftRoot} <-> ${domains[rightIndex].id}:${rightRoot}.`);
          }
        }
      }
    }
  }
  return { domains };
}
function readProjectMutationAuthority(root2) {
  const profilePath = getWorkflowProfilePath(root2);
  if (!fs9.existsSync(profilePath))
    return null;
  let profile;
  try {
    profile = loadProfile(profilePath);
  } catch (error) {
    fail("MUTATION_AUTHORITY_PROJECT_INVALID", error instanceof Error ? error.message : String(error));
  }
  const raw = profileAuthorityValue(profile);
  if (raw === undefined)
    return null;
  try {
    return normalizeProjectMutationAuthority(raw);
  } catch (error) {
    if (error instanceof MutationAuthorityError && error.code === "MUTATION_AUTHORITY_SCHEMA_INVALID") {
      fail("MUTATION_AUTHORITY_PROJECT_INVALID", error.message);
    }
    throw error;
  }
}
function normalizeTaskMutationAuthority(value) {
  const authority = record3(value, "mutation_authority");
  exactKeys(authority, ["domains", "exact_exceptions", "forbidden"], "mutation_authority");
  const domains = authority.domains;
  if (!Array.isArray(domains) || domains.length === 0 || domains.length > MAX_DOMAINS) {
    fail("MUTATION_AUTHORITY_SCHEMA_INVALID", "mutation_authority.domains must be a bounded non-empty array.");
  }
  const domainIds = domains.map((item, index) => text(item, `mutation_authority.domains[${index}]`, 128));
  if (domainIds.some((id) => !DOMAIN_ID_PATTERN.test(id)) || new Set(domainIds).size !== domainIds.length) {
    fail("MUTATION_AUTHORITY_SCHEMA_INVALID", "mutation_authority.domains must contain unique valid domain ids.");
  }
  return {
    domains: domainIds,
    exact_exceptions: stringList(authority.exact_exceptions, "mutation_authority.exact_exceptions", { allowEmpty: true, max: MAX_EXCEPTIONS }),
    forbidden: stringList(authority.forbidden, "mutation_authority.forbidden", { allowEmpty: true, max: MAX_FORBIDDEN, allowBoundedGlob: true })
  };
}
function validateTaskMutationAuthority(root2, value) {
  const authority = normalizeTaskMutationAuthority(value);
  const project = readProjectMutationAuthority(root2);
  if (!project)
    fail("MUTATION_AUTHORITY_PROJECT_REQUIRED", "v2 tasks require PROJECT_PROFILE.yaml.mutation_authority.domains.");
  const known = new Set(project.domains.map((domain) => domain.id));
  const missing = authority.domains.filter((domain) => !known.has(domain));
  if (missing.length > 0)
    fail("MUTATION_AUTHORITY_DOMAIN_UNKNOWN", `task authority references unknown domains: ${missing.join(", ")}.`);
  return authority;
}
function authorityDomainForPath(project, target) {
  const matches = project.domains.filter((domain) => domain.roots.some((root2) => {
    if (root2.endsWith("/**"))
      return target === root2.slice(0, -3) || target.startsWith(`${root2.slice(0, -3)}/`);
    return target === root2;
  }));
  if (matches.length > 1)
    fail("MUTATION_AUTHORITY_DOMAIN_AMBIGUOUS", `path ${target} maps to multiple authority domains: ${matches.map((domain) => domain.id).join(", ")}.`);
  return matches[0]?.id ?? null;
}
function projectMutationAuthorityRevision(project) {
  const canonical = {
    version: MUTATION_AUTHORITY_VERSION,
    domains: project.domains.map((domain) => ({ id: domain.id, roots: [...domain.roots].sort() })).sort((left, right) => left.id.localeCompare(right.id))
  };
  return crypto8.createHash("sha256").update(JSON.stringify(canonical)).digest("hex");
}
function mutationAuthorityPatternIsSubset(candidate, boundary) {
  const normalizedCandidate = normalizeAuthorityPath(candidate, "authority candidate pattern", true);
  const normalizedBoundary = normalizeAuthorityPath(boundary, "authority boundary pattern", true);
  if (normalizedCandidate === normalizedBoundary)
    return true;
  if (normalizedCandidate.endsWith("/**")) {
    if (!normalizedBoundary.endsWith("/**"))
      return false;
    const candidatePrefix = normalizedCandidate.slice(0, -3);
    const boundaryPrefix = normalizedBoundary.slice(0, -3);
    return candidatePrefix === boundaryPrefix || candidatePrefix.startsWith(`${boundaryPrefix}/`);
  }
  return mutationScopePatternMatchesPath(normalizedCandidate, normalizedBoundary);
}
function mutationAuthorityPatternsOverlap(left, right) {
  const normalizedLeft = normalizeAuthorityPath(left, "authority pattern", true);
  const normalizedRight = normalizeAuthorityPath(right, "authority pattern", true);
  if (normalizedLeft.endsWith("/**") && normalizedRight.endsWith("/**")) {
    const leftPrefix = normalizedLeft.slice(0, -3);
    const rightPrefix = normalizedRight.slice(0, -3);
    return leftPrefix === rightPrefix || leftPrefix.startsWith(`${rightPrefix}/`) || rightPrefix.startsWith(`${leftPrefix}/`);
  }
  if (normalizedLeft.endsWith("/**"))
    return mutationScopePatternMatchesPath(normalizedRight, normalizedLeft);
  if (normalizedRight.endsWith("/**"))
    return mutationScopePatternMatchesPath(normalizedLeft, normalizedRight);
  return normalizedLeft === normalizedRight;
}
var AUTHORITY_GOVERNANCE_PATTERNS = [
  ".git/**",
  ".workflow-system/**",
  ".agents/**",
  ".claude/**",
  ".codex/**",
  "node_modules/**",
  "docs/workflow/**",
  "runtime/vnext/dist/**",
  "packages/vibe-governance/**"
];
function mutationAuthorityPlanBlockerCode(decision) {
  if (decision.reason === "target is explicitly forbidden")
    return "MUTATION_AUTHORITY_FORBIDDEN";
  if (decision.reason === "target overlaps a Runtime/governance boundary")
    return "MUTATION_AUTHORITY_GOVERNANCE_BOUNDARY";
  if (decision.kind === "command-write")
    return "COMMAND_FOOTPRINT_AUTHORITY_BLOCKED";
  return "MUTATION_AUTHORITY_PLANNING_BLOCKED";
}
function projectDomainsContainingPattern(project, pattern) {
  if (!pattern.endsWith("/**")) {
    const domain = authorityDomainForPath(project, pattern);
    return domain === null ? [] : project.domains.filter((item) => item.id === domain);
  }
  return project.domains.filter((domain) => domain.roots.some((root2) => mutationAuthorityPatternIsSubset(pattern, root2)));
}
function evaluateTaskAuthorityPattern(project, task, rawPattern, kind) {
  const pattern = normalizeAuthorityPath(rawPattern, `${kind} authority pattern`, true);
  if (task.forbidden.some((forbidden) => mutationAuthorityPatternsOverlap(pattern, forbidden))) {
    return { kind, path: pattern, admitted: false, domain: null, reason: "target is explicitly forbidden" };
  }
  if (AUTHORITY_GOVERNANCE_PATTERNS.some((governance) => mutationAuthorityPatternsOverlap(pattern, governance))) {
    return { kind, path: pattern, admitted: false, domain: null, reason: "target overlaps a Runtime/governance boundary" };
  }
  const exactException = !pattern.endsWith("/**") && task.exact_exceptions.includes(pattern);
  const containingDomains = projectDomainsContainingPattern(project, pattern);
  if (exactException) {
    return { kind, path: pattern, admitted: true, domain: containingDomains[0]?.id ?? null, reason: "exact exception is explicitly authorized" };
  }
  if (containingDomains.length === 0) {
    return { kind, path: pattern, admitted: false, domain: null, reason: pattern.endsWith("/**") ? "directory footprint is not contained by one project authority root" : "target is unclassified" };
  }
  const domain = containingDomains[0];
  if (!task.domains.includes(domain.id)) {
    return { kind, path: pattern, admitted: false, domain: domain.id, reason: `target belongs to unauthorized domain ${domain.id}` };
  }
  return { kind, path: pattern, admitted: true, domain: domain.id, reason: "target is contained by the task authority envelope" };
}
function evaluateTaskMutationAuthorityPlan(input) {
  const declarations = [
    ...input.planned_targets.map((path) => ({ kind: "planned-target", path })),
    ...input.command_write_targets.map((path) => ({ kind: "command-write", path })),
    ...input.persistent_test_paths.map((path) => ({ kind: "persistent-test", path }))
  ];
  const decisions = declarations.map((item) => evaluateTaskAuthorityPattern(input.project, input.task, item.path, item.kind));
  const blockers = decisions.filter((item) => !item.admitted).map((item) => `${item.kind}: ${item.path} — ${item.reason}`);
  return { status: blockers.length === 0 ? "pass" : "blocked", decisions, blockers };
}
function firstTouchState(root2, target) {
  if (!root2)
    return;
  const absolute = path10.resolve(root2, ...target.split("/"));
  if (!fs9.existsSync(absolute))
    return "absent";
  const stat = fs9.lstatSync(absolute);
  if (stat.isSymbolicLink())
    return "symlink";
  return stat.isFile() ? "file" : undefined;
}
function validateAssessment(value, index) {
  const assessment = record3(value, `blast_radius_assessments[${index}]`);
  exactKeys(assessment, ["target", "reason", "blast_radius", "evidence_refs", "disposition"], `blast_radius_assessments[${index}]`);
  const target = record3(assessment.target, `blast_radius_assessments[${index}].target`);
  exactKeys(target, ["path", ...target.symbol === undefined ? [] : ["symbol"]], `blast_radius_assessments[${index}].target`);
  const blast = record3(assessment.blast_radius, `blast_radius_assessments[${index}].blast_radius`);
  exactKeys(blast, ["locality", "visibility", "cross_component_consumers", "contract_impact"], `blast_radius_assessments[${index}].blast_radius`);
  const locality = blast.locality;
  const visibility = blast.visibility;
  const consumers = blast.cross_component_consumers;
  const contractImpact = blast.contract_impact;
  if (!["local", "elevated", "high"].includes(String(locality)) || !["private", "shared", "public", "unknown"].includes(String(visibility)) || !["none", "present", "unknown"].includes(String(consumers)) || !["none", "possible", "known"].includes(String(contractImpact))) {
    fail("MUTATION_AUTHORITY_ASSESSMENT_INVALID", `blast_radius_assessments[${index}].blast_radius contains an unsupported classification.`);
  }
  if (!Array.isArray(assessment.evidence_refs) || assessment.evidence_refs.length === 0 || assessment.evidence_refs.length > 64 || assessment.evidence_refs.some((value) => typeof value !== "string" || !value.trim())) {
    fail("MUTATION_AUTHORITY_ASSESSMENT_INVALID", `blast_radius_assessments[${index}].evidence_refs must be non-empty.`);
  }
  const normalizedPath = normalizeAuthorityPath(target.path, `blast_radius_assessments[${index}].target.path`);
  return {
    target: {
      path: normalizedPath,
      ...target.symbol === undefined ? {} : { symbol: text(target.symbol, `blast_radius_assessments[${index}].target.symbol`, 256) }
    },
    reason: text(assessment.reason, `blast_radius_assessments[${index}].reason`, 4096),
    blast_radius: {
      locality,
      visibility,
      cross_component_consumers: consumers,
      contract_impact: contractImpact
    },
    evidence_refs: [...new Set(assessment.evidence_refs.map((value) => value.trim()))],
    disposition: assessmentDisposition(assessment.disposition, index)
  };
}
function assessmentDisposition(value, index) {
  if (value === "self-admit" || value === "escalate")
    return value;
  fail("MUTATION_AUTHORITY_ASSESSMENT_INVALID", `blast_radius_assessments[${index}].disposition must be self-admit or escalate.`);
}
function normalizeBlastRadiusAssessments(value) {
  if (value === undefined)
    return [];
  if (!Array.isArray(value) || value.length > 256)
    fail("MUTATION_AUTHORITY_ASSESSMENT_INVALID", "blast_radius_assessments must be a bounded array.");
  const result = value.map((item, index) => validateAssessment(item, index));
  if (new Set(result.map((item) => item.target.path)).size !== result.length)
    fail("MUTATION_AUTHORITY_ASSESSMENT_INVALID", "blast_radius_assessments must contain one assessment per target path.");
  return result;
}
function requiresDynamicReview(assessment) {
  const radius = assessment.blast_radius;
  return assessment.disposition !== "self-admit" || radius.locality !== "local" || radius.visibility !== "private" || radius.cross_component_consumers !== "none" || radius.contract_impact !== "none";
}
function evaluateMutationAuthority(input) {
  const assessments = input.assessments ?? [];
  const byPath = new Map(assessments.map((item) => [item.target.path, item]));
  const decisions = [];
  const blockers = [];
  let dynamicReviewRequired = false;
  for (const rawPath of input.candidate_paths) {
    const target = normalizeAuthorityPath(rawPath, "candidate_paths");
    const forbidden = input.task.forbidden.some((pattern) => mutationScopePatternMatchesPath(target, pattern));
    const touch = firstTouchState(input.root, target);
    if (forbidden) {
      decisions.push({ path: target, status: "blocked", domain: null, reason: "target is explicitly forbidden", first_touch_state: touch });
      blockers.push(`MUTATION_AUTHORITY_FORBIDDEN: ${target}`);
      continue;
    }
    if (isMutationAuthorityGovernanceBoundary(target)) {
      decisions.push({ path: target, status: "blocked", domain: null, reason: "target is a Runtime/governance boundary", first_touch_state: touch });
      blockers.push(`MUTATION_AUTHORITY_GOVERNANCE_BOUNDARY: ${target}`);
      continue;
    }
    const domain = authorityDomainForPath(input.project, target);
    const exception = input.task.exact_exceptions.includes(target);
    const inEnvelope = exception || domain !== null && input.task.domains.includes(domain);
    if (!inEnvelope) {
      decisions.push({ path: target, status: "blocked", domain, reason: domain === null ? "target is unclassified" : `target belongs to unauthorized domain ${domain}`, first_touch_state: touch });
      blockers.push(`MUTATION_AUTHORITY_EXPANSION_REQUIRED: ${target}${domain ? ` (${domain})` : ""}`);
      continue;
    }
    if (touch === "symlink" || touch === undefined) {
      decisions.push({ path: target, status: "blocked", domain, reason: "target is not a regular file or absent path", first_touch_state: touch });
      blockers.push(`MUTATION_AUTHORITY_TARGET_INVALID: ${target} is not a regular file or absent path`);
      continue;
    }
    const planned = input.planned_targets.some((pattern) => mutationScopePatternMatchesPath(target, pattern));
    const assessment = byPath.get(target);
    const persistentAllowed = (input.persistent_test_paths ?? []).includes(target);
    const persistentTest = persistentAllowed || isLikelyPersistentTestPath(target);
    if (persistentTest && touch === "absent" && !persistentAllowed) {
      decisions.push({ path: target, status: "blocked", domain, reason: "new persistent test lacks P-12 admission", first_touch_state: touch });
      blockers.push(`PERSISTENT_TEST_UNADMITTED: ${target}`);
      continue;
    }
    if (planned) {
      decisions.push({ path: target, status: "planned", domain, reason: exception ? "exact exception is planned" : "target is in planned mutation footprint", first_touch_state: touch });
      continue;
    }
    if (!assessment) {
      decisions.push({ path: target, status: "blocked", domain, reason: "in-envelope footprint expansion has no blast-radius assessment", first_touch_state: touch });
      blockers.push(`MUTATION_AUTHORITY_EXPANSION_REQUIRED: ${target} requires blast-radius assessment`);
      continue;
    }
    if (assessment.disposition !== "self-admit") {
      decisions.push({ path: target, status: "blocked", domain, reason: "blast-radius assessment escalates the target", assessment, first_touch_state: touch });
      blockers.push(`MUTATION_AUTHORITY_EXPANSION_REQUIRED: ${target} was escalated by its blast-radius assessment`);
      continue;
    }
    dynamicReviewRequired ||= requiresDynamicReview(assessment);
    decisions.push({ path: target, status: "self-admitted", domain, reason: "in-envelope expansion self-admitted with assessment", assessment, first_touch_state: touch });
  }
  return { status: blockers.length === 0 ? "pass" : "blocked", decisions, dynamic_review_required: dynamicReviewRequired, blockers };
}

// runtime/vnext/src/execution-admission.ts
var EXECUTION_ADMISSION_CLASSIFICATIONS = [
  "planned-admitted",
  "dynamic-self-admitted",
  "persistent-test-admitted",
  "blocked-authority",
  "blocked-assessment",
  "blocked-test-strategy",
  "blocked-persistent-test",
  "blocked-non-executable-policy",
  "blocked-governance"
];
function blocked(path, classification, reason, domain = null, firstTouchState, assessment) {
  return {
    path,
    classification,
    admitted: false,
    dynamic_review_required: false,
    domain,
    reason,
    ...firstTouchState === undefined ? {} : { first_touch_state: firstTouchState },
    ...assessment === undefined ? {} : { assessment }
  };
}
function admitted(path, classification, reason, domain, firstTouchState, dynamicReviewRequired = false, assessment) {
  return {
    path,
    classification,
    admitted: true,
    dynamic_review_required: dynamicReviewRequired,
    domain,
    reason,
    ...firstTouchState === undefined ? {} : { first_touch_state: firstTouchState },
    ...assessment === undefined ? {} : { assessment }
  };
}
function blockerForDecision(target, decision, blockers) {
  const firstTouchState = decision?.first_touch_state;
  const domain = decision?.domain ?? null;
  const assessment = decision?.assessment;
  const matchingBlocker = blockers.find((item) => item.includes(`: ${target}`)) ?? blockers.find((item) => item.includes(target));
  if (matchingBlocker?.startsWith("MUTATION_AUTHORITY_FORBIDDEN") || matchingBlocker?.startsWith("MUTATION_AUTHORITY_GOVERNANCE_BOUNDARY")) {
    return blocked(target, "blocked-governance", decision?.reason ?? matchingBlocker, domain, firstTouchState, assessment);
  }
  if (matchingBlocker?.startsWith("PERSISTENT_TEST_UNADMITTED")) {
    return blocked(target, "blocked-persistent-test", decision?.reason ?? matchingBlocker, domain, firstTouchState, assessment);
  }
  if (decision?.reason.includes("blast-radius assessment") || decision?.reason.includes("assessment escalates")) {
    return blocked(target, "blocked-assessment", decision.reason, domain, firstTouchState, assessment);
  }
  return blocked(target, "blocked-authority", decision?.reason ?? matchingBlocker ?? "target is not admitted by the current mutation authority", domain, firstTouchState, assessment);
}
function strategyAdmission(input, target) {
  if (input.phase === "not-applicable") {
    const boundaries = input.non_executable_change_paths ?? [];
    if (boundaries.length === 0 || !boundaries.some((boundary) => {
      try {
        return mutationScopePatternMatchesPath(target, boundary);
      } catch {
        return false;
      }
    })) {
      return blocked(target, "blocked-non-executable-policy", "not-applicable execution may mutate only the project non-executable boundary.");
    }
  }
  if (input.phase === "red" && !(input.persistent_test_paths ?? []).includes(target)) {
    return blocked(target, "blocked-test-strategy", "test-first Red execution admits only frozen Persistent Tests; product targets require the later execution phase.");
  }
  return null;
}
function evaluateV1(input, target) {
  const scope = input.legacy_scope;
  if (!scope)
    return blocked(target, "blocked-authority", "v1 execution admission requires the canonical mutation scope.");
  const result = evaluateMutationScope(scope, { changed_paths: [target] });
  const decision = result.decisions[0];
  if (result.status !== "pass" || !decision?.mutation_admitted) {
    const classification = decision?.classification === "persistent-test-unadmitted" ? "blocked-persistent-test" : decision?.classification === "forbidden" ? "blocked-governance" : "blocked-authority";
    return blocked(target, classification, decision?.reason ?? (result.blockers.join(" ") || "target is outside v1 task scope."));
  }
  if (!input.step_mutation_scope.some((pattern) => mutationScopePatternMatchesPath(target, pattern))) {
    return blocked(target, "blocked-authority", "target is outside the current v1 step mutation scope.");
  }
  const persistent = isLikelyPersistentTestPath(target) || input.persistent_test_paths?.includes(target) === true;
  return admitted(target, persistent && input.persistent_test_paths?.includes(target) === true ? "persistent-test-admitted" : "planned-admitted", "target is admitted by the v1 task and current-step scope.", null, undefined);
}
function evaluateV2(input, target) {
  const project = input.project_authority;
  const task = input.task_authority;
  if (!project || !task)
    return blocked(target, "blocked-authority", "v2 execution admission requires the project domain map and task authority envelope.");
  const result = evaluateMutationAuthority({
    root: input.root,
    project,
    task,
    candidate_paths: [target],
    planned_targets: input.step_planned_targets,
    assessments: input.assessments ?? [],
    persistent_test_paths: input.persistent_test_paths ?? []
  });
  const decision = result.decisions[0];
  if (result.status !== "pass" || !decision || decision.status === "blocked") {
    return blockerForDecision(target, decision, result.blockers);
  }
  const isNewPersistentTest = (input.persistent_test_paths ?? []).includes(target) && decision.first_touch_state === "absent";
  const classification = decision.status === "self-admitted" ? "dynamic-self-admitted" : isNewPersistentTest ? "persistent-test-admitted" : "planned-admitted";
  return admitted(target, classification, decision.reason, decision.domain, decision.first_touch_state, result.dynamic_review_required, decision.assessment);
}
function evaluateExecutionTargetAdmission(input) {
  let target;
  try {
    target = input.version === 2 ? normalizeAuthorityPath(input.target, "execution admission target") : input.target;
  } catch (error) {
    return blocked(input.target, "blocked-authority", error instanceof Error ? error.message : String(error));
  }
  const explicitlyForbidden = input.task_authority?.forbidden.some((pattern) => {
    try {
      return mutationScopePatternMatchesPath(target, pattern);
    } catch {
      return false;
    }
  }) ?? input.legacy_scope?.forbidden.some((pattern) => {
    try {
      return mutationScopePatternMatchesPath(target, pattern.pattern);
    } catch {
      return false;
    }
  }) ?? false;
  if (explicitlyForbidden) {
    return blocked(target, "blocked-governance", "target is explicitly forbidden by the current task.");
  }
  if (isMutationAuthorityGovernanceBoundary(target) || input.governance_paths?.includes(target)) {
    return blocked(target, "blocked-governance", "target is a Runtime/governance boundary.");
  }
  const strategyBlock = strategyAdmission(input, target);
  if (strategyBlock)
    return strategyBlock;
  return input.version === 2 ? evaluateV2(input, target) : evaluateV1(input, target);
}
function evaluateExecutionTargetAdmissions(input) {
  const decisions = input.targets.map((target) => evaluateExecutionTargetAdmission({ ...input, target }));
  const blockers = decisions.filter((decision) => !decision.admitted).map((decision) => `${decision.classification}: ${decision.path} — ${decision.reason}`);
  if (input.phase === "red") {
    const persistentTests = input.persistent_test_paths ?? [];
    const missingTests = persistentTests.filter((testPath) => !input.targets.includes(testPath));
    if (missingTests.length > 0) {
      blockers.push(`blocked-test-strategy: Red execution must include every frozen persistent test: ${missingTests.join(", ")}`);
    }
  }
  return {
    status: blockers.length === 0 ? "pass" : "blocked",
    decisions,
    dynamic_review_required: decisions.some((decision) => decision.dynamic_review_required),
    blockers
  };
}

// runtime/vnext/src/bootstrap.ts
import * as crypto9 from "crypto";
import * as fs10 from "fs";
import * as path11 from "path";
var VNEXT_BOOTSTRAP_PROPOSAL_SCHEMA_VERSION = 1;
var VNEXT_BOOTSTRAP_PROPOSAL_KIND = "vnext-bootstrap-proposal";
var BOOTSTRAP_MODES = ["design", "greenfield", "inventory", "adopt", "realign"];
var BOOTSTRAP_OPERATION_KINDS = [
  "contract-candidate-commit",
  "decision-record-transaction",
  "project-status-transaction",
  "paired-host-guidance-transaction"
];
var BOOTSTRAP_ASSET_CATEGORIES = ["config", "generated", "governance"];
var SAFE_KEY_PATTERN = /^[A-Za-z0-9][A-Za-z0-9._:-]{2,127}$/u;
var TARGET_IDENTITY_PATTERN = /^[a-f0-9]{32}$/u;
var SHA256_PATTERN = /^[a-f0-9]{64}$/u;

class BootstrapRuntimeError extends Error {
  code;
  constructor(code, message) {
    super(`${code}: ${message}`);
    this.name = "BootstrapRuntimeError";
    this.code = code;
  }
}
function fail2(code, message) {
  throw new BootstrapRuntimeError(code, message);
}
function isRecord3(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function expectRecord(value, location) {
  if (!isRecord3(value))
    fail2("BOOTSTRAP_SCHEMA_INVALID", `${location} must be a mapping.`);
  return value;
}
function expectString(value, location) {
  if (typeof value !== "string" || value.trim().length === 0)
    fail2("BOOTSTRAP_SCHEMA_INVALID", `${location} must be a non-empty string.`);
  return value.trim();
}
function expectStringArray(value, location, allowEmpty = false) {
  if (!Array.isArray(value) || !allowEmpty && value.length === 0)
    fail2("BOOTSTRAP_SCHEMA_INVALID", `${location} must be ${allowEmpty ? "an array" : "a non-empty array"}.`);
  const values = value.map((item, index) => expectString(item, `${location}[${index}]`));
  if (new Set(values).size !== values.length)
    fail2("BOOTSTRAP_SCHEMA_INVALID", `${location} must not contain duplicates.`);
  return values;
}
function expectExactKeys(value, expected, location) {
  const expectedSet = new Set(expected);
  const missing = expected.filter((key) => !(key in value));
  const extra = Object.keys(value).filter((key) => !expectedSet.has(key));
  if (missing.length > 0 || extra.length > 0)
    fail2("BOOTSTRAP_SCHEMA_INVALID", `${location} keys mismatch; missing=[${missing.join(", ")}], unexpected=[${extra.join(", ")}].`);
}
function normalizeRepoPath(value, location) {
  const normalized = value.trim().replace(/\\/gu, "/").replace(/^\.\//u, "").replace(/\/+/gu, "/");
  if (!normalized || normalized.startsWith("/") || /^[A-Za-z]:\//u.test(normalized) || normalized.split("/").some((segment) => segment === ".." || segment.length === 0) || /[\0-\x1F\x7F]/u.test(normalized) || normalized.includes("*")) {
    fail2("BOOTSTRAP_PATH_INVALID", `${location} must be a repository-relative concrete path.`);
  }
  return normalized;
}
function normalizePathArray(value, location, allowEmpty = false) {
  return expectStringArray(value, location, allowEmpty).map((item, index) => normalizeRepoPath(item, `${location}[${index}]`));
}
function sha2564(value) {
  return crypto9.createHash("sha256").update(value).digest("hex");
}
function computeBootstrapTargetIdentity(root2) {
  const resolved = path11.resolve(root2).replace(/\\/gu, "/").replace(/\/+$/u, "").toLocaleLowerCase();
  return sha2564(resolved).slice(0, 32);
}
function isAllowedAssetPath(value) {
  if (value === "AGENTS.md" || value === "CLAUDE.md")
    return true;
  return value === ".workflow-system/PROJECT_PROFILE.yaml" || value === ".workflow-system/vnext/BOOTSTRAP_RECEIPT.json" || value.startsWith("docs/workflow/") || value.startsWith("docs/designs/") || value.startsWith("docs/adoption/");
}
function isForbiddenAssetPath(value) {
  const distributionOwned = value === ".workflow-system/WORKFLOW_PROTOCOL.md" || value === ".workflow-system/FILE_SCHEMAS.md" || value === ".workflow-system/vnext/SOURCE_CONTRACT.yaml" || value === ".workflow-system/vnext/RUNTIME_CONTRACT.yaml" || value === ".workflow-system/vnext/DISTRIBUTION_STATE.json" || value === ".workflow-system/vnext/DISTRIBUTION_IN_PROGRESS.json" || value.startsWith(".workflow-system/runtime/") || value.startsWith(".agents/skills/");
  if (distributionOwned)
    return true;
  const segments = value.split("/");
  if (segments.includes(".git"))
    return true;
  if (segments.includes("node_modules"))
    return true;
  if (["src", "app", "lib", "packages"].some((segment) => segments.includes(segment)))
    return true;
  if (["package.json", "package-lock.json", "bun.lockb", "pnpm-lock.yaml", "yarn.lock"].includes(value))
    return true;
  return false;
}
function validateAuthorityEvidence(value) {
  if (!Array.isArray(value) || value.length === 0)
    fail2("BOOTSTRAP_AUTHORITY_MISSING", "authority_evidence must be non-empty.");
  return value.map((item, index) => {
    const record = expectRecord(item, `authority_evidence[${index}]`);
    expectExactKeys(record, ["kind", "source", "subject"], `authority_evidence[${index}]`);
    const kind = expectString(record.kind, `authority_evidence[${index}].kind`);
    if (!["project-owner", "scope-admission", "evidence-admission", "dangerous-operation"].includes(kind))
      fail2("BOOTSTRAP_AUTHORITY_INVALID", `authority_evidence[${index}].kind is unsupported.`);
    return { kind, source: expectString(record.source, `authority_evidence[${index}].source`), subject: expectString(record.subject, `authority_evidence[${index}].subject`) };
  });
}
function validateSemanticOperations(value, assets) {
  if (!Array.isArray(value))
    fail2("BOOTSTRAP_SCHEMA_INVALID", "semantic_operations must be an array.");
  const assetPaths = new Set(assets.map((asset) => asset.path));
  const operations = [];
  for (const [index, item] of value.entries()) {
    const record = expectRecord(item, `semantic_operations[${index}]`);
    expectExactKeys(record, ["operation_kind", "target_paths", "evidence_refs"], `semantic_operations[${index}]`);
    const operationKind = expectString(record.operation_kind, `semantic_operations[${index}].operation_kind`);
    if (!BOOTSTRAP_OPERATION_KINDS.includes(operationKind))
      fail2("BOOTSTRAP_SCHEMA_INVALID", `semantic_operations[${index}].operation_kind is unsupported.`);
    const targetPaths = normalizePathArray(record.target_paths, `semantic_operations[${index}].target_paths`);
    for (const target of targetPaths) {
      if (!assetPaths.has(target))
        fail2("BOOTSTRAP_TARGET_CONFLICT", `semantic operation ${operationKind} targets an asset that is not in the generated set: ${target}`);
    }
    const evidenceRefs = expectStringArray(record.evidence_refs, `semantic_operations[${index}].evidence_refs`);
    operations.push({ operation_kind: operationKind, target_paths: targetPaths, evidence_refs: evidenceRefs });
  }
  if (new Set(operations.map((operation) => operation.operation_kind)).size !== operations.length)
    fail2("BOOTSTRAP_SCHEMA_INVALID", "semantic_operations must contain at most one record for each operation kind.");
  const expected = new Map([
    ["decision-record-transaction", ["docs/workflow/DECISIONS.md"]],
    ["contract-candidate-commit", ["docs/workflow/CONTRACTS.md"]],
    ["project-status-transaction", ["docs/workflow/STATUS.md"]]
  ]);
  const hasAgents = assetPaths.has("AGENTS.md");
  const hasClaude = assetPaths.has("CLAUDE.md");
  if (hasAgents !== hasClaude)
    fail2("BOOTSTRAP_BOUNDARY_VIOLATION", "paired host guidance must include both AGENTS.md and CLAUDE.md.");
  if (hasAgents)
    expected.set("paired-host-guidance-transaction", ["AGENTS.md", "CLAUDE.md"]);
  for (const [operationKind, targetPaths] of expected) {
    if (!targetPaths.every((target) => assetPaths.has(target)))
      continue;
    const operation = operations.find((candidate) => candidate.operation_kind === operationKind);
    if (!operation || operation.target_paths.slice().sort().join("|") !== targetPaths.slice().sort().join("|")) {
      fail2("BOOTSTRAP_BOUNDARY_VIOLATION", `${operationKind} must declare the exact generated target set: ${targetPaths.join(", ")}.`);
    }
  }
  return operations;
}
function validateAsset(value, location) {
  const record = expectRecord(value, location);
  expectExactKeys(record, ["path", "category", "content"], location);
  const assetPath = normalizeRepoPath(expectString(record.path, `${location}.path`), `${location}.path`);
  if (!isAllowedAssetPath(assetPath) || isForbiddenAssetPath(assetPath))
    fail2("BOOTSTRAP_TARGET_FORBIDDEN", `asset target is outside the bootstrap boundary: ${assetPath}`);
  const category = expectString(record.category, `${location}.category`);
  if (!BOOTSTRAP_ASSET_CATEGORIES.includes(category))
    fail2("BOOTSTRAP_SCHEMA_INVALID", `${location}.category is unsupported.`);
  if (typeof record.content !== "string")
    fail2("BOOTSTRAP_SCHEMA_INVALID", `${location}.content must be text.`);
  return { path: assetPath, category, content: record.content };
}
function validateTargetSet(proposal, assets) {
  const assetPaths = assets.map((asset) => asset.path);
  const requested = new Set(proposal.requested_write_targets);
  const actual = new Set(assetPaths);
  if (requested.size !== actual.size || [...requested].some((value) => !actual.has(value)))
    fail2("BOOTSTRAP_TARGET_CONFLICT", "requested_write_targets must equal the generated asset target set.");
  if (proposal.requested_directory_targets.length > 0)
    fail2("BOOTSTRAP_TARGET_FORBIDDEN", "Bootstrap cannot promote Distribution directories; project-local Runtime dependencies are installed by Distribution.");
  const expectedChanged = new Set([...assetPaths, ...proposal.requested_directory_targets]);
  if (expectedChanged.size !== proposal.changed_paths.length || proposal.changed_paths.some((value) => !expectedChanged.has(value)))
    fail2("BOOTSTRAP_SCOPE_INVALID", "changed_paths must enumerate every generated governance asset exactly once.");
  if (proposal.delete_targets.length > 0)
    fail2("BOOTSTRAP_SCOPE_INVALID", "bootstrap does not support untyped deletion; use realign with an explicit implementation change.");
}
function validateModeOperationBoundary(proposal) {
  const kinds = new Set(proposal.semantic_operations.map((operation) => operation.operation_kind));
  if (proposal.mode === "design" && kinds.has("contract-candidate-commit"))
    fail2("BOOTSTRAP_BOUNDARY_VIOLATION", "design mode must not commit a locked Contract candidate.");
  if (proposal.mode === "inventory" && kinds.has("contract-candidate-commit"))
    fail2("BOOTSTRAP_BOUNDARY_VIOLATION", "inventory mode must not commit a locked Contract candidate.");
  if (proposal.mode === "design" && kinds.has("paired-host-guidance-transaction"))
    fail2("BOOTSTRAP_BOUNDARY_VIOLATION", "design mode must not install host guidance.");
  if (proposal.mode === "inventory" && kinds.has("paired-host-guidance-transaction"))
    fail2("BOOTSTRAP_BOUNDARY_VIOLATION", "inventory mode must not install host guidance.");
}
function validateBootstrapProjectProposal(value) {
  const record = expectRecord(value, "bootstrap proposal");
  expectExactKeys(record, [
    "schema_version",
    "kind",
    "caller",
    "mode",
    "target_identity",
    "source_revision",
    "source_tree_hash",
    "scope_document",
    "changed_paths",
    "conditional_authorizations",
    "transformation_kind",
    "authority_evidence",
    "semantic_operations",
    "preconditions",
    "evidence_refs",
    "idempotency_key",
    "requested_write_targets",
    "requested_directory_targets",
    "delete_targets",
    "assets"
  ], "bootstrap proposal");
  if (record.schema_version !== VNEXT_BOOTSTRAP_PROPOSAL_SCHEMA_VERSION || record.kind !== VNEXT_BOOTSTRAP_PROPOSAL_KIND || record.caller !== "bootstrap-project")
    fail2("BOOTSTRAP_SCHEMA_INVALID", "bootstrap proposal envelope marker is invalid.");
  const mode = expectString(record.mode, "bootstrap proposal.mode");
  if (!BOOTSTRAP_MODES.includes(mode))
    fail2("BOOTSTRAP_MODE_INVALID", `bootstrap mode must be one of ${BOOTSTRAP_MODES.join(", ")}.`);
  const targetIdentity = expectString(record.target_identity, "bootstrap proposal.target_identity");
  if (!TARGET_IDENTITY_PATTERN.test(targetIdentity))
    fail2("BOOTSTRAP_IDENTITY_INVALID", "target_identity must be a 32-character lowercase SHA-256 prefix.");
  const sourceRevision = expectString(record.source_revision, "bootstrap proposal.source_revision");
  const sourceTreeHash = expectString(record.source_tree_hash, "bootstrap proposal.source_tree_hash");
  if (!SHA256_PATTERN.test(sourceTreeHash))
    fail2("BOOTSTRAP_SCHEMA_INVALID", "source_tree_hash must be SHA-256.");
  const scopeDocument = expectString(record.scope_document, "bootstrap proposal.scope_document");
  const changedPaths = normalizePathArray(record.changed_paths, "bootstrap proposal.changed_paths");
  const conditionalAuthorizations = record.conditional_authorizations === undefined ? [] : record.conditional_authorizations;
  const transformationKind = expectString(record.transformation_kind, "bootstrap proposal.transformation_kind");
  if (transformationKind !== "localized" && transformationKind !== "inherently-broad")
    fail2("BOOTSTRAP_SCOPE_INVALID", "transformation_kind is unsupported.");
  const authorityEvidence = validateAuthorityEvidence(record.authority_evidence);
  const preconditions = expectStringArray(record.preconditions, "bootstrap proposal.preconditions");
  const evidenceRefs = expectStringArray(record.evidence_refs, "bootstrap proposal.evidence_refs");
  const idempotencyKey = expectString(record.idempotency_key, "bootstrap proposal.idempotency_key");
  if (!SAFE_KEY_PATTERN.test(idempotencyKey))
    fail2("BOOTSTRAP_SCHEMA_INVALID", "idempotency_key is invalid.");
  const requestedWriteTargets = normalizePathArray(record.requested_write_targets, "bootstrap proposal.requested_write_targets");
  const requestedDirectoryTargets = normalizePathArray(record.requested_directory_targets, "bootstrap proposal.requested_directory_targets", true);
  const deleteTargets = normalizePathArray(record.delete_targets, "bootstrap proposal.delete_targets", true);
  if (!Array.isArray(record.assets) || record.assets.length === 0)
    fail2("BOOTSTRAP_SCHEMA_INVALID", "bootstrap proposal.assets must be non-empty.");
  const assets = record.assets.map((item, index) => validateAsset(item, `bootstrap proposal.assets[${index}]`));
  if (new Set(assets.map((asset) => asset.path)).size !== assets.length)
    fail2("BOOTSTRAP_TARGET_CONFLICT", "bootstrap proposal.assets must not contain duplicate paths.");
  const proposal = {
    schema_version: 1,
    kind: VNEXT_BOOTSTRAP_PROPOSAL_KIND,
    caller: "bootstrap-project",
    mode,
    target_identity: targetIdentity,
    source_revision: sourceRevision,
    source_tree_hash: sourceTreeHash,
    scope_document: scopeDocument,
    changed_paths: changedPaths,
    conditional_authorizations: conditionalAuthorizations,
    transformation_kind: transformationKind,
    authority_evidence: authorityEvidence,
    semantic_operations: validateSemanticOperations(record.semantic_operations, assets),
    preconditions,
    evidence_refs: evidenceRefs,
    idempotency_key: idempotencyKey,
    requested_write_targets: requestedWriteTargets,
    requested_directory_targets: requestedDirectoryTargets,
    delete_targets: deleteTargets,
    assets
  };
  validateTargetSet(proposal, assets);
  validateModeOperationBoundary(proposal);
  const scope = parseMutationScope(scopeDocument, sha2564(scopeDocument));
  const scopeResult = evaluateMutationScope(scope, {
    changed_paths: changedPaths,
    conditional_authorizations: conditionalAuthorizations,
    transformation_kind: transformationKind
  });
  if (scopeResult.status !== "pass")
    fail2("BOOTSTRAP_SCOPE_BLOCKED", scopeResult.blockers.join(" "));
  return { proposal, scope: scopeResult };
}
function absoluteTarget(root2, relativePath) {
  const resolvedRoot = path11.resolve(root2);
  const resolved = path11.resolve(resolvedRoot, ...relativePath.split("/"));
  const prefix = resolvedRoot.endsWith(path11.sep) ? resolvedRoot : `${resolvedRoot}${path11.sep}`;
  if (resolved !== resolvedRoot && !resolved.startsWith(prefix))
    fail2("BOOTSTRAP_PATH_INVALID", `target escapes project root: ${relativePath}`);
  return resolved;
}
function contentHash(content) {
  return sha2564(Buffer.from(content, "utf8"));
}
function applyAtomicBootstrapTransaction(root2, proposal, verify) {
  const resolvedRoot = path11.resolve(root2);
  const stagingRoot = fs10.mkdtempSync(path11.join(path11.dirname(resolvedRoot), ".workflow-vnext-bootstrap-"));
  const backups = [];
  const newlyPromoted = [];
  try {
    const stagedFiles = [];
    for (const [index, asset] of proposal.assets.entries()) {
      const stagedPath = path11.join(stagingRoot, "files", `${index}.tmp`);
      fs10.mkdirSync(path11.dirname(stagedPath), { recursive: true });
      fs10.writeFileSync(stagedPath, asset.content, "utf8");
      stagedFiles.push({ relative: asset.path, path: stagedPath });
    }
    const touched = stagedFiles.map((item) => item.relative);
    for (const [index, relative] of touched.entries()) {
      const targetPath = absoluteTarget(resolvedRoot, relative);
      if (!fs10.existsSync(targetPath))
        continue;
      const backupPath = path11.join(stagingRoot, "backups", `${index}.bak`);
      fs10.mkdirSync(path11.dirname(backupPath), { recursive: true });
      fs10.renameSync(targetPath, backupPath);
      backups.push({ targetPath, backupPath });
    }
    for (const staged of stagedFiles) {
      const targetPath = absoluteTarget(resolvedRoot, staged.relative);
      fs10.mkdirSync(path11.dirname(targetPath), { recursive: true });
      fs10.renameSync(staged.path, targetPath);
      newlyPromoted.push(targetPath);
    }
    for (const asset of proposal.assets) {
      const targetPath = absoluteTarget(resolvedRoot, asset.path);
      if (!fs10.existsSync(targetPath) || contentHash(fs10.readFileSync(targetPath, "utf8")) !== contentHash(asset.content)) {
        fail2("BOOTSTRAP_READ_BACK_FAILED", `promoted asset did not read back identically: ${asset.path}`);
      }
    }
    verify?.();
    fs10.rmSync(stagingRoot, { recursive: true, force: true });
  } catch (error) {
    for (const targetPath of newlyPromoted.reverse()) {
      if (fs10.existsSync(targetPath))
        fs10.rmSync(targetPath, { recursive: true, force: true });
    }
    for (const entry of backups.reverse()) {
      if (fs10.existsSync(entry.targetPath))
        fs10.rmSync(entry.targetPath, { recursive: true, force: true });
      if (fs10.existsSync(entry.backupPath)) {
        fs10.mkdirSync(path11.dirname(entry.targetPath), { recursive: true });
        fs10.renameSync(entry.backupPath, entry.targetPath);
      }
    }
    fs10.rmSync(stagingRoot, { recursive: true, force: true });
    throw error;
  }
}
function applyBootstrapProjectProposal(root2, value, options = {}) {
  const validation = validateBootstrapProjectProposal(value);
  const { proposal, scope } = validation;
  if (computeBootstrapTargetIdentity(root2) !== proposal.target_identity)
    fail2("BOOTSTRAP_IDENTITY_CONFLICT", "proposal target_identity does not match the target root.");
  const plannedWrites = proposal.assets.map((asset) => asset.path);
  const base = {
    operation_kind: "bootstrap-project",
    mode: proposal.mode,
    idempotency_key: proposal.idempotency_key,
    target_identity: proposal.target_identity,
    dry_run: options.dryRun === true,
    planned_writes: plannedWrites,
    planned_directories: [...proposal.requested_directory_targets],
    scope
  };
  if (options.dryRun)
    return { ...base, status: "ready", committed: false, read_back_verified: false, message: "bootstrap proposal validated; no files were written." };
  applyAtomicBootstrapTransaction(root2, proposal, options.verify);
  return { ...base, status: "success", committed: true, read_back_verified: true, message: "bootstrap proposal committed and read-back verified." };
}
function parseJsonFile(filePath) {
  try {
    return JSON.parse(fs10.readFileSync(path11.resolve(filePath), "utf8"));
  } catch (error) {
    throw new BootstrapRuntimeError("BOOTSTRAP_SCHEMA_INVALID", `proposal file is not valid JSON: ${error instanceof Error ? error.message : String(error)}`);
  }
}
function readFlag(argv, name) {
  const index = argv.indexOf(name);
  return index >= 0 ? argv[index + 1] : undefined;
}
async function runBootstrapCli(argv = process.argv.slice(1)) {
  try {
    const root2 = readFlag(argv, "--root") ?? process.cwd();
    const proposalFile = readFlag(argv, "--proposal-file") ?? readFlag(argv, "--proposal");
    if (!proposalFile)
      throw new BootstrapRuntimeError("BOOTSTRAP_SCHEMA_INVALID", "bootstrap-project requires --proposal-file <json>.");
    const dryRun = argv.includes("--dry-run");
    const result = applyBootstrapProjectProposal(root2, parseJsonFile(proposalFile), { dryRun });
    console.log(JSON.stringify(result, null, 2));
    return result.status === "ready" || result.status === "success" || result.status === "no-op" ? 0 : 1;
  } catch (error) {
    console.error(error instanceof Error ? error.message : String(error));
    return 1;
  }
}

// runtime/vnext/src/status-schema.ts
var STATUS_SCHEMA = [
  { key: "overview", title: "项目概览" },
  { key: "completed", title: "✅ 已完成且稳定" },
  { key: "inProgress", title: "\uD83D\uDD28 正在开发" },
  { key: "pending", title: "\uD83D\uDCCB 待开发" },
  { key: "risks", title: "⚠️ 已知风险 / 观察点" },
  { key: "removedOrDeferred", title: "❌ 已移除 / 推迟" },
  { key: "nextCheckpoint", title: "\uD83D\uDD1C 下一检查点" },
  { key: "recentUpdates", title: "最近更新记录", aliases: ["最近更新记录", "Recent Updates"] }
];
function deriveStatusSectionKeys(schema) {
  return schema.map((section) => section.key);
}
var STATUS_SECTION_KEYS = deriveStatusSectionKeys(STATUS_SCHEMA);
var STATUS_SECTIONS = Object.fromEntries(STATUS_SCHEMA.map(({ key, ...section }) => [key, section]));
var STATUS_REQUIRED_SECTION_TITLES = Object.freeze(STATUS_SCHEMA.map((section) => section.title));

// runtime/vnext/src/kernel.ts
var VNEXT_RUNTIME_SCHEMA_VERSION = 1;
var VNEXT_RUNTIME_PROPOSAL_KIND = "vnext-runtime-proposal";
var VNEXT_CURRENT_TASK_KIND = "vnext-current-task";
var VNEXT_RUNTIME_STATE_KIND = "vnext-current-task-runtime-state";
var VNEXT_RUNTIME_CONTRACT_RELATIVE_PATH = ".workflow-system/vnext/RUNTIME_CONTRACT.yaml";
var VNEXT_RUNTIME_PACKAGE_RELATIVE_PATH = ".workflow-system/runtime";
var VNEXT_RUNTIME_ENTRYPOINT_RELATIVE_PATH = ".workflow-system/runtime/dist/cli.js";
var VNEXT_RUNTIME_PACKAGE_MANIFEST_RELATIVE_PATH = ".workflow-system/runtime/package.json";
var VNEXT_RUNTIME_LOCKFILE_RELATIVE_PATH = ".workflow-system/runtime/package-lock.json";
var VNEXT_RUNTIME_PACKAGE_NAME = "vibe-coding-vnext-runtime";
var VNEXT_RUNTIME_NODE_MIN_VERSION = ">=20.0.0";
var VNEXT_RUNTIME_PACKAGE_VERSION = "0.20.1";
var RUNTIME_OPERATION_KINDS = [
  "task-state-transaction",
  "finding-queue-transaction",
  "lifecycle-transaction",
  "inbox-record-transaction",
  "project-status-transaction",
  "archive-transaction",
  "lesson-record-transaction",
  "contract-candidate-commit",
  "decision-record-transaction"
];
var RUNTIME_SOURCE_TUPLE_FIELDS = [
  "path",
  "revision",
  "document_id",
  "task_id",
  "task_slug",
  "workflow_status",
  "lifecycle_state",
  "active_step_id",
  "active_step_status",
  "finding_queue_revision",
  "resume_requires_review",
  "resume_review_reasons"
];
var RUNTIME_REQUIRED_ENVELOPE_FIELDS = [
  "authority_evidence",
  "semantic_delta",
  "preconditions",
  "evidence_refs",
  "idempotency_key",
  "requested_write_targets"
];
var RUNTIME_STATE_FIELDS = [
  "business_evidence_version",
  "evidence_plan_revision",
  "task_evolution_version",
  "preservation_source_revision",
  "task_id",
  "task_slug",
  "workflow_status",
  "lifecycle_state",
  "resume_requires_review",
  "resume_review_reasons",
  "active_step_id",
  "active_step_status",
  "finding_queue_revision",
  "review_cycle",
  "findings",
  "execution_log",
  "applied_proposals",
  "claim_evidence_required",
  "claim_evidence",
  "pending_review_result",
  "review_coverage",
  "step_attempts",
  "evidence_challenges",
  "evidence_carry_forward",
  "artifact_checkpoint_ids",
  "dynamic_review_required",
  "dynamic_expansions",
  "execution_preflight",
  "authority_domain_revision"
];
var REVIEW_CYCLE_FIELDS = [
  "id",
  "cycle_phase",
  "repair_round",
  "counted_repair_wave_ids",
  "active_repair_wave_id",
  "verification_new_finding_wave_used",
  "verification_new_finding_wave_id"
];
var RUNTIME_RESULT_STATES = ["success", "no-op", "conflict", "blocked"];
var VNEXT_EXECUTE_STEP_MODES = ["default", "repair"];
var PREPARE_TASK_MODES = ["default", "confirm", "replan", "amend-scope"];
var LIFECYCLE_MODES = ["pause", "interrupt", "resume-paused", "resume-interrupted", "supersede"];
var CLOSE_TASK_MODES = ["default"];
var INBOX_ITEM_TYPES = ["requirement", "idea", "bug", "chore", "question"];
var INBOX_ITEM_SOURCES = ["user", "implementation", "review", "regression", "root_cause", "other"];
var INBOX_SUGGESTED_NEXT_ACTIONS = ["triage_later", "ask_user"];
var REVIEW_CYCLE_PHASES = ["discovery", "verification"];
var STEP_STATUSES = ["ready", "in-progress", "completed", "blocked"];
var STEP_EXECUTION_RESULT_STATUSES = ["passed", "expected-failure", "failed", "blocked", "not-run"];
var FINDING_STATUSES = ["admitted", "in-progress", "resolved", "deferred", "rejected"];
var REPLAN_TASK_STATE_ACTIONS = ["mark-replan-blocked", "clear-replan-block", "commit-replan"];
var DRAFT_TASK_STATE_ACTIONS = ["create-draft", "update-draft", "confirm-draft"];
var CLAIM_EVIDENCE_MIGRATION_ACTIONS = ["migrate-claim-evidence"];
var REVIEW_TASK_STATE_ACTIONS = ["record-review-result"];
var DRAFT_AUDIT_ACTIONS = ["create-draft", "update-draft", "confirm-draft"];
var REPLAN_AUDIT_ACTIONS = [
  "supersede",
  "mark-replan-blocked",
  "clear-replan-block",
  "commit-replan",
  "commit-scope-amendment"
];
var STEP_ADVANCEMENT_OUTCOMES = [
  "not-applicable",
  "repair-awaiting-verification",
  "advanced",
  "task-complete"
];
var DOCUMENT_ID_PATTERN = /^doc-[a-f0-9]{24}$/;
var SHA256_PATTERN2 = /^[a-f0-9]{64}$/;
var SAFE_KEY_PATTERN2 = /^[A-Za-z0-9][A-Za-z0-9._:-]{2,127}$/;
var FINGERPRINT_PATTERN = /^[a-z0-9][a-z0-9._:-]{2,127}$/;
var STEP_ID_PATTERN2 = /^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/;
var MAX_TEXT_LENGTH = 4000;
var MAX_EVIDENCE_REFS = 32;
var MAX_FINDINGS = 256;
var MAX_APPLIED_PROPOSALS = 256;
var MAX_EXECUTION_LOG = 256;
var MAX_EXECUTION_RESULT_ITEMS = 256;
var MAX_CLAIM_EVIDENCE_RECORDS = 256;
var MAX_CLAIM_EVIDENCE_SLOTS = 32;
var MAX_REPLAN_SECTION_CONTENT_LENGTH = 32768;
var MAX_REPAIR_ROUNDS = 3;
var CLAIM_EVIDENCE_COMPLETION_RULE = "non-empty acceptance-bearing frozen plan; due slots require bound successful reports or exact applicable user waivers; waivers never imply PASS; close checks all slots and consumed prerequisites";
var DRAFT_CLAIM_EVIDENCE_REQUIREMENT = "required-and-non-empty-for-new-or-refined-drafts; must-include-an-acceptance-claim; legacy-documents-remain-readable-but-require-migration-before-terminal-completion";
var CLOSE_TASK_CLAIM_EVIDENCE_RULE = "derive acceptance_satisfied and validation_complete from the non-empty frozen CURRENT_TASK claim_evidence plan; require an acceptance claim; aggregate command success is insufficient";
var MAX_REPAIR_ATTEMPTS = 2;
var CURRENT_TASK_RELATIVE_FALLBACK = "docs/workflow/CURRENT_TASK.md";
var INBOX_RECORD_ITEM_ID_PATTERN = /^(\d{8})-([a-z0-9]{4,})$/;
var INBOX_RECORD_PATH_PATTERN = /^TASKS\/inbox\/INBOX-(\d{8})-([a-z0-9]{4,})-([a-z0-9]+(?:-[a-z0-9]+)*)\.md$/;
var INBOX_RECORD_PROVENANCE_MARKER = "<!-- vNext inbox record:";
var MAX_INBOX_TEXT_LENGTH = 32768;
var INBOX_CAPTURE_PRECONDITIONS = [
  "current-task-is-active",
  "relation-proven-unrelated",
  "duplicate-check-clear",
  "owner-route-resolved"
];
var TEST_STRATEGY_MODES = [
  "flexible",
  "test-first",
  "implementation-first",
  "not-applicable"
];
var TEST_STRATEGY_SOURCES = [
  "explicit-user",
  "project-policy",
  "inferred-default"
];
var TEST_STRATEGY_CLASSIFICATIONS = [
  "contract-clear-behavior",
  "exploratory-or-infrastructure",
  "non-executable-change"
];
var CLAIM_KINDS = [
  "acceptance",
  "regression",
  "invariant",
  "bug-reproduction",
  "compatibility",
  "release",
  "exploration"
];
var CLAIM_EVIDENCE_DISPOSITIONS = [
  "existing",
  "reused",
  "newly-executed",
  "missing",
  "deferred",
  "blocked"
];
var LEGACY_EVIDENCE_EXECUTION_SELECTION_SCOPES = [
  "focused",
  "target",
  "integration",
  "e2e",
  "broad-regression"
];
var EVIDENCE_CHECK_BOUNDARIES = ["local", "business-flow", "e2e"];
var EVIDENCE_EXECUTION_GRANULARITIES = ["focused", "target", "broad-regression"];
var EVIDENCE_EXECUTION_BREADTH_BASES = [
  "claim-risk-contract",
  "acceptance-boundary",
  "low-cost-evidence-insufficient",
  "project-policy",
  "release-gate",
  "explicit-user"
];
var REVIEW_RESULT_VERDICTS = ["clean", "findings", "blocked"];
var REVIEW_BLOCKER_ROUTES = ["review-change", "debug-task", "prepare-task:replan", "prepare-task:amend-scope", "user"];
function createReviewCycleZero() {
  return {
    id: "review-cycle-0",
    cycle_phase: "discovery",
    repair_round: 0,
    counted_repair_wave_ids: [],
    active_repair_wave_id: null,
    verification_new_finding_wave_used: false,
    verification_new_finding_wave_id: null
  };
}
function hasModernDynamicExpansionIdentity(expansion) {
  return expansion.execution_id !== undefined && expansion.preflight_id !== undefined && expansion.step_id !== undefined && expansion.plan_revision !== undefined && expansion.change_set_id !== undefined && expansion.mode !== undefined;
}
function dynamicExpansionMatchesExecution(expansion, identity) {
  return hasModernDynamicExpansionIdentity(expansion) && expansion.execution_id === identity.execution_id && expansion.step_id === identity.step_id && expansion.plan_revision === identity.plan_revision && expansion.change_set_id === identity.change_set_id && expansion.mode === identity.mode;
}
function currentExecutionDynamicExpansions(current, mode) {
  const active = current.runtimeState.execution_preflight;
  if (!active || active.step_id !== current.runtimeState.active_step_id || mode !== undefined && active.mode !== mode)
    return [];
  return (current.runtimeState.dynamic_expansions ?? []).filter((item) => dynamicExpansionMatchesExecution(item, active));
}
function dynamicReviewRequiredForCurrentExecution(current) {
  const pending = new Set(current.runtimeState.review_coverage?.pending_paths ?? []);
  if (current.runtimeState.dynamic_review_required === true && (current.runtimeState.dynamic_expansions ?? []).some((item) => item.review_required !== false && item.reviewed_by_review_id === undefined && pending.has(item.path)))
    return true;
  const active = current.runtimeState.execution_preflight;
  if (!active || active.step_id !== current.runtimeState.active_step_id) {
    return current.runtimeState.dynamic_review_required === true;
  }
  const currentExpansions = currentExecutionDynamicExpansions(current);
  if (currentExpansions.length > 0)
    return currentExpansions.some((item) => item.review_required !== false && item.reviewed_by_review_id === undefined);
  const legacyCurrentExpansion = (current.runtimeState.dynamic_expansions ?? []).some((item) => item.execution_id === active.execution_id && item.step_id === active.step_id && item.mode === active.mode);
  return legacyCurrentExpansion ? current.runtimeState.dynamic_review_required === true : false;
}

class VNextRuntimeError extends Error {
  code;
  constructor(code, message) {
    super(`${code}: ${message}`);
    this.name = "VNextRuntimeError";
    this.code = code;
  }
}
function isRecord4(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function fail3(code, message) {
  throw new VNextRuntimeError(code, message);
}
function expectRecord2(value, location) {
  if (!isRecord4(value))
    fail3("RUNTIME_SCHEMA_INVALID", `${location} must be a mapping.`);
  return value;
}
function expectExactKeys2(value, expected, location) {
  const expectedSet = new Set(expected);
  const missing = expected.filter((key) => !(key in value));
  const extra = Object.keys(value).filter((key) => !expectedSet.has(key));
  if (missing.length > 0 || extra.length > 0) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} keys mismatch; missing=[${missing.join(", ")}], unexpected=[${extra.join(", ")}].`);
  }
}
function expectString2(value, location, pattern) {
  if (typeof value !== "string" || value.trim().length === 0) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} must be a non-empty string.`);
  }
  const normalized = value.trim();
  if (pattern && !pattern.test(normalized)) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} has an invalid value.`);
  }
  return normalized;
}
function expectNullableString(value, location, pattern) {
  if (value === null)
    return null;
  return expectString2(value, location, pattern);
}
function expectText(value, location, maxLength = MAX_TEXT_LENGTH) {
  const text = expectString2(value, location);
  if (text.length > maxLength)
    fail3("RUNTIME_SCHEMA_INVALID", `${location} exceeds ${maxLength} characters.`);
  return text;
}
function expectEnum(value, allowed, location) {
  const normalized = expectString2(value, location);
  if (!allowed.includes(normalized)) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} must be one of [${allowed.join(", ")}].`);
  }
  return normalized;
}
function expectBoolean(value, location) {
  if (typeof value !== "boolean")
    fail3("RUNTIME_SCHEMA_INVALID", `${location} must be a boolean.`);
  return value;
}
function expectInteger(value, location, min = 0, max = Number.MAX_SAFE_INTEGER) {
  if (typeof value !== "number" || !Number.isInteger(value) || value < min || value > max) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} must be an integer in [${min}, ${max}].`);
  }
  return value;
}
function expectStringArray2(value, location, allowEmpty = false, maxLength = 128) {
  if (!Array.isArray(value) || !allowEmpty && value.length === 0) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} must be ${allowEmpty ? "an array" : "a non-empty array"}.`);
  }
  if (value.length > maxLength)
    fail3("RUNTIME_SCHEMA_INVALID", `${location} has too many entries.`);
  const items = value.map((item, index) => expectText(item, `${location}[${index}]`, 512));
  if (new Set(items).size !== items.length)
    fail3("RUNTIME_SCHEMA_INVALID", `${location} contains duplicates.`);
  return items;
}
function expectSetEqual(actual, expected, location) {
  const actualSet = new Set(actual);
  const expectedSet = new Set(expected);
  const missing = expected.filter((item) => !actualSet.has(item));
  const extra = actual.filter((item) => !expectedSet.has(item));
  if (missing.length > 0 || extra.length > 0 || actualSet.size !== actual.length) {
    fail3("RUNTIME_CONTRACT_INVALID", `${location} differs from the closed set; missing=[${missing.join(", ")}], extra=[${extra.join(", ")}].`);
  }
}
function normalizeRepoPath2(value, location) {
  const normalized = value.replace(/\\/g, "/").replace(/^\.\//, "");
  if (!normalized || normalized.startsWith("/") || normalized.split("/").includes("..")) {
    fail3("RUNTIME_PATH_INVALID", `${location} must be a repository-relative path.`);
  }
  return normalized;
}
function sha2565(value) {
  return crypto10.createHash("sha256").update(value).digest("hex");
}
function stableValue2(value) {
  if (Array.isArray(value))
    return value.map(stableValue2);
  if (isRecord4(value)) {
    return Object.fromEntries(Object.keys(value).sort().map((key) => [key, stableValue2(value[key])]));
  }
  return value;
}
function digest3(value) {
  return sha2565(JSON.stringify(stableValue2(value)));
}
function captureReviewTarget(root2, paths) {
  const resolvedRoot = path12.resolve(root2);
  const normalizedPaths = [...new Set(paths.map((item, index) => normalizeRepoPath2(item, `review_target.paths[${index}]`)))].sort();
  const entries = normalizedPaths.map((relativePath) => {
    const filePath = path12.resolve(resolvedRoot, ...relativePath.split("/"));
    if (filePath !== resolvedRoot && !filePath.startsWith(`${resolvedRoot}${path12.sep}`)) {
      fail3("RUNTIME_PATH_INVALID", `review target escapes the target root: ${relativePath}`);
    }
    if (!fs11.existsSync(filePath))
      return { path: relativePath, state: "absent", sha256: null };
    const stat = fs11.lstatSync(filePath);
    if (stat.isSymbolicLink()) {
      return { path: relativePath, state: "symlink", sha256: sha2565(fs11.readlinkSync(filePath)) };
    }
    if (!stat.isFile()) {
      fail3("REVIEW_TARGET_INVALID", `review target must be a file, symlink, or absent path: ${relativePath}`);
    }
    return { path: relativePath, state: "file", sha256: sha2565(fs11.readFileSync(filePath)) };
  });
  return {
    kind: "runtime-file-manifest/v1",
    revision: digest3({ kind: "runtime-file-manifest/v1", entries }),
    entries
  };
}
function manifest(entries) {
  const sorted = [...entries].sort((a, b) => a.path < b.path ? -1 : a.path > b.path ? 1 : 0);
  return { kind: "runtime-file-manifest/v1", entries: sorted, revision: digest3({ kind: "runtime-file-manifest/v1", entries: sorted }) };
}
function createReviewTargetManifest(entries) {
  return manifest(entries);
}
function nextStepAttemptId(current) {
  const attempts = current.runtimeState.step_attempts?.[current.runtimeState.active_step_id]?.attempts;
  return attempts?.at(-1)?.attempt_id ?? `attempt-${digest3({ document: current.sourceTuple.document_id, plan: current.runtimeState.evidence_plan_revision, step: current.runtimeState.active_step_id, n: 1 }).slice(0, 40)}`;
}
function validateStepAttempts(value) {
  const source = expectRecord2(value, "step_attempts");
  const result = {};
  for (const [step, raw] of Object.entries(source)) {
    expectString2(step, "attempt step", STEP_ID_PATTERN2);
    const ledger = expectRecord2(raw, "step attempt ledger");
    expectExactKeys2(ledger, ["evidence_plan_revision", "max_attempts", "attempts"], "step attempt ledger");
    if (ledger.max_attempts !== 3 || !Array.isArray(ledger.attempts) || !ledger.attempts.length || ledger.attempts.length > 3)
      fail3("RETRY_LEDGER_INVALID", "Attempt budget must be three including the initial execution.");
    const attempts = ledger.attempts.map((raw) => {
      const item = expectRecord2(raw, "attempt");
      expectExactKeys2(item, ["attempt_id", "idempotency_key", "request_digest", "status", "blocker", ...item.recovery === undefined ? [] : ["recovery"], "evidence_refs"], "attempt");
      let blocker = null;
      if (item.blocker !== null) {
        const rawBlocker = expectRecord2(item.blocker, "attempt blocker");
        expectExactKeys2(rawBlocker, ["kind", "execution_result", "subject_snapshot"], "attempt blocker");
        blocker = { kind: expectEnum(rawBlocker.kind, ["environment", "unknown"], "blocker.kind"), execution_result: validateStepExecutionResult(rawBlocker.execution_result, "blocker.execution_result"), subject_snapshot: validateReviewTarget(rawBlocker.subject_snapshot, "blocker.subject_snapshot") };
        if (blocker.execution_result.outcome !== "blocked")
          fail3("RETRY_LEDGER_INVALID", "Retained failure must be blocked.");
      }
      const status = expectEnum(item.status, ["ready", "preflighted", "blocked", "implemented"], "attempt.status");
      if (status === "blocked" !== (blocker !== null))
        fail3("RETRY_LEDGER_INVALID", "Blocked attempts retain their original failure.");
      return { attempt_id: expectString2(item.attempt_id, "attempt_id", SAFE_KEY_PATTERN2), idempotency_key: expectString2(item.idempotency_key, "attempt.idempotency_key", SAFE_KEY_PATTERN2), request_digest: item.request_digest === null ? null : expectString2(item.request_digest, "request_digest", /^[a-f0-9]{64}$/u), status, blocker, ...item.recovery === undefined ? {} : { recovery: validateStepRepairDiagnosis(item.recovery) }, evidence_refs: validateEvidenceRefs(item.evidence_refs, "attempt.evidence_refs") };
    });
    if (new Set(attempts.map((a) => a.attempt_id)).size !== attempts.length || new Set(attempts.map((a) => a.idempotency_key)).size !== attempts.length)
      fail3("RETRY_LEDGER_INVALID", "Attempt identities must be unique.");
    result[step] = { evidence_plan_revision: expectString2(ledger.evidence_plan_revision, "attempt plan", /^[a-f0-9]{64}$/u), max_attempts: 3, attempts };
  }
  return result;
}
function retryRequestDigest(current, delta) {
  return digest3({ document: current.sourceTuple.document_id, plan: current.runtimeState.evidence_plan_revision, step: delta.step_id, blocked_attempt_id: delta.blocked_attempt_id, refs: delta.blocker_resolution_refs, ...delta.repair_diagnosis ? { repair_diagnosis: delta.repair_diagnosis } : {} });
}
function reviewCycleForNextStep(previousCycleId, nextStepId, completionKey) {
  return {
    ...createReviewCycleZero(),
    id: `review-cycle-${digest3({ previous_cycle_id: previousCycleId, next_step_id: nextStepId, completion_key: completionKey }).slice(0, 32)}`
  };
}
function validateStepRepairDiagnosis(value) {
  const report = expectRecord2(value, "repair diagnosis");
  expectExactKeys2(report, ["kind", "status", "owner", "failed_check", "cause", "repair_paths"], "repair diagnosis");
  if (report.kind !== "same-plan-repair/v1" || report.status !== "confirmed" || report.owner !== "current-step")
    fail3("RETRY_DIAGNOSIS_REQUIRED", "Same-plan repair requires a confirmed current-step diagnosis.");
  return { kind: "same-plan-repair/v1", status: "confirmed", owner: "current-step", failed_check: expectText(report.failed_check, "repair failed_check"), cause: expectText(report.cause, "repair cause"), repair_paths: expectStringArray2(report.repair_paths, "repair repair_paths", false, 256).map((p) => normalizeRepoPath2(p, "repair path")) };
}
function validateRetryResolution(root2, current, delta, failure) {
  if (captureReviewTarget(root2, failure.subject_snapshot.entries.map((e) => e.path)).revision !== failure.subject_snapshot.revision)
    fail3("RETRY_SUBJECT_STALE", "Declared code, fixture or check objects changed after failure; use authorized repair or replan.");
  const failures = [...failure.execution_result.command_results, ...failure.execution_result.validation_results].filter((item) => item.status === "failed" || item.status === "blocked");
  if (!delta.repair_diagnosis && failures.length && failures.every((item) => applicableResultWaiver(root2, current, delta.step_id, item))) {
    const basis = readCanonicalTaskBasis(root2, current);
    if (delta.blocker_resolution_refs.length !== 1 || delta.blocker_resolution_refs[0] !== basis.path) {
      fail3("RETRY_RESOLUTION_REQUIRED", "Risk-accepted retry cites the exact retained Task Basis, not a fabricated repair result.");
    }
    return;
  }
  if (delta.repair_diagnosis) {
    if (failure.kind !== "unknown")
      fail3("RETRY_DIAGNOSIS_REQUIRED", "Same-plan repair requires the retained non-environment failure.");
    const failed = [...failure.execution_result.command_results.filter((item) => item.status === "failed"), ...failure.execution_result.validation_results.filter((item) => item.status === "failed")];
    const checkNames = failed.map((item) => ("command" in item) ? item.command : item.validation);
    if (!checkNames.includes(delta.repair_diagnosis.failed_check))
      fail3("RETRY_DIAGNOSIS_REQUIRED", "The diagnosis must identify a check that actually failed in the retained attempt.");
    const failedEvidence = new Set(failed.flatMap((item) => item.evidence_refs));
    if (!delta.blocker_resolution_refs.every((ref) => failedEvidence.has(ref)))
      fail3("RETRY_DIAGNOSIS_REQUIRED", "Recovery evidence must cite the retained failed check.");
    const admittedPaths = new Set(failure.execution_result.review_base.entries.map((entry) => entry.path));
    if (delta.repair_diagnosis.repair_paths.some((p) => !admittedPaths.has(p)))
      fail3("RETRY_SCOPE_BLOCKED", "Same-plan repair paths must be a subset of the failed preflight candidate paths.");
    return;
  }
  if (failure.kind !== "environment" || [...failure.execution_result.command_results, ...failure.execution_result.validation_results].some((item) => item.status === "failed"))
    fail3("RETRY_DIAGNOSIS_REQUIRED", "Environment retry requires a recorded environment blocker without failed checks.");
  for (const ref of delta.blocker_resolution_refs) {
    const relative = normalizeRepoPath2(ref, "blocker_resolution_ref");
    const absolute = path12.resolve(root2, relative);
    if (!fs11.existsSync(absolute) || !fs11.statSync(absolute).isFile() || fs11.statSync(absolute).size > 65536)
      fail3("RETRY_RESOLUTION_REQUIRED", "Resolution must be a retained bounded JSON report.");
    let parsed;
    try {
      parsed = JSON.parse(fs11.readFileSync(absolute, "utf8"));
    } catch {
      fail3("RETRY_RESOLUTION_REQUIRED", "Resolution must be structured JSON, not a free-form unlock note.");
    }
    const report = expectRecord2(parsed, "retry resolution");
    expectExactKeys2(report, ["kind", "task_id", "document_id", "step_id", "blocked_attempt_id", "evidence_plan_revision", "subject_revision", "status", "diagnosis", "resolution"], "retry resolution");
    if (report.kind !== "environment-restored/v1" || report.status !== "passed" || report.task_id !== current.runtimeState.task_id || report.document_id !== current.sourceTuple.document_id || report.step_id !== delta.step_id || report.blocked_attempt_id !== delta.blocked_attempt_id || report.evidence_plan_revision !== current.runtimeState.evidence_plan_revision || report.subject_revision !== failure.subject_snapshot.revision)
      fail3("RETRY_RESOLUTION_REQUIRED", "Resolution report must bind this exact environment failure, plan and subjects.");
    expectText(report.diagnosis, "retry diagnosis");
    expectText(report.resolution, "retry resolution");
  }
}
function emptyReviewCoverage(planRevision) {
  return {
    change_set_id: `change-set-${planRevision.slice(0, 32)}`,
    base: manifest([]),
    target: manifest([]),
    preimages: [],
    pending_paths: [],
    last_clean_revision: null
  };
}
function validateReviewCoverage(value) {
  const source = expectRecord2(value, "review_coverage");
  expectExactKeys2(source, ["change_set_id", "base", "target", "preimages", "pending_paths", "last_clean_revision"], "review_coverage");
  const base = validateReviewTarget(source.base, "review_coverage.base");
  const target = validateReviewTarget(source.target, "review_coverage.target");
  createReviewChangeDelta(base, target);
  if (!Array.isArray(source.preimages) || source.preimages.length !== base.entries.length)
    fail3("REVIEW_COVERAGE_INVALID", "preimages must cover the initial manifest.");
  const preimages = source.preimages.map((value, index) => {
    const item = expectRecord2(value, "preimage");
    const hasLegacyContent = Object.prototype.hasOwnProperty.call(item, "content_base64");
    expectExactKeys2(item, hasLegacyContent ? ["path", "state", "sha256", "content_base64"] : ["path", "state", "sha256"], "preimage");
    const entry = base.entries[index];
    if (digest3({ path: item.path, state: item.state, sha256: item.sha256 }) !== digest3(entry))
      fail3("REVIEW_COVERAGE_INVALID", "preimage identity differs from base.");
    if (entry.state === "absent" && entry.sha256 !== null)
      fail3("REVIEW_COVERAGE_INVALID", "absent preimage must have a null hash.");
    if (entry.state !== "absent" && entry.sha256 === null)
      fail3("REVIEW_COVERAGE_INVALID", "file or symlink preimage must have a hash.");
    if (hasLegacyContent) {
      const content = item.content_base64;
      if (entry.state === "absent") {
        if (content !== null)
          fail3("REVIEW_COVERAGE_INVALID", "absent preimage has content.");
      } else {
        if (typeof content !== "string")
          fail3("REVIEW_COVERAGE_INVALID", "legacy preimage content is required.");
        const buffer = Buffer.from(content, "base64");
        if (buffer.toString("base64") !== content || sha2565(buffer) !== entry.sha256)
          fail3("REVIEW_COVERAGE_INVALID", "legacy preimage content hash is invalid.");
      }
    }
    return hasLegacyContent ? { ...entry, content_base64: item.content_base64 } : { ...entry };
  });
  const pending = expectStringArray2(source.pending_paths, "review_coverage.pending_paths", true);
  if (new Set(pending).size !== pending.length || pending.some((p) => !base.entries.some((e) => e.path === p)))
    fail3("REVIEW_COVERAGE_INVALID", "pending paths must be covered.");
  return { change_set_id: expectString2(source.change_set_id, "review_coverage.change_set_id", SAFE_KEY_PATTERN2), base, target, preimages, pending_paths: pending, last_clean_revision: source.last_clean_revision === null ? null : expectString2(source.last_clean_revision, "last_clean_revision", /^[a-f0-9]{64}$/u) };
}
function validateDynamicExpansions(value) {
  if (!Array.isArray(value) || value.length > 256)
    fail3("RUNTIME_SCHEMA_INVALID", "dynamic_expansions must be a bounded array.");
  const expansions = value.map((raw, index) => {
    const item = expectRecord2(raw, `dynamic_expansions[${index}]`);
    const legacyIdentityKeys = ["execution_id", "preflight_id", "step_id", "mode"];
    const modernIdentityKeys = [...legacyIdentityKeys, "plan_revision", "change_set_id"];
    const reviewKeys = ["reviewed_by_review_id", "reviewed_at"];
    expectExactKeys2(item, ["path", "domain", "assessment", "first_touch_state", "admitted_at", ...modernIdentityKeys.filter((key) => (key in item)), ...reviewKeys.filter((key) => (key in item)), ..."review_required" in item ? ["review_required"] : []], `dynamic_expansions[${index}]`);
    let assessment;
    try {
      assessment = normalizeBlastRadiusAssessments([item.assessment])[0];
    } catch (error) {
      fail3(error instanceof MutationAuthorityError ? error.code : "MUTATION_AUTHORITY_ASSESSMENT_INVALID", error instanceof Error ? error.message : String(error));
    }
    if (item.review_required === false && requiresDynamicReview(assessment))
      fail3("MUTATION_AUTHORITY_ASSESSMENT_INVALID", "Elevated or uncertain expansion cannot omit dynamic review.");
    const target = normalizeAuthorityPath(item.path, `dynamic_expansions[${index}].path`);
    if (assessment.target.path !== target)
      fail3("RUNTIME_SCHEMA_INVALID", `dynamic_expansions[${index}] assessment target must match path.`);
    const hasLegacyIdentity = legacyIdentityKeys.some((key) => (key in item));
    const hasModernIdentity = modernIdentityKeys.some((key) => (key in item));
    if (hasModernIdentity && modernIdentityKeys.some((key) => !(key in item))) {
      fail3("RUNTIME_SCHEMA_INVALID", `dynamic_expansions[${index}] modern execution identity must be complete.`);
    }
    if (!hasModernIdentity && hasLegacyIdentity && legacyIdentityKeys.some((key) => !(key in item))) {
      fail3("RUNTIME_SCHEMA_INVALID", `dynamic_expansions[${index}] historical execution identity must be complete.`);
    }
    const hasReview = reviewKeys.some((key) => (key in item));
    if (hasReview && (!hasModernIdentity || reviewKeys.some((key) => !(key in item)))) {
      fail3("RUNTIME_SCHEMA_INVALID", `dynamic_expansions[${index}] review consumption identity must be complete and modern.`);
    }
    return {
      path: target,
      domain: item.domain === null ? null : expectString2(item.domain, `dynamic_expansions[${index}].domain`, /^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/u),
      assessment,
      first_touch_state: expectEnum(item.first_touch_state, ["file", "absent"], `dynamic_expansions[${index}].first_touch_state`),
      admitted_at: expectString2(item.admitted_at, `dynamic_expansions[${index}].admitted_at`),
      ...item.review_required === undefined ? {} : { review_required: expectBoolean(item.review_required, "expansion.review_required") },
      ...hasLegacyIdentity ? {
        execution_id: expectString2(item.execution_id, `dynamic_expansions[${index}].execution_id`, SAFE_KEY_PATTERN2),
        preflight_id: expectString2(item.preflight_id, `dynamic_expansions[${index}].preflight_id`, SAFE_KEY_PATTERN2),
        step_id: expectString2(item.step_id, `dynamic_expansions[${index}].step_id`, STEP_ID_PATTERN2),
        mode: expectEnum(item.mode, VNEXT_EXECUTE_STEP_MODES, `dynamic_expansions[${index}].mode`)
      } : {},
      ...hasModernIdentity ? {
        plan_revision: expectString2(item.plan_revision, `dynamic_expansions[${index}].plan_revision`, SHA256_PATTERN2),
        change_set_id: expectString2(item.change_set_id, `dynamic_expansions[${index}].change_set_id`, SAFE_KEY_PATTERN2)
      } : {},
      ...hasReview ? {
        reviewed_by_review_id: expectString2(item.reviewed_by_review_id, `dynamic_expansions[${index}].reviewed_by_review_id`, SAFE_KEY_PATTERN2),
        reviewed_at: expectString2(item.reviewed_at, `dynamic_expansions[${index}].reviewed_at`)
      } : {}
    };
  });
  const identities = expansions.map((item) => hasModernDynamicExpansionIdentity(item) ? `modern:${digest3({ path: item.path, execution_id: item.execution_id, step_id: item.step_id, plan_revision: item.plan_revision, change_set_id: item.change_set_id, mode: item.mode })}` : `historical:${digest3({ path: item.path, execution_id: item.execution_id ?? null, step_id: item.step_id ?? null, mode: item.mode ?? null })}`);
  if (new Set(identities).size !== identities.length)
    fail3("RUNTIME_SCHEMA_INVALID", "dynamic_expansions must not duplicate a path within one execution identity.");
  return expansions;
}
function validateExecutionPreflight(value, location = "runtime_state.execution_preflight") {
  const source = expectRecord2(value, location);
  expectExactKeys2(source, [
    "execution_id",
    "preflight_id",
    "mode",
    "step_id",
    "plan_revision",
    "execution_phase",
    "candidate_paths",
    "change_set_id",
    "repair_fingerprints",
    "repair_wave_id",
    "review_id",
    "review_target_paths"
  ], location);
  const mode = expectEnum(source.mode, VNEXT_EXECUTE_STEP_MODES, `${location}.mode`);
  const repairFingerprints = source.repair_fingerprints === null ? null : expectStringArray2(source.repair_fingerprints, `${location}.repair_fingerprints`, false, MAX_FINDINGS).map((item, index) => expectString2(item, `${location}.repair_fingerprints[${index}]`, FINGERPRINT_PATTERN));
  const repairWaveId = source.repair_wave_id === null ? null : expectString2(source.repair_wave_id, `${location}.repair_wave_id`, SAFE_KEY_PATTERN2);
  const reviewId = source.review_id === null ? null : expectString2(source.review_id, `${location}.review_id`, SAFE_KEY_PATTERN2);
  const reviewTargetPaths = source.review_target_paths === null ? null : expectStringArray2(source.review_target_paths, `${location}.review_target_paths`, true, 256).map((item) => normalizeRepoPath2(item, `${location}.review_target_paths`));
  if (mode === "default" && (repairFingerprints !== null || repairWaveId !== null || reviewId !== null || reviewTargetPaths !== null)) {
    fail3("RUNTIME_STATE_CONFLICT", `${location} default execution must not carry repair identity.`);
  }
  if (mode === "repair" && (repairFingerprints === null || repairFingerprints.length === 0 || repairWaveId === null || reviewId === null || reviewTargetPaths === null || reviewTargetPaths.length === 0)) {
    fail3("RUNTIME_STATE_CONFLICT", `${location} repair execution must carry its review and repair-wave identity.`);
  }
  return {
    execution_id: expectString2(source.execution_id, `${location}.execution_id`, SAFE_KEY_PATTERN2),
    preflight_id: expectString2(source.preflight_id, `${location}.preflight_id`, SAFE_KEY_PATTERN2),
    mode,
    step_id: expectString2(source.step_id, `${location}.step_id`, STEP_ID_PATTERN2),
    plan_revision: expectString2(source.plan_revision, `${location}.plan_revision`, SHA256_PATTERN2),
    execution_phase: expectEnum(source.execution_phase, ["flexible", "test-first", "red", "green", "implementation-first", "not-applicable", "legacy"], `${location}.execution_phase`),
    candidate_paths: expectStringArray2(source.candidate_paths, `${location}.candidate_paths`, false, 256).map((item) => normalizeRepoPath2(item, `${location}.candidate_paths`)),
    change_set_id: expectString2(source.change_set_id, `${location}.change_set_id`, SAFE_KEY_PATTERN2),
    repair_fingerprints: repairFingerprints === null ? null : [...new Set(repairFingerprints)],
    repair_wave_id: repairWaveId,
    review_id: reviewId,
    review_target_paths: reviewTargetPaths
  };
}
function normalizeReviewPreimages(preimages) {
  const writes = [];
  const entries = preimages.map((preimage) => {
    if (Object.prototype.hasOwnProperty.call(preimage, "content_base64")) {
      const write = decodeLegacyReviewPreimage(preimage);
      if (write)
        writes.push(write);
    }
    return { path: preimage.path, state: preimage.state, sha256: preimage.sha256 };
  });
  return { entries, writes };
}
function reviewPreimageWrite(root2, entry) {
  if (entry.state === "absent")
    return null;
  const absolute = path12.resolve(root2, entry.path);
  const content = entry.state === "symlink" ? Buffer.from(fs11.readlinkSync(absolute)) : fs11.readFileSync(absolute);
  if (!entry.sha256 || sha2565(content) !== entry.sha256)
    fail3("REVIEW_BASE_INVALID", `first-touch baseline hash changed while capturing ${entry.path}.`);
  return { sha256: entry.sha256, content };
}
function registerReviewCoverage(root2, current, paths) {
  const old = current.runtimeState.review_coverage;
  if (old && captureReviewTarget(root2, old.target.entries.map((e) => e.path)).revision !== old.target.revision)
    fail3("REVIEW_TARGET_STALE", "Unrecorded changes cannot refresh the cumulative review target.");
  const added = captureReviewTarget(root2, paths.filter((p) => !old?.base.entries.some((e) => e.path === p)));
  const additions = added.entries.map((entry) => ({ entry, write: reviewPreimageWrite(root2, entry) }));
  const normalizedOld = normalizeReviewPreimages(old?.preimages ?? []);
  const preimages = [...normalizedOld.entries, ...additions.map(({ entry }) => ({ ...entry }))].sort((a, b) => a.path < b.path ? -1 : a.path > b.path ? 1 : 0);
  const coverage = validateReviewCoverage({ change_set_id: old?.change_set_id ?? `change-set-${digest3({ document: current.sourceTuple.document_id, plan: current.runtimeState.evidence_plan_revision }).slice(0, 32)}`, base: manifest(preimages.map((entry) => ({ path: entry.path, state: entry.state, sha256: entry.sha256 }))), target: manifest([...old?.target.entries ?? [], ...added.entries]), preimages, pending_paths: old?.pending_paths ?? [], last_clean_revision: old?.last_clean_revision ?? null });
  return { coverage, preimageWrites: [...normalizedOld.writes, ...additions.flatMap(({ write }) => write ? [write] : [])] };
}
function extendReviewCoverage(root2, current, paths) {
  const old = current.runtimeState.review_coverage;
  if (!old)
    fail3("EXECUTE_PREFLIGHT_REQUIRED", "extend-preflight requires an existing recorded preflight.");
  const newPaths = paths.filter((p) => !old.base.entries.some((entry) => entry.path === p));
  const added = captureReviewTarget(root2, newPaths);
  const additions = added.entries.map((entry) => ({ entry, write: reviewPreimageWrite(root2, entry) }));
  const normalizedOld = normalizeReviewPreimages(old.preimages);
  const preimages = [...normalizedOld.entries, ...additions.map(({ entry }) => ({ ...entry }))].sort((a, b) => a.path < b.path ? -1 : a.path > b.path ? 1 : 0);
  const target = manifest([...old.target.entries, ...added.entries]);
  const coverage = validateReviewCoverage({
    change_set_id: old.change_set_id,
    base: manifest(preimages.map((entry) => ({ path: entry.path, state: entry.state, sha256: entry.sha256 }))),
    target,
    preimages,
    pending_paths: old.pending_paths,
    last_clean_revision: null
  });
  return { coverage, preimageWrites: [...normalizedOld.writes, ...additions.flatMap(({ write }) => write ? [write] : [])] };
}
function v2StepPlannedTargets(current) {
  const step = resolveCanonicalTaskStep(current).current;
  const raw = step.planned_mutation_targets ?? step.mutation_scope;
  if (!raw)
    fail3("TASK_STEP_METADATA_INCOMPLETE", `step ${step.id} is missing planned_mutation_targets.`);
  return raw.split(",").map((value, index) => {
    const cleaned = value.trim().replace(/^`|`$/gu, "");
    try {
      return normalizeAuthorityPath(cleaned, `step ${step.id}.planned_mutation_targets[${index}]`, true);
    } catch (error) {
      if (error instanceof MutationAuthorityError)
        fail3(error.code, error.message);
      throw error;
    }
  }).filter(Boolean);
}
function executionAdmissionErrorCode(current, evaluation) {
  const decision = evaluation.decisions.find((item) => !item.admitted);
  if (!decision)
    return "EXECUTION_ADMISSION_BLOCKED";
  if (decision.classification === "blocked-governance")
    return "MUTATION_AUTHORITY_GOVERNANCE_BOUNDARY";
  if (decision.classification === "blocked-test-strategy")
    return "TEST_STRATEGY_SEQUENCE_INVALID";
  if (decision.classification === "blocked-persistent-test")
    return current.mutationAuthority ? "PERSISTENT_TEST_UNADMITTED" : "EXECUTE_SCOPE_BLOCKED";
  if (decision.classification === "blocked-non-executable-policy")
    return "TEST_STRATEGY_NON_EXECUTABLE_SCOPE_VIOLATION";
  if (decision.classification === "blocked-assessment") {
    return decision.assessment?.disposition === "escalate" ? "MUTATION_BLAST_RADIUS_ESCALATION_REQUIRED" : "MUTATION_BLAST_RADIUS_ASSESSMENT_REQUIRED";
  }
  if (current.mutationAuthority && evaluation.blockers.some((item) => item.startsWith("blocked-authority:") && item.includes("requires blast-radius assessment"))) {
    return "MUTATION_BLAST_RADIUS_ASSESSMENT_REQUIRED";
  }
  if (current.mutationAuthority && evaluation.blockers.some((item) => item.includes("MUTATION_AUTHORITY_EXPANSION_REQUIRED"))) {
    return "MUTATION_AUTHORITY_EXPANSION_REQUIRED";
  }
  if (current.mutationAuthority && decision.classification === "blocked-authority" && (decision.reason.startsWith("target belongs to unauthorized domain ") || decision.reason === "target is unclassified")) {
    return "MUTATION_AUTHORITY_EXPANSION_REQUIRED";
  }
  return current.mutationAuthority ? "MUTATION_AUTHORITY_TARGET_BLOCKED" : "EXECUTE_SCOPE_BLOCKED";
}
function resolveNewExecutionPhase(current, strategy, declaredPhase) {
  const phase = executionPhaseForCurrentStep(current, strategy);
  if (declaredPhase !== undefined && declaredPhase !== phase) {
    fail3("EXECUTE_PREFLIGHT_STALE", `preflight execution phase ${declaredPhase} does not match the Runtime-derived phase ${phase}.`);
  }
  return phase;
}
function evaluateCurrentExecutionTargetAdmissions(root2, current, input) {
  const strategy = resolveTestStrategyExecutionContext(current);
  const mode = input.mode ?? "default";
  const phase = executionPhaseForCurrentStep(current, strategy);
  if (input.phase !== undefined && input.phase !== phase) {
    fail3("EXECUTE_PREFLIGHT_STALE", `execution phase ${input.phase} does not match the Runtime-derived phase ${phase}.`);
  }
  const stepScope = input.step_mutation_scope ?? (() => {
    const step = resolveCanonicalTaskStep(current).current;
    return (step.mutation_scope ?? "").split(",").map((value) => value.trim().replace(/^`|`$/gu, "")).filter(Boolean);
  })();
  const plannedTargets = input.planned_targets ?? (current.mutationAuthority ? v2StepPlannedTargets(current) : stepScope);
  const activeExecution = current.runtimeState.execution_preflight?.step_id === current.runtimeState.active_step_id && current.runtimeState.execution_preflight.mode === mode ? current.runtimeState.execution_preflight : undefined;
  const storedAssessments = activeExecution ? currentExecutionDynamicExpansions(current, mode).map((item) => item.assessment) : [];
  const assessments = [...storedAssessments, ...input.assessments ?? []].filter((item, index, values) => values.findIndex((other) => other.target.path === item.target.path) === index);
  let project = null;
  if (current.mutationAuthority) {
    try {
      project = readProjectMutationAuthority(root2);
    } catch (error) {
      fail3(error instanceof MutationAuthorityError ? error.code : "MUTATION_AUTHORITY_PROJECT_INVALID", error instanceof Error ? error.message : String(error));
    }
    if (!project)
      fail3("MUTATION_AUTHORITY_PROJECT_REQUIRED", "v2 tasks require PROJECT_PROFILE.yaml.mutation_authority.domains.");
    const currentDomainRevision = projectMutationAuthorityRevision(project);
    if (!current.runtimeState.authority_domain_revision) {
      fail3("MUTATION_AUTHORITY_DOMAIN_REVISION_REQUIRED", "v2 execution must bind the project authority domain-map revision before mutation admission.");
    }
    if (current.runtimeState.authority_domain_revision !== currentDomainRevision) {
      fail3("MUTATION_AUTHORITY_DOMAIN_REVISION_STALE", "the project authority domain map changed after this task was confirmed; explicitly revalidate or replan the task before execution.");
    }
  }
  const legacyScope = current.mutationAuthority ? null : parseMutationScope(current.body, current.sourceTuple.revision);
  const evaluation = evaluateExecutionTargetAdmissions({
    version: current.mutationAuthority ? 2 : 1,
    root: root2,
    project_authority: project,
    task_authority: current.mutationAuthority,
    legacy_scope: legacyScope,
    step_planned_targets: plannedTargets,
    step_mutation_scope: stepScope,
    targets: input.target_paths,
    assessments,
    persistent_test_paths: strategy.persistent_tests,
    non_executable_change_paths: phase === "not-applicable" ? nonExecutableChangePatterns(root2) : [],
    phase,
    mode,
    governance_paths: [current.relativePath]
  });
  return evaluation;
}
function assertExecutionTargetAdmissions(root2, current, input) {
  const evaluation = evaluateCurrentExecutionTargetAdmissions(root2, current, input);
  assertExecutionAdmissionEvaluation(current, evaluation, input.location ?? "execution target");
  return evaluation;
}
function assertExecutionAdmissionEvaluation(current, evaluation, location) {
  if (evaluation.status !== "pass") {
    fail3(executionAdmissionErrorCode(current, evaluation), `${location} was rejected by the unified execution admission evaluator: ${evaluation.blockers.join(" ")}`);
  }
}
function assertStepProgressExecutionIdentity(current, delta) {
  const active = current.runtimeState.execution_preflight;
  if (!active)
    return;
  const executionResult = delta.execution_result;
  if (executionResult) {
    if (executionResult.execution_id !== active.execution_id) {
      fail3("EXECUTE_PREFLIGHT_STALE", "step-progress execution_result must bind the current stable execution identity.");
    }
    if (executionResult.change_set_id !== active.change_set_id) {
      fail3("EXECUTE_PREFLIGHT_STALE", "step-progress execution_result must bind the current execution change set.");
    }
    if (active.mode !== (delta.repair_fingerprints || delta.repair_fingerprint ? "repair" : "default")) {
      fail3("EXECUTE_PREFLIGHT_STALE", "step-progress repair mode does not match the current execution identity.");
    }
    return;
  }
  const pendingReview = current.runtimeState.pending_review_result;
  const pendingExecution = pendingReview ? currentDefinitionExecutionLog(current).find((item) => !("action" in item) && item.idempotency_key === pendingReview.execution_id) : undefined;
  if (!delta.review_receipt || pendingExecution?.execution_result?.execution_id !== active.execution_id) {
    fail3("EXECUTE_PREFLIGHT_REQUIRED", "v2 step-progress without an execution result must bind the current review execution identity.");
  }
}
function cumulativeReviewExecution(current, execution) {
  const coverage = current.runtimeState.review_coverage;
  if (!coverage || !execution.execution_result)
    return execution;
  return {
    ...execution,
    change_set_id: coverage.change_set_id,
    execution_result: {
      ...execution.execution_result,
      change_set_id: coverage.change_set_id,
      review_base: coverage.base,
      review_target: coverage.target,
      change_delta: createReviewChangeDelta(coverage.base, coverage.target)
    }
  };
}
function validateTestAssessment(value) {
  const source = expectRecord2(value, "test_assessment");
  expectExactKeys2(source, ["applicable", "reason", "evidence_refs", "necessity", "oracle", "boundary", "reuse", "applicability"], "test_assessment");
  if (typeof source.applicable !== "boolean")
    fail3("REVIEW_ASSESSMENT_REQUIRED", "applicable must be boolean.");
  return { applicable: source.applicable, reason: expectText(source.reason, "test_assessment.reason"), evidence_refs: validateEvidenceRefs(source.evidence_refs, "test_assessment.evidence_refs"), necessity: expectText(source.necessity, "test_assessment.necessity"), oracle: expectText(source.oracle, "test_assessment.oracle"), boundary: expectText(source.boundary, "test_assessment.boundary"), reuse: expectText(source.reuse, "test_assessment.reuse"), applicability: expectText(source.applicability, "test_assessment.applicability") };
}
function createReviewChangeDelta(base, target) {
  const basePaths = base.entries.map((item) => item.path);
  const targetPaths = target.entries.map((item) => item.path);
  if (digest3(basePaths) !== digest3(targetPaths)) {
    fail3("REVIEW_TARGET_PATH_CONFLICT", "review base and target must contain the same canonical path set.");
  }
  const entries = base.entries.flatMap((before, index) => {
    const after = target.entries[index];
    if (before.state === after.state && before.sha256 === after.sha256)
      return [];
    return [{
      path: before.path,
      before_state: before.state,
      before_sha256: before.sha256,
      after_state: after.state,
      after_sha256: after.sha256
    }];
  });
  return {
    kind: "runtime-file-delta/v1",
    base_revision: base.revision,
    target_revision: target.revision,
    entries
  };
}
function parseYamlFrontmatter(content, location) {
  const match = /^---\r?\n([\s\S]*?)\r?\n---\r?\n?([\s\S]*)$/.exec(content);
  if (!match) {
    fail3("MIGRATION_REQUIRED", `${location} is not a vNext CURRENT_TASK document; run the Migration Pack.`);
  }
  const document = parseDocument(match[1], { uniqueKeys: true });
  const diagnostics = [...document.errors, ...document.warnings];
  if (diagnostics.length > 0) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} has invalid frontmatter YAML: ${diagnostics.map((item) => item.message).join("; ")}`);
  }
  const frontmatter = document.toJS();
  if (!isRecord4(frontmatter)) {
    fail3("MIGRATION_REQUIRED", `${location} does not declare a supported vNext CURRENT_TASK schema; run the Migration Pack.`);
  }
  return { frontmatter, body: match[2] };
}
function parseYamlMappingFile(filePath) {
  if (!fs11.existsSync(filePath))
    fail3("RUNTIME_CONTRACT_MISSING", `Runtime contract is missing: ${filePath}`);
  const document = parseDocument(fs11.readFileSync(filePath, "utf8"), { uniqueKeys: true });
  const diagnostics = [...document.errors, ...document.warnings];
  if (diagnostics.length > 0)
    fail3("RUNTIME_CONTRACT_INVALID", `${filePath} has invalid YAML: ${diagnostics.map((item) => item.message).join("; ")}`);
  return expectRecord2(document.toJS(), filePath);
}
function validateNodeMinimum(nodeMinVersion) {
  const match = /^>=([0-9]+)\.([0-9]+)\.([0-9]+)$/.exec(nodeMinVersion);
  if (!match)
    fail3("RUNTIME_CONTRACT_INVALID", "runtime_distribution.node_min_version must use >=MAJOR.MINOR.PATCH.");
  const major = Number(match[1]);
  if (!Number.isSafeInteger(major) || major < 20)
    fail3("RUNTIME_CONTRACT_INVALID", "runtime_distribution.node_min_version must require Node 20 or newer.");
}
function validateRuntimeEnvironment(nodeVersion = process.versions.node, nodeMinVersion = VNEXT_RUNTIME_NODE_MIN_VERSION) {
  const minimumMatch = /^>=([0-9]+)\.([0-9]+)\.([0-9]+)$/.exec(nodeMinVersion);
  const currentMatch = /^([0-9]+)\.([0-9]+)\.([0-9]+)/.exec(nodeVersion ?? "");
  if (!minimumMatch || !currentMatch)
    throw new VNextRuntimeError("RUNTIME_ENV_UNSUPPORTED", "Unable to determine a supported Node.js version.");
  const minimum = minimumMatch.slice(1).map(Number);
  const current = currentMatch.slice(1).map(Number);
  const belowMinimum = current[0] < minimum[0] || current[0] === minimum[0] && current[1] < minimum[1] || current[0] === minimum[0] && current[1] === minimum[1] && current[2] < minimum[2];
  if (belowMinimum) {
    throw new VNextRuntimeError("RUNTIME_ENV_UNSUPPORTED", "Node.js " + nodeVersion + " is below the required minimum " + nodeMinVersion + ".");
  }
}
function readJsonObject(filePath, code) {
  if (!fs11.existsSync(filePath))
    fail3(code, "Required Runtime distribution file is missing: " + filePath);
  let parsed;
  try {
    parsed = JSON.parse(fs11.readFileSync(filePath, "utf8"));
  } catch (error) {
    fail3(code, filePath + " is not valid JSON: " + (error instanceof Error ? error.message : String(error)));
  }
  return expectRecord2(parsed, filePath);
}
function resolveRuntimeDistributionDirectory(root2) {
  const resolvedRoot = path12.resolve(root2);
  const installedDirectory = path12.join(resolvedRoot, ...VNEXT_RUNTIME_PACKAGE_RELATIVE_PATH.split("/"));
  if (fs11.existsSync(path12.join(installedDirectory, "package.json"))) {
    return { directory: installedDirectory, installed: true };
  }
  return { directory: path12.join(resolvedRoot, "runtime", "vnext"), installed: false };
}
function validateVNextRuntimeDistribution(root2, contract, requireDependencies = false) {
  const { directory } = resolveRuntimeDistributionDirectory(root2);
  const packagePath = path12.join(directory, "package.json");
  const lockfilePath = path12.join(directory, "package-lock.json");
  const entrypointPath = path12.join(directory, "dist", "cli.js");
  const packageManifest = readJsonObject(packagePath, "RUNTIME_PACKAGE_INVALID");
  if (packageManifest.name !== contract.package_name || packageManifest.version !== contract.package_version || packageManifest.private !== true || packageManifest.type !== "module") {
    fail3("RUNTIME_PACKAGE_INVALID", "Runtime package.json must declare the contract name, version, private=true, and type=module.");
  }
  const engines = expectRecord2(packageManifest.engines, "Runtime package.json.engines");
  if (engines.node !== contract.node_min_version)
    fail3("RUNTIME_PACKAGE_INVALID", "Runtime package.json.engines.node does not match runtime_distribution.node_min_version.");
  const dependencies = expectRecord2(packageManifest.dependencies, "Runtime package.json.dependencies");
  if (dependencies.yaml !== "2.8.3")
    fail3("RUNTIME_PACKAGE_INVALID", "Runtime package.json must pin yaml to 2.8.3.");
  const lockfile = readJsonObject(lockfilePath, "RUNTIME_PACKAGE_INVALID");
  if (lockfile.name !== contract.package_name || lockfile.version !== contract.package_version || lockfile.lockfileVersion !== 3) {
    fail3("RUNTIME_PACKAGE_INVALID", "Runtime package-lock.json identity or lockfileVersion is invalid.");
  }
  const lockPackages = expectRecord2(lockfile.packages, "Runtime package-lock.json.packages");
  const rootLock = expectRecord2(lockPackages[""], 'Runtime package-lock.json.packages[""]');
  if (rootLock.version !== contract.package_version)
    fail3("RUNTIME_PACKAGE_INVALID", "Runtime package-lock.json root version does not match the Runtime contract.");
  const yamlLock = expectRecord2(lockPackages["node_modules/yaml"], "Runtime package-lock.json.packages[node_modules/yaml]");
  if (yamlLock.version !== "2.8.3")
    fail3("RUNTIME_PACKAGE_INVALID", "Runtime package-lock.json must lock yaml to 2.8.3.");
  if (!fs11.existsSync(entrypointPath) || !fs11.statSync(entrypointPath).isFile())
    fail3("RUNTIME_PACKAGE_INVALID", "Runtime entrypoint is missing: " + entrypointPath);
  const entrypoint = fs11.readFileSync(entrypointPath, "utf8");
  if (!entrypoint.includes("vnext-runtime-proposal") || !entrypoint.includes("runCli"))
    fail3("RUNTIME_PACKAGE_INVALID", "Runtime dist/cli.js is not the generated vNext Runtime entrypoint.");
  if (requireDependencies) {
    const localYaml = path12.join(directory, "node_modules", "yaml", "package.json");
    const localYamlManifest = readJsonObject(localYaml, "RUNTIME_DEPENDENCY_MISSING");
    if (localYamlManifest.version !== "2.8.3")
      fail3("RUNTIME_DEPENDENCY_INVALID", "Runtime-local yaml dependency does not match package-lock.json.");
  }
  return {
    kind: contract.kind,
    package_path: contract.package_path,
    entrypoint: contract.entrypoint,
    package_version: contract.package_version,
    node_min_version: contract.node_min_version,
    package_lock_sha256: sha2565(fs11.readFileSync(lockfilePath)),
    entrypoint_sha256: sha2565(fs11.readFileSync(entrypointPath))
  };
}
function validateRuntimeDistributionContract(value) {
  const distribution = expectRecord2(value, "Runtime contract.runtime_distribution");
  expectExactKeys2(distribution, ["kind", "package_path", "entrypoint", "package_manifest", "lockfile", "package_name", "package_version", "node_min_version"], "Runtime contract.runtime_distribution");
  const result = {
    kind: expectEnum(distribution.kind, ["project-local-node"], "Runtime contract.runtime_distribution.kind"),
    package_path: normalizeRepoPath2(expectString2(distribution.package_path, "Runtime contract.runtime_distribution.package_path"), "Runtime contract.runtime_distribution.package_path"),
    entrypoint: normalizeRepoPath2(expectString2(distribution.entrypoint, "Runtime contract.runtime_distribution.entrypoint"), "Runtime contract.runtime_distribution.entrypoint"),
    package_manifest: normalizeRepoPath2(expectString2(distribution.package_manifest, "Runtime contract.runtime_distribution.package_manifest"), "Runtime contract.runtime_distribution.package_manifest"),
    lockfile: normalizeRepoPath2(expectString2(distribution.lockfile, "Runtime contract.runtime_distribution.lockfile"), "Runtime contract.runtime_distribution.lockfile"),
    package_name: expectString2(distribution.package_name, "Runtime contract.runtime_distribution.package_name"),
    package_version: expectString2(distribution.package_version, "Runtime contract.runtime_distribution.package_version"),
    node_min_version: expectString2(distribution.node_min_version, "Runtime contract.runtime_distribution.node_min_version")
  };
  if (result.package_path !== VNEXT_RUNTIME_PACKAGE_RELATIVE_PATH || result.entrypoint !== VNEXT_RUNTIME_ENTRYPOINT_RELATIVE_PATH || result.package_manifest !== VNEXT_RUNTIME_PACKAGE_MANIFEST_RELATIVE_PATH || result.lockfile !== VNEXT_RUNTIME_LOCKFILE_RELATIVE_PATH || result.package_name !== VNEXT_RUNTIME_PACKAGE_NAME || result.package_version !== VNEXT_RUNTIME_PACKAGE_VERSION || result.node_min_version !== VNEXT_RUNTIME_NODE_MIN_VERSION) {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime distribution must use the canonical project-local Node package identity.");
  }
  validateNodeMinimum(result.node_min_version);
  return result;
}
function validateTaskContextContract(value) {
  const context = expectRecord2(value, "Runtime contract.task_context");
  expectExactKeys2(context, ["schema_version", "kind", "commands", "entry_points", "views", "projection", "paging", "receipts", "read_only"], "Runtime contract.task_context");
  if (context.schema_version !== 1 || context.kind !== "vnext-task-context-contract" || context.read_only !== true)
    fail3("RUNTIME_CONTRACT_INVALID", "task_context must be the read-only vNext projection contract.");
  expectSetEqual(expectStringArray2(context.commands, "Runtime contract.task_context.commands"), ["task-context", "task-read", "task-storage-migration", "task-export"], "task context commands");
  expectSetEqual(expectStringArray2(context.entry_points, "Runtime contract.task_context.entry_points"), ["validate --summary", "task-context", "task-read", "review-context", "review-read", "preflight-step", "evidence-context"], "task context entry points");
  expectSetEqual(expectStringArray2(context.views, "Runtime contract.task_context.views"), ["overview", "operation-context", "history-on-demand"], "task context views");
  const projection = expectRecord2(context.projection, "Runtime contract.task_context.projection");
  expectExactKeys2(projection, ["default", "required_blocks", "review_requires_cumulative_target", "forbidden_expansions"], "Runtime contract.task_context.projection");
  if (projection.default !== "task-context" || projection.review_requires_cumulative_target !== true)
    fail3("RUNTIME_CONTRACT_INVALID", "task_context projection must use the shared bounded projector and cumulative review target.");
  expectSetEqual(expectStringArray2(projection.required_blocks, "Runtime contract.task_context.projection.required_blocks"), ["current-definition-or-visible-definition-revision", "current-step", "mutation-authority", "unfinished-obligations", "required-dependencies", "unknown-dependencies", "global-gates", "latest-execution"], "task context required blocks");
  expectSetEqual(expectStringArray2(projection.forbidden_expansions, "Runtime contract.task_context.projection.forbidden_expansions"), ["raw-runtime-state", "unbounded-execution-log", "unbounded-applied-proposals"], "task context forbidden expansions");
  const paging = expectRecord2(context.paging, "Runtime contract.task_context.paging");
  expectExactKeys2(paging, ["unit", "default_bytes", "min_bytes", "max_bytes", "required_fields", "stale_binding"], "Runtime contract.task_context.paging");
  if (paging.unit !== "utf8-bytes" || paging.default_bytes !== 16384 || paging.min_bytes !== 256 || paging.max_bytes !== 65536)
    fail3("RUNTIME_CONTRACT_INVALID", "task_context paging must retain the bounded UTF-8 byte budget.");
  expectSetEqual(expectStringArray2(paging.required_fields, "Runtime contract.task_context.paging.required_fields"), ["returned", "total_bytes", "continuation", "complete_for_operation"], "task context paging fields");
  expectSetEqual(expectStringArray2(paging.stale_binding, "Runtime contract.task_context.paging.stale_binding"), ["source_revision", "definition_revision", "state_revision", "exact_reference"], "task context stale binding");
  const receipts = expectRecord2(context.receipts, "Runtime contract.task_context.receipts");
  expectExactKeys2(receipts, ["context", "read", "proves", "not_authority"], "Runtime contract.task_context.receipts");
  if (receipts.context !== "task-context-receipt/v1" || receipts.read !== "task-read-receipt/v1" || receipts.proves !== "version-and-return-range-only" || receipts.not_authority !== true)
    fail3("RUNTIME_CONTRACT_INVALID", "task context receipts must not be treated as execution authority.");
}
function validateTaskStoreContract(value) {
  const store = expectRecord2(value, "Runtime contract.task_store");
  expectExactKeys2(store, ["schema_version", "kind", "root", "objects", "events", "indexes", "commit_head", "hot_window", "full_history", "dedup_key", "event_identity", "migration", "garbage_collection", "target_owned_data", "active_projection", ...store.diagnostics === undefined ? [] : ["diagnostics"]], "Runtime contract.task_store");
  if (store.schema_version !== 1 || store.kind !== "vnext-task-store-contract" || store.root !== "<workflow_home>/task-data/<document_id>" || store.objects !== "objects/<sha256>.json" || store.events !== "events/<sequence>-<sha256>.json" || store.indexes !== "rebuildable-and-non-authoritative" || store.commit_head !== "CURRENT_TASK-single-submission-head" || store.hot_window !== "cache-only" || store.full_history !== "persistent-and-queryable" || store.dedup_key !== "schema-object-type-document-id-complete-content" || store.event_identity !== "sequence-time-cause-and-idempotency-preserved" || store.migration !== "preview-confirm-commit-with-source-revision" || store.garbage_collection !== "disabled-in-v1" || store.target_owned_data !== "task-data-is-never-removed-by-distribution-upgrade") {
    fail3("RUNTIME_CONTRACT_INVALID", "task_store must retain the content-addressed, append-only aggregate contract.");
  }
  const projection = expectRecord2(store.active_projection, "Runtime contract.task_store.active_projection");
  const expected = {
    format: ACTIVE_TASK_FORMAT,
    canonical_roots: "CURRENT_TASK-definition-state-and-acknowledged-manifest",
    material: "immutable-content-addressed-definition-state-reports",
    history: "preserve-exact-preimages-events-and-idempotency",
    legacy: "read-without-rewrite-and-explicit-storage-migration",
    admission: "no-size-quota-or-business-transition",
    receipt_continuity: "committed-storage-only-lineage-and-equal-logical-state",
    wire_compatibility: "frozen-v3-model-not-current-serializer-bytes"
  };
  if (digest3(projection) !== digest3(expected))
    fail3("RUNTIME_CONTRACT_INVALID", "task projection must preserve the canonical aggregate and legacy semantics.");
  if (store.diagnostics !== undefined && digest3(store.diagnostics) !== digest3({
    response: "validate --summary/storage_metrics",
    kind: "task-storage-metrics/v1",
    read_only: true,
    authority: "none",
    persistence: "none",
    incomplete: "null-with-reason-not-a-gate"
  }))
    fail3("RUNTIME_CONTRACT_INVALID", "Storage diagnostics must remain optional read-only observations.");
}
function validateBootstrapRuntimeContract(value) {
  const bootstrap = expectRecord2(value, "vNext Runtime contract.bootstrap_project");
  expectExactKeys2(bootstrap, ["schema_version", "kind", "caller", "modes", "required_envelope", "mutation_scope", "asset_boundary", "operations", "recovery", "read_back"], "vNext Runtime contract.bootstrap_project");
  if (bootstrap.schema_version !== 1 || bootstrap.kind !== "vnext-bootstrap-runtime-contract") {
    fail3("RUNTIME_CONTRACT_INVALID", "bootstrap_project must declare the vNext bootstrap Runtime contract marker.");
  }
  expectSetEqual(expectStringArray2(bootstrap.caller, "Runtime contract.bootstrap_project.caller"), ["bootstrap-project"], "bootstrap Runtime callers");
  expectSetEqual(expectStringArray2(bootstrap.modes, "Runtime contract.bootstrap_project.modes"), [...BOOTSTRAP_MODES], "bootstrap Runtime modes");
  expectSetEqual(expectStringArray2(bootstrap.required_envelope, "Runtime contract.bootstrap_project.required_envelope"), ["authority_evidence", "semantic_operations", "preconditions", "evidence_refs", "idempotency_key", "requested_write_targets", "requested_directory_targets", "changed_paths", "scope_document", "conditional_authorizations", "transformation_kind", "assets"], "bootstrap Runtime proposal envelope");
  const scope = expectRecord2(bootstrap.mutation_scope, "Runtime contract.bootstrap_project.mutation_scope");
  expectExactKeys2(scope, ["status", "binding", "source", "default_write_policy", "conditional_expansion_requires", "read_discovery_is_not_write_authority", "check_command", "input", "output"], "Runtime contract.bootstrap_project.mutation_scope");
  if (scope.status !== "bound" || scope.binding !== "vnext-runtime-read-only" || scope.source !== "bootstrap proposal.scope_document" || scope.default_write_policy !== "deny" || scope.conditional_expansion_requires !== "evidence-and-authority" || scope.read_discovery_is_not_write_authority !== true || scope.check_command !== "shared-mutation-scope-evaluator") {
    fail3("RUNTIME_CONTRACT_INVALID", "bootstrap mutation scope must keep the shared default-deny evaluator boundary.");
  }
  const scopeInput = expectRecord2(scope.input, "Runtime contract.bootstrap_project.mutation_scope.input");
  expectExactKeys2(scopeInput, ["required"], "Runtime contract.bootstrap_project.mutation_scope.input");
  expectSetEqual(expectStringArray2(scopeInput.required, "Runtime contract.bootstrap_project.mutation_scope.input.required"), ["explicit_changed_paths", "conditional_authorizations_with_evidence_and_authority", "transformation_kind"], "bootstrap mutation scope input");
  const scopeOutput = expectRecord2(scope.output, "Runtime contract.bootstrap_project.mutation_scope.output");
  expectExactKeys2(scopeOutput, ["required"], "Runtime contract.bootstrap_project.mutation_scope.output");
  expectSetEqual(expectStringArray2(scopeOutput.required, "Runtime contract.bootstrap_project.mutation_scope.output.required"), ["per-path-admission-and-blocker", "source-revision"], "bootstrap mutation scope output");
  const assetBoundary = expectRecord2(bootstrap.asset_boundary, "Runtime contract.bootstrap_project.asset_boundary");
  expectExactKeys2(assetBoundary, ["allowed_roots", "forbidden_targets", "generated_categories"], "Runtime contract.bootstrap_project.asset_boundary");
  expectSetEqual(expectStringArray2(assetBoundary.allowed_roots, "Runtime contract.bootstrap_project.asset_boundary.allowed_roots"), [".workflow-system/PROJECT_PROFILE.yaml", ".workflow-system/vnext/BOOTSTRAP_RECEIPT.json", "docs/workflow/", "docs/designs/", "docs/adoption/", "AGENTS.md", "CLAUDE.md"], "bootstrap allowed asset roots");
  const forbiddenTargets = expectStringArray2(assetBoundary.forbidden_targets, "Runtime contract.bootstrap_project.asset_boundary.forbidden_targets");
  for (const required of [".workflow-system/WORKFLOW_PROTOCOL.md", ".workflow-system/FILE_SCHEMAS.md", ".workflow-system/vnext/SOURCE_CONTRACT.yaml", ".workflow-system/vnext/RUNTIME_CONTRACT.yaml", ".workflow-system/runtime/**", ".agents/skills/**"]) {
    if (!forbiddenTargets.includes(required))
      fail3("RUNTIME_CONTRACT_INVALID", `bootstrap forbidden target must include ${required}.`);
  }
  expectSetEqual(expectStringArray2(assetBoundary.generated_categories, "Runtime contract.bootstrap_project.asset_boundary.generated_categories"), ["config", "generated", "governance"], "bootstrap generated asset categories");
  const operations = bootstrap.operations;
  if (!Array.isArray(operations) || operations.length !== BOOTSTRAP_OPERATION_KINDS.length)
    fail3("RUNTIME_CONTRACT_INVALID", `bootstrap_project must declare exactly ${BOOTSTRAP_OPERATION_KINDS.length} typed operations.`);
  const bound = [];
  const expectedOperations = {
    "contract-candidate-commit": { source: ["source-authority evidence", "existing CONTRACTS.md when present"], writes: ["CONTRACTS.md"] },
    "decision-record-transaction": { source: ["source-authority evidence", "existing DECISIONS.md when present"], writes: ["DECISIONS.md"] },
    "project-status-transaction": { source: ["STATUS.md"], writes: ["STATUS.md"] },
    "paired-host-guidance-transaction": { source: ["target host guidance"], writes: ["paired host guidance"] }
  };
  for (const [index, rawOperation] of operations.entries()) {
    const operation = expectRecord2(rawOperation, `Runtime contract.bootstrap_project.operations[${index}]`);
    expectExactKeys2(operation, ["id", "status", "binding", "operation", "source_targets", "write_targets", "allowed_callers", "result_states", "atomic", "idempotence", "conflict_policy"], `Runtime contract.bootstrap_project.operations[${index}]`);
    const id = expectString2(operation.id, `Runtime contract.bootstrap_project.operations[${index}].id`);
    if (!BOOTSTRAP_OPERATION_KINDS.includes(id) || bound.includes(id))
      fail3("RUNTIME_CONTRACT_INVALID", `bootstrap operation ${id} is not in the closed operation set.`);
    if (operation.status !== "bound" || operation.binding !== "vnext-runtime" || operation.operation !== id)
      fail3("RUNTIME_CONTRACT_INVALID", `bootstrap operation ${id} must be bound to vnext-runtime.`);
    const expected = expectedOperations[id];
    expectSetEqual(expectStringArray2(operation.source_targets, `bootstrap operation ${id}.source_targets`), expected.source, `bootstrap operation ${id}.source_targets`);
    expectSetEqual(expectStringArray2(operation.write_targets, `bootstrap operation ${id}.write_targets`), expected.writes, `bootstrap operation ${id}.write_targets`);
    expectSetEqual(expectStringArray2(operation.allowed_callers, `bootstrap operation ${id}.allowed_callers`), ["bootstrap-project"], `bootstrap operation ${id}.allowed_callers`);
    expectSetEqual(expectStringArray2(operation.result_states, `bootstrap operation ${id}.result_states`), [...RUNTIME_RESULT_STATES], `bootstrap operation ${id}.result_states`);
    if (operation.atomic !== true || operation.idempotence !== "fail-closed" || operation.conflict_policy !== "fail-closed")
      fail3("RUNTIME_CONTRACT_INVALID", `bootstrap operation ${id} must be atomic, fail-closed, and conflict-safe.`);
    bound.push(id);
  }
  expectSetEqual(bound, [...BOOTSTRAP_OPERATION_KINDS], "bootstrap Runtime bound operations");
  const recovery = expectRecord2(bootstrap.recovery, "Runtime contract.bootstrap_project.recovery");
  expectExactKeys2(recovery, ["marker", "interrupted", "rollback"], "Runtime contract.bootstrap_project.recovery");
  if (recovery.marker !== ".workflow-system/vnext/BOOTSTRAP_IN_PROGRESS.json" || recovery.interrupted !== "fail-closed-explicit-recovery" || recovery.rollback !== "verify-scoped-pre-bootstrap-preimage-before-marker-clear")
    fail3("RUNTIME_CONTRACT_INVALID", "bootstrap recovery must use the explicit interruption marker and verified scoped rollback boundary.");
  const readBack = expectRecord2(bootstrap.read_back, "Runtime contract.bootstrap_project.read_back");
  expectExactKeys2(readBack, ["required"], "Runtime contract.bootstrap_project.read_back");
  expectSetEqual(expectStringArray2(readBack.required, "Runtime contract.bootstrap_project.read_back.required"), ["asset-checksums", "project-identity", "runtime-contract", "distribution-prerequisite", "canonical-CURRENT_TASK", "host-isolation"], "bootstrap read-back evidence");
  return bound;
}
function validateVNextRuntimeContract(root2, requireDependencies = false) {
  const filePath = path12.join(path12.resolve(root2), ...VNEXT_RUNTIME_CONTRACT_RELATIVE_PATH.split("/"));
  const contract = parseYamlMappingFile(filePath);
  expectExactKeys2(contract, ["schema_version", "kind", "phase", "runtime_distribution", "task_context", "task_store", "proposal", "mutation_scope", "mutation_authority", "review_coverage_preimages", "canonical_current_task", "concurrency", "operations", "unbound_operations", "bootstrap_project"], "vNext Runtime contract");
  if (contract.schema_version !== 1 || contract.kind !== "vnext-runtime-contract" || contract.phase !== "Phase 2") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract must declare schema_version=1, kind=vnext-runtime-contract, phase=Phase 2.");
  }
  const runtimeDistribution = validateRuntimeDistributionContract(contract.runtime_distribution);
  const distributionIdentity = validateVNextRuntimeDistribution(root2, runtimeDistribution, requireDependencies);
  validateTaskContextContract(contract.task_context);
  validateTaskStoreContract(contract.task_store);
  const proposal = expectRecord2(contract.proposal, "Runtime contract.proposal");
  expectExactKeys2(proposal, ["schema_version", "kind", "caller", "operation_kinds", "source_tuple", "required_envelope", "finding_queue_admission", "finding_queue_repair", "process_control", "task_state", "execute_step", "review_change", "prepare_task", "inbox_record", "lifecycle", "close_task", "lesson_marker"], "Runtime contract.proposal");
  if (proposal.schema_version !== 1 || proposal.kind !== VNEXT_RUNTIME_PROPOSAL_KIND)
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime proposal contract has an invalid envelope marker.");
  expectSetEqual(expectStringArray2(proposal.caller, "Runtime contract.proposal.caller"), ["execute-step", "review-change", "prepare-task", "task-lifecycle", "capture-work-item", "close-task"], "Runtime contract proposal callers");
  expectSetEqual(expectStringArray2(proposal.operation_kinds, "Runtime contract.proposal.operation_kinds"), [...RUNTIME_OPERATION_KINDS], "Runtime contract operation kinds");
  expectSetEqual(expectStringArray2(proposal.source_tuple, "Runtime contract.proposal.source_tuple"), [...RUNTIME_SOURCE_TUPLE_FIELDS], "Runtime contract source tuple");
  expectSetEqual(expectStringArray2(proposal.required_envelope, "Runtime contract.proposal.required_envelope"), [...RUNTIME_REQUIRED_ENVELOPE_FIELDS], "Runtime contract proposal envelope");
  const findingQueueRepair = expectRecord2(proposal.finding_queue_repair, "Runtime contract.proposal.finding_queue_repair");
  expectExactKeys2(findingQueueRepair, ["required"], "Runtime contract.proposal.finding_queue_repair");
  expectSetEqual(expectStringArray2(findingQueueRepair.required, "Runtime contract.proposal.finding_queue_repair.required"), ["review_cycle_id", "repair_wave_id"], "Runtime contract finding-queue repair fields");
  const findingQueueAdmission = expectRecord2(proposal.finding_queue_admission, "Runtime contract.proposal.finding_queue_admission");
  expectExactKeys2(findingQueueAdmission, ["required"], "Runtime contract.proposal.finding_queue_admission");
  expectSetEqual(expectStringArray2(findingQueueAdmission.required, "Runtime contract.proposal.finding_queue_admission.required"), ["cycle_phase", "finding_admission_wave_id"], "Runtime contract finding-queue admission fields");
  const processControl = expectRecord2(proposal.process_control, "Runtime process_control");
  const processSemantics = {
    user_evidence: "exact-frozen-slot-and-subject; append-verbatim-Task-Basis; caller-reported-not-authenticated",
    waiver: "user-owned-current-obligation-only; preserve-failed-or-missing-report; exact-result-waiver-decision-id; validation-labels-require-exclusive-frozen-check-ownership-at-record-and-consume; no-review-or-policy-bypass",
    successor: "explicit-predecessor-decision-and-complete-obligation-map; fresh-draft-identity; predecessor-remains-superseded; exact-prepared-orphan-retry-only; ordinary-confirmation-required",
    validation_adjustment: "replace-validation; one-read-only-check; preserve-observation-boundary-subjects-validation-ownership-exact-granularity-selector-authority-and-budgets; no-task-redefinition"
  };
  expectExactKeys2(processControl, Object.keys(processSemantics), "Runtime process_control");
  for (const [key, expected] of Object.entries(processSemantics)) {
    if (processControl[key] !== expected)
      fail3("RUNTIME_CONTRACT_INVALID", `Runtime process_control.${key} differs from its task-level semantics.`);
  }
  const taskStateContract = expectRecord2(proposal.task_state, "Runtime contract.proposal.task_state");
  expectExactKeys2(taskStateContract, ["actions", "execution_admission", "retry_step", "step_progress", "preflight", "claim_evidence", "claim_evidence_migration", "advancement_outcomes", "review_receipt", "review_result", "draft", "confirm"], "Runtime contract.proposal.task_state");
  expectSetEqual(expectStringArray2(taskStateContract.actions, "Runtime contract.proposal.task_state.actions"), ["retry-step", "record-step-preflight", "extend-preflight", "step-progress", "consume-retained-review", "clear-resume-review-gate", "record-evidence-challenge", "dismiss-evidence-challenge", "record-user-evidence", ...DRAFT_TASK_STATE_ACTIONS, ...CLAIM_EVIDENCE_MIGRATION_ACTIONS, ...REVIEW_TASK_STATE_ACTIONS, ...REPLAN_TASK_STATE_ACTIONS, "commit-scope-amendment"], "Runtime contract task-state actions");
  const executionAdmissionContract = expectRecord2(taskStateContract.execution_admission, "Runtime contract.proposal.task_state.execution_admission");
  expectExactKeys2(executionAdmissionContract, ["evaluator", "question", "shared_by", "classifications", "mutation_order", "error_mapping", "semantic_boundary"], "Runtime contract.proposal.task_state.execution_admission");
  if (executionAdmissionContract.evaluator !== "runtime-owned-exact-target-admission/v1" || executionAdmissionContract.question !== "current-task-step-phase-target-assessment-may-enter-this-execution" || executionAdmissionContract.semantic_boundary !== "runtime-owns-structural-checks-agent-owns-blast-radius-judgment") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime execution admission evaluator contract is invalid.");
  }
  expectSetEqual(expectStringArray2(executionAdmissionContract.shared_by, "Runtime contract.proposal.task_state.execution_admission.shared_by"), ["record-step-preflight", "extend-preflight", "begin-repair", "repair-extension", "record-step-result", "command-footprint-preflight"], "Runtime execution admission shared entry points");
  expectSetEqual(expectStringArray2(executionAdmissionContract.classifications, "Runtime contract.proposal.task_state.execution_admission.classifications"), [...EXECUTION_ADMISSION_CLASSIFICATIONS], "Runtime execution admission classifications");
  expectSetEqual(expectStringArray2(executionAdmissionContract.mutation_order, "Runtime contract.proposal.task_state.execution_admission.mutation_order"), ["explicit-forbidden-and-governance", "test-strategy-and-non-executable-policy", "v1-or-v2-authority", "assessment-and-persistent-test-admission"], "Runtime execution admission mutation order");
  const executionAdmissionErrors = expectRecord2(executionAdmissionContract.error_mapping, "Runtime contract.proposal.task_state.execution_admission.error_mapping");
  expectExactKeys2(executionAdmissionErrors, ["blocked_assessment", "blocked_escalation", "blocked_authority"], "Runtime contract.proposal.task_state.execution_admission.error_mapping");
  if (executionAdmissionErrors.blocked_assessment !== "MUTATION_BLAST_RADIUS_ASSESSMENT_REQUIRED" || executionAdmissionErrors.blocked_escalation !== "MUTATION_BLAST_RADIUS_ESCALATION_REQUIRED" || executionAdmissionErrors.blocked_authority !== "MUTATION_AUTHORITY_EXPANSION_REQUIRED") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime execution admission error mapping is invalid.");
  }
  const retryContract = expectRecord2(taskStateContract.retry_step, "Runtime contract.proposal.task_state.retry_step");
  expectExactKeys2(retryContract, ["max_attempts", "environment_report", "same_plan_repair_diagnosis", "repair_paths", "failure_preservation", "result_required"], "Runtime contract.proposal.task_state.retry_step");
  if (retryContract.max_attempts !== 3 || retryContract.environment_report !== "environment-restored/v1" || retryContract.same_plan_repair_diagnosis !== "same-plan-repair/v1" || retryContract.repair_paths !== "failed-preflight-subset" || retryContract.failure_preservation !== "durable-step-attempts" || retryContract.result_required !== "fresh-preflight-and-execution")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime retry contract must retain bounded same-plan recovery and fresh execution.");
  const stepProgressContract = expectRecord2(taskStateContract.step_progress, "Runtime contract.proposal.task_state.step_progress");
  expectExactKeys2(stepProgressContract, ["required", "optional"], "Runtime contract.proposal.task_state.step_progress");
  expectSetEqual(expectStringArray2(stepProgressContract.required, "Runtime contract.proposal.task_state.step_progress.required"), ["step_id", "status", "evidence_refs"], "Runtime contract task-state required fields");
  expectSetEqual(expectStringArray2(stepProgressContract.optional, "Runtime contract.proposal.task_state.step_progress.optional", true), ["note", "repair_fingerprint", "repair_fingerprints", "repair_wave_id", "change_set_id", "review_receipt", "claim_evidence", "execution_result"], "Runtime contract task-state optional fields");
  const preflightContract = expectRecord2(taskStateContract.preflight, "Runtime contract.proposal.task_state.preflight");
  expectExactKeys2(preflightContract, ["initial", "extension"], "Runtime contract.proposal.task_state.preflight");
  const initialPreflight = expectRecord2(preflightContract.initial, "Runtime contract.proposal.task_state.preflight.initial");
  expectExactKeys2(initialPreflight, ["action", "planned_footprint", "in_envelope_unplanned", "outside_envelope", "first_touch", "evaluator", "execution_identity"], "Runtime contract.proposal.task_state.preflight.initial");
  if (initialPreflight.action !== "record-step-preflight" || initialPreflight.planned_footprint !== "normal-admission" || initialPreflight.in_envelope_unplanned !== "blast-radius-assessment-and-self-admit" || initialPreflight.outside_envelope !== "MUTATION_AUTHORITY_EXPANSION_REQUIRED" || initialPreflight.first_touch !== "runtime-captured-before-state" || initialPreflight.evaluator !== "execution-admission" || initialPreflight.execution_identity !== "stable-per-execution-with-replacement-receipt") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime initial preflight authority semantics are invalid.");
  }
  const extensionPreflight = expectRecord2(preflightContract.extension, "Runtime contract.proposal.task_state.preflight.extension");
  expectExactKeys2(extensionPreflight, ["action", "required", "optional", "same_attempt", "plan_revision_change", "retry_budget_change", "continuation", "replacement_receipt", "review", "repair", "first_touch"], "Runtime contract.proposal.task_state.preflight.extension");
  expectSetEqual(expectStringArray2(extensionPreflight.required, "Runtime contract.proposal.task_state.preflight.extension.required"), ["current_preflight_id", "additional_targets", "blast_radius_assessments", "evidence_refs"], "Runtime extend-preflight required fields");
  expectSetEqual(expectStringArray2(extensionPreflight.optional, "Runtime contract.proposal.task_state.preflight.extension.optional", true), ["mode", "execution_id", "execution_phase", "repair_wave_id", "review_id"], "Runtime extend-preflight optional fields");
  if (extensionPreflight.action !== "extend-preflight" || extensionPreflight.same_attempt !== true || extensionPreflight.plan_revision_change !== "forbidden" || extensionPreflight.retry_budget_change !== "forbidden" || extensionPreflight.continuation !== "forbidden" || extensionPreflight.replacement_receipt !== "required" || extensionPreflight.review !== "risk-selected-cumulative-review-for-self-admitted-expansion" || extensionPreflight.repair !== "same-evaluator-same-wave-no-retry-consumption" || extensionPreflight.first_touch !== "append-only-for-new-targets") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime extend-preflight semantics are invalid.");
  }
  const claimEvidenceContract = expectRecord2(taskStateContract.claim_evidence, "Runtime contract.proposal.task_state.claim_evidence");
  expectExactKeys2(claimEvidenceContract, ["stored_in", "report_binding", "record_fields", "slot_fields", "complete_dispositions", "incomplete_dispositions", "execution_selection", "completion_rule"], "Runtime contract.proposal.task_state.claim_evidence");
  if (claimEvidenceContract.stored_in !== "canonical CURRENT_TASK.runtime_state") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime claim evidence must be stored in the canonical CURRENT_TASK runtime state.");
  }
  if (claimEvidenceContract.report_binding !== "exact-claim-slot-check-only; command pass never creates or satisfies another slot") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime claim evidence reports must remain bound to one exact claim/slot/check.");
  }
  expectSetEqual(expectStringArray2(claimEvidenceContract.record_fields, "Runtime contract claim evidence record_fields"), ["claim_id", "claim_kind", "requirement", "source_ref", "slots"], "Runtime claim evidence record fields");
  expectSetEqual(expectStringArray2(claimEvidenceContract.slot_fields, "Runtime contract claim evidence slot_fields"), ["slot_id", "minimum_type", "disposition", "evidence_refs", "due_step_id", "applicability", "before_step_id", "check", "report", "prerequisite_receipt", "user_decision"], "Runtime claim evidence slot fields");
  expectSetEqual(expectStringArray2(claimEvidenceContract.complete_dispositions, "Runtime contract claim evidence complete_dispositions"), ["existing", "reused", "newly-executed"], "Runtime claim evidence complete dispositions");
  expectSetEqual(expectStringArray2(claimEvidenceContract.incomplete_dispositions, "Runtime contract claim evidence incomplete_dispositions"), ["missing", "deferred", "blocked"], "Runtime claim evidence incomplete dispositions");
  const executionSelection = expectRecord2(claimEvidenceContract.execution_selection, "Runtime contract claim evidence execution_selection");
  expectExactKeys2(executionSelection, ["fields", "invocation_kinds", "check_boundaries", "granularities", "breadth_bases", "freshness", "command_binding", "business_flow", "authority_binding", "e2e_admission", "broad_admission"], "Runtime contract claim evidence execution_selection");
  expectSetEqual(expectStringArray2(executionSelection.invocation_kinds, "Runtime execution invocation kinds"), ["structured", "opaque"], "Runtime execution invocation kinds");
  expectSetEqual(expectStringArray2(executionSelection.fields, "Runtime claim evidence execution selection fields"), ["granularity", "selector", "selection_reason", "breadth_reason", "breadth_basis", "breadth_source_ref", "invocation"], "Runtime claim evidence execution selection fields");
  expectSetEqual(expectStringArray2(executionSelection.granularities, "Runtime claim evidence execution granularities"), [...EVIDENCE_EXECUTION_GRANULARITIES], "Runtime claim evidence execution granularities");
  expectSetEqual(expectStringArray2(executionSelection.check_boundaries, "Runtime claim evidence check boundaries"), [...EVIDENCE_CHECK_BOUNDARIES], "Runtime claim evidence check boundaries");
  expectSetEqual(expectStringArray2(executionSelection.breadth_bases, "Runtime claim evidence execution selection breadth bases"), [...EVIDENCE_EXECUTION_BREADTH_BASES], "Runtime claim evidence execution selection breadth bases");
  if (executionSelection.freshness !== "new-or-changed-check-identity-across-draft-update-confirm-replan-recovery; unchanged-confirmed-legacy-compatible" || executionSelection.command_binding !== "structured-argv-selector-or-verbatim-opaque-command-equals-entry-and-exact-due-step-planned-command; opaque-cannot-claim-focused" || executionSelection.business_flow !== "per-check-boundary-independent-of-granularity; same-claim-rule-and-flow-slots; no-test-type-hierarchy" || executionSelection.authority_binding !== "explicit-user-exact-task-basis; policy-and-release-canonical-repository-source; claim-bases-exact-current-claim-and-its-existing-task-basis-or-repository-source" || executionSelection.e2e_admission !== "acceptance-boundary-or-low-cost-insufficiency-or-project-policy-or-release-gate-or-explicit-user" || executionSelection.broad_admission !== "claim-risk-contract-or-project-policy-or-release-gate-or-explicit-user") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime execution selection contract is invalid.");
  }
  if (claimEvidenceContract.completion_rule !== CLAIM_EVIDENCE_COMPLETION_RULE) {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime claim evidence completion must require a non-empty frozen plan with an acceptance claim and every planned slot with its evidence_refs.");
  }
  const claimEvidenceMigrationContract = expectRecord2(taskStateContract.claim_evidence_migration, "Runtime contract.proposal.task_state.claim_evidence_migration");
  expectExactKeys2(claimEvidenceMigrationContract, ["mode", "action", "required", "from", "to", "mutation", "preserves"], "Runtime contract.proposal.task_state.claim_evidence_migration");
  if (claimEvidenceMigrationContract.mode !== "default" || claimEvidenceMigrationContract.action !== "migrate-claim-evidence") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime claim evidence migration must use default/migrate-claim-evidence.");
  }
  expectSetEqual(expectStringArray2(claimEvidenceMigrationContract.required, "Runtime contract claim evidence migration required"), ["claim_evidence", "evidence_refs"], "Runtime contract claim evidence migration required fields");
  if (claimEvidenceMigrationContract.from !== "active + active legacy-or-empty-plan" || claimEvidenceMigrationContract.to !== "active + active") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime claim evidence migration transition is invalid.");
  }
  if (claimEvidenceMigrationContract.mutation !== "reconstruct legacy claim/slot/check identities; execution entries must equal existing planned commands at their original due steps; modern selection metadata is not required") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime claim evidence migration mutation boundary is invalid.");
  }
  expectSetEqual(expectStringArray2(claimEvidenceMigrationContract.preserves, "Runtime contract claim evidence migration preserves"), ["TASK_ID", "TASK_SLUG", "document_id", "goal", "scope", "acceptance", "implementation steps", "active step/status", "execution history", "findings", "review state", "lifecycle tuple"], "Runtime claim evidence migration preserved fields");
  expectSetEqual(expectStringArray2(taskStateContract.advancement_outcomes, "Runtime contract.proposal.task_state.advancement_outcomes"), [...STEP_ADVANCEMENT_OUTCOMES], "Runtime contract task-state advancement outcomes");
  const reviewReceiptContract = expectRecord2(taskStateContract.review_receipt, "Runtime contract.proposal.task_state.review_receipt");
  expectExactKeys2(reviewReceiptContract, ["required", "verdict", "cycle_phase", "target_verification"], "Runtime contract.proposal.task_state.review_receipt");
  expectSetEqual(expectStringArray2(reviewReceiptContract.required, "Runtime contract.proposal.task_state.review_receipt.required"), ["cycle_id", "cycle_phase", "change_set_id", "review_target_revision", "verdict", "admitted_fingerprints", "evidence_refs"], "Runtime contract review receipt required fields");
  if (reviewReceiptContract.verdict !== "clean")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract review receipt verdict must remain clean.");
  expectSetEqual(expectStringArray2(reviewReceiptContract.cycle_phase, "Runtime contract.proposal.task_state.review_receipt.cycle_phase"), [...REVIEW_CYCLE_PHASES], "Runtime contract review receipt cycle phases");
  if (reviewReceiptContract.target_verification !== "runtime-file-manifest")
    fail3("RUNTIME_CONTRACT_INVALID", "review receipt target verification must be runtime-file-manifest.");
  const reviewResultContract = expectRecord2(taskStateContract.review_result, "Runtime contract.proposal.task_state.review_result");
  expectExactKeys2(reviewResultContract, ["stored_in", "verdicts", "binds", "consumed_by", "test_assessment"], "Runtime contract.proposal.task_state.review_result");
  expectSetEqual(expectStringArray2(reviewResultContract.test_assessment, "review_result.test_assessment"), ["applicable", "reason", "evidence_refs", "necessity", "oracle", "boundary", "reuse", "applicability"], "test assessment fields");
  if (reviewResultContract.stored_in !== "canonical CURRENT_TASK.runtime_state.pending_review_result")
    fail3("RUNTIME_CONTRACT_INVALID", "review result must use canonical pending review storage.");
  expectSetEqual(expectStringArray2(reviewResultContract.verdicts, "Runtime contract review-result verdicts"), [...REVIEW_RESULT_VERDICTS], "Runtime contract review-result verdicts");
  expectSetEqual(expectStringArray2(reviewResultContract.binds, "Runtime contract review-result bindings"), ["active_step_id", "review_cycle_id", "latest_execution_id", "change_set_id", "review_target_revision"], "Runtime contract review-result bindings");
  const reviewResultConsumers = expectRecord2(reviewResultContract.consumed_by, "Runtime contract review-result consumers");
  expectExactKeys2(reviewResultConsumers, ["clean", "findings", "blocked"], "Runtime contract review-result consumers");
  if (reviewResultConsumers.clean !== "execute-step:complete-reviewed-step" || reviewResultConsumers.findings !== "execute-step:begin-repair" || reviewResultConsumers.blocked !== "caller-route")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime review-result consumers are invalid.");
  const draftContract = expectRecord2(taskStateContract.draft, "Runtime contract.proposal.task_state.draft");
  expectExactKeys2(draftContract, ["mode", "actions", "identity_required", "definition_required", "claim_evidence", "task_basis", "create_from", "update_from", "target", "previous_close_reconciliation", "step_admission", "preserves"], "Runtime contract.proposal.task_state.draft");
  if (draftContract.mode !== "default")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract task-state draft mode must remain default.");
  expectSetEqual(expectStringArray2(draftContract.actions, "Runtime contract.proposal.task_state.draft.actions"), ["create-draft", "update-draft"], "Runtime contract task-state draft actions");
  expectSetEqual(expectStringArray2(draftContract.identity_required, "Runtime contract.proposal.task_state.draft.identity_required"), ["task_id", "task_slug", "document_id", "task_title"], "Runtime contract task-state draft identity fields");
  expectSetEqual(expectStringArray2(draftContract.definition_required, "Runtime contract.proposal.task_state.draft.definition_required"), [...REPLAN_REPLACEMENT_FIELDS], "Runtime contract task-state draft definition fields");
  if (draftContract.claim_evidence !== DRAFT_CLAIM_EVIDENCE_REQUIREMENT) {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime draft claim evidence requirement is invalid.");
  }
  const draftTaskBasis = expectRecord2(draftContract.task_basis, "Runtime contract.proposal.task_state.draft.task_basis");
  expectExactKeys2(draftTaskBasis, ["required", "source_fields", "storage", "write_binding", "review_results_forbidden"], "Runtime contract.proposal.task_state.draft.task_basis");
  expectSetEqual(expectStringArray2(draftTaskBasis.required, "Runtime contract draft task basis required fields"), ["original_request", "user_decisions"], "Runtime contract draft task basis required fields");
  expectSetEqual(expectStringArray2(draftTaskBasis.source_fields, "Runtime contract draft task basis source fields"), ["source", "verbatim"], "Runtime contract draft task basis source fields");
  if (draftTaskBasis.storage !== "identity-derived-linked-document" || draftTaskBasis.write_binding !== "same-atomic-draft-transaction" || draftTaskBasis.review_results_forbidden !== true) {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime draft task basis storage boundary is invalid.");
  }
  for (const [field, expected] of [["create_from", "closed + archived"], ["update_from", "draft + active"], ["target", "draft + active"]]) {
    if (draftContract[field] !== expected)
      fail3("RUNTIME_CONTRACT_INVALID", `Runtime contract task-state draft ${field} must be ${expected}.`);
  }
  const draftReconciliation = expectRecord2(draftContract.previous_close_reconciliation, "Runtime contract.proposal.task_state.draft.previous_close_reconciliation");
  expectExactKeys2(draftReconciliation, ["archive", "status", "admitted_lesson"], "Runtime contract.proposal.task_state.draft.previous_close_reconciliation");
  if (draftReconciliation.archive !== "required" || draftReconciliation.status !== "non-blocking" || draftReconciliation.admitted_lesson !== "required-or-durable-reuse-proof") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract task-state draft previous_close_reconciliation requirements are invalid.");
  }
  const draftStepAdmission = expectRecord2(draftContract.step_admission, "Runtime contract.proposal.task_state.draft.step_admission");
  expectExactKeys2(draftStepAdmission, ["all_steps_metadata_complete", "active_step"], "Runtime contract.proposal.task_state.draft.step_admission");
  if (draftStepAdmission.all_steps_metadata_complete !== true || draftStepAdmission.active_step !== "first-admitted-step") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract task-state draft step_admission requirements are invalid.");
  }
  expectSetEqual(expectStringArray2(draftContract.preserves, "Runtime contract.proposal.task_state.draft.preserves"), ["TASK_ID", "TASK_SLUG", "document_id on update", "execution_log", "applied_proposals", "canonical provenance"], "Runtime contract task-state draft preserved fields");
  const confirmContract = expectRecord2(taskStateContract.confirm, "Runtime contract.proposal.task_state.confirm");
  expectExactKeys2(confirmContract, ["mode", "action", "required", "authority", "authority_coordinates", "from", "to"], "Runtime contract.proposal.task_state.confirm");
  if (confirmContract.mode !== "confirm" || confirmContract.action !== "confirm-draft")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract task-state confirm must use confirm/confirm-draft.");
  expectSetEqual(expectStringArray2(confirmContract.required, "Runtime contract.proposal.task_state.confirm.required"), ["task_id", "task_slug", "document_id", "draft_revision", "evidence_refs"], "Runtime contract task-state confirm required fields");
  expectSetEqual(expectStringArray2(confirmContract.authority, "Runtime contract.proposal.task_state.confirm.authority"), ["user-confirmation", "authorized-caller"], "Runtime contract task-state confirm authority");
  const confirmCoords = expectRecord2(confirmContract.authority_coordinates, "Runtime contract.proposal.task_state.confirm.authority_coordinates");
  expectExactKeys2(confirmCoords, ["required", "exact_draft_revision"], "Runtime contract.proposal.task_state.confirm.authority_coordinates");
  expectSetEqual(expectStringArray2(confirmCoords.required, "Runtime contract.proposal.task_state.confirm.authority_coordinates.required"), ["task_id", "document_id", "draft_revision"], "Runtime contract task-state confirm authority coordinates");
  if (confirmCoords.exact_draft_revision !== true)
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract task-state confirm authority_coordinates exact_draft_revision must be true.");
  if (confirmContract.from !== "draft + active" || confirmContract.to !== "active + active")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract task-state confirm transition is invalid.");
  const executeStepContract = expectRecord2(proposal.execute_step, "Runtime contract.proposal.execute_step");
  expectExactKeys2(executeStepContract, ["semantic_adapter", "bound_actions"], "Runtime contract.proposal.execute_step");
  const executeStepAdapter = expectRecord2(executeStepContract.semantic_adapter, "Runtime contract.proposal.execute_step.semantic_adapter");
  expectExactKeys2(executeStepAdapter, [
    "input",
    "commands",
    "step_source",
    "scope_enforcement",
    "command_plan_source",
    "persistent_tests_enforcement",
    "test_strategy_execution",
    "completion_evidence_source",
    "change_detection",
    "proposal_file_policy",
    "advancement_owner",
    "post_completion_commit_owner",
    "post_completion_route"
  ], "Runtime contract.proposal.execute_step.semantic_adapter");
  if (executeStepAdapter.input !== "stdin-json")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime execute-step adapter input must remain stdin-json.");
  expectSetEqual(expectStringArray2(executeStepAdapter.commands, "Runtime contract.proposal.execute_step.semantic_adapter.commands"), ["preflight-step", "extend-preflight", "evidence-context", "retry-step", "replace-validation", "begin-repair", "record-step-result", "complete-reviewed-step"], "Runtime contract execute-step adapter commands");
  const testStrategyExecution = expectRecord2(executeStepAdapter.test_strategy_execution, "Runtime contract.proposal.execute_step.semantic_adapter.test_strategy_execution");
  expectExactKeys2(testStrategyExecution, ["phase_source", "legacy_behavior", "test_first", "red_evidence", "non_red_outcome"], "Runtime contract execute-step test-strategy execution");
  const testFirstExecution = expectRecord2(testStrategyExecution.test_first, "Runtime contract execute-step test-first execution");
  expectExactKeys2(testFirstExecution, ["first_step_phase", "later_step_phase", "first_step_outcome", "later_step_outcome", "advancement_gate"], "Runtime contract execute-step test-first execution");
  const redEvidence = expectRecord2(testStrategyExecution.red_evidence, "Runtime contract execute-step red evidence");
  expectExactKeys2(redEvidence, ["result_status", "kind", "companion_statuses", "forbidden_statuses", "acceptance_evidence", "unexpected_failure_outcome", "review_checkpoint"], "Runtime contract execute-step red evidence");
  expectSetEqual(expectStringArray2(redEvidence.companion_statuses, "Runtime contract execute-step red companion statuses"), ["passed"], "Runtime contract execute-step red companion statuses");
  expectSetEqual(expectStringArray2(redEvidence.forbidden_statuses, "Runtime contract execute-step red forbidden statuses"), ["failed", "blocked", "not-run"], "Runtime contract execute-step red forbidden statuses");
  if (executeStepAdapter.step_source !== "confirmed-current-task" || executeStepAdapter.scope_enforcement !== "task-authority-envelope-with-v1-step-compatibility" || executeStepAdapter.command_plan_source !== "confirmed-current-step" || executeStepAdapter.persistent_tests_enforcement !== "frozen-section-plus-authority-evaluator-existing-test-distinction" || executeStepAdapter.completion_evidence_source !== "recorded-step-result-only" || executeStepAdapter.change_detection !== "runtime-preflight-candidate-before-after-delta" || executeStepAdapter.proposal_file_policy !== "project-external-only" || executeStepAdapter.advancement_owner !== "runtime" || executeStepAdapter.post_completion_commit_owner !== "user-or-explicit-outer-orchestrator" || executeStepAdapter.post_completion_route !== "git-commit" || testStrategyExecution.phase_source !== "versioned-frozen-test-strategy-and-current-step-evidence-obligation" || testStrategyExecution.legacy_behavior !== "read-history-block-unversioned-execution" || testStrategyExecution.non_red_outcome !== "implemented-with-passed-results-or-bound-reproduction" || testFirstExecution.first_step_phase !== "test-first" || testFirstExecution.later_step_phase !== "test-first" || testFirstExecution.first_step_outcome !== "implemented" || testFirstExecution.later_step_outcome !== "implemented" || testFirstExecution.advancement_gate !== "runtime-consumed-bound-prerequisite-evidence" || redEvidence.result_status !== "expected-failure" || redEvidence.kind !== "behavior-not-implemented" || redEvidence.acceptance_evidence !== "forbidden" || redEvidence.unexpected_failure_outcome !== "blocked" || redEvidence.review_checkpoint !== "required") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime execute-step adapter semantic boundary is invalid.");
  }
  expectSetEqual(expectStringArray2(executeStepContract.bound_actions, "Runtime contract.proposal.execute_step.bound_actions"), ["admit", "retry-step", "record-step-preflight", "extend-preflight", "step-progress", "record-repair-attempt", "resolve"], "Runtime contract execute-step adapter bound actions");
  const reviewChangeContract = expectRecord2(proposal.review_change, "Runtime contract.proposal.review_change");
  expectExactKeys2(reviewChangeContract, ["semantic_adapter", "bound_actions"], "Runtime contract.proposal.review_change");
  const reviewChangeAdapter = expectRecord2(reviewChangeContract.semantic_adapter, "Runtime contract.proposal.review_change.semantic_adapter");
  expectExactKeys2(reviewChangeAdapter, ["input", "commands", "context_source", "reviewable_execution", "change_set", "review_target", "result_storage", "direct_product_writes", "advancement_owner"], "Runtime contract.proposal.review_change.semantic_adapter");
  if (reviewChangeAdapter.input !== "stdin-json" || reviewChangeAdapter.context_source !== "latest-recorded-execution" || reviewChangeAdapter.reviewable_execution !== "implemented-or-test-red-awaiting-required-checkpoint-or-dynamic-review-or-repair-verification" || reviewChangeAdapter.change_set !== "runtime-owned-stable-id" || reviewChangeAdapter.review_target !== "runtime-cumulative-before-after-file-delta" || reviewChangeAdapter.result_storage !== "canonical-pending-review-result" || reviewChangeAdapter.direct_product_writes !== "deny" || reviewChangeAdapter.advancement_owner !== "execute-step")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime review-change adapter semantic boundary is invalid.");
  expectSetEqual(expectStringArray2(reviewChangeAdapter.commands, "Runtime contract review-change commands"), ["review-context", "review-read", "record-review-result", "record-evidence-challenge", "dismiss-evidence-challenge"], "Runtime contract review-change commands");
  expectSetEqual(expectStringArray2(reviewChangeContract.bound_actions, "Runtime contract review-change actions"), ["record-review-result", "record-evidence-challenge", "dismiss-evidence-challenge"], "Runtime contract review-change actions");
  const prepareTaskContract = expectRecord2(proposal.prepare_task, "Runtime contract.proposal.prepare_task");
  expectExactKeys2(prepareTaskContract, ["semantic_adapter", "bound_actions", "draft_mode", "draft_actions", "confirm_mode", "confirm_actions", "migration_mode", "migration_actions", "replan_mode", "replan_actions", "scope_amendment_mode", "scope_amendment_actions", "direct_replan_result", "task_history"], "Runtime contract.proposal.prepare_task");
  const prepareTaskAdapter = expectRecord2(prepareTaskContract.semantic_adapter, "Runtime contract.proposal.prepare_task.semantic_adapter");
  expectExactKeys2(prepareTaskAdapter, [
    "input",
    "commands",
    "draft_fields",
    "optional_draft_fields",
    "task_basis_storage",
    "decision_partition",
    "command_footprint_preflight",
    "confirmation_binding",
    "resume_review_binding",
    "replan_entry",
    "test_strategy",
    "persistent_tests_storage",
    "persistent_tests_enforcement",
    "proposal_file_policy",
    "internal_action_owners"
  ], "Runtime contract.proposal.prepare_task.semantic_adapter");
  if (prepareTaskAdapter.input !== "stdin-json")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime prepare-task adapter input must remain stdin-json.");
  expectSetEqual(expectStringArray2(prepareTaskAdapter.commands, "Runtime contract.proposal.prepare_task.semantic_adapter.commands"), ["prepare-draft", "prepare-successor", "record-human-acceptance", "record-evidence-waiver", "confirm-draft", "clear-resume-review", "replan", "prepare-replan", "confirm-replan", "discard-replan", "prepare-scope-amendment", "discard-scope-amendment", "initialize-preservation"], "Runtime contract prepare-task adapter commands");
  expectSetEqual(expectStringArray2(prepareTaskAdapter.draft_fields, "Runtime contract.proposal.prepare_task.semantic_adapter.draft_fields"), ["task_basis", "goal", "acceptance", "out_of_scope", "design_decisions", "mutation_authority_version", "mutation_authority", "planned_mutation_targets", "test_strategy", "implementation_steps", "validation_plan", "persistent_tests"], "Runtime contract prepare-task adapter semantic fields");
  expectSetEqual(expectStringArray2(prepareTaskAdapter.optional_draft_fields, "prepare-task optional fields"), ["project_documents", "affected_contracts"], "prepare-task optional fields");
  if (prepareTaskAdapter.task_basis_storage !== "linked-TASK_BASIS-with-path-and-revision") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime prepare-task adapter must persist the linked task basis reference.");
  }
  const decisionPartition = expectRecord2(prepareTaskAdapter.decision_partition, "Runtime contract.proposal.prepare_task.semantic_adapter.decision_partition");
  expectExactKeys2(decisionPartition, ["decided", "unresolved"], "Runtime contract.proposal.prepare_task.semantic_adapter.decision_partition");
  if (decisionPartition.decided !== "confirmed_decisions" || decisionPartition.unresolved !== "open_questions") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime prepare-task adapter must preserve decided and unresolved design choices separately.");
  }
  const commandFootprintPreflight = expectRecord2(prepareTaskAdapter.command_footprint_preflight, "Runtime contract.proposal.prepare_task.semantic_adapter.command_footprint_preflight");
  expectExactKeys2(commandFootprintPreflight, ["source", "fields", "evaluator", "timing"], "Runtime contract.proposal.prepare_task.semantic_adapter.command_footprint_preflight");
  if (commandFootprintPreflight.source !== "implementation_steps[].commands" || commandFootprintPreflight.evaluator !== "shared-mutation-scope-evaluator-and-v2-authority-evaluator" || commandFootprintPreflight.timing !== "before-draft-commit") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime prepare-task adapter command-footprint preflight is not bound to declared step commands before draft commit.");
  }
  expectSetEqual(expectStringArray2(commandFootprintPreflight.fields, "Runtime contract.proposal.prepare_task.semantic_adapter.command_footprint_preflight.fields"), ["command", "expected_repo_writes"], "Runtime prepare-task adapter command-footprint fields");
  if (prepareTaskAdapter.persistent_tests_storage !== "existing-scope-and-regression-sections") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime prepare-task adapter must map persistent tests into existing canonical sections.");
  }
  if (prepareTaskAdapter.confirmation_binding !== "runtime-issued-draft-receipt-plus-authorized-caller") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime prepare-task confirmation must bind a Runtime-issued draft receipt to the authorized caller.");
  }
  if (prepareTaskAdapter.resume_review_binding !== "caller-provided-exact-readiness-receipt") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime prepare-task resume review must consume a caller-provided exact readiness receipt.");
  }
  if (prepareTaskAdapter.replan_entry !== "blocked-pending-confirmed-candidate") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime prepare-task replan must remain blocked pending a confirmed candidate.");
  }
  const testStrategy = expectRecord2(prepareTaskAdapter.test_strategy, "Runtime contract.proposal.prepare_task.semantic_adapter.test_strategy");
  expectExactKeys2(testStrategy, ["storage", "fields", "modes", "sources", "classifications", "source_binding", "precedence", "ambiguity", "frozen_by", "change_after_confirm", "enforcement", "ordering_enforcement", "non_executable_scope"], "Runtime contract.proposal.prepare_task.semantic_adapter.test_strategy");
  expectSetEqual(expectStringArray2(testStrategy.fields, "Runtime contract prepare-task test-strategy fields"), ["mode", "source", "source_ref", "task_classification", "rationale"], "Runtime contract prepare-task test-strategy fields");
  expectSetEqual(expectStringArray2(testStrategy.modes, "Runtime contract prepare-task test-strategy modes"), ["flexible", "test-first", "implementation-first", "not-applicable"], "Runtime contract prepare-task test-strategy modes");
  expectSetEqual(expectStringArray2(testStrategy.sources, "Runtime contract prepare-task test-strategy sources"), ["explicit-user", "project-policy", "inferred-default"], "Runtime contract prepare-task test-strategy sources");
  expectSetEqual(expectStringArray2(testStrategy.classifications, "Runtime contract prepare-task test-strategy classifications"), ["contract-clear-behavior", "exploratory-or-infrastructure", "non-executable-change"], "Runtime contract prepare-task test-strategy classifications");
  const strategySourceBinding = expectRecord2(testStrategy.source_binding, "Runtime contract prepare-task test-strategy source_binding");
  expectExactKeys2(strategySourceBinding, ["explicit-user", "project-policy", "inferred-default"], "Runtime contract prepare-task test-strategy source_binding");
  const strategyPrecedence = expectStringArray2(testStrategy.precedence, "Runtime contract prepare-task test-strategy precedence");
  const nonExecutableScope = expectRecord2(testStrategy.non_executable_scope, "Runtime contract prepare-task test-strategy non_executable_scope");
  expectExactKeys2(nonExecutableScope, ["policy_source", "required_for", "evaluated_surfaces", "policy_pattern_grammar", "relation", "missing_or_ambiguous", "historical_active_tasks", "error_codes"], "Runtime contract prepare-task test-strategy non_executable_scope");
  expectSetEqual(expectStringArray2(nonExecutableScope.evaluated_surfaces, "Runtime contract prepare-task test-strategy non-executable evaluated surfaces"), ["task-allowed-scope", "task-conditional-scope", "implementation-step-mutation-scope"], "Runtime contract prepare-task test-strategy non-executable evaluated surfaces");
  expectSetEqual(expectStringArray2(nonExecutableScope.error_codes, "Runtime contract prepare-task test-strategy non-executable error codes"), ["TEST_STRATEGY_NON_EXECUTABLE_UNPROVEN", "TEST_STRATEGY_NON_EXECUTABLE_SCOPE_VIOLATION"], "Runtime contract prepare-task test-strategy non-executable error codes");
  if (testStrategy.storage !== "current-task-regression-checks/test-strategy" || strategySourceBinding["explicit-user"] !== "exact-task-basis-source-coordinate" || strategySourceBinding["project-policy"] !== "existing-project-policy-file" || strategySourceBinding["inferred-default"] !== "prepare-task-default" || strategyPrecedence.join("\x00") !== ["explicit-user", "project-policy", "inferred-default"].join("\x00") || testStrategy.ambiguity !== "resolve-as-open-question-before-draft-commit" || testStrategy.frozen_by !== "confirm-draft" || testStrategy.change_after_confirm !== "replan-only" || testStrategy.enforcement !== "create-update-confirm-and-replan" || testStrategy.ordering_enforcement !== "runtime-consumed-before-step-slots" || nonExecutableScope.policy_source !== ".workflow-system/PROJECT_PROFILE.yaml#boundaries.non_executable_change_paths" || nonExecutableScope.required_for !== "not-applicable" || nonExecutableScope.policy_pattern_grammar !== "exact-path-or-literal-directory-prefix-globstar" || nonExecutableScope.relation !== "exact-or-proven-subset" || nonExecutableScope.missing_or_ambiguous !== "block-not-applicable-only" || nonExecutableScope.historical_active_tasks !== "not-revalidated") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime prepare-task test-strategy classification, precedence, or freeze boundary is invalid.");
  }
  if (prepareTaskAdapter.persistent_tests_enforcement !== "frozen-section-plus-authority-evaluator-existing-test-distinction") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime prepare-task persistent tests must be enforced by the frozen section and scope evaluator.");
  }
  if (prepareTaskAdapter.proposal_file_policy !== "project-external-only") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime prepare-task adapter proposal files must remain project-external-only.");
  }
  const internalActionOwners = expectRecord2(prepareTaskAdapter.internal_action_owners, "Runtime contract.proposal.prepare_task.semantic_adapter.internal_action_owners");
  expectExactKeys2(internalActionOwners, ["migrate-claim-evidence", "mark-replan-blocked", "clear-replan-block"], "Runtime contract.proposal.prepare_task.semantic_adapter.internal_action_owners");
  if (internalActionOwners["migrate-claim-evidence"] !== "runtime-compatibility" || internalActionOwners["mark-replan-blocked"] !== "runtime-convergence" || internalActionOwners["clear-replan-block"] !== "runtime-convergence") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime prepare-task internal action ownership is invalid.");
  }
  expectSetEqual(expectStringArray2(prepareTaskContract.bound_actions, "Runtime contract.proposal.prepare_task.bound_actions"), ["clear-resume-review-gate", "record-user-evidence", ...DRAFT_TASK_STATE_ACTIONS, ...CLAIM_EVIDENCE_MIGRATION_ACTIONS, "mark-replan-blocked", "clear-replan-block", "commit-scope-amendment"], "Runtime contract prepare-task bound actions");
  if (prepareTaskContract.draft_mode !== "default" || prepareTaskContract.confirm_mode !== "confirm")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract prepare-task draft/confirm modes are invalid.");
  expectSetEqual(expectStringArray2(prepareTaskContract.draft_actions, "Runtime contract.proposal.prepare_task.draft_actions"), ["create-draft", "update-draft"], "Runtime contract prepare-task draft actions");
  expectSetEqual(expectStringArray2(prepareTaskContract.confirm_actions, "Runtime contract.proposal.prepare_task.confirm_actions"), ["confirm-draft"], "Runtime contract prepare-task confirm actions");
  if (prepareTaskContract.migration_mode !== "default")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract prepare-task migration_mode must be default.");
  expectSetEqual(expectStringArray2(prepareTaskContract.migration_actions, "Runtime contract prepare-task migration_actions"), [...CLAIM_EVIDENCE_MIGRATION_ACTIONS], "Runtime contract prepare-task migration actions");
  if (prepareTaskContract.replan_mode !== "replan")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract prepare-task replan_mode must be replan.");
  expectSetEqual(expectStringArray2(prepareTaskContract.replan_actions, "Runtime contract.proposal.prepare_task.replan_actions"), [...REPLAN_TASK_STATE_ACTIONS], "Runtime contract prepare-task replan actions");
  if (prepareTaskContract.scope_amendment_mode !== "amend-scope")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract prepare-task scope_amendment_mode must be amend-scope.");
  expectSetEqual(expectStringArray2(prepareTaskContract.scope_amendment_actions, "Runtime contract.proposal.prepare_task.scope_amendment_actions"), ["commit-scope-amendment"], "Runtime contract prepare-task scope amendment actions");
  if (prepareTaskContract.direct_replan_result !== "REPLAN_CONFIRMATION_REQUIRED") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract must describe the disabled direct replan result.");
  }
  const taskHistory = expectRecord2(prepareTaskContract.task_history, "Runtime contract.proposal.prepare_task.task_history");
  expectExactKeys2(taskHistory, ["initialization_preimage", "supersede_preimage", "confirmed_replan_preimage", "correction_candidate", "scope_amendment_candidate", "evidence_carry_forward", "external_evidence", "interrupted_commit", "recovery_protocol"], "Runtime contract.proposal.prepare_task.task_history");
  if (digest3(taskHistory.recovery_protocol) !== digest3(TASK_RECOVERY_PROTOCOL))
    fail3("RUNTIME_CONTRACT_INVALID", "Recovery protocol must match the versioned Kernel semantics.");
  if (taskHistory.initialization_preimage !== "exact-current-task-and-linked-task-basis-before-version-marker" || taskHistory.supersede_preimage !== "exact-current-task-and-linked-task-basis" || taskHistory.confirmed_replan_preimage !== "exact-current-task-and-linked-task-basis-before-publish" || taskHistory.correction_candidate !== "independent-revision-bound-candidate-with-full-old-obligations" || taskHistory.scope_amendment_candidate !== "independent-versioned-additive-candidate-with-authority-or-typed-persistent-test-admission-runtime-commit-and-preserved-obligations" || taskHistory.evidence_carry_forward !== "runtime-derived-old-report-and-subject-verified" || taskHistory.external_evidence !== "references-remain-references-explicit-bounded-content-ingestion" || taskHistory.interrupted_commit !== "fail-closed-lock-and-hash-recovery") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime task history contract is invalid.");
  }
  const inboxRecordContract = expectRecord2(proposal.inbox_record, "Runtime contract.proposal.inbox_record");
  expectExactKeys2(inboxRecordContract, ["mode", "action", "required", "record_fields", "relation", "duplicate_check", "proposed_owner", "target_pattern", "provenance_fields"], "Runtime contract.proposal.inbox_record");
  if (inboxRecordContract.mode !== "default" || inboxRecordContract.action !== "record") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract inbox_record must use mode=default and action=record.");
  }
  expectSetEqual(expectStringArray2(inboxRecordContract.required, "Runtime contract.proposal.inbox_record.required"), ["item_slug", "record", "relation_evidence_refs", "duplicate_check", "proposed_owner", "target_path", "evidence_refs"], "Runtime contract inbox record required fields");
  expectSetEqual(expectStringArray2(inboxRecordContract.record_fields, "Runtime contract.proposal.inbox_record.record_fields"), ["artifact_kind", "item_id", "title", "type", "source", "captured_at", "relation_to_current_task", "current_task_id", "description", "evidence", "suggested_next_action", "status"], "Runtime contract inbox record durable fields");
  if (inboxRecordContract.relation !== "unrelated" || inboxRecordContract.duplicate_check !== "clear") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract inbox records must be proven unrelated and have duplicate_check=clear.");
  }
  expectSetEqual(expectStringArray2(inboxRecordContract.proposed_owner, "Runtime contract.proposal.inbox_record.proposed_owner"), [...INBOX_SUGGESTED_NEXT_ACTIONS], "Runtime contract inbox record owner routes");
  if (inboxRecordContract.target_pattern !== "TASKS/inbox/INBOX-<YYYYMMDD>-<short-id>-<slug>.md") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract inbox record target pattern is invalid.");
  }
  expectSetEqual(expectStringArray2(inboxRecordContract.provenance_fields, "Runtime contract.proposal.inbox_record.provenance_fields"), ["idempotency_key", "proposal_digest", "source_revision", "source_task_id", "source_task_slug", "source_document_id", "relation_evidence_refs", "duplicate_check", "proposed_owner"], "Runtime contract inbox record provenance fields");
  const lifecycleContract = expectRecord2(proposal.lifecycle, "Runtime contract.proposal.lifecycle");
  expectExactKeys2(lifecycleContract, ["modes", "bound_modes", "proposal_only_modes", "pause_required", "interrupt_required", "resume_required", "supersede_required"], "Runtime contract.proposal.lifecycle");
  expectSetEqual(expectStringArray2(lifecycleContract.modes, "Runtime contract.proposal.lifecycle.modes"), [...LIFECYCLE_MODES], "Runtime contract lifecycle modes");
  expectSetEqual(expectStringArray2(lifecycleContract.bound_modes, "Runtime contract.proposal.lifecycle.bound_modes"), [...LIFECYCLE_MODES], "Runtime contract bound lifecycle modes");
  expectSetEqual(expectStringArray2(lifecycleContract.proposal_only_modes, "Runtime contract.proposal.lifecycle.proposal_only_modes", true), [], "Runtime contract proposal-only lifecycle modes");
  const lifecycleRequiredFields = {
    pause_required: ["lifecycle_state", "suspension_reason", "task_start_base", "last_reviewed_checkpoint", "current_diff_review_target", "rollback_conditions", "resume_review_reasons", "evidence_refs"],
    interrupt_required: ["lifecycle_state", "suspension_reason", "task_start_base", "last_reviewed_checkpoint", "current_diff_review_target", "rollback_conditions", "resume_review_reasons", "checkpoint_evidence", "dirty_attribution", "environment_state", "recovery_strategy", "evidence_refs"],
    resume_required: ["artifact_kind", "recovery_package_path", "recovery_package_revision", "resume_review_reasons", "evidence_refs"],
    supersede_required: ["invalidation_kind", "invalidation_reason", "evidence_refs", "partial_diff_disposition"]
  };
  for (const [field, expected] of Object.entries(lifecycleRequiredFields)) {
    const required = expectRecord2(lifecycleContract[field], `Runtime contract.proposal.lifecycle.${field}`);
    expectExactKeys2(required, ["required"], `Runtime contract.proposal.lifecycle.${field}`);
    expectSetEqual(expectStringArray2(required.required, `Runtime contract.proposal.lifecycle.${field}.required`), expected, `Runtime contract lifecycle ${field}`);
  }
  const closeTaskContract = expectRecord2(proposal.close_task, "Runtime contract.proposal.close_task");
  expectExactKeys2(closeTaskContract, ["default_mode", "preview_mode", "terminal_from", "terminal_to", "claim_evidence", "lesson_admission", "knowledge_admission"], "Runtime contract.proposal.close_task");
  if (closeTaskContract.default_mode !== "default" || closeTaskContract.preview_mode !== "preview") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract close-task must reserve default closure and preview read-only semantics.");
  }
  expectSetEqual(expectStringArray2(closeTaskContract.terminal_from, "Runtime contract close-task terminal_from"), ["active + active"], "Runtime contract close-task terminal_from");
  expectSetEqual(expectStringArray2(closeTaskContract.terminal_to, "Runtime contract close-task terminal_to"), ["closed + archived"], "Runtime contract close-task terminal_to");
  expectSetEqual(expectStringArray2(closeTaskContract.lesson_admission, "Runtime contract close-task lesson_admission"), ["admit", "defer", "no-op"], "Runtime contract close-task lesson admission");
  if (closeTaskContract.claim_evidence !== CLOSE_TASK_CLAIM_EVIDENCE_RULE) {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime close-task claim evidence derivation rule is invalid.");
  }
  const knowledgeAdmissionContract = expectRecord2(closeTaskContract.knowledge_admission, "Runtime contract.proposal.close_task.knowledge_admission");
  expectExactKeys2(knowledgeAdmissionContract, ["dispositions", "durable_dispositions", "candidate_fields", "implementation_anchors", "reentry_source"], "Runtime contract close-task knowledge_admission");
  expectSetEqual(expectStringArray2(knowledgeAdmissionContract.dispositions, "Runtime contract close-task knowledge dispositions"), ["admit", "defer", "merge", "no-op", "reject", "supersede"], "Runtime contract close-task knowledge dispositions");
  expectSetEqual(expectStringArray2(knowledgeAdmissionContract.durable_dispositions, "Runtime contract close-task durable knowledge dispositions"), ["admit", "merge", "supersede"], "Runtime contract close-task durable knowledge dispositions");
  expectSetEqual(expectStringArray2(knowledgeAdmissionContract.candidate_fields, "Runtime contract close-task knowledge candidate fields"), [...KNOWLEDGE_CANDIDATE_KEYS], "Runtime contract close-task knowledge candidate fields");
  const anchorContract = expectRecord2(knowledgeAdmissionContract.implementation_anchors, "Runtime contract close-task implementation_anchors");
  expectExactKeys2(anchorContract, ["coverage", "max_anchors", "line_number_locators", "missing_symbol_behavior"], "Runtime contract close-task implementation_anchors");
  expectSetEqual(expectStringArray2(anchorContract.coverage, "Runtime contract close-task implementation_anchors.coverage"), ["observed", "verified-scope"], "Runtime contract implementation anchor coverage");
  if (expectInteger(anchorContract.max_anchors, "Runtime contract close-task implementation_anchors.max_anchors", 0, 5) !== 5 || anchorContract.line_number_locators !== "forbidden" || anchorContract.missing_symbol_behavior !== "live-search-fallback") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime implementation anchors must remain bounded hints with live-search fallback.");
  }
  if (knowledgeAdmissionContract.reentry_source !== "canonical-task-archive-admission")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime knowledge re-entry must use canonical task archive admission provenance.");
  const lessonMarkerContract = expectRecord2(proposal.lesson_marker, "Runtime contract.proposal.lesson_marker");
  expectExactKeys2(lessonMarkerContract, ["contract", "marker_version_field", "noncanonical_behavior", "persisted", "reused"], "Runtime contract.proposal.lesson_marker");
  if (lessonMarkerContract.contract !== "vnext-lesson-marker/canonical-v1" || lessonMarkerContract.marker_version_field !== "absent" || lessonMarkerContract.noncanonical_behavior !== "fail-closed") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract lesson marker must expose the canonical closed schema.");
  }
  const persistedLessonMarker = expectRecord2(lessonMarkerContract.persisted, "Runtime contract.proposal.lesson_marker.persisted");
  expectExactKeys2(persistedLessonMarker, ["fields", "disposition"], "Runtime contract.proposal.lesson_marker.persisted");
  expectSetEqual(expectStringArray2(persistedLessonMarker.fields, "Runtime contract.proposal.lesson_marker.persisted.fields"), ["task_id", "task_slug", "document_id", "archive_path", "archive_revision", "source_revision", "candidate_ref", "candidate_digest", "evidence_refs"], "Runtime contract persisted Lesson marker fields");
  if (persistedLessonMarker.disposition !== "omitted")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract persisted Lesson markers must omit disposition.");
  const reusedLessonMarker = expectRecord2(lessonMarkerContract.reused, "Runtime contract.proposal.lesson_marker.reused");
  expectExactKeys2(reusedLessonMarker, ["fields", "disposition", "reused_candidate_fields"], "Runtime contract.proposal.lesson_marker.reused");
  expectSetEqual(expectStringArray2(reusedLessonMarker.fields, "Runtime contract.proposal.lesson_marker.reused.fields"), ["task_id", "task_slug", "document_id", "archive_path", "archive_revision", "source_revision", "candidate_ref", "candidate_digest", "evidence_refs", "disposition", "reused_candidate"], "Runtime contract reused Lesson marker fields");
  if (reusedLessonMarker.disposition !== "reused")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract reused Lesson markers must use disposition=reused.");
  expectSetEqual(expectStringArray2(reusedLessonMarker.reused_candidate_fields, "Runtime contract.proposal.lesson_marker.reused.reused_candidate_fields"), ["task_id", "document_id", "archive_revision", "candidate_ref"], "Runtime contract reused Lesson candidate identity fields");
  const mutationScopeContract = expectRecord2(contract.mutation_scope, "Runtime contract.mutation_scope");
  expectExactKeys2(mutationScopeContract, ["status", "binding", "source", "buckets", "default_write_policy", "read_discovery_is_not_write_authority", "ordinary_write_scope", "broad_glob_requires", "conditional_expansion_requires", "persistent_test_policy", "changed_goal_scope_acceptance", "check_command", "input", "output", "command_write_footprint"], "Runtime contract.mutation_scope");
  if (mutationScopeContract.status !== "bound" || mutationScopeContract.binding !== "vnext-runtime-read-only" || mutationScopeContract.source !== "CURRENT_TASK.md" || mutationScopeContract.default_write_policy !== "deny" || mutationScopeContract.read_discovery_is_not_write_authority !== true || mutationScopeContract.ordinary_write_scope !== "exact-file-or-file-plus-symbol" || mutationScopeContract.broad_glob_requires !== "inherently-broad-transformation" || mutationScopeContract.conditional_expansion_requires !== "evidence-and-authority" || mutationScopeContract.changed_goal_scope_acceptance !== "bounded-correction-amendment-or-supersede-successor" || mutationScopeContract.check_command !== "scope-check") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime mutation scope contract must keep the frozen default-deny and read/write separation semantics.");
  }
  expectSetEqual(expectStringArray2(mutationScopeContract.buckets, "Runtime contract.mutation_scope.buckets"), ["Allowed Files", "Conditional Files", "Forbidden Files"], "Runtime mutation scope buckets");
  const persistentTestPolicy = expectRecord2(mutationScopeContract.persistent_test_policy, "Runtime contract.mutation_scope.persistent_test_policy");
  expectExactKeys2(persistentTestPolicy, ["source", "when_present", "conventional_paths", "nonconventional_paths", "legacy_missing_section"], "Runtime contract.mutation_scope.persistent_test_policy");
  if (persistentTestPolicy.source !== "CURRENT_TASK Persistent Tests section" || persistentTestPolicy.when_present !== "exact-allowlist-default-deny" || persistentTestPolicy.conventional_paths !== "runtime-classified" || persistentTestPolicy.nonconventional_paths !== "caller-declared" || persistentTestPolicy.legacy_missing_section !== "compatibility-unenforced") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime persistent-test mutation policy is invalid.");
  }
  const mutationScopeInput = expectRecord2(mutationScopeContract.input, "Runtime contract.mutation_scope.input");
  expectExactKeys2(mutationScopeInput, ["required"], "Runtime contract.mutation_scope.input");
  expectSetEqual(expectStringArray2(mutationScopeInput.required, "Runtime contract.mutation_scope.input.required"), ["explicit_changed_paths", "conditional_authorizations_with_evidence_and_authority", "transformation_kind"], "Runtime mutation scope input");
  const mutationScopeOutput = expectRecord2(mutationScopeContract.output, "Runtime contract.mutation_scope.output");
  expectExactKeys2(mutationScopeOutput, ["required"], "Runtime contract.mutation_scope.output");
  expectSetEqual(expectStringArray2(mutationScopeOutput.required, "Runtime contract.mutation_scope.output.required"), ["per-path-admission-and-blocker", "separate-read-discovery-match", "source-revision"], "Runtime mutation scope output");
  const commandWriteFootprint = expectRecord2(mutationScopeContract.command_write_footprint, "Runtime contract.mutation_scope.command_write_footprint");
  expectExactKeys2(commandWriteFootprint, ["pre_command", "expected", "post_command", "observation_limitations"], "Runtime contract.mutation_scope.command_write_footprint");
  if (commandWriteFootprint.pre_command !== "required") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime command write footprints must be required before a repo-writing command executes.");
  }
  const expectedFootprint = expectRecord2(commandWriteFootprint.expected, "Runtime contract.mutation_scope.command_write_footprint.expected");
  expectExactKeys2(expectedFootprint, ["required", "bounded_kind", "unbounded_behavior", "admission"], "Runtime contract.mutation_scope.command_write_footprint.expected");
  expectSetEqual(expectStringArray2(expectedFootprint.required, "Runtime contract.mutation_scope.command_write_footprint.expected.required"), ["command", "kind", "repository_relative_targets", "evidence_refs"], "Runtime command expected write footprint fields");
  if (expectedFootprint.bounded_kind !== "bounded" || expectedFootprint.unbounded_behavior !== "blocked-before-execution" || expectedFootprint.admission !== "shared-mutation-scope-evaluator") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime command expected write footprint admission is not fail-closed or canonical.");
  }
  const postCommandFootprint = expectRecord2(commandWriteFootprint.post_command, "Runtime contract.mutation_scope.command_write_footprint.post_command");
  expectExactKeys2(postCommandFootprint, ["observed_field", "admission", "cleanup_behavior"], "Runtime contract.mutation_scope.command_write_footprint.post_command");
  if (postCommandFootprint.observed_field !== "observed_write_paths" || postCommandFootprint.admission !== "shared-mutation-scope-evaluator" || postCommandFootprint.cleanup_behavior !== "sticky-blocked-after-unauthorized-observation") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime command observed-write audit must reuse canonical scope judgment and retain unauthorized observations after cleanup.");
  }
  expectSetEqual(expectStringArray2(commandWriteFootprint.observation_limitations, "Runtime contract.mutation_scope.command_write_footprint.observation_limitations"), ["no-os-level-transient-write-history-proof"], "Runtime command write observation limitations");
  const mutationAuthorityContract = expectRecord2(contract.mutation_authority, "Runtime contract.mutation_authority");
  expectExactKeys2(mutationAuthorityContract, ["version", "status", "project_profile", "project_domain_fields", "task_fields", "root_grammar", "path_resolution", "ambiguous_domain_behavior", "unclassified_behavior", "read_discovery_behavior", "planned_footprint_behavior", "in_envelope_expansion", "forbidden_precedence", "assessment_fields", "dynamic_review", "cross_envelope_error", "extension_action", "extension_identity", "repair_extension", "test_strategy_dynamic_policy", "non_executable_dynamic_policy", "planning_time_authority_proof", "command_glob_proof", "domain_map_lifecycle", "domain_map_revision", "dynamic_expansion_identity", "dynamic_review_consumption", "authority_amendment", "authority_amendment_execution_gate", "persistent_test_admission", "existing_test_behavior", "new_persistent_test_behavior", "legacy_behavior"], "Runtime contract.mutation_authority");
  if (mutationAuthorityContract.version !== 2 || mutationAuthorityContract.status !== "bound" || mutationAuthorityContract.project_profile !== ".workflow-system/PROJECT_PROFILE.yaml#mutation_authority.domains" || mutationAuthorityContract.root_grammar !== "bounded-repository-relative-exact-path-or-literal-directory-prefix-globstar" || mutationAuthorityContract.path_resolution !== "path-to-one-domain-or-unclassified" || mutationAuthorityContract.ambiguous_domain_behavior !== "fail-closed" || mutationAuthorityContract.unclassified_behavior !== "write-blocked-unless-exact-exception" || mutationAuthorityContract.read_discovery_behavior !== "allowed-outside-envelope-but-never-write-authority" || mutationAuthorityContract.planned_footprint_behavior !== "guidance-only-and-not-an-independent-authority-boundary" || mutationAuthorityContract.in_envelope_expansion !== "blast-radius-assessment-and-self-admit-or-escalate" || mutationAuthorityContract.forbidden_precedence !== "explicit-forbidden-before-envelope-admission" || mutationAuthorityContract.dynamic_review !== "risk-selected-cumulative-review-for-self-admitted-expansion" || mutationAuthorityContract.cross_envelope_error !== "MUTATION_AUTHORITY_EXPANSION_REQUIRED" || mutationAuthorityContract.extension_action !== "execute-step:extend-preflight" || mutationAuthorityContract.extension_identity !== "stable-execution-id-with-current-receipt-token" || mutationAuthorityContract.repair_extension !== "same-execution-admission-evaluator-and-repair-wave" || mutationAuthorityContract.test_strategy_dynamic_policy !== "every-preflight-and-extension-uses-runtime-derived-current-step-evidence-phase-before-state-mutation" || mutationAuthorityContract.non_executable_dynamic_policy !== "not-applicable-remains-closed-under-all-target-discovery" || mutationAuthorityContract.authority_amendment !== "prepare-task:amend-scope-with-explicit-authority-authorization-or-typed-p-12-admission" || mutationAuthorityContract.existing_test_behavior !== "existing-in-envelope-test-is-ordinary-expansion-with-review" || mutationAuthorityContract.new_persistent_test_behavior !== "absent-test-requires-p-12-admission" || mutationAuthorityContract.legacy_behavior !== "missing-or-version-1-retains-v1-exact-step-scope-semantics") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime Mutation Authority v2 contract semantics are invalid.");
  }
  expectSetEqual(expectStringArray2(mutationAuthorityContract.project_domain_fields, "Runtime mutation authority project domain fields"), ["id", "roots"], "Runtime mutation authority project domain fields");
  expectSetEqual(expectStringArray2(mutationAuthorityContract.task_fields, "Runtime mutation authority task fields"), ["mutation_authority_version", "domains", "exact_exceptions", "forbidden"], "Runtime mutation authority task fields");
  expectSetEqual(expectStringArray2(mutationAuthorityContract.assessment_fields, "Runtime mutation authority assessment fields"), ["target", "reason", "blast_radius", "evidence_refs", "disposition"], "Runtime mutation authority assessment fields");
  const planningProof = expectRecord2(mutationAuthorityContract.planning_time_authority_proof, "Runtime mutation authority planning_time_authority_proof");
  expectExactKeys2(planningProof, ["evaluator", "declarations", "admission", "timing", "unclassified_behavior"], "Runtime mutation authority planning_time_authority_proof");
  if (planningProof.evaluator !== "runtime-owned-task-definition-authority-proof" || planningProof.admission !== "authorized-domain-or-exact-exception" || planningProof.timing !== "before-draft-confirmed-definition-commit" || planningProof.unclassified_behavior !== "blocked") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime planning-time authority proof semantics are invalid.");
  }
  expectSetEqual(expectStringArray2(planningProof.declarations, "Runtime mutation authority planning-time declarations"), ["planned_mutation_targets", "command_write_footprints", "persistent_test_paths"], "Runtime planning-time authority declarations");
  const commandGlobProof = expectRecord2(mutationAuthorityContract.command_glob_proof, "Runtime mutation authority command_glob_proof");
  expectExactKeys2(commandGlobProof, ["grammar", "relation", "synthetic_probe", "exact_exception"], "Runtime mutation authority command_glob_proof");
  if (commandGlobProof.grammar !== "exact-path-or-literal-directory-prefix-globstar" || commandGlobProof.relation !== "candidate-pattern-must-be-subset-of-granted-domain-root" || commandGlobProof.synthetic_probe !== "prohibited" || commandGlobProof.exact_exception !== "cannot-authorize-directory-glob") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime command glob subset proof semantics are invalid.");
  }
  const reviewPreimageContract = expectRecord2(contract.review_coverage_preimages, "Runtime contract.review_coverage_preimages");
  expectExactKeys2(reviewPreimageContract, ["canonical_fields", "inline_content", "baseline_storage", "review_read", "legacy_inline_compatibility"], "Runtime contract.review_coverage_preimages");
  expectSetEqual(expectStringArray2(reviewPreimageContract.canonical_fields, "Runtime review preimage canonical fields"), ["path", "state", "sha256"], "Runtime review preimage canonical fields");
  if (reviewPreimageContract.inline_content !== "forbidden-for-new-writes" || reviewPreimageContract.review_read !== "resolve-by-sha256-read-and-verify; missing-or-corrupt-fails-closed" || reviewPreimageContract.legacy_inline_compatibility !== "readable-and-migrated-by-decode-verify-persist-strip") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime review preimage inline/storage semantics are invalid.");
  }
  const baselineStorage = expectRecord2(reviewPreimageContract.baseline_storage, "Runtime contract.review_coverage_preimages.baseline_storage");
  expectExactKeys2(baselineStorage, ["kind", "relative_directory", "filename", "lookup", "write_order", "deduplication"], "Runtime review preimage baseline storage");
  if (baselineStorage.kind !== "immutable-content-addressed-raw-blob" || baselineStorage.relative_directory !== "<workflow_home>/review-preimages" || baselineStorage.filename !== "<sha256>.blob" || baselineStorage.lookup !== "sha256" || baselineStorage.write_order !== "persist-and-verify-blob-before-current-task-state-commit" || baselineStorage.deduplication !== "identical-sha256-reuses-existing-verified-blob") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime review preimage baseline storage semantics are invalid.");
  }
  const domainMapLifecycle = expectRecord2(mutationAuthorityContract.domain_map_lifecycle, "Runtime mutation authority domain_map_lifecycle");
  expectExactKeys2(domainMapLifecycle, ["proposal", "confirmation", "canonical", "task_selection"], "Runtime mutation authority domain_map_lifecycle");
  if (domainMapLifecycle.proposal !== "bootstrap-inventory-domain-candidate" || domainMapLifecycle.confirmation !== "explicit-project-owner-decision" || domainMapLifecycle.canonical !== ".workflow-system/PROJECT_PROFILE.yaml#mutation_authority.domains" || domainMapLifecycle.task_selection !== "select-existing-map-without-rebuild") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime project domain-map lifecycle semantics are invalid.");
  }
  const domainMapRevision = expectRecord2(mutationAuthorityContract.domain_map_revision, "Runtime mutation authority domain_map_revision");
  expectExactKeys2(domainMapRevision, ["binding", "mismatch", "grant"], "Runtime mutation authority domain_map_revision");
  if (domainMapRevision.binding !== "v2-confirmed-task-runtime-state-authority_domain_revision" || domainMapRevision.mismatch !== "fail-closed-require-explicit-task-authority-domain-revalidation" || domainMapRevision.grant !== "never-inherit-changed-project-map") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime project domain-map revision semantics are invalid.");
  }
  const dynamicExpansionIdentity = expectRecord2(mutationAuthorityContract.dynamic_expansion_identity, "Runtime mutation authority dynamic expansion identity");
  expectExactKeys2(dynamicExpansionIdentity, ["required", "active_view", "historical_records", "repeated_path_policy"], "Runtime mutation authority dynamic expansion identity");
  expectSetEqual(expectStringArray2(dynamicExpansionIdentity.required, "Runtime mutation authority dynamic expansion identity required fields"), ["step_id", "plan_revision", "change_set_id", "execution_id", "preflight_id", "mode", "path", "domain", "assessment", "first_touch_state", "admitted_at"], "Runtime mutation authority dynamic expansion identity required fields");
  if (dynamicExpansionIdentity.active_view !== "current-active-step-plan-revision-execution-only" || dynamicExpansionIdentity.historical_records !== "readable-for-audit-but-never-a-current-write-grant" || dynamicExpansionIdentity.repeated_path_policy !== "same-path-requires-new-assessment-for-a-new-execution-identity") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime dynamic expansion identity semantics are invalid.");
  }
  const dynamicReviewConsumption = expectRecord2(mutationAuthorityContract.dynamic_review_consumption, "Runtime mutation authority dynamic review consumption");
  expectExactKeys2(dynamicReviewConsumption, ["binding", "required", "historical_records"], "Runtime mutation authority dynamic review consumption");
  if (dynamicReviewConsumption.binding !== "expansion-execution-identity-and-clean-review-id" || dynamicReviewConsumption.required !== "current-execution-expansions-only" || dynamicReviewConsumption.historical_records !== "retain-consumed-review-association") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime dynamic review consumption semantics are invalid.");
  }
  const authorityAmendmentGate = expectRecord2(mutationAuthorityContract.authority_amendment_execution_gate, "Runtime mutation authority authority amendment execution gate");
  expectExactKeys2(authorityAmendmentGate, ["blocks", "allows", "repair_binding"], "Runtime mutation authority authority amendment execution gate");
  expectSetEqual(expectStringArray2(authorityAmendmentGate.allows, "Runtime mutation authority authority amendment execution gate allows"), ["ready-attempt", "settled-blocked-result", "settled-repair-result", "pending-review-or-findings-preserved"], "Runtime authority amendment execution gate allowed states");
  if (authorityAmendmentGate.blocks !== "latest-preflighted-attempt-without-recorded-execution-result" || authorityAmendmentGate.repair_binding !== "current-execution-preflight-and-matching-execution-result") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime authority amendment settlement gate semantics are invalid.");
  }
  const persistentTestAdmission = expectRecord2(mutationAuthorityContract.persistent_test_admission, "Runtime mutation authority persistent test admission");
  expectExactKeys2(persistentTestAdmission, ["route", "authority_diff", "ordinary_dynamic_behavior", "required_fields", "runtime_role"], "Runtime mutation authority persistent test admission");
  expectSetEqual(expectStringArray2(persistentTestAdmission.required_fields, "Runtime mutation authority persistent test admission required fields"), ["path", "proves", "owner", "owner_source", "source_ref", "basis", "existing_evidence_insufficiency", "assertion_boundary", "failure_disposition"], "Runtime persistent test admission fields");
  if (persistentTestAdmission.route !== "prepare-task:amend-scope" || persistentTestAdmission.authority_diff !== "none-when-target-is-in-envelope" || persistentTestAdmission.ordinary_dynamic_behavior !== "blocked-before-state-mutation" || persistentTestAdmission.runtime_role !== "validate-bind-persist-no-defaults") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime persistent test admission semantics are invalid.");
  }
  const canonical = expectRecord2(contract.canonical_current_task, "Runtime contract.canonical_current_task");
  expectExactKeys2(canonical, ["frontmatter", "runtime_state", "source_of_truth", "legacy_schema_behavior"], "Runtime contract.canonical_current_task");
  const frontmatter = expectRecord2(canonical.frontmatter, "Runtime contract.canonical_current_task.frontmatter");
  expectExactKeys2(frontmatter, ["schema_version", "kind", "required", "optional"], "Runtime contract.canonical_current_task.frontmatter");
  if (frontmatter.schema_version !== 1 || frontmatter.kind !== VNEXT_CURRENT_TASK_KIND)
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract current-task frontmatter marker is invalid.");
  expectSetEqual(expectStringArray2(frontmatter.required, "Runtime contract.canonical_current_task.frontmatter.required"), ["document_id", "runtime_state"], "Runtime contract current-task frontmatter");
  expectSetEqual(expectStringArray2(frontmatter.optional, "Runtime contract.canonical_current_task.frontmatter.optional", true), ["mutation_authority_version", "mutation_authority"], "Runtime current-task authority frontmatter fields");
  const runtimeState = expectRecord2(canonical.runtime_state, "Runtime contract.canonical_current_task.runtime_state");
  expectExactKeys2(runtimeState, ["schema_version", "kind", "fields", "review_cycle"], "Runtime contract.canonical_current_task.runtime_state");
  if (runtimeState.schema_version !== 1 || runtimeState.kind !== VNEXT_RUNTIME_STATE_KIND)
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract runtime-state marker is invalid.");
  expectSetEqual(expectStringArray2(runtimeState.fields, "Runtime contract.canonical_current_task.runtime_state.fields"), [...RUNTIME_STATE_FIELDS], "Runtime contract runtime-state fields");
  const reviewCycleContract = expectRecord2(runtimeState.review_cycle, "Runtime contract.canonical_current_task.runtime_state.review_cycle");
  expectExactKeys2(reviewCycleContract, ["fields", "repair_round_max", "same_repair_wave_counts_once", "verification_new_finding_wave_max"], "Runtime contract.canonical_current_task.runtime_state.review_cycle");
  expectSetEqual(expectStringArray2(reviewCycleContract.fields, "Runtime contract.canonical_current_task.runtime_state.review_cycle.fields"), [...REVIEW_CYCLE_FIELDS], "Runtime contract review-cycle fields");
  if (expectInteger(reviewCycleContract.repair_round_max, "Runtime contract review-cycle repair_round_max", 0, MAX_REPAIR_ROUNDS) !== MAX_REPAIR_ROUNDS) {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract review-cycle repair_round_max must be 3.");
  }
  if (expectBoolean(reviewCycleContract.same_repair_wave_counts_once, "Runtime contract review-cycle same_repair_wave_counts_once") !== true) {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract must count each repair wave once per review cycle.");
  }
  if (expectInteger(reviewCycleContract.verification_new_finding_wave_max, "Runtime contract review-cycle verification_new_finding_wave_max", 0, 1) !== 1) {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract must allow at most one verification new-finding admission wave per review cycle.");
  }
  if (canonical.source_of_truth !== "CURRENT_TASK-head-plus-selected-immutable-roots-one-canonical-aggregate" || canonical.legacy_schema_behavior !== "migration-required")
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract must keep the CURRENT_TASK head and its selected immutable roots as one canonical task aggregate and stop on legacy schema.");
  const concurrency = expectRecord2(contract.concurrency, "Runtime contract.concurrency");
  expectExactKeys2(concurrency, ["model", "concurrent_state_changing_writers", "stale_detection"], "Runtime contract.concurrency");
  if (concurrency.model !== "single-authorized-writer" || concurrency.concurrent_state_changing_writers !== "forbidden" || concurrency.stale_detection !== "source-revision-and-explicit-recovery-package-revision") {
    fail3("RUNTIME_CONTRACT_INVALID", "Runtime contract must require a single authorized state-changing writer plus explicit recovery package revision stale detection.");
  }
  const operations = contract.operations;
  if (!Array.isArray(operations) || operations.length !== RUNTIME_OPERATION_KINDS.length)
    fail3("RUNTIME_CONTRACT_INVALID", `Runtime contract must declare exactly the ${RUNTIME_OPERATION_KINDS.length} Phase 2 bound operations.`);
  const bound = [];
  for (const [index, rawOperation] of operations.entries()) {
    const operation = expectRecord2(rawOperation, `Runtime contract.operations[${index}]`);
    expectExactKeys2(operation, ["id", "status", "binding", "operation", "source_targets", "write_targets", "allowed_callers", "result_states", "atomic", "idempotence", "conflict_policy"], `Runtime contract.operations[${index}]`);
    const id = expectEnum(operation.id, RUNTIME_OPERATION_KINDS, `Runtime contract.operations[${index}].id`);
    if (bound.includes(id))
      fail3("RUNTIME_CONTRACT_INVALID", `Runtime contract operation ${id} is duplicated.`);
    bound.push(id);
    if (operation.status !== "bound" || operation.binding !== "vnext-runtime")
      fail3("RUNTIME_CONTRACT_INVALID", `Runtime contract operation ${id} must be bound to vnext-runtime.`);
    if (operation.operation !== id)
      fail3("RUNTIME_CONTRACT_INVALID", `Runtime contract operation ${id} must identify its logical operation.`);
    const operationContract = {
      "task-state-transaction": {
        source: ["CURRENT_TASK.md", "task-basis/TASK_BASIS-<TASK_ID>.md"],
        writes: ["CURRENT_TASK.md", "task-basis/TASK_BASIS-<TASK_ID>.md"],
        callers: ["execute-step", "review-change", "prepare-task"]
      },
      "finding-queue-transaction": {
        source: ["CURRENT_TASK.md"],
        writes: ["CURRENT_TASK.md"],
        callers: ["execute-step"]
      },
      "lifecycle-transaction": {
        source: ["CURRENT_TASK.md", "TASKS/paused/**", "TASKS/interrupted/**"],
        writes: ["CURRENT_TASK.md", "TASKS/paused/**", "TASKS/interrupted/**"],
        callers: ["task-lifecycle"]
      },
      "inbox-record-transaction": {
        source: ["CURRENT_TASK.md", "TASKS/inbox/**"],
        writes: ["TASKS/inbox/**"],
        callers: ["capture-work-item"]
      },
      "project-status-transaction": {
        source: ["CURRENT_TASK.md", "STATUS.md", "TASKS/TASK-<TASK_ID>-<TASK_SLUG>.md"],
        writes: ["STATUS.md"],
        callers: ["close-task"]
      },
      "archive-transaction": {
        source: ["CURRENT_TASK.md", "TASKS/TASK-<TASK_ID>-<TASK_SLUG>.md"],
        writes: ["CURRENT_TASK.md", "TASKS/TASK-<TASK_ID>-<TASK_SLUG>.md"],
        callers: ["close-task"]
      },
      "lesson-record-transaction": {
        source: ["CURRENT_TASK.md", "TASKS/TASK-<TASK_ID>-<TASK_SLUG>.md", "LESSONS.md"],
        writes: ["LESSONS.md"],
        callers: ["close-task"]
      },
      "contract-candidate-commit": {
        source: ["CURRENT_TASK.md", "TASKS/TASK-<TASK_ID>-<TASK_SLUG>.md", "CONTRACTS.md"],
        writes: ["CONTRACTS.md"],
        callers: ["close-task"]
      },
      "decision-record-transaction": {
        source: ["CURRENT_TASK.md", "TASKS/TASK-<TASK_ID>-<TASK_SLUG>.md", "DECISIONS.md"],
        writes: ["DECISIONS.md"],
        callers: ["close-task"]
      }
    };
    const expectedTargets = operationContract[id];
    expectSetEqual(expectStringArray2(operation.source_targets, `Runtime contract.operations[${index}].source_targets`), expectedTargets.source, `Runtime contract operation ${id}.source_targets`);
    expectSetEqual(expectStringArray2(operation.write_targets, `Runtime contract.operations[${index}].write_targets`), expectedTargets.writes, `Runtime contract operation ${id}.write_targets`);
    expectSetEqual(expectStringArray2(operation.allowed_callers, `Runtime contract.operations[${index}].allowed_callers`), expectedTargets.callers, `Runtime contract operation ${id}.allowed_callers`);
    expectSetEqual(expectStringArray2(operation.result_states, `Runtime contract.operations[${index}].result_states`), [...RUNTIME_RESULT_STATES], `Runtime contract operation ${id}.result_states`);
    if (operation.atomic !== true || operation.idempotence !== "fail-closed" || operation.conflict_policy !== "fail-closed")
      fail3("RUNTIME_CONTRACT_INVALID", `Runtime contract operation ${id} must be atomic, fail-closed, and conflict-safe.`);
  }
  expectSetEqual(bound, [...RUNTIME_OPERATION_KINDS], "Runtime contract bound operations");
  const unbound = expectStringArray2(contract.unbound_operations, "Runtime contract.unbound_operations", true);
  expectSetEqual(unbound, [], "Runtime contract unbound operations");
  const bootstrapOperations = validateBootstrapRuntimeContract(contract.bootstrap_project);
  return {
    phase: "Phase 2",
    runtime_distribution: distributionIdentity,
    mutation_scope: { status: "bound", binding: "vnext-runtime-read-only", check_command: "scope-check" },
    bound_operations: bound,
    unbound_operations: unbound,
    bootstrap_operations: bootstrapOperations
  };
}
function validateAuthorityEvidence2(value) {
  if (!Array.isArray(value) || value.length === 0)
    fail3("RUNTIME_AUTHORITY_MISSING", "authority_evidence must be non-empty.");
  const result = [];
  for (const [index, raw] of value.entries()) {
    const record = expectRecord2(raw, `authority_evidence[${index}]`);
    const kind = expectEnum(record.kind, ["active-task-owner", "scope-admission", "finding-admission", "evidence-admission", "dangerous-operation", "resume-review", "user-confirmation", "authorized-caller"], `authority_evidence[${index}].kind`);
    const source = normalizeRepoPath2(expectString2(record.source, `authority_evidence[${index}].source`), `authority_evidence[${index}].source`);
    const hasDraftBinding = "draft_revision" in record;
    const hasSourceBinding = "source_revision" in record;
    const hasCoordinateBinding = "task_id" in record || "document_id" in record || hasDraftBinding || hasSourceBinding;
    if (hasDraftBinding && hasSourceBinding) {
      fail3("RUNTIME_SCHEMA_INVALID", `authority_evidence[${index}] cannot bind both draft_revision and source_revision.`);
    }
    if (hasCoordinateBinding) {
      const revisionField = hasSourceBinding ? "source_revision" : "draft_revision";
      const expectedKeys = "subject" in record ? ["kind", "source", "subject", "task_id", "document_id", revisionField] : ["kind", "source", "task_id", "document_id", revisionField];
      expectExactKeys2(record, expectedKeys, `authority_evidence[${index}]`);
      const taskId = expectString2(record.task_id, `authority_evidence[${index}].task_id`);
      try {
        validateTaskId(taskId);
      } catch (error) {
        fail3("RUNTIME_SCHEMA_INVALID", error instanceof Error ? error.message : String(error));
      }
      const documentId = expectString2(record.document_id, `authority_evidence[${index}].document_id`);
      if (!DOCUMENT_ID_PATTERN.test(documentId))
        fail3("RUNTIME_SCHEMA_INVALID", `authority_evidence[${index}].document_id is invalid.`);
      const authorityRevision = expectString2(record[revisionField], `authority_evidence[${index}].${revisionField}`);
      if (!/^[a-f0-9]{64}$/.test(authorityRevision))
        fail3("RUNTIME_SCHEMA_INVALID", `authority_evidence[${index}].${revisionField} must be SHA-256.`);
      result.push({
        kind,
        source,
        subject: "subject" in record ? expectText(record.subject, `authority_evidence[${index}].subject`, 256) : taskId,
        task_id: taskId,
        document_id: documentId,
        [revisionField]: authorityRevision
      });
    } else {
      expectExactKeys2(record, ["kind", "source", "subject"], `authority_evidence[${index}]`);
      result.push({
        kind,
        source,
        subject: expectText(record.subject, `authority_evidence[${index}].subject`, 256)
      });
    }
  }
  return result;
}
function validateSourceTuple(value) {
  const record = expectRecord2(value, "source_tuple");
  expectExactKeys2(record, ["path", "revision", "document_id", "task_id", "task_slug", "workflow_status", "lifecycle_state", "active_step_id", "active_step_status", "finding_queue_revision", "resume_requires_review", "resume_review_reasons"], "source_tuple");
  const taskId = expectString2(record.task_id, "source_tuple.task_id");
  const taskSlug = expectString2(record.task_slug, "source_tuple.task_slug");
  try {
    validateTaskId(taskId);
    validateTaskSlug(taskSlug);
  } catch (error) {
    fail3("RUNTIME_SCHEMA_INVALID", error instanceof Error ? error.message : String(error));
  }
  const documentId = expectString2(record.document_id, "source_tuple.document_id");
  if (!DOCUMENT_ID_PATTERN.test(documentId))
    fail3("RUNTIME_SCHEMA_INVALID", "source_tuple.document_id is invalid.");
  const revision = expectString2(record.revision, "source_tuple.revision");
  if (!/^[a-f0-9]{64}$/.test(revision))
    fail3("RUNTIME_SCHEMA_INVALID", "source_tuple.revision must be SHA-256.");
  const workflowStatus = expectEnum(record.workflow_status, CURRENT_TASK_WORKFLOW_STATUSES, "source_tuple.workflow_status");
  const lifecycleState = expectEnum(record.lifecycle_state, TASK_LIFECYCLE_STATES, "source_tuple.lifecycle_state");
  try {
    validateCurrentTaskStatusTuple(workflowStatus, lifecycleState);
  } catch (error) {
    fail3("RUNTIME_STATE_CONFLICT", error instanceof Error ? error.message : String(error));
  }
  const resumeRequiresReview = expectBoolean(record.resume_requires_review, "source_tuple.resume_requires_review");
  const rawResumeReasons = expectStringArray2(record.resume_review_reasons, "source_tuple.resume_review_reasons", true, RESUME_REVIEW_REASON_ORDER.length);
  const resumeReviewReasons = normalizeResumeReviewReasons(rawResumeReasons);
  if (rawResumeReasons.join("|") !== resumeReviewReasons.join("|")) {
    fail3("RUNTIME_SCHEMA_INVALID", "source_tuple.resume_review_reasons must use the canonical closed-set order.");
  }
  try {
    validateCurrentTaskResumeGate(lifecycleState, resumeRequiresReview, resumeReviewReasons);
  } catch (error) {
    fail3("RUNTIME_STATE_CONFLICT", error instanceof Error ? error.message : String(error));
  }
  if (workflowStatus === "suspended" && !resumeRequiresReview) {
    fail3("RUNTIME_STATE_CONFLICT", "suspended CURRENT_TASK state must remain behind a non-empty resume review gate.");
  }
  return {
    path: normalizeRepoPath2(expectString2(record.path, "source_tuple.path"), "source_tuple.path"),
    revision,
    document_id: documentId,
    task_id: taskId,
    task_slug: taskSlug,
    workflow_status: workflowStatus,
    lifecycle_state: lifecycleState,
    active_step_id: expectString2(record.active_step_id, "source_tuple.active_step_id", STEP_ID_PATTERN2),
    active_step_status: expectEnum(record.active_step_status, STEP_STATUSES, "source_tuple.active_step_status"),
    finding_queue_revision: expectInteger(record.finding_queue_revision, "source_tuple.finding_queue_revision"),
    resume_requires_review: resumeRequiresReview,
    resume_review_reasons: resumeReviewReasons
  };
}
function validateEvidenceRefs(value, location) {
  return expectStringArray2(value, location, false, MAX_EVIDENCE_REFS);
}
function validateExecutionResultPaths(value, location) {
  const values = expectStringArray2(value, location, true, MAX_EXECUTION_RESULT_ITEMS).map((item, index) => normalizeRepoPath2(item, `${location}[${index}]`));
  if (new Set(values).size !== values.length)
    fail3("RUNTIME_SCHEMA_INVALID", `${location} must not contain duplicates.`);
  return values;
}
function validateReviewTarget(value, location) {
  const target = expectRecord2(value, location);
  expectExactKeys2(target, ["kind", "revision", "entries"], location);
  if (target.kind !== "runtime-file-manifest/v1") {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.kind must be runtime-file-manifest/v1.`);
  }
  if (!Array.isArray(target.entries) || target.entries.length > MAX_EXECUTION_RESULT_ITEMS) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.entries must be a bounded array.`);
  }
  const entries = target.entries.map((value, index) => {
    const entryLocation = `${location}.entries[${index}]`;
    const entry = expectRecord2(value, entryLocation);
    expectExactKeys2(entry, ["path", "state", "sha256"], entryLocation);
    const state = expectEnum(entry.state, ["file", "absent", "symlink"], `${entryLocation}.state`);
    const entryDigest = entry.sha256 === null ? null : expectString2(entry.sha256, `${entryLocation}.sha256`);
    if (state === "absent" !== (entryDigest === null)) {
      fail3("RUNTIME_SCHEMA_INVALID", `${entryLocation}.sha256 must be null exactly when state is absent.`);
    }
    if (entryDigest !== null && !/^[a-f0-9]{64}$/u.test(entryDigest)) {
      fail3("RUNTIME_SCHEMA_INVALID", `${entryLocation}.sha256 must be SHA-256.`);
    }
    return {
      path: normalizeRepoPath2(expectString2(entry.path, `${entryLocation}.path`), `${entryLocation}.path`),
      state,
      sha256: entryDigest
    };
  });
  const sortedPaths = entries.map((item) => item.path).sort();
  if (new Set(sortedPaths).size !== sortedPaths.length || entries.some((item, index) => item.path !== sortedPaths[index])) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.entries must use unique paths in canonical sort order.`);
  }
  const revision = expectString2(target.revision, `${location}.revision`);
  if (!/^[a-f0-9]{64}$/u.test(revision) || revision !== digest3({ kind: "runtime-file-manifest/v1", entries })) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.revision does not match its canonical file manifest.`);
  }
  return { kind: "runtime-file-manifest/v1", revision, entries };
}
function validateRuntimeReviewTarget(value, location = "review_target") {
  return validateReviewTarget(value, location);
}
function validateReviewChangeDelta(value, location, base, target) {
  const delta = expectRecord2(value, location);
  expectExactKeys2(delta, ["kind", "base_revision", "target_revision", "entries"], location);
  if (delta.kind !== "runtime-file-delta/v1") {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.kind must be runtime-file-delta/v1.`);
  }
  if (!Array.isArray(delta.entries) || delta.entries.length > MAX_EXECUTION_RESULT_ITEMS) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.entries must be a bounded array.`);
  }
  const entries = delta.entries.map((value, index) => {
    const entryLocation = `${location}.entries[${index}]`;
    const entry = expectRecord2(value, entryLocation);
    expectExactKeys2(entry, ["path", "before_state", "before_sha256", "after_state", "after_sha256"], entryLocation);
    const beforeState = expectEnum(entry.before_state, ["file", "absent", "symlink"], `${entryLocation}.before_state`);
    const afterState = expectEnum(entry.after_state, ["file", "absent", "symlink"], `${entryLocation}.after_state`);
    const beforeDigest = entry.before_sha256 === null ? null : expectString2(entry.before_sha256, `${entryLocation}.before_sha256`);
    const afterDigest = entry.after_sha256 === null ? null : expectString2(entry.after_sha256, `${entryLocation}.after_sha256`);
    if (beforeState === "absent" !== (beforeDigest === null) || afterState === "absent" !== (afterDigest === null)) {
      fail3("RUNTIME_SCHEMA_INVALID", `${entryLocation} digests must be null exactly for absent states.`);
    }
    if (beforeDigest !== null && !/^[a-f0-9]{64}$/u.test(beforeDigest) || afterDigest !== null && !/^[a-f0-9]{64}$/u.test(afterDigest)) {
      fail3("RUNTIME_SCHEMA_INVALID", `${entryLocation} digests must be SHA-256.`);
    }
    return {
      path: normalizeRepoPath2(expectString2(entry.path, `${entryLocation}.path`), `${entryLocation}.path`),
      before_state: beforeState,
      before_sha256: beforeDigest,
      after_state: afterState,
      after_sha256: afterDigest
    };
  });
  const baseRevision = expectString2(delta.base_revision, `${location}.base_revision`, /^[a-f0-9]{64}$/u);
  const targetRevision = expectString2(delta.target_revision, `${location}.target_revision`, /^[a-f0-9]{64}$/u);
  const expected = createReviewChangeDelta(base, target);
  const normalized = {
    kind: "runtime-file-delta/v1",
    base_revision: baseRevision,
    target_revision: targetRevision,
    entries
  };
  if (digest3(normalized) !== digest3(expected)) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} does not match the canonical before/after manifests.`);
  }
  return normalized;
}
function validateStepExpectedFailureEvidence(value, location) {
  const evidence = expectRecord2(value, location);
  expectExactKeys2(evidence, ["kind", "expected_behavior", "observed_failure_signature"], location);
  if (evidence.kind !== "behavior-not-implemented") {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.kind must be behavior-not-implemented.`);
  }
  return {
    kind: "behavior-not-implemented",
    expected_behavior: expectText(evidence.expected_behavior, `${location}.expected_behavior`),
    observed_failure_signature: expectText(evidence.observed_failure_signature, `${location}.observed_failure_signature`)
  };
}
function validateStepExecutionResult(value, location) {
  const result = expectRecord2(value, location);
  expectExactKeys2(result, [
    "outcome",
    "change_set_id",
    "review_base",
    "review_target",
    "change_delta",
    "actual_changed_paths",
    "command_results",
    "validation_results",
    "acceptance_evidence",
    "blocker",
    ...result.execution_id === undefined ? [] : ["execution_id"],
    ...result.attempt_id === undefined ? [] : ["attempt_id"],
    ...result.blocker_kind === undefined ? [] : ["blocker_kind"]
  ], location);
  const outcome = expectEnum(result.outcome, ["implemented", "test-red", "blocked"], `${location}.outcome`);
  const changeSetId = expectString2(result.change_set_id, `${location}.change_set_id`, SAFE_KEY_PATTERN2);
  const reviewBase = validateReviewTarget(result.review_base, `${location}.review_base`);
  const reviewTarget = validateReviewTarget(result.review_target, `${location}.review_target`);
  const changeDelta = validateReviewChangeDelta(result.change_delta, `${location}.change_delta`, reviewBase, reviewTarget);
  const actualChangedPaths = validateExecutionResultPaths(result.actual_changed_paths, `${location}.actual_changed_paths`);
  if (digest3([...actualChangedPaths].sort()) !== digest3(changeDelta.entries.map((item) => item.path))) {
    fail3("RUNTIME_STATE_CONFLICT", `${location}.actual_changed_paths must exactly match the Runtime before/after delta.`);
  }
  const commandValues = result.command_results;
  if (!Array.isArray(commandValues) || commandValues.length > MAX_EXECUTION_RESULT_ITEMS) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.command_results must be a bounded array.`);
  }
  const commandResults = commandValues.map((item, index) => {
    const itemLocation = `${location}.command_results[${index}]`;
    const command = expectRecord2(item, itemLocation);
    const status = expectEnum(command.status, STEP_EXECUTION_RESULT_STATUSES, `${itemLocation}.status`);
    expectExactKeys2(command, status === "expected-failure" ? ["command", "status", "observed_repo_writes", "evidence_refs", "expected_failure"] : ["command", "status", "observed_repo_writes", "evidence_refs", ...command.waiver_decision_id === undefined ? [] : ["waiver_decision_id"]], itemLocation);
    const evidenceRefs = expectStringArray2(command.evidence_refs, `${itemLocation}.evidence_refs`, status === "not-run", MAX_EVIDENCE_REFS);
    const observedRepoWrites = validateExecutionResultPaths(command.observed_repo_writes, `${itemLocation}.observed_repo_writes`);
    if (status === "not-run" && observedRepoWrites.length > 0) {
      fail3("RUNTIME_STATE_CONFLICT", `${itemLocation}.not-run command must not report repository writes.`);
    }
    const expectedFailure = status === "expected-failure" ? validateStepExpectedFailureEvidence(command.expected_failure, `${itemLocation}.expected_failure`) : undefined;
    return {
      command: expectText(command.command, `${itemLocation}.command`),
      ...command.waiver_decision_id === undefined ? {} : { waiver_decision_id: expectString2(command.waiver_decision_id, "waiver_decision_id", SAFE_KEY_PATTERN2) },
      status,
      observed_repo_writes: observedRepoWrites,
      evidence_refs: evidenceRefs,
      ...expectedFailure ? { expected_failure: expectedFailure } : {}
    };
  });
  if (new Set(commandResults.map((item) => item.command)).size !== commandResults.length) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.command_results must not contain duplicate commands.`);
  }
  const validationValues = result.validation_results;
  if (!Array.isArray(validationValues) || validationValues.length > MAX_EXECUTION_RESULT_ITEMS) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.validation_results must be a bounded array.`);
  }
  const validationResults = validationValues.map((item, index) => {
    const itemLocation = `${location}.validation_results[${index}]`;
    const validation = expectRecord2(item, itemLocation);
    const status = expectEnum(validation.status, STEP_EXECUTION_RESULT_STATUSES, `${itemLocation}.status`);
    expectExactKeys2(validation, status === "expected-failure" ? ["validation", "status", "evidence_refs", "expected_failure"] : ["validation", "status", "evidence_refs", ...validation.waiver_decision_id === undefined ? [] : ["waiver_decision_id"]], itemLocation);
    const expectedFailure = status === "expected-failure" ? validateStepExpectedFailureEvidence(validation.expected_failure, `${itemLocation}.expected_failure`) : undefined;
    return {
      validation: expectText(validation.validation, `${itemLocation}.validation`),
      ...validation.waiver_decision_id === undefined ? {} : { waiver_decision_id: expectString2(validation.waiver_decision_id, "waiver_decision_id", SAFE_KEY_PATTERN2) },
      status,
      evidence_refs: expectStringArray2(validation.evidence_refs, `${itemLocation}.evidence_refs`, status === "not-run", MAX_EVIDENCE_REFS),
      ...expectedFailure ? { expected_failure: expectedFailure } : {}
    };
  });
  if (new Set(validationResults.map((item) => item.validation)).size !== validationResults.length) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.validation_results must not contain duplicate validations.`);
  }
  const acceptanceEvidence = Array.isArray(result.acceptance_evidence) && result.acceptance_evidence.every((item) => isRecord4(item) && ("acceptance" in item)) ? result.acceptance_evidence.map((item) => {
    expectExactKeys2(item, ["acceptance", "evidence_refs"], "historical acceptance evidence");
    return { acceptance: expectText(item.acceptance, "acceptance"), evidence_refs: validateEvidenceRefs(item.evidence_refs, "evidence_refs") };
  }) : validateStepAcceptanceEvidence(result.acceptance_evidence, `${location}.acceptance_evidence`);
  const blocker = result.blocker === null ? null : expectText(result.blocker, `${location}.blocker`);
  const statuses = [...commandResults, ...validationResults].map((item) => item.status);
  if (outcome === "implemented" && (blocker !== null || [...commandResults, ...validationResults].some((item) => item.status !== "passed" && item.status !== "expected-failure" && !item.waiver_decision_id))) {
    fail3("RUNTIME_STATE_CONFLICT", `${location}.implemented requires passing results or exact retained waivers and no blocker.`);
  }
  if (outcome === "blocked" && (blocker === null || !statuses.some((status) => status === "failed" || status === "blocked"))) {
    fail3("RUNTIME_STATE_CONFLICT", `${location}.blocked requires a blocker and at least one failed or blocked result.`);
  }
  if (outcome === "test-red") {
    if (blocker !== null || acceptanceEvidence.length > 0 || !statuses.some((status) => status === "expected-failure") || statuses.some((status) => status !== "passed" && status !== "expected-failure")) {
      fail3("RUNTIME_STATE_CONFLICT", `${location}.test-red requires expected-failure evidence, permits only passed companion results, forbids acceptance evidence, and has no blocker.`);
    }
  }
  return {
    ...result.execution_id === undefined ? {} : { execution_id: expectString2(result.execution_id, `${location}.execution_id`, SAFE_KEY_PATTERN2) },
    ...result.attempt_id === undefined ? {} : { attempt_id: expectString2(result.attempt_id, "execution_result.attempt_id", SAFE_KEY_PATTERN2) },
    ...result.blocker_kind === undefined ? {} : { blocker_kind: expectEnum(result.blocker_kind, ["environment", "unknown"], "execution_result.blocker_kind") },
    outcome,
    change_set_id: changeSetId,
    review_base: reviewBase,
    review_target: reviewTarget,
    change_delta: changeDelta,
    actual_changed_paths: changeDelta.entries.map((item) => item.path),
    command_results: commandResults,
    validation_results: validationResults,
    acceptance_evidence: acceptanceEvidence,
    blocker
  };
}
var CLAIM_ID_PATTERN = /^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/;
var CLAIM_EVIDENCE_SLOT_ID_PATTERN = /^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/;
var CLAIM_EVIDENCE_TYPE_PATTERN = /^[A-Za-z0-9][A-Za-z0-9._/-]{0,127}$/;
var COMPLETE_CLAIM_EVIDENCE_DISPOSITIONS = ["existing", "reused", "newly-executed"];
function validateStepAcceptanceEvidence(value, location) {
  if (!Array.isArray(value) || value.length > MAX_EXECUTION_RESULT_ITEMS)
    fail3("RUNTIME_SCHEMA_INVALID", `${location} must be a bounded array.`);
  const results = value.map((raw) => {
    const item = expectRecord2(raw, location);
    expectExactKeys2(item, ["claim_id", "slot_id", "check_id", "minimum_type", "disposition", "evidence_refs", "report"], location);
    return {
      claim_id: expectString2(item.claim_id, "claim_id", CLAIM_ID_PATTERN),
      slot_id: expectString2(item.slot_id, "slot_id", CLAIM_ID_PATTERN),
      check_id: expectString2(item.check_id, "check_id", CLAIM_ID_PATTERN),
      minimum_type: expectString2(item.minimum_type, "minimum_type", CLAIM_EVIDENCE_TYPE_PATTERN),
      disposition: expectEnum(item.disposition, CLAIM_EVIDENCE_DISPOSITIONS, "disposition"),
      evidence_refs: validateEvidenceRefs(item.evidence_refs, "evidence_refs"),
      report: validateEvidenceReport(item.report)
    };
  });
  if (new Set(results.map((item) => `${item.claim_id}/${item.slot_id}`)).size !== results.length)
    fail3("CLAIM_EVIDENCE_PLAN_CONFLICT", "duplicate slot results.");
  return results;
}
function validateEvidenceReport(value) {
  const report = expectRecord2(value, "report");
  expectExactKeys2(report, ["result_id", "status", "evidence_plan_revision", "subject_revision", "actual_method", "environment", "assurance"], "report");
  return {
    result_id: expectString2(report.result_id, "report.result_id", CLAIM_ID_PATTERN),
    status: expectEnum(report.status, ["passed", "failed", "blocked", "not-run", "skipped", "accepted", "expected-failure"], "report.status"),
    evidence_plan_revision: expectString2(report.evidence_plan_revision, "report.evidence_plan_revision", /^[a-f0-9]{64}$/),
    subject_revision: expectString2(report.subject_revision, "report.subject_revision", /^[a-f0-9]{64}$/),
    actual_method: expectEnum(report.actual_method, ["execution", "static", "human"], "report.actual_method"),
    environment: expectText(report.environment, "report.environment"),
    assurance: "caller-reported"
  };
}
function evidenceSlotDefinition(slot) {
  const { disposition, evidence_refs, report, prerequisite_receipt, user_decision, ...definition } = slot;
  return definition;
}
function evidencePlanRevision(definition, records) {
  return digest3({ definition, claims: records.map((record) => ({ ...record, slots: record.slots.map(evidenceSlotDefinition).sort((a, b) => a.slot_id.localeCompare(b.slot_id)) })).sort((a, b) => a.claim_id.localeCompare(b.claim_id)) });
}
function plannedCommandsForEvidence(definition) {
  const commands = new Map;
  for (const step of parseImplementationSteps(definition.implementation_steps)) {
    const entries = new Set;
    for (const match of (step.plan_text ?? "").matchAll(/^\s{2,}-\s+planned_command:\s*(.+?)\s*$/gmu)) {
      entries.add(match[1].trim());
    }
    commands.set(step.id, entries);
  }
  return commands;
}
function evidenceInvocationCommand(invocation) {
  if (invocation.kind === "opaque")
    return invocation.command;
  if (!invocation.argv.length || invocation.argv.some((arg) => !arg || /[$`\r\n\0]/u.test(arg))) {
    fail3("CLAIM_EVIDENCE_INVOCATION_INVALID", "invocation needs literal nonempty arguments without shell expansion.");
  }
  if (!/^[\w./:\\-]+$/u.test(invocation.argv[0]) || /^(?:.*[/\\])?(?:sh|bash|zsh|cmd|powershell|pwsh)(?:\.exe)?$/iu.test(invocation.argv[0])) {
    fail3("CLAIM_EVIDENCE_INVOCATION_INVALID", "invocation must name a runner directly, not a shell command interpreter.");
  }
  return invocation.argv.map((arg) => /^[\w./:=,@+\-]+$/u.test(arg) ? arg : JSON.stringify(arg)).join(" ");
}
function unchangedEvidenceCheck(record, slot, previous) {
  const old = previous.find((item) => item.claim_id === record.claim_id);
  const oldSlot = old?.slots.find((item) => item.slot_id === slot.slot_id);
  if (!old || !oldSlot)
    return false;
  return old.requirement === record.requirement && old.source_ref === record.source_ref && old.claim_kind === record.claim_kind && old.boundary === record.boundary && digest3(oldSlot.check) === digest3(slot.check);
}
function taskBasisHasSource(taskBasis, ref) {
  return taskBasis.original_request.source === ref || taskBasis.user_decisions.some((item) => item.source === ref);
}
function assertSelectionAuthority(selection, record, context) {
  const basis = selection.breadth_basis;
  const ref = selection.breadth_source_ref;
  if (!basis)
    return;
  if (basis === "explicit-user") {
    if (!taskBasisHasSource(context.taskBasis, ref))
      fail3("TEST_STRATEGY_INVALID", "explicit-user source_ref must equal an exact Task Basis source coordinate.");
    return;
  }
  if (basis === "project-policy" || basis === "release-gate") {
    assertRepositoryAuthoritySource(context.root, ref);
    return;
  }
  if (ref !== record.claim_id || basis === "acceptance-boundary" && record.claim_kind !== "acceptance") {
    fail3("CLAIM_EVIDENCE_AUTHORITY_INVALID", "claim/acceptance/insufficiency basis must reference this exact claim identity.");
  }
  if (!taskBasisHasSource(context.taskBasis, record.source_ref))
    assertRepositoryAuthoritySource(context.root, record.source_ref);
}
function assertExecutionSelection(check, required) {
  if (check.method !== "execution") {
    if (check.selection !== undefined)
      fail3("CLAIM_EVIDENCE_SELECTION_INVALID", "only execution checks may declare test selection.");
    return;
  }
  if (!check.selection) {
    if (required)
      fail3("CLAIM_EVIDENCE_SELECTION_REQUIRED", "new execution checks require a frozen minimum-sufficient selection declaration.");
    return;
  }
  const selection = check.selection;
  if (!required && selection.granularity === undefined)
    return;
  if (!selection.granularity || selection.scope !== undefined || selection.claim_scope !== undefined || !check.boundary) {
    fail3("CLAIM_EVIDENCE_SELECTION_INVALID", "new execution checks require check.boundary and orthogonal selection.granularity; legacy scope labels are read-only.");
  }
  if (!selection.invocation || evidenceInvocationCommand(selection.invocation) !== check.entry) {
    fail3("CLAIM_EVIDENCE_INVOCATION_UNBOUND", "selection invocation must produce the exact frozen check entry.");
  }
  if (selection.invocation.kind === "opaque") {
    if (selection.granularity === "focused" || selection.selector !== null) {
      fail3("CLAIM_EVIDENCE_SELECTOR_UNBOUND", "opaque invocation cannot prove focused selection; admit its actual target/broad granularity.");
    }
  } else {
    const selectorIndex = selection.invocation.selector_arg_index;
    if (selection.selector === null ? selectorIndex !== null : selectorIndex === null || !Number.isInteger(selectorIndex) || selectorIndex < 1 || selection.invocation.argv[selectorIndex] !== selection.selector) {
      fail3("CLAIM_EVIDENCE_SELECTOR_UNBOUND", "selector must equal the literal runner argument at selector_arg_index.");
    }
  }
  const hasBreadth = selection.breadth_reason !== null || selection.breadth_basis !== null || selection.breadth_source_ref !== null;
  if (selection.granularity === "focused") {
    if (!selection.selector || check.boundary !== "e2e" && hasBreadth) {
      fail3("CLAIM_EVIDENCE_SELECTION_INVALID", "focused execution requires an exact selector; only E2E needs additional authority at focused granularity.");
    }
    if (check.boundary !== "e2e")
      return;
  } else if (selection.selector !== null) {
    fail3("CLAIM_EVIDENCE_SELECTION_INVALID", "target and broad-regression granularity cannot claim a focused selector.");
  }
  if (!selection.breadth_reason || !selection.breadth_basis || !selection.breadth_source_ref) {
    fail3("CLAIM_EVIDENCE_BREADTH_REQUIRED", "target/broad granularity and every E2E boundary require an explicit reason, basis, and source reference.");
  }
  if (check.boundary === "e2e") {
    if (!["acceptance-boundary", "low-cost-evidence-insufficient", "project-policy", "release-gate", "explicit-user"].includes(selection.breadth_basis)) {
      fail3("CLAIM_EVIDENCE_E2E_UNAUTHORIZED", "E2E execution requires an acceptance boundary, insufficient lower-cost evidence, project policy, bound release gate, or explicit user request.");
    }
  }
  if (selection.granularity === "broad-regression" && !["claim-risk-contract", "project-policy", "release-gate", "explicit-user"].includes(selection.breadth_basis)) {
    fail3("CLAIM_EVIDENCE_BROAD_UNAUTHORIZED", "broad regression requires a claim/risk/contract basis, release gate, project policy, or explicit user request.");
  }
}
function validationItemOwners(records, stepId, label) {
  return records.flatMap((claim) => claim.slots).filter((slot) => slot.due_step_id === stepId && slot.check && (slot.check.validation_items?.includes(label) || slot.check.entry === label));
}
function slotOwnsValidation(current, slot, label) {
  const step = resolveCanonicalTaskStep(current).steps.find((item) => item.id === slot.due_step_id);
  const labels = (step?.required_evidence ?? "").split(";").map((item) => item.trim()).filter(Boolean);
  const owners = validationItemOwners(current.runtimeState.claim_evidence ?? [], slot.due_step_id, label);
  return labels.includes(label) && owners.length === 1 && owners[0] === slot;
}
function assertEvidencePlan(definition, records, fresh = false, context) {
  requireClaimEvidencePlan(records, "evidence plan");
  requireAcceptanceClaim(records, "evidence plan");
  const parsedEvidenceSteps = parseImplementationSteps(definition.implementation_steps);
  const steps = parsedEvidenceSteps.map((step) => step.id);
  const plannedCommands = plannedCommandsForEvidence(definition);
  const ids = new Set;
  const acceptance = records.filter((record) => record.claim_kind === "acceptance").map((record) => record.requirement);
  const projected = definition.acceptance.split(/\r?\n/).map((line) => line.replace(/^\s*[-*]\s+(?:\[[ xX]\]\s*)?/, "").trim()).filter(Boolean);
  if (digest3(acceptance) !== digest3(projected))
    fail3("CLAIM_EVIDENCE_PLAN_CONFLICT", "acceptance must be the exact projection of acceptance claim requirements.");
  for (const record of records) {
    if (!record.requirement || !record.source_ref)
      fail3("CLAIM_EVIDENCE_PLAN_INVALID", "claims require requirement and source_ref.");
    for (const slot of record.slots) {
      const check = slot.check;
      if (!check || !slot.applicability || !steps.includes(slot.due_step_id) || slot.minimum_type === "planned-validation")
        fail3("CLAIM_EVIDENCE_PLAN_INVALID", "slots require a concrete check, applicability and valid due step.");
      if (ids.has(check.check_id))
        fail3("CLAIM_EVIDENCE_PLAN_INVALID", "check_id must be unique across the task.");
      ids.add(check.check_id);
      const validationItems = check.validation_items ?? [];
      const labels = (parsedEvidenceSteps.find((step) => step.id === slot.due_step_id)?.required_evidence ?? "").split(";").map((item) => item.trim()).filter(Boolean);
      if (new Set(validationItems).size !== validationItems.length || validationItems.some((label) => !labels.includes(label) || validationItemOwners(records, slot.due_step_id, label).length !== 1)) {
        fail3("CLAIM_EVIDENCE_VALIDATION_UNBOUND", "Each declared validation item must be an exact due-step label exclusively owned by this check.");
      }
      if (check.subject_paths.some((p) => p.includes("*") || /(?:^|\/)CURRENT_TASK\.md$/.test(p) || p.startsWith(".git/")) || new Set(check.subject_paths).size !== check.subject_paths.length)
        fail3("CLAIM_EVIDENCE_PLAN_INVALID", "subject_paths must be exact unique product paths, excluding Runtime audit state.");
      const isNew = context ? !unchangedEvidenceCheck(record, slot, context.previous) : fresh;
      if (isNew && record.boundary !== undefined && !context?.previous.some((old) => old.claim_id === record.claim_id && old.boundary === record.boundary)) {
        fail3("CLAIM_EVIDENCE_BOUNDARY_INVALID", "boundary belongs to each check, not a new claim.");
      }
      if (isNew && slot.user_decision)
        fail3("USER_EVIDENCE_ACTION_REQUIRED", "A new evidence definition cannot invent or inherit a user decision.");
      assertExecutionSelection(check, isNew);
      const bound = isNew || check.selection?.granularity !== undefined;
      if (bound && check.method === "execution" && !plannedCommands.get(slot.due_step_id)?.has(check.entry)) {
        fail3("CLAIM_EVIDENCE_COMMAND_UNBOUND", `execution check ${check.check_id} must bind its due step's exact planned command.`);
      }
      if (isNew && check.selection?.breadth_basis) {
        if (!context)
          fail3("CLAIM_EVIDENCE_AUTHORITY_REQUIRED", "breadth admission requires canonical task authority context.");
        assertSelectionAuthority(check.selection, record, context);
      }
      if (record.claim_kind === "acceptance" && (slot.applicability !== "current" || check.expected_result === "expected-failure"))
        fail3("CLAIM_EVIDENCE_PLAN_INVALID", "positive acceptance requires current successful evidence.");
      if (check.method === "execution" !== (check.expected_result !== "accepted"))
        fail3("CLAIM_EVIDENCE_PLAN_INVALID", "execution requires passed/expected-failure; static/human requires accepted.");
      if (slot.applicability === "before-step") {
        if (!steps.includes(slot.before_step_id) || steps.indexOf(slot.due_step_id) > steps.indexOf(slot.before_step_id))
          fail3("CLAIM_EVIDENCE_PLAN_INVALID", "prerequisite must be due no later than its constrained step.");
        if (slot.before_step_id === steps[0])
          fail3("TEST_STRATEGY_PREREQUISITE_UNSUPPORTED", "A first-step prerequisite has no earlier authorized result submission step.");
      } else if (slot.before_step_id !== undefined || slot.prerequisite_receipt !== undefined)
        fail3("CLAIM_EVIDENCE_PLAN_INVALID", "current slots cannot carry prerequisite fields.");
      if (fresh && (slot.report || slot.prerequisite_receipt || slot.disposition !== "missing" || slot.evidence_refs.length))
        fail3("CLAIM_EVIDENCE_PLAN_INVALID", "new plans cannot import results or prerequisite receipts.");
    }
  }
  const parsedSteps = parseImplementationSteps(definition.implementation_steps);
  if (fresh && parsedSteps.some((step) => step.review_checkpoint === "not-required")) {
    if (parsedSteps.some((step) => !step.checkpoint_boundary))
      fail3("REVIEW_CHECKPOINT_REQUIRED", "Sparse checkpoints require explicit reasons on every step.");
    const final = parsedSteps.at(-1);
    if (final.review_checkpoint !== "required" && !(final.checkpoint_boundary?.startsWith("final-exemption:") && final.checkpoint_boundary.slice("final-exemption:".length).trim()))
      fail3("REVIEW_CHECKPOINT_REQUIRED", "A final waiver must explicitly cover cumulative changes with final-exemption: and a reason.");
    const earlier = new Set;
    for (const step of parsedSteps) {
      const scope = (step.mutation_scope ?? "").split(",").map((p) => p.trim().replace(/^`|`$/g, ""));
      if (step.review_checkpoint === "required" && [...earlier].some((p) => !scope.includes(p)))
        fail3("REVIEW_REPAIR_SCOPE_REQUIRED", "Required checkpoints must reserve exact earlier paths for repair.");
      scope.forEach((p) => earlier.add(p));
    }
  }
  const strategy = readTestStrategyDefinition(definition);
  if (["test-first", "implementation-first"].includes(strategy.mode) && !records.some((record) => record.slots.some((slot) => slot.applicability === "before-step")))
    fail3("TEST_STRATEGY_PREREQUISITE_UNSUPPORTED", "explicit ordering requires an approved before-step check.");
  assertPersistentTestAdmission(definition, records);
  return evidencePlanRevision(definition, records);
}
function assertPersistentTestAdmission(definition, records) {
  const content = testStrategySection(definition.regression_checks, ["Persistent Tests", "持久测试"], "persistent_tests");
  const fields = ["owner", "owner_source", "source_ref", "basis", "existing_evidence_insufficiency", "assertion_boundary", "failure_disposition"];
  for (const testPath of readPersistentTestPaths(definition)) {
    const block = content.split(/(?=^-\s+`)/m).find((item) => item.startsWith(`- \`${testPath}\``)) ?? "";
    const values = new Map([...block.matchAll(/^\s+- ([a-z_]+): (.+)$/gm)].map((match) => [match[1], match[2].trim()]));
    if (fields.some((field) => !values.get(field)) || !["acceptance", "regression", "critical-invariant", "critical-risk"].includes(values.get("basis")))
      fail3("PERSISTENT_TEST_ADMISSION_INVALID", `${testPath} requires P-12 source, necessity and assertion boundaries.`);
    const proves = [...block.matchAll(/^\s+- proves: (.+)$/gm)].map((match) => match[1].trim());
    if (!proves.length || proves.some((id) => !records.some((record) => record.claim_id === id)))
      fail3("PERSISTENT_TEST_ADMISSION_INVALID", `${testPath}.proves must bind stable claim IDs.`);
  }
}
function recoveryEvidenceContextRevision(root2) {
  return digest3([".workflow-system/PROJECT_PROFILE.yaml", ".workflow-system/vnext/RUNTIME_CONTRACT.yaml", ".workflow-system/runtime/package.json"].map((relative) => {
    const file = path12.resolve(root2, relative);
    return { path: relative, sha256: fs11.existsSync(file) ? sha2565(fs11.readFileSync(file)) : null };
  }));
}
function validateUserEvidenceDecision(value) {
  const d = expectRecord2(value, "user_decision");
  expectExactKeys2(d, ["kind", "decision_id", "decision_source", "statement_sha256", "check_id", "evidence_plan_revision", "subject_revision", "recorded_at", "assurance", ...d.validation_items === undefined ? [] : ["validation_items"]], "user_decision");
  expectEnum(d.assurance, ["caller-reported"], "user_decision.assurance");
  return {
    kind: expectEnum(d.kind, ["human-acceptance", "waiver"], "user_decision.kind"),
    ...d.validation_items === undefined ? {} : { validation_items: expectStringArray2(d.validation_items, "user_decision.validation_items", true, MAX_EXECUTION_RESULT_ITEMS) },
    decision_id: expectString2(d.decision_id, "decision_id", SAFE_KEY_PATTERN2),
    decision_source: expectText(d.decision_source, "decision_source"),
    statement_sha256: expectString2(d.statement_sha256, "statement_sha256", SHA256_PATTERN2),
    check_id: expectString2(d.check_id, "check_id", CLAIM_ID_PATTERN),
    evidence_plan_revision: expectString2(d.evidence_plan_revision, "evidence_plan_revision", SHA256_PATTERN2),
    subject_revision: expectString2(d.subject_revision, "subject_revision", SHA256_PATTERN2),
    recorded_at: expectString2(d.recorded_at, "recorded_at"),
    assurance: "caller-reported"
  };
}
function assertUserEvidenceApplicable(root2, current, slot) {
  const d = slot.user_decision;
  if (!d || !slot.check || d.check_id !== slot.check.check_id) {
    fail3("USER_EVIDENCE_STALE", "The user decision is not bound to this exact check and current evidence plan.");
  }
  if (d.evidence_plan_revision !== current.runtimeState.evidence_plan_revision) {
    const owner = current.runtimeState.claim_evidence?.find((c) => c.slots.some((s) => s.slot_id === slot.slot_id && s.user_decision?.decision_id === d.decision_id));
    const proof = (current.runtimeState.evidence_carry_forward ?? []).find((p) => p.claim_id === owner?.claim_id && p.slot_id === slot.slot_id && p.check_id === slot.check.check_id && p.user_decision_sha256 === digest3(d) && p.old_plan_revision === d.evidence_plan_revision && p.new_plan_revision === current.runtimeState.evidence_plan_revision);
    if (!proof)
      fail3("USER_EVIDENCE_STALE", "No unchanged-decision carry-forward proof exists for this plan.");
    assertTaskHistoryForRevision(current.filePath, current.sourceTuple.document_id, current.runtimeState.task_id, proof.old_source_revision, proof.history_operation ?? "confirm-replan");
    const file = path12.join(path12.dirname(current.filePath), "task-history", current.sourceTuple.document_id, `${proof.old_source_revision}.json`);
    const history = JSON.parse(fs11.readFileSync(file, "utf8"));
    const old = parseCanonicalCurrentTaskContent(Buffer.from(history.current_task_base64, "base64").toString("utf8"), current.filePath, current.relativePath);
    const oldSlot = old.runtimeState.claim_evidence?.find((c) => c.claim_id === proof.claim_id)?.slots.find((s) => s.slot_id === slot.slot_id);
    if (digest3(oldSlot?.user_decision) !== digest3(d) || digest3(oldSlot?.check) !== digest3(slot.check))
      fail3("USER_EVIDENCE_STALE", "The retained user decision or observation changed.");
  }
  const basis = readCanonicalTaskBasis(root2, current).basis;
  const statement = [basis.original_request, ...basis.user_decisions].find((item) => item.source === d.decision_source);
  if (!statement || sha2565(statement.verbatim) !== d.statement_sha256)
    fail3("USER_EVIDENCE_SOURCE_INVALID", "The exact user statement is not retained in the linked Task Basis.");
  const snapshot = slot.prerequisite_receipt?.subject_snapshot ?? captureReviewTarget(root2, slot.check.subject_paths);
  if (snapshot.revision !== d.subject_revision)
    fail3("USER_EVIDENCE_STALE", "The observed objects changed after the user decision.");
  if ((current.runtimeState.evidence_challenges ?? []).some((item) => item.slot_id === slot.slot_id && current.runtimeState.claim_evidence?.find((claim) => claim.claim_id === item.claim_id)?.slots.some((s) => s.slot_id === slot.slot_id && s.check?.check_id === slot.check?.check_id) && item.status !== "resolved"))
    fail3("USER_EVIDENCE_CHALLENGED", "A user decision cannot silently discharge a retained evidence challenge.");
}
function carryUserEvidenceDecision(root2, current, claimId, slot, newPlan, operation) {
  assertUserEvidenceApplicable(root2, current, slot);
  const d = slot.user_decision;
  const origin = current.runtimeState.evidence_carry_forward?.find((p) => p.claim_id === claimId && p.slot_id === slot.slot_id && p.new_plan_revision === current.runtimeState.evidence_plan_revision && p.user_decision_sha256 === digest3(d));
  return {
    kind: "evidence-carry-forward/v2",
    old_source_revision: origin?.old_source_revision ?? current.sourceTuple.revision,
    history_operation: origin ? origin.history_operation ?? "confirm-replan" : operation,
    old_plan_revision: d.evidence_plan_revision,
    new_plan_revision: newPlan,
    receiving_source_revision: current.sourceTuple.revision,
    context_revision: recoveryEvidenceContextRevision(root2),
    evidence_objects: [],
    claim_id: claimId,
    slot_id: slot.slot_id,
    check_id: d.check_id,
    result_id: d.decision_id,
    report_sha256: digest3(slot.report ?? null),
    subject_revision: d.subject_revision,
    user_decision_sha256: digest3(d)
  };
}
function applicableResultWaiver(root2, current, stepId, result, decisionId) {
  const slots = (current.runtimeState.claim_evidence ?? []).flatMap((claim) => claim.slots);
  const candidates = slots.filter((slot) => slot.due_step_id === stepId && slot.user_decision?.kind === "waiver" && (!decisionId || slot.user_decision.decision_id === decisionId) && ("command" in result ? slot.check?.method === "execution" && slot.check.entry === result.command : slot.user_decision.validation_items?.includes(result.validation) && slotOwnsValidation(current, slot, result.validation)));
  for (const slot of candidates) {
    assertUserEvidenceApplicable(root2, current, slot);
    if ("command" in result) {
      const step = resolveCanonicalTaskStep(current).steps.find((item) => item.id === stepId);
      const lines = (step?.plan_text ?? "").split(`
`);
      const indices = lines.flatMap((line, i) => line.trim() === `- planned_command: ${result.command}` ? [i] : []);
      if (indices.length !== 1 || lines[indices[0] + 1]?.trim() !== "- expected_repo_writes: none")
        continue;
      const consumers = slots.filter((other) => other.due_step_id === stepId && other.check?.method === "execution" && other.check.entry === result.command);
      if (consumers.some((other) => other.user_decision?.kind !== "waiver"))
        continue;
      for (const consumer of consumers)
        assertUserEvidenceApplicable(root2, current, consumer);
    }
    return slot.user_decision;
  }
  return null;
}
function assertExecutionResultWaivers(root2, current, stepId, result) {
  for (const item of [...result.command_results, ...result.validation_results]) {
    if (!item.waiver_decision_id)
      continue;
    if (item.status === "passed" || item.status === "expected-failure" || !applicableResultWaiver(root2, current, stepId, item, item.waiver_decision_id)) {
      fail3("EVIDENCE_WAIVER_RESULT_UNBOUND", "A nonpassing result needs its exact retained, applicable user waiver; a waiver is not PASS.");
    }
  }
}
function assertEvidenceReportApplicable(root2, current, slot) {
  const report = slot.report;
  if (!report || !slot.check || report.actual_method !== slot.check.method)
    fail3("CLAIM_EVIDENCE_INCOMPLETE", "report must bind its frozen check and method.");
  if ((current.runtimeState.evidence_challenges ?? []).some((item) => item.result_id === report.result_id && item.status !== "resolved")) {
    fail3("CLAIM_EVIDENCE_INCOMPLETE", "An unresolved counterexample blocks this report even when its subject hash is unchanged.");
  }
  if (report.evidence_plan_revision !== current.runtimeState.evidence_plan_revision) {
    const owner = (current.runtimeState.claim_evidence ?? []).find((record) => record.slots.some((item) => item.slot_id === slot.slot_id && item.report?.result_id === report.result_id));
    const proof = (current.runtimeState.evidence_carry_forward ?? []).find((item) => item.claim_id === owner?.claim_id && item.slot_id === slot.slot_id && item.check_id === slot.check.check_id && item.result_id === report.result_id && item.old_plan_revision === report.evidence_plan_revision && item.new_plan_revision === current.runtimeState.evidence_plan_revision && item.report_sha256 === digest3(report) && item.subject_revision === report.subject_revision);
    if (!proof || (current.runtimeState.evidence_challenges ?? []).some((item) => item.claim_id === proof.claim_id && item.slot_id === proof.slot_id && item.status !== "resolved")) {
      fail3("CLAIM_EVIDENCE_INCOMPLETE", "old-plan report has no applicable unchallenged Runtime carry-forward proof.");
    }
    assertTaskHistoryForRevision(current.filePath, current.sourceTuple.document_id, current.runtimeState.task_id, proof.old_source_revision, proof.history_operation ?? "confirm-replan");
    const historyFile = path12.join(path12.dirname(current.filePath), "task-history", current.sourceTuple.document_id, `${proof.old_source_revision}.json`);
    const history = JSON.parse(fs11.readFileSync(historyFile, "utf8"));
    const previous = parseCanonicalCurrentTaskContent(Buffer.from(history.current_task_base64, "base64").toString("utf8"), current.filePath, current.relativePath);
    const oldSlot = previous.runtimeState.claim_evidence?.find((record) => record.claim_id === proof.claim_id)?.slots.find((item) => item.slot_id === proof.slot_id);
    if (previous.runtimeState.evidence_plan_revision !== proof.old_plan_revision || digest3(oldSlot?.report) !== proof.report_sha256 || digest3(oldSlot?.check) !== digest3(slot.check)) {
      fail3("EVIDENCE_CARRY_FORWARD_STALE", "immutable source does not contain the exact unchanged report and check.");
    }
    if (proof.kind === "evidence-carry-forward/v2") {
      if (proof.context_revision !== recoveryEvidenceContextRevision(root2))
        fail3("EVIDENCE_CARRY_FORWARD_STALE", "Project configuration or installed Runtime context changed; reassess affected evidence.");
      if (!slot.user_decision && digest3(describeEvidenceObjects(root2, slot.evidence_refs)) !== digest3(proof.evidence_objects))
        fail3("EVIDENCE_CARRY_FORWARD_STALE", "Evidence body changed; affected evidence needs reassessment.");
      for (const object of proof.evidence_objects)
        verifyEvidenceObject(root2, current.filePath, object);
    }
  }
  if (report.actual_method === "human") {
    if (slot.user_decision?.kind !== "human-acceptance" || slot.user_decision.decision_id !== report.result_id) {
      fail3("EVIDENCE_AUTHORITY_UNSUPPORTED", "Human evidence requires the explicit record-human-acceptance route; a raw report is not a user statement.");
    }
    assertUserEvidenceApplicable(root2, current, slot);
  }
  if (!slot.evidence_refs.length || slot.evidence_refs.some((ref) => {
    try {
      const p = normalizeRepoPath2(ref.split("#")[0], "evidence_ref");
      return !fs11.statSync(path12.resolve(root2, p)).isFile();
    } catch {
      return true;
    }
  }))
    fail3("EVIDENCE_ARTIFACT_UNAVAILABLE", "Each report needs retained repository-relative artifact files.");
  const snapshot = slot.prerequisite_receipt?.subject_snapshot ?? captureReviewTarget(root2, slot.check.subject_paths);
  if (snapshot.revision !== report.subject_revision || digest3(snapshot.entries.map((entry) => entry.path).sort()) !== digest3([...slot.check.subject_paths].sort()))
    fail3("CLAIM_EVIDENCE_STALE", "evidence subject changed or does not match the frozen object set.");
  if (slot.prerequisite_receipt && (slot.prerequisite_receipt.result_id !== report.result_id || slot.prerequisite_receipt.step_id !== slot.before_step_id))
    fail3("CLAIM_EVIDENCE_PLAN_CONFLICT", "prerequisite receipt does not bind its original report.");
}
function assertEvidenceSlotSatisfied(root2, current, record, slot, closing) {
  if (slot.user_decision?.kind === "waiver") {
    assertUserEvidenceApplicable(root2, current, slot);
    return;
  }
  assertEvidenceReportApplicable(root2, current, slot);
  if (!isClaimEvidenceSlotComplete(slot) || slot.report.status !== slot.check.expected_result)
    fail3("CLAIM_EVIDENCE_INCOMPLETE", `unsatisfied slot ${record.claim_id}/${slot.slot_id}`);
  if (closing && slot.applicability === "before-step" && !slot.prerequisite_receipt)
    fail3("PREREQUISITE_REQUIRED", "prerequisite has not been consumed before execution.");
}
function evaluateEvidenceSlotForContext(root2, current, claim, slot) {
  try {
    assertEvidenceSlotSatisfied(root2, current, claim, slot, false);
    return { satisfied: true, reason: null };
  } catch (error) {
    return { satisfied: false, reason: error instanceof Error ? error.message : String(error) };
  }
}
function validateClaimEvidence(value, location) {
  if (!Array.isArray(value) || value.length > MAX_CLAIM_EVIDENCE_RECORDS) {
    fail3("CLAIM_EVIDENCE_INVALID", `${location} must be a bounded array of claim evidence records.`);
  }
  const records = value.map((raw, index) => {
    const record = expectRecord2(raw, `${location}[${index}]`);
    expectExactKeys2(record, ["claim_id", "claim_kind", "slots", ...["requirement", "source_ref", "boundary"].filter((key) => (key in record))], `${location}[${index}]`);
    const claimId = expectString2(record.claim_id, `${location}[${index}].claim_id`, CLAIM_ID_PATTERN);
    const claimKind = expectEnum(record.claim_kind, CLAIM_KINDS, `${location}[${index}].claim_kind`);
    if (!Array.isArray(record.slots) || record.slots.length === 0 || record.slots.length > MAX_CLAIM_EVIDENCE_SLOTS) {
      fail3("CLAIM_EVIDENCE_INVALID", `${location}[${index}].slots must be a non-empty bounded array.`);
    }
    const slots = record.slots.map((rawSlot, slotIndex) => {
      const slot = expectRecord2(rawSlot, `${location}[${index}].slots[${slotIndex}]`);
      expectExactKeys2(slot, ["slot_id", "minimum_type", "disposition", "evidence_refs", ...["due_step_id", "applicability", "before_step_id", "check", "report", "prerequisite_receipt", "user_decision"].filter((key) => (key in slot))], `${location}[${index}].slots[${slotIndex}]`);
      const slotId = expectString2(slot.slot_id, `${location}[${index}].slots[${slotIndex}].slot_id`, CLAIM_EVIDENCE_SLOT_ID_PATTERN);
      const minimumType = expectString2(slot.minimum_type, `${location}[${index}].slots[${slotIndex}].minimum_type`, CLAIM_EVIDENCE_TYPE_PATTERN);
      const disposition = expectEnum(slot.disposition, CLAIM_EVIDENCE_DISPOSITIONS, `${location}[${index}].slots[${slotIndex}].disposition`);
      const evidenceRefs = expectStringArray2(slot.evidence_refs, `${location}[${index}].slots[${slotIndex}].evidence_refs`, true, MAX_EVIDENCE_REFS);
      if (COMPLETE_CLAIM_EVIDENCE_DISPOSITIONS.includes(disposition) && evidenceRefs.length === 0) {
        fail3("CLAIM_EVIDENCE_INVALID", `${location}[${index}].slots[${slotIndex}] requires evidence_refs for disposition=${disposition}.`);
      }
      const result = { slot_id: slotId, minimum_type: minimumType, disposition, evidence_refs: evidenceRefs };
      if (slot.check !== undefined) {
        const check = expectRecord2(slot.check, "slot.check");
        expectExactKeys2(check, ["check_id", "method", "entry", "expected_observation", "required_boundaries", "allowed_substitutes", "subject_paths", "expected_result", ...["selection", "boundary", "validation_items"].filter((key) => (key in check))], "slot.check");
        let selection;
        if (check.selection !== undefined) {
          const rawSelection = expectRecord2(check.selection, "check.selection");
          const modern = "granularity" in rawSelection;
          expectExactKeys2(rawSelection, [...modern ? ["granularity"] : ["scope", "claim_scope"], "selector", "selection_reason", "breadth_reason", "breadth_basis", "breadth_source_ref", ..."invocation" in rawSelection ? ["invocation"] : []], "check.selection");
          const dimensions = modern ? { granularity: expectEnum(rawSelection.granularity, EVIDENCE_EXECUTION_GRANULARITIES, "check.selection.granularity") } : { scope: expectEnum(rawSelection.scope, LEGACY_EVIDENCE_EXECUTION_SELECTION_SCOPES, "check.selection.scope"), claim_scope: expectEnum(rawSelection.claim_scope, EVIDENCE_CHECK_BOUNDARIES, "check.selection.claim_scope") };
          let invocation;
          if (rawSelection.invocation !== undefined) {
            const raw = expectRecord2(rawSelection.invocation, "selection.invocation");
            if (raw.kind === "opaque") {
              expectExactKeys2(raw, ["kind", "command"], "selection.invocation");
              invocation = { kind: "opaque", command: expectText(raw.command, "invocation.command") };
            } else {
              expectExactKeys2(raw, ["argv", "selector_arg_index", ..."kind" in raw ? ["kind"] : []], "selection.invocation");
              if (raw.kind !== undefined)
                expectEnum(raw.kind, ["structured"], "invocation.kind");
              if (raw.selector_arg_index !== null && (!Number.isInteger(raw.selector_arg_index) || raw.selector_arg_index < 1))
                fail3("CLAIM_EVIDENCE_SELECTOR_UNBOUND", "selector_arg_index must be null or a positive integer.");
              if (!Array.isArray(raw.argv) || raw.argv.length === 0)
                fail3("CLAIM_EVIDENCE_INVOCATION_INVALID", "invocation.argv must be a nonempty ordered argument list.");
              invocation = { ...raw.kind === undefined ? {} : { kind: "structured" }, argv: raw.argv.map((arg, index) => expectText(arg, `invocation.argv[${index}]`)), selector_arg_index: raw.selector_arg_index };
            }
          }
          const selector = rawSelection.selector === null ? null : expectText(rawSelection.selector, "check.selection.selector");
          const breadthReason = rawSelection.breadth_reason === null ? null : expectText(rawSelection.breadth_reason, "check.selection.breadth_reason");
          const breadthBasis = rawSelection.breadth_basis === null ? null : expectEnum(rawSelection.breadth_basis, EVIDENCE_EXECUTION_BREADTH_BASES, "check.selection.breadth_basis");
          const breadthSourceRef = rawSelection.breadth_source_ref === null ? null : expectText(rawSelection.breadth_source_ref, "check.selection.breadth_source_ref");
          selection = {
            ...dimensions,
            selector,
            selection_reason: expectText(rawSelection.selection_reason, "check.selection.selection_reason"),
            breadth_reason: breadthReason,
            breadth_basis: breadthBasis,
            breadth_source_ref: breadthSourceRef,
            ...invocation ? { invocation } : {}
          };
        }
        result.check = {
          check_id: expectString2(check.check_id, "check.check_id", CLAIM_ID_PATTERN),
          method: expectEnum(check.method, ["execution", "static", "human"], "check.method"),
          entry: expectText(check.entry, "check.entry"),
          expected_observation: expectText(check.expected_observation, "check.expected_observation"),
          ...check.validation_items === undefined ? {} : { validation_items: expectStringArray2(check.validation_items, "check.validation_items", true, MAX_EXECUTION_RESULT_ITEMS) },
          ...check.boundary === undefined ? {} : { boundary: expectEnum(check.boundary, EVIDENCE_CHECK_BOUNDARIES, "check.boundary") },
          required_boundaries: expectStringArray2(check.required_boundaries, "check.required_boundaries", false, 256),
          allowed_substitutes: expectStringArray2(check.allowed_substitutes, "check.allowed_substitutes", true, 256),
          subject_paths: expectStringArray2(check.subject_paths, "check.subject_paths", false, 256).map((p) => normalizeRepoPath2(p, "check.subject_paths")),
          expected_result: expectEnum(check.expected_result, ["passed", "accepted", "expected-failure"], "check.expected_result"),
          ...selection === undefined ? {} : { selection }
        };
      }
      if (slot.due_step_id !== undefined)
        result.due_step_id = expectString2(slot.due_step_id, "slot.due_step_id", STEP_ID_PATTERN2);
      if (slot.applicability !== undefined)
        result.applicability = expectEnum(slot.applicability, ["current", "before-step"], "slot.applicability");
      if (slot.before_step_id !== undefined)
        result.before_step_id = expectString2(slot.before_step_id, "slot.before_step_id", STEP_ID_PATTERN2);
      if (slot.report !== undefined)
        result.report = slot.report === null ? null : validateEvidenceReport(slot.report);
      if (slot.user_decision !== undefined)
        result.user_decision = validateUserEvidenceDecision(slot.user_decision);
      if (slot.prerequisite_receipt !== undefined) {
        if (slot.prerequisite_receipt === null)
          result.prerequisite_receipt = null;
        else {
          const receipt = expectRecord2(slot.prerequisite_receipt, "prerequisite_receipt");
          expectExactKeys2(receipt, ["step_id", "preflight_id", "result_id", "subject_snapshot"], "prerequisite_receipt");
          result.prerequisite_receipt = {
            step_id: expectString2(receipt.step_id, "receipt.step_id", STEP_ID_PATTERN2),
            preflight_id: expectText(receipt.preflight_id, "receipt.preflight_id"),
            result_id: expectText(receipt.result_id, "receipt.result_id"),
            subject_snapshot: validateReviewTarget(receipt.subject_snapshot, "receipt.subject_snapshot")
          };
        }
      }
      return result;
    });
    if (new Set(slots.map((slot) => slot.slot_id)).size !== slots.length) {
      fail3("CLAIM_EVIDENCE_INVALID", `${location}[${index}].slots must have unique slot_id values.`);
    }
    return {
      claim_id: claimId,
      claim_kind: claimKind,
      slots,
      ...record.requirement === undefined ? {} : { requirement: expectText(record.requirement, "claim.requirement") },
      ...record.source_ref === undefined ? {} : { source_ref: expectText(record.source_ref, "claim.source_ref") },
      ...record.boundary === undefined ? {} : { boundary: expectEnum(record.boundary, ["local", "business-flow", "e2e"], "claim.boundary") }
    };
  });
  if (new Set(records.map((record) => record.claim_id)).size !== records.length) {
    fail3("CLAIM_EVIDENCE_INVALID", `${location} must have unique claim_id values.`);
  }
  return records;
}
function claimEvidenceRefs(records) {
  return [...new Set(records.flatMap((record) => record.slots.flatMap((slot) => slot.evidence_refs)))];
}
function isClaimEvidenceSlotComplete(slot) {
  return COMPLETE_CLAIM_EVIDENCE_DISPOSITIONS.includes(slot.disposition) && slot.evidence_refs.length > 0;
}
function hasRemainingCorrectionTargets(current, stepId) {
  const challenges = current.runtimeState.evidence_challenges ?? [];
  return challenges.some((item) => item.status === "invalidated" && item.correction_step_id === stepId) && challenges.some((item) => item.status === "contested" && item.correction_step_id === null);
}
function evaluateClaimEvidence(records, context) {
  const { root: root2, current, due_step_id } = context;
  const incomplete = { validation_complete: false, acceptance_satisfied: false };
  try {
    assertBusinessEvidenceVersion(current);
    const revision = assertEvidencePlan(readDraftDefinitionFromBody(current.body), records);
    if (revision !== current.runtimeState.evidence_plan_revision)
      return incomplete;
  } catch {
    return incomplete;
  }
  const steps = resolveCanonicalTaskStep(current).steps.map((step) => step.id);
  const dueIndex = due_step_id === undefined ? steps.length : steps.indexOf(due_step_id);
  if (dueIndex < 0)
    return incomplete;
  const completion = { validation_complete: true, acceptance_satisfied: true };
  const completingCorrection = due_step_id === current.runtimeState.active_step_id && current.runtimeState.evidence_challenges?.some((challenge) => challenge.status === "invalidated" && challenge.correction_step_id === due_step_id);
  for (const record of records) {
    const dueSlots = record.slots.filter((slot) => steps.indexOf(slot.due_step_id) <= dueIndex);
    for (const slot of dueSlots) {
      if (completingCorrection && slot.due_step_id !== due_step_id && slot.before_step_id !== due_step_id && current.runtimeState.evidence_challenges?.some((challenge) => challenge.status === "contested" && challenge.correction_step_id === null && challenge.claim_id === record.claim_id && challenge.slot_id === slot.slot_id))
        continue;
      try {
        assertEvidenceSlotSatisfied(root2, current, record, slot, due_step_id === undefined);
      } catch {
        completion.validation_complete = false;
        if (record.claim_kind === "acceptance")
          completion.acceptance_satisfied = false;
      }
    }
  }
  return completion;
}
function copyClaimEvidence(records) {
  return structuredClone([...records]);
}
function claimEvidenceStateEnabled(runtimeState) {
  return runtimeState.claim_evidence_required === true || (runtimeState.claim_evidence?.length ?? 0) > 0;
}
function requireClaimEvidencePlan(records, location, missingCode = "CLAIM_EVIDENCE_REQUIRED") {
  if (records === undefined || records.length === 0) {
    fail3(missingCode, `${location} must persist a non-empty claim_evidence plan before the task can proceed.`);
  }
  return [...records];
}
function requireAcceptanceClaim(records, location) {
  if (!records.some((record) => record.claim_kind === "acceptance")) {
    fail3("CLAIM_EVIDENCE_ACCEPTANCE_REQUIRED", `${location} must include at least one acceptance claim; invariant evidence cannot substitute for acceptance evidence.`);
  }
}
function assertClaimEvidencePlanPreserved(planned, proposed, location) {
  if (planned.length === 0) {
    fail3("CLAIM_EVIDENCE_REQUIRED", `${location} cannot initialize a claim_evidence plan during execution; prepare-task must persist it first.`);
  }
  if (planned.length !== proposed.length) {
    fail3("CLAIM_EVIDENCE_PLAN_CONFLICT", `${location} cannot add or remove planned claims during step progress.`);
  }
  const proposedByClaim = new Map(proposed.map((record) => [record.claim_id, record]));
  for (const plannedRecord of planned) {
    const proposedRecord = proposedByClaim.get(plannedRecord.claim_id);
    if (!proposedRecord || proposedRecord.claim_kind !== plannedRecord.claim_kind || proposedRecord.requirement !== plannedRecord.requirement || proposedRecord.source_ref !== plannedRecord.source_ref || proposedRecord.boundary !== plannedRecord.boundary) {
      fail3("CLAIM_EVIDENCE_PLAN_CONFLICT", `${location} must preserve every planned claim identity and kind.`);
    }
    if (proposedRecord.slots.length !== plannedRecord.slots.length) {
      fail3("CLAIM_EVIDENCE_PLAN_CONFLICT", `${location} cannot add or remove evidence slots for claim ${plannedRecord.claim_id}.`);
    }
    const proposedBySlot = new Map(proposedRecord.slots.map((slot) => [slot.slot_id, slot]));
    for (const plannedSlot of plannedRecord.slots) {
      const proposedSlot = proposedBySlot.get(plannedSlot.slot_id);
      if (digest3(proposedSlot?.user_decision ?? null) !== digest3(plannedSlot.user_decision ?? null)) {
        fail3("USER_EVIDENCE_ACTION_REQUIRED", "Ordinary progress cannot create, delete or change a user decision.");
      }
      if (!proposedSlot || digest3(evidenceSlotDefinition(proposedSlot)) !== digest3(evidenceSlotDefinition(plannedSlot))) {
        fail3("CLAIM_EVIDENCE_PLAN_CONFLICT", `${location} must preserve evidence slot ${plannedRecord.claim_id}/${plannedSlot.slot_id} and its minimum type.`);
      }
      if (digest3(proposedSlot.prerequisite_receipt ?? null) !== digest3(plannedSlot.prerequisite_receipt ?? null) || "prerequisite_receipt" in proposedSlot !== "prerequisite_receipt" in plannedSlot || plannedSlot.prerequisite_receipt && digest3(proposedSlot) !== digest3(plannedSlot)) {
        fail3("CLAIM_EVIDENCE_PLAN_CONFLICT", "Runtime prerequisite receipts and consumed evidence are immutable.");
      }
    }
  }
}
function validateStepReviewReceipt(value, location) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, [
    "cycle_id",
    "cycle_phase",
    "change_set_id",
    "review_target_revision",
    "verdict",
    "admitted_fingerprints",
    "evidence_refs"
  ], location);
  const cyclePhase = expectEnum(record.cycle_phase, REVIEW_CYCLE_PHASES, `${location}.cycle_phase`);
  const admittedFingerprints = expectStringArray2(record.admitted_fingerprints, `${location}.admitted_fingerprints`, true, MAX_FINDINGS).map((fingerprint, index) => {
    if (!FINGERPRINT_PATTERN.test(fingerprint))
      fail3("RUNTIME_SCHEMA_INVALID", `${location}.admitted_fingerprints[${index}] has an invalid fingerprint.`);
    return fingerprint;
  });
  if (cyclePhase === "discovery" && admittedFingerprints.length > 0) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.discovery receipts must not carry admitted fingerprints.`);
  }
  return {
    cycle_id: expectString2(record.cycle_id, `${location}.cycle_id`, SAFE_KEY_PATTERN2),
    cycle_phase: cyclePhase,
    change_set_id: expectString2(record.change_set_id, `${location}.change_set_id`, SAFE_KEY_PATTERN2),
    review_target_revision: expectString2(record.review_target_revision, `${location}.review_target_revision`, /^[a-f0-9]{64}$/u),
    verdict: expectEnum(record.verdict, ["clean"], `${location}.verdict`),
    admitted_fingerprints: admittedFingerprints,
    evidence_refs: validateEvidenceRefs(record.evidence_refs, `${location}.evidence_refs`)
  };
}
function validateReviewFindingCandidate(value, location) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, ["fingerprint", "category", "file", "failure_condition", "required_behavior", "root_cause_status", "evidence_refs"], location);
  return {
    fingerprint: expectString2(record.fingerprint, `${location}.fingerprint`, FINGERPRINT_PATTERN),
    category: expectText(record.category, `${location}.category`, 256),
    file: normalizeRepoPath2(expectString2(record.file, `${location}.file`), `${location}.file`),
    failure_condition: expectText(record.failure_condition, `${location}.failure_condition`),
    required_behavior: expectText(record.required_behavior, `${location}.required_behavior`, 512),
    root_cause_status: expectEnum(record.root_cause_status, ["confirmed", "bounded"], `${location}.root_cause_status`),
    evidence_refs: validateEvidenceRefs(record.evidence_refs, `${location}.evidence_refs`)
  };
}
function validateReviewBlocker(value, location) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, ["code", "summary", "next_route"], location);
  return {
    code: expectString2(record.code, `${location}.code`, SAFE_KEY_PATTERN2),
    summary: expectText(record.summary, `${location}.summary`),
    next_route: expectEnum(record.next_route, REVIEW_BLOCKER_ROUTES, `${location}.next_route`)
  };
}
function validatePendingReviewResult(value, location, includeRecordedAt) {
  const record = expectRecord2(value, location);
  const keys = [
    "kind",
    "review_id",
    "execution_id",
    "step_id",
    "cycle_id",
    "cycle_phase",
    "change_set_id",
    "review_target_revision",
    "verdict",
    "findings",
    "unresolved_fingerprints",
    "evidence_refs",
    "blocker",
    ...record.test_assessment === undefined ? [] : ["test_assessment"],
    ...includeRecordedAt ? ["recorded_at"] : []
  ];
  expectExactKeys2(record, keys, location);
  if (record.kind !== "review-result/v1")
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.kind must be review-result/v1.`);
  if (!Array.isArray(record.findings) || record.findings.length > MAX_FINDINGS) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.findings must be a bounded array.`);
  }
  const findings = record.findings.map((item, index) => validateReviewFindingCandidate(item, `${location}.findings[${index}]`));
  if (new Set(findings.map((item) => item.fingerprint)).size !== findings.length) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.findings fingerprints must be unique.`);
  }
  const unresolvedFingerprints = expectStringArray2(record.unresolved_fingerprints, `${location}.unresolved_fingerprints`, true, MAX_FINDINGS).map((fingerprint, index) => expectString2(fingerprint, `${location}.unresolved_fingerprints[${index}]`, FINGERPRINT_PATTERN));
  if (new Set(unresolvedFingerprints).size !== unresolvedFingerprints.length) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.unresolved_fingerprints must be unique.`);
  }
  const verdict = expectEnum(record.verdict, REVIEW_RESULT_VERDICTS, `${location}.verdict`);
  const blocker = record.blocker === null ? null : validateReviewBlocker(record.blocker, `${location}.blocker`);
  if (verdict === "clean" && (findings.length > 0 || unresolvedFingerprints.length > 0 || blocker !== null)) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} clean result must not contain findings or a blocker.`);
  }
  if (verdict === "findings" && (findings.length === 0 && unresolvedFingerprints.length === 0 || blocker !== null)) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} findings result requires a finding and must not contain a blocker.`);
  }
  if (verdict === "blocked" && (findings.length > 0 || unresolvedFingerprints.length > 0 || blocker === null)) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} blocked result requires only a blocker.`);
  }
  const result = {
    ...record.test_assessment === undefined ? {} : { test_assessment: validateTestAssessment(record.test_assessment) },
    kind: "review-result/v1",
    review_id: expectString2(record.review_id, `${location}.review_id`, SAFE_KEY_PATTERN2),
    execution_id: expectString2(record.execution_id, `${location}.execution_id`, SAFE_KEY_PATTERN2),
    step_id: expectString2(record.step_id, `${location}.step_id`, STEP_ID_PATTERN2),
    cycle_id: expectString2(record.cycle_id, `${location}.cycle_id`, SAFE_KEY_PATTERN2),
    cycle_phase: expectEnum(record.cycle_phase, REVIEW_CYCLE_PHASES, `${location}.cycle_phase`),
    change_set_id: expectString2(record.change_set_id, `${location}.change_set_id`, SAFE_KEY_PATTERN2),
    review_target_revision: expectString2(record.review_target_revision, `${location}.review_target_revision`, /^[a-f0-9]{64}$/u),
    verdict,
    findings,
    unresolved_fingerprints: unresolvedFingerprints,
    evidence_refs: validateEvidenceRefs(record.evidence_refs, `${location}.evidence_refs`),
    blocker
  };
  return includeRecordedAt ? { ...result, recorded_at: expectString2(record.recorded_at, `${location}.recorded_at`) } : result;
}
var REPLAN_REPLACEMENT_FIELDS = [
  "background_context",
  "acceptance",
  "allowed_scope",
  "conditional_scope",
  "forbidden_scope",
  "affected_contracts",
  "confirmed_decisions",
  "open_questions",
  "implementation_plan",
  "implementation_steps",
  "regression_checks",
  "rollback_points",
  "design_constraints",
  "post_release_validation",
  "propagation_governance"
];
function expectVerbatim(value, location, maxLength) {
  if (typeof value !== "string" || value.trim().length === 0) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} must be a non-empty string.`);
  }
  if (value.length > maxLength || /\0/u.test(value)) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} must be at most ${maxLength} characters and contain no NUL byte.`);
  }
  return value;
}
function validateTaskBasisSource(value, location) {
  const source = expectRecord2(value, location);
  expectExactKeys2(source, ["source", "verbatim"], location);
  const locator = expectText(source.source, `${location}.source`, 1024);
  if (/[\r\n]/u.test(locator))
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.source must be one line.`);
  return {
    source: locator,
    verbatim: expectVerbatim(source.verbatim, `${location}.verbatim`, MAX_REPLAN_SECTION_CONTENT_LENGTH)
  };
}
function validateTaskBasis(value, location) {
  const basis = expectRecord2(value, location);
  expectExactKeys2(basis, ["original_request", "user_decisions"], location);
  if (!Array.isArray(basis.user_decisions) || basis.user_decisions.length > 64) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.user_decisions must be a bounded array.`);
  }
  const userDecisions = basis.user_decisions.map((item, index) => validateTaskBasisSource(item, `${location}.user_decisions[${index}]`));
  const decisionKeys = userDecisions.map((item) => `${item.source}\x00${item.verbatim}`);
  if (new Set(decisionKeys).size !== decisionKeys.length) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.user_decisions must not contain duplicate source excerpts.`);
  }
  return {
    original_request: validateTaskBasisSource(basis.original_request, `${location}.original_request`),
    user_decisions: userDecisions
  };
}
function normalizeReplacementSectionContent(value, location) {
  const normalized = value.replace(/\r\n?/g, `
`).trim();
  if (/^#{1,2}\s+\S/m.test(normalized)) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} must contain section content, not an arbitrary top-level Markdown heading.`);
  }
  return normalized;
}
function validatePartialDiffDisposition(value, location) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, ["reusable", "rollback_required", "stop_propagation"], location);
  return {
    reusable: expectStringArray2(record.reusable, `${location}.reusable`, true, MAX_EVIDENCE_REFS),
    rollback_required: expectStringArray2(record.rollback_required, `${location}.rollback_required`, true, MAX_EVIDENCE_REFS),
    stop_propagation: expectStringArray2(record.stop_propagation, `${location}.stop_propagation`, true, MAX_EVIDENCE_REFS)
  };
}
function validateReplanReplacementDefinition(value, location) {
  const record = expectRecord2(value, location);
  const hasAuthorityVersion = "mutation_authority_version" in record;
  const hasAuthority = "mutation_authority" in record;
  if (hasAuthorityVersion !== hasAuthority) {
    fail3("MUTATION_AUTHORITY_SCHEMA_INVALID", `${location} must provide mutation_authority_version and mutation_authority together.`);
  }
  expectExactKeys2(record, [...REPLAN_REPLACEMENT_FIELDS, ...hasAuthority ? ["mutation_authority_version", "mutation_authority"] : []], location);
  const result = {};
  for (const field of REPLAN_REPLACEMENT_FIELDS) {
    const raw = record[field];
    if (raw === null && ["design_constraints", "post_release_validation", "propagation_governance"].includes(field)) {
      result[field] = null;
      continue;
    }
    if (raw === null)
      fail3("RUNTIME_SCHEMA_INVALID", `${location}.${field} may be null only for optional sections.`);
    result[field] = normalizeReplacementSectionContent(expectText(raw, `${location}.${field}`, MAX_REPLAN_SECTION_CONTENT_LENGTH), `${location}.${field}`);
  }
  if (hasAuthority) {
    try {
      if (record.mutation_authority_version !== MUTATION_AUTHORITY_VERSION) {
        fail3("MUTATION_AUTHORITY_VERSION_UNSUPPORTED", `${location}.mutation_authority_version must be 2.`);
      }
      result.mutation_authority_version = MUTATION_AUTHORITY_VERSION;
      result.mutation_authority = normalizeTaskMutationAuthority(record.mutation_authority);
    } catch (error) {
      if (error instanceof MutationAuthorityError)
        fail3(error.code, error.message);
      throw error;
    }
  }
  readProjectDocuments(result.background_context);
  return result;
}
var MUTATION_AUTHORITY_SECTION_ALIASES = ["Mutation Authority", "变更权限边界"];
function renderMutationAuthoritySectionContent(authority) {
  return [
    "- mutation_authority_version: 2",
    `- domains: ${authority.domains.length > 0 ? authority.domains.join(", ") : "none"}`,
    `- exact_exceptions: ${authority.exact_exceptions.length > 0 ? authority.exact_exceptions.join(", ") : "none"}`,
    `- forbidden: ${authority.forbidden.length > 0 ? authority.forbidden.join(", ") : "none"}`
  ].join(`
`);
}
function parseMutationAuthoritySection(body) {
  const section = findUniqueMarkdownSection(scanMarkdownSections2(body), MUTATION_AUTHORITY_SECTION_ALIASES, 2);
  if (!section)
    return null;
  const values = new Map;
  for (const line of body.slice(section.contentStart, section.contentEnd).replace(/\r\n?/g, `
`).split(`
`)) {
    const match = /^\s*-\s+([a-z_]+):\s*(.*?)\s*$/u.exec(line);
    if (!match)
      continue;
    if (values.has(match[1]))
      fail3("MUTATION_AUTHORITY_SCHEMA_INVALID", `CURRENT_TASK Mutation Authority section repeats ${match[1]}.`);
    values.set(match[1], match[2]);
  }
  const expectedKeys = ["mutation_authority_version", "domains", "exact_exceptions", "forbidden"];
  const unexpected = [...values.keys()].filter((key) => !expectedKeys.includes(key));
  const missing = expectedKeys.filter((key) => !values.has(key));
  if (missing.length > 0 || unexpected.length > 0) {
    fail3("MUTATION_AUTHORITY_SCHEMA_INVALID", `CURRENT_TASK Mutation Authority section keys mismatch; missing=[${missing.join(", ")}], unexpected=[${unexpected.join(", ")}].`);
  }
  if (values.get("mutation_authority_version") !== "2") {
    fail3("MUTATION_AUTHORITY_VERSION_UNSUPPORTED", "CURRENT_TASK Mutation Authority section must declare mutation_authority_version: 2.");
  }
  const parseList = (key) => {
    const raw = values.get(key);
    if (raw === undefined)
      fail3("MUTATION_AUTHORITY_SCHEMA_INVALID", `CURRENT_TASK Mutation Authority section is missing ${key}.`);
    if (!raw || raw.toLowerCase() === "none")
      return [];
    return raw.split(",").map((item) => item.trim()).filter(Boolean);
  };
  const authority = {
    domains: parseList("domains"),
    exact_exceptions: parseList("exact_exceptions"),
    forbidden: parseList("forbidden")
  };
  try {
    return {
      mutation_authority_version: MUTATION_AUTHORITY_VERSION,
      mutation_authority: normalizeTaskMutationAuthority(authority)
    };
  } catch (error) {
    if (error instanceof MutationAuthorityError)
      fail3(error.code, error.message);
    throw error;
  }
}
function testStrategySection(markdown, aliases, location) {
  const section = findUniqueMarkdownSection(scanMarkdownSections2(markdown), aliases, 3);
  if (!section)
    fail3("TEST_STRATEGY_INVALID", `${location} is missing the required ### ${aliases[0]} section.`);
  return markdown.slice(section.contentStart, section.contentEnd).replace(/\r\n?/gu, `
`).trim();
}
function readTestStrategyDefinition(definition) {
  const location = "draft_definition.regression_checks.Test Strategy";
  const content = testStrategySection(definition.regression_checks, ["Test Strategy", "测试策略"], "draft_definition.regression_checks");
  const fields = new Map;
  for (const [index, rawLine] of content.split(`
`).entries()) {
    const line = rawLine.trim();
    if (!line)
      continue;
    const match = /^-\s+(mode|source|source_ref|task_classification|rationale):\s+(.+)$/u.exec(line);
    if (!match)
      fail3("TEST_STRATEGY_INVALID", `${location}:${index + 1} is not a canonical test-strategy field.`);
    if (fields.has(match[1]))
      fail3("TEST_STRATEGY_INVALID", `${location} contains duplicate field ${match[1]}.`);
    fields.set(match[1], match[2].trim());
  }
  const expected = ["mode", "source", "source_ref", "task_classification", "rationale"];
  const missing = expected.filter((field) => !fields.has(field));
  const extra = [...fields.keys()].filter((field) => !expected.includes(field));
  if (missing.length > 0 || extra.length > 0 || fields.size !== expected.length) {
    fail3("TEST_STRATEGY_INVALID", `${location} fields mismatch; missing=[${missing.join(", ")}], extra=[${extra.join(", ")}].`);
  }
  const mode = fields.get("mode");
  const source = fields.get("source");
  const taskClassification = fields.get("task_classification");
  if (!TEST_STRATEGY_MODES.includes(mode)) {
    fail3("TEST_STRATEGY_INVALID", `${location}.mode must be one of ${TEST_STRATEGY_MODES.join(", ")}.`);
  }
  if (!TEST_STRATEGY_SOURCES.includes(source)) {
    fail3("TEST_STRATEGY_INVALID", `${location}.source must be one of ${TEST_STRATEGY_SOURCES.join(", ")}.`);
  }
  if (!TEST_STRATEGY_CLASSIFICATIONS.includes(taskClassification)) {
    fail3("TEST_STRATEGY_INVALID", `${location}.task_classification must be one of ${TEST_STRATEGY_CLASSIFICATIONS.join(", ")}.`);
  }
  const strategy = {
    mode,
    source,
    source_ref: expectText(fields.get("source_ref"), `${location}.source_ref`, 1024),
    task_classification: taskClassification,
    rationale: expectText(fields.get("rationale"), `${location}.rationale`, 4096)
  };
  const nonExecutable = strategy.task_classification === "non-executable-change";
  if (strategy.mode === "not-applicable" !== nonExecutable) {
    fail3("TEST_STRATEGY_INVALID", "not-applicable is valid only for task_classification=non-executable-change, and that classification requires not-applicable.");
  }
  if (strategy.source === "inferred-default") {
    const inferredMode = nonExecutable ? "not-applicable" : "flexible";
    if (strategy.mode !== inferredMode) {
      fail3("TEST_STRATEGY_INVALID", `inferred-default requires mode=${inferredMode} for task_classification=${strategy.task_classification}.`);
    }
  }
  return strategy;
}
function readPersistentTestPaths(definition) {
  const location = "draft_definition.regression_checks.Persistent Tests";
  const content = testStrategySection(definition.regression_checks, ["Persistent Tests", "持久测试"], "draft_definition.regression_checks");
  const paths = [];
  let none = false;
  for (const [index, rawLine] of content.split(`
`).entries()) {
    if (!rawLine.trim() || /^\s{2,}/u.test(rawLine))
      continue;
    const line = rawLine.trim();
    if (line === "- none") {
      none = true;
      continue;
    }
    const match = /^-\s+`([^`]+)`$/u.exec(line);
    if (!match)
      fail3("TEST_STRATEGY_INVALID", `${location}:${index + 1} is not an exact canonical persistent-test path.`);
    const normalized = normalizeRepoPath2(match[1], `${location}:${index + 1}`);
    if (normalized !== match[1] || normalized.includes("*") || /^[A-Za-z]:[\\/]/u.test(normalized)) {
      fail3("TEST_STRATEGY_INVALID", `${location}:${index + 1} must be one canonical repository-relative exact path.`);
    }
    paths.push(normalized);
  }
  if (none && paths.length > 0)
    fail3("TEST_STRATEGY_INVALID", `${location} cannot combine none with exact paths.`);
  if (!none && paths.length === 0)
    fail3("TEST_STRATEGY_INVALID", `${location} must contain none or at least one exact path.`);
  if (new Set(paths).size !== paths.length)
    fail3("TEST_STRATEGY_INVALID", `${location} contains duplicate paths.`);
  return paths;
}
function definitionAuthorityPatterns(definition) {
  let steps;
  try {
    steps = parseImplementationSteps(definition.implementation_steps);
  } catch (error) {
    if (error instanceof TaskStepDefinitionError)
      fail3(error.code, error.message);
    throw error;
  }
  const plannedTargets = [];
  const commandWriteTargets = [];
  for (const [index, step] of steps.entries()) {
    const rawTargets = step.planned_mutation_targets ?? step.mutation_scope;
    if (!rawTargets)
      fail3("TASK_STEP_METADATA_INCOMPLETE", `implementation_steps[${index}] is missing planned_mutation_targets.`);
    plannedTargets.push(...rawTargets.split(",").map((value, targetIndex) => {
      const cleaned = value.trim().replace(/^`|`$/gu, "");
      try {
        return normalizeAuthorityPath(cleaned, `implementation_steps[${index}].planned_mutation_targets[${targetIndex}]`, true);
      } catch (error) {
        if (error instanceof MutationAuthorityError)
          fail3(error.code, error.message);
        throw error;
      }
    }).filter(Boolean));
    const planText = step.plan_text ?? "";
    for (const match of planText.matchAll(/^\s*-\s+expected_repo_writes:\s*(.*?)\s*$/gmu)) {
      const value = match[1].trim();
      if (!value || value === "none")
        continue;
      commandWriteTargets.push(...value.split(",").map((item, targetIndex) => {
        const cleaned = item.trim().replace(/^`|`$/gu, "");
        try {
          return normalizeAuthorityPath(cleaned, `implementation_steps[${index}].commands.expected_repo_writes[${targetIndex}]`, true);
        } catch (error) {
          if (error instanceof MutationAuthorityError)
            fail3(error.code, error.message);
          throw error;
        }
      }).filter(Boolean));
    }
  }
  return { planned_targets: plannedTargets, command_write_targets: commandWriteTargets };
}
function assertV2DraftDefinitionAuthority(root2, definition) {
  if (definition.mutation_authority_version !== MUTATION_AUTHORITY_VERSION || !definition.mutation_authority)
    return;
  let task;
  let project;
  try {
    task = validateTaskMutationAuthority(root2, definition.mutation_authority);
    project = readProjectMutationAuthority(root2);
  } catch (error) {
    fail3(error instanceof MutationAuthorityError ? error.code : "MUTATION_AUTHORITY_PROJECT_INVALID", error instanceof Error ? error.message : String(error));
  }
  if (!project)
    fail3("MUTATION_AUTHORITY_PROJECT_REQUIRED", "v2 task definitions require the project authority domain map.");
  const patterns = definitionAuthorityPatterns(definition);
  const result = evaluateTaskMutationAuthorityPlan({
    project,
    task,
    planned_targets: patterns.planned_targets,
    command_write_targets: patterns.command_write_targets,
    persistent_test_paths: readPersistentTestPaths(definition)
  });
  if (result.status !== "pass") {
    const decision = result.decisions.find((item) => !item.admitted);
    if (decision)
      fail3(mutationAuthorityPlanBlockerCode(decision), `v2 task definition is outside the project authority envelope: ${result.blockers.join(" ")}`);
    fail3("MUTATION_AUTHORITY_PLANNING_BLOCKED", `v2 task definition failed authority planning proof: ${result.blockers.join(" ")}`);
  }
}
function currentAuthorityDomainRevision(root2, current) {
  if (!current.mutationAuthority)
    return;
  let project;
  try {
    project = readProjectMutationAuthority(root2);
  } catch (error) {
    fail3(error instanceof MutationAuthorityError ? error.code : "MUTATION_AUTHORITY_PROJECT_INVALID", error instanceof Error ? error.message : String(error));
  }
  if (!project)
    fail3("MUTATION_AUTHORITY_PROJECT_REQUIRED", "v2 task state requires PROJECT_PROFILE.yaml.mutation_authority.domains.");
  return projectMutationAuthorityRevision(project);
}
function assertV2AuthorityDomainRevisionFreshForTransition(root2, current, transition) {
  if (!current.mutationAuthority)
    return;
  const boundRevision = current.runtimeState.authority_domain_revision;
  const projectRevision = currentAuthorityDomainRevision(root2, current);
  if (!boundRevision) {
    fail3("MUTATION_AUTHORITY_DOMAIN_REVISION_REQUIRED", `${transition} cannot proceed without the task-bound project authority domain-map revision.`);
  }
  if (boundRevision !== projectRevision) {
    fail3("MUTATION_AUTHORITY_DOMAIN_REVISION_STALE", `${transition} cannot rebind a stale task authority revision to the current project map; explicit task authority-domain revalidation is required.`);
  }
}
function strategyStepScopes(definition) {
  let steps;
  try {
    steps = parseImplementationSteps(definition.implementation_steps);
  } catch (error) {
    if (error instanceof TaskStepDefinitionError)
      fail3(error.code, error.message);
    throw error;
  }
  return steps.map((step, index) => {
    if (!step.mutation_scope)
      fail3("TEST_STRATEGY_INVALID", `implementation_steps[${index}] is missing mutation_scope.`);
    const values = step.mutation_scope.split(",").map((value) => value.trim().replace(/^`|`$/gu, "")).filter(Boolean);
    if (values.length === 0 || new Set(values).size !== values.length) {
      fail3("TEST_STRATEGY_INVALID", `implementation_steps[${index}].mutation_scope must contain unique paths.`);
    }
    return values;
  });
}
function definitionMutationScope(definition) {
  const body = [
    "## 允许修改范围",
    "",
    "### Allowed Files",
    "",
    definition.allowed_scope,
    "",
    "### Conditional Files",
    "",
    definition.conditional_scope,
    "",
    "## 禁止修改范围",
    "",
    "### Forbidden Files",
    "",
    definition.forbidden_scope,
    "",
    "## 回归检查项",
    "",
    definition.regression_checks,
    ""
  ].join(`
`);
  try {
    return parseMutationScope(body);
  } catch (error) {
    if (error instanceof MutationScopeError)
      fail3("TEST_STRATEGY_INVALID", error.message);
    throw error;
  }
}
function nonExecutableChangePatterns(root2) {
  let profile;
  try {
    profile = loadProfile(getWorkflowProfilePath(root2));
  } catch (error) {
    fail3("TEST_STRATEGY_NON_EXECUTABLE_UNPROVEN", `not-applicable requires a readable PROJECT_PROFILE.yaml non-executable boundary: ${error instanceof Error ? error.message : String(error)}`);
  }
  const boundaries = profile.boundaries;
  if (!isRecord4(boundaries) || !Array.isArray(boundaries.non_executable_change_paths)) {
    fail3("TEST_STRATEGY_NON_EXECUTABLE_UNPROVEN", "not-applicable requires PROJECT_PROFILE.yaml boundaries.non_executable_change_paths.");
  }
  if (boundaries.non_executable_change_paths.length === 0) {
    fail3("TEST_STRATEGY_NON_EXECUTABLE_UNPROVEN", "not-applicable requires at least one project-owned non-executable path classification.");
  }
  const patterns = boundaries.non_executable_change_paths.map((value, index) => {
    if (typeof value !== "string" || !value.trim()) {
      fail3("TEST_STRATEGY_NON_EXECUTABLE_UNPROVEN", `boundaries.non_executable_change_paths[${index}] must be a non-empty string.`);
    }
    const normalized = value.trim().replace(/\\/gu, "/").replace(/^\.\//u, "").replace(/\/+/gu, "/");
    let bounded;
    try {
      bounded = nonExecutableChangePatternIsBounded(normalized);
    } catch (error) {
      fail3("TEST_STRATEGY_NON_EXECUTABLE_UNPROVEN", `invalid non-executable path classification ${normalized}: ${error instanceof Error ? error.message : String(error)}`);
    }
    if (!bounded) {
      fail3("TEST_STRATEGY_NON_EXECUTABLE_UNPROVEN", `non-executable path classification must be an exact path or a literal directory prefix ending in /**: ${normalized}.`);
    }
    return normalized;
  });
  if (new Set(patterns).size !== patterns.length) {
    fail3("TEST_STRATEGY_NON_EXECUTABLE_UNPROVEN", "boundaries.non_executable_change_paths contains duplicate patterns.");
  }
  return patterns;
}
function assertNonExecutableChangeScope(root2, definition, stepScopes) {
  const policyPatterns = nonExecutableChangePatterns(root2);
  const taskScope = definitionMutationScope(definition);
  const declaredTargets = [...new Set([
    ...taskScope.allowed.map((entry) => entry.pattern),
    ...taskScope.conditional.map((entry) => entry.pattern),
    ...stepScopes.flat(),
    ...definition.mutation_authority_version === MUTATION_AUTHORITY_VERSION ? definitionAuthorityPatterns(definition).command_write_targets : []
  ])];
  const uncovered = declaredTargets.filter((target) => !policyPatterns.some((boundary) => {
    try {
      return mutationScopePatternIsSubset(target, boundary);
    } catch {
      return false;
    }
  }));
  if (uncovered.length > 0) {
    fail3("TEST_STRATEGY_NON_EXECUTABLE_SCOPE_VIOLATION", `not-applicable contains mutation targets outside PROJECT_PROFILE.yaml boundaries.non_executable_change_paths: ${uncovered.join(", ")}.`);
  }
}
function assertTestStrategySource(root2, strategy, taskBasis) {
  if (strategy.source === "inferred-default") {
    if (strategy.source_ref !== "prepare-task-default") {
      fail3("TEST_STRATEGY_INVALID", "inferred-default requires source_ref=prepare-task-default.");
    }
    return;
  }
  if (strategy.source === "explicit-user") {
    if (!taskBasisHasSource(taskBasis, strategy.source_ref)) {
      fail3("TEST_STRATEGY_INVALID", "explicit-user source_ref must equal an exact Task Basis source coordinate.");
    }
    return;
  }
  assertRepositoryAuthoritySource(root2, strategy.source_ref);
}
function assertRepositoryAuthoritySource(root2, sourceRef) {
  const [file, locator, ...extra] = sourceRef.split("#");
  if (extra.length || locator === "")
    fail3("CLAIM_EVIDENCE_AUTHORITY_INVALID", "source reference must name a file and optionally one existing heading.");
  const normalized = normalizeRepoPath2(file, "authority.source_ref");
  if (normalized !== file || normalized.includes("*") || /^[A-Za-z]:[\\/]/u.test(normalized)) {
    fail3("TEST_STRATEGY_INVALID", "authority source must be a canonical repository-relative file.");
  }
  const resolvedRoot = path12.resolve(root2);
  const resolved = path12.resolve(resolvedRoot, ...normalized.split("/"));
  const relative = path12.relative(resolvedRoot, resolved);
  if (!relative || relative.startsWith(`..${path12.sep}`) || path12.isAbsolute(relative) || !fs11.existsSync(resolved) || !fs11.statSync(resolved).isFile()) {
    fail3("TEST_STRATEGY_INVALID", `authority source does not identify an existing project file: ${sourceRef}.`);
  }
  const realRelative = path12.relative(fs11.realpathSync(resolvedRoot), fs11.realpathSync(resolved));
  if (!realRelative || realRelative.startsWith(`..${path12.sep}`) || path12.isAbsolute(realRelative)) {
    fail3("TEST_STRATEGY_INVALID", "authority source cannot escape the repository through a symlink.");
  }
  if (locator !== undefined) {
    const headings = [...fs11.readFileSync(resolved, "utf8").matchAll(/^#{1,6}\s+(.+?)\s*#*\s*$/gmu)].map((match) => match[1]);
    const matches = headings.filter((heading) => heading === locator || heading.startsWith(`${locator}:`) || heading.toLowerCase().replace(/[^\p{L}\p{N}_\s-]/gu, "").replace(/\s/gu, "-") === locator);
    if (matches.length !== 1)
      fail3("CLAIM_EVIDENCE_AUTHORITY_INVALID", "source heading/record identity must exist and be unambiguous.");
  }
}
function assertPreparedTestStrategy(root2, definition, taskBasis) {
  const strategy = readTestStrategyDefinition(definition);
  const persistentTests = readPersistentTestPaths(definition);
  const scopes = strategyStepScopes(definition);
  assertTestStrategySource(root2, strategy, taskBasis);
  if (strategy.mode === "not-applicable") {
    if (persistentTests.length > 0)
      fail3("TEST_STRATEGY_INVALID", "not-applicable requires Persistent Tests to be none.");
    assertNonExecutableChangeScope(root2, definition, scopes);
  }
  return strategy;
}
function assertBusinessEvidenceVersion(current) {
  if (current.runtimeState.business_evidence_version !== 1) {
    fail3("TASK_SEMANTICS_UPGRADE_REQUIRED", "This task predates business_evidence_version=1; use its previous installation or an explicitly authorized replan.");
  }
}
function executionPhaseForStrategy(strategy) {
  return strategy.mode;
}
function executionPhaseForCurrentStep(current, strategy = resolveTestStrategyExecutionContext(current)) {
  if (strategy.mode !== "test-first")
    return strategy.phase;
  const resolution = resolveCanonicalTaskStep(current);
  const currentStepId = resolution.current.id;
  const currentStepIndex = resolution.index;
  const hasPendingReproduction = (current.runtimeState.claim_evidence ?? []).some((record) => record.slots.some((slot) => {
    if (slot.due_step_id !== currentStepId || slot.applicability !== "before-step" || slot.prerequisite_receipt || slot.check?.expected_result !== "expected-failure" || !slot.before_step_id)
      return false;
    const beforeStepIndex = resolution.steps.findIndex((step) => step.id === slot.before_step_id);
    return beforeStepIndex > currentStepIndex;
  }));
  return hasPendingReproduction ? "red" : strategy.phase;
}
function resolveTestStrategyExecutionContext(current) {
  assertBusinessEvidenceVersion(current);
  const resolution = resolveCanonicalTaskStep(current);
  const strategySection = findUniqueMarkdownSection(scanMarkdownSections2(current.body), ["Test Strategy", "测试策略"], 3);
  if (!strategySection)
    fail3("TEST_STRATEGY_INVALID", "Version 1 tasks require a frozen Test Strategy.");
  const definition = { regression_checks: current.body };
  const strategy = readTestStrategyDefinition(definition);
  const phase = executionPhaseForStrategy(strategy);
  return {
    mode: strategy.mode,
    phase,
    required_outcome: "implemented",
    persistent_tests: readPersistentTestPaths(definition),
    step_index: resolution.index,
    first_step_id: resolution.steps[0].id
  };
}
function assertTestStrategySequenceReady(current, context = resolveTestStrategyExecutionContext(current)) {
  assertBusinessEvidenceVersion(current);
  assertEvidencePlan(readDraftDefinitionFromBody(current.body), current.runtimeState.claim_evidence ?? []);
  if (current.runtimeState.evidence_plan_revision !== evidencePlanRevision(readDraftDefinitionFromBody(current.body), current.runtimeState.claim_evidence ?? []))
    fail3("CLAIM_EVIDENCE_STALE", "canonical evidence plan revision does not match its definitions.");
}
function assertTestStrategyExecutionTransition(current, delta) {
  assertTestStrategySequenceReady(current);
  if (delta.execution_result?.outcome === "test-red") {
    fail3("TEST_STRATEGY_SEQUENCE_INVALID", "test-red requires admitted reproduction evidence (S2) and cannot complete positive acceptance.");
  }
}
function validateDraftTaskIdentityFields(record, location, requireTitle = true) {
  const taskId = expectString2(record.task_id, `${location}.task_id`);
  const taskSlug = expectString2(record.task_slug, `${location}.task_slug`);
  try {
    validateTaskId(taskId);
    validateTaskSlug(taskSlug);
  } catch (error) {
    fail3("RUNTIME_SCHEMA_INVALID", error instanceof Error ? error.message : String(error));
  }
  const documentId = expectString2(record.document_id, `${location}.document_id`);
  if (!DOCUMENT_ID_PATTERN.test(documentId))
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.document_id is invalid.`);
  if (!requireTitle)
    return { task_id: taskId, task_slug: taskSlug, document_id: documentId };
  const taskTitle = expectText(record.task_title, `${location}.task_title`, 512);
  if (/[\r\n]/u.test(taskTitle))
    fail3("RUNTIME_IDENTITY_INVALID", `${location}.task_title must be a single line.`);
  if (/^\{\{[^{}]+\}\}$/.test(taskTitle))
    fail3("RUNTIME_IDENTITY_INVALID", `${location}.task_title must be concrete, not a placeholder.`);
  return { task_id: taskId, task_slug: taskSlug, document_id: documentId, task_title: taskTitle };
}
function replacementStepIds(implementationSteps) {
  try {
    return parseImplementationSteps(implementationSteps).map((step) => step.id);
  } catch (error) {
    if (error instanceof TaskStepDefinitionError)
      fail3("RUNTIME_SECTION_INVALID", error.message);
    throw error;
  }
}
function assertReplacementActiveStep(activeStepId, implementationSteps) {
  const stepIds = replacementStepIds(implementationSteps);
  if (stepIds.length === 0) {
    fail3("RUNTIME_SECTION_INVALID", "replacement implementation_steps must contain at least one labelled step ID.");
  }
  if (new Set(stepIds).size !== stepIds.length) {
    fail3("RUNTIME_SECTION_INVALID", "replacement implementation_steps contains duplicate step IDs.");
  }
  if (!stepIds.includes(activeStepId)) {
    fail3("RUNTIME_SECTION_INVALID", `active_step_id ${activeStepId} does not identify a step in replacement implementation_steps.`);
  }
}
function assertStrictDraftImplementationSteps(activeStepId, implementationSteps) {
  let steps;
  try {
    steps = parseImplementationSteps(implementationSteps);
  } catch (error) {
    if (error instanceof TaskStepDefinitionError)
      fail3(error.code, error.message);
    throw error;
  }
  if (steps.length === 0) {
    fail3("TASK_STEPS_INVALID", "draft implementation_steps must contain at least one step.");
  }
  for (const step of steps) {
    if (!step.metadata_complete) {
      fail3("TASK_STEPS_INVALID", `step ${step.id} has incomplete step metadata; every step in a draft must declare purpose, mutation scope, required evidence, and review checkpoint (with boundary when required).`);
    }
  }
  const firstStep = steps[0];
  if (activeStepId !== firstStep.id) {
    fail3("TASK_STEPS_INVALID", `draft active_step_id must be the first admitted implementation step ${firstStep.id}, got ${activeStepId}.`);
  }
  return steps;
}
function resolveTaskStepForState(body, activeStepId) {
  try {
    const resolution = resolveTaskStep(body, activeStepId);
    if (resolution.steps.length > 1 && resolution.steps.some((step) => !step.metadata_complete)) {
      fail3("TASK_STEPS_INVALID", "every multi-step task step must declare purpose, mutation scope, required evidence, and review checkpoint metadata.");
    }
    return resolution;
  } catch (error) {
    if (error instanceof TaskStepDefinitionError)
      fail3(error.code, error.message);
    throw error;
  }
}
function resolveCanonicalTaskStep(current) {
  return resolveTaskStepForState(current.body, current.runtimeState.active_step_id);
}
function effectiveCheckpointPolicy(resolution) {
  if (resolution.steps.length === 1 && !resolution.current.metadata_complete)
    return "not-required";
  if (!resolution.current.metadata_complete || resolution.current.review_checkpoint === null) {
    fail3("TASK_STEPS_INVALID", `step ${resolution.current.id} has incomplete checkpoint metadata.`);
  }
  return resolution.current.review_checkpoint;
}
function assertReviewExecutionEligible(current, execution) {
  if (execution.step_id !== current.runtimeState.active_step_id) {
    fail3("REVIEW_EXECUTION_STALE", "review-change requires an execution for the active step.");
  }
  if (!execution.execution_result) {
    fail3("REVIEW_TARGET_REQUIRED", "review-change requires a structured Runtime-recorded execution result.");
  }
  if (execution.execution_result.outcome === "blocked") {
    fail3("REVIEW_EXECUTION_NOT_IMPLEMENTED", "a blocked execution is not reviewable.");
  }
  if (execution.execution_result.outcome === "test-red") {
    const context = resolveTestStrategyExecutionContext(current);
    const activePreflight = current.runtimeState.execution_preflight;
    const phase = activePreflight?.step_id === execution.step_id && activePreflight.execution_id === execution.execution_result.execution_id ? activePreflight.execution_phase : context.phase;
    if (phase !== "red") {
      fail3("TEST_STRATEGY_SEQUENCE_INVALID", "a test-red execution is reviewable only on the first test-first step.");
    }
  }
  const executionIndex = current.runtimeState.execution_log.findIndex((item) => !("action" in item) && item.idempotency_key === execution.idempotency_key);
  const laterCompletion = executionIndex >= 0 && current.runtimeState.execution_log.slice(executionIndex + 1).some((item) => !("action" in item) && item.step_id === execution.step_id && item.review_receipt !== undefined);
  if (laterCompletion) {
    fail3("REVIEW_EXECUTION_ALREADY_COMPLETED", "the recorded execution has already passed review and completed its step.");
  }
  const checkpoint = effectiveCheckpointPolicy(resolveCanonicalTaskStep(current));
  if (execution.mode === "default") {
    const dynamicReview = dynamicReviewRequiredForCurrentExecution(current);
    if (checkpoint !== "required" && !dynamicReview) {
      fail3("REVIEW_CHECKPOINT_NOT_REQUIRED", "the current step does not admit a review checkpoint.");
    }
    if (execution.status !== "in-progress" || execution.advancement !== "not-applicable" || current.runtimeState.active_step_status !== "in-progress") {
      fail3("REVIEW_EXECUTION_NOT_REVIEWABLE", "the default execution is not awaiting its required review checkpoint.");
    }
    return;
  }
  if (execution.status !== "completed" || execution.advancement !== "repair-awaiting-verification" || current.runtimeState.active_step_status !== "completed") {
    fail3("REVIEW_EXECUTION_NOT_REVIEWABLE", "the repair execution is not awaiting verification review.");
  }
}
function validateTaskStateDelta(value) {
  const record = expectRecord2(value, "semantic_delta");
  const kind = expectEnum(record.kind, ["task-state"], "semantic_delta.kind");
  const action = expectEnum(record.action, ["retry-step", "record-step-preflight", "extend-preflight", "step-progress", "consume-retained-review", "clear-resume-review-gate", "record-evidence-challenge", "dismiss-evidence-challenge", "record-user-evidence", ...DRAFT_TASK_STATE_ACTIONS, ...CLAIM_EVIDENCE_MIGRATION_ACTIONS, ...REVIEW_TASK_STATE_ACTIONS, ...REPLAN_TASK_STATE_ACTIONS, "commit-scope-amendment"], "semantic_delta.action");
  if (action === "record-user-evidence") {
    expectExactKeys2(record, ["kind", "action", "decision_kind", "claim_id", "slot_id", "check_id", "evidence_plan_revision", "subject_revision", "decision_source", "decision_text", "evidence_refs", ...record.validation_items === undefined ? [] : ["validation_items"]], "record-user-evidence");
    return {
      kind,
      action,
      decision_kind: expectEnum(record.decision_kind, ["human-acceptance", "waiver"], "decision_kind"),
      claim_id: expectString2(record.claim_id, "claim_id", CLAIM_ID_PATTERN),
      slot_id: expectString2(record.slot_id, "slot_id", CLAIM_EVIDENCE_SLOT_ID_PATTERN),
      check_id: expectString2(record.check_id, "check_id", CLAIM_ID_PATTERN),
      evidence_plan_revision: expectString2(record.evidence_plan_revision, "evidence_plan_revision", SHA256_PATTERN2),
      subject_revision: expectString2(record.subject_revision, "subject_revision", SHA256_PATTERN2),
      decision_source: expectText(record.decision_source, "decision_source"),
      decision_text: expectVerbatim(record.decision_text, "decision_text", 32768),
      ...record.validation_items === undefined ? {} : { validation_items: expectStringArray2(record.validation_items, "validation_items", true, MAX_EXECUTION_RESULT_ITEMS) },
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "evidence_refs")
    };
  }
  if (action === "consume-retained-review") {
    expectExactKeys2(record, ["kind", "action", "step_id", "review_receipt", "evidence_refs"], "consume-retained-review");
    return {
      kind,
      action,
      step_id: expectString2(record.step_id, "step_id", STEP_ID_PATTERN2),
      review_receipt: validateStepReviewReceipt(record.review_receipt, "semantic_delta.review_receipt"),
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
    };
  }
  if (action === "retry-step") {
    expectExactKeys2(record, ["kind", "action", "step_id", "blocked_attempt_id", "blocker_resolution_refs", ...record.repair_diagnosis === undefined ? [] : ["repair_diagnosis"], "evidence_refs"], "retry-step");
    return { kind, action, step_id: expectString2(record.step_id, "step_id", STEP_ID_PATTERN2), blocked_attempt_id: expectString2(record.blocked_attempt_id, "blocked_attempt_id", SAFE_KEY_PATTERN2), blocker_resolution_refs: validateEvidenceRefs(record.blocker_resolution_refs, "blocker_resolution_refs"), ...record.repair_diagnosis === undefined ? {} : { repair_diagnosis: validateStepRepairDiagnosis(record.repair_diagnosis) }, evidence_refs: validateEvidenceRefs(record.evidence_refs, "evidence_refs") };
  }
  if (action === "record-step-preflight") {
    expectExactKeys2(record, [
      "kind",
      "action",
      "step_id",
      "candidate_paths",
      "evidence_refs",
      ...record.blast_radius_assessments === undefined ? [] : ["blast_radius_assessments"],
      ...record.mode === undefined ? [] : ["mode"],
      ...record.execution_id === undefined ? [] : ["execution_id"],
      ...record.plan_revision === undefined ? [] : ["plan_revision"],
      ...record.execution_phase === undefined ? [] : ["execution_phase"],
      ...record.repair_fingerprints === undefined ? [] : ["repair_fingerprints"],
      ...record.repair_wave_id === undefined ? [] : ["repair_wave_id"],
      ...record.review_id === undefined ? [] : ["review_id"],
      ...record.review_target_paths === undefined ? [] : ["review_target_paths"],
      ...record.change_set_id === undefined ? [] : ["change_set_id"]
    ], "semantic_delta");
    let assessments;
    if (record.blast_radius_assessments !== undefined) {
      try {
        assessments = normalizeBlastRadiusAssessments(record.blast_radius_assessments);
      } catch (error) {
        fail3(error instanceof MutationAuthorityError ? error.code : "MUTATION_AUTHORITY_ASSESSMENT_INVALID", error instanceof Error ? error.message : String(error));
      }
    }
    const mode = record.mode === undefined ? undefined : expectEnum(record.mode, VNEXT_EXECUTE_STEP_MODES, "mode");
    const executionId = record.execution_id === undefined ? undefined : expectString2(record.execution_id, "execution_id", SAFE_KEY_PATTERN2);
    const planRevision = record.plan_revision === undefined ? undefined : expectString2(record.plan_revision, "plan_revision", SHA256_PATTERN2);
    const executionPhase = record.execution_phase === undefined ? undefined : expectEnum(record.execution_phase, ["flexible", "test-first", "red", "green", "implementation-first", "not-applicable", "legacy"], "execution_phase");
    const repairFingerprints = record.repair_fingerprints === undefined ? undefined : expectStringArray2(record.repair_fingerprints, "repair_fingerprints", false, MAX_FINDINGS).map((item, index) => expectString2(item, `repair_fingerprints[${index}]`, FINGERPRINT_PATTERN));
    if (repairFingerprints && new Set(repairFingerprints).size !== repairFingerprints.length)
      fail3("RUNTIME_SCHEMA_INVALID", "repair_fingerprints must be unique.");
    const repairWaveId = record.repair_wave_id === undefined ? undefined : expectString2(record.repair_wave_id, "repair_wave_id", SAFE_KEY_PATTERN2);
    const reviewId = record.review_id === undefined ? undefined : expectString2(record.review_id, "review_id", SAFE_KEY_PATTERN2);
    const reviewTargetPaths = record.review_target_paths === undefined ? undefined : expectStringArray2(record.review_target_paths, "review_target_paths", true, 256).map((item) => normalizeRepoPath2(item, "review_target_paths"));
    const changeSetId = record.change_set_id === undefined ? undefined : expectString2(record.change_set_id, "change_set_id", SAFE_KEY_PATTERN2);
    return {
      kind,
      action,
      step_id: expectString2(record.step_id, "step_id", STEP_ID_PATTERN2),
      candidate_paths: expectStringArray2(record.candidate_paths, "candidate_paths", true, 256).map((p) => normalizeRepoPath2(p, "candidate_paths")),
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "evidence_refs"),
      ...assessments === undefined ? {} : { blast_radius_assessments: assessments },
      ...mode === undefined ? {} : { mode },
      ...executionId === undefined ? {} : { execution_id: executionId },
      ...planRevision === undefined ? {} : { plan_revision: planRevision },
      ...executionPhase === undefined ? {} : { execution_phase: executionPhase },
      ...repairFingerprints === undefined ? {} : { repair_fingerprints: [...new Set(repairFingerprints)] },
      ...repairWaveId === undefined ? {} : { repair_wave_id: repairWaveId },
      ...reviewId === undefined ? {} : { review_id: reviewId },
      ...reviewTargetPaths === undefined ? {} : { review_target_paths: reviewTargetPaths },
      ...changeSetId === undefined ? {} : { change_set_id: changeSetId }
    };
  }
  if (action === "extend-preflight") {
    expectExactKeys2(record, [
      "kind",
      "action",
      "step_id",
      "current_preflight_id",
      "additional_targets",
      "blast_radius_assessments",
      "evidence_refs",
      ...record.mode === undefined ? [] : ["mode"],
      ...record.execution_id === undefined ? [] : ["execution_id"],
      ...record.execution_phase === undefined ? [] : ["execution_phase"]
    ], "semantic_delta");
    let assessments;
    try {
      assessments = normalizeBlastRadiusAssessments(record.blast_radius_assessments);
    } catch (error) {
      fail3(error instanceof MutationAuthorityError ? error.code : "MUTATION_AUTHORITY_ASSESSMENT_INVALID", error instanceof Error ? error.message : String(error));
    }
    const additionalTargets = expectStringArray2(record.additional_targets, "additional_targets", false, 256).map((p) => normalizeRepoPath2(p, "additional_targets"));
    const mode = record.mode === undefined ? undefined : expectEnum(record.mode, VNEXT_EXECUTE_STEP_MODES, "mode");
    const executionId = record.execution_id === undefined ? undefined : expectString2(record.execution_id, "execution_id", SAFE_KEY_PATTERN2);
    const executionPhase = record.execution_phase === undefined ? undefined : expectEnum(record.execution_phase, ["flexible", "test-first", "red", "green", "implementation-first", "not-applicable", "legacy"], "execution_phase");
    return {
      kind,
      action,
      step_id: expectString2(record.step_id, "step_id", STEP_ID_PATTERN2),
      current_preflight_id: expectString2(record.current_preflight_id, "current_preflight_id", SAFE_KEY_PATTERN2),
      additional_targets: additionalTargets,
      blast_radius_assessments: assessments,
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "evidence_refs"),
      ...mode === undefined ? {} : { mode },
      ...executionId === undefined ? {} : { execution_id: executionId },
      ...executionPhase === undefined ? {} : { execution_phase: executionPhase }
    };
  }
  if (action === "create-draft" || action === "update-draft") {
    const allowedKeys = ["kind", "action", "task_id", "task_slug", "document_id", "task_title", "task_basis", "draft_definition", "active_step_id", "evidence_refs", "claim_evidence", "predecessor"];
    if (Object.keys(record).some((key) => !allowedKeys.includes(key)))
      fail3("RUNTIME_SCHEMA_INVALID", "draft task-state semantic_delta contains unsupported fields.");
    const identity = validateDraftTaskIdentityFields(record, "semantic_delta", true);
    const result = {
      kind,
      action,
      ...identity,
      task_basis: validateTaskBasis(record.task_basis, "semantic_delta.task_basis"),
      draft_definition: validateReplanReplacementDefinition(record.draft_definition, "semantic_delta.draft_definition"),
      active_step_id: expectString2(record.active_step_id, "semantic_delta.active_step_id", STEP_ID_PATTERN2),
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
    };
    if (record.predecessor !== undefined) {
      if (action !== "create-draft")
        fail3("SUCCESSOR_STATE_INVALID", "A predecessor is bound only when creating a new identity.");
      result.predecessor = validateSuccessorDecision(record.predecessor);
    }
    if (record.claim_evidence !== undefined)
      result.claim_evidence = validateClaimEvidence(record.claim_evidence, "semantic_delta.claim_evidence");
    return result;
  }
  if (action === "confirm-draft") {
    expectExactKeys2(record, ["kind", "action", "task_id", "task_slug", "document_id", "draft_revision", "evidence_refs"], "semantic_delta");
    const identity = validateDraftTaskIdentityFields(record, "semantic_delta", false);
    const draftRevision = expectString2(record.draft_revision, "semantic_delta.draft_revision");
    if (!/^[a-f0-9]{64}$/.test(draftRevision))
      fail3("RUNTIME_SCHEMA_INVALID", "semantic_delta.draft_revision must be SHA-256.");
    return {
      kind,
      action,
      ...identity,
      draft_revision: draftRevision,
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
    };
  }
  if (action === "migrate-claim-evidence") {
    expectExactKeys2(record, ["kind", "action", "claim_evidence", "evidence_refs"], "semantic_delta");
    return {
      kind,
      action,
      claim_evidence: validateClaimEvidence(record.claim_evidence, "semantic_delta.claim_evidence"),
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
    };
  }
  if (action === "clear-resume-review-gate") {
    expectExactKeys2(record, ["kind", "action", "evidence_refs"], "semantic_delta");
    return {
      kind,
      action: "clear-resume-review-gate",
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
    };
  }
  if (action === "record-evidence-challenge") {
    expectExactKeys2(record, ["kind", "action", "claim_id", "slot_id", "result_id", "evidence_ref", "evidence_sha256", "reason", "evidence_refs"], "semantic_delta");
    return {
      kind,
      action,
      claim_id: expectString2(record.claim_id, "claim_id", CLAIM_ID_PATTERN),
      slot_id: expectString2(record.slot_id, "slot_id", CLAIM_EVIDENCE_SLOT_ID_PATTERN),
      result_id: expectString2(record.result_id, "result_id", CLAIM_ID_PATTERN),
      evidence_ref: normalizeRepoPath2(expectString2(record.evidence_ref, "evidence_ref"), "evidence_ref"),
      evidence_sha256: expectString2(record.evidence_sha256, "evidence_sha256", /^[a-f0-9]{64}$/),
      reason: expectString2(record.reason, "reason"),
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
    };
  }
  if (action === "dismiss-evidence-challenge") {
    expectExactKeys2(record, ["kind", "action", "challenge_id", "evidence_ref", "evidence_sha256", "reason", "evidence_refs"], "semantic_delta");
    return {
      kind,
      action,
      challenge_id: expectString2(record.challenge_id, "challenge_id", SAFE_KEY_PATTERN2),
      evidence_ref: normalizeRepoPath2(expectString2(record.evidence_ref, "evidence_ref"), "evidence_ref"),
      evidence_sha256: expectString2(record.evidence_sha256, "evidence_sha256", /^[a-f0-9]{64}$/),
      reason: expectString2(record.reason, "reason"),
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
    };
  }
  if (action === "record-review-result") {
    expectExactKeys2(record, ["kind", "action", "review_result", "evidence_refs"], "semantic_delta");
    return {
      kind,
      action,
      review_result: validatePendingReviewResult(record.review_result, "semantic_delta.review_result", false),
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
    };
  }
  if (action === "mark-replan-blocked" || action === "clear-replan-block") {
    expectExactKeys2(record, ["kind", "action", "evidence_refs"], "semantic_delta");
    return {
      kind,
      action,
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
    };
  }
  if (action === "commit-replan" || action === "commit-scope-amendment") {
    const allowedKeys = ["kind", "action", "task_basis", "replacement_definition", "active_step_id", "evidence_refs", "claim_evidence"];
    if (Object.keys(record).some((key) => !allowedKeys.includes(key)))
      fail3("RUNTIME_SCHEMA_INVALID", "replan task-state semantic_delta contains unsupported fields.");
    const result = {
      kind,
      action,
      task_basis: validateTaskBasis(record.task_basis, "semantic_delta.task_basis"),
      replacement_definition: validateReplanReplacementDefinition(record.replacement_definition, "semantic_delta.replacement_definition"),
      active_step_id: expectString2(record.active_step_id, "semantic_delta.active_step_id", STEP_ID_PATTERN2),
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
    };
    if (record.claim_evidence !== undefined)
      result.claim_evidence = validateClaimEvidence(record.claim_evidence, "semantic_delta.claim_evidence");
    return result;
  }
  const keys = Object.keys(record);
  if (keys.some((key) => !["kind", "action", "step_id", "status", "evidence_refs", "note", "repair_fingerprint", "repair_fingerprints", "repair_wave_id", "change_set_id", "review_receipt", "claim_evidence", "execution_result"].includes(key))) {
    fail3("RUNTIME_SCHEMA_INVALID", "task-state semantic_delta contains unsupported fields.");
  }
  const result = {
    kind,
    action: "step-progress",
    step_id: expectString2(record.step_id, "semantic_delta.step_id", STEP_ID_PATTERN2),
    status: expectEnum(record.status, STEP_STATUSES, "semantic_delta.status"),
    evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
  };
  if (record.note !== undefined)
    result.note = expectText(record.note, "semantic_delta.note");
  if (record.repair_fingerprint !== undefined)
    result.repair_fingerprint = expectString2(record.repair_fingerprint, "semantic_delta.repair_fingerprint", FINGERPRINT_PATTERN);
  if (record.repair_fingerprints !== undefined) {
    result.repair_fingerprints = expectStringArray2(record.repair_fingerprints, "semantic_delta.repair_fingerprints", false, MAX_FINDINGS).map((fingerprint, index) => expectString2(fingerprint, `semantic_delta.repair_fingerprints[${index}]`, FINGERPRINT_PATTERN));
    if (new Set(result.repair_fingerprints).size !== result.repair_fingerprints.length) {
      fail3("RUNTIME_SCHEMA_INVALID", "semantic_delta.repair_fingerprints must be unique.");
    }
  }
  if (record.repair_wave_id !== undefined)
    result.repair_wave_id = expectString2(record.repair_wave_id, "semantic_delta.repair_wave_id", SAFE_KEY_PATTERN2);
  if (result.repair_fingerprint !== undefined && result.repair_fingerprints !== undefined) {
    fail3("RUNTIME_SCHEMA_INVALID", "step-progress must not mix repair_fingerprint with repair_fingerprints.");
  }
  if (result.repair_fingerprints !== undefined !== (result.repair_wave_id !== undefined)) {
    fail3("RUNTIME_SCHEMA_INVALID", "repair_fingerprints and repair_wave_id must be supplied together.");
  }
  if (record.change_set_id !== undefined)
    result.change_set_id = expectString2(record.change_set_id, "semantic_delta.change_set_id", SAFE_KEY_PATTERN2);
  if (record.review_receipt !== undefined)
    result.review_receipt = validateStepReviewReceipt(record.review_receipt, "semantic_delta.review_receipt");
  if (record.claim_evidence !== undefined)
    result.claim_evidence = validateClaimEvidence(record.claim_evidence, "semantic_delta.claim_evidence");
  if (record.execution_result !== undefined)
    result.execution_result = validateStepExecutionResult(record.execution_result, "semantic_delta.execution_result");
  return result;
}
function validateLifecycleReasons(value, location) {
  const raw = expectStringArray2(value, location, false, RESUME_REVIEW_REASON_ORDER.length);
  const normalized = normalizeResumeReviewReasons(raw);
  if (normalized.length !== raw.length || !normalized.every((reason, index) => reason === raw[index])) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} must use the canonical closed-set order without duplicates.`);
  }
  return normalized;
}
function validateLifecycleDelta(value) {
  const record = expectRecord2(value, "semantic_delta");
  const kind = expectEnum(record.kind, ["lifecycle"], "semantic_delta.kind");
  const action = expectEnum(record.action, LIFECYCLE_MODES, "semantic_delta.action");
  if (action === "pause") {
    const allowedKeys = [
      "kind",
      "action",
      "lifecycle_state",
      "suspension_reason",
      "task_start_base",
      "last_reviewed_checkpoint",
      "current_diff_review_target",
      "rollback_conditions",
      "resume_review_reasons",
      "evidence_refs",
      "blocker_status",
      "blocking_evidence",
      "remaining_acceptance",
      "failed_checks"
    ];
    if (Object.keys(record).some((key) => !allowedKeys.includes(key)))
      fail3("RUNTIME_SCHEMA_INVALID", "pause lifecycle semantic_delta contains unsupported fields.");
    const lifecycleState = expectEnum(record.lifecycle_state, ["paused_pending_closure", "paused_blocked"], "semantic_delta.lifecycle_state");
    const common = {
      kind,
      action,
      lifecycle_state: lifecycleState,
      suspension_reason: expectText(record.suspension_reason, "semantic_delta.suspension_reason"),
      task_start_base: expectText(record.task_start_base, "semantic_delta.task_start_base"),
      last_reviewed_checkpoint: expectText(record.last_reviewed_checkpoint, "semantic_delta.last_reviewed_checkpoint"),
      current_diff_review_target: expectText(record.current_diff_review_target, "semantic_delta.current_diff_review_target"),
      rollback_conditions: expectText(record.rollback_conditions, "semantic_delta.rollback_conditions"),
      resume_review_reasons: validateLifecycleReasons(record.resume_review_reasons, "semantic_delta.resume_review_reasons"),
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
    };
    try {
      validateCurrentTaskResumeGate(lifecycleState, true, common.resume_review_reasons);
    } catch (error) {
      fail3("RUNTIME_LIFECYCLE_EVIDENCE_INVALID", error instanceof Error ? error.message : String(error));
    }
    if (lifecycleState === "paused_blocked") {
      return {
        ...common,
        lifecycle_state: lifecycleState,
        blocker_status: expectText(record.blocker_status, "semantic_delta.blocker_status"),
        blocking_evidence: expectText(record.blocking_evidence, "semantic_delta.blocking_evidence"),
        remaining_acceptance: expectText(record.remaining_acceptance, "semantic_delta.remaining_acceptance"),
        ...record.failed_checks === undefined ? {} : { failed_checks: expectStringArray2(record.failed_checks, "semantic_delta.failed_checks", false, 32) }
      };
    }
    const forbiddenFields = ["blocker_status", "blocking_evidence", "remaining_acceptance", "failed_checks"];
    if (forbiddenFields.some((field) => record[field] !== undefined))
      fail3("RUNTIME_SCHEMA_INVALID", "paused_pending_closure must not carry paused_blocked-only evidence.");
    return common;
  }
  if (action === "interrupt") {
    const allowedKeys = [
      "kind",
      "action",
      "lifecycle_state",
      "suspension_reason",
      "task_start_base",
      "last_reviewed_checkpoint",
      "current_diff_review_target",
      "rollback_conditions",
      "resume_review_reasons",
      "evidence_refs",
      "checkpoint_evidence",
      "dirty_attribution",
      "environment_state",
      "recovery_strategy"
    ];
    if (Object.keys(record).some((key) => !allowedKeys.includes(key)))
      fail3("RUNTIME_SCHEMA_INVALID", "interrupt lifecycle semantic_delta contains unsupported fields.");
    const lifecycleState = expectEnum(record.lifecycle_state, ["interrupted"], "semantic_delta.lifecycle_state");
    const resumeReviewReasons = validateLifecycleReasons(record.resume_review_reasons, "semantic_delta.resume_review_reasons");
    try {
      validateCurrentTaskResumeGate(lifecycleState, true, resumeReviewReasons);
    } catch (error) {
      fail3("RUNTIME_LIFECYCLE_EVIDENCE_INVALID", error instanceof Error ? error.message : String(error));
    }
    return {
      kind,
      action,
      lifecycle_state: lifecycleState,
      suspension_reason: expectText(record.suspension_reason, "semantic_delta.suspension_reason"),
      task_start_base: expectText(record.task_start_base, "semantic_delta.task_start_base"),
      last_reviewed_checkpoint: expectText(record.last_reviewed_checkpoint, "semantic_delta.last_reviewed_checkpoint"),
      current_diff_review_target: expectText(record.current_diff_review_target, "semantic_delta.current_diff_review_target"),
      rollback_conditions: expectText(record.rollback_conditions, "semantic_delta.rollback_conditions"),
      resume_review_reasons: resumeReviewReasons,
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs"),
      checkpoint_evidence: expectText(record.checkpoint_evidence, "semantic_delta.checkpoint_evidence"),
      dirty_attribution: expectText(record.dirty_attribution, "semantic_delta.dirty_attribution"),
      environment_state: expectText(record.environment_state, "semantic_delta.environment_state"),
      recovery_strategy: expectText(record.recovery_strategy, "semantic_delta.recovery_strategy")
    };
  }
  if (action === "resume-paused" || action === "resume-interrupted") {
    expectExactKeys2(record, ["kind", "action", "artifact_kind", "recovery_package_path", "recovery_package_revision", "resume_review_reasons", "evidence_refs"], "semantic_delta");
    const artifactKind = expectEnum(record.artifact_kind, ["paused", "interrupted"], "semantic_delta.artifact_kind");
    if (action === "resume-paused" && artifactKind !== "paused" || action === "resume-interrupted" && artifactKind !== "interrupted") {
      fail3("RUNTIME_LIFECYCLE_EVIDENCE_INVALID", `${action} must target the matching ${action === "resume-paused" ? "paused" : "interrupted"} artifact kind.`);
    }
    const recoveryPackageRevision = expectString2(record.recovery_package_revision, "semantic_delta.recovery_package_revision");
    if (!/^[a-f0-9]{64}$/.test(recoveryPackageRevision))
      fail3("RUNTIME_SCHEMA_INVALID", "semantic_delta.recovery_package_revision must be SHA-256.");
    return {
      kind,
      action,
      artifact_kind: artifactKind,
      recovery_package_path: normalizeRepoPath2(expectString2(record.recovery_package_path, "semantic_delta.recovery_package_path"), "semantic_delta.recovery_package_path"),
      recovery_package_revision: recoveryPackageRevision,
      resume_review_reasons: validateLifecycleReasons(record.resume_review_reasons, "semantic_delta.resume_review_reasons"),
      evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
    };
  }
  expectExactKeys2(record, ["kind", "action", "invalidation_kind", "invalidation_reason", "evidence_refs", "partial_diff_disposition"], "semantic_delta");
  return {
    kind,
    action: "supersede",
    invalidation_kind: expectEnum(record.invalidation_kind, ["goal", "scope", "acceptance"], "semantic_delta.invalidation_kind"),
    invalidation_reason: expectText(record.invalidation_reason, "semantic_delta.invalidation_reason"),
    evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs"),
    partial_diff_disposition: validatePartialDiffDisposition(record.partial_diff_disposition, "semantic_delta.partial_diff_disposition")
  };
}
function validateFindingRecord(value, location) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, ["kind", "action", "cycle_phase", "finding_admission_wave_id", "finding"], location);
  expectEnum(record.kind, ["finding-queue"], `${location}.kind`);
  expectEnum(record.action, ["admit"], `${location}.action`);
  const finding = expectRecord2(record.finding, `${location}.finding`);
  const findingKeys = [
    "fingerprint",
    "category",
    "owner_task_id",
    "scope",
    "decision",
    "file",
    "failure_condition",
    "violated_invariant",
    "root_cause_status",
    "status",
    "repair_attempts",
    "max_repair_attempts",
    "evidence_refs",
    "review_cycle_id"
  ];
  const findingExtra = Object.keys(finding).filter((key) => !findingKeys.includes(key));
  if (findingExtra.length > 0)
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.finding contains unsupported fields.`);
  const result = {
    kind: "finding-queue",
    action: "admit",
    cycle_phase: expectEnum(record.cycle_phase, REVIEW_CYCLE_PHASES, `${location}.cycle_phase`),
    finding_admission_wave_id: expectString2(record.finding_admission_wave_id, `${location}.finding_admission_wave_id`, SAFE_KEY_PATTERN2),
    finding: {
      fingerprint: expectString2(finding.fingerprint, `${location}.finding.fingerprint`, FINGERPRINT_PATTERN),
      category: expectText(finding.category, `${location}.finding.category`, 256),
      owner_task_id: expectString2(finding.owner_task_id, `${location}.finding.owner_task_id`),
      scope: expectEnum(finding.scope, ["admitted"], `${location}.finding.scope`),
      decision: expectEnum(finding.decision, ["mechanical"], `${location}.finding.decision`),
      file: normalizeRepoPath2(expectString2(finding.file, `${location}.finding.file`), `${location}.finding.file`),
      failure_condition: expectText(finding.failure_condition, `${location}.finding.failure_condition`),
      violated_invariant: expectText(finding.violated_invariant, `${location}.finding.violated_invariant`, 512),
      root_cause_status: expectEnum(finding.root_cause_status, ["confirmed", "bounded"], `${location}.finding.root_cause_status`),
      max_repair_attempts: expectInteger(finding.max_repair_attempts, `${location}.finding.max_repair_attempts`, 1, MAX_REPAIR_ATTEMPTS),
      evidence_refs: validateEvidenceRefs(finding.evidence_refs, `${location}.finding.evidence_refs`),
      review_cycle_id: expectString2(finding.review_cycle_id, `${location}.finding.review_cycle_id`, SAFE_KEY_PATTERN2)
    }
  };
  if (finding.status !== undefined && finding.status !== "admitted")
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.finding.status must be admitted.`);
  if (finding.repair_attempts !== undefined && finding.repair_attempts !== 0)
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.finding.repair_attempts must be 0.`);
  return result;
}
function validateFindingAction(value) {
  const record = expectRecord2(value, "semantic_delta");
  const action = expectEnum(record.action, ["record-repair-attempt", "resolve", "defer", "reject"], "semantic_delta.action");
  const allowedKeys = action === "record-repair-attempt" ? ["kind", "action", "fingerprint", "review_cycle_id", "repair_wave_id", "evidence_refs", "note"] : ["kind", "action", "fingerprint", "evidence_refs", "note"];
  if (Object.keys(record).some((key) => !allowedKeys.includes(key)))
    fail3("RUNTIME_SCHEMA_INVALID", "finding-queue semantic_delta contains unsupported fields.");
  const result = action === "record-repair-attempt" ? {
    kind: "finding-queue",
    action,
    fingerprint: expectString2(record.fingerprint, "semantic_delta.fingerprint", FINGERPRINT_PATTERN),
    review_cycle_id: expectString2(record.review_cycle_id, "semantic_delta.review_cycle_id", SAFE_KEY_PATTERN2),
    repair_wave_id: expectString2(record.repair_wave_id, "semantic_delta.repair_wave_id", SAFE_KEY_PATTERN2),
    evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
  } : {
    kind: "finding-queue",
    action,
    fingerprint: expectString2(record.fingerprint, "semantic_delta.fingerprint", FINGERPRINT_PATTERN),
    evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
  };
  if (record.note !== undefined)
    result.note = expectText(record.note, "semantic_delta.note");
  return result;
}
var CLOSURE_EVIDENCE_FIELDS = [
  "acceptance_satisfied",
  "validation_complete",
  "no_admitted_or_in_progress_findings",
  "no_unresolved_closure_blocker",
  "release_evidence",
  "rollback_evidence",
  "observation_evidence",
  "remaining_risks_non_blocking",
  "archive_path_verified"
];
function validateClosureEvidence(value, location) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, CLOSURE_EVIDENCE_FIELDS, location);
  const validateEvidenceGate = (raw, field) => {
    const gate = expectRecord2(raw, `${location}.${field}`);
    expectExactKeys2(gate, ["triggered", "complete", "evidence_refs"], `${location}.${field}`);
    const triggered = expectBoolean(gate.triggered, `${location}.${field}.triggered`);
    const complete = expectBoolean(gate.complete, `${location}.${field}.complete`);
    const evidenceRefs = expectStringArray2(gate.evidence_refs, `${location}.${field}.evidence_refs`, true, MAX_EVIDENCE_REFS);
    if (triggered && !complete) {
      fail3("CLOSURE_EVIDENCE_INVALID", `${location}.${field} is triggered but incomplete.`);
    }
    return { triggered, complete, evidence_refs: evidenceRefs };
  };
  return {
    acceptance_satisfied: expectBoolean(record.acceptance_satisfied, `${location}.acceptance_satisfied`),
    validation_complete: expectBoolean(record.validation_complete, `${location}.validation_complete`),
    no_admitted_or_in_progress_findings: expectBoolean(record.no_admitted_or_in_progress_findings, `${location}.no_admitted_or_in_progress_findings`),
    no_unresolved_closure_blocker: expectBoolean(record.no_unresolved_closure_blocker, `${location}.no_unresolved_closure_blocker`),
    release_evidence: validateEvidenceGate(record.release_evidence, "release_evidence"),
    rollback_evidence: validateEvidenceGate(record.rollback_evidence, "rollback_evidence"),
    observation_evidence: validateEvidenceGate(record.observation_evidence, "observation_evidence"),
    remaining_risks_non_blocking: expectBoolean(record.remaining_risks_non_blocking, `${location}.remaining_risks_non_blocking`),
    archive_path_verified: expectBoolean(record.archive_path_verified, `${location}.archive_path_verified`)
  };
}
function validateDeliverySummary(value, location) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, ["goal", "actual_changes", "verification", "release_evidence", "rollback_evidence", "observation_evidence", "next_action"], location);
  return {
    goal: expectText(record.goal, `${location}.goal`, MAX_TEXT_LENGTH),
    actual_changes: expectStringArray2(record.actual_changes, `${location}.actual_changes`, false, 64),
    verification: expectStringArray2(record.verification, `${location}.verification`, false, 64),
    release_evidence: expectStringArray2(record.release_evidence, `${location}.release_evidence`, true, 64),
    rollback_evidence: expectStringArray2(record.rollback_evidence, `${location}.rollback_evidence`, true, 64),
    observation_evidence: expectStringArray2(record.observation_evidence, `${location}.observation_evidence`, true, 64),
    next_action: expectText(record.next_action, `${location}.next_action`, MAX_TEXT_LENGTH)
  };
}
function validateLessonAdmission(value, location) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, ["decision", "candidate_refs", "evidence_refs"], location);
  const decision = expectEnum(record.decision, ["admit", "defer", "no-op"], `${location}.decision`);
  const candidateRefs = expectStringArray2(record.candidate_refs, `${location}.candidate_refs`, true, MAX_EVIDENCE_REFS);
  const evidenceRefs = expectStringArray2(record.evidence_refs, `${location}.evidence_refs`, true, MAX_EVIDENCE_REFS);
  if (decision === "admit" && candidateRefs.length === 0)
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location}.candidate_refs must be non-empty when decision is admit.`);
  if (decision === "admit" && evidenceRefs.length === 0)
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location}.evidence_refs must be non-empty when decision is admit.`);
  return { decision, candidate_refs: candidateRefs, evidence_refs: evidenceRefs };
}
var KNOWLEDGE_ADMISSION_DISPOSITIONS = [
  "admit",
  "merge",
  "supersede",
  "defer",
  "reject",
  "no-op"
];
var KNOWLEDGE_CANDIDATE_KEYS = [
  "candidateId",
  "kind",
  "fingerprint",
  "statement",
  "sourceRefs",
  "applicability",
  "authoritySource",
  "stability",
  "evidenceRefs",
  "noveltyAgainst",
  "conflictSet",
  "supersedes",
  "reviewOrExpiryTrigger",
  "expectedConsumers",
  "decisionContext",
  "systemicSeverity",
  "implementation_anchors"
];
function validateImplementationAnchors(value, location) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, ["coverage", "source_revision", "anchors"], location);
  const sourceRevision = expectString2(record.source_revision, `${location}.source_revision`);
  if (sourceRevision.length > 256)
    fail3("IMPLEMENTATION_ANCHOR_INVALID", `${location}.source_revision exceeds 256 characters.`);
  if (!Array.isArray(record.anchors) || record.anchors.length > 5) {
    fail3("IMPLEMENTATION_ANCHOR_INVALID", `${location}.anchors must contain at most five anchors.`);
  }
  const anchors = record.anchors.map((raw, index) => {
    const anchor = expectRecord2(raw, `${location}.anchors[${index}]`);
    const extra = Object.keys(anchor).filter((key) => !["path", "symbol", "role", "evidence_refs"].includes(key));
    const missing = ["path", "role", "evidence_refs"].filter((key) => !(key in anchor));
    if (missing.length > 0 || extra.length > 0) {
      fail3("IMPLEMENTATION_ANCHOR_INVALID", `${location}.anchors[${index}] keys mismatch; missing=[${missing.join(", ")}], unexpected=[${extra.join(", ")}].`);
    }
    const rawAnchorPath = expectString2(anchor.path, `${location}.anchors[${index}].path`);
    const anchorPath = normalizeRepoPath2(rawAnchorPath, `${location}.anchors[${index}].path`);
    if (/^[A-Za-z]:\//u.test(anchorPath) || anchorPath.includes(":") || anchorPath !== path12.posix.normalize(anchorPath)) {
      fail3("IMPLEMENTATION_ANCHOR_INVALID", `${location}.anchors[${index}].path must be a canonical repository-relative path.`);
    }
    if (anchorPath.includes("*") || /:\d+(?:-\d+)?$/u.test(anchorPath)) {
      fail3("IMPLEMENTATION_ANCHOR_INVALID", `${location}.anchors[${index}].path must not be a wildcard or line-number locator.`);
    }
    const symbol = anchor.symbol === undefined || anchor.symbol === null ? null : expectText(anchor.symbol, `${location}.anchors[${index}].symbol`, 256);
    if (symbol !== null && /\r|\n/u.test(symbol))
      fail3("IMPLEMENTATION_ANCHOR_INVALID", `${location}.anchors[${index}].symbol must be single-line.`);
    if (symbol !== null && /^.+:\d+(?:-\d+)?$/u.test(symbol))
      fail3("IMPLEMENTATION_ANCHOR_INVALID", `${location}.anchors[${index}].symbol must not be a line-number locator.`);
    const role = expectText(anchor.role, `${location}.anchors[${index}].role`, 256);
    const evidenceRefs = validateEvidenceRefs(anchor.evidence_refs, `${location}.anchors[${index}].evidence_refs`);
    return { path: anchorPath, symbol, role, evidence_refs: evidenceRefs };
  });
  const anchorKeys = anchors.map((anchor) => `${anchor.path}#${anchor.symbol ?? ""}`);
  if (new Set(anchorKeys).size !== anchorKeys.length)
    fail3("IMPLEMENTATION_ANCHOR_INVALID", `${location}.anchors must not contain duplicate path/symbol locators.`);
  return {
    coverage: expectEnum(record.coverage, ["observed", "verified-scope"], `${location}.coverage`),
    source_revision: sourceRevision,
    anchors
  };
}
function validateKnowledgeCandidate(value, location, expectedKind) {
  const record = expectRecord2(value, location);
  const allowedKeys = new Set(KNOWLEDGE_CANDIDATE_KEYS);
  const requiredKeys = KNOWLEDGE_CANDIDATE_KEYS.filter((key) => !["decisionContext", "systemicSeverity", "implementation_anchors"].includes(key));
  const missing = requiredKeys.filter((key) => !(key in record));
  const extra = Object.keys(record).filter((key) => !allowedKeys.has(key));
  if (missing.length > 0 || extra.length > 0) {
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location} keys mismatch; missing=[${missing.join(", ")}], unexpected=[${extra.join(", ")}].`);
  }
  const kind = expectEnum(record.kind, ["contract", "decision"], `${location}.kind`);
  if (expectedKind !== undefined && kind !== expectedKind)
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location}.kind must be ${expectedKind}.`);
  const sourceRefsRaw = record.sourceRefs;
  if (!Array.isArray(sourceRefsRaw) || sourceRefsRaw.length === 0 || sourceRefsRaw.length > 32)
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location}.sourceRefs must contain between one and 32 entries.`);
  const sourceRefs = sourceRefsRaw.map((raw, index) => {
    const sourceRef = expectRecord2(raw, `${location}.sourceRefs[${index}]`);
    expectExactKeys2(sourceRef, ["locator", "revision"], `${location}.sourceRefs[${index}]`);
    return { locator: expectText(sourceRef.locator, `${location}.sourceRefs[${index}].locator`, 512), revision: expectText(sourceRef.revision, `${location}.sourceRefs[${index}].revision`, 256) };
  });
  const applicabilityRecord = expectRecord2(record.applicability, `${location}.applicability`);
  expectExactKeys2(applicabilityRecord, ["projectTypes", "pathsSymbolsOrSurfaces", "triggerConditions"], `${location}.applicability`);
  const applicability = {
    projectTypes: expectStringArray2(applicabilityRecord.projectTypes, `${location}.applicability.projectTypes`, true, 32),
    pathsSymbolsOrSurfaces: expectStringArray2(applicabilityRecord.pathsSymbolsOrSurfaces, `${location}.applicability.pathsSymbolsOrSurfaces`, true, 32),
    triggerConditions: expectStringArray2(applicabilityRecord.triggerConditions, `${location}.applicability.triggerConditions`, true, 32)
  };
  if (applicability.projectTypes.length === 0 && applicability.pathsSymbolsOrSurfaces.length === 0 && applicability.triggerConditions.length === 0) {
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location}.applicability must identify at least one project, surface, or trigger.`);
  }
  const decisionContext = record.decisionContext === undefined ? undefined : (() => {
    const context = expectRecord2(record.decisionContext, `${location}.decisionContext`);
    expectExactKeys2(context, ["alternatives", "constraints"], `${location}.decisionContext`);
    return {
      alternatives: expectStringArray2(context.alternatives, `${location}.decisionContext.alternatives`, false, 32),
      constraints: expectStringArray2(context.constraints, `${location}.decisionContext.constraints`, false, 32)
    };
  })();
  if (kind === "decision" && decisionContext === undefined)
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location}.decisionContext is required for a Decision.`);
  const implementationAnchors = record.implementation_anchors === undefined ? undefined : validateImplementationAnchors(record.implementation_anchors, `${location}.implementation_anchors`);
  const candidate = {
    candidateId: expectString2(record.candidateId, `${location}.candidateId`, SAFE_KEY_PATTERN2),
    kind,
    fingerprint: expectString2(record.fingerprint, `${location}.fingerprint`, FINGERPRINT_PATTERN),
    statement: expectText(record.statement, `${location}.statement`, MAX_TEXT_LENGTH),
    sourceRefs,
    applicability,
    authoritySource: expectEnum(record.authoritySource, ["user", "existing-contract", "accepted-decision", "verified-evidence", "none"], `${location}.authoritySource`),
    stability: expectEnum(record.stability, ["stable", "provisional", "exploratory"], `${location}.stability`),
    evidenceRefs: validateEvidenceRefs(record.evidenceRefs, `${location}.evidenceRefs`),
    noveltyAgainst: expectStringArray2(record.noveltyAgainst, `${location}.noveltyAgainst`, true, 32),
    conflictSet: expectStringArray2(record.conflictSet, `${location}.conflictSet`, true, 32),
    supersedes: expectNullableString(record.supersedes, `${location}.supersedes`, SAFE_KEY_PATTERN2),
    reviewOrExpiryTrigger: expectNullableString(record.reviewOrExpiryTrigger, `${location}.reviewOrExpiryTrigger`),
    expectedConsumers: expectStringArray2(record.expectedConsumers, `${location}.expectedConsumers`, false, 32),
    ...decisionContext ? { decisionContext } : {},
    ...record.systemicSeverity === undefined ? {} : { systemicSeverity: expectEnum(record.systemicSeverity, ["ordinary", "high"], `${location}.systemicSeverity`) },
    ...implementationAnchors ? { implementation_anchors: implementationAnchors } : {}
  };
  return candidate;
}
function validateKnowledgeAdmissionRecord(value, location, expectedKind) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, ["candidate", "disposition", "matched_knowledge_id", "reasons"], location);
  const candidate = validateKnowledgeCandidate(record.candidate, `${location}.candidate`, expectedKind);
  const disposition = expectEnum(record.disposition, KNOWLEDGE_ADMISSION_DISPOSITIONS, `${location}.disposition`);
  const matchedKnowledgeId = expectNullableString(record.matched_knowledge_id, `${location}.matched_knowledge_id`, SAFE_KEY_PATTERN2);
  const reasons = expectStringArray2(record.reasons, `${location}.reasons`, true, 32);
  const durableDisposition = ["admit", "merge", "supersede"].includes(disposition);
  if (durableDisposition && reasons.length === 0) {
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location}.reasons must be non-empty for a durable admission.`);
  }
  if (durableDisposition && candidate.authoritySource === "none") {
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location} cannot admit knowledge without an authority source.`);
  }
  if (durableDisposition && expectedKind === "decision" && !["user", "accepted-decision"].includes(candidate.authoritySource)) {
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location} Decision admission requires user or accepted-decision authority.`);
  }
  if (disposition === "merge" && matchedKnowledgeId === null)
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location}.matched_knowledge_id is required for merge.`);
  if (disposition === "supersede" && (matchedKnowledgeId === null || candidate.supersedes !== matchedKnowledgeId))
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location}.supersede must identify the same predecessor in matched_knowledge_id and candidate.supersedes.`);
  if (durableDisposition) {
    if (candidate.stability !== "stable" || candidate.conflictSet.length > 0 || candidate.evidenceRefs.length === 0) {
      fail3("KNOWLEDGE_ADMISSION_INVALID", `${location} durable admission requires stable, conflict-free candidate evidence.`);
    }
  }
  return { candidate, disposition, matched_knowledge_id: matchedKnowledgeId, reasons };
}
function validateKnowledgeAdmissionBundle(value, location) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, ["contracts", "decisions"], location);
  if (!Array.isArray(record.contracts) || record.contracts.length > 32)
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location}.contracts must contain at most 32 records.`);
  if (!Array.isArray(record.decisions) || record.decisions.length > 32)
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location}.decisions must contain at most 32 records.`);
  const contracts = record.contracts.map((item, index) => validateKnowledgeAdmissionRecord(item, `${location}.contracts[${index}]`, "contract"));
  const decisions = record.decisions.map((item, index) => validateKnowledgeAdmissionRecord(item, `${location}.decisions[${index}]`, "decision"));
  if (new Set(contracts.map((item) => item.candidate.candidateId)).size !== contracts.length)
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location}.contracts candidate identity must be unique.`);
  if (new Set(decisions.map((item) => item.candidate.candidateId)).size !== decisions.length)
    fail3("KNOWLEDGE_ADMISSION_INVALID", `${location}.decisions candidate identity must be unique.`);
  return { contracts, decisions };
}
function emptyKnowledgeAdmissionBundle() {
  return { contracts: [], decisions: [] };
}
function validateKnowledgeProvenance(value, location) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, ["task_id", "task_slug", "document_id", "archive_path", "archive_revision", "source_revision", "evidence_refs"], location);
  const taskId = expectString2(record.task_id, `${location}.task_id`);
  const taskSlug = expectString2(record.task_slug, `${location}.task_slug`);
  try {
    validateTaskId(taskId);
    validateTaskSlug(taskSlug);
  } catch (error) {
    fail3("KNOWLEDGE_PROVENANCE_MISMATCH", error instanceof Error ? error.message : String(error));
  }
  const documentId = expectString2(record.document_id, `${location}.document_id`);
  const archiveRevision = expectString2(record.archive_revision, `${location}.archive_revision`);
  const sourceRevision = expectString2(record.source_revision, `${location}.source_revision`);
  if (!DOCUMENT_ID_PATTERN.test(documentId) || !SHA256_PATTERN2.test(archiveRevision) || !SHA256_PATTERN2.test(sourceRevision)) {
    fail3("KNOWLEDGE_PROVENANCE_MISMATCH", `${location} contains an invalid document or revision.`);
  }
  const archivePath = normalizeRepoPath2(expectString2(record.archive_path, `${location}.archive_path`), `${location}.archive_path`);
  if (!/^TASKS\/TASK-[0-9]{3,}-[a-z0-9]+(?:-[a-z0-9]+)*\.md$/u.test(archivePath))
    fail3("KNOWLEDGE_PROVENANCE_MISMATCH", `${location}.archive_path is not a canonical task archive path.`);
  return {
    task_id: taskId,
    task_slug: taskSlug,
    document_id: documentId,
    archive_path: archivePath,
    archive_revision: archiveRevision,
    source_revision: sourceRevision,
    evidence_refs: validateEvidenceRefs(record.evidence_refs, `${location}.evidence_refs`)
  };
}
function validateKnowledgeDelta(value, expectedKind) {
  const record = expectRecord2(value, "semantic_delta");
  expectExactKeys2(record, ["kind", "action", "knowledge_kind", "admission", "provenance", "evidence_refs"], "semantic_delta");
  const knowledgeKind = expectEnum(record.knowledge_kind, ["contract", "decision"], "semantic_delta.knowledge_kind");
  if (expectedKind !== undefined && knowledgeKind !== expectedKind)
    fail3("RUNTIME_SCHEMA_INVALID", `semantic_delta.knowledge_kind must be ${expectedKind}.`);
  const admission = validateKnowledgeAdmissionRecord(record.admission, "semantic_delta.admission", knowledgeKind);
  if (!["admit", "merge", "supersede"].includes(admission.disposition)) {
    fail3("KNOWLEDGE_ADMISSION_INVALID", "Only admitted, merged, or superseded knowledge may be submitted to a Runtime promotion operation.");
  }
  const provenance = validateKnowledgeProvenance(record.provenance, "semantic_delta.provenance");
  const evidenceRefs = validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs");
  const candidateEvidenceRefs = [
    ...admission.candidate.evidenceRefs,
    ...admission.candidate.implementation_anchors?.anchors.flatMap((anchor) => anchor.evidence_refs) ?? [],
    ...provenance.evidence_refs
  ];
  if (!candidateEvidenceRefs.every((ref) => evidenceRefs.includes(ref)))
    fail3("RUNTIME_EVIDENCE_INVALID", "knowledge proposal evidence_refs must cover candidate, anchor, and provenance evidence_refs.");
  return {
    kind: "knowledge",
    action: "promote",
    knowledge_kind: knowledgeKind,
    admission,
    provenance,
    evidence_refs: evidenceRefs
  };
}
function validateArchiveDelta(value) {
  const record = expectRecord2(value, "semantic_delta");
  const allowedKeys = ["kind", "action", "closure_evidence", "delivery_summary", "remaining_risks", "lesson_admission", "knowledge_admissions", "evidence_refs"];
  const extra = Object.keys(record).filter((key) => !allowedKeys.includes(key));
  const required = allowedKeys.filter((key) => !["knowledge_admissions"].includes(key) && !(key in record));
  if (required.length > 0 || extra.length > 0) {
    fail3("RUNTIME_SCHEMA_INVALID", `semantic_delta keys mismatch; missing=[${required.join(", ")}], unexpected=[${extra.join(", ")}].`);
  }
  const evidenceRefs = validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs");
  const closureEvidence = validateClosureEvidence(record.closure_evidence, "semantic_delta.closure_evidence");
  const lessonAdmission = validateLessonAdmission(record.lesson_admission, "semantic_delta.lesson_admission");
  const knowledgeAdmissions = record.knowledge_admissions === undefined ? emptyKnowledgeAdmissionBundle() : validateKnowledgeAdmissionBundle(record.knowledge_admissions, "semantic_delta.knowledge_admissions");
  const referencedEvidence = [
    ...closureEvidence.release_evidence.evidence_refs,
    ...closureEvidence.rollback_evidence.evidence_refs,
    ...closureEvidence.observation_evidence.evidence_refs,
    ...lessonAdmission.evidence_refs,
    ...knowledgeAdmissions.contracts.flatMap((item) => [...item.candidate.evidenceRefs, ...item.candidate.implementation_anchors?.anchors.flatMap((anchor) => anchor.evidence_refs) ?? []]),
    ...knowledgeAdmissions.decisions.flatMap((item) => [...item.candidate.evidenceRefs, ...item.candidate.implementation_anchors?.anchors.flatMap((anchor) => anchor.evidence_refs) ?? []])
  ];
  if (!referencedEvidence.every((ref) => evidenceRefs.includes(ref))) {
    fail3("RUNTIME_EVIDENCE_INVALID", "archive proposal evidence_refs must cover closure and lesson-admission evidence_refs.");
  }
  return {
    kind: expectEnum(record.kind, ["archive"], "semantic_delta.kind"),
    action: expectEnum(record.action, ["archive"], "semantic_delta.action"),
    closure_evidence: closureEvidence,
    delivery_summary: validateDeliverySummary(record.delivery_summary, "semantic_delta.delivery_summary"),
    remaining_risks: expectStringArray2(record.remaining_risks, "semantic_delta.remaining_risks", true, 64),
    lesson_admission: lessonAdmission,
    knowledge_admissions: knowledgeAdmissions,
    evidence_refs: evidenceRefs
  };
}
var LESSON_CATEGORIES = ["通用", "数据与存储", "前端与交互", "后端与服务", "测试与回归", "部署与运行时"];
var LESSON_REQUIRED_SECTION_HEADINGS = ["使用规则", ...LESSON_CATEGORIES];
function validateLessonCandidate(value, location) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, ["candidate_ref", "category", "scene", "conclusion", "trigger", "cause", "action", "consumer", "evidence_refs"], location);
  return {
    candidate_ref: expectString2(record.candidate_ref, `${location}.candidate_ref`, SAFE_KEY_PATTERN2),
    category: expectEnum(record.category, LESSON_CATEGORIES, `${location}.category`),
    scene: expectText(record.scene, `${location}.scene`),
    conclusion: expectText(record.conclusion, `${location}.conclusion`),
    trigger: expectText(record.trigger, `${location}.trigger`),
    cause: expectText(record.cause, `${location}.cause`),
    action: expectText(record.action, `${location}.action`),
    consumer: expectText(record.consumer, `${location}.consumer`),
    evidence_refs: validateEvidenceRefs(record.evidence_refs, `${location}.evidence_refs`)
  };
}
function validateProjectStatusDelta(value) {
  const record = expectRecord2(value, "semantic_delta");
  expectExactKeys2(record, ["kind", "action", "status", "summary", "completed_items", "remaining_risks", "next_checkpoint", "evidence_refs"], "semantic_delta");
  return {
    kind: expectEnum(record.kind, ["project-status"], "semantic_delta.kind"),
    action: expectEnum(record.action, ["sync"], "semantic_delta.action"),
    status: expectEnum(record.status, ["completed", "observing"], "semantic_delta.status"),
    summary: expectText(record.summary, "semantic_delta.summary"),
    completed_items: expectStringArray2(record.completed_items, "semantic_delta.completed_items", false, 64),
    remaining_risks: expectStringArray2(record.remaining_risks, "semantic_delta.remaining_risks", true, 64),
    next_checkpoint: expectText(record.next_checkpoint, "semantic_delta.next_checkpoint"),
    evidence_refs: validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs")
  };
}
function validateLessonRecordDelta(value) {
  const record = expectRecord2(value, "semantic_delta");
  expectExactKeys2(record, ["kind", "action", "candidates", "evidence_refs"], "semantic_delta");
  if (!Array.isArray(record.candidates) || record.candidates.length === 0 || record.candidates.length > 32) {
    fail3("RUNTIME_SCHEMA_INVALID", "semantic_delta.candidates must contain between 1 and 32 candidates.");
  }
  const candidates = record.candidates.map((candidate, index) => validateLessonCandidate(candidate, `semantic_delta.candidates[${index}]`));
  if (new Set(candidates.map((candidate) => candidate.candidate_ref)).size !== candidates.length) {
    fail3("RUNTIME_SCHEMA_INVALID", "semantic_delta.candidates must have unique candidate_ref values.");
  }
  const evidenceRefs = validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs");
  if (!candidates.every((candidate) => candidate.evidence_refs.every((ref) => evidenceRefs.includes(ref)))) {
    fail3("RUNTIME_EVIDENCE_INVALID", "lesson-record proposal evidence_refs must cover every candidate evidence reference.");
  }
  return {
    kind: expectEnum(record.kind, ["lesson-record"], "semantic_delta.kind"),
    action: expectEnum(record.action, ["record"], "semantic_delta.action"),
    candidates,
    evidence_refs: evidenceRefs
  };
}
function validateInboxItemId(value, location) {
  const itemId = expectString2(value, location);
  const match = INBOX_RECORD_ITEM_ID_PATTERN.exec(itemId);
  if (!match) {
    fail3("RUNTIME_IDENTITY_INVALID", `${location} must use YYYYMMDD-short-id with a lowercase alphanumeric short-id.`);
  }
  const year = Number(match[1].slice(0, 4));
  const month = Number(match[1].slice(4, 6));
  const day = Number(match[1].slice(6, 8));
  const date = new Date(Date.UTC(year, month - 1, day));
  if (year < 1 || date.getUTCFullYear() !== year || date.getUTCMonth() !== month - 1 || date.getUTCDate() !== day) {
    fail3("RUNTIME_IDENTITY_INVALID", `${location} must begin with a valid YYYYMMDD date.`);
  }
  return itemId;
}
function validateInboxRecord(value, location) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, ["artifact_kind", "item_id", "title", "type", "source", "captured_at", "relation_to_current_task", "current_task_id", "description", "evidence", "suggested_next_action", "status"], location);
  const title = expectText(record.title, `${location}.title`, 512);
  if (/[\r\n]/u.test(title) || /^\{\{[^{}]+\}\}$/.test(title)) {
    fail3("RUNTIME_IDENTITY_INVALID", `${location}.title must be a concrete single-line value.`);
  }
  const capturedAt = expectString2(record.captured_at, `${location}.captured_at`);
  if (/[\r\n]/u.test(capturedAt) || Number.isNaN(Date.parse(capturedAt))) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.captured_at must be a parseable timestamp.`);
  }
  const currentTaskId = expectString2(record.current_task_id, `${location}.current_task_id`);
  try {
    validateTaskId(currentTaskId);
  } catch (error) {
    fail3("RUNTIME_IDENTITY_INVALID", error instanceof Error ? error.message : String(error));
  }
  return {
    artifact_kind: expectEnum(record.artifact_kind, ["inbox_item"], `${location}.artifact_kind`),
    item_id: validateInboxItemId(record.item_id, `${location}.item_id`),
    title,
    type: expectEnum(record.type, INBOX_ITEM_TYPES, `${location}.type`),
    source: expectEnum(record.source, INBOX_ITEM_SOURCES, `${location}.source`),
    captured_at: capturedAt,
    relation_to_current_task: expectEnum(record.relation_to_current_task, ["unrelated"], `${location}.relation_to_current_task`),
    current_task_id: currentTaskId,
    description: expectText(record.description, `${location}.description`, MAX_INBOX_TEXT_LENGTH),
    evidence: expectText(record.evidence, `${location}.evidence`, MAX_INBOX_TEXT_LENGTH),
    suggested_next_action: expectEnum(record.suggested_next_action, INBOX_SUGGESTED_NEXT_ACTIONS, `${location}.suggested_next_action`),
    status: expectEnum(record.status, ["captured"], `${location}.status`)
  };
}
function validateInboxRecordDelta(value) {
  const record = expectRecord2(value, "semantic_delta");
  expectExactKeys2(record, ["kind", "action", "item_slug", "record", "relation_evidence_refs", "duplicate_check", "proposed_owner", "target_path", "evidence_refs"], "semantic_delta");
  const itemSlug = expectString2(record.item_slug, "semantic_delta.item_slug");
  try {
    validateTaskSlug(itemSlug);
  } catch (error) {
    fail3("RUNTIME_IDENTITY_INVALID", error instanceof Error ? error.message : String(error));
  }
  const inboxRecord = validateInboxRecord(record.record, "semantic_delta.record");
  const relationEvidenceRefs = validateEvidenceRefs(record.relation_evidence_refs, "semantic_delta.relation_evidence_refs");
  const evidenceRefs = validateEvidenceRefs(record.evidence_refs, "semantic_delta.evidence_refs");
  if (!relationEvidenceRefs.every((ref) => evidenceRefs.includes(ref))) {
    fail3("RUNTIME_EVIDENCE_INVALID", "semantic_delta.evidence_refs must cover every relation_evidence_refs entry.");
  }
  const duplicateCheck = expectEnum(record.duplicate_check, ["clear"], "semantic_delta.duplicate_check");
  const proposedOwner = expectEnum(record.proposed_owner, INBOX_SUGGESTED_NEXT_ACTIONS, "semantic_delta.proposed_owner");
  if (inboxRecord.suggested_next_action !== proposedOwner) {
    fail3("RUNTIME_RELATION_INVALID", "semantic_delta.proposed_owner must match record.suggested_next_action.");
  }
  const targetPath = normalizeRepoPath2(expectString2(record.target_path, "semantic_delta.target_path"), "semantic_delta.target_path");
  const targetMatch = INBOX_RECORD_PATH_PATTERN.exec(targetPath);
  if (!targetMatch || `${targetMatch[1]}-${targetMatch[2]}` !== inboxRecord.item_id || targetMatch[3] !== itemSlug) {
    fail3("RUNTIME_PATH_INVALID", "semantic_delta.target_path must be the canonical path derived from item_id and item_slug.");
  }
  return {
    kind: expectEnum(record.kind, ["inbox-record"], "semantic_delta.kind"),
    action: expectEnum(record.action, ["record"], "semantic_delta.action"),
    item_slug: itemSlug,
    record: inboxRecord,
    relation_evidence_refs: relationEvidenceRefs,
    duplicate_check: duplicateCheck,
    proposed_owner: proposedOwner,
    target_path: targetPath,
    evidence_refs: evidenceRefs
  };
}
function validateSemanticDelta(value, operationKind) {
  const record = expectRecord2(value, "semantic_delta");
  const kind = expectString2(record.kind, "semantic_delta.kind");
  if (operationKind === "task-state-transaction") {
    if (kind !== "task-state")
      fail3("RUNTIME_SCHEMA_INVALID", "task-state-transaction requires task-state semantic_delta.");
    return validateTaskStateDelta(value);
  }
  if (operationKind === "lifecycle-transaction") {
    if (kind !== "lifecycle")
      fail3("RUNTIME_SCHEMA_INVALID", "lifecycle-transaction requires lifecycle semantic_delta.");
    return validateLifecycleDelta(value);
  }
  if (operationKind === "archive-transaction") {
    if (kind !== "archive")
      fail3("RUNTIME_SCHEMA_INVALID", "archive-transaction requires archive semantic_delta.");
    return validateArchiveDelta(value);
  }
  if (operationKind === "project-status-transaction") {
    if (kind !== "project-status")
      fail3("RUNTIME_SCHEMA_INVALID", "project-status-transaction requires project-status semantic_delta.");
    return validateProjectStatusDelta(value);
  }
  if (operationKind === "lesson-record-transaction") {
    if (kind !== "lesson-record")
      fail3("RUNTIME_SCHEMA_INVALID", "lesson-record-transaction requires lesson-record semantic_delta.");
    return validateLessonRecordDelta(value);
  }
  if (operationKind === "inbox-record-transaction") {
    if (kind !== "inbox-record")
      fail3("RUNTIME_SCHEMA_INVALID", "inbox-record-transaction requires inbox-record semantic_delta.");
    return validateInboxRecordDelta(value);
  }
  if (operationKind === "contract-candidate-commit") {
    if (kind !== "knowledge")
      fail3("RUNTIME_SCHEMA_INVALID", "contract-candidate-commit requires a knowledge semantic_delta.");
    return validateKnowledgeDelta(value, "contract");
  }
  if (operationKind === "decision-record-transaction") {
    if (kind !== "knowledge")
      fail3("RUNTIME_SCHEMA_INVALID", "decision-record-transaction requires a knowledge semantic_delta.");
    return validateKnowledgeDelta(value, "decision");
  }
  if (kind !== "finding-queue")
    fail3("RUNTIME_SCHEMA_INVALID", "finding-queue-transaction requires finding-queue semantic_delta.");
  return record.action === "admit" ? validateFindingRecord(value, "semantic_delta") : validateFindingAction(value);
}
function validateRuntimeProposal(value) {
  const proposal = expectRecord2(value, "proposal");
  expectExactKeys2(proposal, ["schema_version", "kind", "operation_kind", "caller", "mode", "source_tuple", "authority_evidence", "semantic_delta", "preconditions", "evidence_refs", "idempotency_key", "requested_write_targets"], "proposal");
  if (proposal.schema_version !== VNEXT_RUNTIME_SCHEMA_VERSION)
    fail3("RUNTIME_SCHEMA_INVALID", "proposal.schema_version must be 1.");
  if (proposal.kind !== VNEXT_RUNTIME_PROPOSAL_KIND)
    fail3("RUNTIME_SCHEMA_INVALID", `proposal.kind must be ${VNEXT_RUNTIME_PROPOSAL_KIND}.`);
  const operationKind = expectEnum(proposal.operation_kind, RUNTIME_OPERATION_KINDS, "proposal.operation_kind");
  const caller = expectEnum(proposal.caller, ["execute-step", "review-change", "prepare-task", "task-lifecycle", "capture-work-item", "close-task"], "proposal.caller");
  const mode = expectEnum(proposal.mode, [...VNEXT_EXECUTE_STEP_MODES, ...PREPARE_TASK_MODES, ...LIFECYCLE_MODES, ...CLOSE_TASK_MODES], "proposal.mode");
  const sourceTuple = validateSourceTuple(proposal.source_tuple);
  const authorityEvidence = validateAuthorityEvidence2(proposal.authority_evidence);
  const preconditions = expectStringArray2(proposal.preconditions, "proposal.preconditions", false, 32);
  const evidenceRefs = validateEvidenceRefs(proposal.evidence_refs, "proposal.evidence_refs");
  const idempotencyKey = expectString2(proposal.idempotency_key, "proposal.idempotency_key", SAFE_KEY_PATTERN2);
  const requestedTargets = expectStringArray2(proposal.requested_write_targets, "proposal.requested_write_targets", false, 4).map((target, index) => normalizeRepoPath2(target, `proposal.requested_write_targets[${index}]`));
  const semanticDelta = validateSemanticDelta(proposal.semantic_delta, operationKind);
  const writesTaskBasis = semanticDelta.kind === "task-state" && ["create-draft", "update-draft", "commit-replan", "commit-scope-amendment", "record-user-evidence"].includes(semanticDelta.action);
  const isSuccessor = semanticDelta.kind === "task-state" && semanticDelta.action === "create-draft" && semanticDelta.predecessor !== undefined;
  const targetCount = isSuccessor ? 3 : writesTaskBasis || operationKind === "lifecycle-transaction" && mode !== "supersede" || operationKind === "archive-transaction" ? 2 : 1;
  if (requestedTargets.length !== targetCount)
    fail3("RUNTIME_PATH_INVALID", `This Runtime proposal must name exactly ${targetCount} exact write target${targetCount === 1 ? "" : "s"}.`);
  if (writesTaskBasis) {
    const taskId = semanticDelta.kind === "task-state" && (semanticDelta.action === "create-draft" || semanticDelta.action === "update-draft") ? semanticDelta.task_id : sourceTuple.task_id;
    const expectedBasisPath = taskBasisRelativePath(sourceTuple.path, taskId);
    if (requestedTargets[1] !== expectedBasisPath) {
      fail3("RUNTIME_PATH_INVALID", `draft proposal second write target must be the identity-derived task basis ${expectedBasisPath}.`);
    }
  }
  if (operationKind === "task-state-transaction") {
    if (caller === "prepare-task") {
      if (mode === "default") {
        if (semanticDelta.kind !== "task-state" || !["clear-resume-review-gate", "create-draft", "update-draft", "record-user-evidence", ...CLAIM_EVIDENCE_MIGRATION_ACTIONS].includes(semanticDelta.action)) {
          fail3("RUNTIME_CALLER_NOT_BOUND", "prepare-task default mode is bound only to clear-resume-review-gate, create-draft, update-draft, migrate-claim-evidence, or an explicit user-evidence decision.");
        }
        if (semanticDelta.kind === "task-state" && semanticDelta.action === "migrate-claim-evidence") {
          const requiredPreconditions = ["current-task-is-active", "legacy-claim-evidence-state", "acceptance-bearing-plan"];
          const missingPreconditions = requiredPreconditions.filter((precondition) => !preconditions.includes(precondition));
          if (missingPreconditions.length > 0)
            fail3("RUNTIME_PRECONDITION_MISSING", `migrate-claim-evidence is missing required preconditions: ${missingPreconditions.join(", ")}.`);
        }
      } else if (mode === "confirm") {
        if (semanticDelta.kind !== "task-state" || semanticDelta.action !== "confirm-draft") {
          fail3("RUNTIME_CALLER_NOT_BOUND", "prepare-task confirm mode is bound only to confirm-draft.");
        }
      } else if (mode === "replan") {
        if (semanticDelta.kind !== "task-state" || !REPLAN_TASK_STATE_ACTIONS.includes(semanticDelta.action)) {
          fail3("RUNTIME_CALLER_NOT_BOUND", "prepare-task replan mode is bound only to the closed replan task-state action set.");
        }
      } else if (mode === "amend-scope") {
        if (semanticDelta.kind !== "task-state" || semanticDelta.action !== "commit-scope-amendment") {
          fail3("RUNTIME_CALLER_NOT_BOUND", "prepare-task amend-scope mode is bound only to commit-scope-amendment.");
        }
      } else {
        fail3("RUNTIME_MODE_INVALID", "prepare-task task-state proposals must use default, confirm, replan, or amend-scope mode.");
      }
    } else if (caller === "review-change") {
      if (mode !== "default")
        fail3("RUNTIME_MODE_INVALID", "review-change task-state proposals must use default mode.");
      if (semanticDelta.kind !== "task-state" || !["record-review-result", "record-evidence-challenge", "dismiss-evidence-challenge"].includes(semanticDelta.action)) {
        fail3("RUNTIME_CALLER_NOT_BOUND", "review-change is bound only to review results and evidence challenge assessment.");
      }
    } else if (caller === "execute-step") {
      if (!VNEXT_EXECUTE_STEP_MODES.includes(mode))
        fail3("RUNTIME_MODE_INVALID", "execute-step task-state proposals must use default or repair mode.");
      if (semanticDelta.kind !== "task-state" || !["step-progress", "consume-retained-review", "record-step-preflight", "extend-preflight", "retry-step"].includes(semanticDelta.action))
        fail3("RUNTIME_MODE_INVALID", "execute-step requires an execution task-state delta.");
    } else {
      fail3("RUNTIME_CALLER_NOT_BOUND", "task-state-transaction is not bound to task-lifecycle.");
    }
  } else if (operationKind === "finding-queue-transaction") {
    if (caller !== "execute-step" || mode !== "repair")
      fail3("RUNTIME_CALLER_NOT_BOUND", "finding-queue-transaction is bound only to execute-step:repair.");
    if (semanticDelta.kind !== "finding-queue")
      fail3("RUNTIME_MODE_INVALID", "repair mode requires a finding-queue proposal.");
  } else if (operationKind === "lifecycle-transaction") {
    if (caller !== "task-lifecycle" || !LIFECYCLE_MODES.includes(mode))
      fail3("RUNTIME_CALLER_NOT_BOUND", "lifecycle-transaction is bound only to task-lifecycle lifecycle modes.");
    if (semanticDelta.kind !== "lifecycle" || semanticDelta.action !== mode)
      fail3("RUNTIME_MODE_INVALID", "lifecycle mode and semantic transition must match.");
  } else if (operationKind === "inbox-record-transaction") {
    if (caller !== "capture-work-item" || mode !== "default")
      fail3("RUNTIME_CALLER_NOT_BOUND", "inbox-record-transaction is bound only to capture-work-item:record with default mode.");
    if (semanticDelta.kind !== "inbox-record" || semanticDelta.action !== "record")
      fail3("RUNTIME_MODE_INVALID", "inbox-record-transaction requires a record inbox semantic_delta.");
    const missingPreconditions = INBOX_CAPTURE_PRECONDITIONS.filter((precondition) => !preconditions.includes(precondition));
    if (missingPreconditions.length > 0)
      fail3("RUNTIME_PRECONDITION_MISSING", `capture-work-item is missing required preconditions: ${missingPreconditions.join(", ")}.`);
  } else if (operationKind === "contract-candidate-commit" || operationKind === "decision-record-transaction") {
    if (caller !== "close-task" || mode !== "default")
      fail3("RUNTIME_CALLER_NOT_BOUND", `${operationKind} is bound only to close-task default closure.`);
    if (semanticDelta.kind !== "knowledge" || semanticDelta.knowledge_kind !== (operationKind === "contract-candidate-commit" ? "contract" : "decision")) {
      fail3("RUNTIME_MODE_INVALID", `${operationKind} requires a matching knowledge semantic_delta.`);
    }
    const requiredPreconditions = ["archive-committed", "knowledge-admission-complete", "canonical-knowledge-target"];
    const missingPreconditions = requiredPreconditions.filter((precondition) => !preconditions.includes(precondition));
    if (missingPreconditions.length > 0)
      fail3("RUNTIME_PRECONDITION_MISSING", `${operationKind} is missing required preconditions: ${missingPreconditions.join(", ")}.`);
  } else {
    if (caller !== "close-task" || !CLOSE_TASK_MODES.includes(mode)) {
      fail3("RUNTIME_CALLER_NOT_BOUND", `${operationKind} is bound only to close-task default closure.`);
    }
    const expectedKind = operationKind === "archive-transaction" ? "archive" : operationKind === "project-status-transaction" ? "project-status" : "lesson-record";
    if (semanticDelta.kind !== expectedKind)
      fail3("RUNTIME_MODE_INVALID", `${operationKind} requires a ${expectedKind} semantic_delta.`);
  }
  const result = {
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: operationKind,
    caller,
    mode,
    source_tuple: sourceTuple,
    authority_evidence: authorityEvidence,
    semantic_delta: semanticDelta,
    preconditions,
    evidence_refs: evidenceRefs,
    idempotency_key: idempotencyKey,
    requested_write_targets: requestedTargets
  };
  const deltaRefs = semanticDelta.kind === "task-state" ? semanticDelta.evidence_refs : semanticDelta.kind === "finding-queue" ? semanticDelta.action === "admit" ? semanticDelta.finding.evidence_refs : semanticDelta.evidence_refs : semanticDelta.evidence_refs;
  const claimRefs = semanticDelta.kind === "task-state" && "claim_evidence" in semanticDelta && semanticDelta.claim_evidence !== undefined ? claimEvidenceRefs(semanticDelta.claim_evidence) : [];
  const reviewReceiptRefs = semanticDelta.kind === "task-state" && semanticDelta.action === "step-progress" && semanticDelta.review_receipt ? semanticDelta.review_receipt.evidence_refs : [];
  if (![...deltaRefs, ...reviewReceiptRefs, ...claimRefs].every((ref) => evidenceRefs.includes(ref))) {
    fail3("RUNTIME_EVIDENCE_INVALID", "proposal.evidence_refs must cover semantic_delta evidence_refs.");
  }
  return result;
}
function validateFinding(value, location) {
  const finding = expectRecord2(value, location);
  expectExactKeys2(finding, ["fingerprint", "category", "owner_task_id", "scope", "decision", "file", "failure_condition", "violated_invariant", "root_cause_status", "status", "repair_attempts", "max_repair_attempts", "evidence_refs", "review_cycle_id", "last_repair_wave_id", "admitted_at", "updated_at"], location);
  return {
    fingerprint: expectString2(finding.fingerprint, `${location}.fingerprint`, FINGERPRINT_PATTERN),
    category: expectText(finding.category, `${location}.category`, 256),
    owner_task_id: expectString2(finding.owner_task_id, `${location}.owner_task_id`),
    scope: expectEnum(finding.scope, ["admitted"], `${location}.scope`),
    decision: expectEnum(finding.decision, ["mechanical"], `${location}.decision`),
    file: normalizeRepoPath2(expectString2(finding.file, `${location}.file`), `${location}.file`),
    failure_condition: expectText(finding.failure_condition, `${location}.failure_condition`),
    violated_invariant: expectText(finding.violated_invariant, `${location}.violated_invariant`, 512),
    root_cause_status: expectEnum(finding.root_cause_status, ["confirmed", "bounded"], `${location}.root_cause_status`),
    status: expectEnum(finding.status, FINDING_STATUSES, `${location}.status`),
    repair_attempts: expectInteger(finding.repair_attempts, `${location}.repair_attempts`, 0, MAX_REPAIR_ATTEMPTS),
    max_repair_attempts: expectInteger(finding.max_repair_attempts, `${location}.max_repair_attempts`, 1, MAX_REPAIR_ATTEMPTS),
    evidence_refs: validateEvidenceRefs(finding.evidence_refs, `${location}.evidence_refs`),
    review_cycle_id: expectString2(finding.review_cycle_id, `${location}.review_cycle_id`, SAFE_KEY_PATTERN2),
    last_repair_wave_id: expectNullableString(finding.last_repair_wave_id, `${location}.last_repair_wave_id`, SAFE_KEY_PATTERN2),
    admitted_at: expectString2(finding.admitted_at, `${location}.admitted_at`),
    updated_at: expectString2(finding.updated_at, `${location}.updated_at`)
  };
}
function validateReviewCycle(value, location = "runtime_state.review_cycle") {
  const reviewCycle = expectRecord2(value, location);
  expectExactKeys2(reviewCycle, [...REVIEW_CYCLE_FIELDS], location);
  const id = expectString2(reviewCycle.id, `${location}.id`, SAFE_KEY_PATTERN2);
  const cyclePhase = expectEnum(reviewCycle.cycle_phase, REVIEW_CYCLE_PHASES, `${location}.cycle_phase`);
  const repairRound = expectInteger(reviewCycle.repair_round, `${location}.repair_round`, 0, MAX_REPAIR_ROUNDS);
  const countedRepairWaveIds = expectStringArray2(reviewCycle.counted_repair_wave_ids, `${location}.counted_repair_wave_ids`, true, MAX_REPAIR_ROUNDS);
  if (new Set(countedRepairWaveIds).size !== countedRepairWaveIds.length) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.counted_repair_wave_ids must be unique.`);
  }
  if (repairRound !== countedRepairWaveIds.length) {
    fail3("RUNTIME_STATE_CONFLICT", `${location}.repair_round must equal the number of counted repair waves.`);
  }
  const activeRepairWaveId = expectNullableString(reviewCycle.active_repair_wave_id, `${location}.active_repair_wave_id`, SAFE_KEY_PATTERN2);
  if (activeRepairWaveId !== null && !countedRepairWaveIds.includes(activeRepairWaveId)) {
    fail3("RUNTIME_STATE_CONFLICT", `${location}.active_repair_wave_id must be one of counted_repair_wave_ids.`);
  }
  if (activeRepairWaveId !== null && countedRepairWaveIds[countedRepairWaveIds.length - 1] !== activeRepairWaveId) {
    fail3("RUNTIME_STATE_CONFLICT", `${location}.active_repair_wave_id must be the latest counted repair wave.`);
  }
  const verificationNewFindingWaveUsed = expectBoolean(reviewCycle.verification_new_finding_wave_used, `${location}.verification_new_finding_wave_used`);
  const verificationNewFindingWaveId = expectNullableString(reviewCycle.verification_new_finding_wave_id, `${location}.verification_new_finding_wave_id`, SAFE_KEY_PATTERN2);
  if (!verificationNewFindingWaveUsed && verificationNewFindingWaveId !== null) {
    fail3("RUNTIME_STATE_CONFLICT", `${location}.verification_new_finding_wave_id must be null before the verification admission wave is used.`);
  }
  if (verificationNewFindingWaveId !== null && activeRepairWaveId !== null) {
    fail3("RUNTIME_STATE_CONFLICT", `${location}.verification_new_finding_wave_id cannot remain open while a repair wave is active.`);
  }
  if (verificationNewFindingWaveUsed && cyclePhase !== "verification") {
    fail3("RUNTIME_STATE_CONFLICT", `${location}.cycle_phase must be verification after the verification admission wave is used.`);
  }
  return {
    id,
    cycle_phase: cyclePhase,
    repair_round: repairRound,
    counted_repair_wave_ids: countedRepairWaveIds,
    active_repair_wave_id: activeRepairWaveId,
    verification_new_finding_wave_used: verificationNewFindingWaveUsed,
    verification_new_finding_wave_id: verificationNewFindingWaveId
  };
}
function validateArchiveAuditLogEntry(value, location, taskId, taskSlug) {
  const archiveAuditKeys = [
    "action",
    "idempotency_key",
    "operation_kind",
    "caller",
    "mode",
    "task_id",
    "task_slug",
    "document_id",
    "from_workflow_status",
    "from_lifecycle_state",
    "to_workflow_status",
    "to_lifecycle_state",
    "source_revision",
    "archive_path",
    "archive_revision",
    "closure_delta_digest",
    "authority_evidence",
    "evidence_refs",
    "lesson_admission",
    "knowledge_admissions",
    "recorded_at"
  ];
  const missingArchiveAuditKeys = archiveAuditKeys.filter((key) => key !== "knowledge_admissions" && !(key in value));
  const extraArchiveAuditKeys = Object.keys(value).filter((key) => !archiveAuditKeys.includes(key));
  if (missingArchiveAuditKeys.length > 0 || extraArchiveAuditKeys.length > 0) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} keys mismatch; missing=[${missingArchiveAuditKeys.join(", ")}], unexpected=[${extraArchiveAuditKeys.join(", ")}].`);
  }
  const entryTaskId = expectString2(value.task_id, `${location}.task_id`);
  const entryTaskSlug = expectString2(value.task_slug, `${location}.task_slug`);
  try {
    validateTaskId(entryTaskId);
    validateTaskSlug(entryTaskSlug);
  } catch (error) {
    fail3("RUNTIME_SCHEMA_INVALID", error instanceof Error ? error.message : String(error));
  }
  if (entryTaskId !== taskId || entryTaskSlug !== taskSlug)
    fail3("RUNTIME_STATE_CONFLICT", `${location} identity does not match runtime_state.`);
  const documentId = expectString2(value.document_id, `${location}.document_id`);
  if (!DOCUMENT_ID_PATTERN.test(documentId))
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.document_id is invalid.`);
  const sourceRevision = expectString2(value.source_revision, `${location}.source_revision`);
  const archiveRevision = expectString2(value.archive_revision, `${location}.archive_revision`);
  const closureDeltaDigest = expectString2(value.closure_delta_digest, `${location}.closure_delta_digest`);
  if (!/^[a-f0-9]{64}$/.test(sourceRevision) || !/^[a-f0-9]{64}$/.test(archiveRevision) || !/^[a-f0-9]{64}$/.test(closureDeltaDigest)) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} revisions and digest must be SHA-256.`);
  }
  const archivePath = normalizeRepoPath2(expectString2(value.archive_path, `${location}.archive_path`), `${location}.archive_path`);
  if (!/^TASKS\/TASK-[0-9]{3,}-[a-z0-9]+(?:-[a-z0-9]+)*\.md$/.test(archivePath)) {
    fail3("RUNTIME_PATH_INVALID", `${location}.archive_path must be a canonical task archive path.`);
  }
  if (value.action !== "archive" || value.operation_kind !== "archive-transaction" || value.caller !== "close-task" || value.mode !== "default") {
    fail3("RUNTIME_STATE_CONFLICT", `${location} archive audit has an invalid operation binding.`);
  }
  if (value.from_workflow_status !== "active" || value.from_lifecycle_state !== "active" || value.to_workflow_status !== "closed" || value.to_lifecycle_state !== "archived") {
    fail3("RUNTIME_STATE_CONFLICT", `${location} archive audit has an invalid terminal transition.`);
  }
  return {
    action: "archive",
    idempotency_key: expectString2(value.idempotency_key, `${location}.idempotency_key`, SAFE_KEY_PATTERN2),
    operation_kind: "archive-transaction",
    caller: "close-task",
    mode: "default",
    task_id: entryTaskId,
    task_slug: entryTaskSlug,
    document_id: documentId,
    from_workflow_status: "active",
    from_lifecycle_state: "active",
    to_workflow_status: "closed",
    to_lifecycle_state: "archived",
    source_revision: sourceRevision,
    archive_path: archivePath,
    archive_revision: archiveRevision,
    closure_delta_digest: closureDeltaDigest,
    authority_evidence: validateAuthorityEvidence2(value.authority_evidence),
    evidence_refs: validateEvidenceRefs(value.evidence_refs, `${location}.evidence_refs`),
    lesson_admission: validateLessonAdmission(value.lesson_admission, `${location}.lesson_admission`),
    knowledge_admissions: value.knowledge_admissions === undefined ? emptyKnowledgeAdmissionBundle() : validateKnowledgeAdmissionBundle(value.knowledge_admissions, `${location}.knowledge_admissions`),
    recorded_at: expectString2(value.recorded_at, `${location}.recorded_at`)
  };
}
function successorSnapshotPath(currentPath, documentId, revision) {
  return path12.posix.join(path12.posix.dirname(currentPath), "task-history", documentId, `${revision}.successor.md`);
}
function validateSuccessorDecision(value) {
  const d = expectRecord2(value, "successor decision");
  expectExactKeys2(d, ["task_id", "document_id", "source_revision", "basis_revision", "decision_source", "decision_text", "obligations"], "successor decision");
  if (!Array.isArray(d.obligations) || d.obligations.length > 256)
    fail3("SUCCESSOR_OBLIGATIONS_INVALID", "The predecessor obligation map must be bounded.");
  const obligations = d.obligations.map((raw) => {
    const row = expectRecord2(raw, "successor obligation");
    expectExactKeys2(row, ["prior_key", "disposition", "successor_claim_id", "reason"], "successor obligation");
    return {
      prior_key: expectText(row.prior_key, "prior_key"),
      disposition: expectEnum(row.disposition, ["carry-forward", "retired"], "disposition"),
      successor_claim_id: row.successor_claim_id === null ? null : expectString2(row.successor_claim_id, "successor_claim_id", CLAIM_ID_PATTERN),
      reason: expectText(row.reason, "reason")
    };
  });
  if (new Set(obligations.map((row) => row.prior_key)).size !== obligations.length)
    fail3("SUCCESSOR_OBLIGATIONS_INVALID", "A predecessor obligation must be disposed exactly once.");
  const taskId = expectText(d.task_id, "task_id");
  validateTaskId(taskId);
  return {
    task_id: taskId,
    document_id: expectString2(d.document_id, "document_id", DOCUMENT_ID_PATTERN),
    source_revision: expectString2(d.source_revision, "source_revision", SHA256_PATTERN2),
    basis_revision: expectString2(d.basis_revision, "basis_revision", SHA256_PATTERN2),
    decision_source: expectText(d.decision_source, "decision_source"),
    decision_text: expectVerbatim(d.decision_text, "decision_text", 32768),
    obligations
  };
}
function predecessorObligationKeys(current) {
  return [
    ...(current.runtimeState.claim_evidence ?? []).flatMap((claim) => claim.slots.map((slot) => `claim:${claim.claim_id}/slot:${slot.slot_id}`)),
    ...current.runtimeState.findings.filter((f) => !["resolved", "rejected"].includes(f.status)).map((f) => `finding:${f.fingerprint}`)
  ].sort();
}
function assertSuccessorDecision(root2, current, delta) {
  const d = delta.predecessor;
  if (delta.action !== "create-draft" || current.runtimeState.workflow_status !== "superseded" || current.runtimeState.lifecycle_state !== "active")
    fail3("SUCCESSOR_STATE_INVALID", "Only an already superseded task can have an explicitly requested successor.");
  if (d.task_id !== current.runtimeState.task_id || d.document_id !== current.sourceTuple.document_id || d.source_revision !== current.sourceTuple.revision)
    fail3("SUCCESSOR_SOURCE_STALE", "The successor decision must bind the exact superseded predecessor.");
  const priorBasis = readCanonicalTaskBasis(root2, current);
  if (priorBasis.revision !== d.basis_revision)
    fail3("SUCCESSOR_SOURCE_STALE", "The predecessor Task Basis changed.");
  if (!current.runtimeState.execution_log.some((e) => ("action" in e) && e.action === "supersede"))
    fail3("SUCCESSOR_STATE_INVALID", "A superseded status alone does not establish a retained invalidation.");
  const statement = [delta.task_basis.original_request, ...delta.task_basis.user_decisions].find((item) => item.source === d.decision_source);
  if (!statement || statement.verbatim !== d.decision_text)
    fail3("SUCCESSOR_AUTHORITY_INVALID", "Retain the exact user replacement decision in the new Task Basis; do not infer it from supersede.");
  const expected = predecessorObligationKeys(current);
  if (digest3(expected) !== digest3(d.obligations.map((row) => row.prior_key).sort()))
    fail3("SUCCESSOR_OBLIGATIONS_INVALID", "Every old claim slot and open finding needs an explicit carry-forward or retirement decision.");
  for (const row of d.obligations) {
    if (row.disposition === "carry-forward" ? !delta.claim_evidence?.some((c) => c.claim_id === row.successor_claim_id) : row.successor_claim_id !== null)
      fail3("SUCCESSOR_OBLIGATIONS_INVALID", "Carried obligations must name a new claim; retired obligations remain only in predecessor history.");
  }
  if (delta.claim_evidence?.some((c) => c.slots.some((slot) => slot.report || slot.user_decision || slot.prerequisite_receipt || COMPLETE_CLAIM_EVIDENCE_DISPOSITIONS.includes(slot.disposition))))
    fail3("SUCCESSOR_EVIDENCE_INVALID", "A successor starts with unexecuted obligations; old PASS and waivers never transfer automatically.");
}
function validateDraftAuditLogEntry(value, location, taskId, taskSlug) {
  const action = expectEnum(value.action, DRAFT_AUDIT_ACTIONS, `${location}.action`);
  const requiredKeys = [
    "action",
    "idempotency_key",
    "operation_kind",
    "caller",
    "mode",
    "from_task_id",
    "from_task_slug",
    "from_document_id",
    "task_id",
    "task_slug",
    "document_id",
    "from_workflow_status",
    "from_lifecycle_state",
    "to_workflow_status",
    "to_lifecycle_state",
    "source_revision",
    "authority_evidence",
    "evidence_refs",
    "recorded_at"
  ];
  const conditionalKeys = action === "confirm-draft" ? ["draft_revision"] : ["definition_digest", "claim_evidence_digest", "predecessor"];
  const requiredConditionalKeys = action === "confirm-draft" ? ["draft_revision"] : ["definition_digest"];
  const extra = Object.keys(value).filter((key) => !requiredKeys.includes(key) && !conditionalKeys.includes(key));
  const missing = [...requiredKeys, ...requiredConditionalKeys].filter((key) => !(key in value));
  if (missing.length > 0 || extra.length > 0) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location} audit keys mismatch; missing=[${missing.join(", ")}], unexpected=[${extra.join(", ")}].`);
  }
  const fromTaskId = expectString2(value.from_task_id, `${location}.from_task_id`);
  const fromTaskSlug = expectString2(value.from_task_slug, `${location}.from_task_slug`);
  const entryTaskId = expectString2(value.task_id, `${location}.task_id`);
  const entryTaskSlug = expectString2(value.task_slug, `${location}.task_slug`);
  try {
    validateTaskId(fromTaskId);
    validateTaskSlug(fromTaskSlug);
    validateTaskId(entryTaskId);
    validateTaskSlug(entryTaskSlug);
  } catch (error) {
    fail3("RUNTIME_SCHEMA_INVALID", error instanceof Error ? error.message : String(error));
  }
  if (entryTaskId !== taskId || entryTaskSlug !== taskSlug)
    fail3("RUNTIME_STATE_CONFLICT", `${location} target identity does not match runtime_state.`);
  const fromDocumentId = expectString2(value.from_document_id, `${location}.from_document_id`);
  const documentId = expectString2(value.document_id, `${location}.document_id`);
  if (!DOCUMENT_ID_PATTERN.test(fromDocumentId) || !DOCUMENT_ID_PATTERN.test(documentId)) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.document_id fields are invalid.`);
  }
  const fromWorkflowStatus = expectEnum(value.from_workflow_status, CURRENT_TASK_WORKFLOW_STATUSES, `${location}.from_workflow_status`);
  const fromLifecycleState = expectEnum(value.from_lifecycle_state, TASK_LIFECYCLE_STATES, `${location}.from_lifecycle_state`);
  const toWorkflowStatus = expectEnum(value.to_workflow_status, CURRENT_TASK_WORKFLOW_STATUSES, `${location}.to_workflow_status`);
  const toLifecycleState = expectEnum(value.to_lifecycle_state, TASK_LIFECYCLE_STATES, `${location}.to_lifecycle_state`);
  try {
    validateCurrentTaskStatusTuple(fromWorkflowStatus, fromLifecycleState);
    validateCurrentTaskStatusTuple(toWorkflowStatus, toLifecycleState);
  } catch (error) {
    fail3("RUNTIME_STATE_CONFLICT", error instanceof Error ? error.message : String(error));
  }
  const sourceRevision = expectString2(value.source_revision, `${location}.source_revision`);
  if (!/^[a-f0-9]{64}$/.test(sourceRevision))
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.source_revision must be SHA-256.`);
  const authorityEvidence = validateAuthorityEvidence2(value.authority_evidence);
  const evidenceRefs = validateEvidenceRefs(value.evidence_refs, `${location}.evidence_refs`);
  const recordedAt = expectString2(value.recorded_at, `${location}.recorded_at`);
  if (action === "create-draft" || action === "update-draft") {
    if (value.operation_kind !== "task-state-transaction" || value.caller !== "prepare-task" || value.mode !== "default") {
      fail3("RUNTIME_STATE_CONFLICT", `${location} ${action} audit has an invalid operation binding.`);
    }
    const expectedFromIdentity = action === "create-draft" ? value.predecessor === undefined ? ["closed", "archived"] : ["superseded", "active"] : ["draft", "active"];
    if (fromWorkflowStatus !== expectedFromIdentity[0] || fromLifecycleState !== expectedFromIdentity[1] || toWorkflowStatus !== "draft" || toLifecycleState !== "active") {
      fail3("RUNTIME_STATE_CONFLICT", `${location} ${action} audit has an invalid transition.`);
    }
    const definitionDigest = expectString2(value.definition_digest, `${location}.definition_digest`);
    if (!/^[a-f0-9]{64}$/.test(definitionDigest))
      fail3("RUNTIME_SCHEMA_INVALID", `${location}.definition_digest must be SHA-256.`);
    const claimEvidenceDigest = value.claim_evidence_digest === undefined ? undefined : expectString2(value.claim_evidence_digest, `${location}.claim_evidence_digest`);
    if (claimEvidenceDigest !== undefined && !/^[a-f0-9]{64}$/.test(claimEvidenceDigest))
      fail3("RUNTIME_SCHEMA_INVALID", `${location}.claim_evidence_digest must be SHA-256.`);
    return {
      action,
      idempotency_key: expectString2(value.idempotency_key, `${location}.idempotency_key`, SAFE_KEY_PATTERN2),
      operation_kind: "task-state-transaction",
      caller: "prepare-task",
      mode: "default",
      from_task_id: fromTaskId,
      from_task_slug: fromTaskSlug,
      from_document_id: fromDocumentId,
      task_id: entryTaskId,
      task_slug: entryTaskSlug,
      document_id: documentId,
      from_workflow_status: fromWorkflowStatus,
      from_lifecycle_state: fromLifecycleState,
      to_workflow_status: "draft",
      to_lifecycle_state: "active",
      source_revision: sourceRevision,
      authority_evidence: authorityEvidence,
      evidence_refs: evidenceRefs,
      definition_digest: definitionDigest,
      ...value.predecessor === undefined ? {} : { predecessor: validateSuccessorDecision(value.predecessor) },
      ...claimEvidenceDigest === undefined ? {} : { claim_evidence_digest: claimEvidenceDigest },
      recorded_at: recordedAt
    };
  }
  if (value.operation_kind !== "task-state-transaction" || value.caller !== "prepare-task" || value.mode !== "confirm") {
    fail3("RUNTIME_STATE_CONFLICT", `${location} confirm-draft audit has an invalid operation binding.`);
  }
  if (fromWorkflowStatus !== "draft" || fromLifecycleState !== "active" || toWorkflowStatus !== "active" || toLifecycleState !== "active") {
    fail3("RUNTIME_STATE_CONFLICT", `${location} confirm-draft audit has an invalid transition.`);
  }
  const draftRevision = expectString2(value.draft_revision, `${location}.draft_revision`);
  if (!/^[a-f0-9]{64}$/.test(draftRevision))
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.draft_revision must be SHA-256.`);
  return {
    action: "confirm-draft",
    idempotency_key: expectString2(value.idempotency_key, `${location}.idempotency_key`, SAFE_KEY_PATTERN2),
    operation_kind: "task-state-transaction",
    caller: "prepare-task",
    mode: "confirm",
    from_task_id: fromTaskId,
    from_task_slug: fromTaskSlug,
    from_document_id: fromDocumentId,
    task_id: entryTaskId,
    task_slug: entryTaskSlug,
    document_id: documentId,
    from_workflow_status: "draft",
    from_lifecycle_state: "active",
    to_workflow_status: "active",
    to_lifecycle_state: "active",
    source_revision: sourceRevision,
    authority_evidence: authorityEvidence,
    evidence_refs: evidenceRefs,
    draft_revision: draftRevision,
    recorded_at: recordedAt
  };
}
function validateClaimEvidenceMigrationAuditLogEntry(value, location, taskId, taskSlug) {
  expectExactKeys2(value, [
    "action",
    "idempotency_key",
    "operation_kind",
    "caller",
    "mode",
    "from_task_id",
    "from_task_slug",
    "from_document_id",
    "task_id",
    "task_slug",
    "document_id",
    "from_workflow_status",
    "from_lifecycle_state",
    "to_workflow_status",
    "to_lifecycle_state",
    "source_revision",
    "authority_evidence",
    "evidence_refs",
    "claim_evidence_digest",
    "recorded_at"
  ], location);
  if (value.action !== "migrate-claim-evidence" || value.operation_kind !== "task-state-transaction" || value.caller !== "prepare-task" || value.mode !== "default") {
    fail3("RUNTIME_STATE_CONFLICT", `${location} claim evidence migration audit has an invalid operation binding.`);
  }
  const fromTaskId = expectString2(value.from_task_id, `${location}.from_task_id`);
  const fromTaskSlug = expectString2(value.from_task_slug, `${location}.from_task_slug`);
  const entryTaskId = expectString2(value.task_id, `${location}.task_id`);
  const entryTaskSlug = expectString2(value.task_slug, `${location}.task_slug`);
  try {
    validateTaskId(fromTaskId);
    validateTaskSlug(fromTaskSlug);
    validateTaskId(entryTaskId);
    validateTaskSlug(entryTaskSlug);
  } catch (error) {
    fail3("RUNTIME_SCHEMA_INVALID", error instanceof Error ? error.message : String(error));
  }
  if (entryTaskId !== taskId || entryTaskSlug !== taskSlug)
    fail3("RUNTIME_STATE_CONFLICT", `${location} target identity does not match runtime_state.`);
  if (fromTaskId !== entryTaskId || fromTaskSlug !== entryTaskSlug)
    fail3("RUNTIME_STATE_CONFLICT", `${location} migration must preserve task identity.`);
  const fromDocumentId = expectString2(value.from_document_id, `${location}.from_document_id`);
  const documentId = expectString2(value.document_id, `${location}.document_id`);
  if (!DOCUMENT_ID_PATTERN.test(fromDocumentId) || !DOCUMENT_ID_PATTERN.test(documentId)) {
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.document_id fields are invalid.`);
  }
  if (fromDocumentId !== documentId)
    fail3("RUNTIME_STATE_CONFLICT", `${location} migration must preserve document_id.`);
  if (value.from_workflow_status !== "active" || value.from_lifecycle_state !== "active" || value.to_workflow_status !== "active" || value.to_lifecycle_state !== "active") {
    fail3("RUNTIME_STATE_CONFLICT", `${location} migration must preserve the active + active lifecycle tuple.`);
  }
  const sourceRevision = expectString2(value.source_revision, `${location}.source_revision`);
  if (!SHA256_PATTERN2.test(sourceRevision))
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.source_revision must be SHA-256.`);
  const authorityEvidence = validateAuthorityEvidence2(value.authority_evidence);
  const evidenceRefs = validateEvidenceRefs(value.evidence_refs, `${location}.evidence_refs`);
  const claimEvidenceDigest = expectString2(value.claim_evidence_digest, `${location}.claim_evidence_digest`);
  if (!SHA256_PATTERN2.test(claimEvidenceDigest))
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.claim_evidence_digest must be SHA-256.`);
  return {
    action: "migrate-claim-evidence",
    idempotency_key: expectString2(value.idempotency_key, `${location}.idempotency_key`, SAFE_KEY_PATTERN2),
    operation_kind: "task-state-transaction",
    caller: "prepare-task",
    mode: "default",
    from_task_id: fromTaskId,
    from_task_slug: fromTaskSlug,
    from_document_id: fromDocumentId,
    task_id: entryTaskId,
    task_slug: entryTaskSlug,
    document_id: documentId,
    from_workflow_status: "active",
    from_lifecycle_state: "active",
    to_workflow_status: "active",
    to_lifecycle_state: "active",
    source_revision: sourceRevision,
    authority_evidence: authorityEvidence,
    evidence_refs: evidenceRefs,
    claim_evidence_digest: claimEvidenceDigest,
    recorded_at: expectString2(value.recorded_at, `${location}.recorded_at`)
  };
}
function validateExecutionLogEntry(value, location, taskId, taskSlug) {
  const record = expectRecord2(value, location);
  if (record.action === "migrate-claim-evidence")
    return validateClaimEvidenceMigrationAuditLogEntry(record, location, taskId, taskSlug);
  if (DRAFT_AUDIT_ACTIONS.includes(record.action))
    return validateDraftAuditLogEntry(record, location, taskId, taskSlug);
  if (record.action === "archive")
    return validateArchiveAuditLogEntry(record, location, taskId, taskSlug);
  if ("action" in record) {
    const requiredKeys = [
      "action",
      "idempotency_key",
      "operation_kind",
      "caller",
      "mode",
      "task_id",
      "task_slug",
      "document_id",
      "from_workflow_status",
      "from_lifecycle_state",
      "to_workflow_status",
      "to_lifecycle_state",
      "source_revision",
      "authority_evidence",
      "evidence_refs",
      "recorded_at"
    ];
    const optionalKeys = ["partial_diff_disposition", "invalidation_kind", "invalidation_reason", "candidate_digest", "correction_reason"];
    const missing = requiredKeys.filter((key) => !(key in record));
    const extra = Object.keys(record).filter((key) => !requiredKeys.includes(key) && !optionalKeys.includes(key));
    if (missing.length > 0 || extra.length > 0) {
      fail3("RUNTIME_SCHEMA_INVALID", `${location} audit keys mismatch; missing=[${missing.join(", ")}], unexpected=[${extra.join(", ")}].`);
    }
    const action = expectEnum(record.action, REPLAN_AUDIT_ACTIONS, `${location}.action`);
    const operationKind = expectEnum(record.operation_kind, ["task-state-transaction", "lifecycle-transaction"], `${location}.operation_kind`);
    const caller = expectEnum(record.caller, ["prepare-task", "task-lifecycle"], `${location}.caller`);
    const mode = expectString2(record.mode, `${location}.mode`);
    const entryTaskId = expectString2(record.task_id, `${location}.task_id`);
    const entryTaskSlug = expectString2(record.task_slug, `${location}.task_slug`);
    const documentId = expectString2(record.document_id, `${location}.document_id`);
    if (!DOCUMENT_ID_PATTERN.test(documentId))
      fail3("RUNTIME_SCHEMA_INVALID", `${location}.document_id is invalid.`);
    try {
      validateTaskId(entryTaskId);
      validateTaskSlug(entryTaskSlug);
    } catch (error) {
      fail3("RUNTIME_SCHEMA_INVALID", error instanceof Error ? error.message : String(error));
    }
    if (entryTaskId !== taskId || entryTaskSlug !== taskSlug)
      fail3("RUNTIME_STATE_CONFLICT", `${location} identity does not match runtime_state.`);
    const fromWorkflowStatus = expectEnum(record.from_workflow_status, CURRENT_TASK_WORKFLOW_STATUSES, `${location}.from_workflow_status`);
    const fromLifecycleState = expectEnum(record.from_lifecycle_state, TASK_LIFECYCLE_STATES, `${location}.from_lifecycle_state`);
    const toWorkflowStatus = expectEnum(record.to_workflow_status, CURRENT_TASK_WORKFLOW_STATUSES, `${location}.to_workflow_status`);
    const toLifecycleState = expectEnum(record.to_lifecycle_state, TASK_LIFECYCLE_STATES, `${location}.to_lifecycle_state`);
    try {
      validateCurrentTaskStatusTuple(fromWorkflowStatus, fromLifecycleState);
      validateCurrentTaskStatusTuple(toWorkflowStatus, toLifecycleState);
    } catch (error) {
      fail3("RUNTIME_STATE_CONFLICT", error instanceof Error ? error.message : String(error));
    }
    const sourceRevision = expectString2(record.source_revision, `${location}.source_revision`);
    if (!/^[a-f0-9]{64}$/.test(sourceRevision))
      fail3("RUNTIME_SCHEMA_INVALID", `${location}.source_revision must be SHA-256.`);
    const authorityEvidence = validateAuthorityEvidence2(record.authority_evidence);
    const evidenceRefs = validateEvidenceRefs(record.evidence_refs, `${location}.evidence_refs`);
    const recordedAt = expectString2(record.recorded_at, `${location}.recorded_at`);
    if (action === "supersede") {
      if (record.candidate_digest !== undefined || record.correction_reason !== undefined)
        fail3("RUNTIME_SCHEMA_INVALID", `${location} supersede audit cannot bind a correction candidate.`);
      if (operationKind !== "lifecycle-transaction" || caller !== "task-lifecycle" || mode !== "supersede") {
        fail3("RUNTIME_STATE_CONFLICT", `${location} supersede audit has an invalid operation binding.`);
      }
      if (!["active", "blocked_by_replan"].includes(fromWorkflowStatus) || fromLifecycleState !== "active" || toWorkflowStatus !== "superseded" || toLifecycleState !== "active") {
        fail3("RUNTIME_STATE_CONFLICT", `${location} supersede audit has an invalid transition.`);
      }
      if (record.partial_diff_disposition === undefined || record.invalidation_kind === undefined || record.invalidation_reason === undefined) {
        fail3("RUNTIME_SCHEMA_INVALID", `${location} supersede audit must preserve invalidation and partial-diff evidence.`);
      }
      const partialDiffDisposition = validatePartialDiffDisposition(record.partial_diff_disposition, `${location}.partial_diff_disposition`);
      const invalidationKind = expectEnum(record.invalidation_kind, ["goal", "scope", "acceptance"], `${location}.invalidation_kind`);
      const invalidationReason = expectText(record.invalidation_reason, `${location}.invalidation_reason`);
      return {
        action,
        idempotency_key: expectString2(record.idempotency_key, `${location}.idempotency_key`, SAFE_KEY_PATTERN2),
        operation_kind: operationKind,
        caller,
        mode: "supersede",
        task_id: entryTaskId,
        task_slug: entryTaskSlug,
        document_id: documentId,
        from_workflow_status: fromWorkflowStatus,
        from_lifecycle_state: fromLifecycleState,
        to_workflow_status: toWorkflowStatus,
        to_lifecycle_state: toLifecycleState,
        source_revision: sourceRevision,
        authority_evidence: authorityEvidence,
        evidence_refs: evidenceRefs,
        partial_diff_disposition: partialDiffDisposition,
        invalidation_kind: invalidationKind,
        invalidation_reason: invalidationReason,
        recorded_at: recordedAt
      };
    }
    const scopeAmendment = action === "commit-scope-amendment";
    if (operationKind !== "task-state-transaction" || caller !== "prepare-task" || mode !== (scopeAmendment ? "amend-scope" : "replan")) {
      fail3("RUNTIME_STATE_CONFLICT", `${location} ${scopeAmendment ? "scope-amendment" : "replan"} audit has an invalid operation binding.`);
    }
    if (record.partial_diff_disposition !== undefined || record.invalidation_kind !== undefined || record.invalidation_reason !== undefined) {
      fail3("RUNTIME_SCHEMA_INVALID", `${location} non-supersede audit must not carry supersede-only evidence.`);
    }
    const candidateDigest = record.candidate_digest === undefined ? undefined : expectString2(record.candidate_digest, `${location}.candidate_digest`, /^[a-f0-9]{64}$/);
    if (!["commit-replan", "commit-scope-amendment"].includes(action) && candidateDigest !== undefined)
      fail3("RUNTIME_SCHEMA_INVALID", `${location} candidate digest belongs only to confirmed replans.`);
    if (action === "commit-scope-amendment" && candidateDigest === undefined)
      fail3("RUNTIME_SCHEMA_INVALID", `${location} scope amendment audit must bind a confirmed candidate digest.`);
    const correctionReason = record.correction_reason === undefined ? undefined : expectText(record.correction_reason, `${location}.correction_reason`, 1024);
    if (candidateDigest === undefined !== (correctionReason === undefined))
      fail3("RUNTIME_SCHEMA_INVALID", `${location} correction reason must bind an exact candidate digest.`);
    const expectedTransition = action === "mark-replan-blocked" ? ["active", "active", "blocked_by_replan", "active"] : action === "clear-replan-block" ? ["blocked_by_replan", "active", "active", "active"] : ["superseded", "active", "active", "active"];
    const activeCorrection = ["commit-replan", "commit-scope-amendment"].includes(action) && candidateDigest !== undefined && ["active", "blocked_by_replan"].includes(fromWorkflowStatus) && fromLifecycleState === "active" && toWorkflowStatus === "active" && toLifecycleState === "active";
    if (!activeCorrection && (fromWorkflowStatus !== expectedTransition[0] || fromLifecycleState !== expectedTransition[1] || toWorkflowStatus !== expectedTransition[2] || toLifecycleState !== expectedTransition[3])) {
      fail3("RUNTIME_STATE_CONFLICT", `${location} replan audit has an invalid transition.`);
    }
    return {
      action,
      idempotency_key: expectString2(record.idempotency_key, `${location}.idempotency_key`, SAFE_KEY_PATTERN2),
      operation_kind: operationKind,
      caller,
      mode: scopeAmendment ? "amend-scope" : "replan",
      task_id: entryTaskId,
      task_slug: entryTaskSlug,
      document_id: documentId,
      from_workflow_status: fromWorkflowStatus,
      from_lifecycle_state: fromLifecycleState,
      to_workflow_status: toWorkflowStatus,
      to_lifecycle_state: toLifecycleState,
      source_revision: sourceRevision,
      authority_evidence: authorityEvidence,
      evidence_refs: evidenceRefs,
      ...candidateDigest === undefined ? {} : { candidate_digest: candidateDigest },
      ...correctionReason === undefined ? {} : { correction_reason: correctionReason },
      recorded_at: recordedAt
    };
  }
  const executionLogKeys = [
    "idempotency_key",
    "mode",
    "step_id",
    "status",
    "evidence_refs",
    "note",
    "repair_fingerprint",
    "repair_fingerprints",
    "repair_wave_id",
    "change_set_id",
    "checkpoint",
    "advancement",
    "next_step_id",
    "review_receipt",
    "claim_evidence",
    "execution_result",
    "recorded_at"
  ];
  const optionalExecutionLogKeys = ["note", "repair_fingerprint", "repair_fingerprints", "repair_wave_id", "change_set_id", "checkpoint", "advancement", "next_step_id", "review_receipt", "claim_evidence", "execution_result"];
  const missingExecutionLogKeys = executionLogKeys.filter((key) => !optionalExecutionLogKeys.includes(key) && !(key in record));
  const extraExecutionLogKeys = Object.keys(record).filter((key) => !executionLogKeys.includes(key));
  if (missingExecutionLogKeys.length > 0 || extraExecutionLogKeys.length > 0)
    fail3("RUNTIME_SCHEMA_INVALID", `${location} keys mismatch; missing=[${missingExecutionLogKeys.join(", ")}], unexpected=[${extraExecutionLogKeys.join(", ")}].`);
  const result = {
    idempotency_key: expectString2(record.idempotency_key, `${location}.idempotency_key`, SAFE_KEY_PATTERN2),
    mode: expectEnum(record.mode, VNEXT_EXECUTE_STEP_MODES, `${location}.mode`),
    step_id: expectString2(record.step_id, `${location}.step_id`, STEP_ID_PATTERN2),
    status: expectEnum(record.status, STEP_STATUSES, `${location}.status`),
    evidence_refs: validateEvidenceRefs(record.evidence_refs, `${location}.evidence_refs`),
    recorded_at: expectString2(record.recorded_at, `${location}.recorded_at`)
  };
  if (record.note !== undefined && record.note !== null)
    result.note = expectText(record.note, `${location}.note`);
  if (record.repair_fingerprint !== undefined) {
    result.repair_fingerprint = expectString2(record.repair_fingerprint, `${location}.repair_fingerprint`, FINGERPRINT_PATTERN);
    if (result.mode !== "repair")
      fail3("RUNTIME_STATE_CONFLICT", `${location}.repair_fingerprint is only valid for repair execution records.`);
  }
  if (record.repair_fingerprints !== undefined) {
    result.repair_fingerprints = expectStringArray2(record.repair_fingerprints, `${location}.repair_fingerprints`, false, MAX_FINDINGS).map((fingerprint, index) => expectString2(fingerprint, `${location}.repair_fingerprints[${index}]`, FINGERPRINT_PATTERN));
    if (new Set(result.repair_fingerprints).size !== result.repair_fingerprints.length)
      fail3("RUNTIME_SCHEMA_INVALID", `${location}.repair_fingerprints must be unique.`);
    if (result.mode !== "repair")
      fail3("RUNTIME_STATE_CONFLICT", `${location}.repair_fingerprints is only valid for repair execution records.`);
  }
  if (record.repair_wave_id !== undefined)
    result.repair_wave_id = expectString2(record.repair_wave_id, `${location}.repair_wave_id`, SAFE_KEY_PATTERN2);
  if (result.repair_fingerprint !== undefined && result.repair_fingerprints !== undefined)
    fail3("RUNTIME_STATE_CONFLICT", `${location} must not mix legacy and grouped repair fingerprints.`);
  if (result.repair_fingerprints !== undefined !== (result.repair_wave_id !== undefined))
    fail3("RUNTIME_STATE_CONFLICT", `${location}.repair_fingerprints and repair_wave_id must appear together.`);
  if (record.change_set_id !== undefined)
    result.change_set_id = expectString2(record.change_set_id, `${location}.change_set_id`, SAFE_KEY_PATTERN2);
  if (record.checkpoint !== undefined)
    result.checkpoint = expectEnum(record.checkpoint, ["required", "not-required"], `${location}.checkpoint`);
  if (record.advancement !== undefined)
    result.advancement = expectEnum(record.advancement, STEP_ADVANCEMENT_OUTCOMES, `${location}.advancement`);
  if (record.next_step_id !== undefined)
    result.next_step_id = expectNullableString(record.next_step_id, `${location}.next_step_id`, STEP_ID_PATTERN2);
  if (record.review_receipt !== undefined)
    result.review_receipt = validateStepReviewReceipt(record.review_receipt, `${location}.review_receipt`);
  if (record.claim_evidence !== undefined)
    result.claim_evidence = validateClaimEvidence(record.claim_evidence, `${location}.claim_evidence`);
  if (record.execution_result !== undefined)
    result.execution_result = validateStepExecutionResult(record.execution_result, `${location}.execution_result`);
  if (result.review_receipt && result.status !== "completed")
    fail3("RUNTIME_STATE_CONFLICT", `${location}.review_receipt requires a completed execution record.`);
  if (result.advancement !== undefined) {
    if (result.checkpoint === undefined || result.next_step_id === undefined) {
      fail3("RUNTIME_STATE_CONFLICT", `${location}.advancement requires checkpoint and next_step_id.`);
    }
    if (result.advancement === "advanced" && result.next_step_id === null) {
      fail3("RUNTIME_STATE_CONFLICT", `${location}.advanced execution record must name the next step.`);
    }
    if (result.advancement !== "advanced" && result.next_step_id !== null) {
      fail3("RUNTIME_STATE_CONFLICT", `${location}.${result.advancement} execution record must not name a next step.`);
    }
  }
  return result;
}
function validateEvidenceChallenge(value, location) {
  const item = expectRecord2(value, location);
  expectExactKeys2(item, ["challenge_id", "claim_id", "slot_id", "result_id", "evidence_ref", "evidence_sha256", "reason", "status", "source_revision", "correction_step_id", ..."resolution" in item ? ["resolution"] : []], location);
  const resolution = item.resolution === undefined ? undefined : (() => {
    const value = expectRecord2(item.resolution, `${location}.resolution`);
    expectExactKeys2(value, ["kind", "evidence_ref", "evidence_sha256", "reason", "source_revision"], `${location}.resolution`);
    if (value.kind !== "not-substantiated")
      fail3("RUNTIME_SCHEMA_INVALID", `${location}.resolution.kind is invalid.`);
    return {
      kind: "not-substantiated",
      evidence_ref: normalizeRepoPath2(expectString2(value.evidence_ref, `${location}.resolution.evidence_ref`), `${location}.resolution.evidence_ref`),
      evidence_sha256: expectString2(value.evidence_sha256, `${location}.resolution.evidence_sha256`, /^[a-f0-9]{64}$/),
      reason: expectString2(value.reason, `${location}.resolution.reason`),
      source_revision: expectString2(value.source_revision, `${location}.resolution.source_revision`, /^[a-f0-9]{64}$/)
    };
  })();
  if (resolution && (item.status !== "resolved" || item.correction_step_id !== null))
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.resolution requires a dismissed challenge.`);
  if (item.status === "resolved" && item.correction_step_id === null && !resolution)
    fail3("RUNTIME_SCHEMA_INVALID", `${location} needs a correction step or an assessment resolution.`);
  return {
    challenge_id: expectString2(item.challenge_id, `${location}.challenge_id`, SAFE_KEY_PATTERN2),
    claim_id: expectString2(item.claim_id, `${location}.claim_id`, CLAIM_ID_PATTERN),
    slot_id: expectString2(item.slot_id, `${location}.slot_id`, CLAIM_EVIDENCE_SLOT_ID_PATTERN),
    result_id: expectString2(item.result_id, `${location}.result_id`, CLAIM_ID_PATTERN),
    evidence_ref: normalizeRepoPath2(expectString2(item.evidence_ref, `${location}.evidence_ref`), `${location}.evidence_ref`),
    evidence_sha256: expectString2(item.evidence_sha256, `${location}.evidence_sha256`, /^[a-f0-9]{64}$/),
    reason: expectString2(item.reason, `${location}.reason`),
    status: expectEnum(item.status, ["contested", "invalidated", "resolved"], `${location}.status`),
    source_revision: expectString2(item.source_revision, `${location}.source_revision`, /^[a-f0-9]{64}$/),
    correction_step_id: item.correction_step_id === null ? null : expectString2(item.correction_step_id, `${location}.correction_step_id`, STEP_ID_PATTERN2),
    ...resolution ? { resolution } : {}
  };
}
function validateEvidenceCarryForward(value, location) {
  const item = expectRecord2(value, location);
  const v2 = item.kind === "evidence-carry-forward/v2";
  expectExactKeys2(item, ["kind", "old_source_revision", "old_plan_revision", "new_plan_revision", "claim_id", "slot_id", "check_id", "result_id", "report_sha256", "subject_revision", ...v2 ? ["receiving_source_revision", "evidence_objects", "context_revision"] : [], ...["history_operation", "user_decision_sha256"].filter((key) => (key in item))], location);
  if (!v2 && item.kind !== "evidence-carry-forward/v1")
    fail3("RUNTIME_SCHEMA_INVALID", `${location}.kind is invalid.`);
  const hash = (key) => expectString2(item[key], `${location}.${key}`, /^[a-f0-9]{64}$/);
  if (v2 && (!Array.isArray(item.evidence_objects) || item.evidence_objects.length > 128))
    fail3("RUNTIME_SCHEMA_INVALID", "Evidence objects must be bounded.");
  const objects = v2 ? item.evidence_objects.map((raw) => {
    const object = expectRecord2(raw, "evidence object");
    expectExactKeys2(object, ["path", "sha256", "size"], "evidence object");
    if (!Number.isInteger(object.size) || object.size < 0 || object.size > 1048576)
      fail3("RUNTIME_SCHEMA_INVALID", "Evidence object size is invalid.");
    return {
      path: normalizeRepoPath2(expectString2(object.path, "evidence object.path"), "evidence object.path"),
      sha256: expectString2(object.sha256, "evidence object.sha256", /^[a-f0-9]{64}$/),
      size: object.size
    };
  }) : undefined;
  return {
    kind: v2 ? "evidence-carry-forward/v2" : "evidence-carry-forward/v1",
    ...v2 ? { receiving_source_revision: hash("receiving_source_revision"), evidence_objects: objects, context_revision: hash("context_revision") } : {},
    ...item.history_operation === undefined ? {} : { history_operation: expectEnum(item.history_operation, ["confirm-replan", "replace-validation"], "history_operation") },
    ...item.user_decision_sha256 === undefined ? {} : { user_decision_sha256: hash("user_decision_sha256") },
    old_source_revision: hash("old_source_revision"),
    old_plan_revision: hash("old_plan_revision"),
    new_plan_revision: hash("new_plan_revision"),
    claim_id: expectString2(item.claim_id, `${location}.claim_id`, CLAIM_ID_PATTERN),
    slot_id: expectString2(item.slot_id, `${location}.slot_id`, CLAIM_EVIDENCE_SLOT_ID_PATTERN),
    check_id: expectString2(item.check_id, `${location}.check_id`, CLAIM_ID_PATTERN),
    result_id: expectString2(item.result_id, `${location}.result_id`, CLAIM_ID_PATTERN),
    report_sha256: hash("report_sha256"),
    subject_revision: hash("subject_revision")
  };
}
function validateVNextRuntimeState(value, options = {}) {
  const runtime = expectRecord2(value, "runtime_state");
  const requiredRuntimeStateFields = [
    "schema_version",
    "kind",
    "task_id",
    "task_slug",
    "workflow_status",
    "lifecycle_state",
    "resume_requires_review",
    "resume_review_reasons",
    "active_step_id",
    "active_step_status",
    "finding_queue_revision",
    "review_cycle",
    "findings"
  ];
  if (!options.storeBackedHistory)
    requiredRuntimeStateFields.push("execution_log", "applied_proposals");
  const optionalRuntimeStateFields = ["business_evidence_version", "evidence_plan_revision", "task_evolution_version", "preservation_source_revision", "claim_evidence_required", "claim_evidence", "pending_review_result", "scope_amendment_pending_review_step_id", "review_coverage", "step_attempts", "evidence_challenges", "evidence_carry_forward", "artifact_checkpoint_ids", "dynamic_review_required", "dynamic_expansions", "execution_preflight", "authority_domain_revision"];
  if (options.storeBackedHistory)
    optionalRuntimeStateFields.push("execution_log", "applied_proposals");
  const missingRuntimeStateFields = requiredRuntimeStateFields.filter((field) => !(field in runtime));
  const extraRuntimeStateFields = Object.keys(runtime).filter((field) => !requiredRuntimeStateFields.includes(field) && !optionalRuntimeStateFields.includes(field));
  if (missingRuntimeStateFields.length > 0 || extraRuntimeStateFields.length > 0) {
    fail3("RUNTIME_SCHEMA_INVALID", `runtime_state keys mismatch; missing=[${missingRuntimeStateFields.join(", ")}], unexpected=[${extraRuntimeStateFields.join(", ")}].`);
  }
  if (runtime.business_evidence_version !== undefined && runtime.business_evidence_version !== 1) {
    fail3("TASK_SEMANTICS_VERSION_UNSUPPORTED", "runtime_state.business_evidence_version must be 1 when present.");
  }
  if (runtime.task_evolution_version !== undefined && ![1, 2].includes(runtime.task_evolution_version)) {
    fail3("TASK_EVOLUTION_VERSION_UNSUPPORTED", "runtime_state.task_evolution_version must be 1 or 2 when present.");
  }
  if (runtime.preservation_source_revision !== undefined && ![1, 2].includes(runtime.task_evolution_version)) {
    fail3("RUNTIME_SCHEMA_INVALID", "preservation_source_revision requires a supported task_evolution_version.");
  }
  if (runtime.schema_version !== VNEXT_RUNTIME_SCHEMA_VERSION)
    fail3("RUNTIME_SCHEMA_INVALID", "runtime_state.schema_version must be 1.");
  if (runtime.kind !== VNEXT_RUNTIME_STATE_KIND)
    fail3("RUNTIME_SCHEMA_INVALID", `runtime_state.kind must be ${VNEXT_RUNTIME_STATE_KIND}.`);
  const taskId = expectString2(runtime.task_id, "runtime_state.task_id");
  const taskSlug = expectString2(runtime.task_slug, "runtime_state.task_slug");
  try {
    validateTaskId(taskId);
    validateTaskSlug(taskSlug);
  } catch (error) {
    fail3("RUNTIME_SCHEMA_INVALID", error instanceof Error ? error.message : String(error));
  }
  const workflowStatus = expectEnum(runtime.workflow_status, CURRENT_TASK_WORKFLOW_STATUSES, "runtime_state.workflow_status");
  const lifecycleState = expectEnum(runtime.lifecycle_state, TASK_LIFECYCLE_STATES, "runtime_state.lifecycle_state");
  try {
    validateCurrentTaskStatusTuple(workflowStatus, lifecycleState);
  } catch (error) {
    fail3("RUNTIME_STATE_CONFLICT", error instanceof Error ? error.message : String(error));
  }
  const resumeRequiresReview = expectBoolean(runtime.resume_requires_review, "runtime_state.resume_requires_review");
  const rawResumeReasons = expectStringArray2(runtime.resume_review_reasons, "runtime_state.resume_review_reasons", true, RESUME_REVIEW_REASON_ORDER.length);
  const resumeReviewReasons = normalizeResumeReviewReasons(rawResumeReasons);
  if (rawResumeReasons.join("|") !== resumeReviewReasons.join("|")) {
    fail3("RUNTIME_SCHEMA_INVALID", "runtime_state.resume_review_reasons must use the canonical closed-set order.");
  }
  try {
    validateCurrentTaskResumeGate(lifecycleState, resumeRequiresReview, resumeReviewReasons);
  } catch (error) {
    fail3("RUNTIME_STATE_CONFLICT", error instanceof Error ? error.message : String(error));
  }
  const activeStepId = expectString2(runtime.active_step_id, "runtime_state.active_step_id", STEP_ID_PATTERN2);
  const activeStepStatus = expectEnum(runtime.active_step_status, STEP_STATUSES, "runtime_state.active_step_status");
  const claimEvidenceRequired = runtime.claim_evidence_required === undefined ? runtime.claim_evidence !== undefined : expectBoolean(runtime.claim_evidence_required, "runtime_state.claim_evidence_required");
  const claimEvidence = runtime.claim_evidence === undefined ? [] : validateClaimEvidence(runtime.claim_evidence, "runtime_state.claim_evidence");
  const evidenceChallenges = runtime.evidence_challenges === undefined ? [] : (() => {
    if (!Array.isArray(runtime.evidence_challenges) || runtime.evidence_challenges.length > 128)
      fail3("RUNTIME_SCHEMA_INVALID", "evidence_challenges must be a bounded array.");
    return runtime.evidence_challenges.map((item, index) => validateEvidenceChallenge(item, `runtime_state.evidence_challenges[${index}]`));
  })();
  const evidenceCarryForward = runtime.evidence_carry_forward === undefined ? [] : (() => {
    if (!Array.isArray(runtime.evidence_carry_forward) || runtime.evidence_carry_forward.length > 256)
      fail3("RUNTIME_SCHEMA_INVALID", "evidence_carry_forward must be a bounded array.");
    return runtime.evidence_carry_forward.map((item, index) => validateEvidenceCarryForward(item, `runtime_state.evidence_carry_forward[${index}]`));
  })();
  const dynamicExpansions = runtime.dynamic_expansions === undefined ? [] : validateDynamicExpansions(runtime.dynamic_expansions);
  const dynamicReviewRequired = runtime.dynamic_review_required === undefined ? false : expectBoolean(runtime.dynamic_review_required, "runtime_state.dynamic_review_required");
  const executionPreflight = runtime.execution_preflight === undefined ? undefined : validateExecutionPreflight(runtime.execution_preflight);
  if (new Set(evidenceChallenges.map((item) => item.challenge_id)).size !== evidenceChallenges.length)
    fail3("RUNTIME_SCHEMA_INVALID", "evidence_challenges IDs must be unique.");
  const findingsValue = runtime.findings;
  if (!Array.isArray(findingsValue) || findingsValue.length > MAX_FINDINGS)
    fail3("RUNTIME_SCHEMA_INVALID", "runtime_state.findings must be an array within the bounded size.");
  const findings = findingsValue.map((finding, index) => validateFinding(finding, `runtime_state.findings[${index}]`));
  if (new Set(findings.map((finding) => finding.fingerprint)).size !== findings.length)
    fail3("RUNTIME_SCHEMA_INVALID", "runtime_state.findings fingerprints must be unique.");
  for (const finding of findings) {
    if (finding.owner_task_id !== taskId)
      fail3("RUNTIME_STATE_CONFLICT", `finding ${finding.fingerprint} is owned by a different task.`);
    if (finding.repair_attempts > finding.max_repair_attempts)
      fail3("RUNTIME_SCHEMA_INVALID", `finding ${finding.fingerprint} exceeds its declared repair budget.`);
  }
  const executionLogValue = runtime.execution_log;
  if (options.storeBackedHistory && executionLogValue !== undefined) {
    fail3("RUNTIME_STORAGE_COMPACT_INVALID", "compact CURRENT_TASK must not inline execution_log; use task-read against the committed store.");
  }
  if (!options.storeBackedHistory && (!Array.isArray(executionLogValue) || executionLogValue.length > MAX_EXECUTION_LOG))
    fail3("RUNTIME_SCHEMA_INVALID", "runtime_state.execution_log must be a bounded array.");
  const executionLog = options.storeBackedHistory ? [] : executionLogValue.map((entry, index) => validateExecutionLogEntry(entry, `runtime_state.execution_log[${index}]`, taskId, taskSlug));
  const appliedValue = runtime.applied_proposals;
  if (options.storeBackedHistory && appliedValue !== undefined) {
    fail3("RUNTIME_STORAGE_COMPACT_INVALID", "compact CURRENT_TASK must not inline applied_proposals; use task-read against the committed store.");
  }
  if (!options.storeBackedHistory && (!Array.isArray(appliedValue) || appliedValue.length > MAX_APPLIED_PROPOSALS))
    fail3("RUNTIME_SCHEMA_INVALID", "runtime_state.applied_proposals must be a bounded array.");
  const appliedProposals = options.storeBackedHistory ? [] : appliedValue.map((entry, index) => {
    const record = expectRecord2(entry, `runtime_state.applied_proposals[${index}]`);
    expectExactKeys2(record, ["idempotency_key", "operation_kind", "proposal_digest", "source_revision"], `runtime_state.applied_proposals[${index}]`);
    const proposalDigest = expectString2(record.proposal_digest, `runtime_state.applied_proposals[${index}].proposal_digest`);
    if (!/^[a-f0-9]{64}$/.test(proposalDigest))
      fail3("RUNTIME_SCHEMA_INVALID", `runtime_state.applied_proposals[${index}].proposal_digest must be SHA-256.`);
    const sourceRevision = expectString2(record.source_revision, `runtime_state.applied_proposals[${index}].source_revision`);
    if (!/^[a-f0-9]{64}$/.test(sourceRevision))
      fail3("RUNTIME_SCHEMA_INVALID", `runtime_state.applied_proposals[${index}].source_revision must be SHA-256.`);
    return {
      idempotency_key: expectString2(record.idempotency_key, `runtime_state.applied_proposals[${index}].idempotency_key`, SAFE_KEY_PATTERN2),
      operation_kind: expectEnum(record.operation_kind, RUNTIME_OPERATION_KINDS, `runtime_state.applied_proposals[${index}].operation_kind`),
      proposal_digest: proposalDigest,
      source_revision: sourceRevision
    };
  });
  if (new Set(appliedProposals.map((item) => item.idempotency_key)).size !== appliedProposals.length)
    fail3("RUNTIME_SCHEMA_INVALID", "runtime_state.applied_proposals keys must be unique.");
  const reviewCycle = validateReviewCycle(runtime.review_cycle);
  const pendingReviewResult = runtime.pending_review_result === undefined || runtime.pending_review_result === null ? null : validatePendingReviewResult(runtime.pending_review_result, "runtime_state.pending_review_result", true);
  const scopeAmendmentPendingReviewStepId = runtime.scope_amendment_pending_review_step_id === undefined ? undefined : expectString2(runtime.scope_amendment_pending_review_step_id, "runtime_state.scope_amendment_pending_review_step_id", STEP_ID_PATTERN2);
  if (pendingReviewResult !== null) {
    if (pendingReviewResult.step_id !== activeStepId && scopeAmendmentPendingReviewStepId !== pendingReviewResult.step_id) {
      fail3("RUNTIME_STATE_CONFLICT", "runtime_state.pending_review_result must belong to the active step.");
    }
    if (scopeAmendmentPendingReviewStepId !== undefined && scopeAmendmentPendingReviewStepId !== pendingReviewResult.step_id) {
      fail3("RUNTIME_STATE_CONFLICT", "scope_amendment_pending_review_step_id must identify the retained review step.");
    }
    if (pendingReviewResult.cycle_id !== reviewCycle.id) {
      fail3("RUNTIME_STATE_CONFLICT", "runtime_state.pending_review_result must belong to the current review cycle.");
    }
  } else if (scopeAmendmentPendingReviewStepId !== undefined) {
    fail3("RUNTIME_STATE_CONFLICT", "scope_amendment_pending_review_step_id requires a retained pending review.");
  }
  return {
    schema_version: 1,
    kind: VNEXT_RUNTIME_STATE_KIND,
    ...runtime.evidence_plan_revision === undefined ? {} : { evidence_plan_revision: expectString2(runtime.evidence_plan_revision, "evidence_plan_revision", /^[a-f0-9]{64}$/) },
    ...runtime.business_evidence_version === 1 ? { business_evidence_version: 1 } : {},
    ...runtime.task_evolution_version === undefined ? {} : { task_evolution_version: runtime.task_evolution_version },
    ...runtime.preservation_source_revision === undefined ? {} : { preservation_source_revision: expectString2(runtime.preservation_source_revision, "preservation_source_revision", /^[a-f0-9]{64}$/) },
    task_id: taskId,
    task_slug: taskSlug,
    workflow_status: workflowStatus,
    lifecycle_state: lifecycleState,
    resume_requires_review: resumeRequiresReview,
    resume_review_reasons: resumeReviewReasons,
    active_step_id: activeStepId,
    active_step_status: activeStepStatus,
    finding_queue_revision: expectInteger(runtime.finding_queue_revision, "runtime_state.finding_queue_revision"),
    review_cycle: reviewCycle,
    findings,
    execution_log: executionLog,
    applied_proposals: appliedProposals,
    claim_evidence_required: claimEvidenceRequired,
    claim_evidence: claimEvidence,
    ...scopeAmendmentPendingReviewStepId === undefined ? {} : { scope_amendment_pending_review_step_id: scopeAmendmentPendingReviewStepId },
    ...runtime.evidence_challenges === undefined ? {} : { evidence_challenges: evidenceChallenges },
    ...runtime.evidence_carry_forward === undefined ? {} : { evidence_carry_forward: evidenceCarryForward },
    ...runtime.artifact_checkpoint_ids === undefined ? {} : { artifact_checkpoint_ids: expectStringArray2(runtime.artifact_checkpoint_ids, "artifact_checkpoint_ids", true, 256).map((id) => expectString2(id, "checkpoint ID", /^[a-f0-9]{64}$/)) },
    ...runtime.dynamic_review_required === undefined ? {} : { dynamic_review_required: dynamicReviewRequired },
    ...runtime.dynamic_expansions === undefined ? {} : { dynamic_expansions: dynamicExpansions },
    ...executionPreflight === undefined ? {} : { execution_preflight: executionPreflight },
    ...runtime.authority_domain_revision === undefined ? {} : { authority_domain_revision: expectString2(runtime.authority_domain_revision, "runtime_state.authority_domain_revision", /^[a-f0-9]{64}$/) },
    pending_review_result: pendingReviewResult,
    ...runtime.step_attempts === undefined ? {} : { step_attempts: validateStepAttempts(runtime.step_attempts) },
    ...runtime.review_coverage === undefined ? {} : { review_coverage: validateReviewCoverage(runtime.review_coverage) }
  };
}
function replaceTaskInfoField(body, label, value) {
  const escapedLabel = label.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  const pattern = new RegExp(`^-\\s*${escapedLabel}：[^\\r\\n]*$`, "gm");
  const matches = body.match(pattern) ?? [];
  if (matches.length !== 1)
    fail3("RUNTIME_SCHEMA_INVALID", `CURRENT_TASK must contain exactly one task-info field "${label}".`);
  return body.replace(pattern, `- ${label}：${value}`);
}
function renderCurrentTaskLifecycleFields(body, runtimeState) {
  const headingMatch = /^## 任务信息\s*$/m.exec(body);
  if (!headingMatch || headingMatch.index === undefined)
    fail3("RUNTIME_SCHEMA_INVALID", "CURRENT_TASK is missing ## 任务信息.");
  const sectionStart = headingMatch.index + headingMatch[0].length;
  const sectionRemainder = body.slice(sectionStart);
  const nextHeading = /\r?\n##\s/.exec(sectionRemainder);
  const sectionEnd = nextHeading?.index ?? sectionRemainder.length;
  const section = sectionRemainder.slice(0, sectionEnd);
  const nextSection = [
    ["当前状态", runtimeState.workflow_status],
    ["生命周期状态", runtimeState.lifecycle_state],
    ["恢复需审查", runtimeState.resume_requires_review ? "true" : "false"],
    ["恢复审查原因", runtimeState.resume_review_reasons.join(", ")]
  ];
  const renderedSection = nextSection.reduce((current, [label, value]) => replaceTaskInfoField(current, label, value), section);
  return body.slice(0, sectionStart) + renderedSection + body.slice(sectionStart + sectionEnd);
}
var REPLAN_SECTION_HEADINGS = {
  background_context: ["背景与上下文", "Background and Context"],
  acceptance: ["验收标准", "Acceptance Criteria"],
  allowed_scope: ["允许修改范围", "Allowed Files"],
  conditional_scope: ["条件修改范围", "条件允许修改范围", "Conditional Files"],
  forbidden_scope: ["禁止修改范围", "Forbidden Files"],
  affected_contracts: ["受影响的契约", "Affected Contracts"],
  confirmed_decisions: ["已确认决策", "Confirmed Decisions"],
  open_questions: ["待确认问题", "Open Questions"],
  implementation_plan: ["实现方案", "Implementation Plan"],
  implementation_steps: ["实施步骤", "Implementation Steps"],
  regression_checks: ["回归检查项", "Regression Checks", "Validation Checks"],
  rollback_points: ["回滚点", "Rollback Points"],
  design_constraints: ["设计约束", "Design Constraints"],
  post_release_validation: ["发布后验证", "Post-release Validation", "Post-Release Validation"],
  propagation_governance: ["传播治理记录", "Propagation Governance"]
};
function scanMarkdownSections2(body) {
  const headings = [];
  const headingPattern = /^(#{2,6})[ \t]+(.+?)[ \t]*$/gm;
  for (const match of body.matchAll(headingPattern)) {
    const headingStart = match.index ?? 0;
    const headingEnd = headingStart + match[0].length;
    headings.push({ title: match[2].trim(), level: match[1].length, headingStart, headingEnd });
  }
  return headings.map((heading, index) => {
    const afterHeading = heading.headingEnd;
    const contentStart = body.startsWith(`\r
`, afterHeading) ? afterHeading + 2 : body.startsWith(`
`, afterHeading) ? afterHeading + 1 : afterHeading;
    const next = headings.slice(index + 1).find((candidate) => candidate.level <= heading.level);
    return {
      title: heading.title,
      level: heading.level,
      headingStart: heading.headingStart,
      contentStart,
      contentEnd: next?.headingStart ?? body.length
    };
  });
}
function findUniqueMarkdownSection(sections, aliases, level, rangeStart = 0, rangeEnd = Number.MAX_SAFE_INTEGER) {
  const matches = sections.filter((section) => section.level === level && aliases.includes(section.title) && section.headingStart >= rangeStart && section.headingStart < rangeEnd);
  if (matches.length > 1)
    fail3("RUNTIME_SECTION_INVALID", `CURRENT_TASK contains duplicate replacement sections: ${aliases.join(" / ")}.`);
  return matches[0] ?? null;
}
function readCanonicalMarkdownSection(content, aliases, level = 2) {
  const section = findUniqueMarkdownSection(scanMarkdownSections2(content), aliases, level);
  return section ? content.slice(section.contentStart, section.contentEnd) : null;
}
var CANONICAL_GOVERNANCE_KEY_PATTERN = /^[A-Za-z0-9][A-Za-z0-9._/-]*$/u;
function canonicalGovernanceFail(code, location, message) {
  fail3(code, `${location} ${message}`);
}
function normalizeCanonicalGovernanceInlineText(value, location, code = "GOVERNANCE_INPUT_INVALID") {
  const normalized = value.trim();
  if (normalized.length === 0 || /[\r\n\0\x01-\x1f\x7f]/u.test(normalized) || /```/u.test(normalized) || /<!--|-->/u.test(normalized) || /^(?:#{1,6}\s|[-*+]\s|>\s)/u.test(normalized) || /^[-*_]{3,}$/u.test(normalized)) {
    canonicalGovernanceFail(code, location, "must be a non-empty single-line Markdown-safe value.");
  }
  return normalized;
}
function normalizeCanonicalGovernanceKey(value, location, code = "GOVERNANCE_INPUT_INVALID") {
  const normalized = value.trim();
  if (!CANONICAL_GOVERNANCE_KEY_PATTERN.test(normalized)) {
    canonicalGovernanceFail(code, location, "must match the canonical key grammar [A-Za-z0-9][A-Za-z0-9._/-]*.");
  }
  return normalized;
}
function normalizeCanonicalGovernanceFacts(value, location, code = "GOVERNANCE_INPUT_INVALID") {
  if (value === undefined)
    return [];
  if (!Array.isArray(value))
    canonicalGovernanceFail(code, location, "must be an array.");
  const result = new Map;
  for (const [index, raw] of value.entries()) {
    if (!raw || typeof raw !== "object" || Array.isArray(raw))
      canonicalGovernanceFail(code, `${location}[${index}]`, "must be a fact mapping.");
    const fact = raw;
    if (typeof fact.key !== "string" || typeof fact.value !== "string" || typeof fact.source !== "string" || !["confirmed", "inferred", "unknown"].includes(fact.certainty)) {
      canonicalGovernanceFail(code, `${location}[${index}]`, "has an invalid key, value, source, or certainty.");
    }
    const normalized = {
      key: normalizeCanonicalGovernanceKey(fact.key, `${location}[${index}].key`, code),
      value: normalizeCanonicalGovernanceInlineText(fact.value, `${location}[${index}].value`, code),
      source: normalizeCanonicalGovernanceInlineText(fact.source, `${location}[${index}].source`, code),
      certainty: fact.certainty
    };
    if (/\s+\(source:\s|\s+\[(?:confirmed|inferred|unknown);\s+source:\s/u.test(normalized.value)) {
      canonicalGovernanceFail(code, `${location}[${index}].value`, "contains a reserved fact rendering delimiter.");
    }
    const previous = result.get(normalized.key);
    if (previous && JSON.stringify(previous) !== JSON.stringify(normalized)) {
      canonicalGovernanceFail(code, `${location}[${index}].key`, `conflicts with another fact for key ${normalized.key}.`);
    }
    if (!previous)
      result.set(normalized.key, normalized);
  }
  return [...result.values()];
}
function parseCanonicalGovernanceFactLines(lines, location) {
  const parsed = [];
  for (const [index, line] of lines.entries()) {
    const normalized = line.trim();
    if (!normalized.startsWith("- ") || normalized === "- none")
      continue;
    const confirmed = /^-\s+([^:\r\n]+):\s+(.+?)\s+\(source:\s+(.+)\)$/u.exec(normalized);
    if (confirmed) {
      parsed.push({ key: confirmed[1], value: confirmed[2], source: confirmed[3], certainty: "confirmed" });
      continue;
    }
    const unresolved = /^-\s+([^:\r\n]+):\s+(.+?)\s+\[(confirmed|inferred|unknown);\s+source:\s+(.+)\]$/u.exec(normalized);
    if (unresolved) {
      parsed.push({ key: unresolved[1], value: unresolved[2], source: unresolved[4], certainty: unresolved[3] });
      continue;
    }
    if (normalized.includes(":"))
      canonicalGovernanceFail("GOVERNANCE_FACT_INVALID", `${location}:${index + 1}`, "contains an unreadable canonical fact.");
  }
  return normalizeCanonicalGovernanceFacts(parsed, location, "GOVERNANCE_FACT_INVALID");
}
function normalizeCanonicalBaselineEntries(value, location, code = "GOVERNANCE_INPUT_INVALID") {
  if (value === undefined)
    return {};
  if (!value || typeof value !== "object" || Array.isArray(value))
    canonicalGovernanceFail(code, location, "must be a mapping.");
  const result = {};
  for (const [rawKey, rawValue] of Object.entries(value)) {
    const key = normalizeCanonicalGovernanceKey(rawKey, `${location}.${rawKey}`, code);
    if (key === "none")
      canonicalGovernanceFail(code, `${location}.${rawKey}`, 'cannot use the reserved empty-baseline placeholder key "none".');
    if (typeof rawValue !== "string")
      canonicalGovernanceFail(code, `${location}.${rawKey}`, "must have a string value.");
    const normalizedValue = normalizeCanonicalGovernanceInlineText(rawValue, `${location}.${rawKey}`, code);
    if (key in result && result[key] !== normalizedValue)
      canonicalGovernanceFail(code, `${location}.${rawKey}`, `conflicts with another baseline entry for key ${key}.`);
    result[key] = normalizedValue;
  }
  return result;
}
function parseCanonicalBaselineKeys(lines, location) {
  const result = new Set;
  for (const [index, line] of lines.entries()) {
    const normalized = line.trim();
    if (!normalized.startsWith("- "))
      continue;
    const rawKey = normalized.slice(2).trim();
    if (!rawKey || rawKey === "none")
      continue;
    const key = normalizeCanonicalGovernanceKey(rawKey, `${location}:${index + 1}`, "GOVERNANCE_BASELINE_INVALID");
    result.add(key);
  }
  return [...result].sort((left, right) => left.localeCompare(right));
}
function resolveReplanSectionRanges(body) {
  const sections = scanMarkdownSections2(body);
  const resolved = {};
  const topAllowed = findUniqueMarkdownSection(sections, REPLAN_SECTION_HEADINGS.allowed_scope, 2);
  const topConditional = findUniqueMarkdownSection(sections, REPLAN_SECTION_HEADINGS.conditional_scope, 2);
  const topForbidden = findUniqueMarkdownSection(sections, REPLAN_SECTION_HEADINGS.forbidden_scope, 2);
  const nestedAllowed = topAllowed ? findUniqueMarkdownSection(sections, REPLAN_SECTION_HEADINGS.allowed_scope, 3, topAllowed.contentStart, topAllowed.contentEnd) : null;
  const nestedConditional = topAllowed ? findUniqueMarkdownSection(sections, REPLAN_SECTION_HEADINGS.conditional_scope, 3, topAllowed.contentStart, topAllowed.contentEnd) : null;
  const nestedConditionalUnderTopSection = topConditional ? findUniqueMarkdownSection(sections, REPLAN_SECTION_HEADINGS.conditional_scope, 3, topConditional.contentStart, topConditional.contentEnd) : null;
  const nestedForbidden = topForbidden ? findUniqueMarkdownSection(sections, REPLAN_SECTION_HEADINGS.forbidden_scope, 3, topForbidden.contentStart, topForbidden.contentEnd) : null;
  if (nestedConditional && !nestedAllowed) {
    fail3("RUNTIME_SECTION_INVALID", "Conditional scope must have a distinct existing Allowed Files section when both are nested under the scope heading.");
  }
  if (nestedAllowed) {
    resolved.allowed_scope = nestedAllowed;
    if (nestedConditional)
      resolved.conditional_scope = nestedConditional;
    else if (nestedConditionalUnderTopSection)
      resolved.conditional_scope = nestedConditionalUnderTopSection;
    else if (topConditional)
      resolved.conditional_scope = topConditional;
  } else {
    if (topAllowed)
      resolved.allowed_scope = topAllowed;
    if (nestedConditionalUnderTopSection)
      resolved.conditional_scope = nestedConditionalUnderTopSection;
    else if (topConditional)
      resolved.conditional_scope = topConditional;
  }
  if (nestedForbidden)
    resolved.forbidden_scope = nestedForbidden;
  else if (topForbidden)
    resolved.forbidden_scope = topForbidden;
  const nonScopeKeys = [
    "background_context",
    "acceptance",
    "affected_contracts",
    "confirmed_decisions",
    "open_questions",
    "implementation_plan",
    "implementation_steps",
    "regression_checks",
    "rollback_points",
    "design_constraints",
    "post_release_validation",
    "propagation_governance"
  ];
  for (const key of nonScopeKeys) {
    const section = findUniqueMarkdownSection(sections, REPLAN_SECTION_HEADINGS[key], 2);
    if (section)
      resolved[key] = section;
  }
  return resolved;
}
function replacementSectionValue(replacement, key) {
  return replacement[key];
}
function replaceReplanDefinitionSections(body, replacement) {
  const ranges = resolveReplanSectionRanges(body);
  const replacements = [];
  for (const key of REPLAN_REPLACEMENT_FIELDS) {
    const value = replacementSectionValue(replacement, key);
    const range = ranges[key];
    const optional = key === "design_constraints" || key === "post_release_validation" || key === "propagation_governance";
    if (!range) {
      if (!optional || value !== null)
        fail3("RUNTIME_SECTION_INVALID", `CURRENT_TASK is missing the existing replacement section for ${key}.`);
      continue;
    }
    replacements.push({ range, content: value ?? "" });
  }
  if (replacement.mutation_authority_version === MUTATION_AUTHORITY_VERSION && replacement.mutation_authority) {
    const authoritySection = findUniqueMarkdownSection(scanMarkdownSections2(body), MUTATION_AUTHORITY_SECTION_ALIASES, 2);
    if (!authoritySection)
      fail3("RUNTIME_SECTION_INVALID", "CURRENT_TASK is missing its Mutation Authority section.");
    replacements.push({ range: authoritySection, content: renderMutationAuthoritySectionContent(replacement.mutation_authority) });
  }
  replacements.sort((left, right) => right.range.contentStart - left.range.contentStart);
  for (let index = 1;index < replacements.length; index += 1) {
    const previous = replacements[index - 1].range;
    const current = replacements[index].range;
    if (current.contentEnd > previous.contentStart) {
      fail3("RUNTIME_SECTION_INVALID", "Replan replacement sections overlap and cannot be replaced atomically.");
    }
  }
  let nextBody = body;
  for (const { range, content } of replacements) {
    const normalized = normalizeReplacementSectionContent(content, `CURRENT_TASK.${range.title}`);
    const rendered = normalized.length === 0 ? `

` : `
${normalized}

`;
    nextBody = nextBody.slice(0, range.contentStart) + rendered + nextBody.slice(range.contentEnd);
  }
  return nextBody;
}
function assertReplanDefinitionSections(body, replacement) {
  const ranges = resolveReplanSectionRanges(body);
  for (const key of REPLAN_REPLACEMENT_FIELDS) {
    const value = replacementSectionValue(replacement, key);
    const range = ranges[key];
    const optional = key === "design_constraints" || key === "post_release_validation" || key === "propagation_governance";
    if (!range) {
      if (!optional || value !== null)
        fail3("RUNTIME_REPLAY_INCOMPLETE", `replan replay is missing the replacement section for ${key}.`);
      continue;
    }
    const actual = normalizeReplacementSectionContent(body.slice(range.contentStart, range.contentEnd), `CURRENT_TASK.${range.title}`);
    const expected = value ?? "";
    if (actual !== expected)
      fail3("RUNTIME_REPLAY_INCOMPLETE", `replan replay section ${key} no longer matches the committed replacement.`);
  }
  const bodyAuthority = parseMutationAuthoritySection(body);
  const replacementHasAuthority = replacement.mutation_authority_version === MUTATION_AUTHORITY_VERSION && replacement.mutation_authority !== undefined;
  if (replacementHasAuthority) {
    if (!bodyAuthority || bodyAuthority.mutation_authority_version !== MUTATION_AUTHORITY_VERSION || !bodyAuthority.mutation_authority || digest3(bodyAuthority.mutation_authority) !== digest3(replacement.mutation_authority)) {
      fail3("RUNTIME_REPLAY_INCOMPLETE", "replan replay Mutation Authority section no longer matches the committed replacement.");
    }
  } else if (bodyAuthority) {
    fail3("RUNTIME_REPLAY_INCOMPLETE", "replan replay unexpectedly contains a Mutation Authority v2 section.");
  }
}
function auditList(values) {
  return `[${values.map((value) => JSON.stringify(value)).join(", ")}]`;
}
function renderExecutionAuditRecord(audit, includeEmptyKnowledge = true) {
  const authorityRefs = audit.authority_evidence.map((item) => `${item.kind}:${item.source}:${item.subject}`);
  const lines = [
    `- action: ${audit.action}`,
    `  old_status: ${audit.from_workflow_status}+${audit.from_lifecycle_state}`,
    `  new_status: ${audit.to_workflow_status}+${audit.to_lifecycle_state}`,
    `  task_id: ${audit.task_id}`,
    `  task_slug: ${audit.task_slug}`,
    `  document_id: ${audit.document_id}`,
    `  proposal_idempotency_key: ${audit.idempotency_key}`,
    `  source_revision: ${audit.source_revision}`,
    `  authority_refs: ${auditList(authorityRefs)}`,
    `  evidence_refs: ${auditList(audit.evidence_refs)}`
  ];
  if (audit.action === "archive") {
    lines.push(`  archive_path: ${audit.archive_path}`);
    lines.push(`  archive_revision: ${audit.archive_revision}`);
    lines.push(`  closure_delta_digest: ${audit.closure_delta_digest}`);
    lines.push("  lesson_admission:");
    lines.push(`    decision: ${audit.lesson_admission.decision}`);
    lines.push(`    candidate_refs: ${auditList(audit.lesson_admission.candidate_refs)}`);
    lines.push(`    evidence_refs: ${auditList(audit.lesson_admission.evidence_refs)}`);
    if (includeEmptyKnowledge || audit.knowledge_admissions.contracts.length > 0 || audit.knowledge_admissions.decisions.length > 0) {
      lines.push(`  knowledge_admissions: ${JSON.stringify(audit.knowledge_admissions)}`);
    }
  } else if (audit.action === "migrate-claim-evidence") {
    const migrationAudit = audit;
    lines.push(`  from_task_id: ${migrationAudit.from_task_id}`);
    lines.push(`  from_task_slug: ${migrationAudit.from_task_slug}`);
    lines.push(`  from_document_id: ${migrationAudit.from_document_id}`);
    lines.push(`  claim_evidence_digest: ${migrationAudit.claim_evidence_digest}`);
  } else if (DRAFT_AUDIT_ACTIONS.includes(audit.action)) {
    const draftAudit = audit;
    lines.push(`  from_task_id: ${draftAudit.from_task_id}`);
    lines.push(`  from_task_slug: ${draftAudit.from_task_slug}`);
    lines.push(`  from_document_id: ${draftAudit.from_document_id}`);
    if (draftAudit.definition_digest !== undefined)
      lines.push(`  definition_digest: ${draftAudit.definition_digest}`);
    if (draftAudit.draft_revision !== undefined)
      lines.push(`  draft_revision: ${draftAudit.draft_revision}`);
  } else {
    const replanAudit = audit;
    if (replanAudit.candidate_digest !== undefined)
      lines.push(`  candidate_digest: ${replanAudit.candidate_digest}`);
    if (replanAudit.correction_reason !== undefined)
      lines.push(`  correction_reason: ${replanAudit.correction_reason}`);
    if (replanAudit.invalidation_kind !== undefined)
      lines.push(`  invalidation_kind: ${replanAudit.invalidation_kind}`);
    if (replanAudit.invalidation_reason !== undefined)
      lines.push(`  invalidation_reason: ${replanAudit.invalidation_reason}`);
    if (replanAudit.partial_diff_disposition !== undefined) {
      lines.push("  partial_diff_disposition:");
      lines.push(`    reusable: ${auditList(replanAudit.partial_diff_disposition.reusable)}`);
      lines.push(`    rollback_required: ${auditList(replanAudit.partial_diff_disposition.rollback_required)}`);
      lines.push(`    stop_propagation: ${auditList(replanAudit.partial_diff_disposition.stop_propagation)}`);
    }
  }
  lines.push(`  recorded_at: ${audit.recorded_at}`);
  return lines.join(`
`);
}
function appendExecutionAuditToBody(body, audit) {
  const section = findUniqueMarkdownSection(scanMarkdownSections2(body), ["执行记录", "Execution Log"], 2);
  if (!section)
    fail3("RUNTIME_SECTION_INVALID", "CURRENT_TASK is missing the required ## 执行记录 audit section.");
  const existing = body.slice(section.contentStart, section.contentEnd).replace(/\r\n?/g, `
`).trimEnd();
  const auditText = renderExecutionAuditRecord(audit);
  const rendered = `${existing.trim().length > 0 ? `${existing}

` : ""}${auditText}

`;
  return body.slice(0, section.contentStart) + `
${rendered}` + body.slice(section.contentEnd);
}
function compactHistoryPreview(body, runtimeState, audit) {
  const section = findUniqueMarkdownSection(scanMarkdownSections2(body), ["执行记录", "Execution Log"], 2);
  if (!section)
    fail3("RUNTIME_SECTION_INVALID", "CURRENT_TASK is missing the required ## 执行记录 audit section.");
  const entries = [...runtimeState.execution_log];
  if (audit && !entries.some((item) => isRecord4(item) && item.idempotency_key === audit.idempotency_key))
    entries.push(audit);
  const preview = entries.filter(isRecord4).slice(-8).map((item) => {
    const action = typeof item.action === "string" ? item.action : "step-execution";
    const idempotencyKey = typeof item.idempotency_key === "string" ? item.idempotency_key : "unknown";
    const stepId = typeof item.step_id === "string" ? item.step_id : null;
    const status = typeof item.status === "string" ? item.status : null;
    const recordedAt = typeof item.recorded_at === "string" ? item.recorded_at : null;
    return `- ${action} | key=${idempotencyKey}${stepId ? ` | step=${stepId}` : ""}${status ? ` | status=${status}` : ""}${recordedAt ? ` | at=${recordedAt}` : ""}`;
  });
  const lines = [
    "- Store-backed history is retained under task-data; use task-read for exact event, proposal, result, and report reads.",
    "- This section is a bounded navigation preview only; it is not the Runtime fact source.",
    ...preview.length > 0 ? ["", ...preview] : []
  ];
  let rendered = lines.join(`
`);
  if (Buffer.byteLength(rendered, "utf8") > 8192) {
    rendered = Buffer.from(rendered, "utf8").subarray(0, 8192).toString("utf8");
  }
  return body.slice(0, section.contentStart) + `
${rendered}

` + body.slice(section.contentEnd);
}
function assertExecutionAudit(root2, current, audit) {
  if (current.frontmatter.task_store !== undefined) {
    const history = TaskStore.forCurrent(root2, current).readExecutionLog();
    if (!history.some((entry) => digest3(entry) === digest3(audit))) {
      fail3("RUNTIME_REPLAY_INCOMPLETE", `replay is missing the durable task-store audit for ${audit.action}.`);
    }
    return;
  }
  const body = current.body;
  const section = findUniqueMarkdownSection(scanMarkdownSections2(body), ["执行记录", "Execution Log"], 2);
  if (!section)
    fail3("RUNTIME_REPLAY_INCOMPLETE", "replay is missing the required ## 执行记录 audit section.");
  const content = body.slice(section.contentStart, section.contentEnd).replace(/\r\n?/g, `
`);
  const currentAudit = renderExecutionAuditRecord(audit);
  const historicalAudit = audit.action === "archive" && audit.knowledge_admissions.contracts.length === 0 && audit.knowledge_admissions.decisions.length === 0 ? renderExecutionAuditRecord(audit, false) : null;
  if (!content.includes(currentAudit) && (historicalAudit === null || !content.includes(historicalAudit))) {
    fail3("RUNTIME_REPLAY_INCOMPLETE", `replay is missing the durable body audit for ${audit.action}.`);
  }
}
var VNEXT_TASK_BASIS_KIND = "vnext-task-basis";
var TASK_BASIS_HEADING_ALIASES = ["任务输入依据", "Task Basis"];
function taskBasisRelativePath(currentTaskPath, taskId) {
  try {
    validateTaskId(taskId);
  } catch (error) {
    fail3("RUNTIME_IDENTITY_INVALID", error instanceof Error ? error.message : String(error));
  }
  const directory = path12.posix.dirname(normalizeRepoPath2(currentTaskPath, "CURRENT_TASK source path"));
  return path12.posix.join(directory, "task-basis", `TASK_BASIS-${taskId}.md`);
}
function taskBasisFilePath(root2, relativePath) {
  const resolvedRoot = path12.resolve(root2);
  const filePath = path12.resolve(resolvedRoot, ...normalizeRepoPath2(relativePath, "task basis path").split("/"));
  const relative = path12.relative(resolvedRoot, filePath).replace(/\\/g, "/");
  if (!relative || relative.startsWith("../") || path12.isAbsolute(relative)) {
    fail3("RUNTIME_PATH_INVALID", `task basis path escapes the target root: ${relativePath}`);
  }
  return filePath;
}
function renderTaskBasisContent(identity, basis) {
  const frontmatter = {
    schema_version: 1,
    kind: VNEXT_TASK_BASIS_KIND,
    task_id: identity.task_id,
    document_id: identity.document_id,
    original_request: basis.original_request,
    user_decisions: basis.user_decisions
  };
  return [
    "---",
    stringify4(frontmatter).trimEnd(),
    "---",
    "# vNext TASK_BASIS",
    "",
    "This file preserves request evidence for independent draft review.",
    "It is not a review result and does not replace source-authority or decision-authority policy.",
    ""
  ].join(`
`);
}
function materializeTaskBasis(root2, current, identity, basis) {
  const relativePath = taskBasisRelativePath(current.relativePath, identity.task_id);
  const content = renderTaskBasisContent(identity, basis);
  return {
    path: relativePath,
    filePath: taskBasisFilePath(root2, relativePath),
    revision: sha2565(content),
    content,
    basis
  };
}
function readTaskBasisReferenceFromBody(body) {
  const section = findUniqueMarkdownSection(scanMarkdownSections2(body), TASK_BASIS_HEADING_ALIASES, 2);
  if (!section)
    return null;
  const content = body.slice(section.contentStart, section.contentEnd).replace(/\r\n?/g, `
`).trim();
  const match = /^- path: `([^`]+)`\n- revision: `([a-f0-9]{64})`$/u.exec(content);
  if (!match)
    fail3("TASK_BASIS_REFERENCE_INVALID", "CURRENT_TASK task basis reference must contain one exact path and SHA-256 revision.");
  return {
    path: normalizeRepoPath2(match[1], "CURRENT_TASK task basis path"),
    revision: match[2]
  };
}
function renderTaskBasisReference(reference) {
  return [`- path: \`${reference.path}\``, `- revision: \`${reference.revision}\``].join(`
`);
}
function replaceTaskBasisReference(body, reference) {
  const sections = scanMarkdownSections2(body);
  const existing = findUniqueMarkdownSection(sections, TASK_BASIS_HEADING_ALIASES, 2);
  const rendered = renderTaskBasisReference(reference);
  if (existing) {
    return body.slice(0, existing.contentStart) + `
${rendered}

` + body.slice(existing.contentEnd);
  }
  const background = findUniqueMarkdownSection(sections, ["背景与上下文", "Background and Context"], 2);
  if (!background)
    fail3("RUNTIME_SECTION_INVALID", "CURRENT_TASK is missing the background section required to insert its task basis reference.");
  return body.slice(0, background.headingStart) + `## 任务输入依据

${rendered}

` + body.slice(background.headingStart);
}
function renderNewDraftBody(identity, definition, runtimeState, taskBasisReference) {
  const optionalSection = (value) => value ?? "";
  return [
    "# vNext CURRENT_TASK",
    "",
    "## 任务信息",
    "",
    `- 任务 ID：${identity.task_id}`,
    `- 任务标题：${identity.task_title}`,
    `- 任务 slug：${identity.task_slug}`,
    `- 当前状态：${runtimeState.workflow_status}`,
    `- 生命周期状态：${runtimeState.lifecycle_state}`,
    `- 恢复需审查：${runtimeState.resume_requires_review ? "true" : "false"}`,
    `- 恢复审查原因：${runtimeState.resume_review_reasons.join(", ")}`,
    "",
    "## 任务输入依据",
    "",
    renderTaskBasisReference(taskBasisReference),
    "",
    "## 背景与上下文",
    "",
    definition.background_context,
    "",
    "## 验收标准",
    "",
    definition.acceptance,
    "",
    ...definition.mutation_authority_version === MUTATION_AUTHORITY_VERSION && definition.mutation_authority ? [
      "## Mutation Authority",
      "",
      renderMutationAuthoritySectionContent(definition.mutation_authority),
      ""
    ] : [],
    "## 允许修改范围",
    "",
    "### Read / discovery context",
    "",
    "- none",
    "",
    "### Allowed Files",
    "",
    definition.allowed_scope,
    "",
    "### Conditional Files",
    "",
    definition.conditional_scope,
    "",
    "## 禁止修改范围",
    "",
    "### Forbidden Files",
    "",
    definition.forbidden_scope,
    "",
    "## 受影响的契约",
    "",
    definition.affected_contracts,
    "",
    "## 已确认决策",
    "",
    definition.confirmed_decisions,
    "",
    "## 待确认问题",
    "",
    definition.open_questions,
    "",
    "## 实现方案",
    "",
    definition.implementation_plan,
    "",
    "## 传播治理记录",
    "",
    optionalSection(definition.propagation_governance),
    "",
    "## 实施步骤",
    "",
    definition.implementation_steps,
    "",
    "## 回归检查项",
    "",
    definition.regression_checks,
    "",
    "## 回滚点",
    "",
    definition.rollback_points,
    "",
    "## 设计约束",
    "",
    optionalSection(definition.design_constraints),
    "",
    "## 发布后验证",
    "",
    optionalSection(definition.post_release_validation),
    "",
    "## 执行记录",
    "",
    "- Draft created by prepare-task; execution is blocked until explicit confirm-draft.",
    ""
  ].join(`
`);
}
function renderCanonicalCurrentTask(frontmatter, body, runtimeState, options = {}) {
  const existingBinding = frontmatter.task_store;
  const compact = options.compactTaskStore === true || existingBinding !== undefined;
  const compactBinding = compact ? options.draftDocumentId !== undefined ? {
    schema_version: 1,
    kind: "vnext-current-task-store-binding",
    format: "compact-v2",
    manifest_path: options.taskStoreManifestPath ?? fail3("RUNTIME_STORAGE_COMPACT_INVALID", "compact rendering needs a task-store manifest path."),
    history: { execution_log: "task-store", applied_proposals: "task-store" }
  } : existingBinding ?? {
    schema_version: 1,
    kind: "vnext-current-task-store-binding",
    format: "compact-v2",
    manifest_path: options.taskStoreManifestPath ?? fail3("RUNTIME_STORAGE_COMPACT_INVALID", "compact rendering needs a task-store manifest path."),
    history: { execution_log: "task-store", applied_proposals: "task-store" }
  } : undefined;
  const { execution_log: _executionLog, applied_proposals: _appliedProposals, ...compactRuntimeState } = runtimeState;
  const nextFrontmatter = {
    ...frontmatter,
    ...options.draftDocumentId === undefined ? {} : { document_id: options.draftDocumentId },
    ...compactBinding === undefined ? {} : { task_store: compactBinding },
    runtime_state: compact ? compactRuntimeState : runtimeState
  };
  const definitionAuthority = options.draftDefinition?.mutation_authority ?? options.replacementDefinition?.mutation_authority ?? frontmatter.mutation_authority;
  const definitionAuthorityVersion = options.draftDefinition?.mutation_authority_version ?? options.replacementDefinition?.mutation_authority_version ?? frontmatter.mutation_authority_version;
  if (definitionAuthorityVersion === MUTATION_AUTHORITY_VERSION && definitionAuthority) {
    nextFrontmatter.mutation_authority_version = MUTATION_AUTHORITY_VERSION;
    nextFrontmatter.mutation_authority = definitionAuthority;
  }
  if (options.draftDefinition && options.draftIdentity && !options.taskBasisReference) {
    fail3("TASK_BASIS_MISSING", "A new or refined draft must link its exact task basis.");
  }
  let nextBody = options.draftDefinition && options.draftIdentity && options.taskBasisReference ? renderNewDraftBody(options.draftIdentity, options.draftDefinition, runtimeState, options.taskBasisReference) : options.replacementDefinition ? replaceReplanDefinitionSections(body, options.replacementDefinition) : body;
  if (options.taskBasisReference && !(options.draftDefinition && options.draftIdentity)) {
    nextBody = replaceTaskBasisReference(nextBody, options.taskBasisReference);
  }
  if (options.draftIdentity && !(options.draftDefinition && options.draftIdentity)) {
    nextBody = replaceTaskInfoField(nextBody, "任务 ID", options.draftIdentity.task_id);
    nextBody = replaceTaskInfoField(nextBody, "任务标题", options.draftIdentity.task_title);
    nextBody = replaceTaskInfoField(nextBody, "任务 slug", options.draftIdentity.task_slug);
  }
  nextBody = renderCurrentTaskLifecycleFields(nextBody, runtimeState);
  if (compact)
    nextBody = compactHistoryPreview(nextBody, runtimeState, options.audit);
  else if (options.audit)
    nextBody = appendExecutionAuditToBody(nextBody, options.audit);
  const expandedContent = `---
${stringify4(nextFrontmatter).trimEnd()}
---
${nextBody}`;
  return compact && (options.draftDocumentId !== undefined || isRecord4(existingBinding) && existingBinding.format === ACTIVE_TASK_FORMAT) ? prepareTaskProjection(expandedContent) : { content: expandedContent, expandedContent, objects: [] };
}
function currentTaskPathForRoot(root2) {
  const resolvedRoot = path12.resolve(root2);
  const profilePath = getWorkflowProfilePath(resolvedRoot);
  if (!fs11.existsSync(profilePath))
    fail3("RUNTIME_SOURCE_MISSING", `PROJECT_PROFILE.yaml is missing: ${profilePath}`);
  const profile = loadProfile(profilePath);
  const filePath = getWorkflowDocPath(resolvedRoot, profile, "CURRENT_TASK.md");
  const relativePath = path12.relative(resolvedRoot, filePath).replace(/\\/g, "/");
  if (!relativePath || relativePath.startsWith("../") || path12.isAbsolute(relativePath))
    fail3("RUNTIME_PATH_INVALID", "CURRENT_TASK path escapes the target root.");
  return { filePath, relativePath: relativePath || CURRENT_TASK_RELATIVE_FALLBACK };
}
function walkMarkdownFiles(directory) {
  if (!fs11.existsSync(directory))
    return [];
  const files = [];
  const visit = (currentDirectory) => {
    for (const entry of fs11.readdirSync(currentDirectory, { withFileTypes: true })) {
      const entryPath = path12.join(currentDirectory, entry.name);
      if (entry.isDirectory())
        visit(entryPath);
      else if (entry.isFile() && entry.name.endsWith(".md"))
        files.push(entryPath);
    }
  };
  visit(directory);
  return files;
}
function allocateNextTaskId(root2, currentTaskId) {
  try {
    validateTaskId(currentTaskId);
  } catch (error) {
    fail3("RUNTIME_IDENTITY_INVALID", error instanceof Error ? error.message : String(error));
  }
  const current = BigInt(currentTaskId);
  const taskDirectory = path12.join(path12.resolve(root2), "TASKS");
  const usedIds = new Set([current]);
  const taskFilePattern = /^TASK-([0-9]{3,})-[a-z0-9]+(?:-[a-z0-9]+)*\.md$/;
  for (const taskFile of walkMarkdownFiles(taskDirectory)) {
    const match = taskFilePattern.exec(path12.basename(taskFile));
    if (match) {
      usedIds.add(BigInt(match[1]));
    }
  }
  let next = current + 1n;
  while (usedIds.has(next)) {
    next += 1n;
  }
  return next.toString().padStart(Math.max(3, currentTaskId.length), "0");
}
function collectTaskDocumentIds(root2) {
  const { filePath } = currentTaskPathForRoot(root2);
  const documentIds = new Set;
  const allFiles = [filePath, ...walkMarkdownFiles(path12.join(path12.resolve(root2), "TASKS"))];
  for (const file of allFiles) {
    if (!fs11.existsSync(file))
      continue;
    const content = fs11.readFileSync(file, "utf8");
    for (const match of content.matchAll(/^\s*(?:-\s*)?document_id:\s*['"]?(doc-[a-f0-9]{24})['"]?\s*$/gim)) {
      documentIds.add(match[1]);
    }
  }
  return documentIds;
}
function generatedDraftDocumentId(identity, sourceRevision) {
  return `doc-${sha2565(`${identity.task_id}:${identity.task_slug}:${sourceRevision}`).slice(0, 24)}`;
}
function validateCurrentTaskStoreBinding(value, documentId, location) {
  const binding = expectRecord2(value, location);
  expectExactKeys2(binding, ["schema_version", "kind", "format", "manifest_path", "history", ...binding.format === ACTIVE_TASK_FORMAT ? ["projection"] : []], location);
  if (binding.schema_version !== 1 || binding.kind !== "vnext-current-task-store-binding" || !["compact-v2", ACTIVE_TASK_FORMAT].includes(binding.format)) {
    fail3("RUNTIME_STORAGE_COMPACT_INVALID", `${location} is not a supported compact task-store binding.`);
  }
  const manifestPath = expectString2(binding.manifest_path, `${location}.manifest_path`);
  if (manifestPath.startsWith("/") || /^[A-Za-z]:/u.test(manifestPath) || manifestPath.split("/").includes("..") || !manifestPath.endsWith(`/task-data/${documentId}/manifest.json`)) {
    fail3("RUNTIME_STORAGE_COMPACT_INVALID", `${location}.manifest_path must point to this document's repository-relative task-data manifest.`);
  }
  const history = expectRecord2(binding.history, `${location}.history`);
  expectExactKeys2(history, ["execution_log", "applied_proposals"], `${location}.history`);
  if (history.execution_log !== "task-store" || history.applied_proposals !== "task-store") {
    fail3("RUNTIME_STORAGE_COMPACT_INVALID", `${location}.history must bind both histories to task-store.`);
  }
  return {
    schema_version: 1,
    kind: "vnext-current-task-store-binding",
    format: binding.format,
    ...binding.format === ACTIVE_TASK_FORMAT ? { projection: binding.projection } : {},
    manifest_path: manifestPath,
    history: { execution_log: "task-store", applied_proposals: "task-store" }
  };
}
function parseCanonicalCurrentTaskContent(raw, filePath, relativePath, prepared) {
  const { expandedContent } = expandTaskProjection(raw, filePath, relativePath, prepared);
  const { frontmatter, body } = parseYamlFrontmatter(expandedContent, relativePath);
  if (frontmatter.kind !== VNEXT_CURRENT_TASK_KIND) {
    fail3("MIGRATION_REQUIRED", `${relativePath} is not a pure vNext CURRENT_TASK document; run the Migration Pack.`);
  }
  const authorityVersionPresent = frontmatter.mutation_authority_version !== undefined;
  const authorityValuePresent = frontmatter.mutation_authority !== undefined;
  const authorityMarkerPresent = authorityVersionPresent || authorityValuePresent;
  expectExactKeys2(frontmatter, [
    "schema_version",
    "kind",
    "document_id",
    "runtime_state",
    ...frontmatter.task_store === undefined ? [] : ["task_store"],
    ...authorityVersionPresent ? ["mutation_authority_version"] : [],
    ...authorityValuePresent ? ["mutation_authority"] : []
  ], `${relativePath} frontmatter`);
  if (frontmatter.schema_version !== 1)
    fail3("RUNTIME_SCHEMA_INVALID", `${relativePath}.schema_version must be 1 for a vNext CURRENT_TASK document.`);
  if (authorityMarkerPresent && authorityVersionPresent && ![1, MUTATION_AUTHORITY_VERSION].includes(frontmatter.mutation_authority_version)) {
    fail3("MUTATION_AUTHORITY_VERSION_UNSUPPORTED", `${relativePath}.mutation_authority_version must be 1 or 2.`);
  }
  let mutationAuthority = null;
  if (frontmatter.mutation_authority_version === MUTATION_AUTHORITY_VERSION) {
    if (!authorityValuePresent)
      fail3("MUTATION_AUTHORITY_SCHEMA_INVALID", `${relativePath}.mutation_authority is required when mutation_authority_version=2.`);
    try {
      mutationAuthority = normalizeTaskMutationAuthority(frontmatter.mutation_authority);
    } catch (error) {
      fail3(error instanceof MutationAuthorityError ? error.code : "MUTATION_AUTHORITY_SCHEMA_INVALID", error instanceof Error ? error.message : String(error));
    }
  } else if (authorityValuePresent) {
    fail3("MUTATION_AUTHORITY_SCHEMA_INVALID", `${relativePath}.mutation_authority is only valid when mutation_authority_version=2.`);
  }
  const documentId = expectString2(frontmatter.document_id, `${relativePath}.document_id`);
  if (!DOCUMENT_ID_PATTERN.test(documentId))
    fail3("RUNTIME_SCHEMA_INVALID", `${relativePath}.document_id is invalid.`);
  const storeBinding = frontmatter.task_store === undefined ? null : validateCurrentTaskStoreBinding(frontmatter.task_store, documentId, `${relativePath}.task_store`);
  const runtimeState = validateVNextRuntimeState(frontmatter.runtime_state, { storeBackedHistory: storeBinding !== null });
  try {
    parseMutationScope(body, sha2565(raw));
  } catch (error) {
    if (error instanceof MutationScopeError)
      fail3(error.code, error.message);
    fail3("MUTATION_SCOPE_INVALID", error instanceof Error ? error.message : String(error));
  }
  const bodyAuthority = parseMutationAuthoritySection(body);
  if (mutationAuthority !== null !== (bodyAuthority !== null)) {
    fail3("MUTATION_AUTHORITY_SCHEMA_INVALID", `${relativePath} frontmatter and body must declare the same Mutation Authority v2 marker.`);
  }
  if (mutationAuthority && bodyAuthority && digest3(mutationAuthority) !== digest3(bodyAuthority.mutation_authority)) {
    fail3("MUTATION_AUTHORITY_SCHEMA_INVALID", `${relativePath} frontmatter and body Mutation Authority values conflict.`);
  }
  const identity = extractTaskIdentityFromCurrentTask(body);
  const bodyState = extractCurrentTaskStateFromCurrentTask(body);
  if (identity.id !== runtimeState.task_id || identity.slug !== runtimeState.task_slug) {
    fail3("RUNTIME_SOURCE_CONFLICT", "CURRENT_TASK body identity conflicts with runtime_state.");
  }
  if (bodyState.workflowStatus !== runtimeState.workflow_status || bodyState.lifecycleState !== runtimeState.lifecycle_state) {
    fail3("RUNTIME_SOURCE_CONFLICT", "CURRENT_TASK body lifecycle tuple conflicts with runtime_state.");
  }
  if (bodyState.resumeRequiresReview !== runtimeState.resume_requires_review) {
    fail3("RUNTIME_SOURCE_CONFLICT", "CURRENT_TASK body resume gate conflicts with runtime_state.");
  }
  let bodyResumeReasons;
  try {
    bodyResumeReasons = normalizeResumeReviewReasons(bodyState.resumeReviewReasons);
  } catch (error) {
    fail3("RUNTIME_SOURCE_CONFLICT", error instanceof Error ? error.message : String(error));
  }
  if (bodyResumeReasons.join("|") !== runtimeState.resume_review_reasons.join("|")) {
    fail3("RUNTIME_SOURCE_CONFLICT", "CURRENT_TASK body resume review reasons conflict with runtime_state.");
  }
  resolveTaskStepForState(body, runtimeState.active_step_id);
  const sourceTuple = {
    path: relativePath,
    revision: sha2565(raw),
    document_id: documentId,
    task_id: runtimeState.task_id,
    task_slug: runtimeState.task_slug,
    workflow_status: runtimeState.workflow_status,
    lifecycle_state: runtimeState.lifecycle_state,
    active_step_id: runtimeState.active_step_id,
    active_step_status: runtimeState.active_step_status,
    finding_queue_revision: runtimeState.finding_queue_revision,
    resume_requires_review: runtimeState.resume_requires_review,
    resume_review_reasons: [...runtimeState.resume_review_reasons]
  };
  return { filePath, relativePath, raw, frontmatter, body, runtimeState, mutationAuthority, sourceTuple };
}
function taskSourceRevisionMatches(root2, current, sourceRevision) {
  if (sourceRevision === current.sourceTuple.revision)
    return true;
  const preimages = readStorageMigrationLineage(root2, current, sourceRevision);
  if (!preimages)
    return false;
  const expected = digest3(migrationSemanticModel(current));
  return preimages.every((raw) => {
    const previous = parseCanonicalCurrentTaskContent(raw, current.filePath, current.relativePath);
    return digest3(migrationSemanticModel(previous)) === expected;
  });
}
function hydrateCompactRuntimeHistory(root2, current) {
  const binding = current.frontmatter.task_store;
  const store = TaskStore.forCurrent(root2, current);
  let manifest;
  try {
    manifest = store.manifest;
  } catch (error) {
    fail3("RUNTIME_STORAGE_RECOVERY_REQUIRED", error instanceof Error ? error.message : String(error));
  }
  if (!manifest)
    fail3("RUNTIME_STORAGE_RECOVERY_REQUIRED", "compact CURRENT_TASK has no committed task-store manifest; explicit storage recovery is required.");
  if (store.hasPendingCommit)
    fail3("RUNTIME_STORAGE_RECOVERY_REQUIRED", "task-store has a pending commit; recover it before reading or executing the task.");
  if (binding.manifest_path !== `${manifest.storage_root}/manifest.json`) {
    fail3("RUNTIME_STORAGE_COMPACT_INVALID", "CURRENT_TASK task-store manifest path does not match the committed aggregate.");
  }
  if (!["compact-v2", ACTIVE_TASK_FORMAT].includes(manifest.current_representation) || manifest.storage_format !== "vnext-task-store/v2") {
    fail3("RUNTIME_STORAGE_COMPACT_INVALID", "compact CURRENT_TASK is bound to a non-compact task-store manifest.");
  }
  if (manifest.head.source_revision !== current.sourceTuple.revision) {
    fail3("RUNTIME_STORAGE_RECOVERY_REQUIRED", "CURRENT_TASK and task-store manifest do not name the same committed source revision.");
  }
  const history = store.hydrateHistory(current);
  const executionLog = history.execution_log.map((entry, index) => validateExecutionLogEntry(entry, `task-store.execution_log[${index}]`, current.runtimeState.task_id, current.runtimeState.task_slug));
  const appliedProposals = history.applied_proposals.map((entry, index) => {
    const item = expectRecord2(entry, `task-store.applied_proposals[${index}]`);
    expectExactKeys2(item, ["idempotency_key", "operation_kind", "proposal_digest", "source_revision"], `task-store.applied_proposals[${index}]`);
    return {
      idempotency_key: expectString2(item.idempotency_key, `task-store.applied_proposals[${index}].idempotency_key`, SAFE_KEY_PATTERN2),
      operation_kind: expectEnum(item.operation_kind, RUNTIME_OPERATION_KINDS, `task-store.applied_proposals[${index}].operation_kind`),
      proposal_digest: expectString2(item.proposal_digest, `task-store.applied_proposals[${index}].proposal_digest`, SHA256_PATTERN2),
      source_revision: expectString2(item.source_revision, `task-store.applied_proposals[${index}].source_revision`, SHA256_PATTERN2)
    };
  });
  current.runtimeState = { ...current.runtimeState, execution_log: executionLog, applied_proposals: appliedProposals };
  Object.defineProperty(current.runtimeState, "__vnext_compact_history", { value: true, enumerable: false, configurable: true });
  const validation = store.validateCurrentAggregate(current);
  if (validation.status !== "valid")
    fail3("RUNTIME_STORAGE_RECOVERY_REQUIRED", `compact task-store validation failed: ${validation.errors.join(" | ")}`);
}
function readCanonicalCurrentTask(root2) {
  const { filePath, relativePath } = currentTaskPathForRoot(root2);
  assertGovernanceReadable(filePath);
  assertNoArtifactPublication(filePath);
  recoverTaskEvolution(filePath);
  if (!fs11.existsSync(filePath))
    fail3("RUNTIME_SOURCE_MISSING", `CURRENT_TASK.md is missing: ${relativePath}`);
  const current = parseCanonicalCurrentTaskContent(fs11.readFileSync(filePath, "utf8"), filePath, relativePath);
  if (current.mutationAuthority) {
    current.mutationAuthority = validateTaskMutationAuthority(root2, current.mutationAuthority);
  }
  if (current.frontmatter.task_store !== undefined)
    hydrateCompactRuntimeHistory(root2, current);
  else {
    const legacyStore = TaskStore.forCurrent(root2, current);
    if (legacyStore.hasPendingCommit && !governanceWriteLockIsHeld(root2)) {
      fail3("RUNTIME_STORAGE_RECOVERY_REQUIRED", "CURRENT_TASK has a pending task-store commit; recover it before reading or executing the task.");
    }
  }
  assertRecoveryHistory(root2, current);
  if (current.runtimeState.preservation_source_revision) {
    assertTaskHistoryForRevision(filePath, current.sourceTuple.document_id, current.runtimeState.task_id, current.runtimeState.preservation_source_revision, "initialize-preservation");
  }
  return current;
}
function recoveryCurrentFromRaw(raw, filePath, relativePath, runtimeState) {
  const parsed = parseCanonicalCurrentTaskContent(raw, filePath, relativePath);
  if (!isRecord4(runtimeState))
    throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", "pending commit is missing its exact runtime after-image.");
  parsed.runtimeState = runtimeState;
  if (parsed.frontmatter.task_store !== undefined) {
    Object.defineProperty(parsed.runtimeState, "__vnext_compact_history", { value: true, enumerable: false, configurable: true });
  }
  return parsed;
}
function recoverPendingTaskStoreCommit(root2) {
  const { filePath, relativePath } = currentTaskPathForRoot(root2);
  if (!fs11.existsSync(filePath))
    return;
  const raw = fs11.readFileSync(filePath, "utf8");
  const parsed = parseCanonicalCurrentTaskContent(raw, filePath, relativePath);
  const store = TaskStore.forCurrent(root2, parsed);
  const pending = store.pendingCommit;
  if (!isRecord4(pending))
    return;
  const manifest = store.manifest;
  if (!manifest)
    throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", "pending task-store commit has no manifest to recover against.");
  const sequence = pending.sequence;
  const sourceRevision = pending.source_revision;
  const resultingRevision = pending.resulting_source_revision;
  if (typeof sequence !== "number" || typeof sourceRevision !== "string" || typeof resultingRevision !== "string") {
    throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", "pending task-store commit has incomplete recovery coordinates.");
  }
  if (manifest.head.event_sequence > sequence || manifest.head.event_sequence === sequence) {
    if (manifest.head.event_sequence !== sequence || manifest.head.source_revision !== resultingRevision) {
      throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", "pending task-store commit conflicts with the already-published aggregate head.");
    }
    if (parsed.sourceTuple.revision !== resultingRevision) {
      throw new TaskStoreError("TASK_STORE_SOURCE_CONFLICT", "CURRENT_TASK does not match the already-published pending task-store head.");
    }
    reconcilePendingWriteSet(root2, parsed.sourceTuple.revision, pending);
    store.clearPendingForNoCommit();
    return;
  }
  if (manifest.head.event_sequence !== sequence - 1 || manifest.head.source_revision !== sourceRevision) {
    throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", "pending task-store commit is not directly after the committed aggregate head.");
  }
  if (parsed.sourceTuple.revision === sourceRevision) {
    reconcilePendingWriteSet(root2, parsed.sourceTuple.revision, pending);
    store.clearPendingForNoCommit();
    return;
  }
  if (parsed.sourceTuple.revision !== resultingRevision || typeof pending.before_raw !== "string" || typeof pending.after_raw !== "string" || pending.before_raw === undefined || pending.after_raw === undefined || !isRecord4(pending.before_runtime_state) || !isRecord4(pending.after_runtime_state) || !isRecord4(pending.proposal) || !isRecord4(pending.result)) {
    throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", "CURRENT_TASK advanced during a pending commit but exact recovery material is unavailable.");
  }
  if (sha2565(pending.before_raw) !== sourceRevision || sha2565(pending.after_raw) !== resultingRevision) {
    throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", "pending task-store after-image bytes do not match their recorded revisions.");
  }
  let beforeRuntimeState = pending.before_runtime_state;
  let afterRuntimeState = pending.after_runtime_state;
  if (parsed.frontmatter.task_store !== undefined) {
    const persistedExecutionLog = store.readExecutionLog();
    const persistedAppliedProposals = store.readAppliedProposals();
    if (!Array.isArray(beforeRuntimeState.execution_log))
      beforeRuntimeState = { ...beforeRuntimeState, execution_log: persistedExecutionLog };
    if (!Array.isArray(beforeRuntimeState.applied_proposals))
      beforeRuntimeState = { ...beforeRuntimeState, applied_proposals: persistedAppliedProposals };
    if (!Array.isArray(afterRuntimeState.execution_log)) {
      const delta = Array.isArray(pending.execution_log_entries) ? pending.execution_log_entries : [];
      afterRuntimeState = { ...afterRuntimeState, execution_log: [...persistedExecutionLog, ...delta] };
    }
    if (!Array.isArray(afterRuntimeState.applied_proposals)) {
      const delta = Array.isArray(pending.applied_proposals) ? pending.applied_proposals : [];
      afterRuntimeState = { ...afterRuntimeState, applied_proposals: [...persistedAppliedProposals, ...delta] };
    }
  }
  const before = recoveryCurrentFromRaw(pending.before_raw, filePath, relativePath, beforeRuntimeState);
  const after = recoveryCurrentFromRaw(pending.after_raw, filePath, relativePath, afterRuntimeState);
  if (before.sourceTuple.revision !== sourceRevision || after.sourceTuple.revision !== resultingRevision || before.sourceTuple.document_id !== parsed.sourceTuple.document_id || after.sourceTuple.document_id !== parsed.sourceTuple.document_id) {
    throw new TaskStoreError("TASK_STORE_IDENTITY_CONFLICT", "pending task-store recovery images do not describe one task aggregate.");
  }
  reconcilePendingWriteSet(root2, parsed.sourceTuple.revision, pending);
  store.markCurrentPublished(resultingRevision);
  const recovered = store.recordCommit({
    before,
    after,
    proposal: pending.proposal,
    result: pending.result
  });
  if (!recovered || recovered.head.source_revision !== resultingRevision || recovered.head.event_sequence !== sequence) {
    throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", "pending task-store recovery did not publish the expected aggregate head.");
  }
}
function readCanonicalTaskBasis(root2, current = readCanonicalCurrentTask(root2)) {
  const reference = readTaskBasisReferenceFromBody(current.body);
  if (!reference)
    fail3("TASK_BASIS_MISSING", "CURRENT_TASK does not link an exact task basis.");
  const expectedPath = taskBasisRelativePath(current.relativePath, current.runtimeState.task_id);
  if (reference.path !== expectedPath) {
    fail3("TASK_BASIS_REFERENCE_INVALID", `CURRENT_TASK task basis path must be ${expectedPath}.`);
  }
  const filePath = taskBasisFilePath(root2, reference.path);
  if (!fs11.existsSync(filePath))
    fail3("TASK_BASIS_MISSING", `Linked task basis is missing: ${reference.path}`);
  const content = fs11.readFileSync(filePath, "utf8");
  const revision = sha2565(content);
  if (revision !== reference.revision)
    fail3("TASK_BASIS_REVISION_CONFLICT", "Linked task basis revision does not match its file content.");
  const { frontmatter, body } = parseYamlFrontmatter(content, reference.path);
  expectExactKeys2(frontmatter, ["schema_version", "kind", "task_id", "document_id", "original_request", "user_decisions"], `${reference.path} frontmatter`);
  if (frontmatter.schema_version !== 1 || frontmatter.kind !== VNEXT_TASK_BASIS_KIND) {
    fail3("TASK_BASIS_INVALID", `${reference.path} is not a supported vNext task basis.`);
  }
  const taskId = expectString2(frontmatter.task_id, `${reference.path}.task_id`);
  const documentId = expectString2(frontmatter.document_id, `${reference.path}.document_id`);
  if (taskId !== current.runtimeState.task_id || documentId !== current.sourceTuple.document_id) {
    fail3("TASK_BASIS_IDENTITY_CONFLICT", "Linked task basis identity does not match CURRENT_TASK.");
  }
  if (!/^# vNext TASK_BASIS\s*$/mu.test(body))
    fail3("TASK_BASIS_INVALID", `${reference.path} is missing its canonical heading.`);
  const basis = validateTaskBasis({
    original_request: frontmatter.original_request,
    user_decisions: frontmatter.user_decisions
  }, reference.path);
  return {
    ...reference,
    filePath,
    content,
    basis,
    task_id: taskId,
    document_id: documentId
  };
}
function workflowDocPathForRoot(root2, file, missingCode = "RUNTIME_SOURCE_MISSING") {
  const resolvedRoot = path12.resolve(root2);
  const profilePath = getWorkflowProfilePath(resolvedRoot);
  if (!fs11.existsSync(profilePath))
    fail3(missingCode, `PROJECT_PROFILE.yaml is missing: ${profilePath}`);
  const profile = loadProfile(profilePath);
  const filePath = getWorkflowDocPath(resolvedRoot, profile, file);
  const relativePath = path12.relative(resolvedRoot, filePath).replace(/\\/g, "/");
  if (!relativePath || relativePath.startsWith("../") || path12.isAbsolute(relativePath)) {
    fail3("RUNTIME_PATH_INVALID", `${file} path escapes the target root.`);
  }
  return { filePath, relativePath };
}
function archivePathForTask(root2, current) {
  let relativePath;
  try {
    relativePath = getTaskArtifactPath(current.runtimeState.task_id, current.runtimeState.task_slug, "archive");
  } catch (error) {
    fail3("RUNTIME_PATH_INVALID", error instanceof Error ? error.message : String(error));
  }
  const resolvedRoot = path12.resolve(root2);
  const filePath = path12.resolve(resolvedRoot, ...relativePath.split("/"));
  const relativeCheck = path12.relative(resolvedRoot, filePath).replace(/\\/g, "/");
  if (relativeCheck !== relativePath || relativeCheck.startsWith("../") || path12.isAbsolute(relativeCheck)) {
    fail3("RUNTIME_PATH_INVALID", `archive path escapes the target root: ${relativePath}`);
  }
  return { filePath, relativePath };
}
function canonicalInboxRecordTarget(root2, delta) {
  const itemId = validateInboxItemId(delta.record.item_id, "semantic_delta.record.item_id");
  const itemSlug = expectString2(delta.item_slug, "semantic_delta.item_slug");
  try {
    validateTaskSlug(itemSlug);
  } catch (error) {
    fail3("RUNTIME_IDENTITY_INVALID", error instanceof Error ? error.message : String(error));
  }
  const relativePath = `TASKS/inbox/INBOX-${itemId}-${itemSlug}.md`;
  if (delta.target_path !== relativePath) {
    fail3("RUNTIME_PATH_INVALID", "inbox target_path is not the canonical identity-derived path.");
  }
  const resolvedRoot = path12.resolve(root2);
  const filePath = path12.resolve(resolvedRoot, ...relativePath.split("/"));
  const relativeCheck = path12.relative(resolvedRoot, filePath).replace(/\\/g, "/");
  if (relativeCheck !== relativePath || relativeCheck.startsWith("../") || path12.isAbsolute(relativeCheck)) {
    fail3("RUNTIME_PATH_INVALID", `inbox path escapes the target root: ${relativePath}`);
  }
  return { filePath, relativePath };
}
function renderInboxTextBlock(value) {
  return value.replace(/\r\n?/g, `
`).split(`
`).map((line) => `    ${line}`).join(`
`);
}
function inboxRecordProvenance(proposal) {
  const delta = proposal.semantic_delta;
  return {
    idempotency_key: proposal.idempotency_key,
    proposal_digest: digest3(proposal),
    source_revision: proposal.source_tuple.revision,
    source_task_id: proposal.source_tuple.task_id,
    source_task_slug: proposal.source_tuple.task_slug,
    source_document_id: proposal.source_tuple.document_id,
    relation_evidence_refs: [...delta.relation_evidence_refs],
    duplicate_check: delta.duplicate_check,
    proposed_owner: delta.proposed_owner
  };
}
function renderInboxRecord(proposal) {
  const delta = proposal.semantic_delta;
  const record = delta.record;
  const marker = `<!-- vNext inbox record: ${JSON.stringify(inboxRecordProvenance(proposal))} -->`;
  return [
    `# INBOX-${record.item_id}-${delta.item_slug}`,
    "",
    marker,
    "",
    `- artifact_kind: ${record.artifact_kind}`,
    `- item_id: ${record.item_id}`,
    `- title: ${record.title}`,
    `- type: ${record.type}`,
    `- source: ${record.source}`,
    `- captured_at: ${record.captured_at}`,
    `- relation_to_current_task: ${record.relation_to_current_task}`,
    `- current_task_id: ${record.current_task_id}`,
    "- description: |",
    renderInboxTextBlock(record.description),
    "- evidence: |",
    renderInboxTextBlock(record.evidence),
    `- suggested_next_action: ${record.suggested_next_action}`,
    `- status: ${record.status}`,
    ""
  ].join(`
`);
}
function validateInboxProvenance(value, location) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, ["idempotency_key", "proposal_digest", "source_revision", "source_task_id", "source_task_slug", "source_document_id", "relation_evidence_refs", "duplicate_check", "proposed_owner"], location);
  const proposalDigest = expectString2(record.proposal_digest, `${location}.proposal_digest`);
  const sourceRevision = expectString2(record.source_revision, `${location}.source_revision`);
  if (!SHA256_PATTERN2.test(proposalDigest) || !SHA256_PATTERN2.test(sourceRevision)) {
    fail3("INBOX_PROVENANCE_INVALID", `${location} contains an invalid proposal or source revision.`);
  }
  const sourceTaskId = expectString2(record.source_task_id, `${location}.source_task_id`);
  const sourceTaskSlug = expectString2(record.source_task_slug, `${location}.source_task_slug`);
  try {
    validateTaskId(sourceTaskId);
    validateTaskSlug(sourceTaskSlug);
  } catch (error) {
    fail3("INBOX_PROVENANCE_INVALID", error instanceof Error ? error.message : String(error));
  }
  const sourceDocumentId = expectString2(record.source_document_id, `${location}.source_document_id`);
  if (!DOCUMENT_ID_PATTERN.test(sourceDocumentId))
    fail3("INBOX_PROVENANCE_INVALID", `${location}.source_document_id is invalid.`);
  return {
    idempotency_key: expectString2(record.idempotency_key, `${location}.idempotency_key`, SAFE_KEY_PATTERN2),
    proposal_digest: proposalDigest,
    source_revision: sourceRevision,
    source_task_id: sourceTaskId,
    source_task_slug: sourceTaskSlug,
    source_document_id: sourceDocumentId,
    relation_evidence_refs: validateEvidenceRefs(record.relation_evidence_refs, `${location}.relation_evidence_refs`),
    duplicate_check: expectEnum(record.duplicate_check, ["clear"], `${location}.duplicate_check`),
    proposed_owner: expectEnum(record.proposed_owner, INBOX_SUGGESTED_NEXT_ACTIONS, `${location}.proposed_owner`)
  };
}
function readInboxProvenanceMarkers(content, location) {
  if (!content.includes(INBOX_RECORD_PROVENANCE_MARKER))
    return [];
  const pattern = /<!-- vNext inbox record: (\{[^\r\n]+\}) -->/g;
  const markers = [];
  for (const match of content.matchAll(pattern)) {
    let parsed;
    try {
      parsed = JSON.parse(match[1]);
    } catch {
      fail3("INBOX_PROVENANCE_INVALID", `${location} contains an invalid vNext inbox provenance marker.`);
    }
    markers.push(validateInboxProvenance(parsed, `${location}.inbox_marker`));
  }
  if (countExactOccurrences(content, INBOX_RECORD_PROVENANCE_MARKER) !== markers.length) {
    fail3("INBOX_PROVENANCE_INVALID", `${location} contains a malformed vNext inbox provenance marker.`);
  }
  return markers;
}
function scanInboxRecordFiles(root2) {
  const resolvedRoot = path12.resolve(root2);
  const inboxRoot = path12.join(resolvedRoot, "TASKS", "inbox");
  return walkMarkdownFiles(inboxRoot).map((filePath) => {
    const relativePath = path12.relative(resolvedRoot, filePath).replace(/\\/g, "/");
    const pathMatch = INBOX_RECORD_PATH_PATTERN.exec(relativePath);
    const content = fs11.readFileSync(filePath, "utf8");
    const provenance = readInboxProvenanceMarkers(content, relativePath);
    if (provenance.length > 0 && !pathMatch) {
      fail3("INBOX_PATH_INVALID", `${relativePath} contains vNext inbox provenance but is not a canonical inbox path.`);
    }
    if (!pathMatch) {
      fail3("INBOX_PATH_INVALID", `${relativePath} is not a canonical vNext inbox record path.`);
    }
    return {
      filePath,
      relativePath,
      itemId: pathMatch ? `${pathMatch[1]}-${pathMatch[2]}` : null,
      provenance
    };
  });
}
function assertCanonicalInboxRecordContent(content, proposal, location) {
  const expected = renderInboxRecord(proposal);
  if (content !== expected) {
    fail3("INBOX_PROVENANCE_MISMATCH", `${location} does not match the exact typed inbox record bytes.`);
  }
  const markers = readInboxProvenanceMarkers(content, location);
  if (markers.length !== 1)
    fail3("INBOX_PROVENANCE_INVALID", `${location} must contain exactly one vNext inbox provenance marker.`);
  const expectedMarker = inboxRecordProvenance(proposal);
  if (JSON.stringify(markers[0]) !== JSON.stringify(expectedMarker)) {
    fail3("INBOX_PROVENANCE_MISMATCH", `${location} provenance does not match the typed proposal.`);
  }
  validateInboxRecord(proposal.semantic_delta.record, `${location}.record`);
}
function inspectInboxRecordTransaction(root2, proposal) {
  const delta = proposal.semantic_delta;
  const target = canonicalInboxRecordTarget(root2, delta);
  if (fs11.existsSync(target.filePath)) {
    if (!fs11.statSync(target.filePath).isFile()) {
      fail3("INBOX_IDENTITY_CONFLICT", `${target.relativePath} exists but is not a regular inbox record file.`);
    }
    const existingContent = fs11.readFileSync(target.filePath, "utf8");
    try {
      assertCanonicalInboxRecordContent(existingContent, proposal, target.relativePath);
    } catch (error) {
      if (error instanceof VNextRuntimeError)
        fail3("INBOX_IDENTITY_CONFLICT", `${target.relativePath} already exists with different semantic or provenance content.`);
      fail3("INBOX_IDENTITY_CONFLICT", `${target.relativePath} could not be validated as the exact replay record.`);
    }
    return { ...target, nextContent: existingContent, existing: true };
  }
  const files = scanInboxRecordFiles(root2);
  for (const file of files) {
    if (file.itemId === delta.record.item_id && file.relativePath !== target.relativePath) {
      fail3("INBOX_IDENTITY_CONFLICT", `inbox item identity ${delta.record.item_id} is already claimed by ${file.relativePath}.`);
    }
    if (file.provenance.some((marker) => marker.idempotency_key === proposal.idempotency_key && file.relativePath !== target.relativePath)) {
      fail3("IDEMPOTENCY_CONFLICT", "inbox idempotency key is already durably bound to another target.");
    }
  }
  return { ...target, nextContent: renderInboxRecord(proposal), existing: false };
}
function prepareInboxRecordTransaction(root2, current, proposal) {
  ensureAuthorityKinds(proposal, ["evidence-admission"]);
  if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active") {
    fail3("INBOX_CAPTURE_BLOCKED", "inbox record capture requires an active + active current task.");
  }
  const delta = proposal.semantic_delta;
  if (delta.record.current_task_id !== current.runtimeState.task_id) {
    fail3("INBOX_RELATION_INVALID", "inbox record current_task_id does not match the canonical active task.");
  }
  return inspectInboxRecordTransaction(root2, proposal);
}
function yamlScalar(value) {
  if (/^[A-Za-z0-9][A-Za-z0-9._:/+@ -]*$/.test(value) && !value.endsWith(" ") && !value.includes("  "))
    return value;
  return JSON.stringify(value);
}
function yamlStringArray(values) {
  return JSON.stringify(values);
}
function readArchiveScalar(section, field, location) {
  const escaped = field.replace(/[.*+?^${}()|[\\]\\]/g, "\\$&");
  const match = new RegExp(`^-\\s*${escaped}\\s*:\\s*(.*?)\\s*$`, "m").exec(section);
  if (!match)
    fail3("ARCHIVE_INVALID", `${location} is missing ${field}.`);
  const raw = match[1].trim();
  if (raw.startsWith('"') || raw.startsWith("'")) {
    try {
      return JSON.parse(raw);
    } catch {
      fail3("ARCHIVE_INVALID", `${location}.${field} is not a valid scalar.`);
    }
  }
  return expectString2(raw, `${location}.${field}`);
}
function readArchiveArray(raw, location) {
  let parsed;
  try {
    parsed = JSON.parse(raw);
  } catch {
    fail3("ARCHIVE_INVALID", `${location} must be a JSON/YAML inline string array.`);
  }
  return expectStringArray2(parsed, location, true, MAX_EVIDENCE_REFS);
}
function readArchiveLessonAdmission(section, location) {
  const match = /(?:^|\n)lesson_admission:\s*\n\s+decision:\s*(admit|defer|no-op)\s*\n\s+candidate_refs:\s*(\[[^\r\n]*\])\s*\n\s+evidence_refs:\s*(\[[^\r\n]*\])/m.exec(section);
  if (!match)
    fail3("ARCHIVE_INVALID", `${location} is missing the durable lesson_admission record.`);
  return validateLessonAdmission({
    decision: match[1],
    candidate_refs: readArchiveArray(match[2], `${location}.candidate_refs`),
    evidence_refs: readArchiveArray(match[3], `${location}.evidence_refs`)
  }, location);
}
function readArchiveKnowledgeAdmissions(section, location) {
  const match = /(?:^|\n)knowledge_admissions:\s*\n\s+contracts:\s*(\[[^\r\n]*\])\s*\n\s+decisions:\s*(\[[^\r\n]*\])/m.exec(section);
  if (!match)
    fail3("ARCHIVE_INVALID", `${location} is missing the durable knowledge_admissions record.`);
  let contracts;
  let decisions;
  try {
    contracts = JSON.parse(match[1]);
    decisions = JSON.parse(match[2]);
  } catch {
    fail3("ARCHIVE_INVALID", `${location}.knowledge_admissions must contain valid JSON arrays.`);
  }
  return validateKnowledgeAdmissionBundle({ contracts, decisions }, location);
}
function requiredArchiveSections(raw) {
  const sections = scanMarkdownSections2(raw);
  const requiredHeadings = [
    "任务元数据",
    "原始任务包快照",
    "实际改动摘要",
    "契约与决策记录",
    "验证与交付证据",
    "Lessons 回写",
    "后续关联"
  ];
  const result = {};
  for (const heading of requiredHeadings) {
    const section = findUniqueMarkdownSection(sections, [heading], 2);
    if (!section)
      fail3("ARCHIVE_INVALID", `canonical task archive is missing ## ${heading}.`);
    result[heading] = section;
  }
  return result;
}
function readCanonicalArchive(root2, current, expectedPath) {
  const expected = archivePathForTask(root2, current);
  if (expectedPath !== undefined && expectedPath !== expected.relativePath) {
    fail3("RUNTIME_PATH_INVALID", "archive path is not the exact identity-derived path.");
  }
  if (!fs11.existsSync(expected.filePath))
    fail3("ARCHIVE_MISSING", `canonical task archive is missing: ${expected.relativePath}`);
  const raw = fs11.readFileSync(expected.filePath, "utf8");
  const sections = requiredArchiveSections(raw);
  const metadata = raw.slice(sections["任务元数据"].contentStart, sections["任务元数据"].contentEnd);
  const lessonSection = raw.slice(sections["Lessons 回写"].contentStart, sections["Lessons 回写"].contentEnd);
  const knowledgeSection = findUniqueMarkdownSection(scanMarkdownSections2(raw), ["知识晋升", "Knowledge Promotion"], 2);
  const workflowStatus = readArchiveScalar(metadata, "workflow_status", "archive.任务元数据");
  const lifecycleState = readArchiveScalar(metadata, "lifecycle_state", "archive.任务元数据");
  const archiveOperation = readArchiveScalar(metadata, "archive_operation", "archive.任务元数据");
  const archiveCaller = readArchiveScalar(metadata, "archive_caller", "archive.任务元数据");
  const receipt = {
    filePath: expected.filePath,
    relativePath: expected.relativePath,
    raw,
    revision: sha2565(raw),
    taskId: readArchiveScalar(metadata, "task_id", "archive.任务元数据"),
    taskSlug: readArchiveScalar(metadata, "task_slug", "archive.任务元数据"),
    taskTitle: readArchiveScalar(metadata, "task_title", "archive.任务元数据"),
    documentId: readArchiveScalar(metadata, "document_id", "archive.任务元数据"),
    sourceRevision: readArchiveScalar(metadata, "source_revision", "archive.任务元数据"),
    archivePath: readArchiveScalar(metadata, "archive_path", "archive.任务元数据"),
    idempotencyKey: readArchiveScalar(metadata, "proposal_idempotency_key", "archive.任务元数据"),
    closureDeltaDigest: readArchiveScalar(metadata, "closure_delta_digest", "archive.任务元数据"),
    lessonAdmission: readArchiveLessonAdmission(lessonSection, "archive.Lessons 回写.lesson_admission"),
    knowledgeAdmissions: knowledgeSection ? readArchiveKnowledgeAdmissions(raw.slice(knowledgeSection.contentStart, knowledgeSection.contentEnd), "archive.知识晋升.knowledge_admissions") : emptyKnowledgeAdmissionBundle()
  };
  if (!/^[a-f0-9]{64}$/.test(receipt.revision) || !/^[a-f0-9]{64}$/.test(receipt.sourceRevision) || !/^[a-f0-9]{64}$/.test(receipt.closureDeltaDigest)) {
    fail3("ARCHIVE_INVALID", "canonical task archive contains an invalid revision or digest.");
  }
  if (!SAFE_KEY_PATTERN2.test(receipt.idempotencyKey) || !DOCUMENT_ID_PATTERN.test(receipt.documentId)) {
    fail3("ARCHIVE_INVALID", "canonical task archive contains an invalid idempotency key or document_id.");
  }
  if (workflowStatus !== "closed" || lifecycleState !== "archived" || archiveOperation !== "archive-transaction" || archiveCaller !== "close-task") {
    fail3("ARCHIVE_PROVENANCE_MISMATCH", "archive metadata does not declare the frozen close-task terminal provenance.");
  }
  if (receipt.taskId !== current.runtimeState.task_id || receipt.taskSlug !== current.runtimeState.task_slug) {
    fail3("ARCHIVE_PROVENANCE_MISMATCH", "archive identity does not match CURRENT_TASK.");
  }
  const identity = extractTaskIdentityFromCurrentTask(current.body);
  if (identity.title === null || receipt.taskTitle !== identity.title)
    fail3("ARCHIVE_PROVENANCE_MISMATCH", "archive task_title does not match CURRENT_TASK.");
  if (receipt.documentId !== String(current.frontmatter.document_id))
    fail3("ARCHIVE_PROVENANCE_MISMATCH", "archive document_id does not match CURRENT_TASK.");
  if (receipt.archivePath !== expected.relativePath)
    fail3("ARCHIVE_PROVENANCE_MISMATCH", "archive metadata path does not match its canonical path.");
  return receipt;
}
function archiveAudits(current) {
  return current.runtimeState.execution_log.filter((item) => ("action" in item) && item.action === "archive");
}
function assertArchiveReceiptMatches(current, receipt, audit) {
  if (current.runtimeState.workflow_status !== "closed" || current.runtimeState.lifecycle_state !== "archived") {
    fail3("LIFECYCLE_REPLAY_INCOMPLETE", "archive receipt requires the closed + archived CURRENT_TASK tuple.");
  }
  if (audit.task_id !== current.runtimeState.task_id || audit.task_slug !== current.runtimeState.task_slug || audit.document_id !== String(current.frontmatter.document_id)) {
    fail3("ARCHIVE_PROVENANCE_MISMATCH", "archive audit identity does not match CURRENT_TASK.");
  }
  if (audit.archive_path !== receipt.relativePath || audit.archive_revision !== receipt.revision || audit.source_revision !== receipt.sourceRevision || audit.idempotency_key !== receipt.idempotencyKey || audit.closure_delta_digest !== receipt.closureDeltaDigest) {
    fail3("ARCHIVE_PROVENANCE_MISMATCH", "archive receipt does not match the durable CURRENT_TASK archive audit.");
  }
  if (audit.lesson_admission.decision !== receipt.lessonAdmission.decision || audit.lesson_admission.candidate_refs.join("|") !== receipt.lessonAdmission.candidate_refs.join("|") || audit.lesson_admission.evidence_refs.join("|") !== receipt.lessonAdmission.evidence_refs.join("|")) {
    fail3("ARCHIVE_PROVENANCE_MISMATCH", "archive lesson admission does not match the durable CURRENT_TASK archive audit.");
  }
  if (digest3(audit.knowledge_admissions) !== digest3(receipt.knowledgeAdmissions)) {
    fail3("ARCHIVE_PROVENANCE_MISMATCH", "archive knowledge admission does not match the durable CURRENT_TASK archive audit.");
  }
}
function matchingArchiveReceipt(root2, current) {
  const audits = archiveAudits(current);
  if (audits.length !== 1)
    fail3("LIFECYCLE_REPLAY_INCOMPLETE", "CURRENT_TASK must contain exactly one durable archive audit for reconciliation.");
  const audit = audits[0];
  assertExecutionAudit(root2, current, audit);
  if (audit.from_workflow_status !== "active" || audit.from_lifecycle_state !== "active" || audit.to_workflow_status !== "closed" || audit.to_lifecycle_state !== "archived") {
    fail3("LIFECYCLE_REPLAY_INCOMPLETE", "archive audit does not describe the frozen active + active to closed + archived transition.");
  }
  const receipt = readCanonicalArchive(root2, current, audit.archive_path);
  assertArchiveReceiptMatches(current, receipt, audit);
  return { audit, receipt };
}
function closureEligibilityBlockers(root2, current, delta, archiveAlreadyExists) {
  const blockers = [];
  try {
    assertTestStrategySequenceReady(current);
  } catch (error) {
    blockers.push(error instanceof Error ? error.message : String(error));
  }
  const coverage = current.runtimeState.review_coverage;
  if (coverage && (coverage.pending_paths.length || coverage.last_clean_revision !== coverage.target.revision || captureReviewTarget(root2, coverage.target.entries.map((e) => e.path)).revision !== coverage.target.revision))
    blockers.push("cumulative review coverage is pending or stale.");
  const identity = extractTaskIdentityFromCurrentTask(current.body);
  if (identity.id === null || identity.slug === null || identity.title === null)
    blockers.push("task identity is not fully materialized in CURRENT_TASK.");
  if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active")
    blockers.push("first successful close requires active + active.");
  if (current.runtimeState.resume_requires_review || current.runtimeState.resume_review_reasons.length > 0)
    blockers.push("resume review gate is not cleared.");
  if (current.runtimeState.active_step_status !== "completed")
    blockers.push("the admitted current step is not completed.");
  if ((current.runtimeState.evidence_challenges ?? []).some((item) => item.status !== "resolved"))
    blockers.push("challenged evidence remains unresolved.");
  try {
    const stepResolution = resolveCanonicalTaskStep(current);
    const checkpoint = effectiveCheckpointPolicy(stepResolution);
    if (stepResolution.next !== null) {
      blockers.push("remaining implementation steps have not been durably advanced to completion.");
    }
    if (stepResolution.steps.length > 1) {
      const completedRecord = currentDefinitionExecutionLog(current).find((item) => !("action" in item) && item.step_id === stepResolution.current.id && item.status === "completed" && item.advancement === "task-complete");
      if (!completedRecord)
        blockers.push("the final multi-step completion lacks a durable task-complete advancement record.");
      if (checkpoint === "required" && !completedRecord?.review_receipt) {
        blockers.push("the final required review checkpoint has no durable clean receipt.");
      }
    }
    const repairRecords = currentDefinitionExecutionLog(current).filter((item) => !("action" in item) && item.step_id === stepResolution.current.id && item.mode === "repair");
    if (repairRecords.length > 0) {
      const repairFingerprints = [...new Set(repairRecords.flatMap((item) => [
        ...item.repair_fingerprints ?? [],
        ...item.repair_fingerprint ? [item.repair_fingerprint] : []
      ]))];
      const repairTargets = [...new Set(repairRecords.map((item) => item.change_set_id).filter((value) => Boolean(value)))];
      const verified = currentDefinitionExecutionLog(current).some((item) => {
        if ("action" in item || item.step_id !== stepResolution.current.id || item.review_receipt?.cycle_phase !== "verification")
          return false;
        const receipt = item.review_receipt;
        return receipt !== undefined && receipt.admitted_fingerprints.length === repairFingerprints.length && repairFingerprints.every((fingerprint) => receipt.admitted_fingerprints.includes(fingerprint)) && (repairTargets.length === 0 || receipt.change_set_id === repairTargets[0]);
      });
      if (!verified)
        blockers.push("every repair route must have a durable same-diff verification receipt before closure.");
      if (repairRecords.some((item) => !(item.repair_fingerprint || item.repair_fingerprints?.length) || !item.change_set_id))
        blockers.push("a repair execution record is missing its finding fingerprints or logical change-set identity.");
    }
  } catch (error) {
    blockers.push(error instanceof Error ? error.message : String(error));
  }
  if (current.runtimeState.findings.some((item) => item.status === "admitted" || item.status === "in-progress"))
    blockers.push("an admitted or in-progress finding remains unresolved.");
  if (!claimEvidenceStateEnabled(current.runtimeState)) {
    blockers.push("structured claim-bound evidence migration is required before terminal closure; legacy CURRENT_TASK completion evidence is not sufficient.");
  } else {
    const durableClaimEvidence = evaluateClaimEvidence(current.runtimeState.claim_evidence ?? [], { root: root2, current });
    if (delta.closure_evidence.acceptance_satisfied !== durableClaimEvidence.acceptance_satisfied) {
      blockers.push("closure acceptance_satisfied does not match durable claim-bound evidence state.");
    }
    if (delta.closure_evidence.validation_complete !== durableClaimEvidence.validation_complete) {
      blockers.push("closure validation_complete does not match durable claim-bound evidence state.");
    }
    if (!durableClaimEvidence.acceptance_satisfied)
      blockers.push("durable claim-bound acceptance evidence is incomplete.");
    if (!durableClaimEvidence.validation_complete)
      blockers.push("durable claim-bound validation evidence is incomplete.");
  }
  if (!delta.closure_evidence.acceptance_satisfied)
    blockers.push("acceptance evidence is not satisfied.");
  if (!delta.closure_evidence.validation_complete)
    blockers.push("required validation evidence is incomplete.");
  if (!delta.closure_evidence.no_admitted_or_in_progress_findings)
    blockers.push("closure evidence does not prove the finding queue is clear.");
  if (!delta.closure_evidence.no_unresolved_closure_blocker)
    blockers.push("an unresolved closure blocker remains.");
  for (const [label, gate] of [
    ["release", delta.closure_evidence.release_evidence],
    ["rollback", delta.closure_evidence.rollback_evidence],
    ["observation", delta.closure_evidence.observation_evidence]
  ]) {
    if (gate.triggered && !gate.complete)
      blockers.push(`${label} evidence is triggered but incomplete.`);
  }
  if (!delta.closure_evidence.remaining_risks_non_blocking)
    blockers.push("remaining risks are not explicitly non-blocking.");
  if (!delta.closure_evidence.archive_path_verified)
    blockers.push("the archive path has not been uniquely verified.");
  if (archiveAlreadyExists)
    blockers.push("the canonical archive path is already occupied before the first close.");
  return blockers;
}
function assertArchiveReplay(root2, current, proposal) {
  const { audit, receipt } = matchingArchiveReceipt(root2, current);
  if (audit.idempotency_key !== proposal.idempotency_key || audit.source_revision !== proposal.source_tuple.revision || audit.task_id !== proposal.source_tuple.task_id || audit.task_slug !== proposal.source_tuple.task_slug || audit.document_id !== proposal.source_tuple.document_id || audit.closure_delta_digest !== digest3(proposal.semantic_delta) || audit.evidence_refs.join("|") !== proposal.semantic_delta.evidence_refs.join("|") || digest3(audit.authority_evidence) !== digest3(proposal.authority_evidence) || digest3(audit.knowledge_admissions) !== digest3(proposal.semantic_delta.knowledge_admissions ?? emptyKnowledgeAdmissionBundle()) || receipt.revision !== audit.archive_revision) {
    fail3("LIFECYCLE_REPLAY_INCOMPLETE", "archive replay identity, source revision, closure evidence, or archive revision does not match the committed receipt.");
  }
}
function quotedSnapshot(raw) {
  return raw.replace(/\r\n?/g, `
`).split(`
`).map((line) => `> ${line}`).join(`
`);
}
function renderArchiveList(label, values) {
  return [
    `- ${label}:`,
    ...values.length === 0 ? ["  - none"] : values.map((value) => `  - ${yamlScalar(value)}`)
  ];
}
function renderArchiveDocument(current, proposal, delta, archiveRelativePath, closureDeltaDigest) {
  const identity = extractTaskIdentityFromCurrentTask(current.body);
  if (identity.id === null || identity.slug === null || identity.title === null) {
    fail3("RUNTIME_IDENTITY_INVALID", "CURRENT_TASK task identity is incomplete for archive rendering.");
  }
  const closure = delta.closure_evidence;
  const lines = [
    "# TASK_ARCHIVE.md",
    "",
    "## 任务元数据",
    "",
    `- task_id: ${yamlScalar(identity.id)}`,
    `- task_title: ${yamlScalar(identity.title)}`,
    `- task_slug: ${yamlScalar(identity.slug)}`,
    `- document_id: ${yamlScalar(current.sourceTuple.document_id)}`,
    `- workflow_status: closed`,
    `- lifecycle_state: archived`,
    `- source_revision: ${current.sourceTuple.revision}`,
    `- archive_path: ${archiveRelativePath}`,
    `- archive_operation: archive-transaction`,
    `- archive_caller: close-task`,
    `- proposal_idempotency_key: ${yamlScalar(proposal.idempotency_key)}`,
    `- closure_delta_digest: ${closureDeltaDigest}`,
    "",
    "## 原始任务包快照",
    "",
    `- source_document_revision: ${current.sourceTuple.revision}`,
    "- CURRENT_TASK snapshot:",
    quotedSnapshot(current.raw),
    "",
    "## 实际改动摘要",
    "",
    `- goal: ${yamlScalar(delta.delivery_summary.goal)}`,
    ...renderArchiveList("actual_changes", delta.delivery_summary.actual_changes),
    "",
    "## 契约与决策记录",
    "",
    "- affected_contracts: preserved in the CURRENT_TASK snapshot; admitted Contract candidates are reconciled after archive through the typed Runtime operation.",
    "- confirmed_decisions: preserved in the CURRENT_TASK snapshot; admitted Decision candidates are reconciled after archive through the typed Runtime operation.",
    "",
    "## 验证与交付证据",
    "",
    "- closure_evidence:",
    `  - acceptance_satisfied: ${String(closure.acceptance_satisfied)}`,
    `  - validation_complete: ${String(closure.validation_complete)}`,
    `  - no_admitted_or_in_progress_findings: ${String(closure.no_admitted_or_in_progress_findings)}`,
    `  - no_unresolved_closure_blocker: ${String(closure.no_unresolved_closure_blocker)}`,
    "  - release_evidence:",
    `    - triggered: ${String(closure.release_evidence.triggered)}`,
    `    - complete: ${String(closure.release_evidence.complete)}`,
    `    - evidence_refs: ${yamlStringArray(closure.release_evidence.evidence_refs)}`,
    "  - rollback_evidence:",
    `    - triggered: ${String(closure.rollback_evidence.triggered)}`,
    `    - complete: ${String(closure.rollback_evidence.complete)}`,
    `    - evidence_refs: ${yamlStringArray(closure.rollback_evidence.evidence_refs)}`,
    "  - observation_evidence:",
    `    - triggered: ${String(closure.observation_evidence.triggered)}`,
    `    - complete: ${String(closure.observation_evidence.complete)}`,
    `    - evidence_refs: ${yamlStringArray(closure.observation_evidence.evidence_refs)}`,
    `  - remaining_risks_non_blocking: ${String(closure.remaining_risks_non_blocking)}`,
    `  - archive_path_verified: ${String(closure.archive_path_verified)}`,
    "",
    `- acceptance_satisfied: ${String(closure.acceptance_satisfied)}`,
    `- validation_complete: ${String(closure.validation_complete)}`,
    ...renderArchiveList("verification", delta.delivery_summary.verification),
    ...(current.runtimeState.claim_evidence ?? []).some((c) => c.slots.some((s) => s.user_decision)) ? renderArchiveList("explicit_user_evidence_decisions", (current.runtimeState.claim_evidence ?? []).flatMap((c) => c.slots.filter((s) => s.user_decision).map((s) => `${c.claim_id}/${s.slot_id}: ${s.user_decision.kind}; source=${s.user_decision.decision_source}; caller-reported; waiver is not tested PASS`))) : [],
    ...renderArchiveList("release_evidence", delta.delivery_summary.release_evidence),
    ...renderArchiveList("rollback_evidence", delta.delivery_summary.rollback_evidence),
    ...renderArchiveList("observation_evidence", delta.delivery_summary.observation_evidence),
    `- next_action: ${yamlScalar(delta.delivery_summary.next_action)}`,
    "",
    "## 知识晋升",
    "",
    "knowledge_admissions:",
    `  contracts: ${JSON.stringify(delta.knowledge_admissions?.contracts ?? [])}`,
    `  decisions: ${JSON.stringify(delta.knowledge_admissions?.decisions ?? [])}`,
    "",
    "## Lessons 回写",
    "",
    "lesson_admission:",
    `  decision: ${delta.lesson_admission.decision}`,
    `  candidate_refs: ${yamlStringArray(delta.lesson_admission.candidate_refs)}`,
    `  evidence_refs: ${yamlStringArray(delta.lesson_admission.evidence_refs)}`,
    "",
    "## 后续关联",
    "",
    ...renderArchiveList("remaining_risks", delta.remaining_risks),
    `- remaining_risks_non_blocking: ${String(closure.remaining_risks_non_blocking)}`,
    "- next_task: none created by close-task.",
    ""
  ];
  return lines.join(`
`);
}
function makeArchiveAudit(current, proposal, delta, archiveRelativePath, archiveRevision, closureDeltaDigest, next, now) {
  return {
    action: "archive",
    idempotency_key: proposal.idempotency_key,
    operation_kind: "archive-transaction",
    caller: "close-task",
    mode: "default",
    task_id: current.runtimeState.task_id,
    task_slug: current.runtimeState.task_slug,
    document_id: current.sourceTuple.document_id,
    from_workflow_status: "active",
    from_lifecycle_state: "active",
    to_workflow_status: next.workflow_status,
    to_lifecycle_state: next.lifecycle_state,
    source_revision: current.sourceTuple.revision,
    archive_path: archiveRelativePath,
    archive_revision: archiveRevision,
    closure_delta_digest: closureDeltaDigest,
    authority_evidence: proposal.authority_evidence.map((item) => ({ ...item })),
    evidence_refs: [...delta.evidence_refs],
    lesson_admission: {
      decision: delta.lesson_admission.decision,
      candidate_refs: [...delta.lesson_admission.candidate_refs],
      evidence_refs: [...delta.lesson_admission.evidence_refs]
    },
    knowledge_admissions: {
      contracts: (delta.knowledge_admissions?.contracts ?? []).map((item) => ({
        candidate: item.candidate,
        disposition: item.disposition,
        matched_knowledge_id: item.matched_knowledge_id,
        reasons: [...item.reasons]
      })),
      decisions: (delta.knowledge_admissions?.decisions ?? []).map((item) => ({
        candidate: item.candidate,
        disposition: item.disposition,
        matched_knowledge_id: item.matched_knowledge_id,
        reasons: [...item.reasons]
      }))
    },
    recorded_at: now
  };
}
function prepareArchiveTransaction(root2, current, proposal, now) {
  const delta = proposal.semantic_delta;
  ensureAuthorityKinds(proposal, ["active-task-owner", "evidence-admission"]);
  if (current.runtimeState.workflow_status === "closed" && current.runtimeState.lifecycle_state === "archived") {
    const { audit, receipt } = matchingArchiveReceipt(root2, current);
    if (digest3(delta) !== audit.closure_delta_digest) {
      fail3("ARCHIVE_PROVENANCE_MISMATCH", "reconciliation closure evidence does not match the committed archive receipt.");
    }
    if (delta.lesson_admission.decision !== audit.lesson_admission.decision || delta.lesson_admission.candidate_refs.join("|") !== audit.lesson_admission.candidate_refs.join("|") || delta.lesson_admission.evidence_refs.join("|") !== audit.lesson_admission.evidence_refs.join("|")) {
      fail3("ARCHIVE_PROVENANCE_MISMATCH", "reconciliation lesson admission does not match the committed archive receipt.");
    }
    if (digest3(delta.knowledge_admissions ?? emptyKnowledgeAdmissionBundle()) !== digest3(audit.knowledge_admissions)) {
      fail3("ARCHIVE_PROVENANCE_MISMATCH", "reconciliation knowledge admission does not match the committed archive receipt.");
    }
    if (receipt.sourceRevision !== audit.source_revision)
      fail3("ARCHIVE_PROVENANCE_MISMATCH", "archive source revision does not match the committed archive audit.");
    return null;
  }
  if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active") {
    fail3("CLOSURE_TUPLE_INVALID", "first successful close requires active + active.");
  }
  const archiveTarget = archivePathForTask(root2, current);
  const blockers = closureEligibilityBlockers(root2, current, delta, fs11.existsSync(archiveTarget.filePath));
  if (blockers.length > 0)
    fail3("CLOSURE_NOT_ELIGIBLE", blockers.join(" "));
  const closureDeltaDigest = digest3(delta);
  const nextWithoutAudit = {
    ...current.runtimeState,
    workflow_status: "closed",
    lifecycle_state: "archived",
    resume_requires_review: false,
    resume_review_reasons: [],
    applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
  };
  const nextArchiveContent = renderArchiveDocument(current, proposal, delta, archiveTarget.relativePath, closureDeltaDigest);
  const archiveRevision = sha2565(nextArchiveContent);
  const audit = makeArchiveAudit(current, proposal, delta, archiveTarget.relativePath, archiveRevision, closureDeltaDigest, nextWithoutAudit, now);
  const next = {
    ...nextWithoutAudit,
    execution_log: appendExecutionLogEntry(current.runtimeState, audit)
  };
  const prepared = renderCanonicalCurrentTask(current.frontmatter, current.body, next, { audit });
  const nextContent = prepared.content;
  return {
    next,
    nextContent,
    prepared,
    archiveFilePath: archiveTarget.filePath,
    archiveRelativePath: archiveTarget.relativePath,
    nextArchiveContent,
    audit,
    archiveRevision
  };
}
var STATUS_RECONCILIATION_BEGIN = "<!-- BEGIN vNext close-task STATUS reconciliation -->";
var STATUS_RECONCILIATION_END = "<!-- END vNext close-task STATUS reconciliation -->";
function renderStatusReconciliation(proposal, delta, archive) {
  return [
    STATUS_RECONCILIATION_BEGIN,
    `- task_id: ${yamlScalar(archive.taskId)}`,
    `- task_slug: ${yamlScalar(archive.taskSlug)}`,
    `- document_id: ${yamlScalar(archive.documentId)}`,
    `- archive_path: ${archive.relativePath}`,
    `- archive_revision: ${archive.revision}`,
    `- source_revision: ${archive.sourceRevision}`,
    `- proposal_idempotency_key: ${yamlScalar(proposal.idempotency_key)}`,
    `- delta_digest: ${digest3(delta)}`,
    `- status: ${delta.status}`,
    `- summary: ${yamlScalar(delta.summary)}`,
    `- completed_items: ${yamlStringArray(delta.completed_items)}`,
    `- remaining_risks: ${yamlStringArray(delta.remaining_risks)}`,
    `- next_checkpoint: ${yamlScalar(delta.next_checkpoint)}`,
    `- evidence_refs: ${yamlStringArray(delta.evidence_refs)}`,
    STATUS_RECONCILIATION_END
  ].join(`
`);
}
function readStatusScalar(body, field, location) {
  const escaped = field.replace(/[.*+?^${}()|[\\]\\]/g, "\\$&");
  const match = new RegExp(`^-\\s*${escaped}\\s*:\\s*(.*?)\\s*$`, "m").exec(body);
  if (!match)
    fail3("STATUS_INVALID", `${location} reconciliation receipt is missing ${field}.`);
  const raw = match[1].trim();
  if (raw.startsWith('"') || raw.startsWith("'")) {
    try {
      return JSON.parse(raw);
    } catch {
      fail3("STATUS_INVALID", `${location}.${field} is not a valid scalar.`);
    }
  }
  return expectString2(raw, `${location}.${field}`);
}
function readStatusArray(body, field, location) {
  const raw = readStatusScalar(body, field, location);
  let parsed;
  try {
    parsed = JSON.parse(raw);
  } catch {
    fail3("STATUS_INVALID", `${location}.${field} must be a JSON/YAML inline string array.`);
  }
  return expectStringArray2(parsed, `${location}.${field}`, true, 64);
}
function statusDeltaFromReceipt(receipt) {
  return {
    kind: "project-status",
    action: "sync",
    status: receipt.status,
    summary: receipt.summary,
    completed_items: [...receipt.completedItems],
    remaining_risks: [...receipt.remainingRisks],
    next_checkpoint: receipt.nextCheckpoint,
    evidence_refs: [...receipt.evidenceRefs]
  };
}
function readStatusReceipts(content, location) {
  const pattern = new RegExp(`${STATUS_RECONCILIATION_BEGIN.replace(/[.*+?^${}()|[\\]\\]/g, "\\$&")}\\r?\\n([\\s\\S]*?)\\r?\\n${STATUS_RECONCILIATION_END.replace(/[.*+?^${}()|[\\]\\]/g, "\\$&")}`, "g");
  const receipts = [];
  for (const match of content.matchAll(pattern)) {
    const body = match[1] ?? "";
    const archiveRevision = readStatusScalar(body, "archive_revision", location);
    const sourceRevision = readStatusScalar(body, "source_revision", location);
    const deltaDigest = readStatusScalar(body, "delta_digest", location);
    if (!/^[a-f0-9]{64}$/.test(archiveRevision) || !/^[a-f0-9]{64}$/.test(sourceRevision) || !/^[a-f0-9]{64}$/.test(deltaDigest)) {
      fail3("STATUS_INVALID", `${location} reconciliation receipt has an invalid revision or digest.`);
    }
    const receipt = {
      taskId: readStatusScalar(body, "task_id", location),
      taskSlug: readStatusScalar(body, "task_slug", location),
      documentId: readStatusScalar(body, "document_id", location),
      archivePath: normalizeRepoPath2(readStatusScalar(body, "archive_path", location), `${location}.archive_path`),
      archiveRevision,
      sourceRevision,
      idempotencyKey: readStatusScalar(body, "proposal_idempotency_key", location),
      deltaDigest,
      status: expectEnum(readStatusScalar(body, "status", location), ["completed", "observing"], `${location}.status`),
      summary: readStatusScalar(body, "summary", location),
      completedItems: readStatusArray(body, "completed_items", location),
      remainingRisks: readStatusArray(body, "remaining_risks", location),
      nextCheckpoint: readStatusScalar(body, "next_checkpoint", location),
      evidenceRefs: readStatusArray(body, "evidence_refs", location)
    };
    if (!SAFE_KEY_PATTERN2.test(receipt.idempotencyKey))
      fail3("STATUS_INVALID", `${location}.proposal_idempotency_key is invalid.`);
    if (!DOCUMENT_ID_PATTERN.test(receipt.documentId))
      fail3("STATUS_INVALID", `${location}.document_id is invalid.`);
    try {
      validateTaskId(receipt.taskId);
      validateTaskSlug(receipt.taskSlug);
    } catch (error) {
      fail3("STATUS_INVALID", error instanceof Error ? error.message : String(error));
    }
    if (digest3(statusDeltaFromReceipt(receipt)) !== receipt.deltaDigest) {
      fail3("STATUS_INVALID", `${location} reconciliation receipt delta digest does not match its typed fields.`);
    }
    if (receipts.some((existing) => existing.archivePath === receipt.archivePath)) {
      fail3("STATUS_INVALID", `${location} contains duplicate reconciliation receipts for ${receipt.archivePath}.`);
    }
    receipts.push(receipt);
  }
  return receipts;
}
function matchingStatusReceipt(content, location, archive) {
  const receipts = readStatusReceipts(content, location);
  const matches = receipts.filter((receipt) => receipt.archivePath === archive.relativePath || receipt.taskId === archive.taskId && receipt.taskSlug === archive.taskSlug && receipt.documentId === archive.documentId);
  if (matches.length > 1)
    fail3("STATUS_INVALID", `${location} contains multiple receipts for the same archived task.`);
  return matches[0] ?? null;
}
var STATUS_PLACEHOLDER_VALUES = new Set(["none", "n/a", "无", "暂无"]);
function statusItemText(line) {
  const match = /^-\s+(?:\[[ xX]\]\s*)?(.+?)\s*$/.exec(line);
  if (!match)
    return null;
  const value = match[1].trim();
  if (value.length === 0 || STATUS_PLACEHOLDER_VALUES.has(value.toLowerCase()))
    return null;
  return value;
}
function isStatusPlaceholderLine(line) {
  const match = /^-\s+(?:\[[ xX]\]\s*)?(.+?)\s*$/.exec(line);
  return match !== null && STATUS_PLACEHOLDER_VALUES.has(match[1].trim().toLowerCase());
}
function validateStatusProjectionText(value, location) {
  if (value.includes(`
`) || value.includes("\r")) {
    fail3("STATUS_RECONCILIATION_CONFLICT", `${location} cannot contain a line break.`);
  }
  return value.trim();
}
function requiredStatusSection(content, sectionKey, location) {
  const definition = STATUS_SECTIONS[sectionKey];
  const title = definition.title;
  const aliases = "aliases" in definition ? definition.aliases : [title];
  const section = findUniqueMarkdownSection(scanMarkdownSections2(content), aliases, 2);
  if (!section)
    fail3("STATUS_INVALID", `${location} is missing the required ## ${title} section.`);
  return section;
}
function validateStatusDocument(content, location = "STATUS.md") {
  const sections = scanMarkdownSections2(content);
  for (const key of STATUS_SECTION_KEYS) {
    const definition = STATUS_SECTIONS[key];
    const title = definition.title;
    const aliases = "aliases" in definition ? definition.aliases : [title];
    if (!findUniqueMarkdownSection(sections, aliases, 2))
      fail3("STATUS_INVALID", `${location} is missing the required ## ${title} section.`);
  }
}
function readStatusReconciliationReceipts(content, location = "STATUS.md") {
  validateStatusDocument(content, location);
  const beginCount = countExactOccurrences(content, STATUS_RECONCILIATION_BEGIN);
  const endCount = countExactOccurrences(content, STATUS_RECONCILIATION_END);
  if (beginCount !== endCount)
    fail3("STATUS_INVALID", `${location} contains an incomplete reconciliation marker.`);
  return readStatusReceipts(content, location).map((receipt) => ({
    ...receipt,
    completedItems: [...receipt.completedItems],
    remainingRisks: [...receipt.remainingRisks],
    evidenceRefs: [...receipt.evidenceRefs]
  }));
}
function assertStatusReceiptProjection(content, receipts, location) {
  if (receipts.length === 0)
    return;
  const completedLines = readStatusSectionLines(content, "completed", location);
  const developmentLines = readStatusSectionLines(content, "inProgress", location);
  const riskLines = readStatusSectionLines(content, "risks", location);
  for (const receipt of receipts) {
    for (const rawItem of receipt.completedItems) {
      const item = validateStatusProjectionText(rawItem, `${location}.completed_items`);
      if (statusItemMatchCount(completedLines, item) !== 1 || statusItemMatchCount(developmentLines, item) !== 0) {
        fail3("STATUS_PROVENANCE_MISMATCH", `${location} completed item projection no longer matches "${item}".`);
      }
    }
    for (const rawItem of receipt.remainingRisks) {
      const item = validateStatusProjectionText(rawItem, `${location}.remaining_risks`);
      if (statusItemMatchCount(riskLines, item) !== 1) {
        fail3("STATUS_PROVENANCE_MISMATCH", `${location} remaining risk projection no longer matches "${item}".`);
      }
    }
  }
  assertStatusProjection(content, statusDeltaFromReceipt(receipts[receipts.length - 1]), location);
}
function readCanonicalStatusDocumentForBootstrap(content, location = "STATUS.md") {
  const receipts = readStatusReconciliationReceipts(content, location);
  assertStatusReceiptProjection(content, receipts, location);
  return receipts;
}
function canonicalizeStatusOverviewLines(lines, project, mode, location) {
  const fields = [
    { pattern: /^-\s*(?:项目|project)\s*[:：]\s*.*$/i, line: `- 项目：${project.name}` },
    { pattern: /^-\s*(?:slug|project[_ ]slug)\s*[:：]\s*.*$/i, line: `- slug：${project.slug}` },
    { pattern: /^-\s*(?:bootstrap[_ ]mode|bootstrap mode)\s*[:：]\s*.*$/i, line: `- bootstrap mode：${mode}` }
  ];
  const next = [...lines];
  for (const field of fields) {
    const matches = next.map((line, index) => ({ line, index })).filter((item) => field.pattern.test(item.line));
    if (matches.length > 1)
      fail3("STATUS_RECONCILIATION_CONFLICT", `${location} contains multiple Bootstrap overview fields.`);
    if (matches.length === 1)
      next[matches[0].index] = field.line;
    else
      next.push(field.line);
  }
  return next;
}
function canonicalizeStatusDocumentForBootstrap(content, project, mode, location = "STATUS.md") {
  readCanonicalStatusDocumentForBootstrap(content, location);
  const sections = scanMarkdownSections2(content);
  const canonicalSections = new Set;
  const output = [];
  const firstTopLevel = sections.filter((section) => section.level === 2).sort((left, right) => left.headingStart - right.headingStart)[0];
  const prefix = firstTopLevel ? content.slice(0, firstTopLevel.headingStart).replace(/\r\n?/g, `
`).trimEnd() : "";
  output.push(prefix || "# STATUS.md", "");
  for (const key of STATUS_SECTION_KEYS) {
    const definition = STATUS_SECTIONS[key];
    const aliases = "aliases" in definition ? definition.aliases : [definition.title];
    const section = findUniqueMarkdownSection(sections, aliases, 2);
    if (!section)
      fail3("STATUS_INVALID", `${location} is missing the required ## ${definition.title} section.`);
    canonicalSections.add(section.headingStart);
    const body = content.slice(section.contentStart, section.contentEnd).replace(/\r\n?/g, `
`).trim();
    const bodyLines = body.length > 0 ? body.split(`
`) : [];
    output.push(`## ${definition.title}`, "", ...key === "overview" ? canonicalizeStatusOverviewLines(bodyLines, project, mode, location) : bodyLines, "");
  }
  for (const section of sections.filter((item) => item.level === 2).sort((left, right) => left.headingStart - right.headingStart)) {
    if (canonicalSections.has(section.headingStart))
      continue;
    const preserved = content.slice(section.headingStart, section.contentEnd).replace(/\r\n?/g, `
`).trimEnd();
    if (preserved.trim().length > 0)
      output.push(preserved, "");
  }
  const next = output.join(`
`);
  readCanonicalStatusDocumentForBootstrap(next, location);
  return next;
}
function readStatusSectionLines(content, sectionKey, location) {
  const section = requiredStatusSection(content, sectionKey, location);
  const body = content.slice(section.contentStart, section.contentEnd).replace(/\r\n?/g, `
`).trim();
  return body.length > 0 ? body.split(`
`) : [];
}
function replaceStatusSectionBody(content, sectionKey, lines, location) {
  const section = requiredStatusSection(content, sectionKey, location);
  const body = lines.join(`
`).trim();
  return content.slice(0, section.contentStart) + `${body.length > 0 ? `
${body}

` : `
`}` + content.slice(section.contentEnd);
}
function statusItemMatchCount(lines, item) {
  return lines.filter((line) => statusItemText(line) === item).length;
}
function projectStatusOverview(content, delta, location) {
  const lines = readStatusSectionLines(content, "overview", location);
  const statusFieldPattern = /^-\s*(?:当前状态|status)\s*[:：]\s*.*$/i;
  const matches = lines.map((line, index) => ({ line, index })).filter((item) => statusFieldPattern.test(item.line));
  if (matches.length > 1)
    fail3("STATUS_RECONCILIATION_CONFLICT", `${location} contains multiple project status fields.`);
  const statusLine = `- 当前状态：${delta.status}`;
  if (matches.length === 1) {
    const next = [...lines];
    next[matches[0].index] = statusLine;
    return replaceStatusSectionBody(content, "overview", next, location);
  }
  return replaceStatusSectionBody(content, "overview", [...lines, statusLine], location);
}
function projectStatusCompletedItems(content, delta, location) {
  const completedLines = readStatusSectionLines(content, "completed", location);
  const developmentLines = readStatusSectionLines(content, "inProgress", location);
  const removeDevelopmentIndexes = new Set;
  const appendCompleted = [];
  for (const rawItem of delta.completed_items) {
    const item = validateStatusProjectionText(rawItem, `${location}.completed_items`);
    const completedMatches = statusItemMatchCount(completedLines, item);
    if (completedMatches > 1)
      fail3("STATUS_RECONCILIATION_CONFLICT", `${location} contains duplicate completed item "${item}".`);
    const developmentMatches = developmentLines.map((line, index) => ({ line, index })).filter((entry) => statusItemText(entry.line) === item);
    if (developmentMatches.length > 1) {
      fail3("STATUS_RECONCILIATION_CONFLICT", `${location} cannot determine which in-progress record to remove for "${item}".`);
    }
    if (developmentMatches.length === 1)
      removeDevelopmentIndexes.add(developmentMatches[0].index);
    if (completedMatches === 0)
      appendCompleted.push(item);
  }
  const nextDevelopmentLines = developmentLines.filter((_, index) => !removeDevelopmentIndexes.has(index));
  let nextCompletedLines = [...completedLines];
  if (appendCompleted.length > 0) {
    nextCompletedLines = nextCompletedLines.filter((line) => !isStatusPlaceholderLine(line));
    while (nextCompletedLines.length > 0 && nextCompletedLines[nextCompletedLines.length - 1].trim() === "")
      nextCompletedLines.pop();
    nextCompletedLines.push(...appendCompleted.map((item) => `- ${item}`));
  }
  let next = replaceStatusSectionBody(content, "inProgress", nextDevelopmentLines, location);
  return replaceStatusSectionBody(next, "completed", nextCompletedLines, location);
}
function projectStatusRemainingRisks(content, delta, location) {
  const riskItems = delta.remaining_risks.map((item) => validateStatusProjectionText(item, `${location}.remaining_risks`));
  if (riskItems.length === 0)
    return content;
  const lines = readStatusSectionLines(content, "risks", location);
  const appendItems = riskItems.filter((item) => {
    const matches = statusItemMatchCount(lines, item);
    if (matches > 1)
      fail3("STATUS_RECONCILIATION_CONFLICT", `${location} contains duplicate remaining risk "${item}".`);
    return matches === 0;
  });
  if (appendItems.length === 0)
    return content;
  const nextLines = lines.filter((line) => !isStatusPlaceholderLine(line));
  while (nextLines.length > 0 && nextLines[nextLines.length - 1].trim() === "")
    nextLines.pop();
  nextLines.push(...appendItems.map((item) => `- ${item}`));
  return replaceStatusSectionBody(content, "risks", nextLines, location);
}
function projectStatusCheckpoint(content, delta, location) {
  const checkpoint = validateStatusProjectionText(delta.next_checkpoint, `${location}.next_checkpoint`);
  const lines = readStatusSectionLines(content, "nextCheckpoint", location);
  const matchingIndexSet = new Set(lines.map((line, index) => ({ line, index })).filter((entry) => statusItemText(entry.line) === checkpoint).map((entry) => entry.index));
  if (matchingIndexSet.size === 1)
    return content;
  const nextLines = lines.filter((_, index) => !matchingIndexSet.has(index));
  nextLines.push(`- ${checkpoint}`);
  return replaceStatusSectionBody(content, "nextCheckpoint", nextLines, location);
}
function projectStatusDelta(content, delta, location) {
  let next = projectStatusOverview(content, delta, location);
  next = projectStatusCompletedItems(next, delta, location);
  next = projectStatusRemainingRisks(next, delta, location);
  return projectStatusCheckpoint(next, delta, location);
}
function assertStatusProjection(content, delta, location) {
  const overviewLines = readStatusSectionLines(content, "overview", location);
  const statusLines = overviewLines.filter((line) => /^-\s*(?:当前状态|status)\s*[:：]\s*.*$/i.test(line));
  if (statusLines.length !== 1 || statusLines[0] !== `- 当前状态：${delta.status}`) {
    fail3("STATUS_PROVENANCE_MISMATCH", `${location} project status projection no longer matches the typed status delta.`);
  }
  const completedLines = readStatusSectionLines(content, "completed", location);
  const developmentLines = readStatusSectionLines(content, "inProgress", location);
  for (const rawItem of delta.completed_items) {
    const item = validateStatusProjectionText(rawItem, `${location}.completed_items`);
    if (statusItemMatchCount(completedLines, item) !== 1 || statusItemMatchCount(developmentLines, item) !== 0) {
      fail3("STATUS_PROVENANCE_MISMATCH", `${location} completed item projection no longer matches "${item}".`);
    }
  }
  const riskLines = readStatusSectionLines(content, "risks", location);
  for (const rawItem of delta.remaining_risks) {
    const item = validateStatusProjectionText(rawItem, `${location}.remaining_risks`);
    if (statusItemMatchCount(riskLines, item) !== 1) {
      fail3("STATUS_PROVENANCE_MISMATCH", `${location} remaining risk projection no longer matches "${item}".`);
    }
  }
  const checkpointLines = readStatusSectionLines(content, "nextCheckpoint", location);
  if (statusItemMatchCount(checkpointLines, delta.next_checkpoint) !== 1) {
    fail3("STATUS_PROVENANCE_MISMATCH", `${location} next checkpoint projection no longer matches the typed status delta.`);
  }
}
function appendStatusReconciliation(content, marker, location) {
  const section = findUniqueMarkdownSection(scanMarkdownSections2(content), STATUS_SECTIONS.recentUpdates.aliases, 2);
  if (!section)
    fail3("STATUS_INVALID", `${location} is missing the required ## ${STATUS_SECTIONS.recentUpdates.title} section.`);
  const existing = content.slice(section.contentStart, section.contentEnd).replace(/\r\n?/g, `
`).trimEnd();
  return content.slice(0, section.contentStart) + `
${existing.trim().length > 0 ? `${existing}

` : ""}${marker}
` + content.slice(section.contentEnd);
}
function prepareProjectStatusTransaction(root2, current, proposal) {
  ensureAuthorityKinds(proposal, ["evidence-admission"]);
  const { receipt } = matchingArchiveReceipt(root2, current);
  const target = workflowDocPathForRoot(root2, "STATUS.md");
  if (!fs11.existsSync(target.filePath))
    fail3("RUNTIME_SOURCE_MISSING", `STATUS.md is missing: ${target.relativePath}`);
  const originalStatusContent = fs11.readFileSync(target.filePath, "utf8");
  validateStatusDocument(originalStatusContent, target.relativePath);
  const existingReceipt = matchingStatusReceipt(originalStatusContent, target.relativePath, receipt);
  const deltaDigest = digest3(proposal.semantic_delta);
  if (existingReceipt) {
    if (existingReceipt.taskId !== receipt.taskId || existingReceipt.taskSlug !== receipt.taskSlug || existingReceipt.documentId !== receipt.documentId || existingReceipt.archivePath !== receipt.relativePath || existingReceipt.archiveRevision !== receipt.revision || existingReceipt.sourceRevision !== receipt.sourceRevision) {
      fail3("STATUS_PROVENANCE_MISMATCH", "STATUS reconciliation receipt does not match the canonical archive.");
    }
    if (existingReceipt.deltaDigest !== deltaDigest || existingReceipt.status !== proposal.semantic_delta.status) {
      fail3("STATUS_RECONCILIATION_CONFLICT", "STATUS already contains a different reconciliation for this archived task.");
    }
    if (digest3(statusDeltaFromReceipt(existingReceipt)) !== deltaDigest) {
      fail3("STATUS_RECONCILIATION_CONFLICT", "STATUS reconciliation receipt no longer matches its typed status delta.");
    }
    try {
      assertStatusProjection(originalStatusContent, proposal.semantic_delta, target.relativePath);
      return null;
    } catch (error) {
      if (!(error instanceof VNextRuntimeError) || error.code !== "STATUS_PROVENANCE_MISMATCH")
        throw error;
    }
    const nextStatusContent = projectStatusDelta(originalStatusContent, proposal.semantic_delta, target.relativePath);
    assertStatusProjection(nextStatusContent, proposal.semantic_delta, target.relativePath);
    return {
      statusFilePath: target.filePath,
      statusRelativePath: target.relativePath,
      nextStatusContent,
      originalStatusContent,
      statusRevision: sha2565(nextStatusContent),
      archive: receipt
    };
  }
  const marker = renderStatusReconciliation(proposal, proposal.semantic_delta, receipt);
  const projectedStatusContent = projectStatusDelta(originalStatusContent, proposal.semantic_delta, target.relativePath);
  const nextStatusContent = appendStatusReconciliation(projectedStatusContent, marker, target.relativePath);
  return {
    statusFilePath: target.filePath,
    statusRelativePath: target.relativePath,
    nextStatusContent,
    originalStatusContent,
    statusRevision: sha2565(nextStatusContent),
    archive: receipt
  };
}
function lessonCandidateDigest(candidate) {
  return digest3({
    category: candidate.category,
    scene: candidate.scene,
    conclusion: candidate.conclusion,
    trigger: candidate.trigger,
    cause: candidate.cause,
    action: candidate.action,
    consumer: candidate.consumer
  });
}
function renderLessonMarker(candidate, archive) {
  return `<!-- vNext lesson record: ${JSON.stringify({
    task_id: archive.taskId,
    task_slug: archive.taskSlug,
    document_id: archive.documentId,
    archive_path: archive.relativePath,
    archive_revision: archive.revision,
    source_revision: archive.sourceRevision,
    candidate_ref: candidate.candidate_ref,
    candidate_digest: lessonCandidateDigest(candidate),
    evidence_refs: candidate.evidence_refs
  })} -->`;
}
function expectLessonRecord(value, location) {
  if (!isRecord4(value))
    fail3("LESSON_INVALID", `${location} is not a canonical Lesson marker mapping.`);
  return value;
}
function expectLessonExactKeys(value, expected, location) {
  const expectedSet = new Set(expected);
  const missing = expected.filter((key) => !(key in value));
  const extra = Object.keys(value).filter((key) => !expectedSet.has(key));
  if (missing.length === 0 && extra.length === 0)
    return;
  const details = [];
  if (missing.length > 0)
    details.push(`missing=[${missing.join(", ")}]`);
  if (extra.length > 0)
    details.push(`unsupported Lesson marker field(s)=[${extra.join(", ")}]`);
  fail3("LESSON_INVALID", `${location} is a non-canonical Lesson marker (${details.join("; ")}).`);
}
function expectLessonString(value, location, pattern) {
  if (typeof value !== "string" || value.trim().length === 0) {
    fail3("LESSON_INVALID", `${location} must be a non-empty string.`);
  }
  const normalized = value.trim();
  if (pattern && !pattern.test(normalized)) {
    fail3("LESSON_INVALID", `${location} has an invalid value.`);
  }
  return normalized;
}
function validateLessonTaskSlug(value, location) {
  const taskSlug = expectLessonString(value, location);
  try {
    validateTaskSlug(taskSlug);
  } catch (error) {
    fail3("LESSON_INVALID", error instanceof Error ? error.message : String(error));
  }
  return taskSlug;
}
function validateLessonCandidateKey(value, location) {
  if (!isRecord4(value))
    fail3("LESSON_INVALID", `${location} must be a mapping.`);
  const taskId = expectLessonString(value.task_id, `${location}.task_id`);
  try {
    validateTaskId(taskId);
  } catch (error) {
    fail3("LESSON_INVALID", error instanceof Error ? error.message : String(error));
  }
  const documentId = expectLessonString(value.document_id, `${location}.document_id`);
  if (!DOCUMENT_ID_PATTERN.test(documentId)) {
    fail3("LESSON_INVALID", `${location}.document_id is invalid.`);
  }
  const archiveRevision = expectLessonString(value.archive_revision, `${location}.archive_revision`);
  if (!SHA256_PATTERN2.test(archiveRevision)) {
    fail3("LESSON_INVALID", `${location}.archive_revision must be an exact SHA-256.`);
  }
  const candidateRef = expectLessonString(value.candidate_ref, `${location}.candidate_ref`, SAFE_KEY_PATTERN2);
  return {
    task_id: taskId,
    document_id: documentId,
    archive_revision: archiveRevision,
    candidate_ref: candidateRef
  };
}
function readLessonMarkers(content, location) {
  const pattern = /<!-- vNext lesson record: (\{[^\r\n]+\}) -->/g;
  const result = [];
  for (const match of content.matchAll(pattern)) {
    let parsed;
    try {
      parsed = JSON.parse(match[1]);
    } catch {
      fail3("LESSON_INVALID", `${location} contains an invalid vNext lesson provenance marker.`);
    }
    const record = expectLessonRecord(parsed, `${location}.lesson_marker`);
    const hasDisposition = "disposition" in record;
    if (hasDisposition) {
      if (record.disposition !== "reused") {
        fail3("LESSON_INVALID", `${location}.lesson_marker has an invalid Lesson marker disposition: ${String(record.disposition)}.`);
      }
      expectLessonExactKeys(record, ["task_id", "task_slug", "document_id", "archive_path", "archive_revision", "source_revision", "candidate_ref", "candidate_digest", "evidence_refs", "disposition", "reused_candidate"], `${location}.lesson_marker`);
    } else {
      expectLessonExactKeys(record, ["task_id", "task_slug", "document_id", "archive_path", "archive_revision", "source_revision", "candidate_ref", "candidate_digest", "evidence_refs"], `${location}.lesson_marker`);
    }
    const archiveRevision = expectLessonString(record.archive_revision, `${location}.lesson_marker.archive_revision`);
    const sourceRevision = expectLessonString(record.source_revision, `${location}.lesson_marker.source_revision`);
    const candidateDigest = expectLessonString(record.candidate_digest, `${location}.lesson_marker.candidate_digest`);
    if (!SHA256_PATTERN2.test(archiveRevision) || !SHA256_PATTERN2.test(sourceRevision) || !SHA256_PATTERN2.test(candidateDigest)) {
      fail3("LESSON_INVALID", `${location} contains a non-canonical Lesson marker revision or digest.`);
    }
    const candidateIdentity = validateLessonCandidateKey(record, `${location}.lesson_marker`);
    const taskSlug = validateLessonTaskSlug(record.task_slug, `${location}.lesson_marker.task_slug`);
    const marker = {
      task_id: candidateIdentity.task_id,
      task_slug: taskSlug,
      document_id: candidateIdentity.document_id,
      archive_path: normalizeRepoPath2(expectLessonString(record.archive_path, `${location}.lesson_marker.archive_path`), `${location}.lesson_marker.archive_path`),
      archive_revision: candidateIdentity.archive_revision,
      source_revision: sourceRevision,
      candidate_ref: candidateIdentity.candidate_ref,
      candidate_digest: candidateDigest,
      evidence_refs: validateEvidenceRefs(record.evidence_refs, `${location}.lesson_marker.evidence_refs`)
    };
    if (hasDisposition) {
      if (!isRecord4(record.reused_candidate)) {
        fail3("LESSON_INVALID", `${location}.lesson_marker.reused_candidate must be a mapping.`);
      }
      const reusedRecord = record.reused_candidate;
      expectLessonExactKeys(reusedRecord, ["task_id", "document_id", "archive_revision", "candidate_ref"], `${location}.lesson_marker.reused_candidate`);
      marker.disposition = "reused";
      marker.reused_candidate = validateLessonCandidateKey(reusedRecord, `${location}.lesson_marker.reused_candidate`);
    }
    result.push(marker);
  }
  return result;
}
function renderLessonCandidate(candidate, archive) {
  return [
    renderLessonMarker(candidate, archive),
    `- 场景：${yamlScalar(candidate.scene)}`,
    `  - 结论：${yamlScalar(candidate.conclusion)}`,
    `  - 触发信号：${yamlScalar(candidate.trigger)}`,
    `  - 原因：${yamlScalar(candidate.cause)}`,
    `  - 应对动作：${yamlScalar(candidate.action)}`,
    `  - 消费者：${yamlScalar(candidate.consumer)}`,
    `  - 证据引用：${yamlStringArray(candidate.evidence_refs)}`
  ].join(`
`);
}
function countExactOccurrences(content, value) {
  if (value.length === 0)
    return 0;
  let count = 0;
  let offset = 0;
  while (true) {
    const index = content.indexOf(value, offset);
    if (index < 0)
      return count;
    count += 1;
    offset = index + value.length;
  }
}
function renderLessonMarkerFromData(marker) {
  const data = {
    task_id: marker.task_id,
    task_slug: marker.task_slug,
    document_id: marker.document_id,
    archive_path: marker.archive_path,
    archive_revision: marker.archive_revision,
    source_revision: marker.source_revision,
    candidate_ref: marker.candidate_ref,
    candidate_digest: marker.candidate_digest,
    evidence_refs: marker.evidence_refs
  };
  if (marker.disposition === "reused" && marker.reused_candidate) {
    data.disposition = "reused";
    data.reused_candidate = {
      task_id: marker.reused_candidate.task_id,
      document_id: marker.reused_candidate.document_id,
      archive_revision: marker.reused_candidate.archive_revision,
      candidate_ref: marker.reused_candidate.candidate_ref
    };
  }
  return `<!-- vNext lesson record: ${JSON.stringify(data)} -->`;
}
function archiveReceiptFromLessonMarker(marker) {
  return {
    filePath: "",
    relativePath: marker.archive_path,
    raw: "",
    revision: marker.archive_revision,
    taskId: marker.task_id,
    taskSlug: marker.task_slug,
    taskTitle: "",
    documentId: marker.document_id,
    sourceRevision: marker.source_revision,
    archivePath: marker.archive_path,
    idempotencyKey: "lesson-marker-replay",
    closureDeltaDigest: "0".repeat(64),
    lessonAdmission: { decision: "defer", candidate_refs: [], evidence_refs: [] },
    knowledgeAdmissions: emptyKnowledgeAdmissionBundle()
  };
}
function parseLessonRenderedScalar(raw, location) {
  const value = raw.trim();
  if (value.startsWith('"')) {
    try {
      return expectText(JSON.parse(value), location);
    } catch (error) {
      if (error instanceof VNextRuntimeError)
        throw error;
      fail3("LESSON_INVALID", `${location} is not a valid rendered scalar.`);
    }
  }
  return expectText(value, location);
}
function readLessonRenderedField(block, label, indent, location) {
  const escaped = label.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  const match = new RegExp(`^${indent}-\\s*${escaped}：(.+?)\\s*$`, "m").exec(block);
  if (!match)
    fail3("LESSON_INVALID", `${location} is missing the visible ${label} field.`);
  return parseLessonRenderedScalar(match[1], `${location}.${label}`);
}
function readLessonRenderedEvidenceRefs(block, location) {
  const match = /^\s{2}-\s*证据引用：(.+?)\s*$/m.exec(block);
  if (!match)
    fail3("LESSON_INVALID", `${location} is missing the visible 证据引用 field.`);
  let parsed;
  try {
    parsed = JSON.parse(match[1].trim());
  } catch {
    fail3("LESSON_INVALID", `${location}.evidence_refs is not a JSON array.`);
  }
  return validateEvidenceRefs(parsed, `${location}.evidence_refs`);
}
function readDurableLessonRecord(content, marker, location) {
  const markerText = renderLessonMarkerFromData(marker);
  if (countExactOccurrences(content, markerText) !== 1) {
    fail3("LESSON_INVALID", `${location} contains a non-canonical or duplicate lesson provenance marker.`);
  }
  const markerStart = content.indexOf(markerText);
  const sections = scanMarkdownSections2(content);
  const categorySection = sections.find((section) => section.level === 2 && LESSON_CATEGORIES.includes(section.title) && markerStart >= section.contentStart && markerStart < section.contentEnd);
  if (!categorySection)
    fail3("LESSON_INVALID", `${location} lesson marker is not inside a canonical lesson category section.`);
  const nextMarker = content.indexOf("<!-- vNext lesson record:", markerStart + markerText.length);
  const nextSection = sections.filter((section) => section.level <= 2 && section.headingStart > markerStart).map((section) => section.headingStart).sort((left, right) => left - right)[0];
  const candidateEnd = Math.min(nextMarker < 0 ? content.length : nextMarker, nextSection === undefined ? content.length : nextSection);
  const block = content.slice(markerStart, candidateEnd).replace(/\r\n?/g, `
`);
  const candidate = {
    candidate_ref: marker.candidate_ref,
    category: categorySection.title,
    scene: readLessonRenderedField(block, "场景", "", `${location}.${marker.candidate_ref}`),
    conclusion: readLessonRenderedField(block, "结论", "  ", `${location}.${marker.candidate_ref}`),
    trigger: readLessonRenderedField(block, "触发信号", "  ", `${location}.${marker.candidate_ref}`),
    cause: readLessonRenderedField(block, "原因", "  ", `${location}.${marker.candidate_ref}`),
    action: readLessonRenderedField(block, "应对动作", "  ", `${location}.${marker.candidate_ref}`),
    consumer: readLessonRenderedField(block, "消费者", "  ", `${location}.${marker.candidate_ref}`),
    evidence_refs: readLessonRenderedEvidenceRefs(block, `${location}.${marker.candidate_ref}`)
  };
  if (marker.evidence_refs.join("|") !== candidate.evidence_refs.join("|")) {
    fail3("LESSON_PROVENANCE_MISMATCH", `${location}.${marker.candidate_ref} marker evidence_refs do not match the visible Lesson record.`);
  }
  if (lessonCandidateDigest(candidate) !== marker.candidate_digest) {
    fail3("LESSON_PROVENANCE_MISMATCH", `${location}.${marker.candidate_ref} marker digest does not match the visible Lesson record.`);
  }
  const archive = archiveReceiptFromLessonMarker(marker);
  if (countExactOccurrences(content, renderLessonCandidate(candidate, archive)) !== 1) {
    fail3("LESSON_PROVENANCE_MISMATCH", `${location}.${marker.candidate_ref} visible Lesson record drifted from its deterministic rendering.`);
  }
  return { marker, candidate };
}
function readDurableLessonRecords(content, location) {
  const markers = readLessonMarkers(content, location);
  const persistedRecords = [];
  const reusedMarkers = [];
  for (const [index, marker] of markers.entries()) {
    if (marker.disposition === "reused") {
      reusedMarkers.push({ marker, index });
    } else {
      persistedRecords.push(readDurableLessonRecord(content, marker, `${location}.lesson[${index}]`));
    }
  }
  const allRecords = [...persistedRecords];
  for (const { marker, index } of reusedMarkers) {
    const markerText = renderLessonMarkerFromData(marker);
    if (countExactOccurrences(content, markerText) !== 1) {
      fail3("LESSON_INVALID", `${location}.lesson[${index}] contains a non-canonical or duplicate lesson reuse marker.`);
    }
    if (!marker.reused_candidate) {
      fail3("LESSON_INVALID", `${location}.lesson[${index}] reuse marker is missing reused_candidate target.`);
    }
    const matchingTargets = persistedRecords.filter((record) => record.marker.task_id === marker.reused_candidate.task_id && record.marker.document_id === marker.reused_candidate.document_id && record.marker.archive_revision === marker.reused_candidate.archive_revision && record.marker.candidate_ref === marker.reused_candidate.candidate_ref);
    if (matchingTargets.length !== 1) {
      fail3("LESSON_PROVENANCE_MISMATCH", `${location}.lesson[${index}] references ${matchingTargets.length === 0 ? "missing" : "ambiguous"} persisted candidate target ${marker.reused_candidate.task_id}/${marker.reused_candidate.candidate_ref}.`);
    }
    const target = matchingTargets[0];
    if (target.marker.candidate_digest !== marker.candidate_digest) {
      fail3("LESSON_PROVENANCE_MISMATCH", `${location}.lesson[${index}] digest does not match referenced candidate target ${marker.reused_candidate.task_id}/${marker.reused_candidate.candidate_ref}.`);
    }
    allRecords.push({
      marker,
      candidate: {
        ...target.candidate,
        candidate_ref: marker.candidate_ref,
        evidence_refs: [...marker.evidence_refs]
      }
    });
  }
  return allRecords;
}
function readCanonicalLessonsDocument(content, location) {
  const sections = scanMarkdownSections2(content);
  for (const heading of LESSON_REQUIRED_SECTION_HEADINGS) {
    if (!findUniqueMarkdownSection(sections, [heading], 2))
      fail3("LESSON_INVALID", `${location} is missing the required ## ${heading} section.`);
  }
  readDurableLessonRecords(content, location);
  return content;
}
function isLegacyBootstrapLessonsDocument(content, location) {
  const sections = scanMarkdownSections2(content);
  const topLevelTitles = sections.filter((section) => section.level === 2).map((section) => section.title);
  const isLegacySingleSection = topLevelTitles.length === 1 && topLevelTitles[0] === "Reusable lessons";
  const isLegacyPairedSections = topLevelTitles.length === 2 && topLevelTitles.includes("使用规则") && topLevelTitles.includes("Reusable lessons");
  if (!isLegacySingleSection && !isLegacyPairedSections)
    return false;
  readDurableLessonRecords(content, location);
  return true;
}
function readBootstrapLessonsDocument(content, location = "LESSONS.md") {
  if (isLegacyBootstrapLessonsDocument(content, location))
    return content;
  return readCanonicalLessonsDocument(content, location);
}
function canonicalizeLessonsDocumentForBootstrap(content, location = "LESSONS.md") {
  const sections = scanMarkdownSections2(content);
  const legacy = isLegacyBootstrapLessonsDocument(content, location);
  if (!legacy)
    readCanonicalLessonsDocument(content, location);
  const canonicalSections = new Set;
  const firstTopLevel = sections.filter((section) => section.level === 2).sort((left, right) => left.headingStart - right.headingStart)[0];
  const prefix = firstTopLevel ? content.slice(0, firstTopLevel.headingStart).replace(/\r\n?/g, `
`).trimEnd() : "";
  const output = [prefix || "# LESSONS.md", ""];
  if (legacy) {
    const rules = findUniqueMarkdownSection(sections, ["使用规则"], 2);
    const reusable = findUniqueMarkdownSection(sections, ["Reusable lessons"], 2);
    if (!reusable)
      fail3("LESSON_INVALID", `${location} is missing the legacy Bootstrap Lessons section.`);
    if (rules)
      canonicalSections.add(rules.headingStart);
    canonicalSections.add(reusable.headingStart);
    const rulesBody = rules ? content.slice(rules.contentStart, rules.contentEnd).replace(/\r\n?/g, `
`).trim() : "";
    output.push("## 使用规则", "", ...rulesBody.length > 0 ? rulesBody.split(`
`) : [], "");
    for (const heading of LESSON_CATEGORIES)
      output.push(`## ${heading}`, "", "- none", "");
    const reusableBody = content.slice(reusable.contentStart, reusable.contentEnd).replace(/\r\n?/g, `
`).trim();
    output.push("## Reusable lessons", "", ...reusableBody.length > 0 ? reusableBody.split(`
`) : [], "");
  } else {
    for (const heading of LESSON_REQUIRED_SECTION_HEADINGS) {
      const section = findUniqueMarkdownSection(sections, [heading], 2);
      if (!section)
        fail3("LESSON_INVALID", `${location} is missing the required ## ${heading} section.`);
      canonicalSections.add(section.headingStart);
      const body = content.slice(section.contentStart, section.contentEnd).replace(/\r\n?/g, `
`).trim();
      output.push(`## ${heading}`, "", ...body.length > 0 ? body.split(`
`) : [], "");
    }
  }
  for (const section of sections.filter((item) => item.level === 2).sort((left, right) => left.headingStart - right.headingStart)) {
    if (canonicalSections.has(section.headingStart))
      continue;
    const preserved = content.slice(section.headingStart, section.contentEnd).replace(/\r\n?/g, `
`).trimEnd();
    if (preserved.trim().length > 0)
      output.push(preserved, "");
  }
  const next = output.join(`
`);
  readCanonicalLessonsDocument(next, location);
  return next;
}
function appendLessonCandidates(content, candidates, archive, location) {
  const additions = new Map;
  for (const candidate of candidates) {
    const list = additions.get(candidate.category) ?? [];
    list.push(candidate);
    additions.set(candidate.category, list);
  }
  let nextContent = content;
  let candidateCount = 0;
  const ordered = [...additions.entries()].sort((left, right) => left[0].localeCompare(right[0]));
  for (const [category, categoryCandidates] of ordered) {
    const sections = scanMarkdownSections2(nextContent);
    const section = findUniqueMarkdownSection(sections, [category], 2);
    if (!section)
      fail3("LESSON_INVALID", `${location} is missing the required ## ${category} section.`);
    const rendered = categoryCandidates.map((candidate) => renderLessonCandidate(candidate, archive)).join(`

`);
    const existing = nextContent.slice(section.contentStart, section.contentEnd).replace(/\r\n?/g, `
`).trimEnd();
    nextContent = nextContent.slice(0, section.contentStart) + `
${existing.trim().length > 0 ? `${existing}

` : ""}${rendered}
` + nextContent.slice(section.contentEnd);
    candidateCount += categoryCandidates.length;
  }
  return { content: nextContent, candidateCount };
}
function appendLessonReuseMarkers(content, reuseMarkers, availableRecords, location) {
  let nextContent = content;
  for (const reuseMarker of reuseMarkers) {
    if (!reuseMarker.reused_candidate) {
      fail3("LESSON_INVALID", `${location} reuse marker missing reused_candidate coordinates.`);
    }
    const matchingTargets = availableRecords.filter((record) => record.marker.task_id === reuseMarker.reused_candidate.task_id && record.marker.document_id === reuseMarker.reused_candidate.document_id && record.marker.archive_revision === reuseMarker.reused_candidate.archive_revision && record.marker.candidate_ref === reuseMarker.reused_candidate.candidate_ref);
    if (matchingTargets.length !== 1) {
      fail3("LESSON_INVALID", `${location} target candidate for reuse ${reuseMarker.reused_candidate.task_id}/${reuseMarker.reused_candidate.candidate_ref} was not uniquely resolved (matches=${matchingTargets.length}).`);
    }
    const targetRecord = matchingTargets[0];
    if (targetRecord.marker.candidate_digest !== reuseMarker.candidate_digest) {
      fail3("LESSON_INVALID", `${location} target candidate for reuse ${reuseMarker.reused_candidate.task_id}/${reuseMarker.reused_candidate.candidate_ref} digest mismatched.`);
    }
    const targetArchive = archiveReceiptFromLessonMarker(targetRecord.marker);
    const targetRendered = renderLessonCandidate(targetRecord.candidate, targetArchive);
    const targetIndex = nextContent.indexOf(targetRendered);
    if (targetIndex < 0) {
      fail3("LESSON_INVALID", `${location} could not locate rendered block for candidate ${reuseMarker.reused_candidate.task_id}/${reuseMarker.reused_candidate.candidate_ref}.`);
    }
    let insertionIndex = targetIndex + targetRendered.length;
    while (true) {
      const rest = nextContent.slice(insertionIndex);
      const match = /^\r?\n<!-- vNext lesson record: (\{[^\r\n]+\}) -->/.exec(rest);
      if (!match)
        break;
      insertionIndex += match[0].length;
    }
    const markerText = renderLessonMarkerFromData(reuseMarker);
    nextContent = nextContent.slice(0, insertionIndex) + `
${markerText}` + nextContent.slice(insertionIndex);
  }
  return nextContent;
}
function prepareLessonRecordTransaction(root2, current, proposal) {
  ensureAuthorityKinds(proposal, ["evidence-admission"]);
  const { receipt } = matchingArchiveReceipt(root2, current);
  if (receipt.lessonAdmission.decision !== "admit") {
    fail3("KNOWLEDGE_ADMISSION_INVALID", "lesson-record-transaction is allowed only when the durable archive lesson admission is admit.");
  }
  const delta = proposal.semantic_delta;
  const admissionRefs = new Set(receipt.lessonAdmission.candidate_refs);
  const candidateRefs = new Set(delta.candidates.map((candidate) => candidate.candidate_ref));
  if (admissionRefs.size !== candidateRefs.size || [...admissionRefs].some((ref) => !candidateRefs.has(ref))) {
    fail3("KNOWLEDGE_ADMISSION_INVALID", "lesson-record candidates must exactly match the durable archive lesson admission candidate_refs.");
  }
  if (delta.candidates.length !== candidateRefs.size) {
    fail3("KNOWLEDGE_ADMISSION_INVALID", "lesson-record candidates must not contain duplicate candidate_refs.");
  }
  if (!receipt.lessonAdmission.evidence_refs.every((ref) => delta.evidence_refs.includes(ref))) {
    fail3("KNOWLEDGE_ADMISSION_INVALID", "lesson-record evidence_refs must cover the durable archive lesson admission evidence_refs.");
  }
  const target = workflowDocPathForRoot(root2, "LESSONS.md");
  if (!fs11.existsSync(target.filePath))
    fail3("RUNTIME_SOURCE_MISSING", `LESSONS.md is missing: ${target.relativePath}`);
  const originalLessonsContent = fs11.readFileSync(target.filePath, "utf8");
  const sections = scanMarkdownSections2(originalLessonsContent);
  for (const heading of LESSON_REQUIRED_SECTION_HEADINGS) {
    if (!findUniqueMarkdownSection(sections, [heading], 2))
      fail3("LESSON_INVALID", `LESSONS.md is missing the required ## ${heading} section.`);
  }
  const existingRecords = readDurableLessonRecords(originalLessonsContent, target.relativePath);
  const stagedSemanticTargets = new Map;
  for (const record of existingRecords) {
    if (record.marker.disposition !== "reused") {
      if (!stagedSemanticTargets.has(record.marker.candidate_digest)) {
        stagedSemanticTargets.set(record.marker.candidate_digest, {
          task_id: record.marker.task_id,
          document_id: record.marker.document_id,
          archive_revision: record.marker.archive_revision,
          candidate_ref: record.marker.candidate_ref
        });
      }
    }
  }
  const newCandidates = [];
  const newReuseMarkers = [];
  for (const candidate of delta.candidates) {
    const matchingRefs = existingRecords.filter((record) => record.marker.candidate_ref === candidate.candidate_ref && record.marker.task_id === receipt.taskId);
    if (matchingRefs.length > 1) {
      fail3("LESSON_INVALID", `LESSONS contains duplicate durable records for candidate ${candidate.candidate_ref}.`);
    }
    if (matchingRefs.length > 0) {
      for (const existing of matchingRefs) {
        const marker = existing.marker;
        if (marker.task_id !== receipt.taskId || marker.task_slug !== receipt.taskSlug || marker.document_id !== receipt.documentId || marker.archive_path !== receipt.relativePath || marker.archive_revision !== receipt.revision || marker.source_revision !== receipt.sourceRevision || marker.candidate_digest !== lessonCandidateDigest(candidate) || marker.evidence_refs.join("|") !== candidate.evidence_refs.join("|") || lessonCandidateDigest(existing.candidate) !== lessonCandidateDigest(candidate)) {
          fail3("LESSON_PROVENANCE_MISMATCH", `lesson candidate ${candidate.candidate_ref} has conflicting durable provenance.`);
        }
      }
      continue;
    }
    const candidateDigest = lessonCandidateDigest(candidate);
    const existingTarget = stagedSemanticTargets.get(candidateDigest);
    if (existingTarget) {
      const reuseMarker = {
        task_id: receipt.taskId,
        task_slug: receipt.taskSlug,
        document_id: receipt.documentId,
        archive_path: receipt.relativePath,
        archive_revision: receipt.revision,
        source_revision: receipt.sourceRevision,
        candidate_ref: candidate.candidate_ref,
        candidate_digest: candidateDigest,
        evidence_refs: [...candidate.evidence_refs],
        disposition: "reused",
        reused_candidate: {
          task_id: existingTarget.task_id,
          document_id: existingTarget.document_id,
          archive_revision: existingTarget.archive_revision,
          candidate_ref: existingTarget.candidate_ref
        }
      };
      newReuseMarkers.push(reuseMarker);
      continue;
    }
    newCandidates.push(candidate);
    stagedSemanticTargets.set(candidateDigest, {
      task_id: receipt.taskId,
      document_id: receipt.documentId,
      archive_revision: receipt.revision,
      candidate_ref: candidate.candidate_ref
    });
  }
  if (newCandidates.length === 0 && newReuseMarkers.length === 0)
    return null;
  let nextLessonsContent = originalLessonsContent;
  let candidateCount = 0;
  if (newCandidates.length > 0) {
    const appended = appendLessonCandidates(nextLessonsContent, newCandidates, receipt, target.relativePath);
    nextLessonsContent = appended.content;
    candidateCount += appended.candidateCount;
  }
  if (newReuseMarkers.length > 0) {
    const availableRecords = [...existingRecords];
    for (const candidate of newCandidates) {
      availableRecords.push({
        marker: {
          task_id: receipt.taskId,
          task_slug: receipt.taskSlug,
          document_id: receipt.documentId,
          archive_path: receipt.relativePath,
          archive_revision: receipt.revision,
          source_revision: receipt.sourceRevision,
          candidate_ref: candidate.candidate_ref,
          candidate_digest: lessonCandidateDigest(candidate),
          evidence_refs: [...candidate.evidence_refs]
        },
        candidate
      });
    }
    nextLessonsContent = appendLessonReuseMarkers(nextLessonsContent, newReuseMarkers, availableRecords, target.relativePath);
    candidateCount += newReuseMarkers.length;
  }
  return {
    lessonsFilePath: target.filePath,
    lessonsRelativePath: target.relativePath,
    nextLessonsContent,
    originalLessonsContent,
    lessonsRevision: sha2565(nextLessonsContent),
    archive: receipt,
    candidateCount
  };
}
function knowledgeTarget(root2, knowledgeKind) {
  return workflowDocPathForRoot(root2, knowledgeKind === "contract" ? "CONTRACTS.md" : "DECISIONS.md");
}
function knowledgeSectionTitle(knowledgeKind) {
  return knowledgeKind === "contract" ? "vNext Contract Records" : "vNext Decision Records";
}
function knowledgeMarkerPrefix(knowledgeKind) {
  return `<!-- vNext ${knowledgeKind} record:`;
}
function knowledgeCandidateSemanticDigest(candidate) {
  return digest3({
    kind: candidate.kind,
    fingerprint: candidate.fingerprint,
    statement: candidate.statement,
    applicability: candidate.applicability,
    authoritySource: candidate.authoritySource,
    stability: candidate.stability,
    supersedes: candidate.supersedes,
    decisionContext: candidate.decisionContext ?? null
  });
}
function validateDurableKnowledgeRecord(value, location, expectedKind) {
  const record = expectRecord2(value, location);
  expectExactKeys2(record, ["schema_version", "knowledge_kind", "candidate_id", "candidate_fingerprint", "disposition", "matched_knowledge_id", "candidate", "provenance", "proposal_idempotency_key", "proposal_digest", "semantic_digest"], location);
  if (record.schema_version !== 1)
    fail3("KNOWLEDGE_RECORD_INVALID", `${location}.schema_version must be 1.`);
  const knowledgeKind = expectEnum(record.knowledge_kind, ["contract", "decision"], `${location}.knowledge_kind`);
  if (knowledgeKind !== expectedKind)
    fail3("KNOWLEDGE_RECORD_INVALID", `${location}.knowledge_kind must be ${expectedKind}.`);
  const candidate = validateKnowledgeCandidate(record.candidate, `${location}.candidate`, expectedKind);
  const candidateId = expectString2(record.candidate_id, `${location}.candidate_id`, SAFE_KEY_PATTERN2);
  const candidateFingerprint = expectString2(record.candidate_fingerprint, `${location}.candidate_fingerprint`, FINGERPRINT_PATTERN);
  if (candidateId !== candidate.candidateId || candidateFingerprint !== candidate.fingerprint)
    fail3("KNOWLEDGE_PROVENANCE_MISMATCH", `${location} candidate identity does not match the embedded candidate.`);
  const disposition = expectEnum(record.disposition, ["admit", "merge", "supersede"], `${location}.disposition`);
  const matchedKnowledgeId = expectNullableString(record.matched_knowledge_id, `${location}.matched_knowledge_id`, SAFE_KEY_PATTERN2);
  if (disposition === "merge" && matchedKnowledgeId === null)
    fail3("KNOWLEDGE_RECORD_INVALID", `${location}.matched_knowledge_id is required for merge.`);
  if (disposition === "supersede" && (matchedKnowledgeId === null || candidate.supersedes !== matchedKnowledgeId))
    fail3("KNOWLEDGE_RECORD_INVALID", `${location}.supersede predecessor identity is inconsistent.`);
  if (candidate.authoritySource === "none")
    fail3("KNOWLEDGE_RECORD_INVALID", `${location} cannot be a durable record without an authority source.`);
  if (knowledgeKind === "decision" && !["user", "accepted-decision"].includes(candidate.authoritySource)) {
    fail3("KNOWLEDGE_RECORD_INVALID", `${location} Decision record requires user or accepted-decision authority.`);
  }
  if (candidate.stability !== "stable" || candidate.conflictSet.length > 0 || candidate.evidenceRefs.length === 0) {
    fail3("KNOWLEDGE_RECORD_INVALID", `${location} durable record must contain stable, conflict-free candidate evidence.`);
  }
  const provenance = validateKnowledgeProvenance(record.provenance, `${location}.provenance`);
  const proposalIdempotencyKey = expectString2(record.proposal_idempotency_key, `${location}.proposal_idempotency_key`, SAFE_KEY_PATTERN2);
  const proposalDigest = expectString2(record.proposal_digest, `${location}.proposal_digest`);
  const semanticDigest = expectString2(record.semantic_digest, `${location}.semantic_digest`);
  if (!SHA256_PATTERN2.test(proposalDigest) || !SHA256_PATTERN2.test(semanticDigest))
    fail3("KNOWLEDGE_RECORD_INVALID", `${location} proposal and semantic digests must be SHA-256.`);
  if (semanticDigest !== knowledgeCandidateSemanticDigest(candidate))
    fail3("KNOWLEDGE_PROVENANCE_MISMATCH", `${location}.semantic_digest does not match the canonical candidate.`);
  return {
    schema_version: 1,
    knowledge_kind: knowledgeKind,
    candidate_id: candidateId,
    candidate_fingerprint: candidateFingerprint,
    disposition,
    matched_knowledge_id: matchedKnowledgeId,
    candidate,
    provenance,
    proposal_idempotency_key: proposalIdempotencyKey,
    proposal_digest: proposalDigest,
    semantic_digest: semanticDigest
  };
}
function renderDurableKnowledgeRecord(record) {
  const label = record.knowledge_kind === "contract" ? "Contract" : "Decision";
  const candidate = record.candidate;
  const anchors = candidate.implementation_anchors;
  return [
    `### ${label}: ${candidate.candidateId}`,
    "",
    `<!-- vNext ${record.knowledge_kind} record: ${JSON.stringify(record)} -->`,
    "",
    `- candidate_id: ${yamlScalar(candidate.candidateId)}`,
    `- fingerprint: ${yamlScalar(candidate.fingerprint)}`,
    `- disposition: ${record.disposition}`,
    `- statement: ${yamlScalar(candidate.statement)}`,
    `- authority_source: ${yamlScalar(candidate.authoritySource)}`,
    `- applicability: ${JSON.stringify(candidate.applicability)}`,
    `- evidence_refs: ${JSON.stringify(candidate.evidenceRefs)}`,
    `- implementation_anchors: ${anchors ? JSON.stringify(anchors) : "none"}`,
    `- provenance: ${JSON.stringify(record.provenance)}`,
    `- proposal_idempotency_key: ${yamlScalar(record.proposal_idempotency_key)}`,
    ""
  ].join(`
`);
}
function readDurableKnowledgeRecords(content, location, expectedKind) {
  const markers = [];
  const markerPattern = /<!-- vNext (contract|decision) record: (\{[^\r\n]+\}) -->/g;
  for (const match of content.matchAll(markerPattern)) {
    const markerKind = match[1];
    if (markerKind !== expectedKind)
      fail3("KNOWLEDGE_RECORD_INVALID", `${location} contains a ${markerKind} record in the ${expectedKind} document.`);
    let parsed;
    try {
      parsed = JSON.parse(match[2]);
    } catch {
      fail3("KNOWLEDGE_RECORD_INVALID", `${location} contains an invalid vNext knowledge marker.`);
    }
    const record = validateDurableKnowledgeRecord(parsed, `${location}.${expectedKind}[${markers.length}]`, expectedKind);
    const markerText = `<!-- vNext ${expectedKind} record: ${JSON.stringify(record)} -->`;
    if (countExactOccurrences(content, markerText) !== 1 || countExactOccurrences(content, renderDurableKnowledgeRecord(record)) !== 1) {
      fail3("KNOWLEDGE_PROVENANCE_MISMATCH", `${location}.${expectedKind}[${markers.length}] visible bytes do not match the canonical durable record.`);
    }
    const markerStart = content.indexOf(markerText);
    const section = findUniqueMarkdownSection(scanMarkdownSections2(content), [knowledgeSectionTitle(expectedKind)], 2);
    if (!section || markerStart < section.contentStart || markerStart >= section.contentEnd) {
      fail3("KNOWLEDGE_RECORD_INVALID", `${location}.${expectedKind}[${markers.length}] is outside the canonical knowledge section.`);
    }
    markers.push(record);
  }
  for (const markerKind of ["contract", "decision"]) {
    const prefix = knowledgeMarkerPrefix(markerKind);
    const markerCount = countExactOccurrences(content, prefix);
    const parsedCount = markers.filter((record) => record.knowledge_kind === markerKind).length;
    if (markerCount !== parsedCount) {
      fail3("KNOWLEDGE_RECORD_INVALID", `${location} contains a malformed or partially unreadable ${markerKind} record marker.`);
    }
  }
  if (content.includes("<!-- vNext contract record:") && expectedKind !== "contract")
    fail3("KNOWLEDGE_RECORD_INVALID", `${location} contains a Contract record in the wrong target.`);
  if (content.includes("<!-- vNext decision record:") && expectedKind !== "decision")
    fail3("KNOWLEDGE_RECORD_INVALID", `${location} contains a Decision record in the wrong target.`);
  return markers;
}
function readCanonicalDurableKnowledgeSection(content, location, expectedKind) {
  const records = readDurableKnowledgeRecords(content, location, expectedKind);
  const section = findUniqueMarkdownSection(scanMarkdownSections2(content), [knowledgeSectionTitle(expectedKind)], 2);
  if (!section) {
    if (records.length > 0)
      fail3("KNOWLEDGE_RECORD_INVALID", `${location} durable records are missing their canonical knowledge section.`);
    return null;
  }
  return content.slice(section.contentStart, section.contentEnd);
}
function appendKnowledgeSectionIfMissing(content, knowledgeKind) {
  const title = knowledgeSectionTitle(knowledgeKind);
  const sections = scanMarkdownSections2(content);
  const existing = findUniqueMarkdownSection(sections, [title], 2);
  if (existing)
    return content;
  return `${content.trimEnd()}

## ${title}

`;
}
function replaceDurableKnowledgeRecord(content, knowledgeKind, previous, next) {
  const previousText = renderDurableKnowledgeRecord(previous);
  if (countExactOccurrences(content, previousText) !== 1) {
    fail3("KNOWLEDGE_PROVENANCE_MISMATCH", `canonical ${knowledgeKind} document does not contain exactly one predecessor record for merge.`);
  }
  const start = content.indexOf(previousText);
  if (start < 0)
    fail3("KNOWLEDGE_PROVENANCE_MISMATCH", `canonical ${knowledgeKind} predecessor record cannot be located for merge.`);
  return `${content.slice(0, start)}${renderDurableKnowledgeRecord(next)}${content.slice(start + previousText.length)}`;
}
function knowledgeAdmissionMatches(left, right) {
  return JSON.stringify(left) === JSON.stringify(right);
}
function knowledgeProvenanceMatchesArchive(provenance, archive, proposal) {
  const admission = knowledgeAdmissionFromArchive(archive, proposal.semantic_delta.knowledge_kind, proposal.semantic_delta.admission.candidate.candidateId);
  const admissionEvidenceRefs = [
    ...admission.candidate.evidenceRefs,
    ...admission.candidate.implementation_anchors?.anchors.flatMap((anchor) => anchor.evidence_refs) ?? []
  ];
  if (provenance.task_id !== archive.taskId || provenance.task_slug !== archive.taskSlug || provenance.document_id !== archive.documentId || provenance.archive_path !== archive.relativePath || provenance.archive_revision !== archive.revision || provenance.source_revision !== archive.sourceRevision || !admissionEvidenceRefs.every((ref) => provenance.evidence_refs.includes(ref)) || !provenance.evidence_refs.every((ref) => archiveEvidenceRefsForKnowledge(archive).includes(ref))) {
    fail3("KNOWLEDGE_PROVENANCE_MISMATCH", "knowledge proposal provenance does not match the canonical archive receipt.");
  }
  if (!provenance.evidence_refs.every((ref) => proposal.evidence_refs.includes(ref))) {
    fail3("RUNTIME_EVIDENCE_INVALID", "knowledge proposal evidence_refs must cover provenance evidence_refs.");
  }
}
function archiveEvidenceRefsForKnowledge(archive) {
  return [...new Set([
    ...archive.lessonAdmission.evidence_refs,
    ...archive.knowledgeAdmissions.contracts.flatMap((item) => [...item.candidate.evidenceRefs, ...item.candidate.implementation_anchors?.anchors.flatMap((anchor) => anchor.evidence_refs) ?? []]),
    ...archive.knowledgeAdmissions.decisions.flatMap((item) => [...item.candidate.evidenceRefs, ...item.candidate.implementation_anchors?.anchors.flatMap((anchor) => anchor.evidence_refs) ?? []])
  ])];
}
function durableKnowledgeRecordFromProposal(proposal, archive) {
  const delta = proposal.semantic_delta;
  knowledgeProvenanceMatchesArchive(delta.provenance, archive, proposal);
  const admission = delta.admission;
  return {
    schema_version: 1,
    knowledge_kind: delta.knowledge_kind,
    candidate_id: admission.candidate.candidateId,
    candidate_fingerprint: admission.candidate.fingerprint,
    disposition: admission.disposition,
    matched_knowledge_id: admission.matched_knowledge_id,
    candidate: admission.candidate,
    provenance: delta.provenance,
    proposal_idempotency_key: proposal.idempotency_key,
    proposal_digest: digest3(proposal),
    semantic_digest: knowledgeCandidateSemanticDigest(admission.candidate)
  };
}
function knowledgeAdmissionFromArchive(archive, knowledgeKind, candidateId) {
  const admissions = knowledgeKind === "contract" ? archive.knowledgeAdmissions.contracts : archive.knowledgeAdmissions.decisions;
  const matches = admissions.filter((item) => item.candidate.candidateId === candidateId);
  if (matches.length !== 1)
    fail3("KNOWLEDGE_PROVENANCE_MISMATCH", `archive does not contain exactly one ${knowledgeKind} admission for ${candidateId}.`);
  return matches[0];
}
function scanKnowledgeRecords(root2) {
  const result = [];
  for (const knowledgeKind of ["contract", "decision"]) {
    const target = knowledgeTarget(root2, knowledgeKind);
    if (!fs11.existsSync(target.filePath))
      continue;
    if (!fs11.statSync(target.filePath).isFile())
      fail3("KNOWLEDGE_RECORD_INVALID", `${target.relativePath} is not a regular file.`);
    result.push(...readDurableKnowledgeRecords(fs11.readFileSync(target.filePath, "utf8"), target.relativePath, knowledgeKind));
  }
  return result;
}
function inspectKnowledgeRecordTransaction(root2, current, proposal) {
  const { receipt } = matchingArchiveReceipt(root2, current);
  const delta = proposal.semantic_delta;
  const archiveAdmission = knowledgeAdmissionFromArchive(receipt, delta.knowledge_kind, delta.admission.candidate.candidateId);
  if (!knowledgeAdmissionMatches(archiveAdmission, delta.admission))
    fail3("KNOWLEDGE_PROVENANCE_MISMATCH", "knowledge proposal admission does not match the archived close-task admission decision.");
  const expectedRecord = durableKnowledgeRecordFromProposal(proposal, receipt);
  const target = knowledgeTarget(root2, delta.knowledge_kind);
  if (!fs11.existsSync(target.filePath))
    fail3("RUNTIME_SOURCE_MISSING", `knowledge target is missing: ${target.relativePath}`);
  const originalContent = fs11.readFileSync(target.filePath, "utf8");
  const records = readDurableKnowledgeRecords(originalContent, target.relativePath, delta.knowledge_kind);
  const allRecords = scanKnowledgeRecords(root2);
  const sameIdempotency = allRecords.filter((record) => record.proposal_idempotency_key === proposal.idempotency_key);
  if (sameIdempotency.some((record) => JSON.stringify(record) !== JSON.stringify(expectedRecord))) {
    fail3("IDEMPOTENCY_CONFLICT", "knowledge idempotency key is already durably bound to a different candidate or target.");
  }
  const sameIdentity = records.filter((record) => record.candidate_id === expectedRecord.candidate_id);
  if (sameIdentity.length > 1)
    fail3("KNOWLEDGE_RECORD_INVALID", `knowledge target contains duplicate candidate identity ${expectedRecord.candidate_id}.`);
  if (sameIdentity.length === 1) {
    const existingRecord = sameIdentity[0];
    if (JSON.stringify(existingRecord) === JSON.stringify(expectedRecord)) {
      return { filePath: target.filePath, relativePath: target.relativePath, nextContent: originalContent, originalContent, record: expectedRecord, existing: true };
    }
    if (expectedRecord.disposition === "merge" && expectedRecord.matched_knowledge_id === existingRecord.candidate_id) {
      return {
        filePath: target.filePath,
        relativePath: target.relativePath,
        nextContent: replaceDurableKnowledgeRecord(originalContent, delta.knowledge_kind, existingRecord, expectedRecord),
        originalContent,
        record: expectedRecord,
        existing: false
      };
    }
    fail3("KNOWLEDGE_IDENTITY_CONFLICT", `${target.relativePath} contains different semantic or provenance content for ${expectedRecord.candidate_id}.`);
  }
  if (expectedRecord.disposition === "merge") {
    const predecessor = records.find((record) => record.candidate_id === expectedRecord.matched_knowledge_id);
    if (!predecessor)
      fail3("KNOWLEDGE_ADMISSION_INVALID", `knowledge merge target ${expectedRecord.matched_knowledge_id} is not durably present.`);
    const semanticMatches = records.filter((record) => record.semantic_digest === expectedRecord.semantic_digest);
    if (semanticMatches.some((record) => record.candidate_id !== predecessor.candidate_id)) {
      fail3("KNOWLEDGE_IDENTITY_CONFLICT", "knowledge merge semantic content already belongs to a different durable item.");
    }
    return {
      filePath: target.filePath,
      relativePath: target.relativePath,
      nextContent: replaceDurableKnowledgeRecord(originalContent, delta.knowledge_kind, predecessor, expectedRecord),
      originalContent,
      record: expectedRecord,
      existing: false
    };
  }
  const semanticMatches = records.filter((record) => record.semantic_digest === expectedRecord.semantic_digest);
  if (semanticMatches.length > 0) {
    return { filePath: target.filePath, relativePath: target.relativePath, nextContent: originalContent, originalContent, record: expectedRecord, existing: true };
  }
  if (expectedRecord.disposition === "supersede") {
    const predecessor = records.find((record) => record.candidate_id === expectedRecord.matched_knowledge_id);
    if (!predecessor)
      fail3("KNOWLEDGE_ADMISSION_INVALID", `knowledge supersede target ${expectedRecord.matched_knowledge_id} is not durably present.`);
  }
  const withSection = appendKnowledgeSectionIfMissing(originalContent, delta.knowledge_kind);
  return {
    filePath: target.filePath,
    relativePath: target.relativePath,
    nextContent: `${withSection}${renderDurableKnowledgeRecord(expectedRecord)}`,
    originalContent,
    record: expectedRecord,
    existing: false
  };
}
function prepareKnowledgeRecordTransaction(root2, current, proposal) {
  ensureAuthorityKinds(proposal, ["evidence-admission"]);
  if (current.runtimeState.workflow_status !== "closed" || current.runtimeState.lifecycle_state !== "archived") {
    fail3("KNOWLEDGE_ADMISSION_INVALID", "knowledge promotion requires a closed + archived task.");
  }
  return inspectKnowledgeRecordTransaction(root2, current, proposal);
}
function assertRequestedInboxTargets(root2, current, proposal) {
  if (proposal.source_tuple.path !== current.relativePath)
    fail3("RUNTIME_PATH_INVALID", "capture proposal source path is not the exact canonical CURRENT_TASK path.");
  const target = canonicalInboxRecordTarget(root2, proposal.semantic_delta);
  if (proposal.requested_write_targets.length !== 1 || proposal.requested_write_targets[0] !== target.relativePath) {
    fail3("RUNTIME_PATH_INVALID", "capture proposal must name only its exact identity-derived inbox path.");
  }
}
function assertRequestedCloseTargets(root2, current, proposal) {
  if (proposal.source_tuple.path !== current.relativePath)
    fail3("RUNTIME_PATH_INVALID", "close-task proposal source path is not the exact canonical CURRENT_TASK path.");
  if (proposal.operation_kind === "archive-transaction") {
    const archive = archivePathForTask(root2, current);
    if (proposal.requested_write_targets.length !== 2 || proposal.requested_write_targets[0] !== current.relativePath || proposal.requested_write_targets[1] !== archive.relativePath) {
      fail3("RUNTIME_PATH_INVALID", "archive proposal must name CURRENT_TASK and its exact identity-derived archive path.");
    }
    return;
  }
  const file = proposal.operation_kind === "project-status-transaction" ? "STATUS.md" : proposal.operation_kind === "lesson-record-transaction" ? "LESSONS.md" : proposal.operation_kind === "contract-candidate-commit" ? "CONTRACTS.md" : proposal.operation_kind === "decision-record-transaction" ? "DECISIONS.md" : null;
  if (file === null)
    fail3("RUNTIME_PATH_INVALID", `${proposal.operation_kind} is not a close-task document operation.`);
  const target = workflowDocPathForRoot(root2, file);
  if (proposal.requested_write_targets.length !== 1 || proposal.requested_write_targets[0] !== target.relativePath) {
    fail3("RUNTIME_PATH_INVALID", `${file} proposal must name only its exact canonical path.`);
  }
}
function assertPreviousTaskReconciliationComplete(root2, current, receipt) {
  const knowledgeAdmissions = [
    ...receipt.knowledgeAdmissions.contracts,
    ...receipt.knowledgeAdmissions.decisions
  ].filter((admission) => ["admit", "merge", "supersede"].includes(admission.disposition));
  if (knowledgeAdmissions.length > 0) {
    for (const admission of knowledgeAdmissions) {
      const knowledgeKind = admission.candidate.kind;
      const target = knowledgeTarget(root2, knowledgeKind);
      if (!fs11.existsSync(target.filePath)) {
        fail3("PREVIOUS_TASK_RECONCILIATION_INCOMPLETE", `previous task ${current.runtimeState.task_id} ${knowledgeKind} reconciliation is incomplete: ${target.relativePath} does not exist.`);
      }
      const records = readDurableKnowledgeRecords(fs11.readFileSync(target.filePath, "utf8"), target.relativePath, knowledgeKind);
      const exactIdentity = records.find((record) => record.candidate_id === admission.candidate.candidateId);
      const equivalent = records.some((record) => record.semantic_digest === knowledgeCandidateSemanticDigest(admission.candidate));
      if (exactIdentity) {
        if (JSON.stringify(exactIdentity.candidate) !== JSON.stringify(admission.candidate) || exactIdentity.candidate_fingerprint !== admission.candidate.fingerprint || exactIdentity.semantic_digest !== knowledgeCandidateSemanticDigest(admission.candidate) || exactIdentity.disposition !== admission.disposition || exactIdentity.matched_knowledge_id !== admission.matched_knowledge_id || exactIdentity.provenance.task_id !== receipt.taskId || exactIdentity.provenance.task_slug !== receipt.taskSlug || exactIdentity.provenance.document_id !== receipt.documentId || exactIdentity.provenance.archive_path !== receipt.relativePath || exactIdentity.provenance.archive_revision !== receipt.revision || exactIdentity.provenance.source_revision !== receipt.sourceRevision || !exactIdentity.provenance.evidence_refs.every((ref) => archiveEvidenceRefsForKnowledge(receipt).includes(ref))) {
          fail3("PREVIOUS_TASK_RECONCILIATION_INCOMPLETE", `previous task ${current.runtimeState.task_id} ${knowledgeKind} reconciliation provenance conflicts with the canonical archive.`);
        }
      } else if (!equivalent) {
        fail3("PREVIOUS_TASK_RECONCILIATION_INCOMPLETE", `previous task ${current.runtimeState.task_id} ${knowledgeKind} candidate ${admission.candidate.candidateId} has not been reconciled.`);
      }
    }
  }
  if (receipt.lessonAdmission.decision === "admit") {
    const lessonsTarget = workflowDocPathForRoot(root2, "LESSONS.md");
    if (!fs11.existsSync(lessonsTarget.filePath)) {
      fail3("PREVIOUS_TASK_RECONCILIATION_INCOMPLETE", `previous task ${current.runtimeState.task_id} lesson reconciliation is incomplete: LESSONS.md does not exist.`);
    }
    const lessonsContent = fs11.readFileSync(lessonsTarget.filePath, "utf8");
    const existingRecords = readDurableLessonRecords(lessonsContent, lessonsTarget.relativePath);
    for (const candidateRef of receipt.lessonAdmission.candidate_refs) {
      const matching = existingRecords.find((record) => record.marker.candidate_ref === candidateRef && record.marker.task_id === receipt.taskId && record.marker.task_slug === receipt.taskSlug && record.marker.document_id === receipt.documentId && record.marker.archive_path === receipt.relativePath && record.marker.archive_revision === receipt.revision && record.marker.source_revision === receipt.sourceRevision);
      if (!matching) {
        fail3("PREVIOUS_TASK_RECONCILIATION_INCOMPLETE", `previous task ${current.runtimeState.task_id} lesson candidate ${candidateRef} has not been reconciled.`);
      }
    }
  }
}
function ensureAuthorityKinds(proposal, required) {
  const kinds = new Set(proposal.authority_evidence.map((item) => item.kind));
  const missing = required.filter((kind) => !kinds.has(kind));
  if (missing.length > 0)
    fail3("RUNTIME_AUTHORITY_MISSING", `proposal is missing authority evidence: ${missing.join(", ")}`);
}
function compareSourceTuple(expected, actual) {
  const fields = [
    "path",
    "revision",
    "document_id",
    "task_id",
    "task_slug",
    "workflow_status",
    "lifecycle_state",
    "active_step_id",
    "active_step_status",
    "finding_queue_revision",
    "resume_requires_review",
    "resume_review_reasons"
  ];
  for (const field of fields) {
    if (field === "resume_review_reasons") {
      if (expected[field].join("|") !== actual[field].join("|"))
        return field;
    } else if (expected[field] !== actual[field]) {
      return field;
    }
  }
  return null;
}
function appendAppliedProposal(current, proposal, sourceRevision) {
  const next = [
    ...current.applied_proposals,
    {
      idempotency_key: proposal.idempotency_key,
      operation_kind: proposal.operation_kind,
      proposal_digest: digest3(proposal),
      source_revision: sourceRevision
    }
  ];
  return current.__vnext_compact_history === true ? next : next.slice(-MAX_APPLIED_PROPOSALS);
}
function appendExecutionLogEntry(current, entry) {
  const next = [...current.execution_log, entry];
  return current.__vnext_compact_history === true ? next : next.slice(-MAX_EXECUTION_LOG);
}
function replaceValidation(root2, rawInput, options = {}) {
  return withGovernanceWriteLock(root2, () => {
    const input = expectRecord2(rawInput, "replace-validation");
    expectExactKeys2(input, ["source_revision", "claim_id", "slot_id", "replaces_check_id", "replacement_check", "reason"], "replace-validation");
    const sourceRevision = expectString2(input.source_revision, "source_revision", SHA256_PATTERN2);
    const reason = expectText(input.reason, "reason");
    if (!options.dryRun)
      recoverPendingTaskStoreCommit(root2);
    const current = readCanonicalCurrentTask(root2);
    const key = `replace-validation-${digest3({ document: current.sourceTuple.document_id, input }).slice(0, 40)}`;
    const prior = current.runtimeState.applied_proposals.find((p) => p.idempotency_key === key);
    if (prior) {
      assertTaskHistoryForRevision(current.filePath, current.sourceTuple.document_id, current.runtimeState.task_id, sourceRevision, "replace-validation");
      return {
        status: "no-op",
        operation_kind: "task-state-transaction",
        idempotency_key: key,
        target_path: current.relativePath,
        dry_run: !!options.dryRun,
        committed: false,
        message: "This exact engineering replacement was already committed.",
        planned_writes: [],
        governed_mutation_count: 0,
        read_back_verified: true,
        evidence_assurance: "caller-reported"
      };
    }
    if (current.sourceTuple.revision !== sourceRevision)
      fail3("VALIDATION_REPLACEMENT_STALE", "Read the current step again; the request names an older task revision.");
    assertBusinessEvidenceVersion(current);
    const state = current.runtimeState;
    if (state.workflow_status !== "active" || state.lifecycle_state !== "active" || state.resume_requires_review || !["ready", "blocked"].includes(state.active_step_status) || state.pending_review_result)
      fail3("VALIDATION_REPLACEMENT_STATE_INVALID", "Replace a verification between attempts, not during execution or pending review.");
    if ((state.evidence_challenges ?? []).some((c) => c.status !== "resolved"))
      fail3("EVIDENCE_CHALLENGE_UNRESOLVED", "A challenged obligation needs its existing correction owner, not engineering substitution.");
    const ledger = state.step_attempts?.[state.active_step_id];
    if (ledger?.attempts.at(-1)?.status === "preflighted")
      fail3("EXECUTE_ATTEMPT_OUTSTANDING", "Record the current attempt before changing its verification.");
    const claims = copyClaimEvidence(state.claim_evidence ?? []);
    const claim = claims.find((c) => c.claim_id === input.claim_id);
    const target = claim?.slots.find((s) => s.slot_id === input.slot_id);
    const old = target?.check;
    if (!claim || !target || !old || old.check_id !== input.replaces_check_id || target.due_step_id !== state.active_step_id || target.applicability !== "current" || target.user_decision || target.prerequisite_receipt)
      fail3("VALIDATION_REPLACEMENT_TARGET_INVALID", "Engineering replacement preserves one current-step, unconsumed evidence obligation; it never overrides a user decision or prerequisite.");
    const replacement = validateClaimEvidence([{ ...claim, slots: [{ ...target, check: input.replacement_check }] }])[0].slots[0].check;
    if ((state.claim_evidence ?? []).some((c) => c.slots.some((s) => s.check?.check_id === replacement.check_id)) || old.method !== "execution" || replacement.method !== "execution")
      fail3("VALIDATION_REPLACEMENT_ID_INVALID", "An execution replacement requires a new check identity.");
    const semantics = (check) => ({
      method: check.method,
      boundary: check.boundary,
      expected_observation: check.expected_observation,
      expected_result: check.expected_result,
      subject_paths: check.subject_paths,
      required_boundaries: check.required_boundaries,
      allowed_substitutes: check.allowed_substitutes,
      validation_items: check.validation_items
    });
    const oldGranularity = old.selection?.granularity, newGranularity = replacement.selection?.granularity;
    if (digest3(semantics(old)) !== digest3(semantics(replacement)) || !oldGranularity || oldGranularity !== newGranularity || old.selection?.selector !== replacement.selection?.selector || old.selection?.breadth_basis !== replacement.selection?.breadth_basis || old.selection?.breadth_source_ref !== replacement.selection?.breadth_source_ref) {
      fail3("VALIDATION_REPLACEMENT_WEAKENED", "Preserve the observation, boundary, subjects, validation ownership, exact granularity, selector and breadth authority. Changed selection needs its explicit planning route.");
    }
    const definition = readDraftDefinitionFromBody(current.body);
    const block = implementationStepBlock(definition, state.active_step_id);
    if (!block)
      fail3("VALIDATION_REPLACEMENT_TARGET_INVALID", "The frozen current step is missing.");
    const lines = block.split(`
`);
    const indices = lines.flatMap((line, i) => line.trim() === `- planned_command: ${old.entry}` ? [i] : []);
    if (indices.length !== 1 || lines[indices[0] + 1]?.trim() !== "- expected_repo_writes: none")
      fail3("VALIDATION_REPLACEMENT_WRITES_FORBIDDEN", "This route only replaces one exact read-only planned check invocation.");
    if (claims.some((c) => c.slots.some((s) => s !== target && s.due_step_id === state.active_step_id && s.check?.entry === old.entry)))
      fail3("VALIDATION_REPLACEMENT_SHARED_COMMAND", "A shared invocation must retain every bound obligation; use a reviewed multi-check correction.");
    lines[indices[0]] = lines[indices[0]].replace(old.entry, replacement.entry);
    const evidenceIndex = lines.findIndex((line) => /^\s*- required_evidence:/.test(line));
    if (evidenceIndex >= 0 && !old.validation_items?.length)
      lines[evidenceIndex] = lines[evidenceIndex].split(old.entry).join(replacement.entry);
    definition.implementation_steps = definition.implementation_steps.replace(block, lines.join(`
`));
    target.check = replacement;
    target.report = null;
    target.evidence_refs = [];
    target.disposition = "missing";
    const basis = readCanonicalTaskBasis(root2, current);
    const newPlan = assertEvidencePlan(definition, claims, false, { root: root2, taskBasis: basis.basis, previous: state.claim_evidence ?? [] });
    const carry = [];
    for (const c of claims)
      for (const slot of c.slots) {
        if (slot === target || !slot.report && !slot.user_decision || !slot.check)
          continue;
        const before = state.claim_evidence.find((x) => x.claim_id === c.claim_id).slots.find((x) => x.slot_id === slot.slot_id);
        if (before.user_decision) {
          try {
            carry.push(carryUserEvidenceDecision(root2, current, c.claim_id, before, newPlan, "replace-validation"));
          } catch {}
          continue;
        }
        try {
          assertEvidenceReportApplicable(root2, current, before);
        } catch {
          continue;
        }
        const resultId = slot.report.result_id;
        const origin = state.evidence_carry_forward?.find((p) => p.claim_id === c.claim_id && p.slot_id === slot.slot_id && p.new_plan_revision === state.evidence_plan_revision && p.result_id === resultId);
        carry.push({
          kind: "evidence-carry-forward/v2",
          old_source_revision: origin?.old_source_revision ?? sourceRevision,
          history_operation: origin ? origin.history_operation ?? "confirm-replan" : "replace-validation",
          old_plan_revision: slot.report?.evidence_plan_revision ?? slot.user_decision.evidence_plan_revision,
          new_plan_revision: newPlan,
          receiving_source_revision: sourceRevision,
          context_revision: recoveryEvidenceContextRevision(root2),
          evidence_objects: slot.user_decision ? [] : describeEvidenceObjects(root2, slot.evidence_refs),
          claim_id: c.claim_id,
          slot_id: slot.slot_id,
          check_id: slot.check.check_id,
          result_id: resultId,
          report_sha256: digest3(slot.report ?? null),
          subject_revision: slot.report?.subject_revision ?? slot.user_decision.subject_revision,
          ...slot.user_decision ? { user_decision_sha256: digest3(slot.user_decision) } : {}
        });
      }
    const proposal = {
      schema_version: 1,
      kind: VNEXT_RUNTIME_PROPOSAL_KIND,
      operation_kind: "task-state-transaction",
      caller: "execute-step",
      mode: "default",
      source_tuple: current.sourceTuple,
      semantic_delta: { kind: "task-state", action: "replace-validation", ...input, reason },
      evidence_refs: [],
      idempotency_key: key,
      requested_write_targets: [current.relativePath]
    };
    const stepAttempts = structuredClone(state.step_attempts ?? {});
    for (const entry of Object.values(stepAttempts))
      entry.evidence_plan_revision = newPlan;
    if (ledger?.attempts.at(-1)?.status === "blocked") {
      if (ledger.attempts.length >= ledger.max_attempts)
        fail3("RETRY_BUDGET_EXHAUSTED", "Engineering replacement does not reset the existing attempt budget.");
      stepAttempts[state.active_step_id].attempts.push({
        attempt_id: `attempt-${digest3({ document: current.sourceTuple.document_id, plan: newPlan, step: state.active_step_id, n: ledger.attempts.length + 1 }).slice(0, 40)}`,
        idempotency_key: key,
        request_digest: digest3(input),
        status: "ready",
        blocker: null,
        evidence_refs: [...ledger.attempts.at(-1).evidence_refs]
      });
    }
    const next = {
      ...state,
      active_step_status: "ready",
      claim_evidence: claims,
      evidence_plan_revision: newPlan,
      evidence_carry_forward: carry,
      step_attempts: stepAttempts,
      applied_proposals: appendAppliedProposal(state, proposal, sourceRevision)
    };
    delete next.execution_preflight;
    const prepared = renderCanonicalCurrentTask(current.frontmatter, current.body, next, { replacementDefinition: definition });
    const nextContent = prepared.content;
    const historyInput = {
      currentPath: current.filePath,
      previousContent: current.raw,
      nextContent,
      documentId: current.sourceTuple.document_id,
      taskId: state.task_id,
      basisPath: basis.filePath,
      basisContent: basis.content,
      operation: "replace-validation",
      evidencePlanRevision: state.evidence_plan_revision
    };
    const history = taskHistoryLocation(historyInput);
    const writes = [path12.posix.join(path12.posix.dirname(current.relativePath), history.relativePath), current.relativePath];
    if (options.dryRun)
      return {
        status: "success",
        operation_kind: "task-state-transaction",
        idempotency_key: key,
        target_path: current.relativePath,
        dry_run: true,
        committed: false,
        message: "Equivalent invocation replacement validated; task intent is unchanged.",
        planned_writes: writes,
        governed_mutation_count: 0,
        read_back_verified: false,
        evidence_assurance: "caller-reported"
      };
    for (const proof of carry)
      preserveEvidenceObjects(root2, current.filePath, proof.evidence_objects ?? []);
    const staged = stageTaskEvolutionStoreCommit(root2, current, nextContent, next, proposal, [{ path: history.path, content: history.content }, { path: current.filePath, content: nextContent }], prepared);
    try {
      commitTaskEvolutionWithHistory(historyInput, (raw) => {
        const parsed = parseCanonicalCurrentTaskContent(raw, current.filePath, current.relativePath);
        if (parsed.runtimeState.evidence_plan_revision !== newPlan)
          fail3("VALIDATION_REPLACEMENT_READ_BACK_FAILED", "Evidence plan read-back differs.");
      });
    } catch (error) {
      if (fs11.readFileSync(current.filePath, "utf8") === current.raw)
        clearPendingTaskStoreAfterRollback(root2, current);
      throw error;
    }
    completeTaskEvolutionStoreCommit(root2, current, staged, proposal, { status: "success", committed: true, operation_kind: "task-state-transaction", idempotency_key: key });
    return {
      status: "success",
      operation_kind: "task-state-transaction",
      idempotency_key: key,
      target_path: current.relativePath,
      dry_run: false,
      committed: true,
      message: "One verification invocation replaced; task identity, obligations, history, scope and retry budget preserved.",
      planned_writes: writes,
      governed_mutation_count: 2,
      read_back_verified: true,
      previous_revision: sourceRevision,
      resulting_revision: staged.sourceTuple.revision,
      state: resultState(next),
      evidence_assurance: "caller-reported"
    };
  });
}
function initializeTaskPreservation(root2, rawInput, options = {}) {
  return withGovernanceWriteLock(root2, () => initializeTaskPreservationLocked(root2, rawInput, options));
}
function initializeTaskPreservationLocked(root2, rawInput, options) {
  const input = expectRecord2(rawInput, "initialize-preservation input");
  expectExactKeys2(input, ["source_revision", "basis_revision"], "initialize-preservation input");
  const sourceRevision = expectString2(input.source_revision, "source_revision", /^[a-f0-9]{64}$/);
  const basisRevision = expectString2(input.basis_revision, "basis_revision", /^[a-f0-9]{64}$/);
  if (!options.dryRun)
    recoverPendingTaskStoreCommit(root2);
  const current = readCanonicalCurrentTask(root2);
  const idempotencyKey = `initialize-preservation-${current.sourceTuple.document_id}`;
  if (current.runtimeState.task_evolution_version === 2) {
    if (sourceRevision !== (current.runtimeState.preservation_source_revision ?? current.sourceTuple.revision)) {
      fail3("TASK_EVOLUTION_SOURCE_STALE", "Preservation replay does not name the initialized source revision.");
    }
    const boundBasisRevision = current.runtimeState.preservation_source_revision ? JSON.parse(fs11.readFileSync(path12.join(path12.dirname(current.filePath), "task-history", current.sourceTuple.document_id, `${current.runtimeState.preservation_source_revision}.json`), "utf8")).task_basis_revision : readCanonicalTaskBasis(root2, current).revision;
    if (basisRevision !== boundBasisRevision)
      fail3("TASK_EVOLUTION_BASIS_STALE", "Preservation replay does not name the initialized Task Basis revision.");
    return {
      status: "no-op",
      operation_kind: "task-state-transaction",
      idempotency_key: idempotencyKey,
      target_path: current.relativePath,
      dry_run: options.dryRun === true,
      committed: false,
      message: "This task already uses task-evolution preservation.",
      planned_writes: [],
      governed_mutation_count: 0,
      read_back_verified: true,
      resulting_revision: current.sourceTuple.revision,
      evidence_assurance: "caller-reported"
    };
  }
  if (current.sourceTuple.revision !== sourceRevision)
    fail3("TASK_EVOLUTION_SOURCE_STALE", "CURRENT_TASK changed after preservation admission.");
  if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active") {
    fail3("TASK_EVOLUTION_STATE_INVALID", "Preservation initialization requires a confirmed active task.");
  }
  const basis = readCanonicalTaskBasis(root2, current);
  if (basis.revision !== basisRevision)
    fail3("TASK_EVOLUTION_BASIS_STALE", "Task Basis changed after preservation admission.");
  assertTestStrategySequenceReady(current);
  const idempotencyProposal = {
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "task-state-transaction",
    caller: "task-lifecycle",
    mode: "default",
    source_tuple: current.sourceTuple,
    semantic_delta: { kind: "task-state", action: "initialize-preservation", source_revision: sourceRevision, basis_revision: basisRevision },
    evidence_refs: [],
    idempotency_key: idempotencyKey,
    requested_write_targets: [current.relativePath]
  };
  const nextState = {
    ...current.runtimeState,
    task_evolution_version: 2,
    preservation_source_revision: current.sourceTuple.revision
  };
  const prepared = renderCanonicalCurrentTask(current.frontmatter, current.body, nextState);
  const nextContent = prepared.content;
  const preview = parseCanonicalCurrentTaskContent(nextContent, current.filePath, current.relativePath, prepared);
  if (preview.body !== current.body || digest3({ ...preview.runtimeState, task_evolution_version: undefined, preservation_source_revision: undefined }) !== digest3({ ...current.runtimeState, task_evolution_version: undefined, preservation_source_revision: undefined })) {
    fail3("TASK_EVOLUTION_INIT_INVALID", "Initialization must preserve the complete definition and runtime state.");
  }
  const history = taskHistoryLocation({
    currentPath: current.filePath,
    previousContent: current.raw,
    nextContent,
    documentId: current.sourceTuple.document_id,
    taskId: current.runtimeState.task_id,
    basisPath: basis.filePath,
    basisContent: basis.content,
    operation: "initialize-preservation",
    evidencePlanRevision: current.runtimeState.evidence_plan_revision
  });
  const plannedWrites = [path12.posix.join(path12.posix.dirname(current.relativePath), history.relativePath), current.relativePath];
  if (options.dryRun)
    return {
      status: "success",
      operation_kind: "task-state-transaction",
      idempotency_key: idempotencyKey,
      target_path: current.relativePath,
      dry_run: true,
      committed: false,
      message: "Preservation initialization is ready without changing the task definition.",
      planned_writes: plannedWrites,
      governed_mutation_count: 0,
      read_back_verified: false,
      evidence_assurance: "caller-reported"
    };
  let stagedAfter;
  try {
    stagedAfter = stageTaskEvolutionStoreCommit(root2, current, nextContent, nextState, idempotencyProposal, [
      { path: history.path, content: history.content },
      { path: current.filePath, content: nextContent }
    ], prepared);
  } catch (error) {
    return buildResult("blocked", idempotencyProposal, current, options, `task-store precommit staging failed: ${error instanceof Error ? error.message : String(error)}`, {
      code: error instanceof TaskStoreError ? error.code : "TASK_STORE_COMMIT_FAILED"
    });
  }
  try {
    commitTaskEvolutionWithHistory({
      currentPath: current.filePath,
      previousContent: current.raw,
      nextContent,
      documentId: current.sourceTuple.document_id,
      taskId: current.runtimeState.task_id,
      basisPath: basis.filePath,
      basisContent: basis.content,
      operation: "initialize-preservation",
      evidencePlanRevision: current.runtimeState.evidence_plan_revision
    }, (content) => {
      const parsed = parseCanonicalCurrentTaskContent(content, current.filePath, current.relativePath);
      if (parsed.body !== current.body || parsed.runtimeState.preservation_source_revision !== sourceRevision || parsed.runtimeState.task_evolution_version !== 2)
        fail3("TASK_EVOLUTION_INIT_INVALID", "Preservation initialization read-back differs.");
    });
  } catch (error) {
    if (fs11.existsSync(current.filePath) && sha2565(fs11.readFileSync(current.filePath, "utf8")) === current.sourceTuple.revision) {
      clearPendingTaskStoreAfterRollback(root2, current);
    }
    throw error;
  }
  const storeResult = {
    status: "success",
    committed: true,
    operation_kind: "task-state-transaction",
    idempotency_key: idempotencyKey,
    message: "Original task and Task Basis were preserved; versioned task protection is active."
  };
  try {
    const manifest = completeTaskEvolutionStoreCommit(root2, current, stagedAfter, idempotencyProposal, storeResult);
    const readBack = readCanonicalCurrentTask(root2);
    return {
      status: "success",
      operation_kind: "task-state-transaction",
      idempotency_key: idempotencyKey,
      target_path: current.relativePath,
      dry_run: false,
      committed: true,
      previous_revision: sourceRevision,
      resulting_revision: readBack.sourceTuple.revision,
      message: storeResult.message,
      planned_writes: plannedWrites,
      governed_mutation_count: plannedWrites.length,
      read_back_verified: true,
      evidence_assurance: "caller-reported",
      task_store: {
        manifest_path: `${manifest.storage_root}/manifest.json`,
        source_revision: manifest.head.source_revision,
        definition_revision: manifest.head.definition_revision,
        state_revision: manifest.head.state_revision,
        event_sequence: manifest.head.event_sequence
      }
    };
  } catch (error) {
    return {
      status: "blocked",
      operation_kind: "task-state-transaction",
      idempotency_key: idempotencyKey,
      target_path: current.relativePath,
      dry_run: false,
      committed: true,
      previous_revision: sourceRevision,
      resulting_revision: stagedAfter.sourceTuple.revision,
      message: `Preservation committed but task-store publication needs recovery: ${error instanceof Error ? error.message : String(error)}`,
      planned_writes: plannedWrites,
      governed_mutation_count: plannedWrites.length,
      read_back_verified: false,
      evidence_assurance: "caller-reported",
      code: error instanceof TaskStoreError ? error.code : "TASK_STORE_COMMIT_FAILED"
    };
  }
}
function normalizeCorrectionInput(input) {
  const value = expectRecord2(input, "prepare-replan input");
  const extensionKeys = ["mode", "strategy", "execution_targets", "recovery_steps", "pending_step_changes", "obligation_map", "restore_plan"];
  expectExactKeys2(value, [value.challenge_ids === undefined ? "challenge_id" : "challenge_ids", "correction_step", ...extensionKeys.filter((key) => (key in value))], "prepare-replan input");
  const mode = value.mode === undefined ? "conclusion-correction" : expectEnum(value.mode, ["conclusion-correction", "execution-recovery"], "recovery mode");
  const strategy = value.strategy === undefined ? "forward-fix" : expectEnum(value.strategy, ["forward-fix", "artifact-restore", "mixed"], "strategy");
  let restore = null;
  if (value.restore_plan != null) {
    const item = expectRecord2(value.restore_plan, "restore_plan");
    expectExactKeys2(item, ["checkpoint_id", "paths"], "restore_plan");
    restore = { checkpoint_id: expectString2(item.checkpoint_id, "checkpoint_id", /^[a-f0-9]{64}$/), paths: expectStringArray2(item.paths, "restore paths", false, 128).map((p) => normalizeRepoPath2(p, "restore path")) };
  }
  if (strategy === "forward-fix" !== (restore === null) || mode === "conclusion-correction" && strategy !== "forward-fix")
    fail3("RECOVERY_STRATEGY_UNSUPPORTED", "Restore strategies require execution-recovery mode and a verified restore plan.");
  const challengeIds = value.challenge_ids === undefined ? [expectString2(value.challenge_id, "challenge_id", SAFE_KEY_PATTERN2)] : expectStringArray2(value.challenge_ids, "challenge_ids", mode === "execution-recovery", 16).map((id) => expectString2(id, "challenge_id", SAFE_KEY_PATTERN2));
  if (new Set(challengeIds).size !== challengeIds.length)
    fail3("REPLAN_CHALLENGE_REQUIRED", "Challenge targets must be unique.");
  const executions = value.execution_targets ?? [];
  if (!Array.isArray(executions) || executions.length > 16)
    fail3("RECOVERY_TARGET_INVALID", "Execution targets must be bounded.");
  const executionTargets = executions.map((raw) => {
    const target = expectRecord2(raw, "execution target");
    expectExactKeys2(target, ["execution_id", "reason", "evidence_ref", "evidence_sha256"], "execution target");
    return {
      execution_id: expectString2(target.execution_id, "execution_id", SAFE_KEY_PATTERN2),
      reason: expectText(target.reason, "reason", 2048),
      evidence_ref: normalizeRepoPath2(expectString2(target.evidence_ref, "evidence_ref"), "evidence_ref"),
      evidence_sha256: expectString2(target.evidence_sha256, "evidence_sha256", /^[a-f0-9]{64}$/)
    };
  });
  if (new Set(executionTargets.map((item) => item.execution_id)).size !== executionTargets.length || mode === "conclusion-correction" && executionTargets.length || !challengeIds.length && !executionTargets.length)
    fail3("RECOVERY_TARGET_INVALID", "Recovery needs unique, correctly typed targets.");
  const recoverySteps = value.recovery_steps ?? [];
  if (!Array.isArray(recoverySteps) || recoverySteps.length > 15)
    fail3("RECOVERY_STEPS_INVALID", "At most 16 recovery steps are supported.");
  const obligations = value.obligation_map ?? [];
  if (!Array.isArray(obligations) || obligations.length > 128)
    fail3("RECOVERY_OBLIGATION_INVALID", "Obligation mapping must be bounded.");
  const obligationMap = obligations.map((raw) => {
    const item = expectRecord2(raw, "obligation");
    expectExactKeys2(item, ["claim_id", "slot_id", "due_step_id", ...item.before_step_id === undefined ? [] : ["before_step_id"], ...item.check === undefined ? [] : ["check", "replaces_check_id"]], "obligation");
    return {
      claim_id: expectString2(item.claim_id, "claim_id", CLAIM_ID_PATTERN),
      slot_id: expectString2(item.slot_id, "slot_id", CLAIM_EVIDENCE_SLOT_ID_PATTERN),
      due_step_id: expectString2(item.due_step_id, "due_step_id", STEP_ID_PATTERN2),
      ...item.before_step_id === undefined ? {} : { before_step_id: expectString2(item.before_step_id, "before_step_id", STEP_ID_PATTERN2) },
      ...item.check === undefined ? {} : { check: validateEvidenceCheck(item.check, "replacement check"), replaces_check_id: expectString2(item.replaces_check_id, "replaces_check_id", CLAIM_ID_PATTERN) }
    };
  });
  let pending = null;
  if (value.pending_step_changes != null) {
    const item = expectRecord2(value.pending_step_changes, "pending_step_changes");
    expectExactKeys2(item, ["steps", "step_map"], "pending_step_changes");
    if (!Array.isArray(item.steps) || item.steps.length > 32 || !Array.isArray(item.step_map) || item.step_map.length > 32)
      fail3("RECOVERY_PENDING_INVALID", "Pending plan and mapping must be bounded.");
    pending = { steps: item.steps.map(normalizeCorrectionStep), step_map: item.step_map.map((raw) => {
      const map = expectRecord2(raw, "step_map entry");
      expectExactKeys2(map, ["old_step_id", "new_step_ids"], "step_map entry");
      return { old_step_id: expectString2(map.old_step_id, "old_step_id", STEP_ID_PATTERN2), new_step_ids: expectStringArray2(map.new_step_ids, "new_step_ids", false, 32) };
    }) };
  }
  return {
    challenge_ids: [...challengeIds].sort(),
    correction_step: normalizeCorrectionStep(value.correction_step),
    mode,
    strategy,
    restore_plan: restore,
    execution_targets: executionTargets,
    recovery_steps: recoverySteps.map(normalizeCorrectionStep),
    pending_step_changes: pending,
    obligation_map: obligationMap
  };
}
function normalizeCorrectionStep(raw, options = {}) {
  const step = expectRecord2(raw, "correction_step");
  expectExactKeys2(step, ["id", "description", "mutation_scope", "required_evidence", "commands"], "correction_step");
  const id = expectString2(step.id, "correction_step.id", STEP_ID_PATTERN2);
  const description = expectText(step.description, "correction_step.description", 512);
  if (/[\r\n]/u.test(description))
    fail3("REPLAN_CORRECTION_INVALID", "Correction description must be one line.");
  const mutationScope = expectStringArray2(step.mutation_scope, "correction_step.mutation_scope", false, 32).map((item) => normalizeRepoPath2(item, "correction_step.mutation_scope"));
  if (mutationScope.some((item) => item.includes("*")) && !options.allowExistingWildcardScope)
    fail3("REPLAN_SCOPE_EXPANSION", "Correction writes require exact authorized non-executable paths.");
  const requiredEvidence = expectStringArray2(step.required_evidence, "correction_step.required_evidence", false, 32);
  if (requiredEvidence.some((item) => /[\r\n]/u.test(item)))
    fail3("REPLAN_CORRECTION_INVALID", "Required evidence must use one line per item.");
  if (!Array.isArray(step.commands) || step.commands.length > 32)
    fail3("REPLAN_CORRECTION_INVALID", "Correction commands must be a bounded array.");
  const commands = step.commands.map((raw, index) => {
    const command = expectRecord2(raw, `correction_step.commands[${index}]`);
    expectExactKeys2(command, ["command", "expected_repo_writes"], `correction_step.commands[${index}]`);
    const text = expectText(command.command, `correction_step.commands[${index}].command`, 2048);
    if (/[\r\n]/u.test(text))
      fail3("REPLAN_CORRECTION_INVALID", "Command must be one line.");
    const writes = command.expected_repo_writes === "none" ? "none" : expectStringArray2(command.expected_repo_writes, `correction_step.commands[${index}].expected_repo_writes`, false, 32).map((item) => normalizeRepoPath2(item, "expected_repo_writes"));
    if (writes !== "none" && writes.some((item) => !mutationScope.includes(item) && !(options.allowExistingWildcardScope && mutationScope.some((pattern) => pattern.includes("*") && mutationScopePatternMatchesPath(item, pattern))))) {
      fail3("REPLAN_SCOPE_EXPANSION", "Command writes must be exact members of the correction step scope.");
    }
    return { command: text, expected_repo_writes: writes };
  });
  return { id, description, mutation_scope: mutationScope, required_evidence: requiredEvidence, commands };
}
function validateEvidenceCheck(value, location) {
  return validateClaimEvidence([{ claim_id: "replacement", claim_kind: "acceptance", slots: [{
    slot_id: "replacement",
    minimum_type: "focused-test",
    disposition: "missing",
    evidence_refs: [],
    check: value
  }] }], location)[0].slots[0].check;
}
function executedStepIds(current) {
  return new Set([
    ...current.runtimeState.execution_log.flatMap((item) => ("step_id" in item) ? [item.step_id] : []),
    ...Object.entries(current.runtimeState.step_attempts ?? {}).filter(([, ledger]) => ledger.attempts.length).map(([id]) => id)
  ]);
}
function revisePendingSteps(current, definition, revision, recoverySteps) {
  const executed = executedStepIds(current);
  const oldSteps = parseImplementationSteps(definition.implementation_steps);
  const pendingIds = oldSteps.filter((step) => !executed.has(step.id)).map((step) => step.id);
  const obligations = [...pendingIds];
  if (current.runtimeState.workflow_status === "blocked_by_replan" && current.runtimeState.active_step_status !== "completed" && !obligations.includes(current.runtimeState.active_step_id))
    obligations.push(current.runtimeState.active_step_id);
  if (digest3(obligations.sort()) !== digest3(revision.step_map.map((item) => item.old_step_id).sort()))
    fail3("RECOVERY_OBLIGATION_INVALID", "Every never-executed step and suspended unfinished attempt requires exactly one replacement mapping.");
  const nextIds = new Set([...recoverySteps, ...revision.steps].map((step) => step.id));
  if (revision.step_map.some((item) => item.new_step_ids.some((id) => !nextIds.has(id))))
    fail3("RECOVERY_OBLIGATION_INVALID", "Step obligation maps to a missing execution step.");
  if (revision.steps.some((step) => oldSteps.some((old) => old.id === step.id)))
    fail3("RECOVERY_STEP_ID_REUSED", "Changed definitions require new step IDs; executed definitions cannot be overwritten.");
  const blocks = definition.implementation_steps.split(/(?=^-\s*[A-Za-z0-9][A-Za-z0-9._:-]*\s*[:：])/m);
  const keepBlocks = blocks.filter((block) => !pendingIds.some((id) => block.startsWith(`- ${id}:`) || block.startsWith(`- ${id}：`)));
  const keptPlan = definition.implementation_plan.split(`
`).filter((line) => !pendingIds.some((id) => line.startsWith(`- ${id}:`)));
  if (!oldSteps.some((step) => executed.has(step.id)))
    fail3("RECOVERY_HISTORY_REQUIRED", "Pending-plan recovery requires retained execution history.");
  let next = { ...definition, implementation_steps: keepBlocks.join(""), implementation_plan: keptPlan.join(`
`) };
  for (const step of revision.steps)
    next = insertCorrectionStep(next, parseImplementationSteps(next.implementation_steps).at(-1).id, step, true);
  return next;
}
function correctionCandidateLocation(current, candidateDigest) {
  if (!/^[a-f0-9]{64}$/u.test(candidateDigest))
    fail3("REPLAN_CANDIDATE_INVALID", "Candidate digest must be SHA-256.");
  const directory = path12.join(path12.dirname(current.filePath), "task-candidates", current.sourceTuple.document_id);
  const filePath = path12.join(directory, `${candidateDigest}.json`);
  return { filePath, relativePath: path12.posix.join(path12.posix.dirname(current.relativePath), "task-candidates", current.sourceTuple.document_id, `${candidateDigest}.json`) };
}
function pendingCorrectionCandidates(root2, current) {
  const directory = path12.join(path12.dirname(current.filePath), "task-candidates", current.sourceTuple.document_id);
  if (!fs11.existsSync(directory))
    return [];
  const names = fs11.readdirSync(directory).filter((item) => /^[a-f0-9]{64}\.json$/u.test(item));
  if (names.length > 128)
    fail3("REPLAN_CANDIDATE_BUDGET_EXHAUSTED", "Task has too many retained candidates to summarize safely.");
  return names.flatMap((name) => {
    const candidateDigest = name.slice(0, -5);
    const location = correctionCandidateLocation(current, candidateDigest);
    if (fs11.existsSync(`${location.filePath}.discarded`))
      return [];
    try {
      const value = JSON.parse(fs11.readFileSync(location.filePath, "utf8"));
      if (!taskSourceRevisionMatches(root2, current, value.source_revision) || value.document_id !== current.sourceTuple.document_id)
        return [];
      const { candidate_digest: _marker, ...payload } = value;
      return [{
        candidate_digest: candidateDigest,
        candidate_path: location.relativePath,
        valid: value.candidate_digest === candidateDigest && digest3(payload) === candidateDigest
      }];
    } catch {
      return [{ candidate_digest: candidateDigest, candidate_path: location.relativePath, valid: false }];
    }
  });
}
function unconfirmedCorrectionCandidatePaths(current) {
  const directory = path12.join(path12.dirname(current.filePath), "task-candidates", current.sourceTuple.document_id);
  if (!fs11.existsSync(directory))
    return [];
  const committed = new Set(current.runtimeState.execution_log.flatMap((entry) => ("action" in entry) && entry.action === "commit-replan" && entry.candidate_digest ? [`${entry.candidate_digest}.json`] : []));
  return fs11.readdirSync(directory).filter((name) => /^[a-f0-9]{64}\.json$/u.test(name) && !name.endsWith(".scope.json")).filter((name) => !committed.has(name) && !fs11.existsSync(path12.join(directory, `${name}.discarded`))).map((name) => path12.posix.join(path12.posix.dirname(current.relativePath), "task-candidates", current.sourceTuple.document_id, name));
}
function pendingScopeAmendmentCandidates(current) {
  const directory = path12.join(path12.dirname(current.filePath), "task-candidates", current.sourceTuple.document_id);
  if (!fs11.existsSync(directory))
    return [];
  const names = fs11.readdirSync(directory).filter((item) => /^[a-f0-9]{64}\.scope\.json$/u.test(item));
  if (names.length > 64)
    fail3("SCOPE_AMENDMENT_CANDIDATE_BUDGET_EXHAUSTED", "Task has too many retained scope-amendment candidates to summarize safely.");
  const committed = new Set(current.runtimeState.execution_log.flatMap((entry) => ("action" in entry) && entry.action === "commit-scope-amendment" && entry.candidate_digest ? [`${entry.candidate_digest}.scope.json`] : []));
  return names.flatMap((name) => {
    if (committed.has(name) || fs11.existsSync(path12.join(directory, `${name}.discarded`)))
      return [];
    const candidateDigest = name.slice(0, -".scope.json".length);
    const location = scopeAmendmentCandidateLocation(current, candidateDigest);
    try {
      const value = JSON.parse(fs11.readFileSync(location.filePath, "utf8"));
      const { candidate_digest: _marker, ...payload } = value;
      return [{ candidate_digest: candidateDigest, candidate_path: location.relativePath, valid: value.candidate_digest === candidateDigest && digest3(payload) === candidateDigest }];
    } catch {
      return [{ candidate_digest: candidateDigest, candidate_path: location.relativePath, valid: false }];
    }
  });
}
function correctionObligations(current) {
  const definition = readDraftDefinitionFromBody(current.body);
  const steps = parseImplementationSteps(definition.implementation_steps);
  return {
    task_id: current.runtimeState.task_id,
    document_id: current.sourceTuple.document_id,
    goal: definition.background_context,
    acceptance: definition.acceptance,
    scope: [definition.allowed_scope, definition.conditional_scope, definition.forbidden_scope],
    steps: steps.map((step) => ({ id: step.id, description: step.description, purpose: step.purpose, mutation_scope: step.mutation_scope, required_evidence: step.required_evidence, review_checkpoint: step.review_checkpoint, checkpoint_boundary: step.checkpoint_boundary })),
    claims: (current.runtimeState.claim_evidence ?? []).map((record) => ({ claim_id: record.claim_id, claim_kind: record.claim_kind, requirement: record.requirement, source_ref: record.source_ref, slots: record.slots.map((slot) => ({
      slot_id: slot.slot_id,
      minimum_type: slot.minimum_type,
      due_step_id: slot.due_step_id,
      applicability: slot.applicability,
      before_step_id: slot.before_step_id,
      check: slot.check,
      disposition: slot.disposition,
      evidence_refs: slot.evidence_refs,
      report_result_id: slot.report?.result_id ?? null,
      report_status: slot.report?.status ?? null,
      report_sha256: slot.report ? digest3(slot.report) : null
    })) })),
    findings: current.runtimeState.findings.map((item) => ({ fingerprint: item.fingerprint, status: item.status }))
  };
}
function insertCorrectionStep(definition, activeStepId, step, append = false) {
  const oldSteps = parseImplementationSteps(definition.implementation_steps);
  if (oldSteps.some((item) => item.id === step.id))
    fail3("REPLAN_CORRECTION_INVALID", "Correction step ID already exists.");
  const activeIndex = oldSteps.findIndex((item) => item.id === activeStepId);
  if (activeIndex < 0)
    fail3("REPLAN_CORRECTION_INVALID", "A correction requires an existing active step.");
  const rendered = [
    `- ${step.id}: ${step.description}`,
    `  - purpose: Correct the challenged result ${step.description}`,
    `  - mutation_scope: ${step.mutation_scope.join(", ")}`,
    `  - required_evidence: ${step.required_evidence.join("; ")}`,
    "  - review_checkpoint: required: fresh correction review of the affected audit documents",
    ...step.commands.flatMap((item) => [
      `  - planned_command: ${item.command}`,
      `    - expected_repo_writes: ${item.expected_repo_writes === "none" ? "none" : item.expected_repo_writes.join(", ")}`,
      "    - transformation_kind: localized"
    ])
  ].join(`
`);
  const lines = definition.implementation_steps.split(`
`);
  const insertion = lines.findIndex((line) => new RegExp(`^-\\s*${activeStepId.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")}\\s*[:：]`).test(line));
  if (insertion < 0)
    fail3("REPLAN_CORRECTION_INVALID", "The current step must have one explicit top-level definition line.");
  lines.splice(append ? lines.length : insertion, 0, rendered);
  const planLines = definition.implementation_plan.split(`
`);
  const planIndex = planLines.findIndex((line) => line.startsWith(`- ${activeStepId}:`));
  if (planIndex < 0)
    fail3("REPLAN_CORRECTION_INVALID", "The current step must be present in the implementation plan.");
  planLines.splice(append ? planLines.length : planIndex, 0, `- ${step.id}: ${step.description}`);
  const next = { ...definition, implementation_steps: lines.join(`
`), implementation_plan: planLines.join(`
`) };
  assertReplacementActiveStep(step.id, next.implementation_steps);
  return next;
}
function normalizeScopeAmendmentInput(input, options = {}) {
  const value = expectRecord2(input, "prepare-scope-amendment input");
  const stepKey = value.amendment_step !== undefined ? "amendment_step" : value.step !== undefined ? "step" : null;
  expectExactKeys2(value, [
    "added_paths",
    ...value.persistent_test_paths === undefined ? [] : ["persistent_test_paths"],
    ...value.persistent_test_admissions === undefined ? [] : ["persistent_test_admissions"],
    ...value.authorization === undefined ? [] : ["authorization"],
    ...stepKey ? [stepKey] : []
  ], "prepare-scope-amendment input");
  if (!stepKey)
    fail3("SCOPE_AMENDMENT_STEP_REQUIRED", "prepare-scope-amendment requires an amendment step definition.");
  const addedPaths = expectStringArray2(value.added_paths, "added_paths", false, 64).map((item) => normalizeRepoPath2(item, "added_paths"));
  if (new Set(addedPaths).size !== addedPaths.length || addedPaths.some((item) => item.includes("*")))
    fail3("SCOPE_AMENDMENT_SCOPE_INVALID", "added_paths must contain unique exact paths and cannot introduce wildcards.");
  const persistentTestPaths = value.persistent_test_paths !== undefined ? expectStringArray2(value.persistent_test_paths, "persistent_test_paths", true, 64).map((item) => normalizeRepoPath2(item, "persistent_test_paths")) : undefined;
  if ((persistentTestPaths ?? []).some((item) => item.includes("*") || !addedPaths.includes(item)))
    fail3("SCOPE_AMENDMENT_SCOPE_INVALID", "persistent_test_paths must be exact paths contained in added_paths.");
  const hasPersistentTestAdmissions = value.persistent_test_admissions !== undefined;
  let persistentTestAdmissions;
  if (hasPersistentTestAdmissions) {
    if (!Array.isArray(value.persistent_test_admissions) || value.persistent_test_admissions.length > 64) {
      persistentTestAdmissionError("persistent_test_admissions", "must be a bounded array.");
    }
    persistentTestAdmissions = value.persistent_test_admissions.map((item, index) => normalizePersistentTestAdmission(item, index));
    if (new Set(persistentTestAdmissions.map((item) => item.path)).size !== persistentTestAdmissions.length) {
      persistentTestAdmissionError("persistent_test_admissions", "must contain one admission per exact path.");
    }
    const missingAdmissionPaths = persistentTestAdmissions.map((item) => item.path).filter((item) => !addedPaths.includes(item));
    if (missingAdmissionPaths.length > 0) {
      persistentTestAdmissionError("persistent_test_admissions", `path(s) must be contained in added_paths: ${missingAdmissionPaths.join(", ")}.`);
    }
  }
  if (persistentTestPaths && persistentTestPaths.length > 0 && persistentTestAdmissions && digest3([...persistentTestPaths].sort()) !== digest3(persistentTestAdmissions.map((item) => item.path).sort())) {
    fail3("PERSISTENT_TEST_ADMISSION_INVALID", "persistent_test_paths must exactly match persistent_test_admissions paths when both are supplied.");
  }
  let authorization = null;
  if (value.authorization !== undefined && value.authorization !== null) {
    const authorizationValue = expectRecord2(value.authorization, "authorization");
    expectExactKeys2(authorizationValue, ["decision_source", "decision_text", "authorized_paths"], "authorization");
    if (!Array.isArray(authorizationValue.authorized_paths)) {
      fail3("SCOPE_AMENDMENT_AUTHORIZATION_REQUIRED", `Existing explicit user authorization is required for exact path(s): ${addedPaths.join(", ")}.`);
    }
    const authorizedPaths = expectStringArray2(authorizationValue.authorized_paths, "authorization.authorized_paths", false, 128).map((item) => normalizeRepoPath2(item, "authorization.authorized_paths"));
    const missingAuthorizedPaths = addedPaths.filter((item) => !authorizedPaths.includes(item));
    if (new Set(authorizedPaths).size !== authorizedPaths.length || authorizedPaths.some((item) => item.includes("*")) || missingAuthorizedPaths.length > 0) {
      fail3("SCOPE_AMENDMENT_AUTHORIZATION_REQUIRED", `Existing explicit user authorization is missing for exact path(s): ${missingAuthorizedPaths.join(", ") || addedPaths.join(", ")}.`);
    }
    const decisionSource = expectText(authorizationValue.decision_source, "authorization.decision_source", 512);
    if (/[\r\n]/u.test(decisionSource))
      fail3("SCOPE_AMENDMENT_AUTHORIZATION_INVALID", "authorization.decision_source must be one line.");
    const decisionText = expectVerbatim(authorizationValue.decision_text, "authorization.decision_text", 4096);
    authorization = { decision_source: decisionSource, decision_text: decisionText, authorized_paths: [...authorizedPaths].sort() };
  }
  return {
    added_paths: [...addedPaths].sort(),
    ...persistentTestPaths === undefined ? {} : { persistent_test_paths: [...new Set(persistentTestPaths)].sort() },
    ...persistentTestAdmissions === undefined ? {} : { persistent_test_admissions: persistentTestAdmissions.sort((left, right) => left.path < right.path ? -1 : left.path > right.path ? 1 : 0) },
    authorization,
    amendment_step: normalizeCorrectionStep(value[stepKey], options)
  };
}
function scopeAmendmentCandidateLocation(current, candidateDigest) {
  if (!SHA256_PATTERN2.test(candidateDigest))
    fail3("SCOPE_AMENDMENT_CANDIDATE_INVALID", "Scope amendment candidate digest must be SHA-256.");
  const directory = path12.join(path12.dirname(current.filePath), "task-candidates", current.sourceTuple.document_id);
  const filePath = path12.join(directory, `${candidateDigest}.scope.json`);
  return { filePath, relativePath: path12.posix.join(path12.posix.dirname(current.relativePath), "task-candidates", current.sourceTuple.document_id, `${candidateDigest}.scope.json`) };
}
function assertScopeAmendmentHistoryForRevision(current, sourceRevision) {
  try {
    assertTaskHistoryForRevision(current.filePath, current.sourceTuple.document_id, current.runtimeState.task_id, sourceRevision, "commit-scope-amendment");
  } catch (error) {
    try {
      assertTaskHistoryForRevision(current.filePath, current.sourceTuple.document_id, current.runtimeState.task_id, sourceRevision, "confirm-scope-amendment");
    } catch {
      throw error;
    }
  }
}
function scopeAmendmentCandidateReceipt(candidate, candidateDigest) {
  return {
    kind: "scope-amendment-candidate-receipt/v1",
    candidate_digest: candidateDigest,
    source_revision: candidate.source_revision,
    basis_revision: candidate.basis_revision,
    old_plan_revision: candidate.old_plan_revision,
    new_plan_revision: candidate.new_plan_revision,
    obligations_digest: candidate.old_obligations_digest,
    authorization_digest: candidate.authorization?.digest ?? digest3(null),
    added_paths: [...candidate.input.added_paths],
    permission_change: "additive-scope",
    ...candidate.authority_diff ? { authority_diff: structuredClone(candidate.authority_diff) } : {},
    ...candidate.persistent_test_admission ? { persistent_test_admission: { added_paths: candidate.persistent_test_admission.added.map((item) => item.path) } } : {}
  };
}
function scopeAmendmentRequestMatchesCandidate(input, candidate) {
  const requestedStep = input.amendment_step;
  const committedStep = candidate.input.amendment_step;
  return digest3({
    added_paths: input.added_paths,
    persistent_test_paths: input.persistent_test_paths ?? null,
    persistent_test_admissions: input.persistent_test_admissions ?? null,
    authorization: input.authorization
  }) === digest3({
    added_paths: candidate.input.added_paths,
    persistent_test_paths: candidate.input.persistent_test_paths ?? null,
    persistent_test_admissions: candidate.input.persistent_test_admissions ?? null,
    authorization: candidate.input.authorization
  }) && requestedStep.id === committedStep.id && requestedStep.description === committedStep.description && (requestedStep.required_evidence.length === 0 || digest3(requestedStep.required_evidence) === digest3(committedStep.required_evidence)) && (requestedStep.commands.length === 0 || digest3(requestedStep.commands) === digest3(committedStep.commands)) && requestedStep.mutation_scope.every((item) => committedStep.mutation_scope.includes(item));
}
function appendExactScopePaths(value, paths) {
  const existing = value.replace(/\r\n?/gu, `
`).trim();
  const existingPaths = new Set;
  for (const line of existing.split(`
`)) {
    const match = /^\s*[-*]\s+`?([^`]+?)`?\s*$/u.exec(line);
    if (match && match[1] !== "none")
      existingPaths.add(match[1].trim());
  }
  const additions = paths.filter((item) => !existingPaths.has(item)).map((item) => `- \`${item}\``);
  if (additions.length === 0)
    return existing;
  if (!existing || /^none\s*$/iu.test(existing))
    return additions.join(`
`);
  return `${existing}
${additions.join(`
`)}`;
}
function persistentTestAdmissionError(location, message) {
  fail3("PERSISTENT_TEST_ADMISSION_INVALID", `${location} ${message}`);
}
function persistentTestAdmissionText(value, location, maxLength = 4096) {
  try {
    return expectText(value, location, maxLength);
  } catch (error) {
    persistentTestAdmissionError(location, error instanceof Error ? error.message : "must be explicit and non-empty.");
  }
}
function persistentTestAdmissionStringArray(value, location) {
  try {
    return expectStringArray2(value, location, false, 64);
  } catch (error) {
    persistentTestAdmissionError(location, error instanceof Error ? error.message : "must be a non-empty bounded array.");
  }
}
function normalizePersistentTestAdmission(value, index) {
  const location = `persistent_test_admissions[${index}]`;
  const source = expectRecord2(value, location);
  const required = ["path", "proves", "owner", "owner_source", "source_ref", "basis", "existing_evidence_insufficiency", "assertion_boundary", "failure_disposition"];
  const missing = required.filter((key) => !(key in source));
  const extra = Object.keys(source).filter((key) => !required.includes(key));
  if (missing.length > 0 || extra.length > 0) {
    persistentTestAdmissionError(location, `keys mismatch; missing=[${missing.join(", ")}], unexpected=[${extra.join(", ")}].`);
  }
  let targetPath;
  try {
    targetPath = normalizeRepoPath2(source.path, `${location}.path`);
  } catch (error) {
    persistentTestAdmissionError(`${location}.path`, error instanceof Error ? error.message : "must be an exact repository-relative path.");
  }
  if (targetPath.includes("*"))
    persistentTestAdmissionError(`${location}.path`, "must be an exact path.");
  const proves = persistentTestAdmissionStringArray(source.proves, `${location}.proves`);
  const basis = source.basis;
  if (!["acceptance", "regression", "critical-invariant", "critical-risk"].includes(String(basis))) {
    persistentTestAdmissionError(`${location}.basis`, "must be acceptance, regression, critical-invariant, or critical-risk.");
  }
  return {
    path: targetPath,
    proves,
    owner: persistentTestAdmissionText(source.owner, `${location}.owner`),
    owner_source: persistentTestAdmissionText(source.owner_source, `${location}.owner_source`),
    source_ref: persistentTestAdmissionText(source.source_ref, `${location}.source_ref`),
    basis,
    existing_evidence_insufficiency: persistentTestAdmissionText(source.existing_evidence_insufficiency, `${location}.existing_evidence_insufficiency`),
    assertion_boundary: persistentTestAdmissionText(source.assertion_boundary, `${location}.assertion_boundary`),
    failure_disposition: persistentTestAdmissionText(source.failure_disposition, `${location}.failure_disposition`)
  };
}
function appendPersistentTestAdmissions(regressionChecks, admissions) {
  if (admissions.length === 0)
    return regressionChecks;
  const normalized = regressionChecks.replace(/\r\n?/gu, `
`);
  const heading = /(^###\s+(?:Persistent Tests|持久化测试)\s*$)/imu.exec(normalized);
  const render = (item) => [
    `- \`${item.path}\``,
    ...item.proves.map((proof) => `  - proves: ${proof}`),
    `  - owner: ${item.owner}`,
    `  - owner_source: ${item.owner_source}`,
    `  - source_ref: ${item.source_ref}`,
    `  - basis: ${item.basis}`,
    `  - existing_evidence_insufficiency: ${item.existing_evidence_insufficiency}`,
    `  - assertion_boundary: ${item.assertion_boundary}`,
    `  - failure_disposition: ${item.failure_disposition}`
  ];
  const additions = admissions.flatMap(render);
  if (!heading || heading.index === undefined)
    return `${normalized.trimEnd()}

### Persistent Tests

${additions.join(`
`)}
`;
  const start = heading.index + heading[0].length;
  const nextHeading = /\n###\s+/gu.exec(normalized.slice(start));
  const end = nextHeading ? start + nextHeading.index : normalized.length;
  const section = normalized.slice(start, end).trim();
  const existing = new Set;
  for (const line of section.split(`
`)) {
    const match = /^\s*[-*]\s+`?([^`]+?)`?\s*$/u.exec(line);
    if (match && match[1] !== "none")
      existing.add(match[1].trim());
  }
  const newAdmissions = admissions.filter((item) => !existing.has(item.path));
  if (newAdmissions.length === 0)
    return normalized;
  const retained = section.replace(/^\s*-\s+none\s*$/imu, "").trim();
  const blocks = newAdmissions.flatMap(render);
  const replacement = `

${[retained, ...blocks].filter(Boolean).join(`
`)}
`;
  return normalized.slice(0, start) + replacement + normalized.slice(end);
}
function appendLegacyPersistentTestPaths(regressionChecks, paths, claimIds = ["A1"]) {
  if (paths.length === 0)
    return regressionChecks;
  const normalized = regressionChecks.replace(/\r\n?/gu, `
`);
  const heading = /(^###\s+(?:Persistent Tests|持久化测试)\s*$)/imu.exec(normalized);
  const proof = claimIds[0] ?? "A1";
  const render = (item) => [
    `- \`${item}\``,
    `  - proves: ${proof}`,
    "  - owner: workflow-system",
    "  - owner_source: task-basis",
    "  - source_ref: task-basis",
    "  - basis: regression",
    "  - existing_evidence_insufficiency: The existing review did not cover the explicitly authorized path.",
    "  - assertion_boundary: The new path is covered by the amended step and its fresh review.",
    "  - failure_disposition: block"
  ];
  const append = (items) => items.flatMap(render);
  if (!heading || heading.index === undefined)
    return `${normalized.trimEnd()}

### Persistent Tests

${append(paths).join(`
`)}
`;
  const start = heading.index + heading[0].length;
  const nextHeading = /\n###\s+/gu.exec(normalized.slice(start));
  const end = nextHeading ? start + nextHeading.index : normalized.length;
  const section = normalized.slice(start, end).trim();
  const existing = new Set;
  for (const line of section.split(`
`)) {
    const match = /^\s*[-*]\s+`?([^`]+?)`?\s*$/u.exec(line);
    if (match && match[1] !== "none")
      existing.add(match[1].trim());
  }
  const newPaths = paths.filter((item) => !existing.has(item));
  if (newPaths.length === 0)
    return normalized;
  const retained = section.replace(/^\s*-\s+none\s*$/imu, "").trim();
  const replacement = `

${[retained, ...append(newPaths)].filter(Boolean).join(`
`)}
`;
  return normalized.slice(0, start) + replacement + normalized.slice(end);
}
function replaceScopeAmendmentStep(definition, previousStepId, step, v2 = false) {
  const oldSteps = parseImplementationSteps(definition.implementation_steps);
  if (oldSteps.some((item) => item.id === step.id))
    fail3("SCOPE_AMENDMENT_STEP_INVALID", "Scope amendment step ID already exists.");
  const lines = definition.implementation_steps.replace(/\r\n?/gu, `
`).split(`
`);
  const topLevel = new RegExp(`^-\\s*${previousStepId.replace(/[.*+?^${}()|[\\]\\\\]/g, "\\$&")}\\s*[:：]`);
  const start = lines.findIndex((line) => topLevel.test(line));
  if (start < 0)
    fail3("SCOPE_AMENDMENT_STEP_INVALID", "The previous active step must have an explicit top-level definition line.");
  let end = lines.length;
  for (let index = start + 1;index < lines.length; index += 1) {
    if (/^-\s*[A-Za-z0-9][A-Za-z0-9._:-]{0,127}\s*[:：]/u.test(lines[index])) {
      end = index;
      break;
    }
  }
  const rendered = [
    `- ${step.id}: ${step.description}`,
    `  - purpose: Continue the explicitly authorized scope amendment ${step.description}`,
    `  - ${v2 ? "planned_mutation_targets" : "mutation_scope"}: ${step.mutation_scope.join(", ")}`,
    `  - required_evidence: ${step.required_evidence.join("; ")}`,
    "  - review_checkpoint: required: fresh review of the amended scope and retained findings",
    ...step.commands.flatMap((item) => [
      `  - planned_command: ${item.command}`,
      `    - expected_repo_writes: ${item.expected_repo_writes === "none" ? "none" : item.expected_repo_writes.join(", ")}`,
      "    - transformation_kind: localized"
    ])
  ];
  lines.splice(end, 0, ...rendered);
  const planLines = definition.implementation_plan.replace(/\r\n?/gu, `
`).split(`
`);
  const planIndex = planLines.findIndex((line) => line.startsWith(`- ${previousStepId}:`));
  if (planIndex < 0)
    fail3("SCOPE_AMENDMENT_STEP_INVALID", "The previous active step must be present in the implementation plan.");
  planLines.splice(planIndex + 1, 0, `- ${step.id}: ${step.description}`);
  const next = { ...definition, implementation_steps: lines.join(`
`), implementation_plan: planLines.join(`
`) };
  assertReplacementActiveStep(step.id, next.implementation_steps);
  return next;
}
function assertScopeAmendmentPathSafe(root2, current, paths) {
  const legacyScope = current.mutationAuthority ? null : parseMutationScope(current.body);
  for (const file of paths) {
    if (file === current.relativePath || file.startsWith(".workflow-system/") || file.startsWith(".git/") || file.startsWith(".agents/") || file.startsWith(".claude/") || file.startsWith(".codex/") || file.startsWith("runtime/vnext/dist/") || file.startsWith("packages/vibe-governance/") || file.startsWith("node_modules/") || file.includes("/task-history/") || file.includes("/task-candidates/") || file.includes("/task-data/")) {
      fail3("SCOPE_AMENDMENT_SCOPE_INVALID", `Scope amendment cannot authorize governance/runtime path ${file}.`);
    }
    const forbidden = current.mutationAuthority ? current.mutationAuthority.forbidden.some((pattern) => mutationScopePatternMatchesPath(file, pattern)) : legacyScope.forbidden.some((entry) => mutationScopePatternMatchesPath(file, entry.pattern));
    if (forbidden) {
      fail3("SCOPE_AMENDMENT_SCOPE_INVALID", `Scope amendment path ${file} is already forbidden.`);
    }
    const absolute = path12.resolve(root2, ...file.split("/"));
    const relative = path12.relative(path12.resolve(root2), absolute).replace(/\\/g, "/");
    if (relative !== file || relative.startsWith("../") || path12.isAbsolute(relative))
      fail3("SCOPE_AMENDMENT_SCOPE_INVALID", `Scope amendment path escapes the repository: ${file}.`);
    if (fs11.existsSync(absolute)) {
      const stat = fs11.lstatSync(absolute);
      if (stat.isSymbolicLink() || !stat.isFile())
        fail3("SCOPE_AMENDMENT_SCOPE_INVALID", `Scope amendment path must be a regular file: ${file}.`);
      const header = fs11.readFileSync(absolute, "utf8").slice(0, 4096);
      if (/@frozen|DO NOT MODIFY/iu.test(header))
        fail3("SCOPE_AMENDMENT_SCOPE_INVALID", `Scope amendment path is frozen: ${file}.`);
    }
  }
}
function executionResultRecordedForPreflight(current, preflight, attemptId) {
  return current.runtimeState.execution_log.some((item) => {
    if ("action" in item || item.step_id !== preflight.step_id || item.mode !== preflight.mode)
      return false;
    const result = item.execution_result;
    return result !== undefined && result.execution_id === preflight.execution_id && result.change_set_id === preflight.change_set_id && (attemptId === undefined || result.attempt_id === attemptId);
  });
}
function assertV2AuthorityAmendmentExecutionSettled(current) {
  if (!current.mutationAuthority)
    return;
  const activePreflight = current.runtimeState.execution_preflight;
  if (activePreflight && activePreflight.step_id === current.runtimeState.active_step_id && !executionResultRecordedForPreflight(current, activePreflight, activePreflight.mode === "default" ? current.runtimeState.step_attempts?.[current.runtimeState.active_step_id]?.attempts.at(-1)?.attempt_id : undefined)) {
    fail3("SCOPE_AMENDMENT_EXECUTION_GATE", "A v2 authority amendment requires the current preflighted execution to have a matching recorded result first.");
  }
  const ledger = current.runtimeState.step_attempts?.[current.runtimeState.active_step_id];
  const latestAttempt = ledger?.attempts.at(-1);
  if (latestAttempt?.status !== "preflighted")
    return;
  const recorded = activePreflight && activePreflight.step_id === current.runtimeState.active_step_id && activePreflight.mode === "default" && executionResultRecordedForPreflight(current, activePreflight, latestAttempt.attempt_id);
  if (!recorded) {
    fail3("SCOPE_AMENDMENT_EXECUTION_GATE", "A v2 authority amendment requires the latest preflighted attempt to have a matching recorded result first.");
  }
}
function buildScopeAmendmentCandidate(root2, current, input) {
  assertV2AuthorityDomainRevisionFreshForTransition(root2, current, "Scope amendment");
  if (!["active", "blocked_by_replan"].includes(current.runtimeState.workflow_status) || current.runtimeState.lifecycle_state !== "active") {
    fail3("SCOPE_AMENDMENT_STATE_INVALID", "Scope amendment requires an active or blocked_by_replan task with an active lifecycle.");
  }
  if (current.runtimeState.task_evolution_version !== 2) {
    fail3("TASK_EVOLUTION_INITIALIZATION_REQUIRED", "Initialize v2 task preservation before preparing a scope amendment.");
  }
  if (current.runtimeState.resume_requires_review)
    fail3("SCOPE_AMENDMENT_STATE_INVALID", "Scope amendment cannot consume a task with an uncleared resume-review gate.");
  if (!["ready", "in-progress", "completed", "blocked"].includes(current.runtimeState.active_step_status))
    fail3("SCOPE_AMENDMENT_STATE_INVALID", "Scope amendment requires a usable current step.");
  assertBusinessEvidenceVersion(current);
  assertTestStrategySequenceReady(current);
  assertScopeAmendmentPathSafe(root2, current, input.added_paths);
  const legacyPersistentTestPaths = input.persistent_test_paths ?? [];
  const persistentTestAdmissions = input.persistent_test_admissions ?? [];
  if (legacyPersistentTestPaths.some((item) => !isLikelyPersistentTestPath(item))) {
    fail3("SCOPE_AMENDMENT_SCOPE_INVALID", "persistent_test_paths must identify test-like paths.");
  }
  let authorityExpansionPaths = [];
  if (current.mutationAuthority) {
    let projectAuthority;
    try {
      projectAuthority = readProjectMutationAuthority(root2);
    } catch (error) {
      fail3(error instanceof MutationAuthorityError ? error.code : "MUTATION_AUTHORITY_PROJECT_INVALID", error instanceof Error ? error.message : String(error));
    }
    if (!projectAuthority)
      fail3("MUTATION_AUTHORITY_PROJECT_REQUIRED", "v2 scope amendment requires PROJECT_PROFILE.yaml.mutation_authority.domains.");
    authorityExpansionPaths = input.added_paths.filter((target) => {
      const domain = authorityDomainForPath(projectAuthority, target);
      return !current.mutationAuthority.exact_exceptions.includes(target) && (domain === null || !current.mutationAuthority.domains.includes(domain));
    });
    const admissionPaths = persistentTestAdmissions.map((item) => item.path);
    for (const admission of persistentTestAdmissions) {
      const absolute = path12.resolve(root2, ...admission.path.split("/"));
      if (fs11.existsSync(absolute)) {
        fail3("PERSISTENT_TEST_ADMISSION_INVALID", `persistent test ${admission.path} already exists; only an absent new persistent test needs P-12 admission.`);
      }
    }
    const missingPersistentAdmissions = input.added_paths.filter((target) => {
      if (!isLikelyPersistentTestPath(target) || admissionPaths.includes(target))
        return false;
      return !fs11.existsSync(path12.resolve(root2, ...target.split("/")));
    });
    if (missingPersistentAdmissions.length > 0) {
      fail3("PERSISTENT_TEST_ADMISSION_REQUIRED", `absent new persistent test path(s) require complete persistent_test_admissions records: ${missingPersistentAdmissions.join(", ")}.`);
    }
    if (legacyPersistentTestPaths.length > 0 && persistentTestAdmissions.length === 0) {
      fail3("PERSISTENT_TEST_ADMISSION_REQUIRED", "v2 persistent-test additions require complete persistent_test_admissions records; Runtime will not invent P-12 facts.");
    }
    if (authorityExpansionPaths.length === 0) {
      if (persistentTestAdmissions.length === 0) {
        fail3("SCOPE_AMENDMENT_NOT_REQUIRED", "All requested paths are already inside the task authority envelope; use execute-step extend-preflight with a blast-radius assessment or submit a typed P-12 admission for an absent persistent test.");
      }
      const ordinaryPaths = input.added_paths.filter((item) => !admissionPaths.includes(item));
      if (ordinaryPaths.length > 0) {
        fail3("SCOPE_AMENDMENT_NOT_REQUIRED", `Same-envelope ordinary target(s) do not need an authority amendment: ${ordinaryPaths.join(", ")}. Use execute-step extend-preflight with a blast-radius assessment.`);
      }
      if (input.authorization !== null) {
        fail3("SCOPE_AMENDMENT_AUTHORIZATION_CONFLICT", "A same-envelope persistent-test admission has authority_diff: none and must not carry a cross-envelope authorization decision.");
      }
    } else {
      if (input.authorization === null) {
        fail3("SCOPE_AMENDMENT_AUTHORIZATION_REQUIRED", `Explicit authority authorization is required for exact path(s): ${authorityExpansionPaths.join(", ")}.`);
      }
      assertV2AuthorityAmendmentExecutionSettled(current);
    }
  } else if (input.authorization === null) {
    fail3("SCOPE_AMENDMENT_AUTHORIZATION_REQUIRED", "Legacy/v1 scope amendments require the caller-reported existing authorization decision.");
  }
  const pendingFindingPaths = current.runtimeState.findings.filter((item) => ["admitted", "in-progress"].includes(item.status)).map((item) => item.file);
  const pendingReviewFindingPaths = current.runtimeState.pending_review_result?.findings.map((item) => item.file) ?? [];
  const requiredPaths = [...new Set([...input.added_paths, ...pendingFindingPaths, ...pendingReviewFindingPaths])];
  assertScopeAmendmentPathSafe(root2, current, requiredPaths);
  const activeStep = resolveCanonicalTaskStep(current).current;
  const currentStepScope = (activeStep.mutation_scope ?? "").split(",").map((value) => value.trim().replace(/^`|`$/g, "")).filter(Boolean);
  const taskScope = parseMutationScope(current.body);
  const taskScopeAddedPaths = current.mutationAuthority ? authorityExpansionPaths : input.added_paths.filter((file) => taskScope.allowed.some((entry) => mutationScopePatternMatchesPath(file, entry.pattern)) === false);
  const newlyAddedStepPaths = requiredPaths.filter((item) => !currentStepScope.some((pattern) => mutationScopePatternMatchesPath(item, pattern)));
  const amendmentStep = {
    ...input.amendment_step,
    mutation_scope: [...new Set([...currentStepScope, ...newlyAddedStepPaths])]
  };
  if (amendmentStep.mutation_scope.length === 0)
    fail3("SCOPE_AMENDMENT_SCOPE_INVALID", "The amendment step must declare exact mutation paths.");
  if (amendmentStep.commands.some((command) => command.expected_repo_writes !== "none" && command.expected_repo_writes.some((item) => !amendmentStep.mutation_scope.some((pattern) => pattern === item || mutationScopePatternMatchesPath(item, pattern))))) {
    fail3("SCOPE_AMENDMENT_SCOPE_INVALID", "Amendment command writes must remain inside the retained or newly authorized step scope.");
  }
  const basis = readCanonicalTaskBasis(root2, current);
  const oldObligations = correctionObligations(current);
  const oldDefinition = readDraftDefinitionFromBody(current.body);
  const retainedStepIds = parseImplementationSteps(oldDefinition.implementation_steps).map((item) => item.id);
  const digestSeed = digest3({ task: current.runtimeState.task_id, source: current.sourceTuple.revision, paths: input.added_paths, step: amendmentStep.id });
  if (!STEP_ID_PATTERN2.test(amendmentStep.id))
    fail3("SCOPE_AMENDMENT_STEP_INVALID", "Amendment step ID is invalid.");
  if (retainedStepIds.includes(amendmentStep.id))
    fail3("SCOPE_AMENDMENT_STEP_INVALID", "Amendment step ID must be new and must not reuse an old step.");
  const commands = structuredClone(amendmentStep.commands);
  const oldLines = (activeStep.plan_text ?? "").split(`
`);
  for (const record of current.runtimeState.claim_evidence ?? [])
    for (const slot of record.slots) {
      if (slot.due_step_id !== current.runtimeState.active_step_id || slot.check?.method !== "execution")
        continue;
      if (commands.some((item) => item.command === slot.check.entry))
        continue;
      const index = oldLines.findIndex((line) => line.trim() === `- planned_command: ${slot.check.entry}`);
      const footprint = index < 0 ? null : /^\s*- expected_repo_writes:\s*(.+)$/u.exec(oldLines[index + 1] ?? "")?.[1];
      if (!footprint)
        fail3("CLAIM_EVIDENCE_COMMAND_UNBOUND", "Supply the existing check command and its exact footprint for this legacy continuation.");
      const writes = footprint === "none" ? "none" : footprint.split(",").map((item) => item.trim());
      commands.push({ command: slot.check.entry, expected_repo_writes: writes });
    }
  const normalizedStep = {
    ...amendmentStep,
    description: amendmentStep.description || `Apply the authorized scope amendment ${digestSeed.slice(0, 8)}`,
    required_evidence: amendmentStep.required_evidence.length > 0 ? amendmentStep.required_evidence : ["fresh execution result", "fresh review of the amended scope"],
    commands
  };
  if (commands.some((command) => command.expected_repo_writes !== "none" && command.expected_repo_writes.some((item) => !normalizedStep.mutation_scope.some((pattern) => pattern === item || mutationScopePatternMatchesPath(item, pattern))))) {
    fail3("SCOPE_AMENDMENT_SCOPE_INVALID", "Retained validation writes must remain inside the explicitly retained or amended step scope.");
  }
  const claims = copyClaimEvidence(current.runtimeState.claim_evidence ?? []);
  if (current.mutationAuthority && persistentTestAdmissions.length > 0) {
    const claimIds = new Set(claims.map((item) => item.claim_id));
    const unknownClaims = [...new Set(persistentTestAdmissions.flatMap((item) => item.proves.filter((claimId) => !claimIds.has(claimId))))];
    if (unknownClaims.length > 0) {
      persistentTestAdmissionError("persistent_test_admissions.proves", `claim id(s) are not present in the current evidence plan: ${unknownClaims.join(", ")}.`);
    }
  }
  const definition = replaceScopeAmendmentStep(oldDefinition, current.runtimeState.active_step_id, normalizedStep, current.mutationAuthority !== null);
  const amendedAuthority = current.mutationAuthority ? {
    ...current.mutationAuthority,
    exact_exceptions: [...new Set([...current.mutationAuthority.exact_exceptions, ...authorityExpansionPaths])]
  } : undefined;
  const persistentTestRegressionChecks = current.mutationAuthority ? appendPersistentTestAdmissions(definition.regression_checks, persistentTestAdmissions) : persistentTestAdmissions.length > 0 ? appendPersistentTestAdmissions(definition.regression_checks, persistentTestAdmissions) : appendLegacyPersistentTestPaths(definition.regression_checks, legacyPersistentTestPaths, claims.filter((item) => item.claim_kind === "acceptance").map((item) => item.claim_id));
  const amendedDefinitionDraft = {
    ...definition,
    allowed_scope: appendExactScopePaths(definition.allowed_scope, input.added_paths),
    regression_checks: persistentTestRegressionChecks,
    ...amendedAuthority ? { mutation_authority_version: MUTATION_AUTHORITY_VERSION, mutation_authority: amendedAuthority } : {}
  };
  const amendedDefinition = {
    ...readDraftDefinitionFromBody(replaceReplanDefinitionSections(current.body, amendedDefinitionDraft)),
    ...amendedAuthority ? { mutation_authority_version: MUTATION_AUTHORITY_VERSION, mutation_authority: amendedAuthority } : {}
  };
  assertV2DraftDefinitionAuthority(root2, amendedDefinition);
  assertPreparedTestStrategy(root2, amendedDefinition, basis.basis);
  for (const record of claims)
    for (const slot of record.slots) {
      if (slot.due_step_id === current.runtimeState.active_step_id) {
        slot.due_step_id = normalizedStep.id;
        slot.disposition = "missing";
        slot.evidence_refs = [];
        slot.report = null;
        delete slot.prerequisite_receipt;
        delete slot.user_decision;
      }
      if (slot.before_step_id === current.runtimeState.active_step_id) {
        slot.before_step_id = normalizedStep.id;
        delete slot.prerequisite_receipt;
      }
    }
  const newPlanRevision = assertEvidencePlan(amendedDefinition, claims, false, { root: root2, taskBasis: basis.basis, previous: current.runtimeState.claim_evidence ?? [] });
  const findingFingerprints = current.runtimeState.findings.filter((item) => ["admitted", "in-progress"].includes(item.status)).map((item) => item.fingerprint).sort();
  const authorization = input.authorization === null ? null : {
    ...input.authorization,
    kind: "caller-reported-existing-decision",
    digest: digest3(input.authorization)
  };
  return {
    kind: "scope-amendment-candidate/v1",
    source_tuple: current.sourceTuple,
    task_id: current.runtimeState.task_id,
    document_id: current.sourceTuple.document_id,
    source_revision: current.sourceTuple.revision,
    basis_revision: basis.revision,
    old_plan_revision: current.runtimeState.evidence_plan_revision ?? digest3(oldDefinition),
    old_obligations: oldObligations,
    old_obligations_digest: digest3(oldObligations),
    retained_budget: { review_cycle: structuredClone(current.runtimeState.review_cycle), step_attempts: structuredClone(current.runtimeState.step_attempts ?? {}) },
    scope_diff: { added_paths: [...taskScopeAddedPaths], removed_paths: [] },
    command_footprint: normalizedStep.commands.map((command) => ({ command: command.command, expected_repo_writes: command.expected_repo_writes === "none" ? "none" : [...command.expected_repo_writes] })),
    step_diff: {
      inserted_step_id: normalizedStep.id,
      inserted_before: current.runtimeState.active_step_id,
      retained_step_ids: retainedStepIds,
      scope_paths: [...newlyAddedStepPaths]
    },
    continuation: {
      prior_step_id: current.runtimeState.active_step_id,
      pending_review_id: current.runtimeState.pending_review_result?.review_id ?? null,
      finding_fingerprints: findingFingerprints,
      pending_review_preserved: current.runtimeState.pending_review_result !== null
    },
    workspace_target: captureReviewTarget(root2, [...new Set([
      ...currentStepScope.filter((item) => !item.includes("*")),
      ...requiredPaths
    ])]),
    authorization,
    input: { ...input, amendment_step: normalizedStep },
    definition: amendedDefinition,
    claim_evidence: claims,
    new_plan_revision: newPlanRevision,
    ...amendedAuthority ? { authority_diff: { added_exact_exceptions: [...authorityExpansionPaths], added_domains: [] } } : {},
    ...persistentTestAdmissions.length > 0 ? { persistent_test_admission: { added: structuredClone(persistentTestAdmissions) } } : {},
    permission_change: "additive-scope"
  };
}
function prepareScopeAmendment(root2, rawInput, options = {}) {
  return withGovernanceWriteLock(root2, () => {
    if (!options.dryRun)
      recoverPendingTaskStoreCommit(root2);
    const current = readCanonicalCurrentTask(root2);
    const pendingCorrection = unconfirmedCorrectionCandidatePaths(current);
    if (pendingCorrection.length > 0) {
      fail3("SCOPE_AMENDMENT_CANDIDATE_CONFLICT", `An unconfirmed correction candidate is already in flight (${pendingCorrection[0]}); discard it before preparing a scope amendment.`);
    }
    const input = normalizeScopeAmendmentInput(rawInput);
    for (const entry of [...current.runtimeState.execution_log].reverse()) {
      if (!("action" in entry) || entry.action !== "commit-scope-amendment" || !entry.candidate_digest)
        continue;
      const priorLocation = scopeAmendmentCandidateLocation(current, entry.candidate_digest);
      if (!fs11.existsSync(priorLocation.filePath) || fs11.existsSync(`${priorLocation.filePath}.discarded`)) {
        fail3("SCOPE_AMENDMENT_HISTORY_CORRUPT", "A committed scope-amendment candidate is missing or discarded.");
      }
      let priorCandidate;
      try {
        priorCandidate = JSON.parse(fs11.readFileSync(priorLocation.filePath, "utf8"));
      } catch (error) {
        fail3("SCOPE_AMENDMENT_HISTORY_CORRUPT", `A committed scope-amendment candidate cannot be read: ${error instanceof Error ? error.message : String(error)}`);
      }
      const { candidate_digest: marker, ...priorContent } = priorCandidate;
      if (marker !== entry.candidate_digest || digest3(priorContent) !== entry.candidate_digest) {
        fail3("SCOPE_AMENDMENT_HISTORY_CORRUPT", "A committed scope-amendment candidate changed after publication.");
      }
      if (scopeAmendmentRequestMatchesCandidate(input, priorCandidate)) {
        const receipt = scopeAmendmentCandidateReceipt(priorCandidate, entry.candidate_digest);
        const idempotencyKey = `scope-amendment-${digest3({ receipt, authorization: input.authorization ? { decision_source: input.authorization.decision_source, decision_text: input.authorization.decision_text } : null }).slice(0, 40)}`;
        return {
          status: "no-op",
          operation_kind: "task-state-transaction",
          idempotency_key: idempotencyKey,
          target_path: current.relativePath,
          dry_run: options.dryRun === true,
          committed: false,
          message: "This exact scope amendment was already committed.",
          planned_writes: [],
          governed_mutation_count: 0,
          read_back_verified: true,
          evidence_assurance: "caller-reported",
          candidate_path: priorLocation.relativePath,
          candidate_receipt: receipt,
          state: resultState(current.runtimeState)
        };
      }
    }
    const candidate = buildScopeAmendmentCandidate(root2, current, input);
    const candidateDigest = digest3(candidate);
    const location = scopeAmendmentCandidateLocation(current, candidateDigest);
    const content = JSON.stringify({ ...candidate, candidate_digest: candidateDigest }, null, 2) + `
`;
    const existed = fs11.existsSync(location.filePath);
    const directory = path12.dirname(location.filePath);
    if (fs11.existsSync(directory)) {
      const prior = fs11.readdirSync(directory).filter((name) => name.endsWith(".scope.json"));
      if (prior.length > 64)
        fail3("SCOPE_AMENDMENT_CANDIDATE_BUDGET_EXHAUSTED", "Too many retained scope-amendment candidates.");
      const committed = new Set(current.runtimeState.execution_log.flatMap((entry) => ("action" in entry) && entry.action === "commit-scope-amendment" && entry.candidate_digest ? [`${entry.candidate_digest}.scope.json`] : []));
      for (const name of prior) {
        if (name === path12.basename(location.filePath) || fs11.existsSync(path12.join(directory, `${name}.discarded`)) || committed.has(name))
          continue;
        fail3("SCOPE_AMENDMENT_CANDIDATE_CONFLICT", "Discard the existing unconfirmed scope-amendment candidate before preparing another.");
      }
    }
    if (!options.dryRun) {
      fs11.mkdirSync(directory, { recursive: true });
      if (existed) {
        if (fs11.readFileSync(location.filePath, "utf8") !== content)
          fail3("SCOPE_AMENDMENT_CANDIDATE_CONFLICT", "Existing scope-amendment candidate bytes differ.");
      } else {
        const descriptor = fs11.openSync(location.filePath, "wx");
        try {
          fs11.writeFileSync(descriptor, content, "utf8");
          fs11.fsyncSync(descriptor);
        } finally {
          fs11.closeSync(descriptor);
        }
      }
      if (fs11.readFileSync(location.filePath, "utf8") !== content)
        fail3("SCOPE_AMENDMENT_CANDIDATE_READ_BACK_FAILED", "Scope-amendment candidate read-back failed.");
    }
    const receipt = scopeAmendmentCandidateReceipt(candidate, candidateDigest);
    if (options.dryRun) {
      return {
        status: existed ? "no-op" : "success",
        operation_kind: "task-state-transaction",
        idempotency_key: `prepare-scope-amendment-${candidateDigest.slice(0, 40)}`,
        target_path: location.relativePath,
        dry_run: true,
        committed: false,
        message: "Scope-amendment dry run prepared the Runtime candidate; no task state was changed.",
        planned_writes: [location.relativePath],
        governed_mutation_count: 0,
        read_back_verified: false,
        evidence_assurance: "caller-reported",
        candidate_path: location.relativePath,
        candidate_receipt: receipt
      };
    }
    const committed = commitScopeAmendmentLocked(root2, {
      candidate_receipt: receipt,
      authorization: input.authorization ? { decision_source: input.authorization.decision_source, decision_text: input.authorization.decision_text } : null
    }, options);
    return {
      ...committed,
      candidate_path: location.relativePath,
      candidate_receipt: receipt,
      planned_writes: [location.relativePath, ...committed.planned_writes],
      governed_mutation_count: committed.governed_mutation_count + (existed ? 0 : 1),
      message: committed.status === "success" || committed.status === "no-op" ? input.authorization ? "Scope amendment prepared and committed from the existing explicit user authorization." : "Persistent-test admission prepared and committed from the complete caller-supplied P-12 decision." : committed.message
    };
  });
}
function commitScopeAmendmentLocked(root2, rawInput, options = {}) {
  const source = expectRecord2(rawInput, "internal scope-amendment commit input");
  expectExactKeys2(source, ["candidate_receipt", "authorization"], "internal scope-amendment commit input");
  const receipt = expectRecord2(source.candidate_receipt, "candidate_receipt");
  expectExactKeys2(receipt, [
    "kind",
    "candidate_digest",
    "source_revision",
    "basis_revision",
    "old_plan_revision",
    "new_plan_revision",
    "obligations_digest",
    "authorization_digest",
    "added_paths",
    "permission_change",
    ...receipt.authority_diff === undefined ? [] : ["authority_diff"],
    ...receipt.persistent_test_admission === undefined ? [] : ["persistent_test_admission"]
  ], "candidate_receipt");
  if (receipt.kind !== "scope-amendment-candidate-receipt/v1" || receipt.permission_change !== "additive-scope")
    fail3("SCOPE_AMENDMENT_CONFIRMATION_INVALID", "Receipt kind or permission change is invalid.");
  const candidateDigest = expectString2(receipt.candidate_digest, "candidate_receipt.candidate_digest", SHA256_PATTERN2);
  const sourceRevision = expectString2(receipt.source_revision, "candidate_receipt.source_revision", SHA256_PATTERN2);
  const basisRevision = expectString2(receipt.basis_revision, "candidate_receipt.basis_revision", SHA256_PATTERN2);
  const oldPlanRevision = expectString2(receipt.old_plan_revision, "candidate_receipt.old_plan_revision", SHA256_PATTERN2);
  const newPlanRevision = expectString2(receipt.new_plan_revision, "candidate_receipt.new_plan_revision", SHA256_PATTERN2);
  const obligationsDigest = expectString2(receipt.obligations_digest, "candidate_receipt.obligations_digest", SHA256_PATTERN2);
  const authorizationDigest = expectString2(receipt.authorization_digest, "candidate_receipt.authorization_digest", SHA256_PATTERN2);
  const addedPaths = expectStringArray2(receipt.added_paths, "candidate_receipt.added_paths", false, 64).map((item) => normalizeRepoPath2(item, "candidate_receipt.added_paths"));
  if (receipt.authority_diff !== undefined) {
    const authorityDiff = expectRecord2(receipt.authority_diff, "candidate_receipt.authority_diff");
    expectExactKeys2(authorityDiff, ["added_exact_exceptions", "added_domains"], "candidate_receipt.authority_diff");
    expectStringArray2(authorityDiff.added_exact_exceptions, "candidate_receipt.authority_diff.added_exact_exceptions", true, 64).forEach((item) => normalizeRepoPath2(item, "candidate_receipt.authority_diff.added_exact_exceptions"));
    expectStringArray2(authorityDiff.added_domains, "candidate_receipt.authority_diff.added_domains", true, 64).forEach((item) => expectText(item, "candidate_receipt.authority_diff.added_domains", 128));
  }
  if (receipt.persistent_test_admission !== undefined) {
    const persistentAdmission = expectRecord2(receipt.persistent_test_admission, "candidate_receipt.persistent_test_admission");
    expectExactKeys2(persistentAdmission, ["added_paths"], "candidate_receipt.persistent_test_admission");
    expectStringArray2(persistentAdmission.added_paths, "candidate_receipt.persistent_test_admission.added_paths", false, 64).forEach((item) => normalizeRepoPath2(item, "candidate_receipt.persistent_test_admission.added_paths"));
  }
  const approval = source.authorization === null ? null : expectRecord2(source.authorization, "authorization");
  const decisionSource = approval ? expectText(approval.decision_source, "authorization.decision_source", 512) : null;
  const decisionText = approval ? expectVerbatim(approval.decision_text, "authorization.decision_text", 4096) : null;
  if (approval)
    expectExactKeys2(approval, ["decision_source", "decision_text"], "authorization");
  if (!options.dryRun)
    recoverPendingTaskStoreCommit(root2);
  const current = readCanonicalCurrentTask(root2);
  const location = scopeAmendmentCandidateLocation(current, candidateDigest);
  const idempotencyKey = `scope-amendment-${digest3({ receipt, authorization: approval ? { decision_source: decisionSource, decision_text: decisionText } : null }).slice(0, 40)}`;
  const previousCommit = current.runtimeState.execution_log.find((item) => ("action" in item) && item.action === "commit-scope-amendment" && item.candidate_digest === candidateDigest);
  if (previousCommit) {
    if (previousCommit.idempotency_key !== idempotencyKey)
      fail3("SCOPE_AMENDMENT_ALREADY_COMMITTED", "This candidate was already committed with different authorization semantics.");
    assertScopeAmendmentHistoryForRevision(current, previousCommit.source_revision);
    return { status: "no-op", operation_kind: "task-state-transaction", idempotency_key: idempotencyKey, target_path: current.relativePath, dry_run: options.dryRun === true, committed: false, message: "This exact scope amendment was already committed.", planned_writes: [], governed_mutation_count: 0, read_back_verified: true, evidence_assurance: "caller-reported", state: resultState(current.runtimeState) };
  }
  if (!["active", "blocked_by_replan"].includes(current.runtimeState.workflow_status) || current.runtimeState.lifecycle_state !== "active")
    fail3("SCOPE_AMENDMENT_STATE_INVALID", "Scope amendment commit requires active or blocked_by_replan + active.");
  if (current.runtimeState.resume_requires_review)
    fail3("SCOPE_AMENDMENT_STATE_INVALID", "Scope amendment commit cannot clear a resume-review gate.");
  if (!taskSourceRevisionMatches(root2, current, sourceRevision))
    fail3("SCOPE_AMENDMENT_SOURCE_STALE", "CURRENT_TASK changed after scope-amendment preparation.");
  const basis = readCanonicalTaskBasis(root2, current);
  if (basis.revision !== basisRevision || (current.runtimeState.evidence_plan_revision ?? oldPlanRevision) !== oldPlanRevision || digest3(correctionObligations(current)) !== obligationsDigest)
    fail3("SCOPE_AMENDMENT_OBLIGATIONS_STALE", "Task Basis, plan revision, or old obligations changed after candidate preparation.");
  if (!fs11.existsSync(location.filePath) || fs11.existsSync(`${location.filePath}.discarded`))
    fail3("SCOPE_AMENDMENT_CANDIDATE_MISSING", "Scope-amendment candidate is absent or discarded.");
  const saved = JSON.parse(fs11.readFileSync(location.filePath, "utf8"));
  if (saved.candidate_digest !== candidateDigest)
    fail3("SCOPE_AMENDMENT_CANDIDATE_INVALID", "Candidate digest marker differs.");
  const { candidate_digest: _marker, ...storedCandidate } = saved;
  if (digest3(storedCandidate) !== candidateDigest)
    fail3("SCOPE_AMENDMENT_CANDIDATE_INVALID", "Candidate content changed after preparation.");
  const candidateSource = { ...current, sourceTuple: { ...current.sourceTuple, revision: sourceRevision } };
  const rebuilt = buildScopeAmendmentCandidate(root2, candidateSource, normalizeScopeAmendmentInput(saved.input, { allowExistingWildcardScope: true }));
  const rebuiltAuthorityDiff = rebuilt.authority_diff ?? null;
  const rebuiltPersistentAdmissionPaths = rebuilt.persistent_test_admission ? { added_paths: rebuilt.persistent_test_admission.added.map((item) => item.path) } : null;
  if (digest3(rebuilt) !== candidateDigest || rebuilt.new_plan_revision !== newPlanRevision || rebuilt.old_plan_revision !== oldPlanRevision || rebuilt.basis_revision !== basisRevision || (rebuilt.authorization?.digest ?? digest3(null)) !== authorizationDigest || digest3(rebuilt.input.added_paths) !== digest3(addedPaths) || receipt.authority_diff !== undefined && digest3(receipt.authority_diff) !== digest3(rebuiltAuthorityDiff) || receipt.persistent_test_admission !== undefined && digest3(receipt.persistent_test_admission) !== digest3(rebuiltPersistentAdmissionPaths)) {
    fail3("SCOPE_AMENDMENT_CANDIDATE_STALE", "Candidate no longer matches the Runtime-computed source, scope, obligations, and plan.");
  }
  const authorityExpansion = Boolean(rebuilt.authority_diff && (rebuilt.authority_diff.added_exact_exceptions.length > 0 || rebuilt.authority_diff.added_domains.length > 0));
  if (authorityExpansion && !approval) {
    fail3("SCOPE_AMENDMENT_AUTHORIZATION_REQUIRED", "A true authority amendment must carry the explicit authorization decision bound to every added path.");
  }
  if (rebuilt.authorization) {
    if (!approval || decisionSource !== rebuilt.authorization.decision_source || decisionText !== rebuilt.authorization.decision_text) {
      fail3("SCOPE_AMENDMENT_AUTHORIZATION_CONFLICT", "Existing explicit authorization must preserve its caller-reported source and verbatim text.");
    }
  } else if (approval) {
    fail3("SCOPE_AMENDMENT_AUTHORIZATION_CONFLICT", "A persistent-test-only admission has authority_diff: none and must not carry an authority authorization decision.");
  }
  const nextBasis = {
    original_request: basis.basis.original_request,
    user_decisions: !approval || basis.basis.user_decisions.some((item) => item.source === decisionSource && item.verbatim === decisionText) ? [...basis.basis.user_decisions] : [...basis.basis.user_decisions, { source: decisionSource, verbatim: decisionText }]
  };
  const nextBasisArtifact = materializeTaskBasis(root2, current, { task_id: current.runtimeState.task_id, task_slug: current.runtimeState.task_slug, task_title: extractTaskIdentityFromCurrentTask(current.body).title, document_id: current.sourceTuple.document_id }, nextBasis);
  const evidenceRefs = [
    location.relativePath,
    ...approval ? [`caller-reported-scope-authorization:${digest3({ decisionSource, decisionText })}`] : [],
    ...rebuilt.persistent_test_admission ? [`persistent-test-admission:${digest3(rebuilt.persistent_test_admission.added)}`] : []
  ];
  const authorityEvidenceKinds = approval ? ["active-task-owner", "scope-admission", "evidence-admission", "authorized-caller"] : ["active-task-owner", "scope-admission", "evidence-admission"];
  const authorityEvidence = authorityEvidenceKinds.map((kind) => ({ kind, source: approval ? decisionSource : evidenceRefs.at(-1), subject: candidateDigest, task_id: current.runtimeState.task_id, document_id: current.sourceTuple.document_id, source_revision: current.sourceTuple.revision }));
  const proposal = createPrepareTaskScopeAmendmentProposal(current, {
    delta: { kind: "task-state", action: "commit-scope-amendment", task_basis: nextBasis, replacement_definition: rebuilt.definition, active_step_id: rebuilt.input.amendment_step.id, claim_evidence: rebuilt.claim_evidence, evidence_refs: evidenceRefs },
    idempotency_key: idempotencyKey,
    authority_evidence: authorityEvidence,
    evidence_refs: evidenceRefs
  });
  const oldState = current.runtimeState;
  const amendmentReviewPaths = [...new Set([
    ...rebuilt.input.added_paths,
    ...rebuilt.continuation.finding_fingerprints.map((fingerprint) => oldState.findings.find((item) => item.fingerprint === fingerprint)?.file).filter((item) => Boolean(item)),
    ...(oldState.pending_review_result?.findings ?? []).map((item) => item.file)
  ])].sort();
  const amendedReviewAdmission = oldState.review_coverage ? registerReviewCoverage(root2, current, amendmentReviewPaths) : undefined;
  const amendedReviewCoverage = amendedReviewAdmission?.coverage;
  const retainedPendingPaths = amendedReviewCoverage ? [...new Set([...amendedReviewCoverage.pending_paths, ...amendmentReviewPaths])].sort() : undefined;
  const { execution_preflight: _amendmentExecutionPreflight, ...stateWithoutExecutionPreflight } = oldState;
  const domainRevision = current.mutationAuthority ? current.runtimeState.authority_domain_revision : undefined;
  const nextWithoutAudit = {
    ...stateWithoutExecutionPreflight,
    workflow_status: "active",
    lifecycle_state: "active",
    active_step_id: rebuilt.input.amendment_step.id,
    active_step_status: "ready",
    evidence_plan_revision: rebuilt.new_plan_revision,
    claim_evidence_required: true,
    claim_evidence: rebuilt.claim_evidence,
    ...domainRevision ? { authority_domain_revision: domainRevision } : {},
    ...oldState.pending_review_result ? { scope_amendment_pending_review_step_id: oldState.pending_review_result.step_id } : {},
    ...amendedReviewCoverage && retainedPendingPaths ? { review_coverage: { ...amendedReviewCoverage, pending_paths: retainedPendingPaths } } : {},
    applied_proposals: appendAppliedProposal(oldState, proposal, current.sourceTuple.revision)
  };
  const correctionReason = rebuilt.authorization ? `caller-reported scope amendment: ${decisionText}` : `persistent-test admission: ${rebuilt.persistent_test_admission.added.map((item) => item.path).join(", ")}`;
  const audit = { ...makeReplanAudit(current, proposal, nextWithoutAudit, options.now?.() ?? new Date().toISOString()), candidate_digest: candidateDigest, correction_reason: correctionReason };
  const nextState = { ...nextWithoutAudit, execution_log: appendExecutionLogEntry(current.runtimeState, audit) };
  const prepared = renderCanonicalCurrentTask(current.frontmatter, current.body, nextState, { replacementDefinition: rebuilt.definition, taskBasisReference: { path: basis.path, revision: nextBasisArtifact.revision }, audit });
  const nextContent = prepared.content;
  const preview = parseCanonicalCurrentTaskContent(nextContent, current.filePath, current.relativePath, prepared);
  if (preview.runtimeState.evidence_plan_revision !== rebuilt.new_plan_revision || preview.runtimeState.active_step_id !== rebuilt.input.amendment_step.id || preview.runtimeState.pending_review_result?.review_id !== oldState.pending_review_result?.review_id)
    fail3("SCOPE_AMENDMENT_CANDIDATE_INVALID", "Rendered scope amendment does not preserve the pending review or admitted plan.");
  const history = taskHistoryLocation({ currentPath: current.filePath, previousContent: current.raw, nextContent, documentId: current.sourceTuple.document_id, taskId: current.runtimeState.task_id, basisPath: basis.filePath, basisContent: basis.content, nextBasisContent: nextBasisArtifact.content, operation: "commit-scope-amendment", evidencePlanRevision: current.runtimeState.evidence_plan_revision, referencedEvidence: [location.relativePath] });
  const plannedWrites = [path12.posix.join(path12.posix.dirname(current.relativePath), history.relativePath), basis.path, current.relativePath];
  if (options.dryRun)
    return { status: "success", operation_kind: "task-state-transaction", idempotency_key: idempotencyKey, target_path: current.relativePath, dry_run: true, committed: false, message: "Scope-amendment commit dry run passed; no live task was changed.", planned_writes: plannedWrites, governed_mutation_count: 0, read_back_verified: false, evidence_assurance: "caller-reported" };
  try {
    persistReviewPreimageWrites(root2, amendedReviewAdmission?.preimageWrites ?? []);
  } catch (error) {
    return buildResult("blocked", proposal, current, options, `review baseline persistence failed: ${error instanceof Error ? error.message : String(error)}`, {
      code: error instanceof ReviewPreimageStoreError ? error.code : "REVIEW_BASELINE_PERSISTENCE_FAILED"
    });
  }
  let stagedAfter;
  try {
    stagedAfter = stageTaskEvolutionStoreCommit(root2, current, nextContent, nextState, proposal, [{ path: history.path, content: history.content }, { path: nextBasisArtifact.filePath, content: nextBasisArtifact.content }, { path: current.filePath, content: nextContent }], prepared);
  } catch (error) {
    return buildResult("blocked", proposal, current, options, `task-store precommit staging failed: ${error instanceof Error ? error.message : String(error)}`, { code: error instanceof TaskStoreError ? error.code : "TASK_STORE_COMMIT_FAILED" });
  }
  try {
    commitTaskEvolutionWithHistory({ currentPath: current.filePath, previousContent: current.raw, nextContent, documentId: current.sourceTuple.document_id, taskId: current.runtimeState.task_id, basisPath: basis.filePath, basisContent: basis.content, nextBasisContent: nextBasisArtifact.content, operation: "commit-scope-amendment", evidencePlanRevision: current.runtimeState.evidence_plan_revision, referencedEvidence: [location.relativePath] }, (content) => {
      const parsed = parseCanonicalCurrentTaskContent(content, current.filePath, current.relativePath);
      if (parsed.runtimeState.evidence_plan_revision !== rebuilt.new_plan_revision || parsed.runtimeState.active_step_id !== rebuilt.input.amendment_step.id || parsed.runtimeState.pending_review_result?.review_id !== oldState.pending_review_result?.review_id)
        fail3("SCOPE_AMENDMENT_READ_BACK_FAILED", "Scope-amendment task/Basis read-back is inconsistent.");
    });
  } catch (error) {
    if (fs11.existsSync(current.filePath) && sha2565(fs11.readFileSync(current.filePath, "utf8")) === current.sourceTuple.revision)
      clearPendingTaskStoreAfterRollback(root2, current);
    throw error;
  }
  const storeResult = { status: "success", committed: true, operation_kind: "task-state-transaction", idempotency_key: idempotencyKey, message: "Scope amendment committed; prior review, findings, obligations, and budget were retained." };
  try {
    const manifest = completeTaskEvolutionStoreCommit(root2, current, stagedAfter, proposal, storeResult);
    const readBack = readCanonicalCurrentTask(root2);
    return { status: "success", operation_kind: "task-state-transaction", idempotency_key: idempotencyKey, target_path: current.relativePath, dry_run: false, committed: true, message: storeResult.message, planned_writes: plannedWrites, governed_mutation_count: 3, read_back_verified: readBack.raw === nextContent, previous_revision: current.sourceTuple.revision, resulting_revision: readBack.sourceTuple.revision, evidence_assurance: "caller-reported", state: resultState(readBack.runtimeState), task_store: { manifest_path: `${manifest.storage_root}/manifest.json`, source_revision: manifest.head.source_revision, definition_revision: manifest.head.definition_revision, state_revision: manifest.head.state_revision, event_sequence: manifest.head.event_sequence } };
  } catch (error) {
    return { status: "blocked", operation_kind: "task-state-transaction", idempotency_key: idempotencyKey, target_path: current.relativePath, dry_run: false, committed: true, message: `Scope amendment committed but task-store publication needs recovery: ${error instanceof Error ? error.message : String(error)}`, planned_writes: plannedWrites, governed_mutation_count: 3, read_back_verified: false, previous_revision: current.sourceTuple.revision, resulting_revision: stagedAfter.sourceTuple.revision, evidence_assurance: "caller-reported", code: error instanceof TaskStoreError ? error.code : "TASK_STORE_COMMIT_FAILED", state: resultState(stagedAfter.runtimeState) };
  }
}
function discardScopeAmendment(root2, rawInput, options = {}) {
  return withGovernanceWriteLock(root2, () => {
    if (!options.dryRun)
      recoverPendingTaskStoreCommit(root2);
    const input = expectRecord2(rawInput, "discard-scope-amendment input");
    expectExactKeys2(input, ["candidate_digest"], "discard-scope-amendment input");
    const current = readCanonicalCurrentTask(root2);
    const candidateDigest = expectString2(input.candidate_digest, "candidate_digest", SHA256_PATTERN2);
    const location = scopeAmendmentCandidateLocation(current, candidateDigest);
    const committed = current.runtimeState.execution_log.some((item) => ("action" in item) && item.action === "commit-scope-amendment" && item.candidate_digest === candidateDigest);
    if (committed) {
      fail3("SCOPE_AMENDMENT_ALREADY_COMMITTED", "A committed scope-amendment candidate is immutable and cannot be discarded.");
    }
    if (!fs11.existsSync(location.filePath))
      fail3("SCOPE_AMENDMENT_CANDIDATE_MISSING", "Scope-amendment candidate does not exist.");
    const marker = `${location.filePath}.discarded`;
    const existed = fs11.existsSync(marker);
    if (!options.dryRun && !existed)
      fs11.writeFileSync(marker, `${candidateDigest}
`, { flag: "wx" });
    return { status: existed ? "no-op" : "success", operation_kind: "task-state-transaction", idempotency_key: `discard-scope-amendment-${candidateDigest.slice(0, 40)}`, target_path: location.relativePath, dry_run: options.dryRun === true, committed: !options.dryRun && !existed, message: "Scope-amendment candidate discarded; CURRENT_TASK was not changed.", planned_writes: [`${location.relativePath}.discarded`], governed_mutation_count: options.dryRun || existed ? 0 : 1, read_back_verified: options.dryRun !== true, evidence_assurance: "caller-reported" };
  });
}
function buildCorrectionCandidate(root2, current, input) {
  assertV2AuthorityDomainRevisionFreshForTransition(root2, current, "Correction replan");
  const suspended = current.runtimeState.workflow_status === "blocked_by_replan";
  if (!["active", "superseded", "blocked_by_replan"].includes(current.runtimeState.workflow_status) || current.runtimeState.lifecycle_state !== "active" || current.runtimeState.resume_requires_review || !suspended && !["ready", "completed"].includes(current.runtimeState.active_step_status) || !suspended && current.runtimeState.pending_review_result || current.runtimeState.findings.some((item) => ["admitted", "in-progress"].includes(item.status))) {
    fail3("REPLAN_CANDIDATE_STATE_INVALID", "Restricted correction requires a ready active/superseded task without a competing review, finding, or resume gate.");
  }
  assertBusinessEvidenceVersion(current);
  assertTestStrategySequenceReady(current);
  const unresolved = (current.runtimeState.evidence_challenges ?? []).filter((item) => item.status !== "resolved");
  if (unresolved.some((item) => item.correction_step_id !== null))
    fail3("REPLAN_CANDIDATE_CONFLICT", "Finish the active recovery batch before preparing another.");
  const challenges = input.challenge_ids.map((id) => unresolved.find((item) => item.challenge_id === id));
  if (challenges.some((item) => !item)) {
    fail3("REPLAN_CHALLENGE_REQUIRED", "Every target must bind an unresolved, unassigned challenge.");
  }
  for (const challenge of challenges) {
    const evidencePath = path12.resolve(root2, challenge.evidence_ref);
    if (!fs11.existsSync(evidencePath) || sha2565(fs11.readFileSync(evidencePath)) !== challenge.evidence_sha256)
      fail3("EVIDENCE_CHALLENGE_SOURCE_STALE", "Challenge evidence changed.");
  }
  const step = input.correction_step;
  const recoverySteps = [step, ...input.recovery_steps];
  if (suspended && !input.pending_step_changes)
    fail3("RECOVERY_OBLIGATION_INVALID", "A suspended attempt requires an explicit future plan; its old step cannot be executed again under the old ID.");
  const allNewSteps = [...recoverySteps, ...input.pending_step_changes?.steps ?? []];
  if (new Set(allNewSteps.map((item) => item.id)).size !== allNewSteps.length)
    fail3("RECOVERY_STEP_ID_REUSED", "Recovery and pending step IDs must be unique.");
  const writes = [...new Set(allNewSteps.flatMap((item) => item.mutation_scope))];
  if (!current.mutationAuthority && evaluateMutationScope(parseMutationScope(current.body), { changed_paths: writes }).status !== "pass") {
    fail3("REPLAN_SCOPE_EXPANSION", "Recovery paths exceed the old task scope.");
  }
  if (input.mode === "conclusion-correction") {
    const boundaries = nonExecutableChangePatterns(root2);
    if (writes.some((p) => !boundaries.some((boundary) => mutationScopePatternIsSubset(p, boundary))))
      fail3("REPLAN_SCOPE_EXPANSION", "Conclusion correction requires project-declared non-executable paths within original task authority.");
  }
  const executionTargets = input.execution_targets.map((target) => {
    const execution = current.runtimeState.execution_log.find((item) => !("action" in item) && item.idempotency_key === target.execution_id);
    if (!execution || "action" in execution || !execution.execution_result)
      fail3("RECOVERY_TARGET_INVALID", "Execution target must name a real retained result in this task.");
    if (describeEvidenceObjects(root2, [target.evidence_ref])[0]?.sha256 !== target.evidence_sha256)
      fail3("RECOVERY_TARGET_STALE", "Execution diagnosis evidence changed.");
    return execution;
  });
  const restorePlan = input.restore_plan ? prepareArtifactRestore(root2, current.filePath, current.runtimeState.task_id, current.sourceTuple.document_id, input.restore_plan.checkpoint_id, input.restore_plan.paths) : null;
  if (restorePlan) {
    if (!current.runtimeState.artifact_checkpoint_ids?.includes(restorePlan.checkpoint_id))
      fail3("ARTIFACT_CHECKPOINT_UNCOMMITTED", "Checkpoint digest does not bind a committed task execution or preflight.");
    if (restorePlan.targets.some((item) => !step.mutation_scope.includes(item.path)) || !step.commands.some((command) => command.command === "runtime:artifact-restore" && command.expected_repo_writes !== "none" && digest3([...command.expected_repo_writes].sort()) === digest3(restorePlan.targets.map((item) => item.path))))
      fail3("ARTIFACT_RESTORE_FOOTPRINT_REQUIRED", "First recovery step must declare runtime:artifact-restore with the exact restore paths.");
  }
  const basis = readCanonicalTaskBasis(root2, current);
  const oldObligations = correctionObligations(current);
  const retainedStepIds = parseImplementationSteps(readDraftDefinitionFromBody(current.body).implementation_steps).map((item) => item.id);
  const append = current.runtimeState.active_step_status === "completed";
  if (append && retainedStepIds.at(-1) !== current.runtimeState.active_step_id)
    fail3("REPLAN_CANDIDATE_STATE_INVALID", "Only a completed final step supports append.");
  let definition = readDraftDefinitionFromBody(current.body);
  if (input.pending_step_changes)
    definition = revisePendingSteps(current, definition, input.pending_step_changes, recoverySteps);
  const pendingAnchor = input.pending_step_changes?.steps[0]?.id;
  const anchor = pendingAnchor ?? (input.pending_step_changes ? parseImplementationSteps(definition.implementation_steps).at(-1).id : current.runtimeState.active_step_id);
  const appendRecovery = input.pending_step_changes ? !pendingAnchor : append;
  for (const recoveryStep of recoverySteps)
    definition = insertCorrectionStep(definition, anchor, recoveryStep, appendRecovery);
  assertPreparedTestStrategy(root2, definition, basis.basis);
  assertV2DraftDefinitionAuthority(root2, definition);
  const records = copyClaimEvidence(current.runtimeState.claim_evidence ?? []);
  const correctedSlots = new Set;
  for (const challenge of challenges) {
    const corrected = records.find((item) => item.claim_id === challenge.claim_id)?.slots.find((item) => item.slot_id === challenge.slot_id);
    const sourceSlot = current.runtimeState.claim_evidence?.find((item) => item.claim_id === challenge.claim_id)?.slots.find((item) => item.slot_id === challenge.slot_id);
    if (!sourceSlot?.report || !corrected?.check || !challengeReportIsRetained(root2, current, challenge, sourceSlot.report.result_id))
      fail3("REPLAN_CHALLENGE_STALE", "The challenged report has no verified correction relationship to the current report.");
    if (corrected && correctedSlots.has(corrected))
      continue;
    correctedSlots.add(corrected);
    corrected.due_step_id = step.id;
    corrected.disposition = "missing";
    corrected.evidence_refs = [];
    corrected.report = null;
    delete corrected.prerequisite_receipt;
  }
  const slots = records.flatMap((record) => record.slots.map((slot) => ({ record, slot })));
  const explicitMap = input.obligation_map;
  if ((input.mode === "execution-recovery" || input.pending_step_changes) && !explicitMap.length)
    fail3("RECOVERY_OBLIGATION_INVALID", "Execution or pending-plan recovery requires the complete old claim/slot obligation map.");
  if (explicitMap.length && (explicitMap.length !== slots.length || new Set(explicitMap.map((item) => `${item.claim_id}/${item.slot_id}`)).size !== slots.length))
    fail3("RECOVERY_OBLIGATION_INVALID", "Each old claim/slot must occur exactly once.");
  const newIds = new Set(allNewSteps.map((item) => item.id));
  for (const { record, slot } of slots) {
    const mapping = explicitMap.find((item) => item.claim_id === record.claim_id && item.slot_id === slot.slot_id);
    if (explicitMap.length && !mapping)
      fail3("RECOVERY_OBLIGATION_INVALID", "Old obligation is missing.");
    const targeted = executionTargets.some((execution) => execution.execution_result.acceptance_evidence.some((evidence) => !("acceptance" in evidence) && evidence.claim_id === record.claim_id && evidence.slot_id === slot.slot_id));
    if (targeted && (!mapping || !newIds.has(mapping.due_step_id)))
      fail3("RECOVERY_OBLIGATION_INVALID", "Affected evidence must be assigned to a new execution or verification step.");
    if (!mapping)
      continue;
    if (mapping.before_step_id && mapping.before_step_id !== slot.before_step_id) {
      if (slot.applicability !== "before-step" || !newIds.has(mapping.before_step_id) || !newIds.has(mapping.due_step_id))
        fail3("RECOVERY_PREREQUISITE_INVALID", "A new consumer requires a new preceding verification step.");
      slot.before_step_id = mapping.before_step_id;
      delete slot.prerequisite_receipt;
    }
    if (!parseImplementationSteps(definition.implementation_steps).some((item) => item.id === mapping.due_step_id))
      fail3("RECOVERY_OBLIGATION_INVALID", "Obligation destination does not exist.");
    if (mapping.check) {
      if (mapping.replaces_check_id !== slot.check?.check_id || (current.runtimeState.claim_evidence ?? []).some((claim) => claim.slots.some((old) => old.check?.check_id === mapping.check.check_id)))
        fail3("RECOVERY_CHECK_ID_REUSED", "Replacement check needs a new identity and the exact superseded check reference.");
      if (slot.check.required_boundaries.some((boundary) => !mapping.check.required_boundaries.includes(boundary)) || slot.check.subject_paths.some((p) => !mapping.check.subject_paths.includes(p)))
        fail3("RECOVERY_CHECK_WEAKENED", "Existing required boundaries and subjects cannot be removed by recovery.");
      if (slot.check.boundary !== undefined && mapping.check.boundary !== slot.check.boundary)
        fail3("RECOVERY_CHECK_WEAKENED", "Replacement checks must preserve the evidence obligation boundary.");
      slot.check = mapping.check;
    }
    if (targeted || mapping.check || mapping.due_step_id !== slot.due_step_id) {
      if (!newIds.has(mapping.due_step_id))
        fail3("RECOVERY_OBLIGATION_INVALID", "Changed evidence must bind a new verification step.");
      slot.due_step_id = mapping.due_step_id;
      slot.report = null;
      slot.evidence_refs = [];
      slot.disposition = "missing";
      delete slot.prerequisite_receipt;
      delete slot.user_decision;
      correctedSlots.add(slot);
    }
  }
  const newPlan = assertEvidencePlan(definition, records, false, { root: root2, taskBasis: basis.basis, previous: current.runtimeState.claim_evidence ?? [] });
  const carry = [];
  for (const record of records)
    for (const slot of record.slots) {
      if (correctedSlots.has(slot))
        continue;
      if (slot.user_decision) {
        const oldSlot = current.runtimeState.claim_evidence?.find((c) => c.claim_id === record.claim_id)?.slots.find((s) => s.slot_id === slot.slot_id);
        if (oldSlot && digest3(oldSlot.check) === digest3(slot.check)) {
          try {
            carry.push(carryUserEvidenceDecision(root2, current, record.claim_id, oldSlot, newPlan, "confirm-replan"));
          } catch {}
        }
        continue;
      }
      if (!slot.report)
        continue;
      if (unresolved.some((item) => item.claim_id === record.claim_id && item.slot_id === slot.slot_id))
        continue;
      const oldSlot = current.runtimeState.claim_evidence?.find((item) => item.claim_id === record.claim_id)?.slots.find((item) => item.slot_id === slot.slot_id);
      if (!oldSlot?.report || !slot.check)
        fail3("REPLAN_CARRY_FORWARD_INVALID", "Candidate evidence source changed.");
      if (!COMPLETE_CLAIM_EVIDENCE_DISPOSITIONS.includes(slot.disposition)) {
        slot.report = null;
        slot.evidence_refs = [];
        slot.disposition = "missing";
        continue;
      }
      assertEvidenceReportApplicable(root2, current, oldSlot);
      const origin = (current.runtimeState.evidence_carry_forward ?? []).find((item) => item.claim_id === record.claim_id && item.slot_id === slot.slot_id && item.result_id === slot.report.result_id && item.new_plan_revision === current.runtimeState.evidence_plan_revision);
      if (origin?.kind === "evidence-carry-forward/v1")
        fail3("EVIDENCE_CARRY_FORWARD_UPGRADE_REQUIRED", "The v1 source lacks preserved evidence bodies; revalidate this affected slot before carrying it again.");
      carry.push({
        kind: "evidence-carry-forward/v2",
        old_source_revision: origin?.old_source_revision ?? current.sourceTuple.revision,
        ...origin?.history_operation ? { history_operation: origin.history_operation } : {},
        ...slot.user_decision ? { user_decision_sha256: digest3(slot.user_decision) } : {},
        old_plan_revision: slot.report.evidence_plan_revision,
        new_plan_revision: newPlan,
        receiving_source_revision: current.sourceTuple.revision,
        evidence_objects: slot.user_decision ? [] : describeEvidenceObjects(root2, slot.evidence_refs),
        context_revision: recoveryEvidenceContextRevision(root2),
        claim_id: record.claim_id,
        slot_id: slot.slot_id,
        check_id: slot.check.check_id,
        result_id: slot.report.result_id,
        report_sha256: digest3(slot.report),
        subject_revision: slot.report.subject_revision
      });
      slot.disposition = "reused";
    }
  return {
    kind: "correction-replan-candidate/v2",
    task_id: current.runtimeState.task_id,
    document_id: current.sourceTuple.document_id,
    problem_keys: [...new Set([
      ...challenges.map((item) => `claim:${item.claim_id}/${item.slot_id}`),
      ...executionTargets.flatMap((item) => {
        const refs = item.execution_result.acceptance_evidence.flatMap((evidence) => ("acceptance" in evidence) ? [] : [`claim:${evidence.claim_id}/${evidence.slot_id}`]);
        const inherited = inheritedRecoveryProblemKeys(current, item.step_id);
        return [...inherited, ...refs.length ? refs : [`execution-step:${item.step_id}`]];
      })
    ])].sort(),
    retained_budget: { review_cycle: current.runtimeState.review_cycle, step_attempts: current.runtimeState.step_attempts ?? {} },
    source_tuple: current.sourceTuple,
    evidence_admission: slots.map(({ record, slot }) => ({
      claim_id: record.claim_id,
      slot_id: slot.slot_id,
      status: carry.some((item) => item.claim_id === record.claim_id && item.slot_id === slot.slot_id) ? "reuse" : slot.report ? "contested" : "revalidate",
      original_result_id: current.runtimeState.claim_evidence?.find((item) => item.claim_id === record.claim_id)?.slots.find((item) => item.slot_id === slot.slot_id)?.report?.result_id ?? null
    })),
    restore_plan: restorePlan,
    result_validity: executionTargets.map((item) => ({ execution_id: item.idempotency_key, status: "affected", recovery_step_id: step.id })),
    historical_completion_refs: current.runtimeState.execution_log.flatMap((item) => !("action" in item) && item.status === "completed" ? [{ execution_id: item.idempotency_key, step_id: item.step_id, definition_revision: digest3(parseImplementationSteps(readDraftDefinitionFromBody(current.body).implementation_steps).find((old) => old.id === item.step_id)) }] : []),
    obligation_map: slots.map(({ record, slot }) => ({
      claim_id: record.claim_id,
      slot_id: slot.slot_id,
      due_step_id: slot.due_step_id,
      ...explicitMap.find((item) => item.claim_id === record.claim_id && item.slot_id === slot.slot_id)
    })),
    evidence_objects: describeEvidenceObjects(root2, [
      ...writes.filter((p) => fs11.existsSync(path12.resolve(root2, p))),
      ...(current.runtimeState.claim_evidence ?? []).flatMap((record) => record.slots.flatMap((slot) => slot.evidence_refs))
    ]),
    source_revision: current.sourceTuple.revision,
    basis_revision: basis.revision,
    old_plan_revision: current.runtimeState.evidence_plan_revision,
    old_obligations: oldObligations,
    old_obligations_digest: digest3(oldObligations),
    scope_diff: { added_paths: [], removed_paths: [] },
    step_diff: { inserted_step_id: step.id, inserted_before: append ? "task-end" : current.runtimeState.active_step_id, retained_step_ids: retainedStepIds },
    challenge_digest: digest3(challenges),
    input,
    definition,
    claim_evidence: records,
    carry_forward: carry,
    new_plan_revision: newPlan,
    permission_change: "none"
  };
}
function prepareCorrectionReplan(root2, rawInput, options = {}) {
  return withGovernanceWriteLock(root2, () => prepareCorrectionReplanLocked(root2, rawInput, options));
}
function prepareCorrectionReplanLocked(root2, rawInput, options) {
  if (!options.dryRun)
    recoverPendingTaskStoreCommit(root2);
  const current = readCanonicalCurrentTask(root2);
  const pendingScope = pendingScopeAmendmentCandidates(current);
  if (pendingScope.length > 0) {
    fail3("REPLAN_CANDIDATE_CONFLICT", `An unconfirmed scope-amendment candidate is already in flight (${pendingScope[0].candidate_path}); discard it before preparing restricted correction.`);
  }
  const input = normalizeCorrectionInput(rawInput);
  const candidate = buildCorrectionCandidate(root2, current, input);
  const candidateDigest = digest3(candidate);
  const location = correctionCandidateLocation(current, candidateDigest);
  const content = JSON.stringify({ ...candidate, candidate_digest: candidateDigest }, null, 2) + `
`;
  const existed = fs11.existsSync(location.filePath);
  const candidateDirectory = path12.dirname(location.filePath);
  if (fs11.existsSync(candidateDirectory)) {
    const prior = fs11.readdirSync(candidateDirectory).filter((item) => item.endsWith(".json"));
    if (prior.length > 128)
      fail3("REPLAN_CANDIDATE_BUDGET_EXHAUSTED", "Task candidate inventory exceeds the bounded limit.");
    let sameChallengeCount = 0;
    let sameProblemCount = 0;
    const committed = new Set(current.runtimeState.execution_log.flatMap((entry) => ("action" in entry) && entry.action === "commit-replan" && entry.candidate_digest ? [`${entry.candidate_digest}.json`] : []));
    for (const name of prior) {
      const discarded = fs11.existsSync(path12.join(candidateDirectory, `${name}.discarded`));
      if (name !== path12.basename(location.filePath) && !discarded && !committed.has(name))
        fail3("REPLAN_CANDIDATE_CONFLICT", "Discard the existing unconfirmed recovery candidate before preparing another batch.");
      let previous;
      try {
        previous = JSON.parse(fs11.readFileSync(path12.join(candidateDirectory, name), "utf8"));
      } catch {
        if (!discarded)
          fail3("REPLAN_CANDIDATE_INVALID", `Unreadable candidate ${name} must be discarded before preparing another.`);
        continue;
      }
      if (previous.problem_keys?.some((key) => candidate.problem_keys.includes(key)))
        sameProblemCount += 1;
      if (!(previous.input?.challenge_ids ?? [previous.input?.challenge_id]).some((id) => id && input.challenge_ids.includes(id)))
        continue;
      sameChallengeCount += 1;
      if (name !== path12.basename(location.filePath) && !discarded) {
        fail3("REPLAN_CANDIDATE_CONFLICT", "Discard the earlier unconfirmed correction candidate before preparing a different one for this challenge.");
      }
    }
    if (sameChallengeCount >= 3 && !existed)
      fail3("REPLAN_CANDIDATE_BUDGET_EXHAUSTED", "Three candidates have already been retained for this challenged result.");
    if (sameProblemCount >= 8 && !existed)
      fail3("REPLAN_CANDIDATE_BUDGET_EXHAUSTED", "Eight candidates already address these task claim/slot or historical-step identities; renaming evidence or recovery IDs cannot reset this budget.");
  }
  if (!options.dryRun) {
    preserveEvidenceObjects(root2, current.filePath, candidate.evidence_objects);
    fs11.mkdirSync(path12.dirname(location.filePath), { recursive: true });
    if (existed) {
      if (fs11.readFileSync(location.filePath, "utf8") !== content)
        fail3("REPLAN_CANDIDATE_CONFLICT", "Existing candidate bytes differ.");
    } else {
      const fd = fs11.openSync(location.filePath, "wx");
      try {
        fs11.writeFileSync(fd, content, "utf8");
        fs11.fsyncSync(fd);
      } finally {
        fs11.closeSync(fd);
      }
    }
    if (fs11.readFileSync(location.filePath, "utf8") !== content)
      fail3("REPLAN_CANDIDATE_READ_BACK_FAILED", "Candidate read-back failed.");
  }
  return {
    status: existed ? "no-op" : "success",
    operation_kind: "task-state-transaction",
    idempotency_key: `prepare-replan-${candidateDigest.slice(0, 40)}`,
    target_path: location.relativePath,
    dry_run: options.dryRun === true,
    committed: !options.dryRun && !existed,
    message: "Restricted correction candidate prepared; CURRENT_TASK is unchanged.",
    planned_writes: [location.relativePath],
    governed_mutation_count: options.dryRun || existed ? 0 : 1,
    read_back_verified: options.dryRun !== true,
    evidence_assurance: "caller-reported",
    candidate_path: location.relativePath,
    candidate_receipt: {
      kind: "correction-replan-candidate-receipt/v2",
      candidate_digest: candidateDigest,
      source_revision: candidate.source_revision,
      basis_revision: candidate.basis_revision,
      obligations_digest: candidate.old_obligations_digest,
      new_plan_revision: candidate.new_plan_revision,
      permission_change: "none"
    }
  };
}
function confirmCorrectionReplan(root2, rawInput, options = {}) {
  return withGovernanceWriteLock(root2, () => confirmCorrectionReplanLocked(root2, rawInput, options));
}
function confirmCorrectionReplanLocked(root2, rawInput, options) {
  const source = expectRecord2(rawInput, "confirm-replan input");
  expectExactKeys2(source, ["candidate_receipt", "authorization"], "confirm-replan input");
  const receipt = expectRecord2(source.candidate_receipt, "candidate_receipt");
  expectExactKeys2(receipt, ["kind", "candidate_digest", "source_revision", "basis_revision", "obligations_digest", "new_plan_revision", "permission_change"], "candidate_receipt");
  if (receipt.kind !== "correction-replan-candidate-receipt/v2" || receipt.permission_change !== "none")
    fail3("REPLAN_CONFIRMATION_INVALID", "Receipt kind or permission change is invalid; prepare a new versioned candidate.");
  for (const field of ["candidate_digest", "source_revision", "basis_revision", "obligations_digest", "new_plan_revision"])
    expectString2(receipt[field], `candidate_receipt.${field}`, /^[a-f0-9]{64}$/);
  const approval = expectRecord2(source.authorization, "authorization");
  expectExactKeys2(approval, ["approved_candidate_digest", "decision_source", "decision_text", "invalidation_reason", ...approval.reactivate_superseded === undefined ? [] : ["reactivate_superseded"]], "authorization");
  if (approval.approved_candidate_digest !== receipt.candidate_digest)
    fail3("REPLAN_CONFIRMATION_INVALID", "Authorization does not name the exact candidate digest.");
  const decisionSource = expectText(approval.decision_source, "authorization.decision_source", 512);
  const decisionText = expectText(approval.decision_text, "authorization.decision_text", 4096);
  const invalidationReason = expectText(approval.invalidation_reason, "authorization.invalidation_reason", 1024);
  if (!options.dryRun)
    recoverPendingTaskStoreCommit(root2);
  const current = readCanonicalCurrentTask(root2);
  const candidateDigest = receipt.candidate_digest;
  const location = correctionCandidateLocation(current, candidateDigest);
  const idempotencyKey = `confirm-replan-${digest3({ receipt, approval }).slice(0, 40)}`;
  const previousCommit = current.runtimeState.execution_log.find((item) => ("action" in item) && item.action === "commit-replan" && item.evidence_refs.includes(location.relativePath));
  if (previousCommit) {
    if (previousCommit.idempotency_key !== idempotencyKey)
      fail3("REPLAN_ALREADY_COMMITTED", "This candidate was confirmed with different authorization semantics.");
    assertTaskHistoryForRevision(current.filePath, current.sourceTuple.document_id, current.runtimeState.task_id, previousCommit.source_revision, "confirm-replan");
    return {
      status: "no-op",
      operation_kind: "task-state-transaction",
      idempotency_key: idempotencyKey,
      target_path: current.relativePath,
      dry_run: options.dryRun === true,
      committed: false,
      message: "Exact correction confirmation was already committed.",
      planned_writes: [],
      governed_mutation_count: 0,
      read_back_verified: true,
      resulting_revision: current.sourceTuple.revision,
      evidence_assurance: "caller-reported",
      state: resultState(current.runtimeState)
    };
  }
  if (current.runtimeState.task_evolution_version !== 2)
    fail3("TASK_EVOLUTION_INITIALIZATION_REQUIRED", "Initialize v2 task preservation before confirming recovery for an older task.");
  if (current.runtimeState.workflow_status === "superseded") {
    const lastSupersede = current.runtimeState.execution_log.findLast((item) => ("action" in item) && item.action === "supersede");
    if (!lastSupersede || !("invalidation_kind" in lastSupersede) || lastSupersede.invalidation_kind !== "acceptance" || approval.reactivate_superseded !== true) {
      fail3("REPLAN_SUPERSEDED_AUTHORITY_REQUIRED", "A superseded task can use restricted correction only for an acceptance-result invalidation with explicit reactivation approval.");
    }
  } else if (approval.reactivate_superseded !== undefined) {
    fail3("REPLAN_CONFIRMATION_INVALID", "An active task cannot carry superseded-reactivation approval.");
  }
  if (!taskSourceRevisionMatches(root2, current, receipt.source_revision))
    fail3("REPLAN_SOURCE_STALE", "CURRENT_TASK changed after candidate preparation.");
  const basis = readCanonicalTaskBasis(root2, current);
  if (basis.revision !== receipt.basis_revision || digest3(correctionObligations(current)) !== receipt.obligations_digest)
    fail3("REPLAN_OBLIGATIONS_STALE", "Task Basis or old obligations changed after candidate preparation.");
  if (!fs11.existsSync(location.filePath) || fs11.existsSync(`${location.filePath}.discarded`))
    fail3("REPLAN_CANDIDATE_MISSING", "Candidate is absent or discarded.");
  const saved = JSON.parse(fs11.readFileSync(location.filePath, "utf8"));
  if (saved.candidate_digest !== candidateDigest)
    fail3("REPLAN_CANDIDATE_INVALID", "Candidate digest marker differs.");
  const { candidate_digest: _marker, ...storedCandidate } = saved;
  if (digest3(storedCandidate) !== candidateDigest)
    fail3("REPLAN_CANDIDATE_INVALID", "Candidate content changed after preparation.");
  const candidateSource = { ...current, sourceTuple: { ...current.sourceTuple, revision: receipt.source_revision } };
  const rebuilt = buildCorrectionCandidate(root2, candidateSource, normalizeCorrectionInput(saved.input));
  if (digest3(rebuilt) !== candidateDigest || rebuilt.new_plan_revision !== receipt.new_plan_revision || rebuilt.old_obligations_digest !== receipt.obligations_digest || rebuilt.basis_revision !== receipt.basis_revision) {
    fail3("REPLAN_CANDIDATE_STALE", "Candidate no longer matches the Runtime-computed source, obligations, and evidence.");
  }
  if (receipt.source_revision !== current.sourceTuple.revision) {
    rebuilt.carry_forward = rebuilt.carry_forward.map((proof) => ({
      ...proof,
      old_source_revision: proof.old_source_revision === receipt.source_revision ? current.sourceTuple.revision : proof.old_source_revision,
      receiving_source_revision: proof.receiving_source_revision === receipt.source_revision ? current.sourceTuple.revision : proof.receiving_source_revision
    }));
  }
  if (current.runtimeState.workflow_status !== "blocked_by_replan" && current.runtimeState.active_step_status !== "completed" && current.runtimeState.step_attempts?.[current.runtimeState.active_step_id]?.attempts.length)
    fail3("REPLAN_ACTIVE_ATTEMPT_PRESENT", "Suspend the retained current attempt before preparing its recovery.");
  const challenges = (current.runtimeState.evidence_challenges ?? []).filter((item) => rebuilt.input.challenge_ids.includes(item.challenge_id));
  const challengeRefs = challenges.map((item) => item.evidence_ref);
  const nextBasis = { original_request: basis.basis.original_request, user_decisions: [...basis.basis.user_decisions, { source: decisionSource, verbatim: decisionText }] };
  const nextBasisArtifact = materializeTaskBasis(root2, current, {
    task_id: current.runtimeState.task_id,
    task_slug: current.runtimeState.task_slug,
    task_title: extractTaskIdentityFromCurrentTask(current.body).title,
    document_id: current.sourceTuple.document_id
  }, nextBasis);
  const authorityEvidence = ["active-task-owner", "scope-admission", "evidence-admission", "authorized-caller"].map((kind) => ({
    kind,
    source: decisionSource,
    subject: candidateDigest,
    task_id: current.runtimeState.task_id,
    document_id: current.sourceTuple.document_id,
    source_revision: current.sourceTuple.revision
  }));
  const proposal = createPrepareTaskReplanProposal(current, {
    delta: {
      kind: "task-state",
      action: "commit-replan",
      task_basis: nextBasis,
      replacement_definition: rebuilt.definition,
      active_step_id: rebuilt.input.correction_step.id,
      claim_evidence: rebuilt.claim_evidence,
      evidence_refs: [location.relativePath, ...challengeRefs]
    },
    idempotency_key: idempotencyKey,
    authority_evidence: authorityEvidence,
    evidence_refs: [location.relativePath, ...challengeRefs]
  });
  const oldState = current.runtimeState;
  const domainRevision = current.mutationAuthority ? current.runtimeState.authority_domain_revision : undefined;
  const nextWithoutAudit = {
    ...oldState,
    workflow_status: "active",
    lifecycle_state: "active",
    active_step_id: rebuilt.input.correction_step.id,
    ...oldState.review_coverage ? { review_coverage: { ...oldState.review_coverage, last_clean_revision: null } } : {},
    active_step_status: "ready",
    evidence_plan_revision: rebuilt.new_plan_revision,
    claim_evidence: rebuilt.claim_evidence,
    evidence_carry_forward: rebuilt.carry_forward,
    evidence_challenges: (current.runtimeState.evidence_challenges ?? []).map((item) => rebuilt.input.challenge_ids.includes(item.challenge_id) ? { ...item, status: "invalidated", correction_step_id: rebuilt.input.correction_step.id } : item),
    pending_review_result: null,
    ...domainRevision ? { authority_domain_revision: domainRevision } : {},
    review_cycle: reviewCycleForNextStep(current.runtimeState.review_cycle.id, rebuilt.input.correction_step.id, idempotencyKey),
    applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
  };
  const audit = { ...makeReplanAudit(current, proposal, nextWithoutAudit, options.now?.() ?? new Date().toISOString()), candidate_digest: candidateDigest, correction_reason: invalidationReason };
  const nextState = { ...nextWithoutAudit, execution_log: appendExecutionLogEntry(current.runtimeState, audit) };
  const prepared = renderCanonicalCurrentTask(current.frontmatter, current.body, nextState, { replacementDefinition: rebuilt.definition, taskBasisReference: { path: basis.path, revision: nextBasisArtifact.revision }, audit });
  const nextContent = prepared.content;
  const preview = parseCanonicalCurrentTaskContent(nextContent, current.filePath, current.relativePath, prepared);
  if (preview.runtimeState.evidence_plan_revision !== rebuilt.new_plan_revision || preview.runtimeState.active_step_id !== rebuilt.input.correction_step.id) {
    fail3("REPLAN_CANDIDATE_INVALID", "Rendered correction does not match the admitted plan and step.");
  }
  const history = taskHistoryLocation({
    currentPath: current.filePath,
    previousContent: current.raw,
    nextContent,
    documentId: current.sourceTuple.document_id,
    taskId: current.runtimeState.task_id,
    basisPath: basis.filePath,
    basisContent: basis.content,
    nextBasisContent: nextBasisArtifact.content,
    operation: "confirm-replan",
    evidencePlanRevision: current.runtimeState.evidence_plan_revision,
    referencedEvidence: [...challengeRefs, ...rebuilt.carry_forward.flatMap((item) => {
      const slot = rebuilt.claim_evidence.find((record) => record.claim_id === item.claim_id)?.slots.find((entry) => entry.slot_id === item.slot_id);
      return slot?.evidence_refs ?? [];
    })]
  });
  const plannedWrites = [path12.posix.join(path12.posix.dirname(current.relativePath), history.relativePath), basis.path, current.relativePath];
  if (options.dryRun)
    return {
      status: "success",
      operation_kind: "task-state-transaction",
      idempotency_key: idempotencyKey,
      target_path: current.relativePath,
      dry_run: true,
      committed: false,
      message: "Correction confirmation dry run passed; no live task was changed.",
      planned_writes: plannedWrites,
      governed_mutation_count: 0,
      read_back_verified: false,
      evidence_assurance: "caller-reported"
    };
  let stagedAfter;
  try {
    stagedAfter = stageTaskEvolutionStoreCommit(root2, current, nextContent, nextState, proposal, [
      { path: history.path, content: history.content },
      { path: nextBasisArtifact.filePath, content: nextBasisArtifact.content },
      { path: current.filePath, content: nextContent }
    ], prepared);
  } catch (error) {
    return buildResult("blocked", proposal, current, options, `task-store precommit staging failed: ${error instanceof Error ? error.message : String(error)}`, {
      code: error instanceof TaskStoreError ? error.code : "TASK_STORE_COMMIT_FAILED"
    });
  }
  try {
    commitTaskEvolutionWithHistory({
      currentPath: current.filePath,
      previousContent: current.raw,
      nextContent,
      documentId: current.sourceTuple.document_id,
      taskId: current.runtimeState.task_id,
      basisPath: basis.filePath,
      basisContent: basis.content,
      nextBasisContent: nextBasisArtifact.content,
      operation: "confirm-replan",
      evidencePlanRevision: current.runtimeState.evidence_plan_revision,
      referencedEvidence: challengeRefs
    }, (content) => {
      const parsed = parseCanonicalCurrentTaskContent(content, current.filePath, current.relativePath);
      if (parsed.runtimeState.evidence_plan_revision !== rebuilt.new_plan_revision || parsed.runtimeState.active_step_id !== rebuilt.input.correction_step.id || readCanonicalTaskBasis(root2, parsed).revision !== nextBasisArtifact.revision)
        fail3("REPLAN_READ_BACK_FAILED", "Correction task/Basis read-back is inconsistent.");
    });
  } catch (error) {
    if (fs11.existsSync(current.filePath) && sha2565(fs11.readFileSync(current.filePath, "utf8")) === current.sourceTuple.revision) {
      clearPendingTaskStoreAfterRollback(root2, current);
    }
    throw error;
  }
  const storeResult = {
    status: "success",
    committed: true,
    operation_kind: "task-state-transaction",
    idempotency_key: idempotencyKey,
    message: "Restricted correction confirmed; old obligations, original next step, and immutable preimage retained."
  };
  try {
    const manifest = completeTaskEvolutionStoreCommit(root2, current, stagedAfter, proposal, storeResult);
    const readBack = readCanonicalCurrentTask(root2);
    return {
      status: "success",
      operation_kind: "task-state-transaction",
      idempotency_key: idempotencyKey,
      target_path: current.relativePath,
      dry_run: false,
      committed: true,
      message: storeResult.message,
      planned_writes: plannedWrites,
      governed_mutation_count: 3,
      read_back_verified: readBack.raw === nextContent,
      previous_revision: current.sourceTuple.revision,
      resulting_revision: readBack.sourceTuple.revision,
      evidence_assurance: "caller-reported",
      state: resultState(readBack.runtimeState),
      task_store: {
        manifest_path: `${manifest.storage_root}/manifest.json`,
        source_revision: manifest.head.source_revision,
        definition_revision: manifest.head.definition_revision,
        state_revision: manifest.head.state_revision,
        event_sequence: manifest.head.event_sequence
      }
    };
  } catch (error) {
    return {
      status: "blocked",
      operation_kind: "task-state-transaction",
      idempotency_key: idempotencyKey,
      target_path: current.relativePath,
      dry_run: false,
      committed: true,
      message: `Correction committed but task-store publication needs recovery: ${error instanceof Error ? error.message : String(error)}`,
      planned_writes: plannedWrites,
      governed_mutation_count: 3,
      read_back_verified: false,
      previous_revision: current.sourceTuple.revision,
      resulting_revision: stagedAfter.sourceTuple.revision,
      evidence_assurance: "caller-reported",
      code: error instanceof TaskStoreError ? error.code : "TASK_STORE_COMMIT_FAILED",
      state: resultState(stagedAfter.runtimeState)
    };
  }
}
function discardCorrectionReplan(root2, rawInput, options = {}) {
  return withGovernanceWriteLock(root2, () => discardCorrectionReplanLocked(root2, rawInput, options));
}
function discardCorrectionReplanLocked(root2, rawInput, options) {
  if (!options.dryRun)
    recoverPendingTaskStoreCommit(root2);
  const input = expectRecord2(rawInput, "discard-replan input");
  expectExactKeys2(input, ["candidate_digest"], "discard-replan input");
  const current = readCanonicalCurrentTask(root2);
  const candidateDigest = expectString2(input.candidate_digest, "candidate_digest", /^[a-f0-9]{64}$/);
  const location = correctionCandidateLocation(current, candidateDigest);
  if (!fs11.existsSync(location.filePath))
    fail3("REPLAN_CANDIDATE_MISSING", "Candidate does not exist.");
  const marker = `${location.filePath}.discarded`;
  const existed = fs11.existsSync(marker);
  if (!options.dryRun && !existed)
    fs11.writeFileSync(marker, `${candidateDigest}
`, { flag: "wx" });
  return {
    status: existed ? "no-op" : "success",
    operation_kind: "task-state-transaction",
    idempotency_key: `discard-replan-${candidateDigest.slice(0, 40)}`,
    target_path: location.relativePath,
    dry_run: options.dryRun === true,
    committed: !options.dryRun && !existed,
    message: "Candidate discarded; CURRENT_TASK lifecycle was not changed.",
    planned_writes: [`${location.relativePath}.discarded`],
    governed_mutation_count: options.dryRun || existed ? 0 : 1,
    read_back_verified: options.dryRun !== true,
    evidence_assurance: "caller-reported"
  };
}
function artifactRestoreCompletion(root2, current, candidate, attempt = current.runtimeState.step_attempts?.[candidate.input.correction_step.id]?.attempts.at(-1), originCompletion) {
  if (!attempt)
    fail3("ARTIFACT_RESTORE_PREFLIGHT_REQUIRED", "Restore requires a durable attempt.");
  const receipt = {
    kind: originCompletion ? "artifact-restore-completion/v2" : "artifact-restore-completion/v1",
    task_id: current.runtimeState.task_id,
    document_id: current.sourceTuple.document_id,
    candidate_digest: digest3(candidate),
    plan_revision: candidate.new_plan_revision,
    step_id: candidate.input.correction_step.id,
    attempt_id: attempt.attempt_id,
    preflight_id: attempt.idempotency_key,
    restore_plan_digest: digest3(candidate.restore_plan),
    ...originCompletion ? { origin_completion: originCompletion } : {}
  };
  const id = digest3(receipt);
  const location = path12.join(path12.dirname(current.filePath), "task-history", current.sourceTuple.document_id, "artifact-restores", `${id}.json`);
  const file = safeRepositoryFile(root2, path12.relative(root2, location).replace(/\\/g, "/"));
  return { id, file, bytes: JSON.stringify(receipt) + `
` };
}
function restoreCompletionMatches(completion) {
  return fs11.existsSync(completion.file) && fs11.statSync(completion.file).size === Buffer.byteLength(completion.bytes) && fs11.readFileSync(completion.file, "utf8") === completion.bytes;
}
function restoreCompletionOrigin(root2, current, candidate) {
  for (const attempt of current.runtimeState.step_attempts?.[candidate.input.correction_step.id]?.attempts ?? []) {
    const origin = artifactRestoreCompletion(root2, current, candidate, attempt);
    if (restoreCompletionMatches(origin))
      return origin;
  }
  return null;
}
function currentRestoreCompletion(root2, current, candidate) {
  const direct = artifactRestoreCompletion(root2, current, candidate);
  if (fs11.existsSync(direct.file))
    return restoreCompletionMatches(direct) ? direct : null;
  const origin = restoreCompletionOrigin(root2, current, candidate);
  if (!origin)
    return null;
  const revalidated = artifactRestoreCompletion(root2, current, candidate, undefined, origin.id);
  return restoreCompletionMatches(revalidated) ? revalidated : null;
}
function persistRestoreCompletion(completion) {
  fs11.mkdirSync(path12.dirname(completion.file), { recursive: true });
  if (!fs11.existsSync(completion.file)) {
    const fd = fs11.openSync(completion.file, "wx");
    try {
      fs11.writeFileSync(fd, completion.bytes);
      fs11.fsyncSync(fd);
    } finally {
      fs11.closeSync(fd);
    }
  }
  if (!restoreCompletionMatches(completion))
    fail3("ARTIFACT_RESTORE_COMPLETION_INVALID", "Immutable restore completion differs.");
}
function assertArtifactRestoreCompleted(root2, current) {
  const candidate = confirmedRecoveryCandidates(current).find((item) => item.input.correction_step.id === current.runtimeState.active_step_id && item.restore_plan);
  if (!candidate)
    return;
  if (!currentRestoreCompletion(root2, current, candidate))
    fail3("ARTIFACT_RESTORE_COMPLETION_REQUIRED", "Run or revalidate the confirmed Runtime restore for this exact attempt before reporting success.");
  if (digest3(captureArtifactImages(root2, current.filePath, candidate.restore_plan.targets.map((image) => image.path))) !== digest3(candidate.restore_plan.targets))
    fail3("ARTIFACT_RESTORE_TARGET_STALE", "Restored paths changed; the confirmed target images are required at result and completion. Put forward edits in a subsequent recovery step.");
}
function executeConfirmedArtifactRestore(root2, sourceRevision, stepId, candidatePaths, dryRun = false) {
  return withGovernanceWriteLock(root2, () => {
    if (!dryRun)
      recoverPendingTaskStoreCommit(root2);
    const current = readCanonicalCurrentTask(root2);
    if (!taskSourceRevisionMatches(root2, current, sourceRevision) || current.runtimeState.active_step_id !== stepId)
      fail3("ARTIFACT_RESTORE_STALE", "Preflight task source changed.");
    assertOrdinaryPreflight(current, root2);
    const audit = current.runtimeState.execution_log.findLast((item) => ("action" in item) && item.action === "commit-replan");
    if (!audit || !("candidate_digest" in audit) || !audit.candidate_digest)
      fail3("ARTIFACT_RESTORE_UNAUTHORIZED", "No confirmed recovery candidate.");
    const location = correctionCandidateLocation(current, audit.candidate_digest);
    const { candidate_digest: marker, ...candidate } = JSON.parse(fs11.readFileSync(location.filePath, "utf8"));
    if (marker !== audit.candidate_digest || digest3(candidate) !== marker || candidate.kind !== "correction-replan-candidate/v2" || candidate.input.correction_step.id !== stepId || !candidate.restore_plan || candidate.new_plan_revision !== current.runtimeState.evidence_plan_revision)
      fail3("ARTIFACT_RESTORE_UNAUTHORIZED", "Restore must bind the exact confirmed candidate and first recovery step.");
    const paths = candidate.restore_plan.targets.map((item) => item.path);
    if (paths.some((p) => !candidatePaths.includes(p)) || evaluateMutationScope(parseMutationScope(current.body), { changed_paths: paths }).status !== "pass")
      fail3("ARTIFACT_RESTORE_SCOPE", "Restore writes exceed current preflight or task scope.");
    if (current.runtimeState.step_attempts?.[stepId]?.attempts.at(-1)?.status !== "preflighted")
      fail3("ARTIFACT_RESTORE_PREFLIGHT_REQUIRED", "Restore needs the current recorded preflight.");
    const checked = prepareArtifactRestore(root2, current.filePath, current.runtimeState.task_id, current.sourceTuple.document_id, candidate.restore_plan.checkpoint_id, paths);
    if (digest3(checked.checkpoint) !== digest3(candidate.restore_plan.checkpoint) || digest3(checked.targets) !== digest3(candidate.restore_plan.targets))
      fail3("ARTIFACT_RESTORE_STALE", "The confirmed checkpoint changed.");
    if (digest3(checked.expected) === digest3(candidate.restore_plan.targets)) {
      const existing = currentRestoreCompletion(root2, current, candidate);
      if (existing)
        return { status: "no-op", committed: false, evidence_assurance: "caller-reported", execution_kind: "revalidated", restored_paths: paths };
      const origin = restoreCompletionOrigin(root2, current, candidate);
      if (origin) {
        const completion = artifactRestoreCompletion(root2, current, candidate, undefined, origin.id);
        if (!dryRun)
          persistRestoreCompletion(completion);
        return {
          status: "success",
          committed: !dryRun,
          evidence_assurance: "caller-reported",
          execution_kind: "revalidated",
          restored_paths: paths,
          origin_completion: origin.id,
          next: "Run fresh post-restore checks and complete required review for this attempt."
        };
      }
      if (digest3(checked.expected) !== digest3(candidate.restore_plan.expected))
        fail3("ARTIFACT_RESTORE_COMPLETION_REQUIRED", "Matching target files alone cannot prove an earlier Runtime restore.");
    }
    if (digest3(checked.expected) !== digest3(candidate.restore_plan.expected))
      fail3("ARTIFACT_RESTORE_STALE", "Current files changed after confirmation.");
    const completion = artifactRestoreCompletion(root2, current, candidate);
    if (!dryRun)
      applyArtifactRestore(root2, current.filePath, candidate.restore_plan, undefined, () => persistRestoreCompletion(completion));
    return {
      status: "success",
      committed: !dryRun,
      evidence_assurance: "caller-reported",
      execution_kind: "restored",
      restored_paths: paths,
      next: "Run the declared post-restore checks, record-step-result, and complete the required review."
    };
  });
}
function listArtifactCheckpoints(root2) {
  const current = readCanonicalCurrentTask(root2);
  const directory = path12.join(path12.dirname(current.filePath), "task-history", current.sourceTuple.document_id, "artifact-checkpoints");
  const names = fs11.existsSync(directory) ? fs11.readdirSync(directory).filter((name) => /^[a-f0-9]{64}\.json$/.test(name)) : [];
  if (names.length > 256)
    fail3("ARTIFACT_BUDGET_EXHAUSTED", "Checkpoint inventory exceeds the bounded summary limit.");
  return { status: "success", checkpoints: names.map((name) => {
    const checkpoint = JSON.parse(fs11.readFileSync(path12.join(directory, name), "utf8"));
    if (digest3(checkpoint) !== name.slice(0, -5))
      fail3("ARTIFACT_CHECKPOINT_CORRUPT", "Checkpoint digest differs.");
    return {
      checkpoint_id: name.slice(0, -5),
      step_id: checkpoint.step_id,
      execution_id: checkpoint.execution_id,
      phase: checkpoint.phase,
      committed: current.runtimeState.artifact_checkpoint_ids?.includes(name.slice(0, -5)) === true,
      paths: checkpoint.images.map((image) => image.path)
    };
  }) };
}
function currentDefinitionExecutionLog(current) {
  const log = current.runtimeState.execution_log;
  const lastCorrection = log.findLastIndex((item) => ("action" in item) && item.action === "commit-replan");
  const lastScopeAmendment = log.findLastIndex((item) => ("action" in item) && item.action === "commit-scope-amendment");
  if (current.runtimeState.scope_amendment_pending_review_step_id !== undefined && lastScopeAmendment > lastCorrection) {
    return log.slice(lastCorrection + 1);
  }
  return log.slice(Math.max(lastCorrection, lastScopeAmendment) + 1);
}
function implementationStepBlock(definition, stepId) {
  return definition.implementation_steps.split(/(?=^-\s*[A-Za-z0-9][A-Za-z0-9._:-]*\s*[:：])/m).find((block) => block.startsWith(`- ${stepId}:`) || block.startsWith(`- ${stepId}：`))?.trimEnd();
}
function confirmedRecoveryCandidates(current) {
  return current.runtimeState.execution_log.flatMap((entry) => {
    if (!("action" in entry) || entry.action !== "commit-replan" || !entry.candidate_digest)
      return [];
    const { candidate_digest: marker, ...candidate } = JSON.parse(fs11.readFileSync(correctionCandidateLocation(current, entry.candidate_digest).filePath, "utf8"));
    if (marker !== entry.candidate_digest || digest3(candidate) !== marker)
      fail3("RECOVERY_HISTORY_CORRUPT", "Confirmed recovery candidate changed.");
    return candidate.kind === "correction-replan-candidate/v2" ? [candidate] : [];
  });
}
function recoveryOwnedStepIds(candidate) {
  return [candidate.input.correction_step.id, ...candidate.input.recovery_steps.map((step) => step.id), ...(candidate.input.pending_step_changes?.steps ?? []).map((step) => step.id)];
}
function inheritedRecoveryProblemKeys(current, stepId) {
  const candidates = confirmedRecoveryCandidates(current);
  const pending = [stepId], visited = new Set, keys = new Set;
  while (pending.length) {
    const id = pending.pop();
    if (visited.has(id))
      continue;
    visited.add(id);
    for (const candidate of candidates)
      if (recoveryOwnedStepIds(candidate).includes(id)) {
        for (const key of candidate.problem_keys)
          keys.add(key);
        for (const target of candidate.input.execution_targets) {
          const execution = current.runtimeState.execution_log.find((entry) => !("action" in entry) && entry.idempotency_key === target.execution_id);
          if (execution && !("action" in execution))
            pending.push(execution.step_id);
        }
        for (const mapping of candidate.input.pending_step_changes?.step_map ?? [])
          pending.push(mapping.old_step_id);
      }
  }
  return [...keys];
}
function challengeReportIsRetained(root2, current, challenge, currentResultId) {
  if (challenge.result_id === currentResultId)
    return true;
  const edges = new Map;
  for (const candidate of confirmedRecoveryCandidates(current)) {
    const stepId = candidate.input.correction_step.id;
    const parents = (current.runtimeState.evidence_challenges ?? []).filter((item) => candidate.input.challenge_ids.includes(item.challenge_id) && item.claim_id === challenge.claim_id && item.slot_id === challenge.slot_id && item.correction_step_id === stepId && item.status === "resolved");
    if (!parents.length)
      continue;
    const completion = current.runtimeState.execution_log.find((entry) => !("action" in entry) && entry.step_id === stepId && entry.status === "completed" && entry.review_receipt?.verdict === "clean");
    if (!completion || "action" in completion)
      continue;
    const slot = completion.claim_evidence?.find((item) => item.claim_id === challenge.claim_id)?.slots.find((item) => item.slot_id === challenge.slot_id);
    if (!slot?.report || !slot.check || slot.report.status !== slot.check.expected_result)
      continue;
    const executed = current.runtimeState.execution_log.some((entry) => !("action" in entry) && entry.step_id === stepId && entry.execution_result?.acceptance_evidence.some((evidence) => !("acceptance" in evidence) && evidence.claim_id === challenge.claim_id && evidence.slot_id === challenge.slot_id && evidence.check_id === slot.check.check_id && digest3(evidence.report) === digest3(slot.report)));
    if (!executed)
      continue;
    const confirmation = current.runtimeState.execution_log.find((entry) => ("action" in entry) && entry.action === "commit-replan" && entry.candidate_digest === digest3(candidate));
    if (!confirmation || !("source_revision" in confirmation))
      fail3("RECOVERY_HISTORY_CORRUPT", "Recovery candidate has no committed confirmation source.");
    const historyRevision = confirmation.source_revision;
    assertTaskHistoryForRevision(current.filePath, current.sourceTuple.document_id, current.runtimeState.task_id, historyRevision, "confirm-replan");
    const historyPath = path12.join(path12.dirname(current.filePath), "task-history", current.sourceTuple.document_id, `${historyRevision}.json`);
    const history = JSON.parse(fs11.readFileSync(safeRepositoryFile(root2, path12.relative(root2, historyPath).replace(/\\/g, "/")), "utf8"));
    const source = parseCanonicalCurrentTaskContent(Buffer.from(history.current_task_base64, "base64").toString("utf8"), current.filePath, current.relativePath);
    const replaced = source.runtimeState.claim_evidence?.find((item) => item.claim_id === challenge.claim_id)?.slots.find((item) => item.slot_id === challenge.slot_id)?.report;
    if (!replaced)
      continue;
    for (const resultId of new Set([replaced.result_id, ...parents.map((parent) => parent.result_id)])) {
      const outputs = edges.get(resultId) ?? new Set;
      outputs.add(slot.report.result_id);
      edges.set(resultId, outputs);
    }
  }
  const pending = [challenge.result_id], visited = new Set;
  while (pending.length) {
    const id = pending.pop();
    if (id === currentResultId)
      return true;
    if (visited.has(id))
      continue;
    visited.add(id);
    pending.push(...edges.get(id) ?? []);
  }
  return false;
}
function recoveryProblemBudget(current) {
  const candidates = confirmedRecoveryCandidates(current);
  const recoveryIds = recoveryOwnedStepIds;
  const active = candidates.find((candidate) => recoveryIds(candidate).includes(current.runtimeState.active_step_id));
  if (!active)
    return null;
  const keys = new Set([...active.problem_keys, ...inheritedRecoveryProblemKeys(current, current.runtimeState.active_step_id)]);
  for (let pass = 0;pass < candidates.length; pass++) {
    for (const candidate of candidates)
      if (candidate.problem_keys.some((key) => keys.has(key))) {
        for (const key of candidate.problem_keys)
          keys.add(key);
      }
  }
  const steps = new Set;
  const repairWaves = new Set;
  for (const candidate of candidates)
    if (candidate.problem_keys.some((key) => keys.has(key))) {
      for (const id of recoveryIds(candidate))
        steps.add(id);
      for (const mapping of candidate.input.pending_step_changes?.step_map ?? [])
        steps.add(mapping.old_step_id);
      for (const wave of candidate.retained_budget.review_cycle.counted_repair_wave_ids)
        repairWaves.add(wave);
    }
  for (const entry of current.runtimeState.execution_log)
    if (!("action" in entry)) {
      if (keys.has(`execution-step:${entry.step_id}`) || entry.execution_result?.acceptance_evidence.some((evidence) => !("acceptance" in evidence) && keys.has(`claim:${evidence.claim_id}/${evidence.slot_id}`)))
        steps.add(entry.step_id);
    }
  for (const entry of current.runtimeState.execution_log)
    if (!("action" in entry) && steps.has(entry.step_id) && entry.repair_wave_id)
      repairWaves.add(entry.repair_wave_id);
  for (const wave of current.runtimeState.review_cycle.counted_repair_wave_ids)
    repairWaves.add(wave);
  const failedAttempts = [...steps].reduce((count, id) => count + (current.runtimeState.step_attempts?.[id]?.attempts.filter((attempt) => attempt.blocker !== null).length ?? 0), 0);
  return { failedAttempts, repairWaves };
}
function inheritedScopeAmendmentAttemptLedger(root2, current, stepId) {
  let childStepId = stepId;
  const visited = new Set;
  const audits = current.runtimeState.execution_log.filter((item) => ("action" in item) && item.action === "commit-scope-amendment" && item.candidate_digest);
  for (let depth = 0;depth < 128; depth += 1) {
    if (visited.has(childStepId))
      fail3("SCOPE_AMENDMENT_HISTORY_CORRUPT", "Scope-amendment continuation ancestry contains a cycle.");
    visited.add(childStepId);
    let match = null;
    for (const entry of [...audits].reverse()) {
      if (!("candidate_digest" in entry) || !entry.candidate_digest)
        continue;
      const location = scopeAmendmentCandidateLocation(current, entry.candidate_digest);
      if (!fs11.existsSync(location.filePath))
        fail3("SCOPE_AMENDMENT_HISTORY_CORRUPT", "Confirmed scope-amendment candidate is missing while inheriting an attempt budget.");
      let candidate;
      try {
        candidate = JSON.parse(fs11.readFileSync(location.filePath, "utf8"));
      } catch (error) {
        fail3("SCOPE_AMENDMENT_HISTORY_CORRUPT", `Confirmed scope-amendment candidate cannot be read: ${error instanceof Error ? error.message : String(error)}`);
      }
      const { candidate_digest: marker, ...candidateContent } = candidate;
      if (marker !== entry.candidate_digest || digest3(candidateContent) !== marker || candidate.task_id !== current.runtimeState.task_id || candidate.document_id !== current.sourceTuple.document_id) {
        fail3("SCOPE_AMENDMENT_HISTORY_CORRUPT", "Confirmed scope-amendment candidate changed or belongs to another task.");
      }
      if (candidate.step_diff.inserted_step_id === childStepId) {
        match = candidate;
        break;
      }
    }
    if (!match)
      return null;
    const candidate = match;
    const priorStepId = candidate.continuation.prior_step_id;
    const ledger = current.runtimeState.step_attempts?.[priorStepId];
    if (ledger)
      return { prior_step_id: priorStepId, ledger };
    childStepId = priorStepId;
  }
  fail3("SCOPE_AMENDMENT_HISTORY_CORRUPT", "Scope-amendment continuation ancestry exceeds the supported depth.");
}
function assertRecoveryHistory(root2, current) {
  for (const event of current.runtimeState.execution_log) {
    if (!("action" in event) || event.action !== "create-draft" || !event.predecessor)
      continue;
    const d = event.predecessor;
    const file = safeRepositoryFile(root2, successorSnapshotPath(current.relativePath, d.document_id, d.source_revision));
    if (sha2565(fs11.readFileSync(file, "utf8")) !== d.source_revision)
      fail3("SUCCESSOR_HISTORY_CONFLICT", "The exact predecessor task must remain retained.");
  }
  for (const entry of current.runtimeState.execution_log) {
    if (!("action" in entry) || !["commit-replan", "commit-scope-amendment"].includes(entry.action) || !("candidate_digest" in entry) || !entry.candidate_digest)
      continue;
    if (entry.action === "commit-scope-amendment") {
      const location = scopeAmendmentCandidateLocation(current, entry.candidate_digest);
      if (!fs11.existsSync(location.filePath))
        fail3("SCOPE_AMENDMENT_HISTORY_CORRUPT", "Confirmed scope-amendment candidate is missing.");
      const { candidate_digest: marker, ...candidate } = JSON.parse(fs11.readFileSync(location.filePath, "utf8"));
      if (digest3(candidate) !== marker || marker !== entry.candidate_digest || candidate.task_id !== current.runtimeState.task_id || candidate.document_id !== current.sourceTuple.document_id)
        fail3("SCOPE_AMENDMENT_HISTORY_CORRUPT", "Confirmed scope-amendment candidate changed or belongs to another task.");
      assertScopeAmendmentHistoryForRevision(current, entry.source_revision);
      continue;
    }
    const location = correctionCandidateLocation(current, entry.candidate_digest);
    const { candidate_digest: marker, ...candidate } = JSON.parse(fs11.readFileSync(location.filePath, "utf8"));
    if (digest3(candidate) !== marker || marker !== entry.candidate_digest || candidate.task_id !== current.runtimeState.task_id || candidate.document_id !== current.sourceTuple.document_id)
      fail3("RECOVERY_HISTORY_CORRUPT", "Confirmed recovery candidate changed or belongs to another task.");
    if (candidate.kind === "correction-replan-candidate/v1")
      continue;
    if (candidate.kind !== "correction-replan-candidate/v2")
      fail3("RECOVERY_HISTORY_VERSION_UNSUPPORTED", "Unknown confirmed recovery protocol version.");
    const definition = readDraftDefinitionFromBody(current.body);
    assertTaskHistoryForRevision(current.filePath, current.sourceTuple.document_id, current.runtimeState.task_id, entry.source_revision, "confirm-replan");
    const historyFile = path12.join(path12.dirname(current.filePath), "task-history", current.sourceTuple.document_id, `${entry.source_revision}.json`);
    const history = JSON.parse(fs11.readFileSync(historyFile, "utf8"));
    const old = parseCanonicalCurrentTaskContent(Buffer.from(history.current_task_base64, "base64").toString("utf8"), current.filePath, current.relativePath);
    if (old.frontmatter.task_store !== undefined) {
      const store = TaskStore.forCurrent(root2, current);
      old.runtimeState = {
        ...old.runtimeState,
        execution_log: store.readExecutionLogAtSourceRevision(entry.source_revision)
      };
    }
    const oldDefinition = readDraftDefinitionFromBody(old.body);
    for (const stepId of executedStepIds(old)) {
      if (implementationStepBlock(oldDefinition, stepId) !== implementationStepBlock(definition, stepId))
        fail3("RECOVERY_EXECUTED_DEFINITION_CHANGED", "An executed/preflighted historical step definition was rewritten.");
    }
    for (const reference of candidate.historical_completion_refs) {
      const completed = old.runtimeState.execution_log.find((item) => !("action" in item) && item.idempotency_key === reference.execution_id && item.step_id === reference.step_id && item.status === "completed");
      if (!completed || digest3(parseImplementationSteps(oldDefinition.implementation_steps).find((item) => item.id === reference.step_id)) !== reference.definition_revision)
        fail3("RECOVERY_COMPLETION_REFERENCE_INVALID", "Historical completion reference lacks its exact completed execution and definition.");
    }
  }
}
function makeReplanAudit(current, proposal, next, now) {
  const delta = proposal.semantic_delta;
  let action;
  if (delta.kind === "lifecycle" && delta.action === "supersede")
    action = "supersede";
  else if (delta.kind === "task-state" && (REPLAN_TASK_STATE_ACTIONS.includes(delta.action) || delta.action === "commit-scope-amendment"))
    action = delta.action;
  else
    fail3("RUNTIME_SCHEMA_INVALID", "Only Slice B transitions may create a replan audit record.");
  if (delta.kind !== "task-state" && delta.kind !== "lifecycle") {
    fail3("RUNTIME_SCHEMA_INVALID", "Only task-state and lifecycle deltas may create a replan audit record.");
  }
  const deltaEvidenceRefs = delta.evidence_refs;
  const base = {
    action,
    idempotency_key: proposal.idempotency_key,
    operation_kind: proposal.operation_kind,
    caller: proposal.caller,
    mode: action === "supersede" ? "supersede" : action === "commit-scope-amendment" ? "amend-scope" : "replan",
    task_id: current.runtimeState.task_id,
    task_slug: current.runtimeState.task_slug,
    document_id: current.sourceTuple.document_id,
    from_workflow_status: current.runtimeState.workflow_status,
    from_lifecycle_state: current.runtimeState.lifecycle_state,
    to_workflow_status: next.workflow_status,
    to_lifecycle_state: next.lifecycle_state,
    source_revision: current.sourceTuple.revision,
    authority_evidence: proposal.authority_evidence.map((item) => ({ ...item })),
    evidence_refs: [...deltaEvidenceRefs],
    recorded_at: now
  };
  if (action === "supersede" && delta.kind === "lifecycle" && delta.action === "supersede") {
    return {
      ...base,
      invalidation_kind: delta.invalidation_kind,
      invalidation_reason: delta.invalidation_reason,
      partial_diff_disposition: {
        reusable: [...delta.partial_diff_disposition.reusable],
        rollback_required: [...delta.partial_diff_disposition.rollback_required],
        stop_propagation: [...delta.partial_diff_disposition.stop_propagation]
      }
    };
  }
  return base;
}
function ensureAnyAuthorityKind(proposal, allowed) {
  if (!proposal.authority_evidence.some((item) => allowed.includes(item.kind))) {
    fail3("RUNTIME_AUTHORITY_MISSING", `proposal is missing one of the required authority evidence kinds: ${allowed.join(", ")}`);
  }
}
function makeDraftAudit(current, proposal, next, now) {
  if (proposal.semantic_delta.kind !== "task-state" || !DRAFT_TASK_STATE_ACTIONS.includes(proposal.semantic_delta.action)) {
    fail3("RUNTIME_SCHEMA_INVALID", "Only draft task-state transitions may create a draft audit record.");
  }
  const delta = proposal.semantic_delta;
  const targetIdentity = { task_id: delta.task_id, task_slug: delta.task_slug, document_id: delta.document_id };
  const base = {
    action: delta.action,
    idempotency_key: proposal.idempotency_key,
    operation_kind: "task-state-transaction",
    caller: "prepare-task",
    mode: proposal.mode,
    from_task_id: current.runtimeState.task_id,
    from_task_slug: current.runtimeState.task_slug,
    from_document_id: current.sourceTuple.document_id,
    task_id: targetIdentity.task_id,
    task_slug: targetIdentity.task_slug,
    document_id: targetIdentity.document_id,
    from_workflow_status: current.runtimeState.workflow_status,
    from_lifecycle_state: current.runtimeState.lifecycle_state,
    to_workflow_status: next.workflow_status,
    to_lifecycle_state: next.lifecycle_state,
    source_revision: current.sourceTuple.revision,
    authority_evidence: proposal.authority_evidence.map((item) => ({ ...item })),
    evidence_refs: [...delta.evidence_refs],
    recorded_at: now
  };
  if (delta.action === "create-draft" || delta.action === "update-draft") {
    const draftDelta = delta;
    return {
      ...base,
      definition_digest: digest3(draftDelta.draft_definition),
      ...draftDelta.predecessor ? { predecessor: draftDelta.predecessor } : {},
      ...draftDelta.claim_evidence === undefined ? {} : { claim_evidence_digest: digest3(draftDelta.claim_evidence) }
    };
  }
  const confirmDelta = delta;
  return { ...base, draft_revision: confirmDelta.draft_revision };
}
function makeClaimEvidenceMigrationAudit(current, proposal, next, now) {
  if (proposal.semantic_delta.kind !== "task-state" || proposal.semantic_delta.action !== "migrate-claim-evidence") {
    fail3("RUNTIME_SCHEMA_INVALID", "Only claim evidence migration transitions may create a claim evidence migration audit record.");
  }
  if (next.workflow_status !== "active" || next.lifecycle_state !== "active") {
    fail3("RUNTIME_STATE_CONFLICT", "Claim evidence migration must preserve the active + active lifecycle tuple.");
  }
  const delta = proposal.semantic_delta;
  return {
    action: "migrate-claim-evidence",
    idempotency_key: proposal.idempotency_key,
    operation_kind: "task-state-transaction",
    caller: "prepare-task",
    mode: "default",
    from_task_id: current.runtimeState.task_id,
    from_task_slug: current.runtimeState.task_slug,
    from_document_id: current.sourceTuple.document_id,
    task_id: current.runtimeState.task_id,
    task_slug: current.runtimeState.task_slug,
    document_id: current.sourceTuple.document_id,
    from_workflow_status: "active",
    from_lifecycle_state: "active",
    to_workflow_status: "active",
    to_lifecycle_state: "active",
    source_revision: current.sourceTuple.revision,
    authority_evidence: proposal.authority_evidence.map((item) => ({ ...item })),
    evidence_refs: [...delta.evidence_refs],
    claim_evidence_digest: digest3(delta.claim_evidence),
    recorded_at: now
  };
}
function readDraftDefinitionFromBody(body) {
  const ranges = resolveReplanSectionRanges(body);
  const values = {};
  for (const key of REPLAN_REPLACEMENT_FIELDS) {
    const range = ranges[key];
    const optional = key === "design_constraints" || key === "post_release_validation" || key === "propagation_governance";
    if (!range) {
      if (!optional)
        fail3("DRAFT_DEFINITION_INVALID", `CURRENT_TASK is missing the draft definition section for ${key}.`);
      values[key] = null;
      continue;
    }
    const content = normalizeReplacementSectionContent(body.slice(range.contentStart, range.contentEnd), `CURRENT_TASK.${range.title}`);
    if (!content && optional)
      values[key] = null;
    else if (!content)
      fail3("DRAFT_DEFINITION_INVALID", `CURRENT_TASK draft definition section ${key} is empty.`);
    else
      values[key] = content;
  }
  const authority = parseMutationAuthoritySection(body);
  return validateReplanReplacementDefinition({
    ...values,
    ...authority ?? {}
  }, "CURRENT_TASK.draft_definition");
}
function assertNoUnresolvedDraftQuestions(body) {
  const range = resolveReplanSectionRanges(body).open_questions;
  if (!range)
    fail3("DRAFT_DEFINITION_INVALID", "CURRENT_TASK is missing the draft confirmation open-questions section.");
  const content = body.slice(range.contentStart, range.contentEnd).replace(/\r\n?/g, `
`).trim();
  if (!content)
    return;
  const emptyMarkers = /^(?:none|n\/a|na|nil|empty|no\s+open\s+questions|no\s+questions|无|暂无|不适用)[.!。]?$/iu;
  const meaningfulLines = content.split(`
`).map((line) => line.trim()).filter((line) => line.length > 0 && !/^<!--.*-->$/u.test(line)).map((line) => line.replace(/^(?:[-*+]\s+|\d+[.)]\s+|\[[ xX]\]\s*)/u, "").trim()).filter((line) => line.length > 0);
  if (meaningfulLines.length === 0 || meaningfulLines.every((line) => emptyMarkers.test(line)))
    return;
  fail3("DRAFT_DECISION_UNRESOLVED", "draft confirmation is blocked by unresolved user-owned questions.");
}
function assertDraftDefinitionReady(root2, current) {
  assertBusinessEvidenceVersion(current);
  const activeStepId = current.runtimeState.active_step_id;
  const body = current.body;
  const definition = readDraftDefinitionFromBody(body);
  assertV2DraftDefinitionAuthority(root2, definition);
  assertStrictDraftImplementationSteps(activeStepId, definition.implementation_steps);
  const taskBasis = readCanonicalTaskBasis(root2, current).basis;
  assertPreparedTestStrategy(root2, definition, taskBasis);
  assertEvidencePlan(definition, current.runtimeState.claim_evidence ?? [], true, { root: root2, taskBasis, previous: [] });
  assertNoUnresolvedDraftQuestions(body);
  return definition;
}
function expectedDraftReplayAudit(current, proposal) {
  const entry = current.runtimeState.execution_log.find((item) => ("action" in item) && DRAFT_AUDIT_ACTIONS.includes(item.action) && item.idempotency_key === proposal.idempotency_key);
  if (!entry)
    fail3("RUNTIME_REPLAY_INCOMPLETE", "draft replay is missing its durable execution audit record.");
  return entry;
}
function assertDraftTaskReplay(root2, current, proposal) {
  if (proposal.semantic_delta.kind !== "task-state" || !DRAFT_TASK_STATE_ACTIONS.includes(proposal.semantic_delta.action))
    return;
  const delta = proposal.semantic_delta;
  const audit = expectedDraftReplayAudit(current, proposal);
  assertExecutionAudit(root2, current, audit);
  const targetIdentity = extractTaskIdentityFromCurrentTask(current.body);
  if (targetIdentity.id !== delta.task_id || targetIdentity.slug !== delta.task_slug) {
    fail3("RUNTIME_REPLAY_INCOMPLETE", "draft replay no longer has the proposal identity in the canonical task document.");
  }
  if (current.runtimeState.task_id !== delta.task_id || current.runtimeState.task_slug !== delta.task_slug || current.sourceTuple.document_id !== delta.document_id) {
    fail3("RUNTIME_REPLAY_INCOMPLETE", "draft replay no longer has the proposal identity tuple.");
  }
  if (audit.idempotency_key !== proposal.idempotency_key || audit.action !== delta.action || audit.source_revision !== proposal.source_tuple.revision || audit.evidence_refs.join("|") !== delta.evidence_refs.join("|") || audit.task_id !== delta.task_id || audit.task_slug !== delta.task_slug || audit.document_id !== delta.document_id) {
    fail3("RUNTIME_REPLAY_INCOMPLETE", "draft replay audit does not match the proposal identity or evidence.");
  }
  if (delta.action === "create-draft" || delta.action === "update-draft") {
    const draftDelta = delta;
    if (targetIdentity.title !== draftDelta.task_title)
      fail3("RUNTIME_REPLAY_INCOMPLETE", "draft replay no longer has the proposal task title in the canonical task document.");
    if (current.runtimeState.workflow_status !== "draft" || current.runtimeState.lifecycle_state !== "active") {
      fail3("RUNTIME_REPLAY_INCOMPLETE", `${delta.action} replay no longer has the draft + active tuple.`);
    }
    const definitionDigest = digest3(draftDelta.draft_definition);
    if (audit.definition_digest !== definitionDigest)
      fail3("RUNTIME_REPLAY_INCOMPLETE", `${delta.action} replay definition digest does not match the proposal.`);
    if (draftDelta.claim_evidence !== undefined) {
      if (audit.claim_evidence_digest !== digest3(draftDelta.claim_evidence)) {
        fail3("RUNTIME_REPLAY_INCOMPLETE", `${delta.action} replay claim evidence digest does not match the proposal.`);
      }
      if (digest3(current.runtimeState.claim_evidence ?? []) !== digest3(draftDelta.claim_evidence)) {
        fail3("RUNTIME_REPLAY_INCOMPLETE", `${delta.action} replay no longer has the proposal claim evidence in canonical CURRENT_TASK.`);
      }
    }
    const basis = readCanonicalTaskBasis(root2, current);
    if (digest3(basis.basis) !== digest3(draftDelta.task_basis)) {
      fail3("RUNTIME_REPLAY_INCOMPLETE", `${delta.action} replay no longer has the proposal task basis.`);
    }
    assertReplanDefinitionSections(current.body, draftDelta.draft_definition);
    if (current.runtimeState.active_step_id !== draftDelta.active_step_id || current.runtimeState.active_step_status !== "ready") {
      fail3("RUNTIME_REPLAY_INCOMPLETE", `${delta.action} replay no longer has the admitted draft step ready.`);
    }
  } else {
    const confirmDelta = delta;
    if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active") {
      fail3("RUNTIME_REPLAY_INCOMPLETE", "confirm-draft replay no longer has the active + active tuple.");
    }
    if (audit.draft_revision !== confirmDelta.draft_revision) {
      fail3("RUNTIME_REPLAY_INCOMPLETE", "confirm-draft replay no longer matches the exact draft revision.");
    }
  }
}
function expectedClaimEvidenceMigrationReplayAudit(current, proposal) {
  const entry = current.runtimeState.execution_log.find((item) => ("action" in item) && item.action === "migrate-claim-evidence" && item.idempotency_key === proposal.idempotency_key);
  if (!entry)
    fail3("RUNTIME_REPLAY_INCOMPLETE", "claim evidence migration replay is missing its durable execution audit record.");
  return entry;
}
function assertClaimEvidenceMigrationReplay(root2, current, proposal) {
  if (proposal.semantic_delta.kind !== "task-state" || proposal.semantic_delta.action !== "migrate-claim-evidence")
    return;
  const delta = proposal.semantic_delta;
  const audit = expectedClaimEvidenceMigrationReplayAudit(current, proposal);
  assertExecutionAudit(root2, current, audit);
  if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active") {
    fail3("RUNTIME_REPLAY_INCOMPLETE", "claim evidence migration replay no longer has the active + active tuple.");
  }
  if (current.runtimeState.claim_evidence_required !== true || current.runtimeState.claim_evidence.length === 0) {
    fail3("RUNTIME_REPLAY_INCOMPLETE", "claim evidence migration replay no longer has a strict non-empty plan.");
  }
  requireAcceptanceClaim(current.runtimeState.claim_evidence, "claim evidence migration replay claim_evidence");
  if (digest3(current.runtimeState.claim_evidence) !== digest3(delta.claim_evidence)) {
    fail3("RUNTIME_REPLAY_INCOMPLETE", "claim evidence migration replay no longer has the proposal plan in canonical CURRENT_TASK.");
  }
  if (audit.idempotency_key !== proposal.idempotency_key || audit.source_revision !== proposal.source_tuple.revision || audit.evidence_refs.join("|") !== delta.evidence_refs.join("|") || audit.claim_evidence_digest !== digest3(delta.claim_evidence) || audit.task_id !== current.runtimeState.task_id || audit.task_slug !== current.runtimeState.task_slug || audit.document_id !== current.sourceTuple.document_id) {
    fail3("RUNTIME_REPLAY_INCOMPLETE", "claim evidence migration replay audit does not match the proposal identity, plan, or evidence.");
  }
}
function expectedReplanReplayAudit(current, proposal) {
  const entry = current.runtimeState.execution_log.find((item) => ("action" in item) && REPLAN_AUDIT_ACTIONS.includes(item.action) && item.idempotency_key === proposal.idempotency_key);
  if (!entry)
    fail3("RUNTIME_REPLAY_INCOMPLETE", "replan replay is missing its durable execution audit record.");
  return entry;
}
function assertNoLaterReplanAudit(current, audit, failureCode = "RUNTIME_REPLAY_INCOMPLETE") {
  const index = current.runtimeState.execution_log.findIndex((item) => item === audit);
  if (index < 0)
    fail3(failureCode, "replay audit record is not part of the current execution log.");
  if (current.runtimeState.execution_log.slice(index + 1).some((item) => ("action" in item) && REPLAN_AUDIT_ACTIONS.includes(item.action))) {
    fail3(failureCode, "a later same-task lifecycle or replan transition has changed the replay boundary.");
  }
}
function expectedStepExecutionLog(current, proposal) {
  const entry = current.runtimeState.execution_log.find((item) => !("action" in item) && item.idempotency_key === proposal.idempotency_key);
  if (!entry)
    fail3("RUNTIME_REPLAY_INCOMPLETE", "step-progress replay is missing its durable execution log record.");
  return entry;
}
function assertStepProgressReplay(current, proposal) {
  if (proposal.semantic_delta.kind !== "task-state" || proposal.semantic_delta.action !== "step-progress")
    return;
  const delta = proposal.semantic_delta;
  const entry = expectedStepExecutionLog(current, proposal);
  const sameOptionalValue = (left, right) => digest3(left ?? null) === digest3(right ?? null);
  if (entry.mode !== proposal.mode || entry.step_id !== delta.step_id || entry.status !== delta.status || entry.evidence_refs.join("|") !== delta.evidence_refs.join("|") || !sameOptionalValue(entry.note, delta.note) || !sameOptionalValue(entry.repair_fingerprint, delta.repair_fingerprint) || !sameOptionalValue(entry.repair_fingerprints, delta.repair_fingerprints) || !sameOptionalValue(entry.repair_wave_id, delta.repair_wave_id) || !sameOptionalValue(entry.change_set_id, delta.change_set_id ?? delta.review_receipt?.change_set_id) || !sameOptionalValue(entry.review_receipt, delta.review_receipt) || !sameOptionalValue(entry.claim_evidence, delta.claim_evidence) || !sameOptionalValue(entry.execution_result, delta.execution_result)) {
    fail3("RUNTIME_REPLAY_INCOMPLETE", "step-progress replay does not match the durable execution record.");
  }
  if (entry.mode === "repair" && entry.repair_fingerprint === undefined && entry.repair_fingerprints === undefined) {
    fail3("RUNTIME_REPLAY_INCOMPLETE", "repair replay is missing its durable finding fingerprints.");
  }
  if (entry.status === "completed" && entry.mode === "default" && entry.advancement === undefined) {
    return;
  }
  if (entry.advancement === undefined || entry.checkpoint === undefined || entry.next_step_id === undefined) {
    fail3("RUNTIME_REPLAY_INCOMPLETE", "step-progress replay is missing its durable advancement outcome.");
  }
}
function assertRetainedReviewReplay(current, proposal) {
  if (proposal.semantic_delta.kind !== "task-state" || proposal.semantic_delta.action !== "consume-retained-review")
    return;
  const delta = proposal.semantic_delta;
  const entry = expectedStepExecutionLog(current, proposal);
  if (entry.step_id !== delta.step_id || entry.status !== "completed" || entry.mode !== "default" || digest3(entry.review_receipt) !== digest3(delta.review_receipt) || entry.advancement !== "advanced" || entry.next_step_id !== current.runtimeState.active_step_id || current.runtimeState.pending_review_result !== null || current.runtimeState.scope_amendment_pending_review_step_id !== undefined || current.runtimeState.active_step_status !== "ready") {
    fail3("RUNTIME_REPLAY_INCOMPLETE", "retained review replay does not match the consumed review and continuation state.");
  }
}
function assertReviewResultReplay(current, proposal) {
  if (proposal.semantic_delta.kind !== "task-state" || proposal.semantic_delta.action !== "record-review-result")
    return;
  const pending = current.runtimeState.pending_review_result;
  if (!pending || pending.review_id !== proposal.semantic_delta.review_result.review_id) {
    fail3("RUNTIME_REPLAY_INCOMPLETE", "review-result replay is no longer the pending result for the current execution generation.");
  }
  const { recorded_at: _recordedAt, ...stored } = pending;
  if (digest3(stored) !== digest3(proposal.semantic_delta.review_result)) {
    fail3("RUNTIME_REPLAY_INCOMPLETE", "review-result replay does not match the durable pending result.");
  }
}
function assertTaskStateReplay(root2, current, proposal) {
  if (proposal.semantic_delta.kind === "task-state" && DRAFT_TASK_STATE_ACTIONS.includes(proposal.semantic_delta.action)) {
    assertDraftTaskReplay(root2, current, proposal);
    return;
  }
  if (proposal.semantic_delta.kind === "task-state" && proposal.semantic_delta.action === "step-progress") {
    assertStepProgressReplay(current, proposal);
    return;
  }
  if (proposal.semantic_delta.kind === "task-state" && proposal.semantic_delta.action === "consume-retained-review") {
    assertRetainedReviewReplay(current, proposal);
    return;
  }
  if (proposal.semantic_delta.kind === "task-state" && proposal.semantic_delta.action === "migrate-claim-evidence") {
    assertClaimEvidenceMigrationReplay(root2, current, proposal);
    return;
  }
  if (proposal.semantic_delta.kind === "task-state" && proposal.semantic_delta.action === "record-review-result") {
    assertReviewResultReplay(current, proposal);
    return;
  }
  if (proposal.semantic_delta.kind !== "task-state" || !REPLAN_TASK_STATE_ACTIONS.includes(proposal.semantic_delta.action) && proposal.semantic_delta.action !== "commit-scope-amendment")
    return;
  const delta = proposal.semantic_delta;
  const audit = expectedReplanReplayAudit(current, proposal);
  assertExecutionAudit(root2, current, audit);
  assertNoLaterReplanAudit(current, audit);
  if (delta.action === "mark-replan-blocked") {
    if (current.runtimeState.workflow_status !== "blocked_by_replan" || current.runtimeState.lifecycle_state !== "active")
      fail3("RUNTIME_REPLAY_INCOMPLETE", "mark-replan-blocked replay no longer has the blocked_by_replan + active tuple.");
  } else if (delta.action === "clear-replan-block") {
    if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active")
      fail3("RUNTIME_REPLAY_INCOMPLETE", "clear-replan-block replay no longer has the active + active tuple.");
  } else {
    const commitDelta = delta;
    if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active")
      fail3("RUNTIME_REPLAY_INCOMPLETE", "commit-replan replay no longer has the active + active tuple.");
    assertReplacementActiveStep(commitDelta.active_step_id, commitDelta.replacement_definition.implementation_steps);
    if (current.runtimeState.active_step_id !== commitDelta.active_step_id || current.runtimeState.active_step_status !== "ready")
      fail3("RUNTIME_REPLAY_INCOMPLETE", "commit-replan replay no longer has the replacement active step ready.");
    if (current.runtimeState.resume_requires_review || current.runtimeState.resume_review_reasons.length > 0)
      fail3("RUNTIME_REPLAY_INCOMPLETE", "commit-replan replay no longer has a cleared resume gate.");
    assertReplanDefinitionSections(current.body, commitDelta.replacement_definition);
    const basis = readCanonicalTaskBasis(root2, current);
    if (digest3(basis.basis) !== digest3(commitDelta.task_basis)) {
      fail3("RUNTIME_REPLAY_INCOMPLETE", "commit-replan replay no longer has the proposal task basis.");
    }
    if (commitDelta.claim_evidence !== undefined && digest3(current.runtimeState.claim_evidence ?? []) !== digest3(commitDelta.claim_evidence)) {
      fail3("RUNTIME_REPLAY_INCOMPLETE", "commit-replan replay no longer has the proposal claim evidence in canonical CURRENT_TASK.");
    }
  }
  const expectedEvidenceRefs = delta.evidence_refs;
  if (audit.idempotency_key !== proposal.idempotency_key || audit.action !== delta.action || audit.source_revision !== proposal.source_tuple.revision || audit.evidence_refs.join("|") !== expectedEvidenceRefs.join("|") || audit.task_id !== current.runtimeState.task_id || audit.task_slug !== current.runtimeState.task_slug || audit.document_id !== current.sourceTuple.document_id) {
    fail3("RUNTIME_REPLAY_INCOMPLETE", "replan replay audit does not match the proposal identity or evidence.");
  }
}
function applyTaskStateDelta(root2, current, proposal, now) {
  if (proposal.semantic_delta.kind !== "task-state")
    fail3("RUNTIME_SCHEMA_INVALID", "Expected task-state delta.");
  const delta = proposal.semantic_delta;
  if (delta.action === "record-user-evidence") {
    assertBusinessEvidenceVersion(current);
    ensureAuthorityKinds(proposal, ["active-task-owner", "evidence-admission", "user-confirmation"]);
    if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active")
      fail3("USER_EVIDENCE_STATE_INVALID", "A task-scoped user decision requires the active confirmed task.");
    const claim = current.runtimeState.claim_evidence?.find((item) => item.claim_id === delta.claim_id);
    const slot = claim?.slots.find((item) => item.slot_id === delta.slot_id);
    if (!claim || !slot?.check || slot.check.check_id !== delta.check_id)
      fail3("USER_EVIDENCE_TARGET_INVALID", "The exact claim/slot/check must already be frozen.");
    if (delta.evidence_plan_revision !== current.runtimeState.evidence_plan_revision || captureReviewTarget(root2, slot.check.subject_paths).revision !== delta.subject_revision)
      fail3("USER_EVIDENCE_STALE", "The plan or observed objects changed; do not rebind the old user statement.");
    if ((current.runtimeState.evidence_challenges ?? []).some((item) => item.claim_id === claim.claim_id && item.slot_id === slot.slot_id && item.status !== "resolved"))
      fail3("USER_EVIDENCE_CHALLENGED", "Resolve the counterexample through its retained owner; a user evidence decision does not dismiss it.");
    const retained = readCanonicalTaskBasis(root2, current);
    const basis = structuredClone(retained.basis);
    const prior = [basis.original_request, ...basis.user_decisions].find((item) => item.source === delta.decision_source);
    if (prior && prior.verbatim !== delta.decision_text)
      fail3("USER_EVIDENCE_SOURCE_INVALID", "An existing source coordinate cannot be assigned different user text.");
    if (!prior)
      basis.user_decisions.push({ source: delta.decision_source, verbatim: delta.decision_text });
    if (delta.decision_kind === "human-acceptance") {
      if (slot.check.method !== "human" || slot.check.expected_result !== "accepted")
        fail3("USER_EVIDENCE_METHOD_INVALID", "A human observation satisfies only an admitted human check; it is not an execution PASS.");
    } else {
      const userOwned = [retained.basis.original_request, ...retained.basis.user_decisions].some((item) => item.source === claim.source_ref);
      if (!userOwned || !["acceptance", "regression", "exploration"].includes(claim.claim_kind) || slot.applicability !== "current" || slot.check.expected_result === "expected-failure" || ["project-policy", "release-gate", "claim-risk-contract"].includes(slot.check.selection?.breadth_basis ?? "")) {
        fail3("EVIDENCE_WAIVER_AUTHORITY_INVALID", "Only current user-owned evidence may be waived; prerequisites, invariants, contracts and release policy are not waived by this route.");
      }
    }
    const validationItems = delta.validation_items ?? [];
    if (validationItems.length) {
      if (delta.decision_kind !== "waiver" || new Set(validationItems).size !== validationItems.length || validationItems.some((item) => !slotOwnsValidation(current, slot, item))) {
        fail3("EVIDENCE_WAIVER_TARGET_INVALID", "Only exact planned validation labels exclusively bound to this frozen claim/slot/check may be waived.");
      }
    }
    const decision = {
      ...validationItems.length ? { validation_items: validationItems } : {},
      kind: delta.decision_kind,
      decision_id: proposal.idempotency_key,
      decision_source: delta.decision_source,
      statement_sha256: sha2565(delta.decision_text),
      check_id: delta.check_id,
      evidence_plan_revision: delta.evidence_plan_revision,
      subject_revision: delta.subject_revision,
      recorded_at: now,
      assurance: "caller-reported"
    };
    const claims = copyClaimEvidence(current.runtimeState.claim_evidence ?? []);
    const target = claims.find((item) => item.claim_id === delta.claim_id).slots.find((item) => item.slot_id === delta.slot_id);
    target.user_decision = decision;
    if (delta.decision_kind === "human-acceptance") {
      target.report = {
        result_id: decision.decision_id,
        status: "accepted",
        actual_method: "human",
        evidence_plan_revision: decision.evidence_plan_revision,
        subject_revision: decision.subject_revision,
        environment: "explicit user observation; source recorded in Task Basis",
        assurance: "caller-reported"
      };
      target.disposition = "newly-executed";
      target.evidence_refs = [...new Set([...slot.evidence_refs, retained.path])];
    }
    return { next: {
      ...current.runtimeState,
      claim_evidence: claims,
      applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
    }, taskBasis: basis };
  }
  if (delta.action === "migrate-claim-evidence") {
    ensureAuthorityKinds(proposal, ["scope-admission", "evidence-admission"]);
    ensureAnyAuthorityKind(proposal, ["active-task-owner", "user-confirmation", "authorized-caller"]);
    if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active") {
      fail3("CLAIM_EVIDENCE_MIGRATION_NOT_APPLICABLE", "claim evidence migration requires a legacy active + active task.");
    }
    if (current.runtimeState.claim_evidence.length > 0) {
      fail3("CLAIM_EVIDENCE_MIGRATION_NOT_APPLICABLE", "claim evidence migration cannot replace an existing structured claim_evidence plan.");
    }
    const claimEvidence = requireClaimEvidencePlan(delta.claim_evidence, "migrate-claim-evidence claim_evidence");
    if (claimEvidence.some((claim) => claim.slots.some((slot) => slot.user_decision)))
      fail3("USER_EVIDENCE_ACTION_REQUIRED", "Legacy migration cannot invent user decisions.");
    requireAcceptanceClaim(claimEvidence, "migrate-claim-evidence claim_evidence");
    const legacyCommands = plannedCommandsForEvidence(readDraftDefinitionFromBody(current.body));
    for (const record of claimEvidence)
      for (const slot of record.slots) {
        if (!legacyCommands.has(slot.due_step_id)) {
          fail3("CLAIM_EVIDENCE_PLAN_INVALID", "migrated evidence must be due at an existing legacy step.");
        }
        if (slot.check?.method === "execution" && !legacyCommands.get(slot.due_step_id).has(slot.check.entry)) {
          fail3("CLAIM_EVIDENCE_COMMAND_UNBOUND", "migration may only reconstruct an exact existing planned command at its original due step.");
        }
      }
    const nextWithoutAudit = {
      ...current.runtimeState,
      workflow_status: "active",
      lifecycle_state: "active",
      claim_evidence_required: true,
      claim_evidence: copyClaimEvidence(claimEvidence),
      applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
    };
    const audit = makeClaimEvidenceMigrationAudit(current, proposal, nextWithoutAudit, now);
    return {
      next: { ...nextWithoutAudit, execution_log: appendExecutionLogEntry(current.runtimeState, audit) },
      audit
    };
  }
  if (delta.action === "consume-retained-review") {
    ensureAuthorityKinds(proposal, ["active-task-owner", "scope-admission", "evidence-admission"]);
    if (proposal.mode !== "default" || current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active") {
      fail3("RETAINED_REVIEW_STATE_INVALID", "Retained review consumption requires an active + active task and default execute-step mode.");
    }
    const pending = current.runtimeState.pending_review_result;
    if (!pending || pending.verdict !== "clean" || current.runtimeState.scope_amendment_pending_review_step_id !== delta.step_id || pending.step_id !== delta.step_id) {
      fail3("RETAINED_REVIEW_STATE_INVALID", "The exact clean review retained by a scope amendment is required.");
    }
    const receipt = delta.review_receipt;
    if (receipt.cycle_id !== pending.cycle_id || receipt.cycle_phase !== pending.cycle_phase || receipt.change_set_id !== pending.change_set_id || receipt.review_target_revision !== pending.review_target_revision || digest3(receipt.evidence_refs) !== digest3(pending.evidence_refs) || receipt.verdict !== "clean") {
      fail3("RETAINED_REVIEW_RECEIPT_CONFLICT", "Retained review consumption must bind the exact pending clean review receipt.");
    }
    const reviewedExecution = currentDefinitionExecutionLog(current).map((item) => ("action" in item) ? item : current.runtimeState.scope_amendment_pending_review_step_id === pending.step_id && item.idempotency_key === pending.execution_id ? item : cumulativeReviewExecution(current, item)).find((item) => !("action" in item) && item.idempotency_key === pending.execution_id);
    if (!reviewedExecution?.execution_result || reviewedExecution.change_set_id !== pending.change_set_id || reviewedExecution.execution_result.review_target.revision !== pending.review_target_revision) {
      fail3("RETAINED_REVIEW_TARGET_CONFLICT", "The retained clean review no longer binds its Runtime-recorded execution target.");
    }
    if (captureReviewTarget(root2, reviewedExecution.execution_result.review_target.entries.map((item) => item.path)).revision !== pending.review_target_revision) {
      fail3("RETAINED_REVIEW_TARGET_STALE", "Product files changed after the retained clean review was recorded.");
    }
    const openFindings = current.runtimeState.findings.filter((item) => ["admitted", "in-progress"].includes(item.status));
    if (openFindings.some((item) => !receipt.admitted_fingerprints.includes(item.fingerprint))) {
      fail3("REVIEW_CONVERGENCE_REQUIRED", "A retained clean review cannot bypass an open finding.");
    }
    const {
      scope_amendment_pending_review_step_id: _retainedReviewStepId,
      execution_preflight: _retainedExecutionPreflight,
      ...stateWithoutRetainedReview
    } = current.runtimeState;
    const next = {
      ...stateWithoutRetainedReview,
      pending_review_result: null,
      applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision),
      execution_log: appendExecutionLogEntry(current.runtimeState, {
        idempotency_key: proposal.idempotency_key,
        mode: "default",
        step_id: delta.step_id,
        status: "completed",
        evidence_refs: [...delta.evidence_refs],
        checkpoint: "required",
        advancement: "advanced",
        next_step_id: current.runtimeState.active_step_id,
        review_receipt: receipt,
        recorded_at: now
      })
    };
    return { next };
  }
  if (delta.action === "create-draft") {
    ensureAuthorityKinds(proposal, ["scope-admission", "evidence-admission"]);
    ensureAnyAuthorityKind(proposal, ["user-confirmation", "authorized-caller"]);
    if (delta.predecessor)
      assertSuccessorDecision(root2, current, delta);
    else if (current.runtimeState.workflow_status !== "closed" || current.runtimeState.lifecycle_state !== "archived") {
      fail3("DRAFT_CREATION_BLOCKED", "Ordinary create-draft requires closed + archived; a superseded predecessor needs the explicit successor route.");
    }
    const expectedTaskId = allocateNextTaskId(root2, current.runtimeState.task_id);
    if (delta.task_id !== expectedTaskId) {
      fail3("TASK_ID_ALLOCATION_CONFLICT", `create-draft must allocate the next unused task identity ${expectedTaskId}.`);
    }
    if (delta.document_id === current.sourceTuple.document_id || collectTaskDocumentIds(root2).has(delta.document_id)) {
      fail3("DOCUMENT_ID_COLLISION", "create-draft document_id must be fresh across canonical task artifacts.");
    }
    if (delta.predecessor) {} else if (current.runtimeState.task_id === "000") {
      const bootstrapArchive = archivePathForTask(root2, current);
      if (fs11.existsSync(bootstrapArchive.filePath))
        fail3("TASK_ARCHIVE_CONFLICT", "bootstrap TASK-000 must not already have a canonical archive before the first ordinary draft.");
    } else {
      const { receipt } = matchingArchiveReceipt(root2, current);
      assertPreviousTaskReconciliationComplete(root2, current, receipt);
    }
    assertStrictDraftImplementationSteps(delta.active_step_id, delta.draft_definition.implementation_steps);
    assertV2DraftDefinitionAuthority(root2, delta.draft_definition);
    assertPreparedTestStrategy(root2, delta.draft_definition, delta.task_basis);
    const claimEvidence = requireClaimEvidencePlan(delta.claim_evidence, "create-draft claim_evidence");
    requireAcceptanceClaim(claimEvidence, "create-draft claim_evidence");
    const planRevision = assertEvidencePlan(delta.draft_definition, claimEvidence, true, { root: root2, taskBasis: delta.task_basis, previous: [] });
    const draftIdentity = {
      task_id: delta.task_id,
      task_slug: delta.task_slug,
      document_id: delta.document_id,
      task_title: delta.task_title
    };
    const emptyDraftState = {
      business_evidence_version: 1,
      task_evolution_version: 2,
      schema_version: 1,
      kind: VNEXT_RUNTIME_STATE_KIND,
      task_id: delta.task_id,
      task_slug: delta.task_slug,
      workflow_status: "draft",
      lifecycle_state: "active",
      resume_requires_review: false,
      resume_review_reasons: [],
      active_step_id: delta.active_step_id,
      active_step_status: "ready",
      finding_queue_revision: 0,
      review_cycle: createReviewCycleZero(),
      findings: [],
      execution_log: [],
      applied_proposals: [],
      claim_evidence_required: true,
      claim_evidence: copyClaimEvidence(claimEvidence),
      evidence_plan_revision: planRevision,
      review_coverage: emptyReviewCoverage(planRevision),
      pending_review_result: null
    };
    const draftStateWithProposal = {
      ...emptyDraftState,
      applied_proposals: appendAppliedProposal(emptyDraftState, proposal, current.sourceTuple.revision)
    };
    let auditRecordedAt = now;
    if (delta.predecessor) {
      const store = new TaskStore(root2, delta.document_id);
      const manifest = store.manifest;
      if (manifest) {
        const history = store.readExecutionLog();
        const initial = history.length === 1 && isRecord4(history[0]) ? history[0] : null;
        if (manifest.task_id !== delta.task_id || manifest.task_slug !== delta.task_slug || manifest.current_task_path !== current.relativePath || manifest.head.event_sequence !== 1 || digest3(store.readAppliedProposals()) !== digest3(draftStateWithProposal.applied_proposals) || initial?.action !== "create-draft" || initial.idempotency_key !== proposal.idempotency_key || initial.source_revision !== current.sourceTuple.revision || typeof initial.recorded_at !== "string" || Number.isNaN(Date.parse(initial.recorded_at))) {
          fail3("SUCCESSOR_HISTORY_CONFLICT", "An existing prepared successor aggregate must belong to this exact proposal and predecessor.");
        }
        auditRecordedAt = initial.recorded_at;
      }
    }
    const audit = makeDraftAudit(current, proposal, draftStateWithProposal, auditRecordedAt);
    const next = { ...draftStateWithProposal, execution_log: appendExecutionLogEntry(draftStateWithProposal, audit) };
    return {
      next,
      draftDefinition: delta.draft_definition,
      draftIdentity,
      draftDocumentId: delta.document_id,
      taskBasis: delta.task_basis,
      audit
    };
  }
  if (delta.action === "update-draft") {
    assertBusinessEvidenceVersion(current);
    ensureAuthorityKinds(proposal, ["scope-admission", "evidence-admission"]);
    ensureAnyAuthorityKind(proposal, ["active-task-owner", "user-confirmation", "authorized-caller"]);
    if (current.runtimeState.workflow_status !== "draft" || current.runtimeState.lifecycle_state !== "active") {
      fail3("DRAFT_REFINEMENT_BLOCKED", "update-draft requires the current task to be draft + active.");
    }
    if (delta.task_id !== current.runtimeState.task_id || delta.task_slug !== current.runtimeState.task_slug || delta.document_id !== current.sourceTuple.document_id) {
      fail3("DRAFT_IDENTITY_IMMUTABLE", "update-draft must preserve TASK_ID, TASK_SLUG, and document_id.");
    }
    const currentIdentity = extractTaskIdentityFromCurrentTask(current.body);
    if (currentIdentity.title !== delta.task_title)
      fail3("DRAFT_IDENTITY_IMMUTABLE", "update-draft must preserve the task title identity.");
    assertStrictDraftImplementationSteps(delta.active_step_id, delta.draft_definition.implementation_steps);
    assertV2DraftDefinitionAuthority(root2, delta.draft_definition);
    assertPreparedTestStrategy(root2, delta.draft_definition, delta.task_basis);
    const claimEvidence = requireClaimEvidencePlan(delta.claim_evidence, "update-draft claim_evidence");
    requireAcceptanceClaim(claimEvidence, "update-draft claim_evidence");
    const planRevision = assertEvidencePlan(delta.draft_definition, claimEvidence, true, { root: root2, taskBasis: delta.task_basis, previous: current.runtimeState.claim_evidence ?? [] });
    for (const claim of claimEvidence) {
      const previous = current.runtimeState.claim_evidence?.find((item) => item.claim_id === claim.claim_id);
      if (previous && (previous.requirement !== claim.requirement || previous.source_ref !== claim.source_ref || previous.claim_kind !== claim.claim_kind || previous.boundary !== claim.boundary))
        fail3("CLAIM_EVIDENCE_PLAN_CONFLICT", "Changing a draft obligation requires a new claim identity.");
      for (const slot of claim.slots) {
        const old = previous?.slots.find((item) => item.slot_id === slot.slot_id);
        if (old && digest3(evidenceSlotDefinition(old)) !== digest3(evidenceSlotDefinition(slot)))
          fail3("CLAIM_EVIDENCE_PLAN_CONFLICT", "Changing a draft check requires a new slot/check identity.");
        const previousCheck = current.runtimeState.claim_evidence?.flatMap((item) => item.slots).find((item) => item.check?.check_id === slot.check?.check_id)?.check;
        if (previousCheck && digest3(previousCheck) !== digest3(slot.check))
          fail3("CLAIM_EVIDENCE_PLAN_CONFLICT", "A check_id cannot be reassigned to a different check during draft refinement.");
      }
    }
    const nextWithoutAudit = {
      ...current.runtimeState,
      workflow_status: "draft",
      lifecycle_state: "active",
      active_step_id: delta.active_step_id,
      active_step_status: "ready",
      claim_evidence_required: true,
      claim_evidence: copyClaimEvidence(claimEvidence),
      evidence_plan_revision: planRevision,
      review_coverage: emptyReviewCoverage(planRevision),
      applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
    };
    const audit = makeDraftAudit(current, proposal, nextWithoutAudit, now);
    const next = { ...nextWithoutAudit, execution_log: appendExecutionLogEntry(current.runtimeState, audit) };
    return {
      next,
      replacementDefinition: delta.draft_definition,
      draftIdentity: {
        task_id: current.runtimeState.task_id,
        task_slug: current.runtimeState.task_slug,
        document_id: current.sourceTuple.document_id,
        task_title: currentIdentity.title
      },
      draftDocumentId: current.sourceTuple.document_id,
      taskBasis: delta.task_basis,
      audit
    };
  }
  if (delta.action === "confirm-draft") {
    ensureAnyAuthorityKind(proposal, ["user-confirmation", "authorized-caller"]);
    ensureAuthorityKinds(proposal, ["evidence-admission"]);
    const confirmationAuthorities = proposal.authority_evidence.filter((item) => item.kind === "user-confirmation" || item.kind === "authorized-caller");
    for (const auth of confirmationAuthorities) {
      if (!auth.task_id || !auth.document_id || !auth.draft_revision) {
        fail3("RUNTIME_AUTHORITY_INVALID", "confirm-draft authority evidence must bind task_id, document_id, and draft_revision.");
      }
      if (auth.task_id !== current.runtimeState.task_id) {
        fail3("DRAFT_IDENTITY_CONFLICT", `confirm-draft authority task_id ${auth.task_id} does not match current task ${current.runtimeState.task_id}.`);
      }
      if (auth.document_id !== current.sourceTuple.document_id) {
        fail3("DRAFT_IDENTITY_CONFLICT", `confirm-draft authority document_id ${auth.document_id} does not match current document ${current.sourceTuple.document_id}.`);
      }
      if (!taskSourceRevisionMatches(root2, current, auth.draft_revision)) {
        fail3("DRAFT_REVISION_CONFLICT", `confirm-draft authority draft_revision ${auth.draft_revision} does not match current draft revision ${current.sourceTuple.revision}.`);
      }
    }
    if (current.runtimeState.workflow_status !== "draft" || current.runtimeState.lifecycle_state !== "active") {
      fail3("DRAFT_CONFIRMATION_BLOCKED", "confirm-draft requires the current task to be draft + active.");
    }
    if (delta.task_id !== current.runtimeState.task_id || delta.task_slug !== current.runtimeState.task_slug || delta.document_id !== current.sourceTuple.document_id) {
      fail3("DRAFT_IDENTITY_CONFLICT", "confirm-draft identity does not match the current draft.");
    }
    if (!taskSourceRevisionMatches(root2, current, delta.draft_revision)) {
      fail3("DRAFT_REVISION_CONFLICT", "confirm-draft must bind the exact current draft source revision.");
    }
    if (current.runtimeState.active_step_status !== "ready") {
      fail3("DRAFT_CONFIRMATION_BLOCKED", "confirm-draft requires the admitted draft step to remain ready.");
    }
    readCanonicalTaskBasis(root2, current);
    assertDraftDefinitionReady(root2, current);
    if (current.runtimeState.claim_evidence_required !== true) {
      fail3("CLAIM_EVIDENCE_MIGRATION_REQUIRED", "prepare-task refinement must persist a strict claim_evidence plan before confirm-draft; legacy tasks are readable but not terminal-completion compatible.");
    }
    const claimEvidence = requireClaimEvidencePlan(current.runtimeState.claim_evidence, "confirm-draft claim_evidence", "CLAIM_EVIDENCE_MIGRATION_REQUIRED");
    requireAcceptanceClaim(claimEvidence, "confirm-draft claim_evidence");
    const domainRevision = currentAuthorityDomainRevision(root2, current);
    const nextWithoutAudit = {
      ...current.runtimeState,
      workflow_status: "active",
      lifecycle_state: "active",
      ...domainRevision ? { authority_domain_revision: domainRevision } : {},
      applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
    };
    const audit = makeDraftAudit(current, proposal, nextWithoutAudit, now);
    const next = { ...nextWithoutAudit, execution_log: appendExecutionLogEntry(current.runtimeState, audit) };
    return { next, audit };
  }
  if (delta.action === "clear-resume-review-gate") {
    ensureAuthorityKinds(proposal, ["authorized-caller", "active-task-owner", "resume-review", "evidence-admission"]);
    const callerAuthorities = proposal.authority_evidence.filter((item) => item.kind === "authorized-caller");
    for (const auth of callerAuthorities) {
      if (!auth.task_id || !auth.document_id || !auth.source_revision) {
        fail3("RUNTIME_AUTHORITY_INVALID", "clear-resume-review-gate caller authority must bind task_id, document_id, and source revision.");
      }
      if (auth.task_id !== current.runtimeState.task_id || auth.document_id !== current.sourceTuple.document_id) {
        fail3("RESUME_READINESS_IDENTITY_CONFLICT", "clear-resume-review-gate caller authority does not identify the current task document.");
      }
      if (!taskSourceRevisionMatches(root2, current, auth.source_revision)) {
        fail3("RESUME_READINESS_REVISION_CONFLICT", "clear-resume-review-gate caller authority does not bind the exact current source revision.");
      }
    }
    if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active") {
      fail3("TASK_STATE_NOT_ACTIVE", "resume review can be cleared only after the task has resumed to active + active.");
    }
    if (!current.runtimeState.resume_requires_review)
      return { next: current.runtimeState };
    return {
      next: {
        ...current.runtimeState,
        resume_requires_review: false,
        resume_review_reasons: [],
        applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
      }
    };
  }
  if (delta.action === "record-review-result") {
    ensureAuthorityKinds(proposal, ["active-task-owner", "scope-admission", "evidence-admission"]);
    if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active") {
      fail3("TASK_STATE_NOT_ACTIVE", "review results may be recorded only for an active + active task.");
    }
    if (current.runtimeState.resume_requires_review) {
      fail3("RESUME_REVIEW_REQUIRED", "review-change cannot record a step review while the resume review gate is active.");
    }
    const review = delta.review_result;
    const isCorrectionReview = (current.runtimeState.evidence_challenges ?? []).some((item) => item.status === "invalidated" && item.correction_step_id === review.step_id);
    if (review.verdict === "clean" && !isCorrectionReview && (current.runtimeState.evidence_challenges ?? []).some((item) => item.status !== "resolved" && item.correction_step_id !== review.step_id)) {
      fail3("EVIDENCE_CHALLENGE_UNRESOLVED", "A clean review cannot consume an unresolved challenge outside its admitted correction step.");
    }
    if (review.step_id !== current.runtimeState.active_step_id)
      fail3("ACTIVE_STEP_CONFLICT", "review result does not belong to the active step.");
    if (review.cycle_id !== current.runtimeState.review_cycle.id)
      fail3("REVIEW_CYCLE_CONFLICT", "review result does not belong to the current review cycle.");
    const stepExecutions = currentDefinitionExecutionLog(current).filter((item) => !("action" in item) && item.step_id === review.step_id && item.idempotency_key.startsWith("execute-step-result-") && item.review_receipt === undefined);
    const execution = stepExecutions[stepExecutions.length - 1];
    if (!execution || execution.idempotency_key !== review.execution_id) {
      fail3("REVIEW_EXECUTION_STALE", "review result does not bind the latest recorded execution for the active step.");
    }
    const activePreflight = current.runtimeState.execution_preflight;
    if (activePreflight && activePreflight.step_id === execution.step_id && execution.execution_result?.execution_id !== activePreflight.execution_id) {
      fail3("REVIEW_EXECUTION_STALE", "review result does not bind the current stable execution identity.");
    }
    assertReviewExecutionEligible(current, execution);
    if (current.runtimeState.review_coverage && !review.test_assessment)
      fail3("REVIEW_ASSESSMENT_REQUIRED", "Cumulative reviews require test necessity, oracle, boundary, reuse and applicability assessment.");
    if (current.runtimeState.review_coverage && review.test_assessment && !review.test_assessment.applicable && (parseMutationScope(current.body).persistent_tests?.length || current.runtimeState.claim_evidence?.some((claim) => claim.slots.some((slot) => slot.check?.method === "execution"))))
      fail3("REVIEW_ASSESSMENT_REQUIRED", "Declared execution checks or persistent tests require an applicable assessment even when test files are unchanged.");
    const executionResult = cumulativeReviewExecution(current, execution).execution_result;
    if (!executionResult || execution.change_set_id !== review.change_set_id || executionResult.change_set_id !== review.change_set_id || executionResult.review_target.revision !== review.review_target_revision) {
      fail3("REVIEW_TARGET_CONFLICT", "review result does not bind the Runtime-recorded execution change set and review target.");
    }
    const currentTarget = captureReviewTarget(root2, executionResult.review_target.entries.map((item) => item.path));
    if (currentTarget.revision !== review.review_target_revision) {
      fail3("REVIEW_TARGET_STALE", "product files changed after the reviewed execution target was recorded.");
    }
    const expectedPhase = execution.mode === "repair" ? "verification" : "discovery";
    if (review.cycle_phase !== expectedPhase)
      fail3("REVIEW_PHASE_INVALID", `review result must use ${expectedPhase} for the latest execution.`);
    if (review.cycle_phase === "discovery" && review.unresolved_fingerprints.length > 0) {
      fail3("REVIEW_PHASE_INVALID", "discovery review cannot reference unresolved admitted findings.");
    }
    const openFingerprints = new Set(current.runtimeState.findings.filter((item) => item.review_cycle_id === review.cycle_id && ["admitted", "in-progress"].includes(item.status)).map((item) => item.fingerprint));
    for (const fingerprint of review.unresolved_fingerprints) {
      if (!openFingerprints.has(fingerprint))
        fail3("FINDING_NOT_FOUND", `review result references non-open finding ${fingerprint}.`);
    }
    const nestedEvidence = [...review.findings.flatMap((item) => item.evidence_refs), ...review.test_assessment?.evidence_refs ?? []];
    if (![...nestedEvidence, ...review.evidence_refs].every((ref) => delta.evidence_refs.includes(ref))) {
      fail3("RUNTIME_EVIDENCE_INVALID", "record-review-result evidence_refs must cover the review result and every finding.");
    }
    const reviewedExecutionId = execution.execution_result?.execution_id;
    const consumedDynamicExpansions = review.verdict === "clean" && reviewedExecutionId ? (current.runtimeState.dynamic_expansions ?? []).map((item) => {
      const belongsToReviewedExecution = item.execution_id === reviewedExecutionId && item.step_id === review.step_id && item.mode === execution.mode && (item.change_set_id === undefined || item.change_set_id === review.change_set_id);
      return belongsToReviewedExecution ? { ...item, reviewed_by_review_id: review.review_id, reviewed_at: now } : item;
    }) : current.runtimeState.dynamic_expansions;
    const stateAfterDynamicReview = {
      ...current.runtimeState,
      ...consumedDynamicExpansions === undefined ? {} : { dynamic_expansions: consumedDynamicExpansions },
      dynamic_review_required: false
    };
    const dynamicReviewRequired = review.verdict === "clean" ? dynamicReviewRequiredForCurrentExecution({ ...current, runtimeState: stateAfterDynamicReview }) : current.runtimeState.dynamic_review_required === true;
    return {
      next: {
        ...current.runtimeState,
        pending_review_result: { ...review, recorded_at: now },
        ...consumedDynamicExpansions === undefined ? {} : { dynamic_expansions: consumedDynamicExpansions },
        ...review.verdict === "clean" ? { dynamic_review_required: dynamicReviewRequired } : {},
        applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
      }
    };
  }
  if (delta.action === "mark-replan-blocked") {
    ensureAuthorityKinds(proposal, ["active-task-owner", "scope-admission", "evidence-admission"]);
    if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active") {
      fail3("REPLAN_TRANSITION_INVALID", "mark-replan-blocked requires active + active.");
    }
    const nextWithoutAudit = {
      ...current.runtimeState,
      workflow_status: "blocked_by_replan",
      lifecycle_state: "active",
      ...current.runtimeState.review_coverage ? { review_coverage: {
        ...current.runtimeState.review_coverage,
        target: captureReviewTarget(root2, current.runtimeState.review_coverage.target.entries.map((item) => item.path)),
        pending_paths: [...new Set([
          ...current.runtimeState.review_coverage.pending_paths,
          ...current.runtimeState.review_coverage.target.entries.map((item) => item.path)
        ])],
        last_clean_revision: null
      } } : {},
      applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
    };
    const audit = makeReplanAudit(current, proposal, nextWithoutAudit, now);
    return {
      next: { ...nextWithoutAudit, execution_log: appendExecutionLogEntry(current.runtimeState, audit) },
      audit
    };
  }
  if (delta.action === "clear-replan-block") {
    ensureAuthorityKinds(proposal, ["active-task-owner", "scope-admission", "evidence-admission"]);
    if (current.runtimeState.workflow_status !== "blocked_by_replan" || current.runtimeState.lifecycle_state !== "active") {
      fail3("REPLAN_TRANSITION_INVALID", "clear-replan-block requires blocked_by_replan + active.");
    }
    const nextWithoutAudit = {
      ...current.runtimeState,
      workflow_status: "active",
      lifecycle_state: "active",
      applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
    };
    const audit = makeReplanAudit(current, proposal, nextWithoutAudit, now);
    return {
      next: { ...nextWithoutAudit, execution_log: appendExecutionLogEntry(current.runtimeState, audit) },
      audit
    };
  }
  if (delta.action === "commit-replan") {
    fail3("REPLAN_CONFIRMATION_REQUIRED", "Direct commit-replan is disabled until a revision-bound candidate, complete prior-obligation disposition, and explicit confirmation are available. CURRENT_TASK was not changed.");
  }
  if (delta.action === "commit-scope-amendment") {
    fail3("SCOPE_AMENDMENT_CONFIRMATION_REQUIRED", "Direct commit-scope-amendment is disabled; use prepare-task:amend-scope so Runtime can generate and bind the candidate internally. CURRENT_TASK was not changed.");
  }
  if (delta.action === "record-evidence-challenge") {
    ensureAuthorityKinds(proposal, ["active-task-owner", "evidence-admission"]);
    if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active")
      fail3("EVIDENCE_CHALLENGE_STATE_INVALID", "Evidence challenges require the active task.");
    const claim = current.runtimeState.claim_evidence?.find((item) => item.claim_id === delta.claim_id);
    const slot = claim?.slots.find((item) => item.slot_id === delta.slot_id);
    if (!slot?.report || slot.report.result_id !== delta.result_id)
      fail3("EVIDENCE_CHALLENGE_TARGET_INVALID", "Challenge must bind an existing claim, slot, and report result.");
    if (!delta.evidence_refs.includes(delta.evidence_ref) || !proposal.evidence_refs.includes(delta.evidence_ref))
      fail3("EVIDENCE_CHALLENGE_ADMISSION_REQUIRED", "Challenge evidence must be admitted by both proposal and delta.");
    const evidencePath = path12.resolve(root2, delta.evidence_ref);
    if (!fs11.existsSync(evidencePath) || !fs11.statSync(evidencePath).isFile() || crypto10.createHash("sha256").update(fs11.readFileSync(evidencePath)).digest("hex") !== delta.evidence_sha256)
      fail3("EVIDENCE_CHALLENGE_SOURCE_STALE", "Challenge artifact is missing or changed.");
    const challengeId = `challenge-${digest3({ task: current.runtimeState.task_id, claim: delta.claim_id, slot: delta.slot_id, result: delta.result_id, evidence_sha256: delta.evidence_sha256 }).slice(0, 32)}`;
    if ((current.runtimeState.evidence_challenges ?? []).some((item) => item.challenge_id === challengeId))
      fail3("EVIDENCE_CHALLENGE_DUPLICATE", "The same challenge is already recorded.");
    if ((current.runtimeState.evidence_challenges ?? []).filter((item) => item.claim_id === delta.claim_id && item.slot_id === delta.slot_id && item.result_id === delta.result_id).length >= 3)
      fail3("EVIDENCE_CHALLENGE_BUDGET_EXHAUSTED", "Three distinct counterevidence submissions for this result are already retained.");
    const challenge = {
      challenge_id: challengeId,
      claim_id: delta.claim_id,
      slot_id: delta.slot_id,
      result_id: delta.result_id,
      evidence_ref: delta.evidence_ref,
      evidence_sha256: delta.evidence_sha256,
      reason: delta.reason,
      status: "contested",
      source_revision: current.sourceTuple.revision,
      correction_step_id: null
    };
    return { next: { ...current.runtimeState, evidence_challenges: [...current.runtimeState.evidence_challenges ?? [], challenge], applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision) } };
  }
  if (delta.action === "dismiss-evidence-challenge") {
    ensureAuthorityKinds(proposal, ["active-task-owner", "evidence-admission"]);
    if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active")
      fail3("EVIDENCE_CHALLENGE_STATE_INVALID", "Challenge assessment requires the active task.");
    const challenge = current.runtimeState.evidence_challenges?.find((item) => item.challenge_id === delta.challenge_id);
    if (!challenge || challenge.status !== "contested" || challenge.correction_step_id !== null)
      fail3("EVIDENCE_CHALLENGE_STATE_INVALID", "Only an unresolved contested challenge may be dismissed.");
    if (!delta.evidence_refs.includes(delta.evidence_ref) || !proposal.evidence_refs.includes(delta.evidence_ref))
      fail3("EVIDENCE_CHALLENGE_ADMISSION_REQUIRED", "Challenge assessment evidence must be admitted by both proposal and delta.");
    const evidencePath = path12.resolve(root2, delta.evidence_ref);
    if (!fs11.existsSync(evidencePath) || !fs11.statSync(evidencePath).isFile() || crypto10.createHash("sha256").update(fs11.readFileSync(evidencePath)).digest("hex") !== delta.evidence_sha256)
      fail3("EVIDENCE_CHALLENGE_SOURCE_STALE", "Challenge assessment artifact is missing or changed.");
    const resolution = {
      kind: "not-substantiated",
      evidence_ref: delta.evidence_ref,
      evidence_sha256: delta.evidence_sha256,
      reason: delta.reason,
      source_revision: current.sourceTuple.revision
    };
    return { next: {
      ...current.runtimeState,
      evidence_challenges: current.runtimeState.evidence_challenges.map((item) => item.challenge_id === challenge.challenge_id ? { ...item, status: "resolved", resolution } : item),
      applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
    } };
  }
  if (delta.action === "retry-step") {
    if ((recoveryProblemBudget(current)?.failedAttempts ?? 0) >= 3)
      fail3("RETRY_BUDGET_EXHAUSTED", "The same recovery problem has exhausted three retained failed attempts across plans.");
    ensureAuthorityKinds(proposal, ["active-task-owner", "scope-admission", "evidence-admission"]);
    assertTestStrategySequenceReady(current);
    if (proposal.mode !== "default" || current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active" || current.runtimeState.resume_requires_review || delta.step_id !== current.runtimeState.active_step_id || current.runtimeState.active_step_status !== "blocked")
      fail3("RETRY_STATE_INVALID", "Retry requires the active blocked ordinary step.");
    if (current.runtimeState.pending_review_result || current.runtimeState.findings.some((f) => ["admitted", "in-progress"].includes(f.status)))
      fail3("RETRY_FINDINGS_BLOCKED", "Retry cannot consume pending review or findings.");
    const ledger = current.runtimeState.step_attempts?.[delta.step_id];
    const failed = ledger?.attempts.at(-1);
    if (!ledger || ledger.evidence_plan_revision !== current.runtimeState.evidence_plan_revision || !failed || failed.attempt_id !== delta.blocked_attempt_id || failed.status !== "blocked" || !failed.blocker)
      fail3("RETRY_ATTEMPT_CONFLICT", "Retry must bind the durable latest failure in the same plan.");
    if (ledger.attempts.length >= ledger.max_attempts)
      fail3("RETRY_BUDGET_EXHAUSTED", "Three attempts exhausted; route to debug-task or the user.");
    if (!delta.blocker_resolution_refs.every((ref) => delta.evidence_refs.includes(ref) && proposal.evidence_refs.includes(ref)))
      fail3("RETRY_RESOLUTION_REQUIRED", "Retry evidence must cover resolution artifacts.");
    validateRetryResolution(root2, current, delta, failed.blocker);
    const attempt = { attempt_id: `attempt-${digest3({ document: current.sourceTuple.document_id, plan: ledger.evidence_plan_revision, step: delta.step_id, n: ledger.attempts.length + 1 }).slice(0, 40)}`, idempotency_key: proposal.idempotency_key, request_digest: retryRequestDigest(current, delta), status: "ready", blocker: null, ...delta.repair_diagnosis ? { recovery: delta.repair_diagnosis } : {}, evidence_refs: [...delta.blocker_resolution_refs] };
    const { execution_preflight: _staleExecutionPreflight, ...stateWithoutExecutionPreflight } = current.runtimeState;
    return { next: { ...stateWithoutExecutionPreflight, active_step_status: "ready", step_attempts: { ...current.runtimeState.step_attempts, [delta.step_id]: { ...ledger, attempts: [...ledger.attempts, attempt] } }, ...current.runtimeState.review_coverage ? { review_coverage: { ...current.runtimeState.review_coverage, last_clean_revision: null } } : {}, applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision) } };
  }
  if (delta.action === "record-step-preflight") {
    if ((recoveryProblemBudget(current)?.failedAttempts ?? 0) >= 3)
      fail3("RETRY_BUDGET_EXHAUSTED", "The same recovery problem has exhausted three retained failed attempts across plans.");
    ensureAuthorityKinds(proposal, ["active-task-owner", "scope-admission", "evidence-admission"]);
    const executionMode = delta.mode ?? proposal.mode;
    if (delta.step_id !== current.runtimeState.active_step_id)
      fail3("ACTIVE_STEP_CONFLICT", "preflight must bind the current active step.");
    if (executionMode === "default")
      assertOrdinaryPreflight(current, root2);
    else if (executionMode !== "repair")
      fail3("RUNTIME_MODE_INVALID", "record-step-preflight mode is invalid.");
    assertTestStrategySequenceReady(current);
    const step = resolveCanonicalTaskStep(current).steps.find((step) => step.id === delta.step_id);
    const strategy = resolveTestStrategyExecutionContext(current);
    const executionPhase = resolveNewExecutionPhase(current, strategy, delta.execution_phase);
    const authorityEvaluation = evaluateCurrentExecutionTargetAdmissions(root2, current, {
      target_paths: delta.candidate_paths,
      planned_targets: current.mutationAuthority ? v2StepPlannedTargets(current) : undefined,
      step_mutation_scope: (step.mutation_scope ?? "").split(",").map((value) => value.trim().replace(/^`|`$/gu, "")).filter(Boolean),
      assessments: delta.blast_radius_assessments ?? [],
      phase: executionPhase,
      mode: executionMode,
      location: `${executionMode} preflight candidates`
    });
    assertExecutionAdmissionEvaluation(current, authorityEvaluation, `${executionMode} preflight candidates`);
    if (executionMode === "repair") {
      const pending = current.runtimeState.pending_review_result;
      const repairFingerprints = delta.repair_fingerprints ?? [];
      if (!pending || pending.verdict !== "findings" || !delta.review_id || delta.review_id !== pending.review_id) {
        fail3("REVIEW_FINDINGS_REQUIRED", "repair preflight requires the current findings review identity.");
      }
      if (repairFingerprints.length === 0 || !delta.repair_wave_id || !delta.change_set_id || delta.change_set_id !== pending.change_set_id || !delta.review_target_paths || delta.review_target_paths.length === 0) {
        fail3("REPAIR_PREFLIGHT_IDENTITY_REQUIRED", "repair preflight must bind findings, repair wave, review, target paths, and change set.");
      }
      for (const fingerprint of repairFingerprints) {
        const finding = current.runtimeState.findings.find((item) => item.fingerprint === fingerprint);
        if (!finding || !["admitted", "in-progress"].includes(finding.status))
          fail3("FINDING_ADMISSION_REQUIRED", `repair fingerprint ${fingerprint} is not an admitted current-task finding.`);
      }
    }
    const claims = copyClaimEvidence(current.runtimeState.claim_evidence ?? []);
    if (executionMode === "default")
      for (const record of claims)
        for (const slot of record.slots) {
          if (slot.before_step_id !== delta.step_id || slot.prerequisite_receipt)
            continue;
          assertEvidenceSlotSatisfied(root2, current, record, slot, false);
          slot.prerequisite_receipt = { step_id: delta.step_id, preflight_id: proposal.idempotency_key, result_id: slot.report.result_id, subject_snapshot: captureReviewTarget(root2, slot.check.subject_paths) };
        }
    const coverageAdmission = registerReviewCoverage(root2, current, delta.candidate_paths);
    const coverage = coverageAdmission.coverage;
    const ledger = current.runtimeState.step_attempts?.[delta.step_id];
    let stepAttempts = current.runtimeState.step_attempts;
    if (executionMode === "default") {
      const inherited = ledger === undefined ? inheritedScopeAmendmentAttemptLedger(root2, current, delta.step_id) : null;
      const recovery = ledger?.attempts.at(-1)?.recovery;
      if (recovery && delta.candidate_paths.some((p) => !recovery.repair_paths.includes(p)))
        fail3("RETRY_SCOPE_BLOCKED", "Recovered preflight candidates must stay within the admitted diagnosis paths.");
      const initialLedger = {
        evidence_plan_revision: current.runtimeState.evidence_plan_revision,
        max_attempts: 3,
        attempts: [{
          attempt_id: nextStepAttemptId(current),
          idempotency_key: proposal.idempotency_key,
          request_digest: null,
          status: "preflighted",
          blocker: null,
          evidence_refs: [...delta.evidence_refs]
        }]
      };
      if (!ledger && inherited) {
        if (inherited.ledger.attempts.length >= inherited.ledger.max_attempts)
          fail3("RETRY_BUDGET_EXHAUSTED", "The scope-amendment continuation has exhausted the retained attempt budget.");
        const inheritedLatest = inherited.ledger.attempts.at(-1);
        const continuationAttempts = inheritedLatest.status === "ready" ? [...inherited.ledger.attempts.slice(0, -1), { ...inheritedLatest, status: "preflighted" }] : [...inherited.ledger.attempts, initialLedger.attempts[0]];
        stepAttempts = {
          ...stepAttempts,
          [delta.step_id]: {
            evidence_plan_revision: current.runtimeState.evidence_plan_revision,
            max_attempts: inherited.ledger.max_attempts,
            attempts: continuationAttempts
          }
        };
      } else if (!ledger) {
        stepAttempts = { ...stepAttempts, [delta.step_id]: initialLedger };
      } else if (ledger.attempts.at(-1)?.status === "ready") {
        stepAttempts = { ...stepAttempts, [delta.step_id]: { ...ledger, attempts: ledger.attempts.map((attempt, index) => index === ledger.attempts.length - 1 ? { ...attempt, status: "preflighted" } : attempt) } };
      }
    }
    const dynamicDecisions = authorityEvaluation?.decisions.filter((item) => item.classification === "dynamic-self-admitted") ?? [];
    const existingExpansions = current.runtimeState.dynamic_expansions ?? [];
    const executionId = delta.execution_id ?? `execution-${digest3({ proposal: proposal.idempotency_key, step: delta.step_id, mode: executionMode })}`;
    const planRevision = delta.plan_revision ?? digest3({ step_id: delta.step_id, evidence_plan_revision: current.runtimeState.evidence_plan_revision });
    const changeSetId = delta.change_set_id ?? coverage.change_set_id;
    const newExpansions = dynamicDecisions.filter((item) => !existingExpansions.some((existing) => existing.path === item.path && existing.execution_id === executionId && existing.step_id === delta.step_id && existing.plan_revision === planRevision && existing.change_set_id === changeSetId)).map((item) => ({
      path: item.path,
      domain: item.domain,
      assessment: item.assessment,
      review_required: item.dynamic_review_required,
      first_touch_state: item.first_touch_state === "absent" ? "absent" : "file",
      admitted_at: now,
      execution_id: executionId,
      preflight_id: proposal.idempotency_key,
      step_id: delta.step_id,
      plan_revision: planRevision,
      change_set_id: changeSetId,
      mode: executionMode
    }));
    const executionPreflight = current.mutationAuthority || executionMode === "repair" ? {
      execution_id: executionId,
      preflight_id: proposal.idempotency_key,
      mode: executionMode,
      step_id: delta.step_id,
      plan_revision: planRevision,
      execution_phase: executionPhase,
      candidate_paths: [...delta.candidate_paths],
      change_set_id: changeSetId,
      repair_fingerprints: executionMode === "repair" ? [...delta.repair_fingerprints ?? []] : null,
      repair_wave_id: executionMode === "repair" ? delta.repair_wave_id : null,
      review_id: executionMode === "repair" ? delta.review_id : null,
      review_target_paths: executionMode === "repair" ? [...delta.review_target_paths] : null
    } : undefined;
    return { next: {
      ...current.runtimeState,
      review_coverage: coverage,
      ...stepAttempts ? { step_attempts: stepAttempts } : {},
      claim_evidence: claims,
      ...authorityEvaluation ? {
        dynamic_review_required: current.runtimeState.dynamic_review_required === true || authorityEvaluation.dynamic_review_required,
        dynamic_expansions: [...existingExpansions, ...newExpansions]
      } : {},
      ...executionPreflight === undefined ? {} : { execution_preflight: executionPreflight },
      applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
    }, reviewPreimageWrites: coverageAdmission.preimageWrites };
  }
  if (delta.action === "extend-preflight") {
    if (!current.mutationAuthority)
      fail3("MUTATION_AUTHORITY_VERSION_REQUIRED", "extend-preflight requires a Mutation Authority v2 task.");
    const executionMode = delta.mode ?? proposal.mode;
    if (executionMode !== "default" && executionMode !== "repair")
      fail3("RUNTIME_MODE_INVALID", "extend-preflight mode is invalid.");
    if (executionMode === "default" && (recoveryProblemBudget(current)?.failedAttempts ?? 0) >= 3)
      fail3("RETRY_BUDGET_EXHAUSTED", "The same recovery problem has exhausted three retained failed attempts across plans.");
    if (executionMode === "default")
      assertOrdinaryPreflight(current, root2);
    assertTestStrategySequenceReady(current);
    ensureAuthorityKinds(proposal, ["active-task-owner", "scope-admission", "evidence-admission"]);
    if (delta.step_id !== current.runtimeState.active_step_id)
      fail3("ACTIVE_STEP_CONFLICT", "extend-preflight must bind the current active step.");
    const activePreflight = current.runtimeState.execution_preflight;
    if (!activePreflight)
      fail3("EXECUTE_PREFLIGHT_REQUIRED", "extend-preflight requires the current stable execution identity.");
    if (activePreflight.step_id !== delta.step_id || activePreflight.mode !== executionMode) {
      fail3("EXECUTE_PREFLIGHT_STALE", "extend-preflight does not bind the current execution mode and step.");
    }
    if (delta.current_preflight_id !== activePreflight.preflight_id)
      fail3("EXECUTE_PREFLIGHT_STALE", "extend-preflight does not bind the current replacement receipt.");
    if (delta.execution_id !== undefined && delta.execution_id !== activePreflight.execution_id)
      fail3("EXECUTE_PREFLIGHT_STALE", "extend-preflight does not bind the stable execution identity.");
    if (delta.execution_phase !== undefined && delta.execution_phase !== activePreflight.execution_phase)
      fail3("EXECUTE_PREFLIGHT_STALE", "extend-preflight does not bind the current execution phase.");
    const stepLedger = current.runtimeState.step_attempts?.[delta.step_id];
    const latestAttempt = stepLedger?.attempts.at(-1);
    if (executionMode === "default" && (!stepLedger || !latestAttempt || latestAttempt.status !== "preflighted"))
      fail3("EXECUTE_PREFLIGHT_REQUIRED", "extend-preflight requires the latest ordinary attempt to be preflighted.");
    if (executionMode === "default" && latestAttempt && latestAttempt.idempotency_key !== delta.current_preflight_id)
      fail3("EXECUTE_PREFLIGHT_STALE", "extend-preflight does not bind the latest ordinary preflight receipt.");
    if (executionMode === "repair") {
      const pending = current.runtimeState.pending_review_result;
      if (!activePreflight || activePreflight.review_id === null || activePreflight.repair_wave_id === null || activePreflight.change_set_id !== pending?.change_set_id || activePreflight.review_id !== pending?.review_id || pending?.verdict !== "findings") {
        fail3("REPAIR_PREFLIGHT_STALE", "repair extension requires the current Runtime repair-wave and review identity.");
      }
    }
    const existingPaths = activePreflight?.candidate_paths ?? current.runtimeState.review_coverage?.base.entries.map((entry) => entry.path) ?? [];
    if (delta.additional_targets.some((target) => existingPaths.includes(target)))
      fail3("EXECUTE_PREFLIGHT_SCOPE_CONFLICT", "extend-preflight cannot re-admit a path already covered by the current preflight.");
    const step = resolveCanonicalTaskStep(current).current;
    const strategy = resolveTestStrategyExecutionContext(current);
    const executionPhase = activePreflight?.execution_phase ?? delta.execution_phase ?? executionPhaseForCurrentStep(current, strategy);
    const authorityEvaluation = evaluateCurrentExecutionTargetAdmissions(root2, current, {
      target_paths: [...existingPaths, ...delta.additional_targets],
      planned_targets: v2StepPlannedTargets(current),
      step_mutation_scope: (step.mutation_scope ?? "").split(",").map((value) => value.trim().replace(/^`|`$/gu, "")).filter(Boolean),
      assessments: delta.blast_radius_assessments,
      phase: executionPhase,
      mode: executionMode,
      location: `${executionMode} preflight extension`
    });
    assertExecutionAdmissionEvaluation(current, authorityEvaluation, `${executionMode} preflight extension`);
    const coverageAdmission = extendReviewCoverage(root2, current, delta.additional_targets);
    const coverage = coverageAdmission.coverage;
    const existingExpansions = current.runtimeState.dynamic_expansions ?? [];
    const executionId = activePreflight?.execution_id ?? delta.execution_id ?? `execution-${digest3({ proposal: current.runtimeState.execution_preflight?.preflight_id ?? delta.current_preflight_id, step: delta.step_id, mode: executionMode })}`;
    const planRevision = activePreflight?.plan_revision ?? digest3({ step_id: delta.step_id, evidence_plan_revision: current.runtimeState.evidence_plan_revision });
    const changeSetId = activePreflight?.change_set_id ?? coverage.change_set_id;
    const newExpansions = authorityEvaluation.decisions.filter((item) => item.classification === "dynamic-self-admitted" && !existingExpansions.some((existing) => existing.path === item.path && existing.execution_id === executionId && existing.step_id === delta.step_id && existing.plan_revision === planRevision && existing.change_set_id === changeSetId)).map((item) => ({
      path: item.path,
      domain: item.domain,
      assessment: item.assessment,
      review_required: item.dynamic_review_required,
      first_touch_state: item.first_touch_state === "absent" ? "absent" : "file",
      admitted_at: now,
      execution_id: executionId,
      preflight_id: proposal.idempotency_key,
      step_id: delta.step_id,
      plan_revision: planRevision,
      change_set_id: changeSetId,
      mode: executionMode
    }));
    const updatedAttempts = stepLedger?.attempts.map((attempt, index) => index === stepLedger.attempts.length - 1 ? { ...attempt, idempotency_key: proposal.idempotency_key, evidence_refs: [...new Set([...attempt.evidence_refs, ...delta.evidence_refs])] } : attempt);
    const executionPreflight = {
      execution_id: executionId,
      preflight_id: proposal.idempotency_key,
      mode: executionMode,
      step_id: delta.step_id,
      plan_revision: planRevision,
      execution_phase: executionPhase,
      candidate_paths: [...existingPaths, ...delta.additional_targets],
      change_set_id: changeSetId,
      repair_fingerprints: activePreflight?.repair_fingerprints ?? null,
      repair_wave_id: activePreflight?.repair_wave_id ?? null,
      review_id: activePreflight?.review_id ?? null,
      review_target_paths: activePreflight?.review_target_paths ?? null
    };
    return { next: {
      ...current.runtimeState,
      review_coverage: coverage,
      dynamic_review_required: current.runtimeState.dynamic_review_required === true || authorityEvaluation.dynamic_review_required,
      dynamic_expansions: [...existingExpansions, ...newExpansions],
      ...executionMode === "default" && stepLedger && updatedAttempts ? { step_attempts: { ...current.runtimeState.step_attempts, [delta.step_id]: { ...stepLedger, attempts: updatedAttempts } } } : {},
      execution_preflight: executionPreflight,
      applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
    }, reviewPreimageWrites: coverageAdmission.preimageWrites };
  }
  if (delta.action !== "step-progress")
    fail3("RUNTIME_SCHEMA_INVALID", "Only step-progress reaches the execute-step state handler.");
  ensureAuthorityKinds(proposal, ["active-task-owner", "scope-admission", "evidence-admission"]);
  if (current.runtimeState.workflow_status === "draft" && current.runtimeState.lifecycle_state === "active") {
    fail3("DRAFT_NOT_EXECUTABLE", "execute-step is blocked for draft + active until prepare-task:confirm commits confirm-draft.");
  }
  if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active") {
    fail3("TASK_STATE_NOT_ACTIVE", "execute-step requires the current task to be active + active.");
  }
  if (current.runtimeState.resume_requires_review) {
    fail3("RESUME_REVIEW_REQUIRED", "execute-step cannot proceed until prepare-task clears the resume review gate.");
  }
  if (delta.step_id !== current.runtimeState.active_step_id)
    fail3("ACTIVE_STEP_CONFLICT", "Proposal step_id does not match the admitted current step.");
  if (proposal.mode === "default" && !delta.review_receipt) {
    const budget = recoveryProblemBudget(current);
    if (budget) {
      const ledger = current.runtimeState.step_attempts?.[delta.step_id], attempt = ledger?.attempts.at(-1);
      if (!attempt && budget.failedAttempts >= 3)
        fail3("RETRY_BUDGET_EXHAUSTED", "The recovery problem has exhausted its retained failed attempts.");
      if (!attempt || ledger.evidence_plan_revision !== current.runtimeState.evidence_plan_revision || !["preflighted", "implemented"].includes(attempt.status) || delta.execution_result && delta.execution_result.attempt_id !== attempt.attempt_id)
        fail3("RETRY_PREFLIGHT_REQUIRED", "Recovery progress requires the durable current preflighted attempt, including through raw apply.");
    }
  }
  if (delta.status === "completed" || delta.execution_result && delta.execution_result.outcome !== "blocked")
    assertArtifactRestoreCompleted(root2, current);
  const currentClaimEvidenceEnabled = claimEvidenceStateEnabled(current.runtimeState);
  if (!currentClaimEvidenceEnabled && delta.claim_evidence !== undefined) {
    fail3("CLAIM_EVIDENCE_MIGRATION_REQUIRED", "execute-step cannot create a claim_evidence plan for a legacy task; prepare-task refinement/migration must persist the plan first.");
  }
  if (currentClaimEvidenceEnabled) {
    const plannedClaimEvidence = current.runtimeState.claim_evidence ?? [];
    if (plannedClaimEvidence.length === 0) {
      fail3("CLAIM_EVIDENCE_REQUIRED", "the strict task has an empty claim_evidence plan; prepare-task must repair it before execution.");
    }
    requireAcceptanceClaim(plannedClaimEvidence, "current task claim_evidence");
  }
  if (proposal.mode !== "repair" && !delta.review_receipt)
    assertOrdinaryPreflight(current, root2);
  const executionMode = proposal.mode;
  assertStepProgressExecutionIdentity(current, delta);
  const stepResolution = resolveCanonicalTaskStep(current);
  const checkpoint = effectiveCheckpointPolicy(stepResolution);
  assertTestStrategyExecutionTransition(current, delta);
  const currentStepRepairLogs = currentDefinitionExecutionLog(current).filter((item) => !("action" in item) && item.step_id === delta.step_id && item.mode === "repair");
  const openFindings = current.runtimeState.findings.filter((item) => item.status === "admitted" || item.status === "in-progress");
  const deltaRepairFingerprints = delta.repair_fingerprints ?? (delta.repair_fingerprint ? [delta.repair_fingerprint] : []);
  if (executionMode === "repair") {
    if (deltaRepairFingerprints.length === 0)
      fail3("FINDING_ADMISSION_REQUIRED", "repair mode requires one or more admitted finding fingerprints.");
    for (const fingerprint of deltaRepairFingerprints) {
      const finding = current.runtimeState.findings.find((item) => item.fingerprint === fingerprint);
      if (!finding || !["admitted", "in-progress"].includes(finding.status))
        fail3("FINDING_ADMISSION_REQUIRED", `repair fingerprint ${fingerprint} is not an admitted current-task finding.`);
      if (delta.repair_wave_id && finding.last_repair_wave_id !== delta.repair_wave_id) {
        fail3("REPAIR_WAVE_CONFLICT", `repair fingerprint ${fingerprint} was not recorded in repair wave ${delta.repair_wave_id}.`);
      }
    }
    if (!delta.change_set_id)
      fail3("REPAIR_CHANGE_SET_REQUIRED", "repair mode requires one Runtime-owned logical change_set_id.");
    if (delta.review_receipt !== undefined)
      fail3("REVIEW_READ_ONLY_VIOLATION", "repair execution cannot attach a review receipt; verification remains a separate review result.");
  } else {
    if (deltaRepairFingerprints.length > 0 || delta.repair_wave_id !== undefined)
      fail3("RUNTIME_MODE_INVALID", "default execution cannot carry repair bookkeeping.");
    if (delta.review_receipt !== undefined && delta.status !== "completed")
      fail3("REVIEW_RECEIPT_REQUIRED", "review receipt is only valid when completing the current step.");
    if (delta.change_set_id !== undefined && delta.review_receipt !== undefined && delta.change_set_id !== delta.review_receipt.change_set_id) {
      fail3("REVIEW_TARGET_CONFLICT", "step-progress change_set_id must match the review receipt change_set_id.");
    }
  }
  const executionChangeSetId = delta.change_set_id ?? delta.review_receipt?.change_set_id;
  const oldStatus = current.runtimeState.active_step_status;
  const newStatus = delta.status;
  if (newStatus === "completed" && executionMode !== "repair" && dynamicReviewRequiredForCurrentExecution(current)) {
    fail3("DYNAMIC_REVIEW_REQUIRED", "An in-envelope mutation footprint expansion requires a clean cumulative review before step completion.");
  }
  if (delta.review_receipt !== undefined && delta.execution_result !== undefined) {
    fail3("REVIEWED_COMPLETION_EXECUTION_RESULT_FORBIDDEN", "reviewed step completion cannot record a new execution result or acceptance evidence.");
  }
  if (delta.execution_result !== undefined) {
    assertExecutionResultWaivers(root2, current, delta.step_id, delta.execution_result);
    if (!executionChangeSetId || delta.execution_result.change_set_id !== executionChangeSetId) {
      fail3("REVIEW_TARGET_CONFLICT", "execution_result must bind the step-progress Runtime change_set_id.");
    }
    const targetPaths = delta.execution_result.review_target.entries.map((item) => item.path);
    const currentTarget = captureReviewTarget(root2, targetPaths);
    if (currentTarget.revision !== delta.execution_result.review_target.revision) {
      fail3("REVIEW_TARGET_STALE", "product files changed while the execution result was being recorded.");
    }
    if (delta.execution_result.outcome === "blocked" !== (newStatus === "blocked")) {
      fail3("RUNTIME_STATE_CONFLICT", "execution_result outcome must match the step-progress status.");
    }
    const structuredEvidenceRefs = [...new Set([
      ...delta.execution_result.command_results.flatMap((item) => item.evidence_refs),
      ...delta.execution_result.validation_results.flatMap((item) => item.evidence_refs),
      ...delta.execution_result.acceptance_evidence.flatMap((item) => item.evidence_refs)
    ])];
    if (structuredEvidenceRefs.length !== delta.evidence_refs.length || structuredEvidenceRefs.some((item) => !delta.evidence_refs.includes(item))) {
      fail3("RUNTIME_STATE_CONFLICT", "step-progress evidence_refs must exactly match the structured execution_result evidence.");
    }
    if (current.mutationAuthority) {
      const strategy = resolveTestStrategyExecutionContext(current);
      const step = stepResolution.current;
      const observedWritePaths = delta.execution_result.command_results.flatMap((item) => item.observed_repo_writes);
      const executionTargetPaths = [...new Set([...targetPaths, ...observedWritePaths])];
      const executionPhase = current.runtimeState.execution_preflight?.step_id === delta.step_id ? current.runtimeState.execution_preflight.execution_phase : executionPhaseForCurrentStep(current, strategy);
      const executionEvaluation = evaluateCurrentExecutionTargetAdmissions(root2, current, {
        target_paths: executionTargetPaths,
        planned_targets: current.mutationAuthority ? v2StepPlannedTargets(current) : undefined,
        step_mutation_scope: (step.mutation_scope ?? "").split(",").map((value) => value.trim().replace(/^`|`$/gu, "")).filter(Boolean),
        phase: executionPhase,
        mode: executionMode,
        location: "step-progress execution result"
      });
      assertExecutionAdmissionEvaluation(current, executionEvaluation, "step-progress execution result");
    }
  }
  const legal = oldStatus === newStatus || oldStatus === "ready" && ["in-progress", "completed", "blocked"].includes(newStatus) || oldStatus === "in-progress" && ["completed", "blocked"].includes(newStatus) || oldStatus === "blocked" && executionMode === "repair" && ["in-progress", "completed"].includes(newStatus);
  if (!legal)
    fail3("TASK_STATE_TRANSITION_INVALID", `Cannot transition active step from ${oldStatus} to ${newStatus}.`);
  const claimEvidenceRequired = currentClaimEvidenceEnabled;
  const transitionClaimEvidence = delta.claim_evidence ?? current.runtimeState.claim_evidence ?? [];
  if (currentClaimEvidenceEnabled && delta.claim_evidence !== undefined) {
    assertClaimEvidencePlanPreserved(current.runtimeState.claim_evidence ?? [], delta.claim_evidence, "semantic_delta.claim_evidence");
    if (delta.review_receipt !== undefined && digest3(current.runtimeState.claim_evidence ?? []) !== digest3(delta.claim_evidence)) {
      fail3("CLAIM_EVIDENCE_AFTER_REVIEW", "reviewed step completion must preserve the claim evidence recorded before review.");
    }
  }
  for (const record of transitionClaimEvidence)
    for (const slot of record.slots) {
      const old = current.runtimeState.claim_evidence?.find((item) => item.claim_id === record.claim_id)?.slots.find((item) => item.slot_id === slot.slot_id);
      if (slot.report && digest3(old) !== digest3(slot) && slot.check?.selection?.invocation) {
        const exactResult = delta.execution_result?.acceptance_evidence.some((result) => !("acceptance" in result) && result.claim_id === record.claim_id && result.slot_id === slot.slot_id && result.check_id === slot.check.check_id && digest3(result.report) === digest3(slot.report));
        if (!exactResult)
          fail3("CLAIM_EVIDENCE_RESULT_UNBOUND", "A new execution report requires its exact invocation result; raw progress cannot invent evidence.");
      }
      if (slot.report && digest3(old) !== digest3(slot))
        assertEvidenceReportApplicable(root2, current, slot);
      if (old?.report && slot.report?.result_id === old.report.result_id && (digest3(old.report) !== digest3(slot.report) || digest3(old.evidence_refs) !== digest3(slot.evidence_refs)))
        fail3("CLAIM_EVIDENCE_PLAN_CONFLICT", "A changed report or artifact mapping requires a new result_id.");
      if (slot.before_step_id === delta.step_id && !slot.prerequisite_receipt)
        fail3("PREREQUISITE_REQUIRED", "record-step-preflight must consume prerequisites before any execution result or progress.");
    }
  const unresolvedChallenges = (current.runtimeState.evidence_challenges ?? []).filter((item) => item.status !== "resolved" && item.correction_step_id === delta.step_id);
  for (const challenge of unresolvedChallenges) {
    const slot = transitionClaimEvidence.find((item) => item.claim_id === challenge.claim_id)?.slots.find((item) => item.slot_id === challenge.slot_id);
    if (slot?.report?.result_id === challenge.result_id)
      fail3("EVIDENCE_CHALLENGE_UNRESOLVED", "The challenged result ID cannot be resubmitted as a correction.");
  }
  if (newStatus === "completed" && unresolvedChallenges.length > 0) {
    if (!delta.review_receipt || unresolvedChallenges.some((item) => item.status !== "invalidated" || item.correction_step_id !== delta.step_id)) {
      fail3("EVIDENCE_CHALLENGE_UNRESOLVED", "Challenge resolution requires the admitted correction step and its fresh clean review.");
    }
    for (const challenge of unresolvedChallenges) {
      const slot = transitionClaimEvidence.find((item) => item.claim_id === challenge.claim_id)?.slots.find((item) => item.slot_id === challenge.slot_id);
      if (!slot?.report || !slot.check || slot.report.result_id === challenge.result_id || slot.report.status !== slot.check.expected_result || !COMPLETE_CLAIM_EVIDENCE_DISPOSITIONS.includes(slot.disposition)) {
        fail3("EVIDENCE_CHALLENGE_UNRESOLVED", "Correction must provide a new successful report for the exact challenged slot.");
      }
      assertEvidenceReportApplicable(root2, current, slot);
    }
  }
  for (const result of [...delta.execution_result?.command_results ?? [], ...delta.execution_result?.validation_results ?? []]) {
    if (result.status !== "expected-failure")
      continue;
    const entry = "command" in result ? result.command : result.validation;
    const hasCurrentReproduction = transitionClaimEvidence.some((record) => record.claim_kind !== "acceptance" && record.slots.some((slot) => {
      if (slot.applicability !== "before-step" || slot.prerequisite_receipt || slot.check?.entry !== entry || slot.check.expected_result !== "expected-failure" || slot.report?.status !== "expected-failure")
        return false;
      const steps = resolveCanonicalTaskStep(current).steps.map((step) => step.id);
      if (steps.indexOf(delta.step_id) >= steps.indexOf(slot.before_step_id))
        return false;
      const previous = current.runtimeState.claim_evidence?.find((claim) => claim.claim_id === record.claim_id)?.slots.find((item) => item.slot_id === slot.slot_id);
      if (previous?.report?.result_id === slot.report.result_id)
        return false;
      return (delta.execution_result?.acceptance_evidence ?? []).some((evidence) => !("acceptance" in evidence) && evidence.claim_id === record.claim_id && evidence.slot_id === slot.slot_id && evidence.check_id === slot.check.check_id && evidence.report?.result_id === slot.report.result_id);
    }));
    if (!hasCurrentReproduction)
      fail3("EXECUTE_EXPECTED_FAILURE_INVALID", "expected failure requires a new report for an unconsumed prerequisite check submitted by this execution before its constrained step.");
  }
  for (const result of delta.execution_result?.acceptance_evidence ?? []) {
    if ("acceptance" in result)
      fail3("CLAIM_EVIDENCE_PLAN_CONFLICT", "Legacy text-based evidence is readable history only; new results require claim/slot/check IDs.");
    const slot = transitionClaimEvidence.find((claim) => claim.claim_id === result.claim_id)?.slots.find((slot) => slot.slot_id === result.slot_id);
    if (!slot || slot.check?.check_id !== result.check_id || slot.minimum_type !== result.minimum_type || slot.disposition !== result.disposition || digest3(slot.report) !== digest3(result.report) || digest3(slot.evidence_refs) !== digest3(result.evidence_refs))
      fail3("CLAIM_EVIDENCE_PLAN_CONFLICT", "Execution reports must exactly match the persisted slot results.");
    if (slot.check.method === "execution" && slot.check.selection?.invocation && result.report) {
      const command = delta.execution_result.command_results.find((item) => item.command === slot.check.entry);
      const successful = result.report.status === "passed" || result.report.status === "expected-failure";
      if (!command || successful && command.status !== result.report.status || slot.due_step_id !== delta.step_id) {
        fail3("CLAIM_EVIDENCE_RESULT_UNBOUND", "execution evidence must bind its frozen invocation result and due step.");
      }
    }
  }
  if (newStatus === "completed" && !evaluateClaimEvidence(transitionClaimEvidence, { root: root2, current, due_step_id: delta.step_id }).validation_complete)
    fail3("CLAIM_EVIDENCE_INCOMPLETE", "Every due slot must have applicable successful evidence before step completion.");
  let advancement = {
    outcome: "not-applicable",
    from_step_id: delta.step_id,
    to_step_id: null,
    checkpoint
  };
  if (executionMode === "repair" && newStatus === "completed") {
    advancement = {
      outcome: "repair-awaiting-verification",
      from_step_id: delta.step_id,
      to_step_id: null,
      checkpoint
    };
  } else if (executionMode === "default" && newStatus === "completed") {
    if (openFindings.length > 0) {
      fail3("REVIEW_CONVERGENCE_REQUIRED", "step advancement is blocked while an admitted or in-progress finding remains open.");
    }
    if (current.runtimeState.review_coverage && checkpoint === "not-required" && stepResolution.next && !delta.execution_result)
      fail3("REVIEW_EXECUTION_REQUIRED", "An exempt intermediate step must record its actual execution before advancement.");
    if (checkpoint === "required" && delta.review_receipt === undefined) {
      fail3("REVIEW_CHECKPOINT_REQUIRED", `step ${delta.step_id} requires a clean review checkpoint before advancement.`);
    }
    if (delta.review_receipt !== undefined) {
      const pendingReview = current.runtimeState.pending_review_result;
      if (!pendingReview || pendingReview.verdict !== "clean") {
        fail3("CLEAN_REVIEW_REQUIRED", "step completion requires the canonical pending clean review result.");
      }
      if (pendingReview.step_id !== delta.step_id || pendingReview.cycle_id !== delta.review_receipt.cycle_id || pendingReview.cycle_phase !== delta.review_receipt.cycle_phase || pendingReview.change_set_id !== delta.review_receipt.change_set_id || pendingReview.review_target_revision !== delta.review_receipt.review_target_revision || digest3(pendingReview.evidence_refs) !== digest3(delta.review_receipt.evidence_refs)) {
        fail3("REVIEW_RECEIPT_CONFLICT", "step completion review receipt must match the canonical pending clean review.");
      }
      const reviewedExecution = current.runtimeState.execution_log.map((item) => ("action" in item) ? item : cumulativeReviewExecution(current, item)).find((item) => !("action" in item) && item.idempotency_key === pendingReview.execution_id);
      if (!reviewedExecution?.execution_result || reviewedExecution.change_set_id !== pendingReview.change_set_id || reviewedExecution.execution_result.review_target.revision !== pendingReview.review_target_revision) {
        fail3("REVIEW_TARGET_CONFLICT", "canonical clean review no longer binds its recorded execution target.");
      }
      const currentTarget = captureReviewTarget(root2, reviewedExecution.execution_result.review_target.entries.map((item) => item.path));
      if (currentTarget.revision !== pendingReview.review_target_revision) {
        fail3("REVIEW_TARGET_STALE", "product files changed after the clean review was recorded.");
      }
      if (delta.review_receipt.cycle_id !== current.runtimeState.review_cycle.id) {
        fail3("REVIEW_CYCLE_CONFLICT", "review receipt cycle_id does not match the current Runtime review cycle.");
      }
      if (currentStepRepairLogs.length > 0 && delta.review_receipt.cycle_phase !== "verification") {
        fail3("REVIEW_VERIFICATION_REQUIRED", "an admitted repair must re-enter review through verification on the same logical diff.");
      }
      if (currentStepRepairLogs.length === 0 && delta.review_receipt.cycle_phase !== "discovery") {
        fail3("REVIEW_PHASE_INVALID", "a checkpoint without an admitted repair must use discovery review.");
      }
    }
    if (currentStepRepairLogs.length > 0) {
      const repairFingerprints = [...new Set(currentStepRepairLogs.flatMap((item) => {
        if (!item.change_set_id)
          fail3("REPAIR_CHANGE_SET_REQUIRED", "a repair execution record is missing its logical change-set identity.");
        const fingerprints = item.repair_fingerprints ?? (item.repair_fingerprint ? [item.repair_fingerprint] : []);
        if (fingerprints.length === 0)
          fail3("REPAIR_VERIFICATION_REQUIRED", "a repair execution record is missing its finding fingerprints.");
        return fingerprints;
      }))];
      const repairTargets = [...new Set(currentStepRepairLogs.map((item) => item.change_set_id))];
      if (repairTargets.length !== 1)
        fail3("REPAIR_CHANGE_SET_CONFLICT", "all repair attempts for one step must use the same logical change set.");
      const receipt = delta.review_receipt;
      if (!receipt)
        fail3("REVIEW_VERIFICATION_REQUIRED", "repair completion requires a clean verification receipt before advancement.");
      if (receipt.change_set_id !== repairTargets[0])
        fail3("REPAIR_CHANGE_SET_CONFLICT", "verification must cover the exact logical change set repaired by the current step.");
      if (receipt.admitted_fingerprints.length !== repairFingerprints.length || receipt.admitted_fingerprints.some((fingerprint) => !repairFingerprints.includes(fingerprint))) {
        fail3("REVIEW_VERIFICATION_REQUIRED", "verification must cover exactly the admitted repair fingerprints for the current step.");
      }
      for (const fingerprint of repairFingerprints) {
        const finding = current.runtimeState.findings.find((item) => item.fingerprint === fingerprint);
        if (!finding || finding.status !== "resolved") {
          fail3("REVIEW_CONVERGENCE_REQUIRED", `repair finding ${fingerprint} must be resolved only after verification before step advancement.`);
        }
      }
      advancement.review_phase = receipt.cycle_phase;
    } else if (delta.review_receipt) {
      advancement.review_phase = delta.review_receipt.cycle_phase;
    }
    if (stepResolution.next) {
      advancement = {
        ...advancement,
        outcome: "advanced",
        to_step_id: stepResolution.next.id
      };
    } else if (hasRemainingCorrectionTargets(current, delta.step_id)) {
      advancement = { ...advancement, outcome: "not-applicable", to_step_id: null };
    } else {
      if (!claimEvidenceRequired) {
        fail3("CLAIM_EVIDENCE_MIGRATION_REQUIRED", "legacy CURRENT_TASK requires prepare-task refinement/migration before task-complete; aggregate evidence cannot provide terminal completion.");
      }
      if (delta.claim_evidence === undefined) {
        fail3("CLAIM_EVIDENCE_REQUIRED", "the final task-complete step-progress must carry the durable claim_evidence snapshot.");
      }
      const completion = evaluateClaimEvidence(transitionClaimEvidence, { root: root2, current });
      if (!completion.validation_complete) {
        fail3("CLAIM_EVIDENCE_INCOMPLETE", "task-complete requires every planned claim evidence slot to be existing, reused, or newly-executed with evidence_refs.");
      }
      if (!completion.acceptance_satisfied) {
        fail3("CLAIM_EVIDENCE_ACCEPTANCE_REQUIRED", "task-complete requires at least one complete acceptance claim; invariant evidence cannot substitute for acceptance evidence.");
      }
      advancement = {
        ...advancement,
        outcome: "task-complete",
        to_step_id: null
      };
    }
  }
  let stepAttempts = current.runtimeState.step_attempts;
  if (executionMode === "default" && delta.execution_result) {
    const result = delta.execution_result;
    const ledger = stepAttempts?.[delta.step_id];
    const activeAttempt = ledger?.attempts.at(-1);
    if (!ledger && result.attempt_id && result.attempt_id !== nextStepAttemptId(current))
      fail3("RETRY_ATTEMPT_CONFLICT", "Initial attempt identity is Runtime-derived.");
    if (ledger && (ledger.evidence_plan_revision !== current.runtimeState.evidence_plan_revision || result.attempt_id !== activeAttempt?.attempt_id || activeAttempt.status === "ready" || activeAttempt.status === "blocked"))
      fail3("RETRY_PREFLIGHT_REQUIRED", "Results must bind the current preflighted attempt; old receipts cannot run or complete a retry.");
    const observations = [...result.command_results, ...result.validation_results];
    if (result.blocker_kind === "environment" && (result.outcome !== "blocked" || observations.some((r) => r.status === "failed") || !observations.some((r) => r.status === "blocked"))) {
      fail3("RETRY_DIAGNOSIS_REQUIRED", "Environment classification requires a blocked execution without business failures.");
    }
    if (current.runtimeState.evidence_plan_revision && result.attempt_id) {
      const paths = [...new Set([...current.runtimeState.review_coverage?.target.entries.map((e) => e.path) ?? [], ...result.review_target.entries.map((e) => e.path), ...(current.runtimeState.claim_evidence ?? []).flatMap((c) => c.slots.flatMap((s) => s.check?.subject_paths ?? []))])];
      const attempt = { attempt_id: result.attempt_id, idempotency_key: activeAttempt?.idempotency_key ?? proposal.idempotency_key, request_digest: activeAttempt?.request_digest ?? null, status: result.outcome === "blocked" ? "blocked" : "implemented", blocker: result.outcome === "blocked" ? { kind: result.blocker_kind ?? "unknown", execution_result: result, subject_snapshot: captureReviewTarget(root2, paths) } : null, ...activeAttempt?.recovery ? { recovery: activeAttempt.recovery } : {}, evidence_refs: [...new Set([...activeAttempt?.evidence_refs ?? [], ...delta.evidence_refs])] };
      stepAttempts = { ...stepAttempts, [delta.step_id]: { evidence_plan_revision: current.runtimeState.evidence_plan_revision, max_attempts: 3, attempts: ledger ? [...ledger.attempts.slice(0, -1), attempt] : [attempt] } };
    }
  }
  if (executionMode === "default" && newStatus === "completed" && !delta.execution_result && stepAttempts?.[delta.step_id]?.attempts.at(-1)?.status !== undefined && stepAttempts[delta.step_id].attempts.at(-1).status !== "implemented")
    fail3("RETRY_EXECUTION_REQUIRED", "A recovered attempt must run and report before completion.");
  let coverage = current.runtimeState.review_coverage;
  if (delta.execution_result && coverage) {
    const result = delta.execution_result;
    if (result.change_set_id !== coverage.change_set_id)
      fail3("REVIEW_TARGET_CONFLICT", "Execution must retain the cumulative change set.");
    for (const entry of result.review_base.entries) {
      if (digest3(entry) !== digest3(coverage.target.entries.find((e) => e.path === entry.path)))
        fail3("REVIEW_PREFLIGHT_REQUIRED", "Execution base must match the registered before-state.");
    }
    const target = captureReviewTarget(root2, coverage.base.entries.map((e) => e.path));
    const changed = createReviewChangeDelta(coverage.target, target).entries.map((e) => e.path);
    if (digest3([...changed].sort()) !== digest3([...result.actual_changed_paths].sort()))
      fail3("REVIEW_TARGET_CONFLICT", "Execution must account for all changes in the cumulative target.");
    coverage = { ...coverage, target, pending_paths: [...new Set([...coverage.pending_paths, ...changed])].sort() };
  }
  if (coverage && advancement.outcome === "task-complete" && checkpoint === "not-required") {
    if (!(stepResolution.current.checkpoint_boundary?.startsWith("final-exemption:") && stepResolution.current.checkpoint_boundary.slice("final-exemption:".length).trim()))
      fail3("REVIEW_CHECKPOINT_REQUIRED", "Final exemption must explicitly cover the cumulative task.");
    coverage = { ...coverage, pending_paths: [], last_clean_revision: coverage.target.revision };
  }
  if (delta.review_receipt && coverage)
    coverage = { ...coverage, pending_paths: [], last_clean_revision: coverage.target.revision };
  const executionLog = appendExecutionLogEntry(current.runtimeState, {
    idempotency_key: proposal.idempotency_key,
    mode: executionMode,
    step_id: delta.step_id,
    status: newStatus,
    evidence_refs: [...delta.evidence_refs],
    ...delta.note ? { note: delta.note } : {},
    ...delta.repair_fingerprint ? { repair_fingerprint: delta.repair_fingerprint } : {},
    ...delta.repair_fingerprints ? { repair_fingerprints: [...delta.repair_fingerprints] } : {},
    ...delta.repair_wave_id ? { repair_wave_id: delta.repair_wave_id } : {},
    ...executionChangeSetId ? { change_set_id: executionChangeSetId } : {},
    checkpoint,
    advancement: advancement.outcome,
    next_step_id: advancement.to_step_id,
    ...delta.review_receipt ? { review_receipt: delta.review_receipt } : {},
    ...delta.claim_evidence === undefined ? {} : { claim_evidence: copyClaimEvidence(delta.claim_evidence) },
    ...delta.execution_result === undefined ? {} : { execution_result: delta.execution_result },
    recorded_at: now
  });
  const {
    scope_amendment_pending_review_step_id: _scopeAmendmentPendingReviewStepId,
    execution_preflight: currentExecutionPreflight,
    ...stateWithoutExecutionMarkers
  } = current.runtimeState;
  const retainExecutionPreflight = newStatus !== "completed" || advancement.outcome === "repair-awaiting-verification";
  const next = {
    ...stateWithoutExecutionMarkers,
    active_step_id: advancement.outcome === "advanced" ? advancement.to_step_id : current.runtimeState.active_step_id,
    active_step_status: advancement.outcome === "advanced" ? "ready" : newStatus,
    ...advancement.outcome === "advanced" ? { review_cycle: reviewCycleForNextStep(current.runtimeState.review_cycle.id, advancement.to_step_id, proposal.idempotency_key) } : {},
    claim_evidence_required: claimEvidenceRequired,
    claim_evidence: copyClaimEvidence(transitionClaimEvidence),
    ...newStatus === "completed" && unresolvedChallenges.length > 0 ? { evidence_challenges: (current.runtimeState.evidence_challenges ?? []).map((item) => unresolvedChallenges.some((challenge) => challenge.challenge_id === item.challenge_id) ? { ...item, status: "resolved" } : item) } : {},
    pending_review_result: null,
    ...coverage ? { review_coverage: coverage } : {},
    ...stepAttempts ? { step_attempts: stepAttempts } : {},
    ...retainExecutionPreflight && currentExecutionPreflight ? { execution_preflight: currentExecutionPreflight } : {},
    execution_log: executionLog,
    applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
  };
  return {
    next,
    findingStatus: deltaRepairFingerprints.length > 0 ? current.runtimeState.findings.find((item) => item.fingerprint === deltaRepairFingerprints[0])?.status : undefined,
    advancement
  };
}
function applyFindingQueueDelta(current, proposal, now) {
  if (proposal.semantic_delta.kind !== "finding-queue")
    fail3("RUNTIME_SCHEMA_INVALID", "Expected finding-queue delta.");
  ensureAuthorityKinds(proposal, ["active-task-owner", "scope-admission", "finding-admission", "evidence-admission"]);
  if (current.runtimeState.workflow_status === "draft" && current.runtimeState.lifecycle_state === "active") {
    fail3("DRAFT_NOT_EXECUTABLE", "finding queue changes are blocked for draft + active until prepare-task:confirm commits confirm-draft.");
  }
  if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active") {
    fail3("TASK_STATE_NOT_ACTIVE", "finding queue changes require the current task to be active + active.");
  }
  if (current.runtimeState.resume_requires_review) {
    fail3("RESUME_REVIEW_REQUIRED", "finding queue changes are blocked until prepare-task clears the resume review gate.");
  }
  const delta = proposal.semantic_delta;
  let findings = current.runtimeState.findings.map((item) => ({ ...item, evidence_refs: [...item.evidence_refs] }));
  let findingStatus;
  let reviewCycle = {
    ...current.runtimeState.review_cycle,
    counted_repair_wave_ids: [...current.runtimeState.review_cycle.counted_repair_wave_ids]
  };
  let pendingReview = current.runtimeState.pending_review_result;
  if (delta.action === "admit") {
    const candidate = delta.finding;
    let reAdmitIndex;
    if (candidate.owner_task_id !== current.runtimeState.task_id)
      fail3("FINDING_OWNER_CONFLICT", "finding owner_task_id must match the active task.");
    if (findings.some((item) => item.fingerprint === candidate.fingerprint)) {
      const reAdmitCandidate = findings.find((item) => item.fingerprint === candidate.fingerprint);
      const equivalent = reAdmitCandidate.owner_task_id === candidate.owner_task_id && reAdmitCandidate.file === candidate.file && reAdmitCandidate.failure_condition === candidate.failure_condition && reAdmitCandidate.violated_invariant === candidate.violated_invariant;
      if (equivalent && ["admitted", "in-progress"].includes(reAdmitCandidate.status))
        return { next: current.runtimeState, findingStatus: reAdmitCandidate.status };
      if (!equivalent)
        fail3("FINDING_DUPLICATE_CONFLICT", `finding fingerprint ${candidate.fingerprint} already exists with different semantics.`);
      reAdmitIndex = findings.findIndex((item) => item.fingerprint === candidate.fingerprint);
    }
    if (reviewCycle.id !== candidate.review_cycle_id) {
      const hasOpenFindings = findings.some((item) => item.status === "admitted" || item.status === "in-progress");
      if (hasOpenFindings) {
        fail3("REVIEW_CYCLE_NOT_CONVERGED", "A new review cycle may start only after all admitted and in-progress findings in the current cycle are terminal.");
      }
      if (pendingReview) {
        const priorCompletion = currentDefinitionExecutionLog(current).findLast((item) => !("action" in item) && item.advancement === "advanced" && item.next_step_id === current.runtimeState.active_step_id && item.review_receipt?.cycle_id === reviewCycle.id);
        const inheritedCycle = pendingReview.verdict === "findings" && pendingReview.step_id === current.runtimeState.active_step_id && pendingReview.cycle_id === reviewCycle.id && pendingReview.cycle_phase === "discovery" && priorCompletion && !("action" in priorCompletion) && reviewCycleForNextStep(reviewCycle.id, pendingReview.step_id, priorCompletion.idempotency_key).id === candidate.review_cycle_id;
        if (!inheritedCycle)
          fail3("REVIEW_CYCLE_PENDING", "A pending review cannot be moved into an unrelated review cycle.");
        pendingReview = { ...pendingReview, cycle_id: candidate.review_cycle_id };
      }
      reviewCycle = {
        id: candidate.review_cycle_id,
        cycle_phase: "discovery",
        repair_round: 0,
        counted_repair_wave_ids: [],
        active_repair_wave_id: null,
        verification_new_finding_wave_used: false,
        verification_new_finding_wave_id: null
      };
    }
    if (delta.cycle_phase === "discovery") {
      if (reviewCycle.cycle_phase !== "discovery" || reviewCycle.repair_round > 0) {
        fail3("REVIEW_CYCLE_PHASE_CONFLICT", "Discovery admission is closed after repair or verification; use the bounded verification admission wave.");
      }
    } else {
      if (reviewCycle.repair_round === 0) {
        fail3("REVIEW_CYCLE_PHASE_CONFLICT", "Verification admission requires at least one completed repair round.");
      }
      if (reviewCycle.verification_new_finding_wave_used) {
        if (reviewCycle.verification_new_finding_wave_id !== delta.finding_admission_wave_id) {
          fail3("NEW_FINDING_WAVE_BUDGET_EXHAUSTED", "This review cycle has already used its one verification new-finding admission wave.");
        }
      } else {
        reviewCycle = {
          ...reviewCycle,
          cycle_phase: "verification",
          active_repair_wave_id: null,
          verification_new_finding_wave_used: true,
          verification_new_finding_wave_id: delta.finding_admission_wave_id
        };
      }
    }
    const finding = {
      ...candidate,
      status: "admitted",
      repair_attempts: 0,
      last_repair_wave_id: null,
      admitted_at: now,
      updated_at: now,
      evidence_refs: [...candidate.evidence_refs]
    };
    if (reAdmitIndex === undefined) {
      findings.push(finding);
    } else {
      const historical = findings[reAdmitIndex];
      findings[reAdmitIndex] = {
        ...historical,
        review_cycle_id: finding.review_cycle_id,
        status: "admitted",
        repair_attempts: current.runtimeState.execution_log.some((item) => ("action" in item) && (item.action === "commit-replan" || item.action === "commit-scope-amendment")) ? historical.repair_attempts : 0,
        last_repair_wave_id: null,
        admitted_at: now,
        updated_at: now,
        evidence_refs: [...new Set([...historical.evidence_refs, ...candidate.evidence_refs])]
      };
    }
    findingStatus = finding.status;
  } else {
    const index = findings.findIndex((item) => item.fingerprint === delta.fingerprint);
    if (index < 0)
      fail3("FINDING_NOT_FOUND", `finding ${delta.fingerprint} is not present in the current queue.`);
    const finding = findings[index];
    if (delta.action === "record-repair-attempt") {
      const recoveryBudget = recoveryProblemBudget(current);
      if (recoveryBudget && !recoveryBudget.repairWaves.has(delta.repair_wave_id) && recoveryBudget.repairWaves.size >= MAX_REPAIR_ROUNDS)
        fail3("REPAIR_BUDGET_EXHAUSTED", "The same recovery problem has exhausted its retained repair waves across plans.");
      if (proposal.mode !== "repair")
        fail3("RUNTIME_MODE_INVALID", "record-repair-attempt requires execute-step:repair.");
      if (!["admitted", "in-progress"].includes(finding.status))
        fail3("FINDING_STATE_INVALID", `finding ${finding.fingerprint} is not repairable from ${finding.status}.`);
      if (finding.repair_attempts >= finding.max_repair_attempts)
        fail3("REPAIR_BUDGET_EXHAUSTED", `finding ${finding.fingerprint} has exhausted its repair budget.`);
      if (delta.review_cycle_id !== reviewCycle.id) {
        fail3("REVIEW_CYCLE_CONFLICT", "record-repair-attempt must target the current review cycle; only finding admission may start a new cycle.");
      }
      if (finding.review_cycle_id !== reviewCycle.id) {
        fail3("REVIEW_CYCLE_CONFLICT", `finding ${finding.fingerprint} does not belong to the current review cycle.`);
      }
      if (reviewCycle.counted_repair_wave_ids.includes(delta.repair_wave_id) && reviewCycle.active_repair_wave_id !== delta.repair_wave_id) {
        fail3("REPAIR_WAVE_CLOSED", `repair wave ${delta.repair_wave_id} has already ended and cannot be reused.`);
      }
      if (finding.last_repair_wave_id === delta.repair_wave_id) {
        fail3("REPAIR_WAVE_FINDING_DUPLICATE", `finding ${finding.fingerprint} already has an attempt in repair wave ${delta.repair_wave_id}.`);
      }
      if (reviewCycle.active_repair_wave_id !== delta.repair_wave_id) {
        if (reviewCycle.repair_round >= MAX_REPAIR_ROUNDS)
          fail3("REPAIR_BUDGET_EXHAUSTED", "review-cycle repair round budget is exhausted.");
        reviewCycle = {
          ...reviewCycle,
          repair_round: reviewCycle.repair_round + 1,
          counted_repair_wave_ids: [...reviewCycle.counted_repair_wave_ids, delta.repair_wave_id],
          active_repair_wave_id: delta.repair_wave_id
        };
      }
      if (reviewCycle.verification_new_finding_wave_id !== null) {
        reviewCycle = { ...reviewCycle, verification_new_finding_wave_id: null };
      }
      finding.repair_attempts += 1;
      finding.last_repair_wave_id = delta.repair_wave_id;
      finding.status = "in-progress";
      finding.updated_at = now;
      finding.evidence_refs = [...new Set([...finding.evidence_refs, ...delta.evidence_refs])];
    } else if (delta.action === "resolve") {
      if (proposal.mode !== "repair")
        fail3("RUNTIME_MODE_INVALID", "resolve requires execute-step:repair.");
      if (!["admitted", "in-progress"].includes(finding.status))
        fail3("FINDING_STATE_INVALID", `finding ${finding.fingerprint} is not resolvable from ${finding.status}.`);
      finding.status = "resolved";
      finding.updated_at = now;
      finding.evidence_refs = [...new Set([...finding.evidence_refs, ...delta.evidence_refs])];
    } else {
      if (proposal.mode !== "repair")
        fail3("RUNTIME_MODE_INVALID", `${delta.action} requires execute-step:repair.`);
      if (!["admitted", "in-progress"].includes(finding.status))
        fail3("FINDING_STATE_INVALID", `finding ${finding.fingerprint} cannot be ${delta.action} from ${finding.status}.`);
      finding.status = delta.action === "defer" ? "deferred" : "rejected";
      finding.updated_at = now;
      finding.evidence_refs = [...new Set([...finding.evidence_refs, ...delta.evidence_refs])];
    }
    findingStatus = finding.status;
  }
  const next = {
    ...current.runtimeState,
    finding_queue_revision: current.runtimeState.finding_queue_revision + 1,
    review_cycle: reviewCycle,
    pending_review_result: pendingReview,
    findings,
    applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
  };
  return { next, findingStatus };
}
var SUSPENDED_PACKAGE_BEGIN = "<!-- BEGIN vNext CURRENT_TASK snapshot -->";
var SUSPENDED_PACKAGE_END = "<!-- END vNext CURRENT_TASK snapshot -->";
function packageText(value, location) {
  const result = expectText(value, location);
  if (/[\r\n]/.test(result))
    fail3("RUNTIME_SCHEMA_INVALID", `${location} must be a single-line value in a suspended package.`);
  return result;
}
function extractSuspendedPackageFields(header, location) {
  const fields = {};
  for (const line of header.split(/\r?\n/)) {
    const match = /^\s*-\s*([a-z][a-z0-9_]*)\s*:\s*(.*?)\s*$/.exec(line);
    if (!match)
      continue;
    if (match[1] in fields)
      fail3("RUNTIME_SCHEMA_INVALID", `${location} contains duplicate field ${match[1]}.`);
    fields[match[1]] = match[2].trim();
  }
  return fields;
}
function requiredPackageField(fields, field, location) {
  const value = fields[field];
  if (value === undefined || value.trim().length === 0)
    fail3("RUNTIME_SCHEMA_INVALID", `${location} is missing required field ${field}.`);
  return value.trim();
}
function packagePathForTask(root2, taskId, taskSlug, artifactKind) {
  let relativePath;
  try {
    relativePath = getTaskArtifactPath(taskId, taskSlug, artifactKind);
  } catch (error) {
    fail3("RUNTIME_PATH_INVALID", error instanceof Error ? error.message : String(error));
  }
  const filePath = path12.resolve(path12.resolve(root2), ...relativePath.split("/"));
  const resolvedRoot = path12.resolve(root2);
  const relativeCheck = path12.relative(resolvedRoot, filePath).replace(/\\/g, "/");
  if (relativeCheck !== relativePath || relativeCheck.startsWith("../") || path12.isAbsolute(relativeCheck)) {
    fail3("RUNTIME_PATH_INVALID", `suspended package path escapes the target root: ${relativePath}`);
  }
  return { filePath, relativePath };
}
function replacePackageField(content, field, value) {
  const pattern = new RegExp(`^-\\s*${field.replace(/[.*+?^${}()|[\\]\\\\]/g, "\\\\$&")}\\s*:\\s*[^\\r\\n]*$`, "gm");
  const matches = content.match(pattern) ?? [];
  if (matches.length !== 1)
    fail3("RUNTIME_SCHEMA_INVALID", `suspended package must contain exactly one ${field} field.`);
  return content.replace(pattern, `- ${field}: ${value}`);
}
function parseSuspendedPackage(root2, current, relativePath, expectedKind) {
  const normalizedPath = normalizeRepoPath2(relativePath, "suspended package path");
  const pathMatch = /^TASKS\/(paused|interrupted)\/TASK-([0-9]{3,})-([a-z0-9]+(?:-[a-z0-9]+)*)\.md$/.exec(normalizedPath);
  if (!pathMatch)
    fail3("RUNTIME_PATH_INVALID", `suspended package path is outside the paused/interrupted contract: ${normalizedPath}`);
  const pathKind = pathMatch[1];
  const pathTaskId = pathMatch[2];
  const pathTaskSlug = pathMatch[3];
  if (expectedKind && pathKind !== expectedKind)
    fail3("RUNTIME_PATH_INVALID", `suspended package path kind ${pathKind} does not match ${expectedKind}.`);
  if (pathTaskId !== current.runtimeState.task_id || pathTaskSlug !== current.runtimeState.task_slug) {
    fail3("RUNTIME_IDENTITY_CONFLICT", "suspended package path identity does not match the canonical current task.");
  }
  const canonicalExpectedPath = packagePathForTask(root2, pathTaskId, pathTaskSlug, pathKind);
  if (normalizedPath !== canonicalExpectedPath.relativePath)
    fail3("RUNTIME_PATH_INVALID", "suspended package path is not the canonical identity-derived path.");
  const filePath = canonicalExpectedPath.filePath;
  if (!fs11.existsSync(filePath))
    fail3("SUSPENDED_PACKAGE_MISSING", `suspended package is missing: ${normalizedPath}`);
  const raw = fs11.readFileSync(filePath, "utf8");
  if (raw.split(SUSPENDED_PACKAGE_BEGIN).length !== 2 || raw.split(SUSPENDED_PACKAGE_END).length !== 2) {
    fail3("SUSPENDED_PACKAGE_INVALID", `${normalizedPath} must contain exactly one complete CURRENT_TASK snapshot.`);
  }
  const beginIndex = raw.indexOf(SUSPENDED_PACKAGE_BEGIN);
  const endIndex = raw.indexOf(SUSPENDED_PACKAGE_END);
  if (beginIndex < 0 || endIndex <= beginIndex)
    fail3("SUSPENDED_PACKAGE_INVALID", `${normalizedPath} has an invalid snapshot marker order.`);
  const header = raw.slice(0, beginIndex);
  const fields = extractSuspendedPackageFields(header, normalizedPath);
  const taskId = requiredPackageField(fields, "task_id", normalizedPath);
  const taskTitle = packageText(requiredPackageField(fields, "task_title", normalizedPath), `${normalizedPath}.task_title`);
  const taskSlug = requiredPackageField(fields, "task_slug", normalizedPath);
  try {
    validateTaskId(taskId);
    validateTaskSlug(taskSlug);
  } catch (error) {
    fail3("RUNTIME_SCHEMA_INVALID", error instanceof Error ? error.message : String(error));
  }
  if (taskId !== pathTaskId || taskSlug !== pathTaskSlug)
    fail3("RUNTIME_IDENTITY_CONFLICT", "suspended package fields do not match its canonical path.");
  const artifactKind = expectEnum(requiredPackageField(fields, "artifact_kind", normalizedPath), ["paused", "interrupted"], `${normalizedPath}.artifact_kind`);
  if (artifactKind !== pathKind)
    fail3("SUSPENDED_PACKAGE_INVALID", "suspended package artifact_kind does not match its path.");
  const lifecycleState = expectEnum(requiredPackageField(fields, "lifecycle_state", normalizedPath), ["paused_pending_closure", "paused_blocked", "interrupted"], `${normalizedPath}.lifecycle_state`);
  if (artifactKind === "paused" && !["paused_pending_closure", "paused_blocked"].includes(lifecycleState) || artifactKind === "interrupted" && lifecycleState !== "interrupted") {
    fail3("SUSPENDED_PACKAGE_INVALID", "suspended package lifecycle_state does not match artifact_kind.");
  }
  const resumeRequiresReview = parseBooleanField(requiredPackageField(fields, "resume_requires_review", normalizedPath), `${normalizedPath}.resume_requires_review`);
  const rawResumeReviewReasons = requiredPackageField(fields, "resume_review_reasons", normalizedPath).split(",").map((reason) => reason.trim()).filter(Boolean);
  const resumeReviewReasons = normalizeResumeReviewReasons(rawResumeReviewReasons);
  if (rawResumeReviewReasons.join("|") !== resumeReviewReasons.join("|")) {
    fail3("SUSPENDED_PACKAGE_INVALID", `${normalizedPath}.resume_review_reasons must use the canonical closed-set order without duplicates.`);
  }
  try {
    validateCurrentTaskResumeGate(lifecycleState, resumeRequiresReview, resumeReviewReasons);
  } catch (error) {
    fail3("SUSPENDED_PACKAGE_INVALID", error instanceof Error ? error.message : String(error));
  }
  const rehydrationStatus = expectEnum(requiredPackageField(fields, "rehydration_status", normalizedPath), ["write_incomplete", "ready_for_resume", "rehydrated"], `${normalizedPath}.rehydration_status`);
  const ownershipState = expectEnum(requiredPackageField(fields, "ownership_state", normalizedPath), ["recovery_only", "rehydrated"], `${normalizedPath}.ownership_state`);
  if (rehydrationStatus === "write_incomplete" && ownershipState !== "recovery_only")
    fail3("SUSPENDED_PACKAGE_INVALID", "write_incomplete package must remain recovery_only.");
  if (rehydrationStatus === "ready_for_resume" && (ownershipState !== "recovery_only" || !resumeRequiresReview))
    fail3("SUSPENDED_PACKAGE_INVALID", "ready_for_resume package must be recovery_only and review-gated.");
  if (rehydrationStatus === "rehydrated" && ownershipState !== "rehydrated")
    fail3("SUSPENDED_PACKAGE_INVALID", "rehydrated package must use ownership_state=rehydrated.");
  const documentId = requiredPackageField(fields, "document_id", normalizedPath);
  if (!DOCUMENT_ID_PATTERN.test(documentId))
    fail3("RUNTIME_SCHEMA_INVALID", `${normalizedPath}.document_id is invalid.`);
  const snapshotSha256 = requiredPackageField(fields, "snapshot_sha256", normalizedPath);
  if (!/^[a-f0-9]{64}$/.test(snapshotSha256))
    fail3("RUNTIME_SCHEMA_INVALID", `${normalizedPath}.snapshot_sha256 must be SHA-256.`);
  let snapshotStart = beginIndex + SUSPENDED_PACKAGE_BEGIN.length;
  if (raw.startsWith(`\r
`, snapshotStart))
    snapshotStart += 2;
  else if (raw.startsWith(`
`, snapshotStart))
    snapshotStart += 1;
  const snapshotRegion = raw.slice(snapshotStart, endIndex);
  const snapshotCandidates = [snapshotRegion, snapshotRegion.endsWith(`
`) ? snapshotRegion.slice(0, -1) : snapshotRegion];
  const snapshotRaw = snapshotCandidates.find((candidate) => sha2565(candidate) === snapshotSha256);
  if (snapshotRaw === undefined)
    fail3("SUSPENDED_PACKAGE_INVALID", `${normalizedPath} snapshot_sha256 does not match the embedded CURRENT_TASK snapshot.`);
  const snapshot = parseCanonicalCurrentTaskContent(snapshotRaw, current.filePath, current.relativePath);
  if (snapshot.frontmatter.document_id !== documentId || snapshot.frontmatter.document_id !== current.frontmatter.document_id) {
    fail3("RUNTIME_SOURCE_CONFLICT", "suspended package document_id conflicts with CURRENT_TASK or its snapshot.");
  }
  if (snapshot.runtimeState.task_id !== taskId || snapshot.runtimeState.task_slug !== taskSlug || snapshot.runtimeState.workflow_status !== "active" || snapshot.runtimeState.lifecycle_state !== "active") {
    fail3("SUSPENDED_PACKAGE_INVALID", "suspended package snapshot must preserve the same active task before suspension.");
  }
  const snapshotIdentity = extractTaskIdentityFromCurrentTask(snapshot.body);
  if (snapshotIdentity.title !== taskTitle)
    fail3("RUNTIME_SOURCE_CONFLICT", "suspended package task_title conflicts with its snapshot.");
  const taskStartBase = packageText(requiredPackageField(fields, "task_start_base", normalizedPath), `${normalizedPath}.task_start_base`);
  const lastReviewedCheckpoint = packageText(requiredPackageField(fields, "last_reviewed_checkpoint", normalizedPath), `${normalizedPath}.last_reviewed_checkpoint`);
  const currentDiffReviewTarget = packageText(requiredPackageField(fields, "current_diff_review_target", normalizedPath), `${normalizedPath}.current_diff_review_target`);
  const rollbackConditions = packageText(requiredPackageField(fields, "rollback_conditions", normalizedPath), `${normalizedPath}.rollback_conditions`);
  const suspensionReason = packageText(requiredPackageField(fields, "suspension_reason", normalizedPath), `${normalizedPath}.suspension_reason`);
  if (lifecycleState === "paused_blocked") {
    packageText(requiredPackageField(fields, "blocker_status", normalizedPath), `${normalizedPath}.blocker_status`);
    packageText(requiredPackageField(fields, "blocking_evidence", normalizedPath), `${normalizedPath}.blocking_evidence`);
    packageText(requiredPackageField(fields, "remaining_acceptance", normalizedPath), `${normalizedPath}.remaining_acceptance`);
  }
  if (artifactKind === "interrupted") {
    packageText(requiredPackageField(fields, "checkpoint_evidence", normalizedPath), `${normalizedPath}.checkpoint_evidence`);
    packageText(requiredPackageField(fields, "dirty_attribution", normalizedPath), `${normalizedPath}.dirty_attribution`);
    packageText(requiredPackageField(fields, "environment_state", normalizedPath), `${normalizedPath}.environment_state`);
    packageText(requiredPackageField(fields, "recovery_strategy", normalizedPath), `${normalizedPath}.recovery_strategy`);
  }
  return {
    filePath,
    relativePath: normalizedPath,
    raw,
    revision: sha2565(raw),
    taskId,
    taskTitle,
    taskSlug,
    artifactKind,
    lifecycleState,
    suspensionReason,
    taskStartBase,
    lastReviewedCheckpoint,
    currentDiffReviewTarget,
    rollbackConditions,
    resumeRequiresReview,
    resumeReviewReasons,
    rehydrationStatus,
    ownershipState,
    documentId,
    snapshotSha256,
    snapshot
  };
}
function renderSuspendedPackage(current, delta, artifactKind) {
  const identity = extractTaskIdentityFromCurrentTask(current.body);
  const taskTitle = packageText(identity.title, "CURRENT_TASK task title");
  const fields = [
    "# vNext suspended task package",
    "",
    `- task_id: ${current.runtimeState.task_id}`,
    `- task_title: ${taskTitle}`,
    `- task_slug: ${current.runtimeState.task_slug}`,
    `- artifact_kind: ${artifactKind}`,
    `- lifecycle_state: ${delta.lifecycle_state}`,
    `- suspension_reason: ${packageText(delta.suspension_reason, "semantic_delta.suspension_reason")}`,
    `- task_start_base: ${packageText(delta.task_start_base, "semantic_delta.task_start_base")}`,
    `- last_reviewed_checkpoint: ${packageText(delta.last_reviewed_checkpoint, "semantic_delta.last_reviewed_checkpoint")}`,
    `- current_diff_review_target: ${packageText(delta.current_diff_review_target, "semantic_delta.current_diff_review_target")}`,
    `- rollback_conditions: ${packageText(delta.rollback_conditions, "semantic_delta.rollback_conditions")}`,
    "- resume_requires_review: true",
    `- resume_review_reasons: ${delta.resume_review_reasons.join(", ")}`,
    "- rehydration_status: ready_for_resume",
    "- ownership_state: recovery_only",
    `- document_id: ${String(current.frontmatter.document_id)}`,
    `- snapshot_sha256: ${sha2565(current.raw)}`
  ];
  if (delta.action === "pause" && delta.lifecycle_state === "paused_blocked") {
    fields.push(`- blocker_status: ${packageText(delta.blocker_status, "semantic_delta.blocker_status")}`);
    fields.push(`- blocking_evidence: ${packageText(delta.blocking_evidence, "semantic_delta.blocking_evidence")}`);
    fields.push(`- remaining_acceptance: ${packageText(delta.remaining_acceptance, "semantic_delta.remaining_acceptance")}`);
    if (delta.failed_checks && delta.failed_checks.length > 0)
      fields.push(`- failed_checks: ${delta.failed_checks.join(", ")}`);
  }
  if (delta.action === "interrupt") {
    fields.push(`- checkpoint_evidence: ${packageText(delta.checkpoint_evidence, "semantic_delta.checkpoint_evidence")}`);
    fields.push(`- dirty_attribution: ${packageText(delta.dirty_attribution, "semantic_delta.dirty_attribution")}`);
    fields.push(`- environment_state: ${packageText(delta.environment_state, "semantic_delta.environment_state")}`);
    fields.push(`- recovery_strategy: ${packageText(delta.recovery_strategy, "semantic_delta.recovery_strategy")}`);
  }
  const snapshot = current.raw;
  return `${fields.join(`
`)}

${SUSPENDED_PACKAGE_BEGIN}
${snapshot}${snapshot.endsWith(`
`) ? "" : `
`}${SUSPENDED_PACKAGE_END}
`;
}
function renderRehydratedPackage(packageArtifact) {
  let content = packageArtifact.raw;
  content = replacePackageField(content, "rehydration_status", "rehydrated");
  content = replacePackageField(content, "ownership_state", "rehydrated");
  return content;
}
function assertSuspendedSourceMatchesSnapshot(current, snapshot) {
  const currentRuntimeState = {
    ...current.runtimeState,
    workflow_status: snapshot.runtimeState.workflow_status,
    lifecycle_state: snapshot.runtimeState.lifecycle_state,
    resume_requires_review: snapshot.runtimeState.resume_requires_review,
    resume_review_reasons: [...snapshot.runtimeState.resume_review_reasons],
    applied_proposals: [...snapshot.runtimeState.applied_proposals]
  };
  if (digest3(currentRuntimeState) !== digest3(snapshot.runtimeState)) {
    fail3("LIFECYCLE_SOURCE_CONFLICT", "suspended CURRENT_TASK runtime_state differs from the recovery snapshot.");
  }
  const currentFrontmatter = { ...current.frontmatter };
  const snapshotFrontmatter = { ...snapshot.frontmatter };
  delete currentFrontmatter.runtime_state;
  delete snapshotFrontmatter.runtime_state;
  if (digest3(currentFrontmatter) !== digest3(snapshotFrontmatter)) {
    fail3("LIFECYCLE_SOURCE_CONFLICT", "suspended CURRENT_TASK frontmatter differs from the recovery snapshot.");
  }
  const normalizedCurrentBody = renderCurrentTaskLifecycleFields(current.body, snapshot.runtimeState);
  const normalizedSnapshotBody = renderCurrentTaskLifecycleFields(snapshot.body, snapshot.runtimeState);
  if (normalizedCurrentBody !== normalizedSnapshotBody) {
    fail3("LIFECYCLE_SOURCE_CONFLICT", "suspended CURRENT_TASK body differs from the recovery snapshot.");
  }
}
function assertSuspendedGateMatchesPackage(current, packageArtifact) {
  if (current.runtimeState.resume_requires_review !== packageArtifact.resumeRequiresReview || current.runtimeState.resume_review_reasons.join("|") !== packageArtifact.resumeReviewReasons.join("|")) {
    fail3("RESUME_GATE_DRIFT", "CURRENT_TASK resume gate differs from the suspended package gate.");
  }
}
function lifecycleArtifactKind(delta) {
  if (delta.action === "pause")
    return "paused";
  if (delta.action === "interrupt")
    return "interrupted";
  if (delta.action === "resume-paused" || delta.action === "resume-interrupted")
    return delta.artifact_kind;
  return null;
}
function assertLifecycleReplayArtifacts(root2, current, proposal) {
  const delta = proposal.semantic_delta;
  const artifactKind = lifecycleArtifactKind(delta);
  if (artifactKind === null) {
    if (delta.action === "supersede") {
      if (current.runtimeState.workflow_status !== "superseded" || current.runtimeState.lifecycle_state !== "active") {
        fail3("LIFECYCLE_REPLAY_INCOMPLETE", "supersede replay no longer has the original superseded + active CURRENT_TASK tuple.");
      }
      const audit = current.runtimeState.execution_log.find((item) => ("action" in item) && item.action === "supersede" && item.idempotency_key === proposal.idempotency_key);
      if (!audit || audit.invalidation_kind !== delta.invalidation_kind || audit.invalidation_reason !== delta.invalidation_reason || audit.source_revision !== proposal.source_tuple.revision || audit.evidence_refs.join("|") !== delta.evidence_refs.join("|") || digest3(audit.partial_diff_disposition) !== digest3(delta.partial_diff_disposition)) {
        fail3("LIFECYCLE_REPLAY_INCOMPLETE", "supersede replay is missing its durable invalidation audit record.");
      }
      assertExecutionAudit(root2, current, audit);
      assertTaskHistoryForRevision(current.filePath, current.sourceTuple.document_id, current.runtimeState.task_id, proposal.source_tuple.revision);
      assertNoLaterReplanAudit(current, audit, "LIFECYCLE_REPLAY_INCOMPLETE");
    }
    return;
  }
  const expected = packagePathForTask(root2, current.runtimeState.task_id, current.runtimeState.task_slug, artifactKind);
  const packageArtifact = parseSuspendedPackage(root2, current, expected.relativePath, artifactKind);
  if (delta.action === "pause" || delta.action === "interrupt") {
    if (packageArtifact.rehydrationStatus !== "ready_for_resume" || packageArtifact.ownershipState !== "recovery_only") {
      fail3("LIFECYCLE_REPLAY_INCOMPLETE", "lifecycle replay requires the original suspended package to remain ready_for_resume + recovery_only.");
    }
    if (current.runtimeState.workflow_status !== "suspended" || current.runtimeState.lifecycle_state !== delta.lifecycle_state) {
      fail3("LIFECYCLE_REPLAY_INCOMPLETE", "lifecycle replay no longer has the original suspended CURRENT_TASK tuple.");
    }
    if (packageArtifact.lifecycleState !== delta.lifecycle_state) {
      fail3("LIFECYCLE_REPLAY_INCOMPLETE", "lifecycle replay package marker does not match the original transition.");
    }
    assertSuspendedGateMatchesPackage(current, packageArtifact);
    return;
  }
  if (packageArtifact.rehydrationStatus !== "rehydrated" || packageArtifact.ownershipState !== "rehydrated") {
    fail3("LIFECYCLE_REPLAY_INCOMPLETE", "resume replay requires the suspended package to remain rehydrated + rehydrated.");
  }
  if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active") {
    fail3("LIFECYCLE_REPLAY_INCOMPLETE", "resume replay no longer has an active + active CURRENT_TASK tuple.");
  }
}
function assertSiblingRecoveryIsReconciled(root2, current, artifactKind) {
  const siblingKind = artifactKind === "paused" ? "interrupted" : "paused";
  const sibling = packagePathForTask(root2, current.runtimeState.task_id, current.runtimeState.task_slug, siblingKind);
  if (!fs11.existsSync(sibling.filePath))
    return;
  const siblingArtifact = parseSuspendedPackage(root2, current, sibling.relativePath, siblingKind);
  if (siblingArtifact.rehydrationStatus === "rehydrated" && siblingArtifact.ownershipState === "rehydrated")
    return;
  fail3("SUSPENDED_PACKAGE_AMBIGUOUS", "another ready or incomplete suspended package for the same task is present; reconcile the sibling before continuing.");
}
function prepareExistingPackageForReplacement(root2, current, packageRelativePath, artifactKind) {
  const expected = packagePathForTask(root2, current.runtimeState.task_id, current.runtimeState.task_slug, artifactKind);
  if (!fs11.existsSync(expected.filePath))
    return;
  const existing = parseSuspendedPackage(root2, current, packageRelativePath, artifactKind);
  if (existing.rehydrationStatus === "rehydrated" && existing.ownershipState === "rehydrated")
    return existing.raw;
  if (existing.rehydrationStatus === "write_incomplete") {
    fail3("SUSPENDED_PACKAGE_RECOVERY_REQUIRED", "the existing suspended package is write_incomplete and requires explicit recovery before replacement.");
  }
  fail3("SUSPENDED_PACKAGE_CONFLICT", `suspended package is already ready_for_resume: ${packageRelativePath}`);
}
function assertRequestedLifecycleTargets(root2, current, proposal) {
  if (proposal.requested_write_targets[0] !== current.relativePath)
    fail3("RUNTIME_PATH_INVALID", "lifecycle proposal must target the exact canonical CURRENT_TASK path first.");
  const delta = proposal.semantic_delta;
  if (delta.action === "supersede") {
    if (proposal.requested_write_targets.length !== 1)
      fail3("RUNTIME_PATH_INVALID", "supersede may write only the exact canonical CURRENT_TASK path.");
    return {};
  }
  const artifactKind = delta.action === "pause" ? "paused" : delta.action === "interrupt" ? "interrupted" : delta.artifact_kind;
  const expected = packagePathForTask(root2, current.runtimeState.task_id, current.runtimeState.task_slug, artifactKind);
  if (delta.action === "resume-paused" || delta.action === "resume-interrupted") {
    if (delta.recovery_package_path !== expected.relativePath)
      fail3("RUNTIME_PATH_INVALID", "resume must use the exact identity-derived suspended package path.");
  }
  if (proposal.requested_write_targets.length !== 2 || proposal.requested_write_targets[1] !== expected.relativePath) {
    fail3("RUNTIME_PATH_INVALID", "lifecycle proposal must name exactly CURRENT_TASK.md and its identity-derived suspended package path.");
  }
  return { packageFilePath: expected.filePath, packageRelativePath: expected.relativePath };
}
function prepareLifecycleTransaction(root2, current, proposal, now) {
  const delta = proposal.semantic_delta;
  if (delta.action === "supersede") {
    if (![1, 2].includes(current.runtimeState.task_evolution_version ?? 0))
      fail3("TASK_EVOLUTION_INITIALIZATION_REQUIRED", "Initialize task preservation before superseding an older task.");
    ensureAuthorityKinds(proposal, ["active-task-owner", "evidence-admission"]);
    if (!["active", "blocked_by_replan"].includes(current.runtimeState.workflow_status) || current.runtimeState.lifecycle_state !== "active") {
      fail3("LIFECYCLE_TRANSITION_INVALID", "supersede requires active + active or blocked_by_replan + active.");
    }
    const nextWithoutAudit = {
      ...current.runtimeState,
      workflow_status: "superseded",
      lifecycle_state: "active",
      applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
    };
    const audit = makeReplanAudit(current, proposal, nextWithoutAudit, now);
    const next = {
      ...nextWithoutAudit,
      execution_log: appendExecutionLogEntry(current.runtimeState, audit)
    };
    const prepared = renderCanonicalCurrentTask(current.frontmatter, current.body, next, { audit });
    const nextContent = prepared.content;
    return { next, nextContent, prepared, audit };
  }
  const target = assertRequestedLifecycleTargets(root2, current, proposal);
  const packageFilePath = target.packageFilePath;
  const packageRelativePath = target.packageRelativePath;
  const activeTuple = current.runtimeState.workflow_status === "active" && current.runtimeState.lifecycle_state === "active";
  if (delta.action === "pause" || delta.action === "interrupt") {
    ensureAuthorityKinds(proposal, ["active-task-owner", "scope-admission", "evidence-admission"]);
    if (!activeTuple)
      fail3("LIFECYCLE_TRANSITION_INVALID", `${delta.action} requires the current task to be active + active.`);
    assertSiblingRecoveryIsReconciled(root2, current, delta.action === "pause" ? "paused" : "interrupted");
    const originalPackageContent = prepareExistingPackageForReplacement(root2, current, packageRelativePath, delta.action === "pause" ? "paused" : "interrupted");
    const next = {
      ...current.runtimeState,
      workflow_status: "suspended",
      lifecycle_state: delta.lifecycle_state,
      resume_requires_review: true,
      resume_review_reasons: [...delta.resume_review_reasons],
      applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
    };
    const prepared = renderCanonicalCurrentTask(current.frontmatter, current.body, next);
    const nextContent = prepared.content;
    const nextPackageContent = renderSuspendedPackage(current, delta, delta.action === "pause" ? "paused" : "interrupted");
    return { next, nextContent, prepared, packageFilePath, packageRelativePath, nextPackageContent, ...originalPackageContent === undefined ? {} : { originalPackageContent } };
  }
  ensureAuthorityKinds(proposal, ["resume-review", "evidence-admission"]);
  if (current.runtimeState.workflow_status !== "suspended")
    fail3("LIFECYCLE_TRANSITION_INVALID", "resume requires a suspended CURRENT_TASK source.");
  const expectedLifecycle = delta.action === "resume-paused" ? ["paused_pending_closure", "paused_blocked"] : ["interrupted"];
  if (!expectedLifecycle.includes(current.runtimeState.lifecycle_state))
    fail3("LIFECYCLE_TRANSITION_INVALID", "resume mode does not match the current suspended lifecycle state.");
  if (!fs11.existsSync(packageFilePath))
    fail3("SUSPENDED_PACKAGE_MISSING", `suspended package is missing: ${packageRelativePath}`);
  const packageArtifact = parseSuspendedPackage(root2, current, packageRelativePath, delta.artifact_kind);
  if (packageArtifact.rehydrationStatus !== "ready_for_resume" || packageArtifact.ownershipState !== "recovery_only") {
    fail3("SUSPENDED_PACKAGE_NOT_READY", "resume accepts only ready_for_resume + recovery_only packages.");
  }
  if (packageArtifact.revision !== delta.recovery_package_revision) {
    fail3("RECOVERY_PACKAGE_STALE", "the suspended package changed after the resume proposal was created.");
  }
  assertSuspendedGateMatchesPackage(current, packageArtifact);
  assertSuspendedSourceMatchesSnapshot(current, packageArtifact.snapshot);
  if (packageArtifact.lifecycleState !== current.runtimeState.lifecycle_state)
    fail3("LIFECYCLE_SOURCE_CONFLICT", "package lifecycle state conflicts with CURRENT_TASK.");
  if (packageArtifact.resumeReviewReasons.join("|") !== delta.resume_review_reasons.join("|"))
    fail3("RESUME_GATE_DRIFT", "resume review reasons drifted between proposal and suspended package.");
  assertSiblingRecoveryIsReconciled(root2, current, delta.artifact_kind);
  if (packageArtifact.documentId !== String(current.frontmatter.document_id))
    fail3("RUNTIME_SOURCE_CONFLICT", "resume package document_id conflicts with CURRENT_TASK.");
  const next = {
    ...packageArtifact.snapshot.runtimeState,
    workflow_status: "active",
    lifecycle_state: "active",
    resume_requires_review: true,
    resume_review_reasons: [...packageArtifact.resumeReviewReasons],
    applied_proposals: appendAppliedProposal(current.runtimeState, proposal, current.sourceTuple.revision)
  };
  const prepared = renderCanonicalCurrentTask(packageArtifact.snapshot.frontmatter, packageArtifact.snapshot.body, next);
  const nextContent = prepared.content;
  const nextPackageContent = renderRehydratedPackage(packageArtifact);
  return {
    next,
    nextContent,
    prepared,
    packageFilePath,
    packageRelativePath,
    nextPackageContent,
    originalPackageContent: packageArtifact.raw
  };
}
function buildResult(status, proposal, current, options, message, extras = {}) {
  return {
    status,
    operation_kind: proposal.operation_kind,
    idempotency_key: proposal.idempotency_key,
    target_path: current.relativePath,
    dry_run: options.dryRun === true,
    committed: false,
    message,
    planned_writes: [...proposal.requested_write_targets],
    governed_mutation_count: 0,
    read_back_verified: false,
    ...extras,
    evidence_assurance: "caller-reported"
  };
}
function resultState(state, findingStatus, recoveryPackagePath) {
  return {
    task_id: state.task_id,
    workflow_status: state.workflow_status,
    lifecycle_state: state.lifecycle_state,
    resume_requires_review: state.resume_requires_review,
    resume_review_reasons: [...state.resume_review_reasons],
    active_step_id: state.active_step_id,
    active_step_status: state.active_step_status,
    finding_queue_revision: state.finding_queue_revision,
    review_cycle_id: state.review_cycle.id,
    repair_round: state.review_cycle.repair_round,
    ...(state.claim_evidence ?? []).some((claim) => claim.slots.some((slot) => slot.user_decision?.kind === "waiver")) ? { recorded_evidence_waivers: (state.claim_evidence ?? []).flatMap((claim) => claim.slots.filter((slot) => slot.user_decision?.kind === "waiver").map((slot) => ({ claim_id: claim.claim_id, slot_id: slot.slot_id, decision_source: slot.user_decision.decision_source }))) } : {},
    ...findingStatus === undefined ? {} : { finding_status: findingStatus },
    ...recoveryPackagePath === undefined ? {} : { recovery_package_path: recoveryPackagePath }
  };
}
function exactPendingFileContent(root2, relativePath) {
  const normalized = normalizeRepoPath2(relativePath, "task-store pending write path");
  const resolvedRoot = path12.resolve(root2);
  const filePath = path12.resolve(resolvedRoot, ...normalized.split("/"));
  const check = path12.relative(resolvedRoot, filePath).replace(/\\/g, "/");
  if (check !== normalized || check.startsWith("../") || path12.isAbsolute(check)) {
    throw new TaskStoreError("TASK_STORE_PATH_INVALID", `pending write path escapes the project root: ${relativePath}`);
  }
  if (!fs11.existsSync(filePath))
    return null;
  const stat = fs11.lstatSync(filePath);
  if (stat.isSymbolicLink())
    throw new TaskStoreError("TASK_STORE_PATH_INVALID", `pending write path traverses a symbolic link: ${normalized}`);
  if (!stat.isFile())
    throw new TaskStoreError("TASK_STORE_PATH_INVALID", `pending write target is not a regular file: ${normalized}`);
  return fs11.readFileSync(filePath, "utf8");
}
function pendingWriteSetForOperations(root2, operations) {
  const resolvedRoot = path12.resolve(root2);
  const seen = new Set;
  return operations.map((operation) => {
    const relativePath = path12.relative(resolvedRoot, path12.resolve(operation.path)).replace(/\\/g, "/");
    const normalized = normalizeRepoPath2(relativePath, "task-store pending write path");
    if (seen.has(normalized))
      throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", `pending write set contains duplicate path ${normalized}.`);
    seen.add(normalized);
    return { path: normalized, before_content: exactPendingFileContent(resolvedRoot, normalized), after_content: operation.content };
  });
}
function stageTaskEvolutionStoreCommit(root2, current, nextContent, nextState, proposal, writeOperations, prepared) {
  if (prepared)
    persistTaskProjection(prepared, current.filePath, current.relativePath);
  const after = parseCanonicalCurrentTaskContent(nextContent, current.filePath, current.relativePath);
  after.runtimeState = nextState;
  if (after.frontmatter.task_store !== undefined) {
    Object.defineProperty(after.runtimeState, "__vnext_compact_history", { value: true, enumerable: false, configurable: true });
  }
  const store = TaskStore.forCurrent(root2, current);
  store.stageCommit({
    before: current,
    after,
    after_source_revision: after.sourceTuple.revision,
    proposal,
    result: {
      status: "success",
      committed: true,
      operation_kind: "task-state-transaction",
      idempotency_key: isRecord4(proposal) && typeof proposal.idempotency_key === "string" ? proposal.idempotency_key : undefined
    },
    write_targets: writeOperations.map((operation) => path12.relative(path12.resolve(root2), path12.resolve(operation.path)).replace(/\\/g, "/")),
    write_set: pendingWriteSetForOperations(root2, writeOperations)
  });
  return after;
}
function completeTaskEvolutionStoreCommit(root2, current, after, proposal, result) {
  const store = TaskStore.forCurrent(root2, current);
  store.markCurrentPublished(after.sourceTuple.revision);
  const manifest = store.recordCommit({
    before: current,
    after,
    proposal,
    result
  });
  if (!manifest || manifest.head.source_revision !== after.sourceTuple.revision) {
    throw new TaskStoreError("TASK_STORE_SOURCE_CONFLICT", "task-evolution store publication did not advance to the exact after-image.");
  }
  const readBack = readCanonicalCurrentTask(root2);
  if (readBack.raw !== after.raw || readBack.sourceTuple.revision !== after.sourceTuple.revision) {
    throw new TaskStoreError("TASK_STORE_SOURCE_CONFLICT", "task-evolution store publication read-back differs from the exact after-image.");
  }
  return manifest;
}
function reconcilePendingWriteSet(root2, canonicalRevision, pending) {
  if (pending.write_set === undefined)
    return;
  if (!Array.isArray(pending.write_set))
    throw new TaskStoreError("TASK_STORE_MANIFEST_INVALID", "pending task-store write_set is not an array.");
  const writes = pending.write_set;
  const sourceRevision = typeof pending.source_revision === "string" ? pending.source_revision : null;
  const resultingRevision = typeof pending.resulting_source_revision === "string" ? pending.resulting_source_revision : null;
  if (!sourceRevision || !resultingRevision || canonicalRevision !== sourceRevision && canonicalRevision !== resultingRevision) {
    throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", "pending task-store write set is not bound to the current canonical revision.");
  }
  const restoreBefore = canonicalRevision === sourceRevision;
  for (const write of writes) {
    const actual = exactPendingFileContent(root2, write.path);
    const expected = restoreBefore ? write.before_content : write.after_content;
    const alternate = restoreBefore ? write.after_content : write.before_content;
    if (actual === expected)
      continue;
    if (actual !== alternate) {
      throw new TaskStoreError("TASK_STORE_SOURCE_CONFLICT", `pending write target ${write.path} contains neither its exact before nor after bytes.`);
    }
    const target = path12.resolve(root2, ...normalizeRepoPath2(write.path, "task-store pending write path").split("/"));
    if (restoreBefore && expected === null) {
      fs11.rmSync(target, { force: true });
    } else if (!restoreBefore && expected !== null) {
      executeWrites([{ path: target, content: expected }], false, "vNext Runtime task-store pending write recovery");
    } else {
      throw new TaskStoreError("TASK_STORE_EVENT_CONFLICT", `pending write target ${write.path} has an unsupported recovery transition.`);
    }
  }
}
function clearPendingTaskStoreAfterRollback(root2, current) {
  try {
    TaskStore.forCurrent(root2, current).clearPendingForNoCommit();
  } catch {}
}
function fileRevisionForPath(filePath) {
  if (!fs11.existsSync(filePath))
    fail3("RUNTIME_SOURCE_MISSING", `Required file is missing: ${filePath}`);
  return sha2565(fs11.readFileSync(filePath, "utf8"));
}
function rollbackCurrentTaskAndVerify(root2, current, readCurrentTask) {
  try {
    executeWrites([{ path: current.filePath, content: current.raw }], false, "vNext Runtime rollback after read-back failure");
    clearPendingTaskStoreAfterRollback(root2, current);
  } catch (error) {
    return {
      verified: false,
      detail: `rollback failed: ${error instanceof Error ? error.message : String(error)}`
    };
  }
  try {
    const rollbackReadBack = readCurrentTask(root2);
    if (rollbackReadBack.raw !== current.raw || rollbackReadBack.sourceTuple.revision !== current.sourceTuple.revision) {
      return {
        verified: false,
        detail: "rollback read-back did not restore the original canonical document."
      };
    }
    return { verified: true, detail: "rollback read-back verified." };
  } catch (error) {
    return {
      verified: false,
      detail: `rollback read-back failed: ${error instanceof Error ? error.message : String(error)}`
    };
  }
}
function rollbackDraftTransactionAndVerify(root2, current, artifact, originalTaskBasisContent, readCurrentTask) {
  try {
    const writes = [{ path: current.filePath, content: current.raw }];
    if (originalTaskBasisContent !== undefined) {
      writes.push({ path: artifact.filePath, content: originalTaskBasisContent });
    }
    executeWrites(writes, false, "vNext Runtime draft rollback after read-back failure");
    if (originalTaskBasisContent === undefined && fs11.existsSync(artifact.filePath)) {
      fs11.rmSync(artifact.filePath, { force: true });
    }
    clearPendingTaskStoreAfterRollback(root2, current);
  } catch (error) {
    return { verified: false, detail: `rollback failed: ${error instanceof Error ? error.message : String(error)}` };
  }
  try {
    const rollbackReadBack = readCurrentTask(root2);
    if (rollbackReadBack.raw !== current.raw || rollbackReadBack.sourceTuple.revision !== current.sourceTuple.revision) {
      return { verified: false, detail: "rollback read-back did not restore the original canonical CURRENT_TASK document." };
    }
    if (originalTaskBasisContent === undefined) {
      if (fs11.existsSync(artifact.filePath))
        return { verified: false, detail: "rollback left a newly-created task basis behind." };
    } else if (!fs11.existsSync(artifact.filePath) || fs11.readFileSync(artifact.filePath, "utf8") !== originalTaskBasisContent) {
      return { verified: false, detail: "rollback read-back did not restore the original task basis." };
    }
    return { verified: true, detail: "rollback read-back verified for CURRENT_TASK and task basis." };
  } catch (error) {
    return { verified: false, detail: `rollback read-back failed: ${error instanceof Error ? error.message : String(error)}` };
  }
}
function rollbackLifecycleTransactionAndVerify(root2, current, plan, readCurrentTask) {
  if (!plan.packageFilePath) {
    return rollbackCurrentTaskAndVerify(root2, current, readCurrentTask);
  }
  try {
    const rollbackOperations = [{ path: current.filePath, content: current.raw }];
    if (plan.originalPackageContent !== undefined) {
      rollbackOperations.push({ path: plan.packageFilePath, content: plan.originalPackageContent });
    }
    executeWrites(rollbackOperations, false, "vNext Runtime lifecycle rollback after read-back failure");
    if (plan.originalPackageContent === undefined && fs11.existsSync(plan.packageFilePath)) {
      fs11.rmSync(plan.packageFilePath, { force: true });
    }
    clearPendingTaskStoreAfterRollback(root2, current);
  } catch (error) {
    return {
      verified: false,
      detail: `rollback failed: ${error instanceof Error ? error.message : String(error)}`
    };
  }
  try {
    const rollbackReadBack = readCurrentTask(root2);
    if (rollbackReadBack.raw !== current.raw || rollbackReadBack.sourceTuple.revision !== current.sourceTuple.revision) {
      return { verified: false, detail: "rollback read-back did not restore the original canonical CURRENT_TASK document." };
    }
    const packageExists = fs11.existsSync(plan.packageFilePath);
    if (plan.originalPackageContent === undefined) {
      if (packageExists)
        return { verified: false, detail: "rollback read-back left a newly-created suspended package behind." };
    } else if (!packageExists || fs11.readFileSync(plan.packageFilePath, "utf8") !== plan.originalPackageContent) {
      return { verified: false, detail: "rollback read-back did not restore the original suspended package." };
    }
    return { verified: true, detail: "rollback read-back verified." };
  } catch (error) {
    return { verified: false, detail: `rollback read-back failed: ${error instanceof Error ? error.message : String(error)}` };
  }
}
function rollbackArchiveTransactionAndVerify(root2, current, plan, readCurrentTask) {
  try {
    executeWrites([{ path: current.filePath, content: current.raw }], false, "vNext Runtime archive rollback CURRENT_TASK");
    if (plan.originalArchiveContent === undefined) {
      if (fs11.existsSync(plan.archiveFilePath))
        fs11.rmSync(plan.archiveFilePath, { force: true });
    } else {
      executeWrites([{ path: plan.archiveFilePath, content: plan.originalArchiveContent }], false, "vNext Runtime archive rollback archive");
    }
    clearPendingTaskStoreAfterRollback(root2, current);
  } catch (error) {
    return { verified: false, detail: `rollback failed: ${error instanceof Error ? error.message : String(error)}` };
  }
  try {
    const rollbackReadBack = readCurrentTask(root2);
    if (rollbackReadBack.raw !== current.raw || rollbackReadBack.sourceTuple.revision !== current.sourceTuple.revision) {
      return { verified: false, detail: "archive rollback read-back did not restore the original CURRENT_TASK document." };
    }
    if (plan.originalArchiveContent === undefined) {
      if (fs11.existsSync(plan.archiveFilePath))
        return { verified: false, detail: "archive rollback left a newly-created archive behind." };
    } else if (!fs11.existsSync(plan.archiveFilePath) || fs11.readFileSync(plan.archiveFilePath, "utf8") !== plan.originalArchiveContent) {
      return { verified: false, detail: "archive rollback did not restore the original archive." };
    }
    return { verified: true, detail: "archive rollback read-back verified for CURRENT_TASK and archive." };
  } catch (error) {
    return { verified: false, detail: `rollback read-back failed: ${error instanceof Error ? error.message : String(error)}` };
  }
}
function rollbackSingleFileAndVerify(filePath, originalContent, label) {
  try {
    executeWrites([{ path: filePath, content: originalContent }], false, `vNext Runtime ${label} rollback`);
    if (fs11.readFileSync(filePath, "utf8") !== originalContent)
      return { verified: false, detail: `${label} rollback read-back did not restore the original document.` };
    return { verified: true, detail: `${label} rollback read-back verified.` };
  } catch (error) {
    return { verified: false, detail: `${label} rollback failed: ${error instanceof Error ? error.message : String(error)}` };
  }
}
function rollbackCreatedInboxRecordAndVerify(filePath) {
  try {
    if (fs11.existsSync(filePath)) {
      if (!fs11.lstatSync(filePath).isFile()) {
        return { verified: false, detail: "inbox rollback refused to remove a non-file target." };
      }
      fs11.rmSync(filePath, { force: true });
    }
    if (fs11.existsSync(filePath))
      return { verified: false, detail: "inbox rollback left the newly-created record behind." };
    return { verified: true, detail: "inbox rollback read-back verified." };
  } catch (error) {
    return { verified: false, detail: `inbox rollback failed: ${error instanceof Error ? error.message : String(error)}` };
  }
}

class GovernanceTransactionKernel {
  root;
  readCurrentTask;
  readFile;
  writeFiles;
  lastApplyCurrent;
  lastApplyAfter;
  lastApplyProposal;
  constructor(root2, readCurrentTask = readCanonicalCurrentTask, readFile = (filePath) => fs11.readFileSync(filePath, "utf8"), writeFiles = (operations, dryRun, summary) => executeWrites(operations, dryRun, summary)) {
    this.root = path12.resolve(root2);
    this.readCurrentTask = readCurrentTask;
    this.readFile = readFile;
    this.writeFiles = writeFiles;
  }
  stageCurrentTaskCommit(current, nextContent, nextState, proposal, writeTargets, writeOperations = [{ path: current.filePath, content: nextContent }], prepared) {
    if (prepared)
      persistTaskProjection(prepared, current.filePath, current.relativePath);
    const after = parseCanonicalCurrentTaskContent(nextContent, current.filePath, current.relativePath);
    after.runtimeState = nextState;
    if (after.frontmatter.task_store !== undefined) {
      Object.defineProperty(after.runtimeState, "__vnext_compact_history", { value: true, enumerable: false, configurable: true });
    }
    if (after.sourceTuple.document_id === current.sourceTuple.document_id) {
      TaskStore.forCurrent(this.root, current).stageCommit({
        before: current,
        after,
        after_source_revision: after.sourceTuple.revision,
        proposal,
        result: {
          status: "success",
          committed: true,
          operation_kind: proposal.operation_kind,
          idempotency_key: proposal.idempotency_key
        },
        write_targets: writeTargets,
        write_set: pendingWriteSetForOperations(this.root, writeOperations)
      });
    }
    this.lastApplyAfter = after;
    return after;
  }
  commitInboxRecordTransaction(current, proposal, plan, options) {
    const targetPath = plan.relativePath;
    const recordRevision = sha2565(plan.nextContent);
    if (plan.existing) {
      return buildResult("no-op", proposal, current, options, "matching canonical inbox record already exists; exact replay is a deterministic no-op.", {
        target_path: targetPath,
        planned_writes: [],
        previous_revision: recordRevision,
        resulting_revision: recordRevision,
        read_back_verified: true,
        state: resultState(current.runtimeState)
      });
    }
    if (options.dryRun) {
      return buildResult("success", proposal, current, options, "typed inbox record proposal validated; one canonical inbox write planned (dry-run).", {
        target_path: targetPath,
        planned_writes: [targetPath],
        resulting_revision: recordRevision,
        state: resultState(current.runtimeState)
      });
    }
    try {
      this.writeFiles([{ path: plan.filePath, content: plan.nextContent }], false, "vNext Runtime inbox record transaction committed");
    } catch (error) {
      const rollback = rollbackCreatedInboxRecordAndVerify(plan.filePath);
      return buildResult("blocked", proposal, current, options, `inbox atomic commit failed: ${error instanceof Error ? error.message : String(error)}; ${rollback.detail}`, {
        target_path: targetPath,
        code: rollback.verified ? "ATOMIC_COMMIT_FAILED" : "ROLLBACK_FAILED"
      });
    }
    try {
      const readBack = this.readFile(plan.filePath);
      if (readBack !== plan.nextContent)
        throw new Error("canonical inbox record read-back did not match the staged record.");
      assertCanonicalInboxRecordContent(readBack, proposal, targetPath);
      return buildResult("success", proposal, current, options, "inbox record transaction committed; canonical record read-back verified.", {
        target_path: targetPath,
        planned_writes: [targetPath],
        committed: true,
        governed_mutation_count: 1,
        resulting_revision: recordRevision,
        read_back_verified: true,
        state: resultState(current.runtimeState)
      });
    } catch (error) {
      const rollback = rollbackCreatedInboxRecordAndVerify(plan.filePath);
      return buildResult("blocked", proposal, current, options, rollback.verified ? `inbox record read-back failed: ${error instanceof Error ? error.message : String(error)}; rollback read-back verified.` : `inbox record read-back failed: ${error instanceof Error ? error.message : String(error)}; ${rollback.detail}`, {
        target_path: targetPath,
        code: rollback.verified ? "READ_BACK_FAILED" : "ROLLBACK_FAILED"
      });
    }
  }
  commitArchiveTransaction(current, proposal, plan, options) {
    if (plan === null) {
      return buildResult("no-op", proposal, current, options, "matching closed + archived archive receipt already exists; archive was not repeated.", {
        previous_revision: current.sourceTuple.revision,
        resulting_revision: current.sourceTuple.revision,
        read_back_verified: true,
        archive_path: archivePathForTask(this.root, current).relativePath,
        archive_revision: archiveAudits(current)[0]?.archive_revision,
        state: resultState(current.runtimeState)
      });
    }
    const nextRevision = sha2565(plan.nextContent);
    if (options.dryRun) {
      return buildResult("success", proposal, current, options, "typed archive proposal validated; atomic CURRENT_TASK + canonical archive write planned (dry-run).", {
        previous_revision: current.sourceTuple.revision,
        resulting_revision: nextRevision,
        archive_path: plan.archiveRelativePath,
        archive_revision: plan.archiveRevision,
        state: resultState(plan.next)
      });
    }
    let stagedAfter;
    try {
      stagedAfter = this.stageCurrentTaskCommit(current, plan.nextContent, plan.next, proposal, proposal.requested_write_targets, [
        { path: current.filePath, content: plan.nextContent },
        { path: plan.archiveFilePath, content: plan.nextArchiveContent }
      ], plan.prepared);
    } catch (error) {
      return buildResult("blocked", proposal, current, options, `task-store precommit staging failed: ${error instanceof Error ? error.message : String(error)}`, {
        code: error instanceof TaskStoreError ? error.code : "TASK_STORE_COMMIT_FAILED"
      });
    }
    try {
      executeWrites([
        { path: current.filePath, content: plan.nextContent },
        { path: plan.archiveFilePath, content: plan.nextArchiveContent }
      ], false, "vNext Runtime archive transaction committed");
    } catch (error) {
      const rollback = rollbackArchiveTransactionAndVerify(this.root, current, plan, this.readCurrentTask);
      return buildResult("blocked", proposal, current, options, rollback.verified ? `archive atomic write failed: ${error instanceof Error ? error.message : String(error)}; exact two-file rollback verified.` : `archive atomic write failed: ${error instanceof Error ? error.message : String(error)}; ${rollback.detail}`, {
        code: rollback.verified ? "ATOMIC_COMMIT_FAILED" : "ROLLBACK_FAILED"
      });
    }
    try {
      if (stagedAfter.sourceTuple.document_id === current.sourceTuple.document_id) {
        TaskStore.forCurrent(this.root, current).markCurrentPublished(nextRevision);
      }
      const readBack = stagedAfter.frontmatter.task_store === undefined ? this.readCurrentTask(this.root) : stagedAfter;
      if (readBack.raw !== plan.nextContent || readBack.sourceTuple.revision !== nextRevision) {
        throw new Error("canonical CURRENT_TASK read-back did not match the staged terminal document.");
      }
      if (!fs11.existsSync(plan.archiveFilePath) || fs11.readFileSync(plan.archiveFilePath, "utf8") !== plan.nextArchiveContent) {
        throw new Error("canonical task archive read-back did not match the staged archive.");
      }
      const receipt = readCanonicalArchive(this.root, readBack, plan.archiveRelativePath);
      if (receipt.revision !== plan.archiveRevision)
        throw new Error("canonical task archive revision changed during read-back.");
      const audits = archiveAudits(readBack);
      if (audits.length !== 1)
        throw new Error("terminal CURRENT_TASK read-back does not contain exactly one archive audit.");
      assertArchiveReceiptMatches(readBack, receipt, audits[0]);
      return buildResult("success", proposal, current, options, "archive transaction committed; CURRENT_TASK and canonical archive read-back verified.", {
        committed: true,
        governed_mutation_count: 2,
        previous_revision: current.sourceTuple.revision,
        resulting_revision: nextRevision,
        archive_path: plan.archiveRelativePath,
        archive_revision: plan.archiveRevision,
        read_back_verified: true,
        state: resultState(readBack.runtimeState)
      });
    } catch (error) {
      const rollback = rollbackArchiveTransactionAndVerify(this.root, current, plan, this.readCurrentTask);
      return buildResult("blocked", proposal, current, options, rollback.verified ? `archive read-back failed: ${error instanceof Error ? error.message : String(error)}; exact two-file rollback verified.` : `archive read-back failed: ${error instanceof Error ? error.message : String(error)}; ${rollback.detail}`, {
        code: rollback.verified ? "READ_BACK_FAILED" : "ROLLBACK_FAILED"
      });
    }
  }
  commitProjectStatusTransaction(current, proposal, plan, options) {
    const targetPath = workflowDocPathForRoot(this.root, "STATUS.md").relativePath;
    if (plan === null) {
      return buildResult("no-op", proposal, current, options, "matching STATUS reconciliation already exists; STATUS was not rewritten.", {
        target_path: targetPath,
        planned_writes: [],
        previous_revision: fileRevisionForPath(workflowDocPathForRoot(this.root, "STATUS.md").filePath),
        resulting_revision: fileRevisionForPath(workflowDocPathForRoot(this.root, "STATUS.md").filePath),
        read_back_verified: true,
        state: resultState(current.runtimeState)
      });
    }
    if (options.dryRun) {
      return buildResult("success", proposal, current, options, "typed project-status proposal validated; STATUS-only write planned (dry-run).", {
        target_path: plan.statusRelativePath,
        previous_revision: sha2565(plan.originalStatusContent),
        resulting_revision: plan.statusRevision,
        state: resultState(current.runtimeState)
      });
    }
    try {
      executeWrites([{ path: plan.statusFilePath, content: plan.nextStatusContent }], false, "vNext Runtime project status transaction committed");
    } catch (error) {
      const rollback = rollbackSingleFileAndVerify(plan.statusFilePath, plan.originalStatusContent, "STATUS");
      return buildResult("blocked", proposal, current, options, rollback.verified ? `STATUS write failed: ${error instanceof Error ? error.message : String(error)}; STATUS rollback verified (archive remains committed).` : `STATUS write failed: ${error instanceof Error ? error.message : String(error)}; ${rollback.detail}`, {
        target_path: plan.statusRelativePath,
        code: rollback.verified ? "ATOMIC_COMMIT_FAILED" : "ROLLBACK_FAILED"
      });
    }
    try {
      const readBack = fs11.readFileSync(plan.statusFilePath, "utf8");
      if (readBack !== plan.nextStatusContent)
        throw new Error("STATUS read-back did not match the staged typed reconciliation.");
      const receipt = matchingStatusReceipt(readBack, plan.statusRelativePath, plan.archive);
      if (receipt === null || receipt.archivePath !== plan.archive.relativePath || receipt.archiveRevision !== plan.archive.revision || receipt.sourceRevision !== plan.archive.sourceRevision || receipt.deltaDigest !== digest3(proposal.semantic_delta)) {
        throw new Error("STATUS read-back receipt did not match the canonical archive.");
      }
      assertStatusProjection(readBack, proposal.semantic_delta, plan.statusRelativePath);
      return buildResult("success", proposal, current, options, "project-status transaction committed; STATUS-only read-back verified.", {
        target_path: plan.statusRelativePath,
        committed: true,
        governed_mutation_count: 1,
        previous_revision: sha2565(plan.originalStatusContent),
        resulting_revision: plan.statusRevision,
        read_back_verified: true,
        state: resultState(current.runtimeState)
      });
    } catch (error) {
      const rollback = rollbackSingleFileAndVerify(plan.statusFilePath, plan.originalStatusContent, "STATUS");
      return buildResult("blocked", proposal, current, options, rollback.verified ? `STATUS read-back failed: ${error instanceof Error ? error.message : String(error)}; STATUS rollback verified (archive remains committed).` : `STATUS read-back failed: ${error instanceof Error ? error.message : String(error)}; ${rollback.detail}`, {
        target_path: plan.statusRelativePath,
        code: rollback.verified ? "READ_BACK_FAILED" : "ROLLBACK_FAILED"
      });
    }
  }
  commitLessonRecordTransaction(current, proposal, plan, options) {
    const targetPath = workflowDocPathForRoot(this.root, "LESSONS.md").relativePath;
    if (plan === null) {
      return buildResult("no-op", proposal, current, options, "lesson admission is defer/no-op or all admitted candidates are already durably recorded; LESSONS was not rewritten.", {
        target_path: targetPath,
        planned_writes: [],
        read_back_verified: true,
        state: resultState(current.runtimeState)
      });
    }
    if (options.dryRun) {
      return buildResult("success", proposal, current, options, "typed lesson-record proposal validated; LESSONS-only write planned (dry-run).", {
        target_path: plan.lessonsRelativePath,
        previous_revision: sha2565(plan.originalLessonsContent),
        resulting_revision: plan.lessonsRevision,
        state: resultState(current.runtimeState)
      });
    }
    try {
      executeWrites([{ path: plan.lessonsFilePath, content: plan.nextLessonsContent }], false, "vNext Runtime lesson record transaction committed");
    } catch (error) {
      const rollback = rollbackSingleFileAndVerify(plan.lessonsFilePath, plan.originalLessonsContent, "LESSONS");
      return buildResult("blocked", proposal, current, options, rollback.verified ? `LESSONS write failed: ${error instanceof Error ? error.message : String(error)}; LESSONS rollback verified (archive and STATUS remain committed).` : `LESSONS write failed: ${error instanceof Error ? error.message : String(error)}; ${rollback.detail}`, {
        target_path: plan.lessonsRelativePath,
        code: rollback.verified ? "ATOMIC_COMMIT_FAILED" : "ROLLBACK_FAILED"
      });
    }
    try {
      const readBack = fs11.readFileSync(plan.lessonsFilePath, "utf8");
      if (readBack !== plan.nextLessonsContent)
        throw new Error("LESSONS read-back did not match the staged typed lesson record.");
      readDurableLessonRecords(readBack, plan.lessonsRelativePath);
      return buildResult("success", proposal, current, options, "lesson-record transaction committed; LESSONS-only read-back verified.", {
        target_path: plan.lessonsRelativePath,
        committed: true,
        governed_mutation_count: 1,
        previous_revision: sha2565(plan.originalLessonsContent),
        resulting_revision: plan.lessonsRevision,
        read_back_verified: true,
        state: resultState(current.runtimeState)
      });
    } catch (error) {
      const rollback = rollbackSingleFileAndVerify(plan.lessonsFilePath, plan.originalLessonsContent, "LESSONS");
      return buildResult("blocked", proposal, current, options, rollback.verified ? `LESSONS read-back failed: ${error instanceof Error ? error.message : String(error)}; LESSONS rollback verified (archive and STATUS remain committed).` : `LESSONS read-back failed: ${error instanceof Error ? error.message : String(error)}; ${rollback.detail}`, {
        target_path: plan.lessonsRelativePath,
        code: rollback.verified ? "READ_BACK_FAILED" : "ROLLBACK_FAILED"
      });
    }
  }
  commitKnowledgeRecordTransaction(current, proposal, plan, options) {
    const targetPath = plan.relativePath;
    if (plan.existing) {
      return buildResult("no-op", proposal, current, options, "matching durable Contract/Decision record already exists; knowledge promotion is a deterministic no-op.", {
        target_path: targetPath,
        planned_writes: [],
        previous_revision: sha2565(plan.originalContent),
        resulting_revision: sha2565(plan.originalContent),
        read_back_verified: true,
        state: resultState(current.runtimeState)
      });
    }
    if (options.dryRun) {
      return buildResult("success", proposal, current, options, "typed knowledge admission validated; one canonical knowledge document write planned (dry-run).", {
        target_path: targetPath,
        previous_revision: sha2565(plan.originalContent),
        resulting_revision: sha2565(plan.nextContent),
        state: resultState(current.runtimeState)
      });
    }
    try {
      this.writeFiles([{ path: plan.filePath, content: plan.nextContent }], false, `vNext Runtime ${proposal.operation_kind} committed`);
    } catch (error) {
      const rollback = rollbackSingleFileAndVerify(plan.filePath, plan.originalContent, "knowledge");
      return buildResult("blocked", proposal, current, options, rollback.verified ? `knowledge write failed: ${error instanceof Error ? error.message : String(error)}; knowledge rollback verified.` : `knowledge write failed: ${error instanceof Error ? error.message : String(error)}; ${rollback.detail}`, {
        target_path: targetPath,
        code: rollback.verified ? "ATOMIC_COMMIT_FAILED" : "ROLLBACK_FAILED"
      });
    }
    try {
      const readBack = this.readFile(plan.filePath);
      if (readBack !== plan.nextContent)
        throw new Error("canonical knowledge document read-back did not match the staged record.");
      const records = readDurableKnowledgeRecords(readBack, targetPath, proposal.semantic_delta.knowledge_kind);
      if (!records.some((record) => JSON.stringify(record) === JSON.stringify(plan.record))) {
        throw new Error("canonical knowledge read-back did not contain the admitted record.");
      }
      return buildResult("success", proposal, current, options, "knowledge promotion committed; canonical Contract/Decision read-back verified.", {
        target_path: targetPath,
        committed: true,
        governed_mutation_count: 1,
        previous_revision: sha2565(plan.originalContent),
        resulting_revision: sha2565(plan.nextContent),
        read_back_verified: true,
        state: resultState(current.runtimeState)
      });
    } catch (error) {
      const rollback = rollbackSingleFileAndVerify(plan.filePath, plan.originalContent, "knowledge");
      return buildResult("blocked", proposal, current, options, rollback.verified ? `knowledge read-back failed: ${error instanceof Error ? error.message : String(error)}; rollback read-back verified.` : `knowledge read-back failed: ${error instanceof Error ? error.message : String(error)}; ${rollback.detail}`, {
        target_path: targetPath,
        code: rollback.verified ? "READ_BACK_FAILED" : "ROLLBACK_FAILED"
      });
    }
  }
  commitLifecycleTransaction(current, proposal, plan, options) {
    const nextRevision = sha2565(plan.nextContent);
    if (proposal.mode === "supersede") {
      let basis;
      let historyPath;
      let historyContent;
      const historyInput = {
        currentPath: current.filePath,
        previousContent: current.raw,
        nextContent: plan.nextContent,
        documentId: current.sourceTuple.document_id,
        taskId: current.runtimeState.task_id,
        evidencePlanRevision: current.runtimeState.evidence_plan_revision,
        referencedEvidence: [
          ...current.runtimeState.execution_log.flatMap((item) => item.evidence_refs),
          ...current.runtimeState.findings.flatMap((item) => item.evidence_refs),
          ...(current.runtimeState.claim_evidence ?? []).flatMap((claim) => claim.slots.flatMap((slot) => slot.evidence_refs))
        ]
      };
      try {
        basis = readTaskBasisReferenceFromBody(current.body) ? readCanonicalTaskBasis(this.root, current) : undefined;
        const location = taskHistoryLocation({ ...historyInput, ...basis ? { basisPath: basis.filePath, basisContent: basis.content } : {} });
        historyPath = path12.relative(this.root, location.path).replace(/\\/gu, "/");
        historyContent = location.content;
      } catch (error) {
        return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
          code: error instanceof VNextRuntimeError ? error.code : "TASK_HISTORY_INVALID"
        });
      }
      const plannedWrites = [current.relativePath, historyPath];
      if (options.dryRun) {
        return buildResult("success", proposal, current, options, "typed supersede proposal validated; immutable history and canonical CURRENT_TASK writes planned (dry-run).", {
          planned_writes: plannedWrites,
          previous_revision: current.sourceTuple.revision,
          resulting_revision: nextRevision,
          state: resultState(plan.next)
        });
      }
      let stagedAfter;
      try {
        stagedAfter = this.stageCurrentTaskCommit(current, plan.nextContent, plan.next, proposal, proposal.requested_write_targets, [
          { path: current.filePath, content: plan.nextContent },
          { path: path12.join(this.root, ...historyPath.split("/")), content: historyContent }
        ], plan.prepared);
      } catch (error) {
        return buildResult("blocked", proposal, current, options, `task-store precommit staging failed: ${error instanceof Error ? error.message : String(error)}`, {
          code: error instanceof TaskStoreError ? error.code : "TASK_STORE_COMMIT_FAILED"
        });
      }
      let readBack;
      try {
        commitSupersedeWithHistory({ ...historyInput, ...basis ? { basisPath: basis.filePath, basisContent: basis.content } : {} }, (content) => {
          const parsed = parseCanonicalCurrentTaskContent(content, current.filePath, current.relativePath);
          readBack = stagedAfter.frontmatter.task_store === undefined ? parsed : stagedAfter;
          if (readBack.sourceTuple.revision !== nextRevision || readBack.runtimeState.workflow_status !== "superseded" || readBack.runtimeState.lifecycle_state !== "active") {
            throw new Error("supersede CURRENT_TASK read-back did not preserve the exact superseded + active state.");
          }
        });
        if (stagedAfter.sourceTuple.document_id === current.sourceTuple.document_id) {
          TaskStore.forCurrent(this.root, current).markCurrentPublished(nextRevision);
        }
      } catch (error) {
        if (fs11.existsSync(current.filePath) && sha2565(fs11.readFileSync(current.filePath, "utf8")) === current.sourceTuple.revision) {
          clearPendingTaskStoreAfterRollback(this.root, current);
        }
        return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
          planned_writes: plannedWrites,
          code: "TASK_EVOLUTION_COMMIT_FAILED"
        });
      }
      return buildResult("success", proposal, current, options, "typed supersede proposal committed; immutable history and canonical CURRENT_TASK read-back verified.", {
        planned_writes: plannedWrites,
        committed: true,
        governed_mutation_count: 2,
        previous_revision: current.sourceTuple.revision,
        resulting_revision: nextRevision,
        read_back_verified: true,
        state: resultState(readBack.runtimeState)
      });
    }
    if (!plan.packageFilePath || !plan.packageRelativePath || plan.nextPackageContent === undefined) {
      return buildResult("blocked", proposal, current, options, "lifecycle transaction is missing its suspended package plan.", { code: "RUNTIME_HANDLER_BLOCKED" });
    }
    if (options.dryRun) {
      return buildResult("success", proposal, current, options, "typed lifecycle proposal validated; atomic CURRENT_TASK + suspended package write planned (dry-run).", {
        previous_revision: current.sourceTuple.revision,
        resulting_revision: nextRevision,
        state: resultState(plan.next, undefined, plan.packageRelativePath)
      });
    }
    let stagedAfter;
    try {
      stagedAfter = this.stageCurrentTaskCommit(current, plan.nextContent, plan.next, proposal, proposal.requested_write_targets, [
        { path: current.filePath, content: plan.nextContent },
        { path: plan.packageFilePath, content: plan.nextPackageContent }
      ], plan.prepared);
    } catch (error) {
      return buildResult("blocked", proposal, current, options, `task-store precommit staging failed: ${error instanceof Error ? error.message : String(error)}`, {
        code: error instanceof TaskStoreError ? error.code : "TASK_STORE_COMMIT_FAILED"
      });
    }
    try {
      executeWrites([
        { path: current.filePath, content: plan.nextContent },
        { path: plan.packageFilePath, content: plan.nextPackageContent }
      ], false, `vNext Runtime ${proposal.mode} lifecycle transaction committed`);
    } catch (error) {
      return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), { code: "ATOMIC_COMMIT_FAILED" });
    }
    try {
      if (stagedAfter.sourceTuple.document_id === current.sourceTuple.document_id) {
        TaskStore.forCurrent(this.root, current).markCurrentPublished(nextRevision);
      }
      const readBack = stagedAfter.frontmatter.task_store === undefined ? this.readCurrentTask(this.root) : stagedAfter;
      if (readBack.raw !== plan.nextContent || readBack.sourceTuple.revision !== nextRevision) {
        throw new Error("canonical CURRENT_TASK read-back did not match the staged lifecycle document.");
      }
      if (!fs11.existsSync(plan.packageFilePath) || fs11.readFileSync(plan.packageFilePath, "utf8") !== plan.nextPackageContent) {
        throw new Error("suspended package read-back did not match the staged lifecycle artifact.");
      }
      const lifecycleDelta = proposal.semantic_delta;
      const artifactKind = lifecycleDelta.action === "pause" ? "paused" : lifecycleDelta.action === "interrupt" ? "interrupted" : lifecycleDelta.action === "resume-paused" || lifecycleDelta.action === "resume-interrupted" ? lifecycleDelta.artifact_kind : "paused";
      const parsedPackage = parseSuspendedPackage(this.root, readBack, plan.packageRelativePath, artifactKind);
      const expectedStatus = proposal.mode === "resume-paused" || proposal.mode === "resume-interrupted" ? "rehydrated" : "ready_for_resume";
      if (parsedPackage.rehydrationStatus !== expectedStatus || parsedPackage.ownershipState !== (expectedStatus === "rehydrated" ? "rehydrated" : "recovery_only")) {
        throw new Error("suspended package marker read-back did not match the lifecycle transaction.");
      }
      return buildResult("success", proposal, current, options, "typed lifecycle proposal committed; CURRENT_TASK and suspended package read-back verified.", {
        committed: true,
        governed_mutation_count: 2,
        previous_revision: current.sourceTuple.revision,
        resulting_revision: nextRevision,
        read_back_verified: true,
        state: resultState(readBack.runtimeState, undefined, plan.packageRelativePath)
      });
    } catch (error) {
      const rollback = rollbackLifecycleTransactionAndVerify(this.root, current, plan, this.readCurrentTask);
      return buildResult("blocked", proposal, current, options, rollback.verified ? `Runtime lifecycle read-back failed: ${error instanceof Error ? error.message : String(error)}; rollback read-back verified.` : `Runtime lifecycle read-back failed: ${error instanceof Error ? error.message : String(error)}; ${rollback.detail}`, { code: rollback.verified ? "READ_BACK_FAILED" : "ROLLBACK_FAILED" });
    }
  }
  apply(rawProposal, options = {}) {
    return withGovernanceWriteLock(this.root, () => {
      this.lastApplyCurrent = undefined;
      this.lastApplyAfter = undefined;
      this.lastApplyProposal = undefined;
      const result = this.applyLocked(rawProposal, options);
      const before = this.lastApplyCurrent;
      if (!options.dryRun && result.committed && before) {
        try {
          const after = this.lastApplyAfter ?? this.readCurrentTask(this.root);
          const afterStore = TaskStore.forCurrent(this.root, after);
          const manifest = before.sourceTuple.document_id === after.sourceTuple.document_id ? afterStore.recordCommit({ before, after, proposal: this.lastApplyProposal ?? rawProposal, result }) : afterStore.ensureInitialized(after);
          if (manifest) {
            const verifiedAfter = this.readCurrentTask(this.root);
            if (verifiedAfter.raw !== after.raw || verifiedAfter.sourceTuple.revision !== after.sourceTuple.revision) {
              throw new Error("task-store publication read-back did not match the committed CURRENT_TASK after-image.");
            }
            return {
              ...result,
              task_store: {
                manifest_path: `${manifest.storage_root}/manifest.json`,
                source_revision: manifest.head.source_revision,
                definition_revision: manifest.head.definition_revision,
                state_revision: manifest.head.state_revision,
                event_sequence: manifest.head.event_sequence
              }
            };
          }
        } catch (error) {
          return {
            ...result,
            status: "blocked",
            code: "TASK_STORE_COMMIT_FAILED",
            message: `${result.message} Task-store publication needs reconciliation: ${error instanceof Error ? error.message : String(error)}`,
            read_back_verified: false
          };
        }
      }
      return result;
    });
  }
  applyLocked(rawProposal, options) {
    let proposal;
    try {
      proposal = validateRuntimeProposal(rawProposal);
    } catch (error) {
      const code = error instanceof VNextRuntimeError ? error.code : "RUNTIME_SCHEMA_INVALID";
      const fallbackOperation = isRecord4(rawProposal) && typeof rawProposal.operation_kind === "string" && RUNTIME_OPERATION_KINDS.includes(rawProposal.operation_kind) ? rawProposal.operation_kind : "task-state-transaction";
      const fallbackKey = isRecord4(rawProposal) && typeof rawProposal.idempotency_key === "string" ? rawProposal.idempotency_key : "invalid-proposal";
      return {
        status: "blocked",
        operation_kind: fallbackOperation,
        idempotency_key: fallbackKey,
        target_path: CURRENT_TASK_RELATIVE_FALLBACK,
        dry_run: options.dryRun === true,
        committed: false,
        message: error instanceof Error ? error.message : String(error),
        code,
        planned_writes: [],
        governed_mutation_count: 0,
        read_back_verified: false
      };
    }
    this.lastApplyProposal = proposal;
    let current;
    if (!options.dryRun) {
      try {
        recoverPendingTaskStoreCommit(this.root);
      } catch (error) {
        return {
          status: "blocked",
          operation_kind: proposal.operation_kind,
          idempotency_key: proposal.idempotency_key,
          target_path: proposal.source_tuple.path,
          dry_run: false,
          committed: false,
          message: error instanceof Error ? error.message : String(error),
          code: error instanceof VNextRuntimeError || error instanceof TaskStoreError ? error.code : "TASK_STORE_RECOVERY_REQUIRED",
          planned_writes: [],
          governed_mutation_count: 0,
          read_back_verified: false
        };
      }
    }
    try {
      current = this.readCurrentTask(this.root);
    } catch (error) {
      return {
        status: "blocked",
        operation_kind: proposal.operation_kind,
        idempotency_key: proposal.idempotency_key,
        target_path: proposal.source_tuple.path,
        dry_run: options.dryRun === true,
        committed: false,
        message: error instanceof Error ? error.message : String(error),
        code: error instanceof VNextRuntimeError ? error.code : "RUNTIME_SOURCE_INVALID",
        planned_writes: [],
        governed_mutation_count: 0,
        read_back_verified: false
      };
    }
    this.lastApplyCurrent = current;
    let taskStore;
    try {
      taskStore = TaskStore.forCurrent(this.root, current);
    } catch (error) {
      return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
        code: error instanceof TaskStoreError ? error.code : "TASK_STORE_INIT_FAILED"
      });
    }
    try {
      if (proposal.source_tuple.path !== current.relativePath)
        fail3("RUNTIME_PATH_INVALID", "proposal source path is not the exact canonical CURRENT_TASK path.");
      if (proposal.operation_kind === "lifecycle-transaction") {
        assertRequestedLifecycleTargets(this.root, current, proposal);
      } else if (proposal.operation_kind === "inbox-record-transaction") {
        assertRequestedInboxTargets(this.root, current, proposal);
      } else if (proposal.operation_kind === "archive-transaction" || proposal.operation_kind === "project-status-transaction" || proposal.operation_kind === "lesson-record-transaction" || proposal.operation_kind === "contract-candidate-commit" || proposal.operation_kind === "decision-record-transaction") {
        assertRequestedCloseTargets(this.root, current, proposal);
      } else if (proposal.operation_kind === "task-state-transaction" && proposal.semantic_delta.kind === "task-state" && ["create-draft", "update-draft", "commit-replan", "record-user-evidence"].includes(proposal.semantic_delta.action)) {
        const taskId = proposal.semantic_delta.action === "create-draft" || proposal.semantic_delta.action === "update-draft" ? proposal.semantic_delta.task_id : current.runtimeState.task_id;
        const expectedBasisPath = taskBasisRelativePath(current.relativePath, taskId);
        if (proposal.requested_write_targets.length !== (proposal.semantic_delta.action === "create-draft" && proposal.semantic_delta.predecessor ? 3 : 2) || proposal.requested_write_targets.length === 3 && proposal.requested_write_targets[2] !== successorSnapshotPath(current.relativePath, current.sourceTuple.document_id, current.sourceTuple.revision) || proposal.requested_write_targets[0] !== current.relativePath || proposal.requested_write_targets[1] !== expectedBasisPath) {
          fail3("RUNTIME_PATH_INVALID", "draft proposal must target the exact canonical CURRENT_TASK and identity-derived task basis paths.");
        }
      } else if (proposal.requested_write_targets.length !== 1 || proposal.requested_write_targets[0] !== current.relativePath) {
        fail3("RUNTIME_PATH_INVALID", "proposal write target is not the exact canonical CURRENT_TASK path.");
      }
    } catch (error) {
      return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), { code: error instanceof VNextRuntimeError ? error.code : "RUNTIME_PATH_INVALID" });
    }
    if (proposal.semantic_delta.kind === "task-state" && proposal.semantic_delta.action === "commit-replan") {
      return buildResult("blocked", proposal, current, options, "Direct commit-replan is disabled until a revision-bound candidate, complete prior-obligation disposition, and explicit confirmation are available. CURRENT_TASK was not changed.", { code: "REPLAN_CONFIRMATION_REQUIRED" });
    }
    if (proposal.operation_kind === "inbox-record-transaction") {
      const inboxProposal = proposal;
      let inspectedPlan;
      try {
        ensureAuthorityKinds(inboxProposal, ["evidence-admission"]);
        inspectedPlan = inspectInboxRecordTransaction(this.root, inboxProposal);
      } catch (error) {
        const code = error instanceof VNextRuntimeError ? error.code : "RUNTIME_HANDLER_BLOCKED";
        return buildResult(code === "IDEMPOTENCY_CONFLICT" ? "conflict" : "blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
          target_path: inboxProposal.semantic_delta.target_path,
          code
        });
      }
      if (inspectedPlan.existing) {
        return this.commitInboxRecordTransaction(current, inboxProposal, inspectedPlan, options);
      }
      const conflictField = compareSourceTuple(proposal.source_tuple, current.sourceTuple);
      if (conflictField) {
        return buildResult("conflict", proposal, current, options, `canonical source tuple is stale at ${conflictField}.`, {
          target_path: inboxProposal.semantic_delta.target_path,
          code: "SOURCE_TUPLE_MISMATCH",
          previous_revision: current.sourceTuple.revision
        });
      }
      try {
        const plan = prepareInboxRecordTransaction(this.root, current, inboxProposal);
        return this.commitInboxRecordTransaction(current, inboxProposal, plan, options);
      } catch (error) {
        return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
          target_path: inboxProposal.semantic_delta.target_path,
          code: error instanceof VNextRuntimeError ? error.code : "RUNTIME_HANDLER_BLOCKED"
        });
      }
    }
    if (proposal.operation_kind === "contract-candidate-commit" || proposal.operation_kind === "decision-record-transaction") {
      const knowledgeProposal = proposal;
      let inspectedPlan;
      try {
        inspectedPlan = inspectKnowledgeRecordTransaction(this.root, current, knowledgeProposal);
      } catch (error) {
        const code = error instanceof VNextRuntimeError ? error.code : "RUNTIME_HANDLER_BLOCKED";
        const conflictCodes = new Set(["IDEMPOTENCY_CONFLICT", "KNOWLEDGE_IDENTITY_CONFLICT"]);
        return buildResult(conflictCodes.has(code) ? "conflict" : "blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
          target_path: proposal.operation_kind === "contract-candidate-commit" ? workflowDocPathForRoot(this.root, "CONTRACTS.md").relativePath : workflowDocPathForRoot(this.root, "DECISIONS.md").relativePath,
          code
        });
      }
      if (inspectedPlan.existing) {
        return this.commitKnowledgeRecordTransaction(current, knowledgeProposal, inspectedPlan, options);
      }
      const conflictField = compareSourceTuple(proposal.source_tuple, current.sourceTuple);
      if (conflictField) {
        return buildResult("conflict", proposal, current, options, `canonical source tuple is stale at ${conflictField}.`, {
          target_path: inspectedPlan.relativePath,
          code: "SOURCE_TUPLE_MISMATCH",
          previous_revision: current.sourceTuple.revision
        });
      }
      try {
        const plan = prepareKnowledgeRecordTransaction(this.root, current, knowledgeProposal);
        return this.commitKnowledgeRecordTransaction(current, knowledgeProposal, plan, options);
      } catch (error) {
        const code = error instanceof VNextRuntimeError ? error.code : "RUNTIME_HANDLER_BLOCKED";
        const conflictCodes = new Set(["IDEMPOTENCY_CONFLICT", "KNOWLEDGE_IDENTITY_CONFLICT"]);
        return buildResult(conflictCodes.has(code) ? "conflict" : "blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
          target_path: proposal.operation_kind === "contract-candidate-commit" ? workflowDocPathForRoot(this.root, "CONTRACTS.md").relativePath : workflowDocPathForRoot(this.root, "DECISIONS.md").relativePath,
          code
        });
      }
    }
    if (proposal.semantic_delta.kind === "task-state" && proposal.semantic_delta.action === "record-step-preflight") {
      const executionMode = proposal.semantic_delta.mode ?? proposal.mode;
      if (executionMode !== "repair") {
        try {
          assertOrdinaryPreflight(current, this.root);
        } catch (error) {
          const code = error instanceof VNextRuntimeError ? error.code : "PREFLIGHT_BLOCKED";
          return buildResult(code === "REVIEW_TARGET_STALE" ? "conflict" : "blocked", proposal, current, options, error instanceof Error ? error.message : String(error), { code });
        }
      }
    }
    if (proposal.semantic_delta.kind === "task-state" && proposal.semantic_delta.action === "retry-step") {
      const delta = proposal.semantic_delta;
      const ledger = current.runtimeState.step_attempts?.[delta.step_id];
      const priorRetry = ledger?.attempts.find((a) => a.idempotency_key === proposal.idempotency_key);
      if (priorRetry) {
        if (proposal.source_tuple.document_id !== current.sourceTuple.document_id || ledger.evidence_plan_revision !== current.runtimeState.evidence_plan_revision || priorRetry.request_digest !== retryRequestDigest(current, delta))
          return buildResult("conflict", proposal, current, options, "Retry key is already bound to different task/plan/request semantics.", { code: "RETRY_IDEMPOTENCY_CONFLICT" });
        return buildResult("no-op", proposal, current, options, "This retry was already admitted; no budget or state changed.", { read_back_verified: true, resulting_revision: current.sourceTuple.revision });
      }
    }
    try {
      if (!options.dryRun)
        taskStore.ensureInitialized(current);
    } catch (error) {
      return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
        code: error instanceof TaskStoreError ? error.code : "TASK_STORE_INIT_FAILED"
      });
    }
    const proposalDigest = digest3(proposal);
    const prior = current.runtimeState.applied_proposals.find((item) => item.idempotency_key === proposal.idempotency_key);
    let persistentPrior = null;
    try {
      persistentPrior = taskStore.lookupIdempotency(proposal.idempotency_key);
    } catch (error) {
      return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
        code: error instanceof TaskStoreError ? error.code : "TASK_STORE_INDEX_INVALID"
      });
    }
    const persistentReplayEligible = true;
    if (!prior && persistentPrior && persistentReplayEligible) {
      if (persistentPrior.proposal_digest !== proposalDigest) {
        return buildResult("conflict", proposal, current, options, "persistent idempotency index binds this key to different proposal bytes.", {
          code: "IDEMPOTENCY_CONFLICT",
          previous_revision: current.sourceTuple.revision
        });
      }
      try {
        if (proposal.operation_kind === "archive-transaction") {
          const plan = prepareArchiveTransaction(this.root, current, proposal, options.now?.() ?? new Date().toISOString());
          if (plan !== null) {
            return buildResult("blocked", proposal, current, options, "persistent archive replay found a visible archive target that is not the committed replay state.", {
              code: "LIFECYCLE_REPLAY_INCOMPLETE"
            });
          }
        } else if (proposal.operation_kind === "project-status-transaction") {
          const plan = prepareProjectStatusTransaction(this.root, current, proposal);
          if (plan !== null) {
            return this.commitProjectStatusTransaction(current, proposal, plan, options);
          }
        } else if (proposal.operation_kind === "lesson-record-transaction") {
          const plan = prepareLessonRecordTransaction(this.root, current, proposal);
          if (plan !== null) {
            return buildResult("blocked", proposal, current, options, "persistent LESSONS replay found visible records that are not the committed lesson admission.", {
              target_path: workflowDocPathForRoot(this.root, "LESSONS.md").relativePath,
              code: "RUNTIME_REPLAY_INCOMPLETE"
            });
          }
        }
      } catch (error) {
        return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
          target_path: proposal.operation_kind === "project-status-transaction" ? workflowDocPathForRoot(this.root, "STATUS.md").relativePath : proposal.operation_kind === "lesson-record-transaction" ? workflowDocPathForRoot(this.root, "LESSONS.md").relativePath : undefined,
          code: error instanceof VNextRuntimeError ? error.code : "RUNTIME_REPLAY_INCOMPLETE"
        });
      }
      return buildResult("no-op", proposal, current, options, "proposal replay was found in the persistent task store; no execution or state change was repeated.", {
        planned_writes: [],
        previous_revision: current.sourceTuple.revision,
        resulting_revision: current.sourceTuple.revision,
        read_back_verified: true
      });
    }
    if (prior) {
      if (prior.proposal_digest !== proposalDigest) {
        return buildResult("conflict", proposal, current, options, "idempotency key was already used by a different proposal.", { code: "IDEMPOTENCY_CONFLICT", previous_revision: current.sourceTuple.revision });
      }
      if (proposal.operation_kind === "lifecycle-transaction") {
        try {
          assertLifecycleReplayArtifacts(this.root, current, proposal);
        } catch (error) {
          return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
            code: error instanceof VNextRuntimeError ? error.code : "LIFECYCLE_REPLAY_INCOMPLETE"
          });
        }
      } else if (proposal.operation_kind === "task-state-transaction") {
        try {
          assertTaskStateReplay(this.root, current, proposal);
        } catch (error) {
          return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
            code: error instanceof VNextRuntimeError ? error.code : "RUNTIME_REPLAY_INCOMPLETE"
          });
        }
      } else if (proposal.operation_kind === "archive-transaction") {
        try {
          assertArchiveReplay(this.root, current, proposal);
        } catch (error) {
          return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
            code: error instanceof VNextRuntimeError ? error.code : "LIFECYCLE_REPLAY_INCOMPLETE"
          });
        }
      } else if (proposal.operation_kind === "project-status-transaction") {
        try {
          const plan = prepareProjectStatusTransaction(this.root, current, proposal);
          return this.commitProjectStatusTransaction(current, proposal, plan, options);
        } catch (error) {
          return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
            target_path: workflowDocPathForRoot(this.root, "STATUS.md").relativePath,
            code: error instanceof VNextRuntimeError ? error.code : "RUNTIME_REPLAY_INCOMPLETE"
          });
        }
      }
      return buildResult("no-op", proposal, current, options, "proposal replay is an idempotent no-op.", {
        previous_revision: current.sourceTuple.revision,
        resulting_revision: current.sourceTuple.revision,
        read_back_verified: true,
        state: resultState(current.runtimeState)
      });
    }
    const conflictField = compareSourceTuple(proposal.source_tuple, current.sourceTuple);
    if (conflictField) {
      return buildResult("conflict", proposal, current, options, `canonical source tuple is stale at ${conflictField}.`, { code: "SOURCE_TUPLE_MISMATCH", previous_revision: current.sourceTuple.revision });
    }
    const now = options.now?.() ?? new Date().toISOString();
    if (Number.isNaN(Date.parse(now))) {
      return buildResult("blocked", proposal, current, options, "Runtime clock returned an invalid timestamp.", { code: "RUNTIME_CLOCK_INVALID" });
    }
    if (proposal.operation_kind === "lifecycle-transaction") {
      try {
        const plan = prepareLifecycleTransaction(this.root, current, proposal, now);
        return this.commitLifecycleTransaction(current, proposal, plan, options);
      } catch (error) {
        return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), { code: error instanceof VNextRuntimeError ? error.code : "RUNTIME_HANDLER_BLOCKED" });
      }
    }
    if (proposal.operation_kind === "archive-transaction") {
      try {
        const plan = prepareArchiveTransaction(this.root, current, proposal, now);
        return this.commitArchiveTransaction(current, proposal, plan, options);
      } catch (error) {
        return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), { code: error instanceof VNextRuntimeError ? error.code : "RUNTIME_HANDLER_BLOCKED" });
      }
    }
    if (proposal.operation_kind === "project-status-transaction") {
      try {
        const plan = prepareProjectStatusTransaction(this.root, current, proposal);
        return this.commitProjectStatusTransaction(current, proposal, plan, options);
      } catch (error) {
        return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
          target_path: workflowDocPathForRoot(this.root, "STATUS.md").relativePath,
          code: error instanceof VNextRuntimeError ? error.code : "RUNTIME_HANDLER_BLOCKED"
        });
      }
    }
    if (proposal.operation_kind === "lesson-record-transaction") {
      try {
        const plan = prepareLessonRecordTransaction(this.root, current, proposal);
        return this.commitLessonRecordTransaction(current, proposal, plan, options);
      } catch (error) {
        return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
          target_path: workflowDocPathForRoot(this.root, "LESSONS.md").relativePath,
          code: error instanceof VNextRuntimeError ? error.code : "RUNTIME_HANDLER_BLOCKED"
        });
      }
    }
    let transition;
    try {
      transition = proposal.operation_kind === "task-state-transaction" ? applyTaskStateDelta(this.root, current, proposal, now) : applyFindingQueueDelta(current, proposal, now);
    } catch (error) {
      return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), { code: error instanceof VNextRuntimeError ? error.code : "RUNTIME_HANDLER_BLOCKED" });
    }
    let taskBasisArtifact;
    let originalTaskBasisContent;
    try {
      if (transition.taskBasis) {
        const currentIdentity = extractTaskIdentityFromCurrentTask(current.body);
        const identity = transition.draftIdentity ?? {
          task_id: current.runtimeState.task_id,
          task_slug: current.runtimeState.task_slug,
          document_id: current.sourceTuple.document_id,
          task_title: currentIdentity.title
        };
        taskBasisArtifact = materializeTaskBasis(this.root, current, identity, transition.taskBasis);
        const existingReference = readTaskBasisReferenceFromBody(current.body);
        const basisExists = fs11.existsSync(taskBasisArtifact.filePath);
        if (proposal.semantic_delta.kind === "task-state" && proposal.semantic_delta.action === "create-draft") {
          if (basisExists) {
            const predecessor = proposal.semantic_delta.predecessor;
            const snapshotPath = predecessor ? safeRepositoryFile(this.root, successorSnapshotPath(current.relativePath, predecessor.document_id, predecessor.source_revision)) : null;
            const exactPreparedSuccessor = predecessor && snapshotPath && fs11.existsSync(snapshotPath) && fs11.lstatSync(snapshotPath).isFile() && fs11.readFileSync(snapshotPath, "utf8") === current.raw && fs11.lstatSync(taskBasisArtifact.filePath).isFile() && fs11.readFileSync(taskBasisArtifact.filePath, "utf8") === taskBasisArtifact.content;
            if (!exactPreparedSuccessor)
              fail3("TASK_BASIS_CONFLICT", `create-draft refuses to overwrite existing task basis ${taskBasisArtifact.path}.`);
          }
        } else if (existingReference) {
          if (existingReference.path !== taskBasisArtifact.path) {
            fail3("TASK_BASIS_REFERENCE_INVALID", "CURRENT_TASK links a different task basis path than the draft transaction target.");
          }
          if (!basisExists)
            fail3("TASK_BASIS_MISSING", `Linked task basis is missing: ${taskBasisArtifact.path}`);
          originalTaskBasisContent = fs11.readFileSync(taskBasisArtifact.filePath, "utf8");
          if (sha2565(originalTaskBasisContent) !== existingReference.revision) {
            fail3("TASK_BASIS_REVISION_CONFLICT", "Linked task basis changed outside the Runtime transaction.");
          }
          const priorBasis = readCanonicalTaskBasis(this.root, current).basis;
          if (digest3(priorBasis.original_request) !== digest3(taskBasisArtifact.basis.original_request) || priorBasis.user_decisions.length > taskBasisArtifact.basis.user_decisions.length || priorBasis.user_decisions.some((decision, index) => digest3(decision) !== digest3(taskBasisArtifact.basis.user_decisions[index]))) {
            fail3("TASK_BASIS_IMMUTABLE", "Task Basis original_request and existing user_decisions must remain unchanged; append a new user decision instead.");
          }
        } else if (basisExists) {
          fail3("TASK_BASIS_CONFLICT", `Unlinked task basis already exists at ${taskBasisArtifact.path}.`);
        }
      }
    } catch (error) {
      return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
        code: error instanceof VNextRuntimeError ? error.code : "TASK_BASIS_INVALID"
      });
    }
    const successorSnapshot = proposal.semantic_delta.kind === "task-state" && proposal.semantic_delta.action === "create-draft" && proposal.semantic_delta.predecessor ? { path: safeRepositoryFile(this.root, successorSnapshotPath(current.relativePath, current.sourceTuple.document_id, current.sourceTuple.revision)), content: current.raw } : null;
    if (successorSnapshot && fs11.existsSync(successorSnapshot.path) && fs11.readFileSync(successorSnapshot.path, "utf8") !== successorSnapshot.content) {
      return buildResult("blocked", proposal, current, options, "The retained predecessor snapshot conflicts with its exact source.", { code: "SUCCESSOR_HISTORY_CONFLICT" });
    }
    let nextContent;
    let prepared;
    try {
      if (current.runtimeState.task_evolution_version === 2 && proposal.semantic_delta.kind === "task-state") {
        const delta = proposal.semantic_delta;
        if (delta.action === "record-step-preflight" || delta.action === "step-progress" && delta.execution_result) {
          if ((current.runtimeState.artifact_checkpoint_ids?.length ?? 0) >= 256)
            fail3("ARTIFACT_BUDGET_EXHAUSTED", "Task checkpoint budget is exhausted.");
          const paths = delta.action === "record-step-preflight" ? delta.candidate_paths : delta.execution_result.review_target.entries.map((item) => item.path);
          const checkpointId = saveArtifactCheckpoint(this.root, current.filePath, {
            task_id: current.runtimeState.task_id,
            document_id: current.sourceTuple.document_id,
            step_id: delta.step_id,
            definition_revision: digest3(resolveCanonicalTaskStep(current).current),
            execution_id: proposal.idempotency_key,
            phase: delta.action === "record-step-preflight" ? "before" : "after"
          }, paths, options.dryRun === true);
          transition.next = { ...transition.next, artifact_checkpoint_ids: [...current.runtimeState.artifact_checkpoint_ids ?? [], checkpointId] };
        }
      }
      prepared = renderCanonicalCurrentTask(current.frontmatter, current.body, transition.next, {
        ...transition.replacementDefinition ? { replacementDefinition: transition.replacementDefinition } : {},
        ...transition.draftDefinition ? { draftDefinition: transition.draftDefinition } : {},
        ...transition.draftIdentity ? { draftIdentity: transition.draftIdentity } : {},
        ...transition.draftDocumentId ? { draftDocumentId: transition.draftDocumentId } : {},
        ...transition.draftDocumentId ? {
          compactTaskStore: true,
          taskStoreManifestPath: `${taskStorePaths(this.root, transition.draftDocumentId).relativeRoot}/manifest.json`
        } : {},
        ...taskBasisArtifact ? { taskBasisReference: { path: taskBasisArtifact.path, revision: taskBasisArtifact.revision } } : {},
        ...transition.audit ? { audit: transition.audit } : {}
      });
      nextContent = prepared.content;
    } catch (error) {
      return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), {
        code: error instanceof VNextRuntimeError ? error.code : "RUNTIME_RENDER_BLOCKED"
      });
    }
    const nextRevision = sha2565(nextContent);
    if (nextContent === current.raw) {
      return buildResult("no-op", proposal, current, options, "proposal produced no canonical state change.", {
        previous_revision: current.sourceTuple.revision,
        resulting_revision: current.sourceTuple.revision,
        read_back_verified: true
      });
    }
    if (options.dryRun) {
      return buildResult("success", proposal, current, options, "typed proposal validated; atomic write planned (dry-run).", {
        previous_revision: current.sourceTuple.revision,
        resulting_revision: nextRevision,
        state: resultState(transition.next, transition.findingStatus),
        ...transition.advancement ? { advancement: transition.advancement } : {}
      });
    }
    try {
      persistReviewPreimageWrites(this.root, transition.reviewPreimageWrites ?? []);
    } catch (error) {
      return buildResult("blocked", proposal, current, options, `review baseline persistence failed: ${error instanceof Error ? error.message : String(error)}`, {
        code: error instanceof ReviewPreimageStoreError ? error.code : "REVIEW_BASELINE_PERSISTENCE_FAILED"
      });
    }
    let stagedAfter;
    try {
      stagedAfter = this.stageCurrentTaskCommit(current, nextContent, transition.next, proposal, proposal.requested_write_targets, [
        { path: current.filePath, content: nextContent },
        ...taskBasisArtifact ? [{ path: taskBasisArtifact.filePath, content: taskBasisArtifact.content }] : [],
        ...successorSnapshot ? [successorSnapshot] : []
      ], prepared);
    } catch (error) {
      return buildResult("blocked", proposal, current, options, `task-store precommit staging failed: ${error instanceof Error ? error.message : String(error)}`, {
        code: error instanceof TaskStoreError ? error.code : "TASK_STORE_COMMIT_FAILED"
      });
    }
    try {
      if (successorSnapshot && taskBasisArtifact) {
        publishPreparedSuccessor(current.filePath, current.raw, nextContent, [
          successorSnapshot,
          { path: taskBasisArtifact.filePath, content: taskBasisArtifact.content }
        ], () => {
          TaskStore.forCurrent(this.root, stagedAfter).ensureInitialized(stagedAfter);
        });
      } else {
        executeWrites([
          { path: current.filePath, content: nextContent },
          ...taskBasisArtifact ? [{ path: taskBasisArtifact.filePath, content: taskBasisArtifact.content }] : []
        ], false, `vNext Runtime ${proposal.operation_kind} committed`);
      }
    } catch (error) {
      return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), { code: "ATOMIC_COMMIT_FAILED" });
    }
    try {
      if (stagedAfter.sourceTuple.document_id === current.sourceTuple.document_id) {
        TaskStore.forCurrent(this.root, current).markCurrentPublished(nextRevision);
      }
    } catch (error) {
      return buildResult("blocked", proposal, current, options, `task-store precommit publication failed after CURRENT_TASK write: ${error instanceof Error ? error.message : String(error)}`, {
        code: error instanceof TaskStoreError ? error.code : "TASK_STORE_COMMIT_FAILED"
      });
    }
    try {
      const readBack = stagedAfter.frontmatter.task_store === undefined ? this.readCurrentTask(this.root) : stagedAfter;
      if (readBack.raw !== nextContent || readBack.sourceTuple.revision !== nextRevision) {
        const rollback = taskBasisArtifact ? rollbackDraftTransactionAndVerify(this.root, current, taskBasisArtifact, originalTaskBasisContent, this.readCurrentTask) : rollbackCurrentTaskAndVerify(this.root, current, this.readCurrentTask);
        return buildResult("blocked", proposal, current, options, rollback.verified ? "Runtime read-back did not match the staged canonical document; rollback read-back verified." : `Runtime read-back did not match the staged canonical document; ${rollback.detail}`, { code: rollback.verified ? "READ_BACK_MISMATCH" : "ROLLBACK_FAILED" });
      }
      if (successorSnapshot && fs11.readFileSync(successorSnapshot.path, "utf8") !== current.raw)
        throw new Error("The predecessor snapshot did not retain the exact unfinished task.");
      if (taskBasisArtifact) {
        const basisReadBack = readCanonicalTaskBasis(this.root, readBack);
        if (basisReadBack.content !== taskBasisArtifact.content || basisReadBack.revision !== taskBasisArtifact.revision) {
          const rollback = rollbackDraftTransactionAndVerify(this.root, current, taskBasisArtifact, originalTaskBasisContent, this.readCurrentTask);
          return buildResult("blocked", proposal, current, options, rollback.verified ? "Runtime task basis read-back did not match the staged artifact; rollback read-back verified." : `Runtime task basis read-back did not match the staged artifact; ${rollback.detail}`, { code: rollback.verified ? "READ_BACK_MISMATCH" : "ROLLBACK_FAILED" });
        }
      }
      return buildResult("success", proposal, current, options, "typed proposal committed and canonical source read-back verified.", {
        committed: true,
        governed_mutation_count: successorSnapshot ? 3 : taskBasisArtifact ? 2 : 1,
        previous_revision: current.sourceTuple.revision,
        resulting_revision: nextRevision,
        read_back_verified: true,
        state: resultState(readBack.runtimeState, transition.findingStatus),
        ...transition.advancement ? { advancement: transition.advancement } : {}
      });
    } catch (error) {
      const rollback = taskBasisArtifact ? rollbackDraftTransactionAndVerify(this.root, current, taskBasisArtifact, originalTaskBasisContent, this.readCurrentTask) : rollbackCurrentTaskAndVerify(this.root, current, this.readCurrentTask);
      return buildResult("blocked", proposal, current, options, rollback.verified ? `Runtime read-back failed: ${error instanceof Error ? error.message : String(error)}; rollback read-back verified.` : `Runtime read-back failed: ${error instanceof Error ? error.message : String(error)}; ${rollback.detail}`, { code: rollback.verified ? "READ_BACK_FAILED" : "ROLLBACK_FAILED" });
    }
  }
}
function applyVNextRuntimeProposal(root2, proposal, options = {}) {
  return new GovernanceTransactionKernel(root2).apply(proposal, options);
}
function createTaskStateProposal(current, input) {
  const proposalEvidenceRefs = [...new Set([
    ...input.evidence_refs,
    ...claimEvidenceRefs(input.claim_evidence ?? [])
  ])];
  return validateRuntimeProposal({
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "task-state-transaction",
    caller: "execute-step",
    mode: input.mode,
    source_tuple: current.sourceTuple,
    authority_evidence: input.authority_evidence,
    semantic_delta: {
      kind: "task-state",
      action: "step-progress",
      step_id: current.runtimeState.active_step_id,
      status: input.status,
      evidence_refs: input.evidence_refs,
      ...input.note ? { note: input.note } : {},
      ...input.repair_fingerprint ? { repair_fingerprint: input.repair_fingerprint } : {},
      ...input.repair_fingerprints ? { repair_fingerprints: input.repair_fingerprints } : {},
      ...input.repair_wave_id ? { repair_wave_id: input.repair_wave_id } : {},
      ...input.change_set_id ? { change_set_id: input.change_set_id } : {},
      ...input.review_receipt ? { review_receipt: input.review_receipt } : {},
      ...input.claim_evidence === undefined ? {} : { claim_evidence: input.claim_evidence },
      ...input.execution_result === undefined ? {} : { execution_result: input.execution_result }
    },
    preconditions: ["current-task-is-active", "active-step-matches", "scope-admitted"],
    evidence_refs: proposalEvidenceRefs,
    idempotency_key: input.idempotency_key,
    requested_write_targets: [current.relativePath]
  });
}
function createRetainedReviewConsumptionProposal(current, input) {
  return validateRuntimeProposal({
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "task-state-transaction",
    caller: "execute-step",
    mode: "default",
    source_tuple: current.sourceTuple,
    authority_evidence: input.authority_evidence,
    semantic_delta: {
      kind: "task-state",
      action: "consume-retained-review",
      step_id: input.step_id,
      review_receipt: input.review_receipt,
      evidence_refs: input.evidence_refs
    },
    preconditions: ["current-task-is-active", "retained-scope-amendment-review", "fresh-review-receipt"],
    evidence_refs: input.evidence_refs,
    idempotency_key: input.idempotency_key,
    requested_write_targets: [current.relativePath]
  });
}
function assertOrdinaryPreflight(current, root2) {
  const unresolvedChallenges = (current.runtimeState.evidence_challenges ?? []).filter((item) => item.status !== "resolved");
  const correctionBatch = unresolvedChallenges.some((item) => item.status === "invalidated" && item.correction_step_id === current.runtimeState.active_step_id);
  if (!correctionBatch && unresolvedChallenges.some((item) => item.correction_step_id !== current.runtimeState.active_step_id)) {
    fail3("EVIDENCE_CHALLENGE_UNRESOLVED", "A challenged result may affect the next step; only its admitted correction step can run.");
  }
  for (const carry of current.runtimeState.evidence_carry_forward ?? []) {
    const slot = current.runtimeState.claim_evidence?.find((item) => item.claim_id === carry.claim_id)?.slots.find((item) => item.slot_id === carry.slot_id);
    if (!slot?.report)
      fail3("EVIDENCE_CARRY_FORWARD_STALE", "A carried slot no longer has its old report.");
    assertEvidenceReportApplicable(root2, current, slot);
  }
  const pending = current.runtimeState.pending_review_result;
  if (pending) {
    const execution = current.runtimeState.execution_log.map((item) => ("action" in item) ? item : cumulativeReviewExecution(current, item)).find((item) => !("action" in item) && item.idempotency_key === pending.execution_id);
    if (!execution || "action" in execution || !execution.execution_result || captureReviewTarget(root2, execution.execution_result.review_target.entries.map((entry) => entry.path)).revision !== pending.review_target_revision)
      fail3("REVIEW_TARGET_STALE", "Pending review target changed; preserve the review and resolve its conflict.");
    let route = pending.blocker?.next_route ?? "its recorded blocker route";
    if (pending.verdict === "clean")
      route = "complete-reviewed-step";
    if (pending.verdict === "findings")
      route = "begin-repair";
    fail3("PENDING_REVIEW_REQUIRED", `Pending review must be consumed by ${route ?? "its recorded blocker route"}; no ordinary execution is admitted.`);
  }
  if (current.runtimeState.findings.some((item) => ["admitted", "in-progress"].includes(item.status))) {
    fail3("REVIEW_CONVERGENCE_REQUIRED", "Open findings require admitted repair; ordinary execution cannot consume them.");
  }
  if (dynamicReviewRequiredForCurrentExecution(current) && current.runtimeState.active_step_status === "in-progress") {
    fail3("DYNAMIC_REVIEW_REQUIRED", "A self-admitted footprint expansion requires fresh cumulative review before another ordinary execution.");
  }
  if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active" || current.runtimeState.resume_requires_review || current.runtimeState.active_step_status === "blocked") {
    fail3("PREFLIGHT_BLOCKED", "ordinary execution requires an active unblocked task without a resume gate.");
  }
}
function createStepRetryProposal(current, input) {
  return validateRuntimeProposal({
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "task-state-transaction",
    caller: "execute-step",
    mode: "default",
    source_tuple: current.sourceTuple,
    authority_evidence: ["active-task-owner", "scope-admission", "evidence-admission"].map((kind) => ({ kind, source: current.relativePath, subject: current.runtimeState.active_step_id })),
    semantic_delta: { kind: "task-state", action: "retry-step", step_id: input.step_id, blocked_attempt_id: input.blocked_attempt_id, blocker_resolution_refs: input.blocker_resolution_refs, ...input.repair_diagnosis ? { repair_diagnosis: input.repair_diagnosis } : {}, evidence_refs: input.blocker_resolution_refs },
    preconditions: ["current-task-is-active", "active-step-matches", "scope-admitted"],
    evidence_refs: input.blocker_resolution_refs,
    idempotency_key: input.idempotency_key,
    requested_write_targets: [current.relativePath]
  });
}
function createStepPreflightProposal(current, candidatePaths, blastRadiusAssessments = [], input = {}) {
  const mode = input.mode ?? "default";
  const strategy = resolveTestStrategyExecutionContext(current);
  const executionPhase = input.execution_phase ?? executionPhaseForCurrentStep(current, strategy);
  const planRevision = input.plan_revision ?? digest3({ step_id: current.runtimeState.active_step_id, evidence_plan_revision: current.runtimeState.evidence_plan_revision });
  const executionId = input.execution_id ?? `execution-${digest3({
    task: current.sourceTuple.document_id,
    step: current.runtimeState.active_step_id,
    plan: planRevision,
    mode,
    attempt: mode === "default" ? nextStepAttemptId(current) : null,
    repair_fingerprints: input.repair_fingerprints ?? null,
    repair_wave_id: input.repair_wave_id ?? null,
    review_id: input.review_id ?? null
  })}`;
  const identity = current.mutationAuthority || mode === "repair" ? {
    mode,
    execution_id: executionId,
    plan_revision: planRevision,
    execution_phase: executionPhase,
    ...mode === "repair" ? {
      repair_fingerprints: input.repair_fingerprints ?? [],
      repair_wave_id: input.repair_wave_id ?? "",
      review_id: input.review_id ?? "",
      review_target_paths: input.review_target_paths ?? []
    } : {},
    ...input.change_set_id ? { change_set_id: input.change_set_id } : {}
  } : {};
  const preflightId = `preflight-${digest3({
    task: current.sourceTuple.document_id,
    plan: planRevision,
    step: current.runtimeState.active_step_id,
    mode,
    execution_id: executionId,
    paths: candidatePaths,
    assessments: blastRadiusAssessments,
    repair_fingerprints: input.repair_fingerprints ?? null,
    repair_wave_id: input.repair_wave_id ?? null,
    review_id: input.review_id ?? null
  })}`;
  return validateRuntimeProposal({
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "task-state-transaction",
    caller: "execute-step",
    mode,
    source_tuple: current.sourceTuple,
    authority_evidence: ["active-task-owner", "scope-admission", "evidence-admission"].map((kind) => ({ kind, source: current.relativePath, subject: current.runtimeState.active_step_id })),
    semantic_delta: { kind: "task-state", action: "record-step-preflight", step_id: current.runtimeState.active_step_id, candidate_paths: candidatePaths, evidence_refs: [current.relativePath], ...blastRadiusAssessments.length > 0 ? { blast_radius_assessments: blastRadiusAssessments } : {}, ...identity },
    preconditions: ["current-task-is-active", "active-step-matches", "scope-admitted", "execution-admission"],
    evidence_refs: [current.relativePath, ...blastRadiusAssessments.flatMap((item) => item.evidence_refs)],
    idempotency_key: preflightId,
    requested_write_targets: [current.relativePath]
  });
}
function createStepExtendPreflightProposal(current, input) {
  const mode = input.mode ?? "default";
  return validateRuntimeProposal({
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "task-state-transaction",
    caller: "execute-step",
    mode,
    source_tuple: current.sourceTuple,
    authority_evidence: ["active-task-owner", "scope-admission", "evidence-admission"].map((kind) => ({ kind, source: current.relativePath, subject: input.step_id })),
    semantic_delta: {
      kind: "task-state",
      action: "extend-preflight",
      step_id: input.step_id,
      current_preflight_id: input.current_preflight_id,
      additional_targets: input.additional_targets,
      blast_radius_assessments: input.blast_radius_assessments,
      evidence_refs: input.evidence_refs,
      ...input.mode === undefined ? {} : { mode: input.mode },
      ...input.execution_id === undefined ? {} : { execution_id: input.execution_id },
      ...input.execution_phase === undefined ? {} : { execution_phase: input.execution_phase }
    },
    preconditions: ["current-task-is-active", "active-step-matches", "same-attempt", "scope-admitted", "execution-admission"],
    evidence_refs: [...new Set([...input.evidence_refs, ...input.blast_radius_assessments.flatMap((item) => item.evidence_refs)])],
    idempotency_key: `preflight-extension-${digest3({ task: current.sourceTuple.document_id, plan: current.runtimeState.evidence_plan_revision, step: input.step_id, mode, execution_id: input.execution_id ?? null, current_preflight_id: input.current_preflight_id, targets: input.additional_targets, assessments: input.blast_radius_assessments })}`,
    requested_write_targets: [current.relativePath]
  });
}
function createReviewResultProposal(current, input) {
  const evidenceRefs = [...new Set([
    ...input.evidence_refs,
    ...input.review_result.evidence_refs,
    ...input.review_result.findings.flatMap((item) => item.evidence_refs)
  ])];
  return validateRuntimeProposal({
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "task-state-transaction",
    caller: "review-change",
    mode: "default",
    source_tuple: current.sourceTuple,
    authority_evidence: input.authority_evidence,
    semantic_delta: {
      kind: "task-state",
      action: "record-review-result",
      review_result: input.review_result,
      evidence_refs: evidenceRefs
    },
    preconditions: ["current-task-is-active", "latest-execution-matches", "review-context-current"],
    evidence_refs: evidenceRefs,
    idempotency_key: input.idempotency_key,
    requested_write_targets: [current.relativePath]
  });
}
function createEvidenceChallengeProposal(current, input) {
  return validateRuntimeProposal({
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "task-state-transaction",
    caller: "review-change",
    mode: "default",
    source_tuple: current.sourceTuple,
    authority_evidence: input.authority_evidence,
    semantic_delta: {
      kind: "task-state",
      action: "record-evidence-challenge",
      claim_id: input.claim_id,
      slot_id: input.slot_id,
      result_id: input.result_id,
      evidence_ref: input.evidence_ref,
      evidence_sha256: input.evidence_sha256,
      reason: input.reason,
      evidence_refs: [input.evidence_ref]
    },
    preconditions: ["current-task-is-active", "evidence-admitted"],
    evidence_refs: [input.evidence_ref],
    idempotency_key: input.idempotency_key,
    requested_write_targets: [current.relativePath]
  });
}
function createEvidenceChallengeDismissalProposal(current, input) {
  return validateRuntimeProposal({
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "task-state-transaction",
    caller: "review-change",
    mode: "default",
    source_tuple: current.sourceTuple,
    authority_evidence: input.authority_evidence,
    semantic_delta: {
      kind: "task-state",
      action: "dismiss-evidence-challenge",
      challenge_id: input.challenge_id,
      evidence_ref: input.evidence_ref,
      evidence_sha256: input.evidence_sha256,
      reason: input.reason,
      evidence_refs: [input.evidence_ref]
    },
    preconditions: ["current-task-is-active", "evidence-admitted"],
    evidence_refs: [input.evidence_ref],
    idempotency_key: input.idempotency_key,
    requested_write_targets: [current.relativePath]
  });
}
function recordUserEvidenceDecision(root2, kind, input, options = {}) {
  const value = expectRecord2(input, "user-evidence input");
  expectExactKeys2(value, ["claim_id", "slot_id", "check_id", "evidence_plan_revision", "subject_revision", "decision_source", "decision_text", ...value.validation_items === undefined ? [] : ["validation_items"]], "user-evidence input");
  const current = readCanonicalCurrentTask(root2);
  const basisPath = taskBasisRelativePath(current.relativePath, current.runtimeState.task_id);
  const delta = validateTaskStateDelta({ kind: "task-state", action: "record-user-evidence", decision_kind: kind, ...value, evidence_refs: [basisPath] });
  const key = `user-evidence-${digest3({ task: current.sourceTuple.document_id, delta }).slice(0, 40)}`;
  const proposal = {
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "task-state-transaction",
    caller: "prepare-task",
    mode: "default",
    source_tuple: current.sourceTuple,
    authority_evidence: ["active-task-owner", "evidence-admission", "user-confirmation"].map((kind) => ({
      kind,
      source: String(value.decision_source),
      subject: current.runtimeState.task_id
    })),
    semantic_delta: delta,
    preconditions: ["current-task-is-active", "exact-user-evidence-target"],
    evidence_refs: [basisPath],
    idempotency_key: key,
    requested_write_targets: [current.relativePath, basisPath]
  };
  const previousSlot = current.runtimeState.claim_evidence?.find((c) => c.claim_id === value.claim_id)?.slots.find((s) => s.slot_id === value.slot_id);
  if (previousSlot?.user_decision?.decision_id === key) {
    try {
      assertUserEvidenceApplicable(root2, current, previousSlot);
      return buildResult("no-op", proposal, current, options, "This exact user decision is already recorded; no evidence, status or budget changed.", { read_back_verified: true, resulting_revision: current.sourceTuple.revision });
    } catch (error) {
      return buildResult("blocked", proposal, current, options, error instanceof Error ? error.message : String(error), { code: error instanceof VNextRuntimeError ? error.code : "USER_EVIDENCE_STALE" });
    }
  }
  return applyVNextRuntimeProposal(root2, proposal, options);
}
function createPrepareTaskResumeReviewProposal(current, input) {
  return validateRuntimeProposal({
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "task-state-transaction",
    caller: "prepare-task",
    mode: input.mode,
    source_tuple: current.sourceTuple,
    authority_evidence: input.authority_evidence,
    semantic_delta: {
      kind: "task-state",
      action: "clear-resume-review-gate",
      evidence_refs: input.evidence_refs
    },
    preconditions: ["current-task-is-active", "resume-review-complete"],
    evidence_refs: input.evidence_refs,
    idempotency_key: input.idempotency_key,
    requested_write_targets: [current.relativePath]
  });
}
function createPrepareTaskDraftProposal(current, input) {
  const documentId = input.document_id ?? generatedDraftDocumentId(input, current.sourceTuple.revision);
  const proposalEvidenceRefs = [...new Set([
    ...input.evidence_refs,
    ...claimEvidenceRefs(input.claim_evidence ?? [])
  ])];
  return validateRuntimeProposal({
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "task-state-transaction",
    caller: "prepare-task",
    mode: "default",
    source_tuple: current.sourceTuple,
    authority_evidence: input.authority_evidence,
    semantic_delta: {
      kind: "task-state",
      action: input.action,
      ...input.predecessor ? { predecessor: input.predecessor } : {},
      task_id: input.task_id,
      task_slug: input.task_slug,
      document_id: documentId,
      task_title: input.task_title,
      task_basis: input.task_basis,
      draft_definition: input.draft_definition,
      active_step_id: input.active_step_id,
      evidence_refs: input.evidence_refs,
      ...input.claim_evidence === undefined ? {} : { claim_evidence: input.claim_evidence }
    },
    preconditions: input.action === "create-draft" ? ["current-task-is-closed-and-archived", "next-unused-task-identity", "closed-draft-definition"] : ["current-task-is-draft-and-active", "same-task-identity", "closed-draft-definition"],
    evidence_refs: proposalEvidenceRefs,
    idempotency_key: input.idempotency_key,
    requested_write_targets: [
      current.relativePath,
      taskBasisRelativePath(current.relativePath, input.task_id),
      ...input.predecessor ? [successorSnapshotPath(current.relativePath, current.sourceTuple.document_id, current.sourceTuple.revision)] : []
    ]
  });
}
function createPrepareTaskConfirmProposal(current, input) {
  return validateRuntimeProposal({
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "task-state-transaction",
    caller: "prepare-task",
    mode: "confirm",
    source_tuple: current.sourceTuple,
    authority_evidence: input.authority_evidence,
    semantic_delta: {
      kind: "task-state",
      action: "confirm-draft",
      task_id: input.task_id,
      task_slug: input.task_slug,
      document_id: input.document_id,
      draft_revision: input.draft_revision,
      evidence_refs: input.evidence_refs
    },
    preconditions: ["current-task-is-draft-and-active", "exact-draft-revision", "explicit-confirmation-authority", "no-unresolved-decisions"],
    evidence_refs: input.evidence_refs,
    idempotency_key: input.idempotency_key,
    requested_write_targets: [current.relativePath]
  });
}
function createPrepareTaskReplanProposal(current, input) {
  const proposalEvidenceRefs = [...new Set([
    ...input.evidence_refs,
    ..."claim_evidence" in input.delta && input.delta.claim_evidence !== undefined ? claimEvidenceRefs(input.delta.claim_evidence) : []
  ])];
  return validateRuntimeProposal({
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "task-state-transaction",
    caller: "prepare-task",
    mode: "replan",
    source_tuple: current.sourceTuple,
    authority_evidence: input.authority_evidence,
    semantic_delta: input.delta,
    preconditions: input.delta.action === "mark-replan-blocked" ? ["current-task-is-active", "replan-blocker-evidence-complete"] : input.delta.action === "clear-replan-block" ? ["blocked-by-replan", "new-authoritative-evidence"] : ["superseded-task", "closed-replacement-definition", "same-task-identity"],
    evidence_refs: proposalEvidenceRefs,
    idempotency_key: input.idempotency_key,
    requested_write_targets: input.delta.action === "commit-replan" ? [current.relativePath, taskBasisRelativePath(current.relativePath, current.runtimeState.task_id)] : [current.relativePath]
  });
}
function createPrepareTaskScopeAmendmentProposal(current, input) {
  const proposalEvidenceRefs = [...new Set([
    ...input.evidence_refs,
    ..."claim_evidence" in input.delta && input.delta.claim_evidence !== undefined ? claimEvidenceRefs(input.delta.claim_evidence) : []
  ])];
  return validateRuntimeProposal({
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "task-state-transaction",
    caller: "prepare-task",
    mode: "amend-scope",
    source_tuple: current.sourceTuple,
    authority_evidence: input.authority_evidence,
    semantic_delta: input.delta,
    preconditions: ["active-or-blocked-task", "authority-or-persistent-test-admission", "revision-bound-candidate", "preserve-review-baseline"],
    evidence_refs: proposalEvidenceRefs,
    idempotency_key: input.idempotency_key,
    requested_write_targets: [current.relativePath, taskBasisRelativePath(current.relativePath, current.runtimeState.task_id)]
  });
}
function createFindingQueueProposal(current, input) {
  return validateRuntimeProposal({
    schema_version: 1,
    kind: VNEXT_RUNTIME_PROPOSAL_KIND,
    operation_kind: "finding-queue-transaction",
    caller: "execute-step",
    mode: input.mode,
    source_tuple: current.sourceTuple,
    authority_evidence: input.authority_evidence,
    semantic_delta: input.delta,
    preconditions: ["current-task-is-active", "finding-admitted", "repair-budget-available"],
    evidence_refs: input.evidence_refs,
    idempotency_key: input.idempotency_key,
    requested_write_targets: [current.relativePath]
  });
}
function parseCli(argv) {
  const [command = "validate", ...rest] = argv;
  if (command !== "validate" && command !== "validate-contract" && command !== "apply" && command !== "scope-check")
    throw new Error("Usage: vnext-runtime <validate-contract|validate|apply|scope-check> --root <path> [--proposal-file <json>] [--path <repo-relative>] [--paths-file <path>] [--paths-stdin] [--persistent-test-path <repo-relative>] [--persistent-test-paths-file <path>] [--command-audit-file <json>] [--command-audit-stdin] [--conditional-authorizations-file <json>] [--transformation-kind <localized|inherently-broad>] [--summary] [--deep] [--dry-run]");
  let root2 = process.cwd();
  let proposalFile;
  let dryRun = false;
  const changedPaths = [];
  let pathsFile;
  let pathsStdin = false;
  const persistentTestPaths = [];
  let persistentTestPathsFile;
  let conditionalAuthorizationsFile;
  let transformationKind = "localized";
  let commandAuditFile;
  let commandAuditStdin = false;
  let summary = false;
  let deep = false;
  for (let index = 0;index < rest.length; index += 1) {
    const arg = rest[index];
    if (arg === "--root")
      root2 = rest[++index] ?? "";
    else if (arg === "--proposal" || arg === "--proposal-file")
      proposalFile = rest[++index];
    else if (arg === "--path")
      changedPaths.push(rest[++index] ?? "");
    else if (arg === "--paths-file")
      pathsFile = rest[++index];
    else if (arg === "--paths-stdin")
      pathsStdin = true;
    else if (arg === "--persistent-test-path")
      persistentTestPaths.push(rest[++index] ?? "");
    else if (arg === "--persistent-test-paths-file")
      persistentTestPathsFile = rest[++index];
    else if (arg === "--command-audit-file")
      commandAuditFile = rest[++index];
    else if (arg === "--command-audit-stdin")
      commandAuditStdin = true;
    else if (arg === "--summary" && command === "validate")
      summary = true;
    else if (arg === "--deep" && command === "validate")
      deep = true;
    else if (arg === "--conditional-authorizations-file")
      conditionalAuthorizationsFile = rest[++index];
    else if (arg === "--transformation-kind") {
      const value = rest[++index];
      if (value !== "localized" && value !== "inherently-broad")
        throw new Error("--transformation-kind must be localized or inherently-broad.");
      transformationKind = value;
    } else if (arg === "--dry-run")
      dryRun = true;
    else
      throw new Error(`Unknown argument: ${arg}`);
  }
  return { command, root: root2, proposalFile, dryRun, changedPaths, pathsFile, pathsStdin, persistentTestPaths, persistentTestPathsFile, conditionalAuthorizationsFile, transformationKind, commandAuditFile, commandAuditStdin, summary, deep };
}
function resolveExternalProposalFile(root2, proposalFile) {
  const resolvedRoot = path12.resolve(root2);
  const resolvedProposal = path12.resolve(proposalFile);
  const relative = path12.relative(resolvedRoot, resolvedProposal);
  const insideProject = relative === "" || !relative.startsWith(`..${path12.sep}`) && relative !== ".." && !path12.isAbsolute(relative);
  if (insideProject) {
    fail3("PROPOSAL_FILE_INSIDE_PROJECT", "proposal/helper files must not be created inside the target project; send the proposal on stdin or use an OS-temporary path outside the project.");
  }
  return resolvedProposal;
}
function readCliStringList(filePath, label) {
  const content = fs11.readFileSync(path12.resolve(filePath), "utf8");
  if (content.trimStart().startsWith("[")) {
    let parsed;
    try {
      parsed = JSON.parse(content);
    } catch (error) {
      throw new Error(`${label} must be valid JSON or newline-delimited text: ${error instanceof Error ? error.message : String(error)}`);
    }
    if (!Array.isArray(parsed) || parsed.some((item) => typeof item !== "string"))
      throw new Error(`${label} JSON form must be an array of strings.`);
    return parsed;
  }
  return content.split(/\r?\n/u).map((line) => line.trim()).filter(Boolean);
}
function readCliConditionalAuthorizations(filePath) {
  let parsed;
  try {
    parsed = JSON.parse(fs11.readFileSync(path12.resolve(filePath), "utf8"));
  } catch (error) {
    throw new Error(`conditional authorizations file must be valid JSON: ${error instanceof Error ? error.message : String(error)}`);
  }
  return parsed;
}
function readCliCommandAudit(args) {
  if (args.commandAuditFile && args.commandAuditStdin)
    throw new Error("--command-audit-file and --command-audit-stdin are mutually exclusive.");
  if (args.changedPaths.length > 0 || args.pathsFile || args.pathsStdin || args.persistentTestPaths.length > 0 || args.persistentTestPathsFile) {
    throw new Error("command audit input cannot be combined with ordinary path or persistent-test scope input.");
  }
  const content = args.commandAuditFile ? fs11.readFileSync(path12.resolve(args.commandAuditFile), "utf8") : (() => {
    if (process.stdin.isTTY)
      throw new Error("--command-audit-stdin requires a JSON command audit on stdin.");
    const input = process.stdin.read();
    if (typeof input !== "string" && !Buffer.isBuffer(input))
      throw new Error("--command-audit-stdin did not receive JSON.");
    return typeof input === "string" ? input : input.toString("utf8");
  })();
  try {
    return JSON.parse(content);
  } catch (error) {
    throw new Error(`command audit input must be valid JSON: ${error instanceof Error ? error.message : String(error)}`);
  }
}
function readScopeCheckInput(args) {
  const changedPaths = [...args.changedPaths];
  if (args.pathsFile)
    changedPaths.push(...readCliStringList(args.pathsFile, "--paths-file"));
  if (args.pathsStdin) {
    if (process.stdin.isTTY)
      throw new Error("--paths-stdin requires newline-delimited paths on stdin.");
    const stdinContent = process.stdin.read();
    if (typeof stdinContent !== "string" && !Buffer.isBuffer(stdinContent))
      throw new Error("--paths-stdin did not receive newline-delimited paths on stdin.");
    const text = typeof stdinContent === "string" ? stdinContent : stdinContent.toString("utf8");
    changedPaths.push(...text.split(/\r?\n/u).map((line) => line.trim()).filter(Boolean));
  }
  const persistentTestPaths = [...args.persistentTestPaths];
  if (args.persistentTestPathsFile) {
    persistentTestPaths.push(...readCliStringList(args.persistentTestPathsFile, "--persistent-test-paths-file"));
  }
  return {
    changed_paths: changedPaths,
    ...args.conditionalAuthorizationsFile ? { conditional_authorizations: readCliConditionalAuthorizations(args.conditionalAuthorizationsFile) } : {},
    ...persistentTestPaths.length > 0 ? { persistent_test_paths: persistentTestPaths } : {},
    transformation_kind: args.transformationKind
  };
}
function validateInstalledRuntimeForCli(root2) {
  const runtimePackagePath = path12.join(path12.resolve(root2), ...VNEXT_RUNTIME_PACKAGE_RELATIVE_PATH.split("/"), "package.json");
  if (fs11.existsSync(runtimePackagePath)) {
    validateVNextRuntimeContract(root2, true);
  }
}
function requireBootstrappedProject(root2) {
  const profilePath = getWorkflowProfilePath(root2);
  if (!fs11.existsSync(profilePath)) {
    fail3("BOOTSTRAP_REQUIRED", "Project governance is not bootstrapped. Invoke the `bootstrap-project` Agent Skill before using daily Runtime entries.");
  }
  let profile;
  try {
    profile = loadProfile(profilePath);
  } catch (error) {
    fail3("BOOTSTRAP_REQUIRED", "Project governance profile is unavailable or invalid; invoke the `bootstrap-project` Agent Skill before using daily Runtime entries.");
  }
  const currentTaskPath = getWorkflowDocPath(root2, profile, "CURRENT_TASK.md");
  if (!fs11.existsSync(currentTaskPath)) {
    fail3("BOOTSTRAP_REQUIRED", "Project governance is not bootstrapped. Invoke the `bootstrap-project` Agent Skill before using daily Runtime entries.");
  }
}
async function runCli(argv = process.argv.slice(2)) {
  try {
    validateRuntimeEnvironment();
    const args = parseCli(argv);
    if (args.command === "validate-contract") {
      validateInstalledRuntimeForCli(args.root);
      console.log(JSON.stringify(validateVNextRuntimeContract(args.root), null, 2));
    } else if (args.command === "validate") {
      validateInstalledRuntimeForCli(args.root);
      requireBootstrappedProject(args.root);
      const current = readCanonicalCurrentTask(args.root);
      const state = current.runtimeState;
      if (state.business_evidence_version === 1) {
        const actualPlanRevision = assertEvidencePlan(readDraftDefinitionFromBody(current.body), state.claim_evidence ?? []);
        if (state.evidence_plan_revision !== actualPlanRevision) {
          fail3("CLAIM_EVIDENCE_STALE", "canonical evidence plan revision does not match its definitions.");
        }
      }
      const sections = resolveReplanSectionRanges(current.body);
      const sectionText = (key) => {
        const section = sections[key];
        return section ? current.body.slice(section.contentStart, section.contentEnd).trim() : null;
      };
      const documentContext = { project_documents: readProjectDocuments(sectionText("background_context") ?? ""), affected_contracts: sectionText("affected_contracts") };
      const taskStore = TaskStore.forCurrent(args.root, current);
      const storageValidation = args.deep ? taskStore.deepValidate() : taskStore.validateCurrentAggregate(current);
      const storageInvalid = storageValidation.status === "invalid";
      const output = args.summary ? {
        status: storageInvalid ? "blocked" : "success",
        source_tuple: current.sourceTuple,
        package_version: VNEXT_RUNTIME_PACKAGE_VERSION,
        validation_scope: args.deep ? "aggregate-and-full-history" : "current-aggregate",
        storage_validation: storageValidation,
        storage_metrics: taskStorageMetrics(args.root, current),
        summary: {
          ...documentContext,
          task_id: state.task_id,
          workflow_status: state.workflow_status,
          lifecycle_state: state.lifecycle_state,
          active_step_id: state.active_step_id,
          active_step_status: state.active_step_status,
          pending_review_verdict: state.pending_review_result?.verdict ?? null,
          review_target_revision: state.review_coverage?.target.revision ?? null,
          pending_review_paths: state.review_coverage?.pending_paths ?? [],
          evidence_plan_revision: state.evidence_plan_revision ?? null,
          task_evolution_version: state.task_evolution_version ?? null,
          preservation_initialization_required: state.task_evolution_version !== 2,
          pending_replan_candidates: pendingCorrectionCandidates(args.root, current),
          carried_evidence_slots: (state.evidence_carry_forward ?? []).map((item) => ({ claim_id: item.claim_id, slot_id: item.slot_id, old_result_id: item.result_id })),
          unresolved_evidence_challenges: (state.evidence_challenges ?? []).filter((item) => item.status !== "resolved").map((item) => ({ challenge_id: item.challenge_id, claim_id: item.claim_id, slot_id: item.slot_id, result_id: item.result_id, correction_step_id: item.correction_step_id }))
        }
      } : { status: "success", source_tuple: current.sourceTuple, runtime_state: state, ...documentContext, ...args.deep ? { validation_scope: "aggregate-and-full-history", storage_validation: storageValidation } : {} };
      console.log(JSON.stringify(output, null, 2));
      if (storageInvalid)
        return 2;
    } else if (args.command === "scope-check") {
      validateInstalledRuntimeForCli(args.root);
      requireBootstrappedProject(args.root);
      const current = readCanonicalCurrentTask(args.root);
      const scope = parseMutationScope(current.body, current.sourceTuple.revision);
      const result = args.commandAuditFile || args.commandAuditStdin ? auditCommandMutation(scope, readCliCommandAudit(args)) : evaluateMutationScope(scope, readScopeCheckInput(args));
      console.log(JSON.stringify(result, null, 2));
      if (result.status === "blocked")
        return 2;
    } else {
      validateInstalledRuntimeForCli(args.root);
      requireBootstrappedProject(args.root);
      const proposalText = args.proposalFile ? fs11.readFileSync(resolveExternalProposalFile(args.root, args.proposalFile), "utf8") : !process.stdin.isTTY ? fs11.readFileSync(0, "utf8") : "";
      if (!proposalText.trim())
        throw new Error("apply requires a JSON proposal on stdin or via --proposal-file <json-file>.");
      const proposal = JSON.parse(proposalText);
      const result = applyVNextRuntimeProposal(args.root, proposal, { dryRun: args.dryRun });
      console.log(JSON.stringify(result, null, 2));
      if (result.status === "blocked" || result.status === "conflict")
        return 2;
    }
    return 0;
  } catch (error) {
    if (error instanceof MutationScopeError) {
      console.error(`${error.code}: ${error.message}`);
      return error.code === "MUTATION_SCOPE_BLOCKED" ? 2 : 1;
    }
    console.error(error instanceof Error ? error.message : String(error));
    return 1;
  }
}

// runtime/vnext/src/bootstrap-support.ts
import * as crypto13 from "crypto";
import * as fs14 from "fs";
import * as path15 from "path";
import { parse as parse6, parseDocument as parseDocument3, stringify as stringify5 } from "yaml";

// runtime/vnext/src/scoped-tree-hash.ts
import * as crypto11 from "crypto";
import * as fs12 from "fs";
import * as path13 from "path";

class ScopedTreeHashError extends Error {
  code = "UNSAFE_PATH";
  constructor(message) {
    super(message);
    this.name = "ScopedTreeHashError";
  }
}
function sha2566(value) {
  return crypto11.createHash("sha256").update(value).digest("hex");
}
function normalizeRepoPath3(value, location) {
  const normalized = value.trim().replace(/\\/gu, "/").replace(/^\.\//u, "").replace(/\/+/gu, "/");
  if (!normalized || normalized.startsWith("/") || /^[A-Za-z]:\//u.test(normalized) || normalized.split("/").some((segment) => segment === ".." || segment.length === 0) || /[\0-\x1F\x7F]/u.test(normalized) || normalized.includes("*")) {
    throw new ScopedTreeHashError(`${location} must be a repository-relative concrete path: ${value}`);
  }
  return normalized;
}
function resolveRepoPath(root2, relativePath, location) {
  const normalized = normalizeRepoPath3(relativePath, location);
  const resolvedRoot = path13.resolve(root2);
  const resolved = path13.resolve(resolvedRoot, ...normalized.split("/"));
  const prefix = resolvedRoot.endsWith(path13.sep) ? resolvedRoot : `${resolvedRoot}${path13.sep}`;
  if (resolved !== resolvedRoot && !resolved.startsWith(prefix)) {
    throw new ScopedTreeHashError(`${location} escapes the target root: ${relativePath}`);
  }
  return resolved;
}
function computeScopedTreeHash(root2, includedRelativePaths, ignoredRelativePaths = []) {
  const resolvedRoot = path13.resolve(root2);
  const included = [...new Set(includedRelativePaths.map((relativePath) => normalizeRepoPath3(relativePath, "scoped tree hash included path")))].sort((left, right) => left.localeCompare(right));
  const ignored = new Set(ignoredRelativePaths.map((relativePath) => normalizeRepoPath3(relativePath, "scoped tree hash ignored path")));
  const visited = new Set;
  const hash = crypto11.createHash("sha256");
  const isMissingPathError = (error) => {
    if (!error || typeof error !== "object" || !("code" in error))
      return false;
    return error.code === "ENOENT";
  };
  const lstatOrMissing = (fullPath) => {
    try {
      return fs12.lstatSync(fullPath);
    } catch (error) {
      if (isMissingPathError(error))
        return null;
      throw error;
    }
  };
  const record = (relativePath, kind, value = "") => {
    hash.update(relativePath);
    hash.update("\x00");
    hash.update(kind);
    hash.update("\x00");
    hash.update(value);
    hash.update(`
`);
  };
  const assertNoSymlinkParent = (relativePath) => {
    const parts = relativePath.split("/");
    let current = resolvedRoot;
    for (let index = 0;index < parts.length - 1; index += 1) {
      current = path13.join(current, parts[index]);
      const status = lstatOrMissing(current);
      if (!status)
        return;
      if (status.isSymbolicLink()) {
        throw new ScopedTreeHashError(`scoped tree hash cannot traverse a symbolic-link parent: ${relativePath}`);
      }
      if (!status.isDirectory())
        return;
    }
  };
  const visit = (relativePath, fullPath) => {
    if (ignored.has(relativePath) || visited.has(relativePath))
      return;
    const status = lstatOrMissing(fullPath);
    if (!status) {
      visited.add(relativePath);
      record(relativePath, "missing");
      return;
    }
    visited.add(relativePath);
    if (status.isSymbolicLink()) {
      record(relativePath, "symlink", fs12.readlinkSync(fullPath));
      return;
    }
    if (status.isDirectory()) {
      record(relativePath, "directory");
      for (const name of fs12.readdirSync(fullPath).sort((left, right) => left.localeCompare(right))) {
        visit(`${relativePath}/${name}`, path13.join(fullPath, name));
      }
      return;
    }
    if (status.isFile()) {
      const content = fs12.readFileSync(fullPath);
      record(relativePath, "file", `${content.byteLength}\x00${sha2566(content)}`);
      return;
    }
    record(relativePath, "special", String(status.mode));
  };
  for (const relativePath of included) {
    assertNoSymlinkParent(relativePath);
    visit(relativePath, resolveRepoPath(resolvedRoot, relativePath, "scoped tree hash included path"));
  }
  return hash.digest("hex");
}

// runtime/vnext/src/migration-provenance.ts
import * as crypto12 from "crypto";

// runtime/vnext/src/migration-preservation.ts
function object(value, keys) {
  if (!value || typeof value !== "object" || Array.isArray(value))
    throw new Error("MIGRATION_DECISIONS_INVALID: expected object");
  const result = value;
  if (Object.keys(result).sort().join("|") !== keys.sort().join("|"))
    throw new Error("MIGRATION_DECISIONS_INVALID: unexpected or missing fields");
  return result;
}
function text2(value) {
  if (typeof value !== "string" || !value.trim() || /[\r\n\0]/.test(value))
    throw new Error("MIGRATION_DECISIONS_INVALID: expected non-empty single-line text");
  return value;
}
function file(value, current) {
  const data = object(value, current ? ["path", "sha256", "original_task_id"] : ["path", "sha256"]);
  const p = text2(data.path);
  if (p.includes("\\") || p.startsWith("/") || p.includes(":") || p.split("/").some((s) => !s || s === "." || s === ".."))
    throw new Error("MIGRATION_DECISIONS_INVALID: unsafe path");
  if (current ? p !== "docs/workflow/CURRENT_TASK.md" : !p.startsWith("TASKS/paused/"))
    throw new Error("MIGRATION_DECISIONS_INVALID: path outside preservation scope");
  const hash = text2(data.sha256);
  if (!/^[a-f0-9]{64}$/.test(hash))
    throw new Error("MIGRATION_DECISIONS_INVALID: invalid SHA-256");
  return { path: p, sha256: hash, ...current ? { original_task_id: text2(data.original_task_id) } : {} };
}
function validateMigrationDecisions(value) {
  const data = object(value, ["schema_version", "target_root", "target_identity", "current_task", "current_task_disposition", "preserved_paused", "user_decision"]);
  if (data.schema_version !== 1 || data.current_task_disposition !== "completed-by-user-confirmation")
    throw new Error("MIGRATION_DECISIONS_INVALID: unsupported decision");
  const targetIdentity = text2(data.target_identity);
  if (!/^[a-f0-9]{32}$/.test(targetIdentity))
    throw new Error("MIGRATION_DECISIONS_INVALID: invalid target identity");
  if (!Array.isArray(data.preserved_paused))
    throw new Error("MIGRATION_DECISIONS_INVALID: paused files must be a list");
  const paused = data.preserved_paused.map((item) => file(item, false));
  if (new Set(paused.map((item) => item.path)).size !== paused.length)
    throw new Error("MIGRATION_DECISIONS_INVALID: duplicate paused path");
  const decision = object(data.user_decision, ["source", "verbatim"]);
  if (typeof decision.verbatim !== "string" || !decision.verbatim.trim())
    throw new Error("MIGRATION_DECISIONS_INVALID: missing user decision");
  return {
    schema_version: 1,
    target_root: text2(data.target_root),
    target_identity: targetIdentity,
    current_task: file(data.current_task, true),
    current_task_disposition: "completed-by-user-confirmation",
    preserved_paused: paused,
    user_decision: { source: text2(decision.source), verbatim: decision.verbatim }
  };
}
function legacyCurrentTaskBackup(decisions) {
  return `TASKS/legacy/CURRENT_TASK-${decisions.current_task.sha256}.md`;
}
function validateMigrationPreservation(value, targetIdentity) {
  const data = object(value, ["decisions", "current_task_backup", "paused_disposition", "assurance"]);
  const decisions = validateMigrationDecisions(data.decisions);
  if (decisions.target_identity !== targetIdentity || data.current_task_backup !== legacyCurrentTaskBackup(decisions) || data.paused_disposition !== "verbatim-unconverted-not-resumable" || data.assurance !== "caller-reported") {
    throw new Error("MIGRATION_DECISIONS_INVALID: preservation receipt mismatch");
  }
  return decisions;
}
function validateMigrationAlignment(value) {
  if (!Array.isArray(value))
    throw new Error("MIGRATION_ALIGNMENT_INVALID: expected original backup list");
  const allowed = ["AGENTS.md", "CLAUDE.md", "package.json", ".workflow-system/PROJECT_PROFILE.yaml", "docs/workflow/WORKFLOW_GUIDE.md", "docs/workflow/DOCUMENT_CATALOG.md", "docs/workflow/STATUS.md"];
  const result = value.map((item) => {
    const data = object(item, ["path", "sha256", "backup_path"]);
    const p = text2(data.path), hash = text2(data.sha256), backup = text2(data.backup_path);
    if (!allowed.includes(p) || !/^[a-f0-9]{64}$/.test(hash) || backup !== `.workflow-system/legacy/${hash}/${p}`)
      throw new Error("MIGRATION_ALIGNMENT_INVALID: backup binding mismatch");
    return { path: p, sha256: hash, backup_path: backup };
  });
  if (new Set(result.map((x) => x.path)).size !== result.length)
    throw new Error("MIGRATION_ALIGNMENT_INVALID: duplicate original");
  return result;
}

// runtime/vnext/src/migration-provenance.ts
import * as fs13 from "fs";
import * as path14 from "path";
import { parseDocument as parseDocument2 } from "yaml";
var COMPLETED_MIGRATION_INSTALL_STATE_RELATIVE_PATH = ".workflow-system/vnext/INSTALL_STATE.json";
var COMPLETED_MIGRATION_RECEIPT_RELATIVE_PATH = ".workflow-system/vnext/MIGRATION_RECEIPT.json";
var SAFE_ID_PATTERN = /^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$/u;
var SHA256_PATTERN3 = /^[a-f0-9]{64}$/u;
var TARGET_IDENTITY_PATTERN2 = /^[a-f0-9]{32}$/u;
var STABLE_ID_PATTERN = /^artifact-[a-f0-9]{24}$/u;
var CANONICAL_DOCUMENT_ID_PATTERN = /^doc-[a-f0-9]{24}$/u;
var CANONICAL_SCHEMA_VERSION = 1;
var CANONICAL_DOCUMENT_KIND = "vnext-canonical-document";
var CANONICAL_CONVERSION_RULE = "canonical-envelope-v1";
var CURRENT_TASK_FILE = "CURRENT_TASK.md";

class MigrationProvenanceError extends Error {
  code;
  constructor(message) {
    super(`COMPLETED_MIGRATION_INVALID: ${message}`);
    this.code = "COMPLETED_MIGRATION_INVALID";
    this.name = "MigrationProvenanceError";
  }
}
function fail4(message) {
  throw new MigrationProvenanceError(message);
}
function isRecord5(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function expectRecord3(value, location) {
  if (!isRecord5(value))
    fail4(`${location} must be a mapping.`);
  return value;
}
function expectString3(value, location) {
  if (typeof value !== "string" || value.trim().length === 0)
    fail4(`${location} must be a non-empty string.`);
  return value.trim();
}
function expectStringArray3(value, location, allowEmpty = false) {
  if (!Array.isArray(value) || !allowEmpty && value.length === 0)
    fail4(`${location} must be ${allowEmpty ? "an array" : "a non-empty array"}.`);
  const values = value.map((item, index) => expectString3(item, `${location}[${index}]`));
  if (new Set(values).size !== values.length)
    fail4(`${location} must not contain duplicates.`);
  return values;
}
function expectExactKeys3(value, expected, location) {
  const expectedSet = new Set(expected);
  const missing = expected.filter((key) => !(key in value));
  const extra = Object.keys(value).filter((key) => !expectedSet.has(key));
  if (missing.length > 0 || extra.length > 0)
    fail4(`${location} keys mismatch; missing=[${missing.join(", ")}], unexpected=[${extra.join(", ")}].`);
}
function sha2567(value) {
  return crypto12.createHash("sha256").update(value).digest("hex");
}
function normalizeRoot(root2) {
  const resolved = path14.resolve(root2);
  const normalized = resolved.replace(/\\/gu, "/");
  const parsedRoot = path14.parse(resolved).root.replace(/\\/gu, "/");
  const rootValue = process.platform === "win32" ? parsedRoot.toLowerCase() : parsedRoot;
  const normalizedValue = process.platform === "win32" ? normalized.toLowerCase() : normalized;
  return normalizedValue === rootValue ? rootValue : normalizedValue.replace(/\/+$/u, "");
}
function normalizeRepoPath4(value, location) {
  const normalized = value.replace(/\\/gu, "/").trim().replace(/^\.\//u, "");
  if (!normalized || normalized.startsWith("/") || /^[A-Za-z]:\//u.test(normalized) || normalized.split("/").some((segment) => segment === ".." || segment.length === 0) || /[\0-\x1F\x7F]/u.test(normalized)) {
    fail4(`${location} is not a safe repository-relative path: ${value}`);
  }
  return normalized;
}
function resolveRepoPath2(root2, relative, location) {
  const normalized = normalizeRepoPath4(relative, location);
  const resolvedRoot = path14.resolve(root2);
  const resolved = path14.resolve(resolvedRoot, ...normalized.split("/"));
  const prefix = resolvedRoot.endsWith(path14.sep) ? resolvedRoot : `${resolvedRoot}${path14.sep}`;
  if (resolved !== resolvedRoot && !resolved.startsWith(prefix))
    fail4(`${location} escapes the target root: ${relative}`);
  return resolved;
}
function readJson2(filePath, location) {
  try {
    return expectRecord3(JSON.parse(fs13.readFileSync(filePath, "utf8")), location);
  } catch (error) {
    if (error instanceof MigrationProvenanceError)
      throw error;
    fail4(`${location} is not valid JSON: ${error instanceof Error ? error.message : String(error)}`);
  }
}
function readYaml(filePath, location) {
  try {
    const document = parseDocument2(fs13.readFileSync(filePath, "utf8"), { uniqueKeys: true });
    const diagnostics = [...document.errors, ...document.warnings];
    if (diagnostics.length > 0)
      fail4(`${location} is invalid YAML: ${diagnostics.map((item) => item.message).join("; ")}`);
    return expectRecord3(document.toJS(), location);
  } catch (error) {
    if (error instanceof MigrationProvenanceError)
      throw error;
    fail4(`${location} is invalid YAML: ${error instanceof Error ? error.message : String(error)}`);
  }
}
function runtimeIdentity(value, location) {
  if (value === null)
    return null;
  const record = expectRecord3(value, location);
  expectExactKeys3(record, ["kind", "package_path", "entrypoint", "package_version", "node_min_version", "package_lock_sha256", "entrypoint_sha256"], location);
  const packageVersion = expectString3(record.package_version, `${location}.package_version`);
  const nodeMinVersion = expectString3(record.node_min_version, `${location}.node_min_version`);
  const packageLock = expectString3(record.package_lock_sha256, `${location}.package_lock_sha256`);
  const entrypoint = expectString3(record.entrypoint_sha256, `${location}.entrypoint_sha256`);
  if (!/^\d+\.\d+\.\d+(?:[-+][0-9A-Za-z.-]+)?$/u.test(packageVersion) || !/^>=\d+\.\d+\.\d+$/u.test(nodeMinVersion) || !SHA256_PATTERN3.test(packageLock) || !SHA256_PATTERN3.test(entrypoint)) {
    fail4(`${location} has invalid Runtime identity fields.`);
  }
  if (record.kind !== "project-local-node" || record.package_path !== VNEXT_RUNTIME_PACKAGE_RELATIVE_PATH || record.entrypoint !== VNEXT_RUNTIME_ENTRYPOINT_RELATIVE_PATH) {
    fail4(`${location} does not declare the canonical project-local Runtime shape.`);
  }
  return {
    kind: "project-local-node",
    package_path: VNEXT_RUNTIME_PACKAGE_RELATIVE_PATH,
    entrypoint: VNEXT_RUNTIME_ENTRYPOINT_RELATIVE_PATH,
    package_version: packageVersion,
    node_min_version: nodeMinVersion,
    package_lock_sha256: packageLock,
    entrypoint_sha256: entrypoint
  };
}
function sameRuntime(left, right) {
  if (left === null || right === null)
    return left === right;
  return left.kind === right.kind && left.package_path === right.package_path && left.entrypoint === right.entrypoint && left.package_version === right.package_version && left.node_min_version === right.node_min_version && left.package_lock_sha256 === right.package_lock_sha256 && left.entrypoint_sha256 === right.entrypoint_sha256;
}
function validateInstallState(value) {
  const legacyShape = "mode" in value && !("distribution_state" in value);
  expectExactKeys3(value, legacyShape ? ["schema_version", "kind", "mode", "migration_pack_id", "bundle_id", "source_revision", "source_tree_hash", "target_identity", "runtime_distribution", "installed_at", "managed_files", "removed_legacy_files", "legacy_compatibility", "recovery_boundary"] : ["schema_version", "kind", "distribution_state", "distribution_version", "migration_pack_id", "bundle_id", "source_revision", "source_tree_hash", "target_identity", "runtime_distribution", "installed_at", "managed_files", "removed_legacy_files", "legacy_compatibility", "recovery_boundary"], "INSTALL_STATE.json");
  if (value.schema_version !== 1 || value.kind !== "vnext-install-state" || (legacyShape ? value.mode !== "pure-vnext" : value.distribution_state !== "vnext") || value.legacy_compatibility !== "absent" || value.recovery_boundary !== "in-progress-marker")
    fail4("INSTALL_STATE.json is not a valid vNext install state.");
  const migrationPackId = expectString3(value.migration_pack_id, "INSTALL_STATE.json.migration_pack_id");
  const bundleId = expectString3(value.bundle_id, "INSTALL_STATE.json.bundle_id");
  if (!/^migration-[a-f0-9]{24}$/u.test(migrationPackId) || !/^bundle-[a-f0-9]{24}$/u.test(bundleId))
    fail4("INSTALL_STATE.json has invalid migration or bundle identity.");
  const sourceRevision = expectString3(value.source_revision, "INSTALL_STATE.json.source_revision");
  const sourceTreeHash = expectString3(value.source_tree_hash, "INSTALL_STATE.json.source_tree_hash");
  const targetIdentity = expectString3(value.target_identity, "INSTALL_STATE.json.target_identity");
  if (!SHA256_PATTERN3.test(sourceTreeHash) || !TARGET_IDENTITY_PATTERN2.test(targetIdentity))
    fail4("INSTALL_STATE.json has invalid source/target identity fields.");
  const installedAt = expectString3(value.installed_at, "INSTALL_STATE.json.installed_at");
  const runtime = runtimeIdentity(value.runtime_distribution, "INSTALL_STATE.json.runtime_distribution");
  if (!Array.isArray(value.managed_files) || value.managed_files.length === 0)
    fail4("INSTALL_STATE.json.managed_files must be non-empty.");
  const seen = new Set;
  const managedFiles = value.managed_files.map((raw, index) => {
    const item = expectRecord3(raw, `INSTALL_STATE.json.managed_files[${index}]`);
    expectExactKeys3(item, ["path", "checksum", "category"], `INSTALL_STATE.json.managed_files[${index}]`);
    const relative = normalizeRepoPath4(expectString3(item.path, `INSTALL_STATE.json.managed_files[${index}].path`), `INSTALL_STATE.json.managed_files[${index}].path`);
    if (seen.has(relative))
      fail4(`INSTALL_STATE.json.managed_files contains duplicate path ${relative}.`);
    seen.add(relative);
    const checksum = item.checksum === "" ? "" : expectString3(item.checksum, `INSTALL_STATE.json.managed_files[${index}].checksum`);
    if (!SHA256_PATTERN3.test(checksum) && !(relative === COMPLETED_MIGRATION_INSTALL_STATE_RELATIVE_PATH && checksum === ""))
      fail4(`INSTALL_STATE.json.managed_files[${index}].checksum is invalid.`);
    return { path: relative, checksum, category: expectString3(item.category, `INSTALL_STATE.json.managed_files[${index}].category`) };
  });
  const removed = expectStringArray3(value.removed_legacy_files, "INSTALL_STATE.json.removed_legacy_files", true).map((item, index) => normalizeRepoPath4(item, `INSTALL_STATE.json.removed_legacy_files[${index}]`));
  return { migration_pack_id: migrationPackId, bundle_id: bundleId, source_revision: sourceRevision, source_tree_hash: sourceTreeHash, target_identity: targetIdentity, runtime_distribution: runtime, installed_at: installedAt, managed_files: managedFiles, removed_legacy_files: removed };
}
function validateReceipt(value) {
  expectExactKeys3(value, ["schema_version", "kind", "migration_pack_id", "bundle_id", "source_revision", "source_tree_hash", "target_identity", "runtime_distribution", "installed_at", "converted_artifact_ids", "legacy_compatibility", ...value.schema_version === 2 || value.schema_version === 3 && value.preservation !== undefined ? ["preservation"] : [], ...value.schema_version === 3 ? ["aligned_originals"] : []], "MIGRATION_RECEIPT.json");
  if (value.schema_version === 3)
    validateMigrationAlignment(value.aligned_originals);
  if (value.schema_version === 2 || value.schema_version === 3 && value.preservation !== undefined)
    validateMigrationPreservation(value.preservation, value.target_identity);
  if (![1, 2, 3].includes(value.schema_version) || value.kind !== "vnext-migration-receipt" || value.legacy_compatibility !== "absent")
    fail4("MIGRATION_RECEIPT.json is not a pure vNext migration receipt.");
  const migrationPackId = expectString3(value.migration_pack_id, "MIGRATION_RECEIPT.json.migration_pack_id");
  const bundleId = expectString3(value.bundle_id, "MIGRATION_RECEIPT.json.bundle_id");
  const sourceRevision = expectString3(value.source_revision, "MIGRATION_RECEIPT.json.source_revision");
  const sourceTreeHash = expectString3(value.source_tree_hash, "MIGRATION_RECEIPT.json.source_tree_hash");
  const targetIdentity = expectString3(value.target_identity, "MIGRATION_RECEIPT.json.target_identity");
  if (!/^migration-[a-f0-9]{24}$/u.test(migrationPackId) || !/^bundle-[a-f0-9]{24}$/u.test(bundleId) || !SHA256_PATTERN3.test(sourceTreeHash) || !TARGET_IDENTITY_PATTERN2.test(targetIdentity))
    fail4("MIGRATION_RECEIPT.json has invalid identity fields.");
  const ids = expectStringArray3(value.converted_artifact_ids, "MIGRATION_RECEIPT.json.converted_artifact_ids", true);
  if (ids.some((item) => !STABLE_ID_PATTERN.test(item)))
    fail4("MIGRATION_RECEIPT.json.converted_artifact_ids contains an invalid artifact ID.");
  return { migration_pack_id: migrationPackId, bundle_id: bundleId, source_revision: sourceRevision, source_tree_hash: sourceTreeHash, target_identity: targetIdentity, runtime_distribution: runtimeIdentity(value.runtime_distribution, "MIGRATION_RECEIPT.json.runtime_distribution"), installed_at: expectString3(value.installed_at, "MIGRATION_RECEIPT.json.installed_at"), converted_artifact_ids: ids };
}
function canonicalDocumentId(kind, sourcePath, sourceSha) {
  return `doc-${sha2567(`${kind}\x00${sourcePath}\x00${sourceSha}`).slice(0, 24)}`;
}
function validConversion(record) {
  if (record.conversion_rule === CANONICAL_CONVERSION_RULE)
    return record.original_text_preserved === true;
  if (record.conversion_rule !== "canonical-verbatim-v2")
    return false;
  if (record.original_text_preserved === true)
    return record.original_backup_path === undefined;
  if (record.original_text_preserved !== false)
    return false;
  validateMigrationAlignment([{ path: record.source_path, sha256: record.source_sha256, backup_path: record.original_backup_path }]);
  return true;
}
function canonicalArtifactIdentity(relativePath, content) {
  let kind;
  let sourcePath;
  let sourceSha;
  let sourceRevision;
  let sourceTreeHash;
  if (relativePath === ".workflow-system/PROJECT_PROFILE.yaml") {
    let document;
    try {
      const parsed = parseDocument2(content, { uniqueKeys: true });
      const diagnostics = [...parsed.errors, ...parsed.warnings];
      if (diagnostics.length > 0)
        fail4(`migrated project profile has invalid YAML: ${diagnostics.map((item) => item.message).join("; ")}`);
      document = expectRecord3(parsed.toJS(), "migrated project profile");
    } catch (error) {
      if (error instanceof MigrationProvenanceError)
        throw error;
      fail4(`migrated project profile is invalid: ${error instanceof Error ? error.message : String(error)}`);
    }
    const metadata = expectRecord3(document.vnext_migration, "migrated project profile.vnext_migration");
    expectExactKeys3(metadata, ["schema_version", "kind", "document_id", "source_path", "source_sha256", "legacy_source_revision", "legacy_source_tree_hash", "legacy_protocol_version", "conversion_rule", "original_text_preserved", "path_references", ...metadata.conversion_rule === "canonical-verbatim-v2" && metadata.original_text_preserved === false ? ["original_backup_path"] : []], "migrated project profile.vnext_migration");
    if (metadata.schema_version !== CANONICAL_SCHEMA_VERSION || metadata.kind !== "vnext-canonical-profile" || !validConversion(metadata))
      fail4("migrated project profile does not carry the canonical Migration Pack provenance envelope.");
    kind = "project-profile";
    sourcePath = normalizeRepoPath4(expectString3(metadata.source_path, "migrated project profile source_path"), "migrated project profile source_path");
    sourceSha = expectString3(metadata.source_sha256, "migrated project profile source_sha256");
    sourceRevision = expectString3(metadata.legacy_source_revision, "migrated project profile legacy_source_revision");
    sourceTreeHash = expectString3(metadata.legacy_source_tree_hash, "migrated project profile legacy_source_tree_hash");
    if (metadata.document_id !== canonicalDocumentId(kind, sourcePath, sourceSha))
      fail4("migrated project profile document_id is not bound to its source identity.");
  } else {
    const match = /^---\r?\n([\s\S]*?)\r?\n---\r?\n([\s\S]*)$/u.exec(content);
    if (!match)
      fail4(`migrated artifact ${relativePath} is missing the canonical frontmatter envelope.`);
    const parsed = parseDocument2(match[1], { uniqueKeys: true });
    const diagnostics = [...parsed.errors, ...parsed.warnings];
    if (diagnostics.length > 0)
      fail4(`migrated artifact ${relativePath} has invalid canonical frontmatter: ${diagnostics.map((item) => item.message).join("; ")}`);
    const header = expectRecord3(parsed.toJS(), `migrated artifact ${relativePath} frontmatter`);
    expectExactKeys3(header, ["schema_version", "kind", "document_kind", "document_id", "source_path", "source_sha256", "legacy_source_revision", "legacy_source_tree_hash", "legacy_protocol_version", "conversion_rule", "original_text_preserved", "heading_index", "path_references", ...header.conversion_rule === "canonical-verbatim-v2" && header.original_text_preserved === false ? ["original_backup_path"] : []], `migrated artifact ${relativePath} frontmatter`);
    if (header.schema_version !== CANONICAL_SCHEMA_VERSION || header.kind !== CANONICAL_DOCUMENT_KIND || !validConversion(header))
      fail4(`migrated artifact ${relativePath} does not carry the canonical Migration Pack provenance envelope.`);
    const documentKind = expectString3(header.document_kind, `migrated artifact ${relativePath}.document_kind`);
    if (!["governance-document", "task-archive", "target-owned-preserved"].includes(documentKind))
      fail4(`migrated artifact ${relativePath} has an unsupported document kind.`);
    kind = documentKind;
    sourcePath = normalizeRepoPath4(expectString3(header.source_path, `migrated artifact ${relativePath}.source_path`), `migrated artifact ${relativePath}.source_path`);
    sourceSha = expectString3(header.source_sha256, `migrated artifact ${relativePath}.source_sha256`);
    sourceRevision = expectString3(header.legacy_source_revision, `migrated artifact ${relativePath}.legacy_source_revision`);
    sourceTreeHash = expectString3(header.legacy_source_tree_hash, `migrated artifact ${relativePath}.legacy_source_tree_hash`);
    if (header.document_id !== canonicalDocumentId(kind, sourcePath, sourceSha))
      fail4(`migrated artifact ${relativePath} document_id is not bound to its source identity.`);
  }
  if (!SHA256_PATTERN3.test(sourceSha) || !SHA256_PATTERN3.test(sourceTreeHash) || sourcePath !== relativePath)
    fail4(`migrated artifact ${relativePath} has an invalid or unbound source identity.`);
  return { stableId: `artifact-${sha2567(`${kind}\x00${sourcePath}\x00${sourceSha}`).slice(0, 24)}`, sourceRevision, sourceTreeHash };
}
function validateCurrentTaskShape(root2, currentTaskPath, runtimeInstalled) {
  const content = fs13.readFileSync(currentTaskPath, "utf8");
  if (runtimeInstalled) {
    try {
      readCanonicalCurrentTask(root2);
      return;
    } catch (error) {
      fail4(`current canonical CURRENT_TASK.md is invalid: ${error instanceof Error ? error.message : String(error)}`);
    }
  }
  const match = /^---\r?\n([\s\S]*?)\r?\n---\r?\n([\s\S]*)$/u.exec(content);
  if (!match)
    fail4("current canonical CURRENT_TASK.md is missing vNext frontmatter.");
  const parsed = parseDocument2(match[1], { uniqueKeys: true });
  const diagnostics = [...parsed.errors, ...parsed.warnings];
  if (diagnostics.length > 0)
    fail4(`current canonical CURRENT_TASK.md frontmatter is invalid: ${diagnostics.map((item) => item.message).join("; ")}`);
  const frontmatter = expectRecord3(parsed.toJS(), "current canonical CURRENT_TASK.md frontmatter");
  if (frontmatter.schema_version !== 1 || frontmatter.kind !== "vnext-current-task" || typeof frontmatter.document_id !== "string" || !CANONICAL_DOCUMENT_ID_PATTERN.test(frontmatter.document_id))
    fail4("current canonical CURRENT_TASK.md has an invalid vNext identity.");
  for (const heading of ["## 任务信息", "## 验收标准", "## 允许修改范围", "## 实施步骤"])
    if (!match[2].includes(heading))
      fail4(`current canonical CURRENT_TASK.md is missing required heading ${heading}.`);
}
function profileTargetIdentity(root2, profile) {
  const project = expectRecord3(profile.project, "PROJECT_PROFILE.yaml.project");
  const slug = expectString3(project.slug, "PROJECT_PROFILE.yaml.project.slug");
  if (!SAFE_ID_PATTERN.test(slug))
    fail4(`PROJECT_PROFILE.yaml.project.slug is not a safe slug: ${slug}`);
  return sha2567(`${normalizeRoot(root2)}\x00${slug}`).slice(0, 32);
}
function validateCompletedMigrationProvenance(targetRoot) {
  const root2 = path14.resolve(targetRoot);
  const statePath = resolveRepoPath2(root2, COMPLETED_MIGRATION_INSTALL_STATE_RELATIVE_PATH, "completed migration install state");
  const receiptPath = resolveRepoPath2(root2, COMPLETED_MIGRATION_RECEIPT_RELATIVE_PATH, "completed migration receipt");
  const hasState = fs13.existsSync(statePath);
  const hasReceipt = fs13.existsSync(receiptPath);
  if (!hasState && !hasReceipt)
    return null;
  if (!hasState || !hasReceipt)
    fail4("completed migration provenance requires both INSTALL_STATE.json and MIGRATION_RECEIPT.json.");
  const state = validateInstallState(readJson2(statePath, "INSTALL_STATE.json"));
  const rawReceipt = readJson2(receiptPath, "MIGRATION_RECEIPT.json");
  const receipt = validateReceipt(rawReceipt);
  if (rawReceipt.schema_version === 3) {
    const originals = validateMigrationAlignment(rawReceipt.aligned_originals);
    const recordedBackups = state.managed_files.filter((x) => x.category === "preserved-original").map((x) => x.path).sort();
    if (JSON.stringify(originals.map((x) => x.backup_path).sort()) !== JSON.stringify(recordedBackups))
      fail4("Alignment original inventory differs from install state.");
    for (const original of originals) {
      const full = resolveRepoPath2(root2, original.backup_path, "alignment original");
      if (!fs13.existsSync(full) || sha2567(fs13.readFileSync(full)) !== original.sha256)
        fail4(`Alignment original is missing or changed: ${original.path}`);
    }
  }
  if (state.migration_pack_id !== receipt.migration_pack_id || state.bundle_id !== receipt.bundle_id || state.source_revision !== receipt.source_revision || state.source_tree_hash !== receipt.source_tree_hash || state.target_identity !== receipt.target_identity || state.installed_at !== receipt.installed_at || !sameRuntime(state.runtime_distribution, receipt.runtime_distribution)) {
    fail4("INSTALL_STATE.json and MIGRATION_RECEIPT.json do not describe the same conversion.");
  }
  const admittedCurrentPaths = new Set(state.managed_files.map((entry) => entry.path));
  for (const removedPath of state.removed_legacy_files) {
    if (admittedCurrentPaths.has(removedPath))
      continue;
    if (fs13.existsSync(resolveRepoPath2(root2, removedPath, `removed legacy path ${removedPath}`)))
      fail4(`completed migration still has a Pack-declared removed legacy path: ${removedPath}`);
  }
  const profilePath = resolveRepoPath2(root2, ".workflow-system/PROJECT_PROFILE.yaml", "completed migration PROJECT_PROFILE");
  if (!fs13.existsSync(profilePath) || !fs13.statSync(profilePath).isFile())
    fail4("completed migration provenance is missing PROJECT_PROFILE.yaml.");
  const profile = readYaml(profilePath, "PROJECT_PROFILE.yaml");
  if (profileTargetIdentity(root2, profile) !== state.target_identity)
    fail4("completed migration target identity does not match the current project profile/root.");
  const workflowHomeValue = isRecord5(profile.paths) ? profile.paths.workflow_home : undefined;
  const workflowHome = workflowHomeValue === undefined ? "docs/workflow" : expectString3(workflowHomeValue, "PROJECT_PROFILE.yaml.paths.workflow_home");
  if (workflowHome !== "docs/workflow")
    fail4("completed migration workflow_home is not canonical.");
  const currentTaskRelative = `${workflowHome}/${CURRENT_TASK_FILE}`;
  const currentTaskPath = resolveRepoPath2(root2, currentTaskRelative, "completed migration CURRENT_TASK");
  if (!fs13.existsSync(currentTaskPath) || !fs13.statSync(currentTaskPath).isFile())
    fail4("completed migration provenance is missing the canonical CURRENT_TASK.md.");
  const currentTaskRecord = state.managed_files.find((entry) => entry.path === currentTaskRelative);
  if (!currentTaskRecord || !SHA256_PATTERN3.test(currentTaskRecord.checksum))
    fail4("completed migration install state does not record the canonical CURRENT_TASK.md baseline.");
  validateCurrentTaskShape(root2, currentTaskPath, state.runtime_distribution !== null);
  const migratedFiles = state.managed_files.filter((entry) => entry.category === "migrated-document").sort((left, right) => left.path.localeCompare(right.path));
  if (migratedFiles.length === 0)
    fail4("completed migration install state contains no converted artifact records.");
  if (!migratedFiles.some((entry) => entry.path === ".workflow-system/PROJECT_PROFILE.yaml"))
    fail4("completed migration install state does not record the converted PROJECT_PROFILE.yaml artifact.");
  const derived = migratedFiles.map((entry) => {
    const fullPath = resolveRepoPath2(root2, entry.path, `completed migration artifact ${entry.path}`);
    if (!fs13.existsSync(fullPath) || !fs13.statSync(fullPath).isFile())
      fail4(`converted migration artifact is missing: ${entry.path}`);
    return { path: entry.path, ...canonicalArtifactIdentity(entry.path, fs13.readFileSync(fullPath, "utf8")) };
  });
  const derivedIds = derived.map((item) => item.stableId);
  if (JSON.stringify(derivedIds) !== JSON.stringify(receipt.converted_artifact_ids))
    fail4("MIGRATION_RECEIPT.json converted_artifact_ids do not match current converted artifact identities.");
  const revision = derived[0].sourceRevision;
  const treeHash = derived[0].sourceTreeHash;
  if (derived.some((item) => item.sourceRevision !== revision || item.sourceTreeHash !== treeHash))
    fail4("converted artifacts do not share one migration provenance identity.");
  return {
    migration_pack_id: state.migration_pack_id,
    bundle_id: state.bundle_id,
    source_revision: state.source_revision,
    source_tree_hash: state.source_tree_hash,
    target_identity: state.target_identity,
    runtime_distribution: state.runtime_distribution,
    installed_at: state.installed_at,
    converted_artifact_ids: [...receipt.converted_artifact_ids],
    converted_artifact_paths: migratedFiles.map((entry) => entry.path)
  };
}

// runtime/vnext/src/bootstrap-support.ts
var BOOTSTRAP_SUPPORT_TEMPLATE_RELATIVE_PATH = ".workflow-system/runtime/support/bootstrap/CURRENT_TASK.md.tmpl";
var BOOTSTRAP_SUPPORT_MARKER_RELATIVE_PATH = ".workflow-system/vnext/BOOTSTRAP_IN_PROGRESS.json";
var BOOTSTRAP_SUPPORT_RECEIPT_RELATIVE_PATH = ".workflow-system/vnext/BOOTSTRAP_RECEIPT.json";
var DISTRIBUTION_STATE_RELATIVE_PATH = ".workflow-system/vnext/DISTRIBUTION_STATE.json";
var DISTRIBUTION_IN_PROGRESS_RELATIVE_PATH = ".workflow-system/vnext/DISTRIBUTION_IN_PROGRESS.json";
var CURRENT_TASK_RELATIVE_PATH = "docs/workflow/CURRENT_TASK.md";
var PROJECT_PROFILE_RELATIVE_PATH = ".workflow-system/PROJECT_PROFILE.yaml";
var FULL_WORKFLOW_DOCS = [
  "docs/workflow/CONTRACTS.md",
  "docs/workflow/DECISIONS.md",
  "docs/workflow/STATUS.md",
  "docs/workflow/LESSONS.md",
  "docs/workflow/ROADMAP.md",
  "docs/workflow/WORKFLOW_GUIDE.md"
];
var BOOTSTRAP_GOVERNANCE_SECTIONS = {
  contractsFacts: "Confirmed boundaries",
  decisionsFacts: "Confirmed facts",
  decisionsUnresolved: "Inferred or unknown facts",
  roadmapBaseline: "Design baseline consumed"
};
var DESIGN_PATHS = [
  "docs/designs/architecture.md",
  "docs/designs/database.md",
  "docs/designs/detailed-design.md",
  "docs/designs/api-contracts.md",
  "docs/designs/domain-model.md"
];
var INVENTORY_PATHS = [
  "docs/adoption/architecture-inventory.md",
  "docs/adoption/database-inventory.md",
  "docs/adoption/API_INVENTORY.md",
  "docs/adoption/RISK_REGISTER.md"
];
var REQUIRED_SKILL_ENTRIES = [
  "bootstrap-project",
  "prepare-task",
  "review-change",
  "execute-step",
  "debug-task",
  "task-lifecycle",
  "capture-work-item",
  "close-task",
  "validate-change"
];
var SAFE_SLUG = /^[a-z0-9]+(?:-[a-z0-9]+)*$/u;
var HASH64 = /^[a-f0-9]{64}$/u;
var BOOTSTRAP_RECEIPT_KEYS = [
  "schema_version",
  "kind",
  "mode",
  "target_identity",
  "project",
  "host",
  "source",
  "input_fingerprint",
  "completed_at",
  "managed_files",
  "legacy_compatibility",
  "recovery_boundary"
];
var DISTRIBUTION_STATE_KEYS = [
  "schema_version",
  "kind",
  "distribution_state",
  "distribution_version",
  "manifest_digest",
  "installed_at",
  "managed_files",
  "legacy_compatibility",
  "recovery_boundary"
];

class BootstrapSupportError extends Error {
  code;
  constructor(code, message) {
    super(`${code}: ${message}`);
    this.name = "BootstrapSupportError";
    this.code = code;
  }
}
function fail5(code, message) {
  throw new BootstrapSupportError(code, message);
}
function isRecord6(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function expectRecord4(value, location) {
  if (!isRecord6(value))
    fail5("BOOTSTRAP_SUPPORT_SCHEMA_INVALID", `${location} must be a mapping.`);
  return value;
}
function expectString4(value, location) {
  if (typeof value !== "string" || value.trim().length === 0)
    fail5("BOOTSTRAP_SUPPORT_SCHEMA_INVALID", `${location} must be a non-empty string.`);
  return value.trim();
}
function expectExactKeys4(value, keys, location) {
  const expected = new Set(keys);
  const missing = keys.filter((key) => !(key in value));
  const extra = Object.keys(value).filter((key) => !expected.has(key));
  if (missing.length > 0 || extra.length > 0)
    fail5("BOOTSTRAP_SUPPORT_SCHEMA_INVALID", `${location} keys mismatch; missing=[${missing.join(", ")}], unexpected=[${extra.join(", ")}].`);
}
function sha2568(value) {
  return crypto13.createHash("sha256").update(value).digest("hex");
}
function stableValue3(value) {
  if (Array.isArray(value))
    return value.map(stableValue3);
  if (value && typeof value === "object")
    return Object.fromEntries(Object.keys(value).sort().map((key) => [key, stableValue3(value[key])]));
  return value;
}
function digest4(value) {
  return sha2568(JSON.stringify(stableValue3(value)));
}
function normalizeRelative2(value, location = "path") {
  const normalized = value.trim().replace(/\\/gu, "/").replace(/^\.\//u, "").replace(/\/+/gu, "/");
  if (!normalized || normalized.startsWith("/") || /^[A-Za-z]:\//u.test(normalized) || normalized.split("/").some((segment) => segment === ".." || segment.length === 0) || /[\0-\x1F\x7F]/u.test(normalized) || normalized.includes("*"))
    fail5("BOOTSTRAP_SUPPORT_PATH_INVALID", `${location} must be a concrete repository-relative path.`);
  return normalized;
}
function targetPath(root2, relative) {
  const resolvedRoot = path15.resolve(root2);
  const resolved = path15.resolve(resolvedRoot, ...normalizeRelative2(relative).split("/"));
  const prefix = resolvedRoot.endsWith(path15.sep) ? resolvedRoot : `${resolvedRoot}${path15.sep}`;
  if (resolved !== resolvedRoot && !resolved.startsWith(prefix))
    fail5("BOOTSTRAP_SUPPORT_PATH_INVALID", `target path escapes root: ${relative}`);
  return resolved;
}
function readJsonObject2(filePath, location) {
  if (!fs14.existsSync(filePath) || !fs14.statSync(filePath).isFile())
    fail5("BOOTSTRAP_SUPPORT_INPUT_INVALID", `${location} is missing.`);
  try {
    return expectRecord4(JSON.parse(fs14.readFileSync(filePath, "utf8")), location);
  } catch (error) {
    if (error instanceof BootstrapSupportError)
      throw error;
    fail5("BOOTSTRAP_SUPPORT_INPUT_INVALID", `${location} is not valid JSON: ${error instanceof Error ? error.message : String(error)}`);
  }
}
function readYamlObject(filePath, location) {
  if (!fs14.existsSync(filePath) || !fs14.statSync(filePath).isFile())
    return {};
  try {
    const value = parse6(fs14.readFileSync(filePath, "utf8"));
    return value === null ? {} : expectRecord4(value, location);
  } catch (error) {
    fail5("BOOTSTRAP_SUPPORT_PROFILE_INVALID", `${location} is invalid: ${error instanceof Error ? error.message : String(error)}`);
  }
}
function fileHash(filePath) {
  return sha2568(fs14.readFileSync(filePath));
}
function existingProfile(root2) {
  const filePath = targetPath(root2, PROJECT_PROFILE_RELATIVE_PATH);
  if (!fs14.existsSync(filePath))
    return null;
  const profile = readYamlObject(filePath, PROJECT_PROFILE_RELATIVE_PATH);
  if (profile.mutation_authority !== undefined) {
    try {
      readProjectMutationAuthority(root2);
    } catch (error) {
      fail5("BOOTSTRAP_SUPPORT_PROFILE_INVALID", error instanceof MutationAuthorityError ? error.message : String(error));
    }
  }
  return profile;
}
function profileProject(profile) {
  const project = profile?.project;
  if (!isRecord6(project) || typeof project.name !== "string" || typeof project.slug !== "string")
    return null;
  return { name: project.name.trim(), slug: project.slug.trim() };
}
function safeSlug(value) {
  const normalized = value.trim().toLocaleLowerCase().replace(/[^a-z0-9]+/gu, "-").replace(/^-+|-+$/gu, "");
  return normalized || "project";
}
function resolveProject(root2, options, receipt) {
  const profile = existingProfile(root2);
  const profileIdentity = profileProject(profile);
  let packageName = null;
  const packagePath = targetPath(root2, "package.json");
  if (fs14.existsSync(packagePath)) {
    try {
      const packageJson = readJsonObject2(packagePath, "package.json");
      packageName = typeof packageJson.name === "string" && packageJson.name.trim() ? packageJson.name.trim() : null;
    } catch {
      packageName = null;
    }
  }
  const name = options.projectName?.trim() || profileIdentity?.name || receipt?.project.name || packageName || path15.basename(path15.resolve(root2));
  const slug = options.projectSlug?.trim() || profileIdentity?.slug || receipt?.project.slug || safeSlug(name);
  if (!SAFE_SLUG.test(slug))
    fail5("BOOTSTRAP_SUPPORT_PROFILE_INVALID", `project slug must be lowercase kebab-case: ${slug}`);
  if (profileIdentity && options.projectName && profileIdentity.name !== options.projectName.trim())
    fail5("BOOTSTRAP_SUPPORT_IDENTITY_CONFLICT", "caller project name conflicts with the existing project profile.");
  if (profileIdentity && options.projectSlug && profileIdentity.slug !== options.projectSlug.trim())
    fail5("BOOTSTRAP_SUPPORT_IDENTITY_CONFLICT", "caller project slug conflicts with the existing project profile.");
  if (profile?.paths && isRecord6(profile.paths) && profile.paths.workflow_home !== undefined && profile.paths.workflow_home !== "docs/workflow")
    fail5("BOOTSTRAP_SUPPORT_IDENTITY_CONFLICT", "existing paths.workflow_home is not the canonical vNext workflow home.");
  return { name, slug };
}
function listTopLevelNames(root2) {
  if (!fs14.existsSync(root2))
    return [];
  return fs14.readdirSync(root2, { withFileTypes: true }).map((entry) => entry.name).sort();
}
var DOMAIN_CANDIDATE_IGNORES = new Set([
  ".git",
  ".workflow-system",
  ".agents",
  ".claude",
  ".codex",
  "node_modules",
  "dist",
  "build",
  "coverage",
  "target"
]);
var DOMAIN_CANDIDATE_FILE_EXTENSIONS = /\.(?:ts|tsx|js|jsx|mjs|cjs|py|go|rs|java|cs|php|rb|swift|kt|kts)$/iu;
function normalizeAuthorityDomainCandidates(value, location = "authority_domain_candidates") {
  if (!Array.isArray(value) || value.length > 128)
    fail5("BOOTSTRAP_AUTHORITY_DOMAIN_CANDIDATES_INVALID", `${location} must be a bounded array.`);
  const candidates = value.map((raw, index) => {
    const candidate = expectRecord4(raw, `${location}[${index}]`);
    expectExactKeys4(candidate, ["id", "roots", "basis", "evidence_refs"], `${location}[${index}]`);
    const authority = normalizeProjectMutationAuthority({ domains: [{ id: candidate.id, roots: candidate.roots }] });
    const domain = authority.domains[0];
    if (typeof candidate.basis !== "string" || candidate.basis.trim().length === 0)
      fail5("BOOTSTRAP_AUTHORITY_DOMAIN_CANDIDATES_INVALID", `${location}[${index}].basis must be non-empty.`);
    if (!Array.isArray(candidate.evidence_refs) || candidate.evidence_refs.length === 0 || candidate.evidence_refs.length > 32 || candidate.evidence_refs.some((item) => typeof item !== "string" || item.trim().length === 0)) {
      fail5("BOOTSTRAP_AUTHORITY_DOMAIN_CANDIDATES_INVALID", `${location}[${index}].evidence_refs must be a non-empty bounded list of strings.`);
    }
    return {
      id: domain.id,
      roots: domain.roots,
      basis: candidate.basis.trim(),
      evidence_refs: candidate.evidence_refs.map((item) => item.trim())
    };
  });
  if (new Set(candidates.map((item) => item.id)).size !== candidates.length)
    fail5("BOOTSTRAP_AUTHORITY_DOMAIN_CANDIDATES_INVALID", `${location} domain IDs must be unique.`);
  try {
    normalizeProjectMutationAuthority({ domains: candidates.map((item) => ({ id: item.id, roots: item.roots })) });
  } catch (error) {
    fail5("BOOTSTRAP_AUTHORITY_DOMAIN_CANDIDATES_INVALID", error instanceof Error ? error.message : String(error));
  }
  return candidates;
}
function proposeAuthorityDomainCandidates(root2) {
  let existing = null;
  try {
    existing = readProjectMutationAuthority(root2);
  } catch (error) {
    fail5("BOOTSTRAP_SUPPORT_PROFILE_INVALID", error instanceof Error ? error.message : String(error));
  }
  if (existing) {
    return existing.domains.map((domain) => ({
      id: domain.id,
      roots: [...domain.roots],
      basis: "existing project-owner-confirmed authority domain retained as a candidate; Bootstrap does not rebuild it.",
      evidence_refs: [`bootstrap:existing-authority-domain:${domain.id}`]
    }));
  }
  const entries = fs14.existsSync(root2) ? fs14.readdirSync(root2, { withFileTypes: true }) : [];
  const directories = entries.filter((entry) => entry.isDirectory() && !entry.name.startsWith(".") && !DOMAIN_CANDIDATE_IGNORES.has(entry.name)).map((entry) => entry.name).sort((left, right) => left.localeCompare(right));
  if (directories.length > 0) {
    const used = new Set;
    return directories.map((name) => {
      const base = safeSlug(name).replace(/-+/gu, "-") || "application";
      let id = base;
      let suffix = 2;
      while (used.has(id))
        id = `${base}-${suffix++}`;
      used.add(id);
      return {
        id,
        roots: [`${name}/**`],
        basis: `bootstrap inventory observed top-level product directory ${name}; owner confirmation is still required before this becomes canonical authority.`,
        evidence_refs: [`bootstrap:inventory:top-level-directory:${name}`]
      };
    });
  }
  const files = entries.filter((entry) => entry.isFile() && DOMAIN_CANDIDATE_FILE_EXTENSIONS.test(entry.name)).map((entry) => entry.name).sort((left, right) => left.localeCompare(right));
  return files.length > 0 ? [{
    id: "application",
    roots: files,
    basis: "bootstrap inventory observed executable source files at the project root; owner confirmation is still required before this becomes canonical authority.",
    evidence_refs: ["bootstrap:inventory:top-level-source-files"]
  }] : [];
}
function normalizeAuthorityDomainConfirmation(value) {
  const confirmation = expectRecord4(value, "authority_domain_confirmation");
  expectExactKeys4(confirmation, ["domains", "decision_source", "decision_text"], "authority_domain_confirmation");
  const authority = normalizeProjectMutationAuthority({ domains: confirmation.domains });
  if (typeof confirmation.decision_source !== "string" || confirmation.decision_source.trim().length === 0)
    fail5("BOOTSTRAP_AUTHORITY_DOMAIN_CONFIRMATION_INVALID", "authority_domain_confirmation.decision_source must be non-empty.");
  if (typeof confirmation.decision_text !== "string" || confirmation.decision_text.trim().length === 0)
    fail5("BOOTSTRAP_AUTHORITY_DOMAIN_CONFIRMATION_INVALID", "authority_domain_confirmation.decision_text must be non-empty.");
  return {
    domains: authority.domains.map((domain) => ({ id: domain.id, roots: [...domain.roots] })),
    decision_source: confirmation.decision_source.trim(),
    decision_text: confirmation.decision_text.trim()
  };
}
function assertAuthorityDomainConfirmationMatchesCandidates(candidates, confirmation) {
  const candidateMap = new Map(candidates.map((candidate) => [candidate.id, candidate]));
  for (const domain of confirmation.domains) {
    const candidate = candidateMap.get(domain.id);
    if (!candidate || JSON.stringify(candidate.roots) !== JSON.stringify(domain.roots)) {
      fail5("BOOTSTRAP_AUTHORITY_DOMAIN_CONFIRMATION_INVALID", `confirmed domain ${domain.id} must match an inventory candidate exactly.`);
    }
  }
}
function readExistingDocument(root2, relative) {
  const filePath = targetPath(root2, relative);
  if (!fs14.existsSync(filePath))
    return null;
  if (!fs14.statSync(filePath).isFile())
    fail5("BOOTSTRAP_SUPPORT_TARGET_CONFLICT", `${relative} is not a regular governance document.`);
  return fs14.readFileSync(filePath, "utf8");
}
function readExistingMarkdownSection(root2, relative, title) {
  const content = readExistingDocument(root2, relative);
  if (content === null)
    return [];
  const body = readCanonicalMarkdownSection(content, [title]);
  return body === null ? [] : body.replace(/\r\n?/gu, `
`).split(`
`);
}
function readExistingRuntimeDocument(relative, reader) {
  try {
    return reader();
  } catch (error) {
    if (error instanceof BootstrapSupportError)
      throw error;
    fail5("BOOTSTRAP_SUPPORT_TARGET_CONFLICT", `${relative} contains invalid canonical Runtime governance state: ${error instanceof Error ? error.message : String(error)}`);
  }
}
function sameGovernanceFact(left, right) {
  return left.key === right.key && left.value === right.value && left.source === right.source && left.certainty === right.certainty;
}
function mergeGovernanceFacts(existing, caller) {
  const merged = new Map;
  for (const fact of existing) {
    const previous = merged.get(fact.key);
    if (previous && !sameGovernanceFact(previous, fact))
      fail5("BOOTSTRAP_SUPPORT_TARGET_CONFLICT", `existing governance facts conflict for key: ${fact.key}.`);
    merged.set(fact.key, fact);
  }
  for (const fact of caller)
    merged.set(fact.key, fact);
  return [...merged.values()];
}
function readExistingBootstrapProjection(root2) {
  return readExistingRuntimeDocument("Bootstrap governance projection", () => {
    const facts = mergeGovernanceFacts([
      ...parseCanonicalGovernanceFactLines(readExistingMarkdownSection(root2, "docs/workflow/CONTRACTS.md", BOOTSTRAP_GOVERNANCE_SECTIONS.contractsFacts), `docs/workflow/CONTRACTS.md > ${BOOTSTRAP_GOVERNANCE_SECTIONS.contractsFacts}`),
      ...parseCanonicalGovernanceFactLines(readExistingMarkdownSection(root2, "docs/workflow/DECISIONS.md", BOOTSTRAP_GOVERNANCE_SECTIONS.decisionsFacts), `docs/workflow/DECISIONS.md > ${BOOTSTRAP_GOVERNANCE_SECTIONS.decisionsFacts}`),
      ...parseCanonicalGovernanceFactLines(readExistingMarkdownSection(root2, "docs/workflow/DECISIONS.md", BOOTSTRAP_GOVERNANCE_SECTIONS.decisionsUnresolved), `docs/workflow/DECISIONS.md > ${BOOTSTRAP_GOVERNANCE_SECTIONS.decisionsUnresolved}`)
    ], []);
    const designBaselineKeys = parseCanonicalBaselineKeys(readExistingMarkdownSection(root2, "docs/workflow/ROADMAP.md", BOOTSTRAP_GOVERNANCE_SECTIONS.roadmapBaseline), `docs/workflow/ROADMAP.md > ${BOOTSTRAP_GOVERNANCE_SECTIONS.roadmapBaseline}`);
    return { facts, designBaselineKeys };
  });
}
function readExistingGovernanceState(root2) {
  const projection = readExistingBootstrapProjection(root2);
  const contracts = readExistingDocument(root2, "docs/workflow/CONTRACTS.md");
  const decisions = readExistingDocument(root2, "docs/workflow/DECISIONS.md");
  const status = readExistingDocument(root2, "docs/workflow/STATUS.md");
  const lessons = readExistingDocument(root2, "docs/workflow/LESSONS.md");
  return {
    ...projection,
    contractRecordsSection: contracts === null ? null : readExistingRuntimeDocument("docs/workflow/CONTRACTS.md", () => readCanonicalDurableKnowledgeSection(contracts, "docs/workflow/CONTRACTS.md", "contract")),
    decisionRecordsSection: decisions === null ? null : readExistingRuntimeDocument("docs/workflow/DECISIONS.md", () => readCanonicalDurableKnowledgeSection(decisions, "docs/workflow/DECISIONS.md", "decision")),
    statusDocument: status === null ? null : readExistingRuntimeDocument("docs/workflow/STATUS.md", () => {
      readCanonicalStatusDocumentForBootstrap(status, "docs/workflow/STATUS.md");
      return status;
    }),
    lessonsDocument: lessons === null ? null : readExistingRuntimeDocument("docs/workflow/LESSONS.md", () => readBootstrapLessonsDocument(lessons, "docs/workflow/LESSONS.md"))
  };
}
function mergeDesignBaselineKeys(existing, caller) {
  return [...new Set([...existing, ...Object.keys(caller)])].sort((left, right) => left.localeCompare(right));
}
function hasMeaningfulImplementation(root2) {
  const names = listTopLevelNames(root2);
  if (names.some((name) => ["src", "app", "lib", "packages", "server", "client"].includes(name)))
    return true;
  return names.some((name) => /\.(?:ts|tsx|js|jsx|py|go|rs|java|cs|php|rb|swift)$/iu.test(name));
}
function isVNextMarkerFile(root2, relative) {
  const filePath = targetPath(root2, relative);
  if (!fs14.existsSync(filePath) || !fs14.statSync(filePath).isFile())
    return false;
  return /vnext|vNext/iu.test(fs14.readFileSync(filePath, "utf8").slice(0, 1600));
}
function isFrozenPath(root2, relative) {
  const normalized = normalizeRelative2(relative, "freeze check");
  const registries = ["FREEZE_REGISTRY.md", ".workflow-system/FREEZE_REGISTRY.md"].map((file) => targetPath(root2, file)).filter((file) => fs14.existsSync(file) && fs14.statSync(file).isFile());
  for (const registry of registries) {
    if (fs14.readFileSync(registry, "utf8").split(/\r?\n/u).some((line) => line.includes(normalized) && !/^\s*[-#]*\s*(unfreeze|not frozen)/iu.test(line)))
      return true;
  }
  const filePath = targetPath(root2, normalized);
  if (fs14.existsSync(filePath) && fs14.statSync(filePath).isFile()) {
    const head = fs14.readFileSync(filePath, "utf8").split(/\r?\n/u).slice(0, 20).join(`
`);
    if (/@frozen|DO NOT MODIFY/iu.test(head))
      return true;
  }
  return false;
}
function readBootstrapReceipt(root2) {
  const filePath = targetPath(root2, BOOTSTRAP_SUPPORT_RECEIPT_RELATIVE_PATH);
  if (!fs14.existsSync(filePath))
    return null;
  const raw = readJsonObject2(filePath, "BOOTSTRAP_RECEIPT.json");
  expectExactKeys4(raw, BOOTSTRAP_RECEIPT_KEYS, "BOOTSTRAP_RECEIPT.json");
  const project = expectRecord4(raw.project, "BOOTSTRAP_RECEIPT.json.project");
  const source = expectRecord4(raw.source, "BOOTSTRAP_RECEIPT.json.source");
  if (raw.schema_version !== 1 || raw.kind !== "vnext-bootstrap-receipt" || !BOOTSTRAP_MODES.includes(raw.mode) || typeof raw.target_identity !== "string" || typeof project.name !== "string" || typeof project.slug !== "string" || !["codex", "claude", "factory"].includes(raw.host) || typeof source.revision !== "string" || typeof source.tree_hash !== "string" || !HASH64.test(source.tree_hash) || typeof raw.input_fingerprint !== "string" || !HASH64.test(raw.input_fingerprint) || typeof raw.completed_at !== "string" || raw.legacy_compatibility !== "absent" || raw.recovery_boundary !== "in-progress-marker") {
    fail5("BOOTSTRAP_SUPPORT_RECEIPT_INVALID", "BOOTSTRAP_RECEIPT.json identity or schema fields are invalid.");
  }
  if (!Array.isArray(raw.managed_files) || raw.managed_files.length === 0)
    fail5("BOOTSTRAP_SUPPORT_RECEIPT_INVALID", "BOOTSTRAP_RECEIPT.json.managed_files must be non-empty.");
  const managed = raw.managed_files.map((item, index) => {
    const record = expectRecord4(item, `BOOTSTRAP_RECEIPT.json.managed_files[${index}]`);
    expectExactKeys4(record, ["path", "checksum"], `BOOTSTRAP_RECEIPT.json.managed_files[${index}]`);
    const relative = normalizeRelative2(expectString4(record.path, `BOOTSTRAP_RECEIPT.json.managed_files[${index}].path`));
    const checksum = expectString4(record.checksum, `BOOTSTRAP_RECEIPT.json.managed_files[${index}].checksum`);
    if (!HASH64.test(checksum))
      fail5("BOOTSTRAP_SUPPORT_RECEIPT_INVALID", `BOOTSTRAP_RECEIPT.json.managed_files[${index}].checksum is invalid.`);
    if (relative.startsWith(".workflow-system/runtime/") || relative.startsWith(".agents/skills/") || relative === ".workflow-system/WORKFLOW_PROTOCOL.md" || relative === ".workflow-system/FILE_SCHEMAS.md" || relative === ".workflow-system/vnext/SOURCE_CONTRACT.yaml" || relative === ".workflow-system/vnext/RUNTIME_CONTRACT.yaml")
      fail5("BOOTSTRAP_SUPPORT_RECEIPT_INVALID", `BOOTSTRAP receipt cannot own Distribution software: ${relative}`);
    return { path: relative, checksum };
  });
  if (new Set(managed.map((item) => item.path)).size !== managed.length)
    fail5("BOOTSTRAP_SUPPORT_RECEIPT_INVALID", "BOOTSTRAP_RECEIPT.json.managed_files contains duplicates.");
  return {
    schema_version: 1,
    kind: "vnext-bootstrap-receipt",
    mode: raw.mode,
    target_identity: raw.target_identity,
    project: { name: project.name, slug: project.slug },
    host: raw.host,
    source: { revision: source.revision, tree_hash: source.tree_hash },
    input_fingerprint: raw.input_fingerprint,
    completed_at: raw.completed_at,
    managed_files: managed,
    legacy_compatibility: "absent",
    recovery_boundary: "in-progress-marker"
  };
}
function isDistributionManagedPath(relative) {
  return relative === ".workflow-system/WORKFLOW_PROTOCOL.md" || relative === ".workflow-system/FILE_SCHEMAS.md" || relative === ".workflow-system/vnext/SOURCE_CONTRACT.yaml" || relative === ".workflow-system/vnext/RUNTIME_CONTRACT.yaml" || relative.startsWith(".workflow-system/runtime/") || /^\.agents\/skills\/[a-z][a-z0-9-]*\/SKILL\.md$/u.test(relative);
}
function validateSkillFile(root2, entry) {
  const relative = `.agents/skills/${entry}/SKILL.md`;
  const filePath = targetPath(root2, relative);
  if (!fs14.existsSync(filePath) || !fs14.statSync(filePath).isFile())
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", `required Distribution Skill is missing: ${relative}`);
  const content = fs14.readFileSync(filePath, "utf8");
  const match = /^---\r?\n([\s\S]*?)\r?\n---\r?\n?/u.exec(content);
  if (!match)
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", `${relative} is missing Agent Skill frontmatter.`);
  const document = parseDocument3(match[1], { uniqueKeys: true });
  const diagnostics = [...document.errors, ...document.warnings];
  if (diagnostics.length > 0)
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", `${relative} frontmatter is invalid: ${diagnostics.map((item) => item.message).join("; ")}`);
  const frontmatter = expectRecord4(document.toJS(), `${relative} frontmatter`);
  if (frontmatter.name !== entry || typeof frontmatter.description !== "string" || !frontmatter.description.trim())
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", `${relative} must declare name=${entry} and a non-empty description.`);
  const contract = expectRecord4(frontmatter.entry_contract, `${relative}.entry_contract`);
  if (contract.entry !== entry)
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", `${relative}.entry_contract.entry is not canonical.`);
}
function validateInstalledDistributionReadback(root2) {
  const statePath = targetPath(root2, DISTRIBUTION_STATE_RELATIVE_PATH);
  const journalPath = targetPath(root2, DISTRIBUTION_IN_PROGRESS_RELATIVE_PATH);
  if (fs14.existsSync(journalPath))
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", "Distribution has an interrupted transaction marker; recover through Distribution before Bootstrap.");
  const raw = readJsonObject2(statePath, "DISTRIBUTION_STATE.json");
  expectExactKeys4(raw, DISTRIBUTION_STATE_KEYS, "DISTRIBUTION_STATE.json");
  if (raw.schema_version !== 1 || raw.kind !== "vibe-governance-distribution-state" || raw.distribution_state !== "vnext" || raw.legacy_compatibility !== "absent" || raw.recovery_boundary !== "distribution-journal")
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", "DISTRIBUTION_STATE.json is not a completed vNext Distribution state.");
  const version = expectString4(raw.distribution_version, "DISTRIBUTION_STATE.json.distribution_version");
  const manifestDigest = expectString4(raw.manifest_digest, "DISTRIBUTION_STATE.json.manifest_digest");
  if (!HASH64.test(manifestDigest) || !/^\d+\.\d+\.\d+$/u.test(version))
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", "Distribution State version or manifest digest is invalid.");
  if (!Array.isArray(raw.managed_files) || raw.managed_files.length === 0)
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", "DISTRIBUTION_STATE.json.managed_files must be non-empty.");
  const managed = raw.managed_files.map((item, index) => {
    const record = expectRecord4(item, `DISTRIBUTION_STATE.json.managed_files[${index}]`);
    expectExactKeys4(record, ["path", "checksum", "category"], `DISTRIBUTION_STATE.json.managed_files[${index}]`);
    const relative = normalizeRelative2(expectString4(record.path, `DISTRIBUTION_STATE.json.managed_files[${index}].path`));
    const checksum = expectString4(record.checksum, `DISTRIBUTION_STATE.json.managed_files[${index}].checksum`);
    if (!isDistributionManagedPath(relative) || !HASH64.test(checksum))
      fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", `Distribution State contains an invalid managed path/checksum: ${relative}`);
    const category = expectString4(record.category, `DISTRIBUTION_STATE.json.managed_files[${index}].category`);
    if (!["protocol", "schema", "skill", "runtime", "config"].includes(category))
      fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", `Distribution State contains an unsupported managed category: ${relative}`);
    return { path: relative, checksum, category };
  }).sort((left, right) => left.path.localeCompare(right.path));
  if (new Set(managed.map((item) => item.path)).size !== managed.length)
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", "DISTRIBUTION_STATE.json.managed_files contains duplicates.");
  const managedMap = new Map(managed.map((item) => [item.path, item]));
  for (const entry of managed) {
    const filePath = targetPath(root2, entry.path);
    if (!fs14.existsSync(filePath) || !fs14.statSync(filePath).isFile() || fileHash(filePath) !== entry.checksum)
      fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", `Distribution managed artifact read-back failed: ${entry.path}`);
  }
  const required = [
    ".workflow-system/WORKFLOW_PROTOCOL.md",
    ".workflow-system/FILE_SCHEMAS.md",
    ".workflow-system/vnext/SOURCE_CONTRACT.yaml",
    ".workflow-system/vnext/RUNTIME_CONTRACT.yaml",
    ".workflow-system/runtime/dist/cli.js",
    ".workflow-system/runtime/package.json",
    ".workflow-system/runtime/package-lock.json",
    BOOTSTRAP_SUPPORT_TEMPLATE_RELATIVE_PATH,
    ...REQUIRED_SKILL_ENTRIES.map((entry) => `.agents/skills/${entry}/SKILL.md`)
  ];
  for (const relative of required) {
    const entry = managedMap.get(relative);
    if (!entry)
      fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", `Distribution State does not admit required artifact: ${relative}`);
  }
  const runtime = validateVNextRuntimeContract(root2, true).runtime_distribution;
  if (runtime.package_version !== version)
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", "Distribution State and project-local Runtime versions differ.");
  const protocol = fs14.readFileSync(targetPath(root2, ".workflow-system/WORKFLOW_PROTOCOL.md"), "utf8");
  const schema = fs14.readFileSync(targetPath(root2, ".workflow-system/FILE_SCHEMAS.md"), "utf8");
  if (!/^kind:\s*vnext-protocol\s*$/imu.test(protocol) || !/schema_version\s*:\s*1/iu.test(protocol))
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", "installed vNext Protocol is invalid.");
  if (!/^kind:\s*vnext-file-schema\s*$/imu.test(schema) || !/CURRENT_TASK\.md/iu.test(schema))
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", "installed vNext Schema is invalid.");
  const supportTemplate = fs14.readFileSync(targetPath(root2, BOOTSTRAP_SUPPORT_TEMPLATE_RELATIVE_PATH), "utf8");
  if (!/^kind:\s*vnext-current-task\s*$/imu.test(supportTemplate) || !supportTemplate.includes("# vNext CURRENT_TASK"))
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", "installed Bootstrap support template is invalid.");
  for (const entry of REQUIRED_SKILL_ENTRIES)
    validateSkillFile(root2, entry);
  return { distribution_version: version, manifest_digest: manifestDigest, state_content: fs14.readFileSync(statePath, "utf8"), managed_files: managed };
}
function renderProfile(project, targetIdentity, mode, host, existing, authorityDomainConfirmation = null) {
  const existingProject = existing?.project && isRecord6(existing.project) ? existing.project : {};
  const existingPaths = existing?.paths && isRecord6(existing.paths) ? existing.paths : {};
  const existingVNext = existing?.vnext && isRecord6(existing.vnext) ? existing.vnext : {};
  const existingHosts = Array.isArray(existingProject.primary_hosts) ? existingProject.primary_hosts.filter((value) => typeof value === "string" && value.trim()) : [];
  return stringify5({
    ...existing ?? {},
    schema_version: 1,
    kind: "vnext-project-profile",
    project: {
      ...existingProject,
      name: project.name,
      slug: project.slug,
      type: typeof existingProject.type === "string" && existingProject.type.trim() ? existingProject.type : "application",
      primary_hosts: existingHosts.length > 0 ? existingHosts : [host]
    },
    paths: { ...existingPaths, workflow_home: "docs/workflow" },
    vnext: {
      ...existingVNext,
      bootstrap_mode: mode,
      target_identity: targetIdentity,
      source_contract: ".workflow-system/vnext/SOURCE_CONTRACT.yaml",
      runtime_contract: ".workflow-system/vnext/RUNTIME_CONTRACT.yaml",
      runtime_entrypoint: ".workflow-system/runtime/dist/cli.js",
      legacy_compatibility: "absent"
    },
    ...authorityDomainConfirmation ? {
      mutation_authority: {
        domains: authorityDomainConfirmation.domains.map((domain) => ({ id: domain.id, roots: [...domain.roots] }))
      }
    } : {}
  });
}
var PUBLIC_ENTRY_TERMINAL_GUIDANCE = [
  "## Public entry terminal boundary",
  "",
  "`public-entry-terminal/v1` applies to every public Skill invocation.",
  "- Complete only this entry intent, its internal capabilities, and its bound Runtime operations.",
  "- After a terminal result, return to the caller and stop.",
  "- Report at most one `next_route` / `recommended_route`; it is recommendation-only for a later caller invocation, and the current invocation must not invoke another public Skill.",
  "- This is instruction-level host guidance; the current Runtime cannot observe conversation-level public Skill chaining."
];
function renderGuidance(project) {
  return [
    "<!-- vNext bootstrap managed guidance; preserve target-owned additions outside this block. -->",
    `# ${project.name} workflow guidance`,
    "",
    "This project uses the pure vNext workflow surface.",
    "",
    "- Use `bootstrap-project` only for project setup or explicit realignment.",
    "- Use `prepare-task` before executing a new task.",
    "- Use `execute-step` only for the admitted current step.",
    "- Use `review-change`, `debug-task`, `task-lifecycle`, `capture-work-item`, and `close-task` according to their contracts.",
    "- The project-local Runtime and canonical `docs/workflow/CURRENT_TASK.md` are authoritative for task state.",
    "- Do not edit governance state directly or treat discovery context as write authority.",
    "",
    ...PUBLIC_ENTRY_TERMINAL_GUIDANCE,
    "",
    `Project slug: ${project.slug}`,
    ""
  ].join(`
`);
}
function renderWorkflowGuide(project) {
  return [
    "# vNext Workflow Guide",
    "",
    `Project: ${project.name} (${project.slug})`,
    "",
    "## Administrative entry",
    "",
    "- `bootstrap-project`: design, greenfield, inventory, adopt, or realign.",
    "",
    "## Daily entries",
    "",
    "- Public-entry progression is caller-driven: each arrow below means a later independent caller invocation.",
    "- Every public Skill returns its terminal result to the caller; a next route is recommendation-only and never an automatic public-entry handoff.",
    "- `prepare-task` → `execute-step` → optional `review-change` / `debug-task` → `close-task`.",
    "- `task-lifecycle` owns pause, interrupt, resume, and supersede transitions.",
    "- `capture-work-item` remains record-only.",
    "",
    "## Authoritative state",
    "",
    "- Runtime state is read from canonical `CURRENT_TASK.md`.",
    "- Contracts, Decisions, Status, and host guidance are changed through typed Runtime proposals.",
    "- Bootstrap completion requires Distribution prerequisite validation, scope admission, governance-only promotion, and read-back.",
    ""
  ].join(`
`);
}
function preservedSectionBody(content, fallback) {
  const body = content?.replace(/\r\n?/gu, `
`).trim();
  return body && body.length > 0 ? body : fallback;
}
function renderContracts(project, facts = [], preservedRecordsSection = null) {
  const confirmed = facts.filter((fact) => fact.certainty === "confirmed");
  const recordsSection = preservedSectionBody(preservedRecordsSection, "- Normal close-task Contract admissions are appended here through the typed Runtime operation; this section is not edited directly by a Skill.");
  return [
    "# vNext Contracts",
    "",
    `Project: ${project.name} (${project.slug})`,
    "",
    `## ${BOOTSTRAP_GOVERNANCE_SECTIONS.contractsFacts}`,
    "",
    "- Bootstrap writes only the admitted workflow asset set.",
    "- Task state remains in canonical `CURRENT_TASK.md`.",
    ...confirmed.map((fact) => `- ${fact.key}: ${fact.value} (source: ${fact.source})`),
    "",
    "## Candidate status",
    "",
    "- These entries are the confirmed bootstrap baseline; unconfirmed facts remain outside the locked Contract.",
    "",
    "## vNext Contract Records",
    "",
    recordsSection,
    ""
  ].join(`
`);
}
function renderDecisions(project, mode, facts = [], draft = false, preservedRecordsSection = null) {
  const confirmed = facts.filter((fact) => fact.certainty === "confirmed");
  const unresolved = facts.filter((fact) => fact.certainty !== "confirmed");
  const recordsSection = preservedSectionBody(preservedRecordsSection, "- Normal close-task Decision admissions are appended here through the typed Runtime operation; this section is not edited directly by a Skill.");
  return [
    "# vNext Decisions",
    "",
    `Project: ${project.name} (${project.slug})`,
    "",
    "## Bootstrap decision",
    "",
    `- mode: ${mode}`,
    `- status: ${draft ? "draft" : "confirmed"}`,
    "- authority: project owner",
    "",
    `## ${BOOTSTRAP_GOVERNANCE_SECTIONS.decisionsFacts}`,
    "",
    ...confirmed.length > 0 ? confirmed.map((fact) => `- ${fact.key}: ${fact.value} (source: ${fact.source})`) : ["- none"],
    "",
    `## ${BOOTSTRAP_GOVERNANCE_SECTIONS.decisionsUnresolved}`,
    "",
    ...unresolved.length > 0 ? unresolved.map((fact) => `- ${fact.key}: ${fact.value} [${fact.certainty}; source: ${fact.source}]`) : ["- none"],
    "",
    "## vNext Decision Records",
    "",
    recordsSection,
    ""
  ].join(`
`);
}
function renderStatus(project, mode, preservedStatusDocument = null) {
  if (preservedStatusDocument !== null)
    return canonicalizeStatusDocumentForBootstrap(preservedStatusDocument, project, mode, "docs/workflow/STATUS.md");
  const sectionBodies = {
    overview: [`- 项目：${project.name}`, `- slug：${project.slug}`, `- bootstrap mode：${mode}`],
    completed: [
      "- Governance assets generated after the installed Distribution was validated read-only.",
      "- Project profile and canonical task baseline read back successfully."
    ],
    inProgress: ["- none"],
    pending: ["- none"],
    risks: ["- none"],
    removedOrDeferred: ["- none"],
    nextCheckpoint: ["- Prepare the next task through the daily Runtime path."],
    recentUpdates: ["- Bootstrap generated this canonical STATUS baseline."]
  };
  return [
    "# STATUS.md",
    "",
    ...STATUS_SECTION_KEYS.flatMap((key) => [
      `## ${STATUS_SECTIONS[key].title}`,
      "",
      ...sectionBodies[key],
      ""
    ])
  ].join(`
`);
}
function renderLessons(preservedLessonsDocument = null) {
  if (preservedLessonsDocument !== null)
    return canonicalizeLessonsDocumentForBootstrap(preservedLessonsDocument, "docs/workflow/LESSONS.md");
  return [
    "# LESSONS.md",
    "",
    "## 使用规则",
    "",
    "- 只记录跨任务可复用的经验",
    "- 每条经验都要说明触发信号和应对动作",
    "- 不要把一次性聊天过程原样粘贴到这里",
    "",
    "## 通用",
    "",
    "### Lesson 模板",
    "",
    "- 场景：",
    "- 结论：",
    "- 触发信号：",
    "- 应对动作：",
    "",
    "## 数据与存储",
    "",
    "- 场景：",
    "  - 结论：",
    "  - 触发信号：",
    "  - 应对动作：",
    "",
    "## 前端与交互",
    "",
    "- 场景：",
    "  - 结论：",
    "  - 触发信号：",
    "  - 应对动作：",
    "",
    "## 后端与服务",
    "",
    "- 场景：",
    "  - 结论：",
    "  - 触发信号：",
    "  - 应对动作：",
    "",
    "## 测试与回归",
    "",
    "- 场景：",
    "  - 结论：",
    "  - 触发信号：",
    "  - 应对动作：",
    "",
    "## 部署与运行时",
    "",
    "- 场景：",
    "  - 结论：",
    "  - 触发信号：",
    "  - 应对动作：",
    ""
  ].join(`
`);
}
function renderRoadmap(project, mode, baseline, preservedBaselineKeys = []) {
  const baselineKeys = [...new Set([...Object.keys(baseline ?? {}), ...preservedBaselineKeys])].sort((left, right) => left.localeCompare(right));
  return ["# ROADMAP.md", "", `Project: ${project.name} (${project.slug})`, "", "## Current boundary", "", `- ${mode} bootstrap completed as an administrative workflow operation.`, "", "## Next boundary", "", "- Prepare a concrete task with confirmed acceptance, exact mutation scope, and minimum-sufficient evidence.", ...baseline ? ["", `## ${BOOTSTRAP_GOVERNANCE_SECTIONS.roadmapBaseline}`, "", ...baselineKeys.map((key) => `- ${key}`)] : [], ""].join(`
`);
}
function renderDesignDocument(filePath, baseline) {
  const key = path15.basename(filePath, ".md");
  const value = baseline[key] ?? baseline[filePath] ?? "No confirmed content was supplied for this design section.";
  return [`# Design baseline: ${key}`, "", "## Authority", "", "- source: caller-provided confirmed design baseline", "- certainty: confirmed or explicitly awaiting confirmation", "", "## Content", "", value, ""].join(`
`);
}
function renderBaselineDoc(baseline) {
  return ["# vNext Design Baselines", "", "The following design inputs are caller-provided proposals. They do not authorize feature implementation.", "", ...Object.keys(baseline).sort().map((key) => `- ${key}: ${baseline[key]}`), ""].join(`
`);
}
function renderInventoryFile(title, root2, facts = []) {
  const names = listTopLevelNames(root2);
  return [`# ${title}`, "", "## Observed project surface", "", ...names.length > 0 ? names.map((name) => `- ${name}`) : ["- empty target root"], "", "## Evidence and certainty", "", ...facts.length > 0 ? facts.map((fact) => `- ${fact.key}: ${fact.value} [${fact.certainty}; source: ${fact.source}]`) : ["- observed from target-root directory listing"], ""].join(`
`);
}
function renderRiskRegister(facts = []) {
  const unknown = facts.filter((fact) => fact.certainty !== "confirmed");
  return ["# Adoption Risk Register", "", "## Open evidence gaps", "", ...unknown.length > 0 ? unknown.map((fact) => `- ${fact.key}: ${fact.value}; source=${fact.source}; disposition=confirm before promotion`) : ["- none recorded"], ""].join(`
`);
}
function mergeAssets(...groups) {
  const map = new Map;
  for (const asset of groups.flat()) {
    if (map.has(asset.path))
      fail5("BOOTSTRAP_SUPPORT_TARGET_CONFLICT", `generated asset path is duplicated: ${asset.path}`);
    map.set(asset.path, asset);
  }
  return [...map.values()].sort((left, right) => left.path.localeCompare(right.path));
}
function renderGovernanceDocument(file, project, mode, facts, baseline, preservedBaselineKeys = [], existingGovernance = null) {
  if (file.endsWith("/CONTRACTS.md"))
    return renderContracts(project, facts, existingGovernance?.contractRecordsSection);
  if (file.endsWith("/DECISIONS.md"))
    return renderDecisions(project, mode, facts, false, existingGovernance?.decisionRecordsSection);
  if (file.endsWith("/STATUS.md"))
    return renderStatus(project, mode, existingGovernance?.statusDocument);
  if (file.endsWith("/LESSONS.md"))
    return renderLessons(existingGovernance?.lessonsDocument);
  if (file.endsWith("/ROADMAP.md"))
    return renderRoadmap(project, mode, baseline, preservedBaselineKeys);
  return renderWorkflowGuide(project);
}
function makeGovernanceAssets(root2, project, targetIdentity, mode, host, facts, baseline, preservedBaselineKeys = [], existingGovernance = null, authorityDomainConfirmation = null) {
  const templatePath = targetPath(root2, BOOTSTRAP_SUPPORT_TEMPLATE_RELATIVE_PATH);
  if (!fs14.existsSync(templatePath) || !fs14.statSync(templatePath).isFile())
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", `Bootstrap support template is missing: ${BOOTSTRAP_SUPPORT_TEMPLATE_RELATIVE_PATH}`);
  return [
    { path: PROJECT_PROFILE_RELATIVE_PATH, category: "config", content: renderProfile(project, targetIdentity, mode, host, existingProfile(root2), authorityDomainConfirmation) },
    { path: CURRENT_TASK_RELATIVE_PATH, category: "generated", content: fs14.readFileSync(templatePath, "utf8") },
    ...FULL_WORKFLOW_DOCS.map((file) => ({ path: file, category: "governance", content: renderGovernanceDocument(file, project, mode, facts, baseline, preservedBaselineKeys, existingGovernance) })),
    { path: "AGENTS.md", category: "governance", content: renderGuidance(project) },
    { path: "CLAUDE.md", category: "governance", content: renderGuidance(project) }
  ];
}
function makeModeAssets(root2, project, mode, baseline, facts) {
  if (mode === "design") {
    return mergeAssets(DESIGN_PATHS.map((file) => ({ path: file, category: "governance", content: renderDesignDocument(file, baseline) })), [
      { path: "docs/workflow/BASELINES.md", category: "governance", content: renderBaselineDoc(baseline) },
      { path: "docs/workflow/ROADMAP.md", category: "governance", content: renderRoadmap(project, mode, baseline) },
      { path: "docs/workflow/DECISIONS.md", category: "governance", content: renderDecisions(project, mode, [], true) }
    ]);
  }
  if (mode === "inventory") {
    return [
      { path: INVENTORY_PATHS[0], category: "governance", content: renderInventoryFile("Architecture Inventory", root2, facts) },
      { path: INVENTORY_PATHS[1], category: "governance", content: renderInventoryFile("Database Inventory", root2, facts) },
      { path: INVENTORY_PATHS[2], category: "governance", content: renderInventoryFile("API Inventory", root2, facts) },
      { path: INVENTORY_PATHS[3], category: "governance", content: renderRiskRegister(facts) }
    ];
  }
  return [];
}
function renderScopeDocument(allowed) {
  return [
    "## 允许修改范围",
    "",
    "### Allowed Files",
    "",
    ...allowed.map((file) => `- \`${file}\``),
    "",
    "### Conditional Files",
    "",
    "- none",
    "",
    "## 禁止修改范围",
    "",
    "### Forbidden Files",
    "",
    "- `.git/**`",
    "- `src/**`",
    "- `app/**`",
    "- `lib/**`",
    "- `packages/**`",
    "- `package.json`",
    "- `package-lock.json`",
    "- `node_modules/**`",
    ""
  ].join(`
`);
}
function semanticOperations(assets, mode) {
  const paths = new Set(assets.map((asset) => asset.path));
  const operations = [];
  const add = (operation_kind, target_paths) => {
    const existing = target_paths.filter((target) => paths.has(target));
    if (existing.length > 0)
      operations.push({ operation_kind, target_paths: existing, evidence_refs: [`evidence:bootstrap:${mode}:${operation_kind}`] });
  };
  add("decision-record-transaction", ["docs/workflow/DECISIONS.md"]);
  add("contract-candidate-commit", ["docs/workflow/CONTRACTS.md"]);
  add("project-status-transaction", ["docs/workflow/STATUS.md"]);
  add("paired-host-guidance-transaction", ["AGENTS.md", "CLAUDE.md"]);
  return operations;
}
function normalizeFacts(value, location) {
  try {
    return normalizeCanonicalGovernanceFacts(value, location, "BOOTSTRAP_SUPPORT_INPUT_INVALID");
  } catch (error) {
    if (error instanceof BootstrapSupportError)
      throw error;
    fail5("BOOTSTRAP_SUPPORT_INPUT_INVALID", error instanceof Error ? error.message : String(error));
  }
}
function normalizeBaseline(value) {
  try {
    return normalizeCanonicalBaselineEntries(value, "designBaseline", "BOOTSTRAP_SUPPORT_INPUT_INVALID");
  } catch (error) {
    if (error instanceof BootstrapSupportError)
      throw error;
    fail5("BOOTSTRAP_SUPPORT_INPUT_INVALID", error instanceof Error ? error.message : String(error));
  }
}
function existingIsWorkflowOwned(root2, relative, receipt = null) {
  const filePath = targetPath(root2, relative);
  if (!fs14.existsSync(filePath))
    return true;
  if (isFrozenPath(root2, relative))
    return false;
  if (relative === BOOTSTRAP_SUPPORT_RECEIPT_RELATIVE_PATH && receipt)
    return true;
  if (receipt?.managed_files.some((file) => file.path === relative))
    return true;
  if (relative.startsWith(".workflow-system/"))
    return relative === PROJECT_PROFILE_RELATIVE_PATH;
  if (relative === "AGENTS.md" || relative === "CLAUDE.md")
    return isVNextMarkerFile(root2, relative);
  if (relative.startsWith("docs/workflow/") || relative.startsWith("docs/designs/") || relative.startsWith("docs/adoption/"))
    return isVNextMarkerFile(root2, relative);
  return false;
}
function renderReceipt(mode, targetIdentity, project, host, source, inputFingerprint, assets) {
  return `${JSON.stringify({
    schema_version: 1,
    kind: "vnext-bootstrap-receipt",
    mode,
    target_identity: targetIdentity,
    project,
    host,
    source,
    input_fingerprint: inputFingerprint,
    completed_at: new Date().toISOString(),
    managed_files: assets.map((asset) => ({ path: asset.path, checksum: sha2568(Buffer.from(asset.content, "utf8")) })).sort((left, right) => left.path.localeCompare(right.path)),
    legacy_compatibility: "absent",
    recovery_boundary: "in-progress-marker"
  }, null, 2)}
`;
}
function migrationAdmission(root2, supplied) {
  if (supplied)
    return supplied;
  try {
    const provenance = validateCompletedMigrationProvenance(root2);
    return provenance ? { status: "valid", provenance } : { status: "none" };
  } catch (error) {
    return { status: "invalid", reason: error instanceof MigrationProvenanceError ? error.message : String(error) };
  }
}
function classifyBootstrapTargetLocal(root2, options = {}) {
  if (fs14.existsSync(targetPath(root2, BOOTSTRAP_SUPPORT_MARKER_RELATIVE_PATH)))
    return { state: "in-progress", receipt: null, migration: { status: "none" }, reasons: ["an explicit Bootstrap interruption marker is present"] };
  let receipt = null;
  try {
    receipt = readBootstrapReceipt(root2);
  } catch (error) {
    return { state: "conflicting", receipt: null, migration: { status: "none" }, reasons: [error instanceof Error ? error.message : String(error)] };
  }
  const migration = migrationAdmission(root2, options.migrationAdmission);
  if (migration.status === "invalid")
    return { state: "conflicting", receipt, migration, reasons: [migration.reason ?? "completed Migration Pack provenance is invalid"] };
  if (options.legacySurfacePresent)
    return { state: "legacy", receipt, migration, reasons: ["a compatibility or legacy workflow surface is present; use the offline Migration Pack boundary"] };
  const targetIdentity = computeBootstrapTargetIdentity(root2);
  if (receipt && receipt.target_identity !== targetIdentity)
    return { state: "conflicting", receipt, migration, reasons: ["Bootstrap receipt target identity does not match the current target root"] };
  try {
    const profile = existingProfile(root2);
    const profileVNext = profile?.vnext;
    if (isRecord6(profileVNext) && profileVNext.target_identity !== undefined && profileVNext.target_identity !== targetIdentity)
      return { state: "conflicting", receipt, migration, reasons: ["project profile target identity does not match the current target root"] };
  } catch (error) {
    return { state: "conflicting", receipt, migration, reasons: [error instanceof Error ? error.message : String(error)] };
  }
  if (receipt) {
    const drift = receipt.managed_files.filter((file) => {
      const filePath = targetPath(root2, file.path);
      return !fs14.existsSync(filePath) || !fs14.statSync(filePath).isFile() || fileHash(filePath) !== file.checksum;
    });
    return drift.length === 0 ? { state: "valid", receipt, migration, reasons: [] } : { state: "stale", receipt, migration, reasons: drift.map((file) => `managed governance asset drifted: ${file.path}`) };
  }
  if (migration.status === "valid")
    return { state: "governed", receipt: null, migration, reasons: ["the project was admitted by the completed Migration Pack provenance verifier; a Bootstrap Receipt is not required"] };
  const governanceSignals = [PROJECT_PROFILE_RELATIVE_PATH, CURRENT_TASK_RELATIVE_PATH, ...FULL_WORKFLOW_DOCS];
  if (governanceSignals.some((relative) => fs14.existsSync(targetPath(root2, relative))))
    return { state: "incomplete", receipt: null, migration, reasons: ["governance assets exist without Bootstrap transaction provenance"] };
  return { state: hasMeaningfulImplementation(root2) ? "existing" : "empty", receipt: null, migration, reasons: [] };
}
function isValidReceiptModeTransition(receiptMode, requestedMode) {
  if (requestedMode === "realign")
    return true;
  if (receiptMode === requestedMode)
    return false;
  return receiptMode === "design" && requestedMode === "greenfield" || receiptMode === "inventory" && requestedMode === "adopt";
}
function modeBlockers(root2, state, mode, options, baseline, facts, receipt, migration) {
  const blockers = [];
  if (state === "in-progress")
    blockers.push({ code: "BOOTSTRAP_IN_PROGRESS", message: "an interrupted Bootstrap marker requires explicit recovery before retry." });
  if (state === "legacy")
    blockers.push({ code: "MIGRATION_REQUIRED", message: "legacy/compatibility assets are outside Bootstrap; run the offline Migration Pack boundary." });
  if (state === "conflicting")
    blockers.push({ code: "BOOTSTRAP_CONFLICT", message: "existing Bootstrap receipt or target identity is conflicting." });
  if (migration.status === "valid" && mode !== "realign" && !["in-progress", "legacy", "conflicting"].includes(state)) {
    blockers.push({ code: "BOOTSTRAP_NOT_REQUIRED", message: "Migration Pack provenance already establishes a governed vNext project; ordinary Bootstrap is not required." });
    return blockers;
  }
  if (["greenfield", "adopt", "realign"].includes(mode) && fs14.existsSync(targetPath(root2, CURRENT_TASK_RELATIVE_PATH))) {
    try {
      const current = readCanonicalCurrentTask(root2);
      if (current.runtimeState.workflow_status !== "closed" || current.runtimeState.lifecycle_state !== "archived" || current.runtimeState.active_step_status !== "completed" || mode !== "realign" && current.runtimeState.task_id !== "000") {
        blockers.push({ code: "BOOTSTRAP_TASK_STATE_INVALID", message: mode === "realign" ? "realign cannot run over an active or invalid canonical CURRENT_TASK; close or recover the task through the daily Runtime first." : "bootstrap cannot replace an existing active or non-baseline canonical CURRENT_TASK; continue through the daily Runtime or close/recover it first." });
      }
    } catch (error) {
      blockers.push({ code: "BOOTSTRAP_TASK_STATE_INVALID", message: `existing canonical CURRENT_TASK is not a readable vNext baseline: ${error instanceof Error ? error.message : String(error)}` });
    }
  }
  if (state === "valid" && receipt && receipt.mode !== mode && !isValidReceiptModeTransition(receipt.mode, mode))
    blockers.push({ code: "BOOTSTRAP_IDENTITY_CONFLICT", message: "an existing Bootstrap receipt does not admit this mode transition; replay the same mode or use realign." });
  if (mode === "design") {
    if (!["empty", "existing", "valid"].includes(state))
      blockers.push({ code: "BOOTSTRAP_STATE_INVALID", message: "design mode only accepts an uninitialized target or valid Bootstrap replay." });
    if (Object.keys(baseline).length === 0)
      blockers.push({ code: "DESIGN_EVIDENCE_MISSING", message: "design mode requires a caller-provided design baseline." });
  }
  if (mode === "greenfield") {
    if (state !== "empty" && state !== "valid" && !(state === "existing" && !hasMeaningfulImplementation(root2)))
      blockers.push({ code: "GREENFIELD_PRECONDITION", message: "greenfield mode requires an empty target, design-only preparation, or a valid Bootstrap replay." });
    if (Object.keys(baseline).length === 0 || options.designConfirmed !== true)
      blockers.push({ code: "DESIGN_CONFIRMATION_REQUIRED", message: "greenfield mode requires a confirmed design baseline." });
  }
  if (mode === "inventory" && state !== "valid" && (state !== "existing" || !hasMeaningfulImplementation(root2)))
    blockers.push({ code: "INVENTORY_PRECONDITION", message: "inventory mode requires an existing project implementation or valid Bootstrap replay." });
  if (mode === "adopt") {
    if (state !== "valid" && (!["existing", "incomplete"].includes(state) || !hasMeaningfulImplementation(root2)))
      blockers.push({ code: "ADOPT_PRECONDITION", message: "adopt mode requires an existing project implementation or valid replay." });
    if (state !== "valid" && !fs14.existsSync(targetPath(root2, "docs/adoption/architecture-inventory.md")))
      blockers.push({ code: "INVENTORY_REQUIRED", message: "adopt mode requires inventory evidence from inventory mode." });
    if (options.adoptionConfirmed !== true)
      blockers.push({ code: "ADOPTION_CONFIRMATION_REQUIRED", message: "adopt mode requires explicit project-owner confirmation." });
    if (facts.length === 0)
      blockers.push({ code: "ADOPTION_FACTS_MISSING", message: "adopt mode requires facts with provenance." });
  }
  if (mode === "realign" && !["valid", "stale", "incomplete", "governed"].includes(state))
    blockers.push({ code: "REALIGN_PRECONDITION", message: "realign mode requires an existing governed vNext workflow asset surface." });
  return blockers;
}
function verifyReceiptReadBack(root2, receipt, assets) {
  const expectedPaths = assets.filter((asset) => asset.path !== BOOTSTRAP_SUPPORT_RECEIPT_RELATIVE_PATH).map((asset) => asset.path).sort((left, right) => left.localeCompare(right));
  const actualPaths = receipt.managed_files.map((file) => file.path).sort((left, right) => left.localeCompare(right));
  if (JSON.stringify(expectedPaths) !== JSON.stringify(actualPaths))
    fail5("BOOTSTRAP_SUPPORT_READ_BACK_FAILED", "Bootstrap receipt managed-file paths do not match the governance proposal.");
  for (const file of receipt.managed_files) {
    const filePath = targetPath(root2, file.path);
    if (!fs14.existsSync(filePath) || !fs14.statSync(filePath).isFile() || fileHash(filePath) !== file.checksum)
      fail5("BOOTSTRAP_SUPPORT_READ_BACK_FAILED", `Bootstrap governance asset read-back failed: ${file.path}`);
  }
}
function verifyReceipt(root2, receipt, assets) {
  verifyReceiptReadBack(root2, receipt, assets);
  const expected = assets.filter((asset) => asset.path !== BOOTSTRAP_SUPPORT_RECEIPT_RELATIVE_PATH).map((asset) => ({ path: asset.path, checksum: sha2568(Buffer.from(asset.content, "utf8")) })).sort((left, right) => left.path.localeCompare(right.path));
  const actual = receipt.managed_files.slice().sort((left, right) => left.path.localeCompare(right.path));
  if (JSON.stringify(expected) !== JSON.stringify(actual))
    fail5("BOOTSTRAP_SUPPORT_READ_BACK_FAILED", "Bootstrap receipt managed_files does not match the governance proposal.");
}
function verifyBootstrapHealth(root2, proposal, distributionBefore) {
  const distributionAfter = validateInstalledDistributionReadback(root2);
  if (distributionAfter.state_content !== distributionBefore.state_content)
    fail5("BOOTSTRAP_SUPPORT_DISTRIBUTION_MUTATED", "Bootstrap changed Distribution State bytes.");
  for (const asset of proposal.assets) {
    const filePath = targetPath(root2, asset.path);
    if (!fs14.existsSync(filePath) || !fs14.statSync(filePath).isFile() || fileHash(filePath) !== sha2568(Buffer.from(asset.content, "utf8")))
      fail5("BOOTSTRAP_SUPPORT_READ_BACK_FAILED", `promoted governance asset did not read back identically: ${asset.path}`);
  }
  const receipt = readBootstrapReceipt(root2);
  if (!receipt || receipt.target_identity !== proposal.target_identity || receipt.mode !== proposal.mode)
    fail5("BOOTSTRAP_SUPPORT_READ_BACK_FAILED", "Bootstrap receipt identity did not read back correctly.");
  verifyReceipt(root2, receipt, proposal.assets);
  if (["greenfield", "adopt", "realign"].includes(proposal.mode)) {
    const current = readCanonicalCurrentTask(root2);
    if (current.runtimeState.workflow_status !== "closed" || current.runtimeState.lifecycle_state !== "archived" || current.runtimeState.active_step_status !== "completed" || proposal.mode !== "realign" && current.runtimeState.task_id !== "000")
      fail5("BOOTSTRAP_SUPPORT_READ_BACK_FAILED", "canonical CURRENT_TASK is not a closed + archived Bootstrap baseline.");
  }
}
function bootstrapRollbackScope(proposal) {
  return [...new Set([...proposal.requested_write_targets, ...proposal.delete_targets, BOOTSTRAP_SUPPORT_MARKER_RELATIVE_PATH])].sort((left, right) => left.localeCompare(right));
}
function computeBootstrapPreimageHash(root2, proposal) {
  return computeScopedTreeHash(root2, bootstrapRollbackScope(proposal), [BOOTSTRAP_SUPPORT_MARKER_RELATIVE_PATH]);
}
function markerValue(proposal) {
  return `${JSON.stringify({ schema_version: 1, kind: "vnext-bootstrap-in-progress", target_identity: proposal.target_identity, mode: proposal.mode, source_revision: proposal.source_revision, source_tree_hash: proposal.source_tree_hash, planned_writes: proposal.requested_write_targets, planned_directories: proposal.requested_directory_targets, recovery: "fail-closed-explicit-recovery" }, null, 2)}
`;
}
function prepareProposal(options, distribution, classification) {
  const root2 = path15.resolve(options.targetRoot);
  const project = resolveProject(root2, options, classification.receipt);
  const host = options.host ?? "codex";
  const baseline = normalizeBaseline(options.designBaseline);
  const callerFacts = normalizeFacts(options.mode === "inventory" ? options.inventoryFacts ?? options.confirmedFacts : options.confirmedFacts, options.mode === "inventory" ? "inventoryFacts" : "confirmedFacts");
  const authorityDomainCandidates = options.authorityDomainCandidates === undefined ? proposeAuthorityDomainCandidates(root2) : normalizeAuthorityDomainCandidates(options.authorityDomainCandidates);
  const authorityDomainConfirmation = options.authorityDomainConfirmation === undefined ? null : normalizeAuthorityDomainConfirmation(options.authorityDomainConfirmation);
  if (authorityDomainConfirmation) {
    if (!["greenfield", "adopt", "realign"].includes(options.mode)) {
      fail5("BOOTSTRAP_AUTHORITY_DOMAIN_CONFIRMATION_INVALID", "domain-map confirmation is applied by greenfield, adopt, or realign; inventory only proposes candidates.");
    }
    assertAuthorityDomainConfirmationMatchesCandidates(authorityDomainCandidates, authorityDomainConfirmation);
  }
  const modeIssues = modeBlockers(root2, classification.state, options.mode, options, baseline, callerFacts, classification.receipt, classification.migration);
  if (modeIssues.length > 0)
    fail5(modeIssues[0].code, modeIssues.map((issue) => issue.message).join(" "));
  const existingGovernance = options.mode === "realign" ? readExistingGovernanceState(root2) : {
    facts: [],
    designBaselineKeys: [],
    contractRecordsSection: null,
    decisionRecordsSection: null,
    statusDocument: null,
    lessonsDocument: null
  };
  const facts = options.mode === "realign" ? mergeGovernanceFacts(existingGovernance.facts, callerFacts) : callerFacts;
  const baselineKeys = options.mode === "realign" ? mergeDesignBaselineKeys(existingGovernance.designBaselineKeys, baseline) : Object.keys(baseline).sort((left, right) => left.localeCompare(right));
  let assets = ["greenfield", "adopt", "realign"].includes(options.mode) ? makeGovernanceAssets(root2, project, computeBootstrapTargetIdentity(root2), options.mode, host, facts, baseline, baselineKeys, existingGovernance, authorityDomainConfirmation) : makeModeAssets(root2, project, options.mode, baseline, facts);
  if (options.mode === "adopt")
    assets = mergeAssets(assets, [{ path: "docs/adoption/ADOPTION_DECISION.md", category: "governance", content: renderDecisions(project, options.mode, facts) }]);
  if (options.mode === "realign") {
    const preservedMigrationPaths = new Set(classification.migration.provenance?.converted_artifact_paths ?? []);
    assets = assets.filter((asset) => asset.path !== CURRENT_TASK_RELATIVE_PATH && !(preservedMigrationPaths.has(asset.path) && fs14.existsSync(targetPath(root2, asset.path))));
  }
  const targetIdentity = computeBootstrapTargetIdentity(root2);
  const source = options.source ?? { revision: `distribution-${distribution.manifest_digest.slice(0, 16)}`, tree_hash: distribution.manifest_digest };
  const inputFingerprint = digest4({
    mode: options.mode,
    project,
    host,
    baseline,
    facts,
    authority_domain_candidates: options.authorityDomainCandidates ?? null,
    authority_domain_confirmation: options.authorityDomainConfirmation ?? null,
    targetIdentity
  });
  assets = mergeAssets(assets, [{ path: BOOTSTRAP_SUPPORT_RECEIPT_RELATIVE_PATH, category: "config", content: renderReceipt(options.mode, targetIdentity, project, host, source, inputFingerprint, assets) }]);
  const plannedWrites = assets.map((asset) => asset.path).sort((left, right) => left.localeCompare(right));
  for (const relative of plannedWrites) {
    if (isFrozenPath(root2, relative))
      fail5("BOOTSTRAP_SUPPORT_FROZEN_PATH", `Bootstrap cannot replace a frozen target: ${relative}`);
    if (!existingIsWorkflowOwned(root2, relative, classification.receipt))
      fail5("BOOTSTRAP_SUPPORT_TARGET_CONFLICT", `target-owned/native asset would be overwritten: ${relative}`);
  }
  const changedPaths = (options.changedPaths ?? []).map((value) => normalizeRelative2(value, "changed_paths")).sort((left, right) => left.localeCompare(right));
  const proposal = {
    schema_version: 1,
    kind: "vnext-bootstrap-proposal",
    caller: "bootstrap-project",
    mode: options.mode,
    target_identity: targetIdentity,
    source_revision: source.revision,
    source_tree_hash: source.tree_hash,
    scope_document: renderScopeDocument(plannedWrites),
    changed_paths: changedPaths.length > 0 ? changedPaths : plannedWrites,
    conditional_authorizations: options.conditionalAuthorizations ?? [],
    transformation_kind: "localized",
    authority_evidence: [
      { kind: "project-owner", source: authorityDomainConfirmation?.decision_source ?? "target-local bootstrap input", subject: authorityDomainConfirmation?.decision_text ?? `${project.slug}:${options.mode}` },
      { kind: "scope-admission", source: "target-local bootstrap proposal.scope_document", subject: targetIdentity },
      { kind: "evidence-admission", source: "target-local Distribution support", subject: distribution.manifest_digest }
    ],
    semantic_operations: semanticOperations(assets, options.mode),
    preconditions: ["project-local Distribution read-back passed", "target-local immutable Bootstrap support is present", "Bootstrap writes governance assets only", "project-local Runtime owns the typed commit"],
    evidence_refs: [
      "evidence:distribution-read-back",
      "evidence:bootstrap-support",
      "evidence:runtime-read-back",
      ...authorityDomainConfirmation ? [`evidence:authority-domain-confirmation:${digest4(authorityDomainConfirmation)}`] : []
    ],
    idempotency_key: `bootstrap-${options.mode}-${targetIdentity}-${inputFingerprint.slice(0, 16)}`,
    requested_write_targets: plannedWrites,
    requested_directory_targets: [],
    delete_targets: [],
    assets
  };
  try {
    validateBootstrapProjectProposal(proposal);
  } catch (error) {
    fail5(error instanceof Error && "code" in error ? String(error.code) : "BOOTSTRAP_SUPPORT_PROPOSAL_INVALID", error instanceof Error ? error.message : String(error));
  }
  return { proposal, project, host, baseline, facts, plannedWrites, inputFingerprint, authorityDomainCandidates };
}
function hasRealignSemanticOverlay(options) {
  const facts = options.confirmedFacts;
  if (facts !== undefined && (!Array.isArray(facts) || facts.length > 0))
    return true;
  const baseline = options.designBaseline;
  if (baseline !== undefined && (!isRecord6(baseline) || Object.keys(baseline).length > 0))
    return true;
  return false;
}
function replayUnchangedRealignment(root2, options, classification) {
  const receipt = classification.receipt;
  if (options.mode !== "realign" || classification.state !== "valid" || !receipt || receipt.mode !== "realign")
    return null;
  if (hasRealignSemanticOverlay(options))
    return null;
  const project = resolveProject(root2, options, receipt);
  if (project.name !== receipt.project.name || project.slug !== receipt.project.slug)
    return null;
  if (options.host !== undefined && options.host !== receipt.host)
    return null;
  if (modeBlockers(root2, classification.state, options.mode, options, {}, [], receipt, classification.migration).length > 0)
    return null;
  const targetIdentity = computeBootstrapTargetIdentity(root2);
  return {
    status: "replayed",
    target_root: root2,
    target_state: "valid",
    target_identity: targetIdentity,
    mode: options.mode,
    project,
    host: receipt.host,
    source: receipt.source,
    planned_writes: [],
    planned_directories: [],
    planned_deletes: [],
    changed_paths: [],
    blockers: [],
    warnings: [],
    read_back_verified: true
  };
}
function publicPlan(plan) {
  const { proposal: _proposal, ...publicValue } = plan;
  return publicValue;
}
function bootstrapProjectTargetLocal(options) {
  const root2 = path15.resolve(options.targetRoot);
  if (!BOOTSTRAP_MODES.includes(options.mode))
    return {
      status: "blocked",
      target_root: root2,
      target_state: "conflicting",
      target_identity: computeBootstrapTargetIdentity(root2),
      mode: options.mode,
      project: { name: "unknown", slug: "unknown" },
      host: options.host ?? "codex",
      source: { revision: "unavailable", tree_hash: "0".repeat(64) },
      planned_writes: [],
      planned_directories: [],
      planned_deletes: [],
      changed_paths: options.changedPaths ?? [],
      blockers: [{ code: "BOOTSTRAP_SUPPORT_MODE_INVALID", message: "mode is outside the closed Bootstrap mode set." }],
      warnings: [],
      read_back_verified: false
    };
  let distribution;
  try {
    distribution = validateInstalledDistributionReadback(root2);
  } catch (error) {
    return {
      status: "blocked",
      target_root: root2,
      target_state: "conflicting",
      target_identity: computeBootstrapTargetIdentity(root2),
      mode: options.mode,
      project: { name: "unknown", slug: "unknown" },
      host: options.host ?? "codex",
      source: { revision: "unavailable", tree_hash: "0".repeat(64) },
      planned_writes: [],
      planned_directories: [],
      planned_deletes: [],
      changed_paths: options.changedPaths ?? [],
      blockers: [{ code: error instanceof BootstrapSupportError ? error.code : "BOOTSTRAP_SUPPORT_DISTRIBUTION_INVALID", message: error instanceof Error ? error.message : String(error) }],
      warnings: [],
      read_back_verified: false
    };
  }
  const classification = classifyBootstrapTargetLocal(root2, {
    legacySurfacePresent: options.legacySurfacePresent,
    migrationAdmission: options.migrationAdmission
  });
  try {
    const replay = replayUnchangedRealignment(root2, options, classification);
    if (replay)
      return replay;
    const prepared = prepareProposal(options, distribution, classification);
    const proposal = prepared.proposal;
    if (classification.state === "valid" && classification.receipt && classification.receipt.mode === options.mode) {
      if (classification.receipt.input_fingerprint !== prepared.inputFingerprint)
        return {
          status: "blocked",
          target_root: root2,
          target_state: "valid",
          target_identity: proposal.target_identity,
          mode: proposal.mode,
          project: prepared.project,
          host: prepared.host,
          source: classification.receipt.source,
          planned_writes: prepared.plannedWrites,
          planned_directories: [],
          planned_deletes: [],
          changed_paths: options.changedPaths ?? [],
          blockers: [{ code: "BOOTSTRAP_IDENTITY_CONFLICT", message: "a valid Bootstrap receipt exists but its mode inputs or project identity differ." }],
          warnings: [],
          authority_domain_candidates: prepared.authorityDomainCandidates,
          read_back_verified: false,
          proposal
        };
      try {
        verifyReceiptReadBack(root2, classification.receipt, proposal.assets);
        return { status: "replayed", target_root: root2, target_state: "valid", target_identity: proposal.target_identity, mode: proposal.mode, project: prepared.project, host: classification.receipt.host, source: classification.receipt.source, planned_writes: [], planned_directories: [], planned_deletes: [], changed_paths: [], blockers: [], warnings: [], authority_domain_candidates: prepared.authorityDomainCandidates, read_back_verified: true };
      } catch (error) {
        return { status: "blocked", target_root: root2, target_state: "valid", target_identity: proposal.target_identity, mode: proposal.mode, project: prepared.project, host: classification.receipt.host, source: classification.receipt.source, planned_writes: prepared.plannedWrites, planned_directories: [], planned_deletes: [], changed_paths: [], blockers: [{ code: error instanceof BootstrapSupportError ? error.code : "BOOTSTRAP_SUPPORT_REPLAY_READ_BACK_FAILED", message: error instanceof Error ? error.message : String(error) }], warnings: [], authority_domain_candidates: prepared.authorityDomainCandidates, read_back_verified: false, proposal };
      }
    }
    if (!options.write) {
      return { status: options.changedPaths && options.changedPaths.length > 0 ? "ready" : "needs-confirmation", target_root: root2, target_state: classification.state, target_identity: proposal.target_identity, mode: proposal.mode, project: prepared.project, host: prepared.host, source: { revision: proposal.source_revision, tree_hash: proposal.source_tree_hash }, planned_writes: prepared.plannedWrites, planned_directories: [], planned_deletes: [], changed_paths: options.changedPaths ?? [], blockers: [], warnings: classification.reasons.map((message) => ({ code: "TARGET_STATE", message })), authority_domain_candidates: prepared.authorityDomainCandidates, read_back_verified: false, proposal };
    }
    if (!options.changedPaths || options.changedPaths.length === 0)
      return { status: "needs-confirmation", target_root: root2, target_state: classification.state, target_identity: proposal.target_identity, mode: proposal.mode, project: prepared.project, host: prepared.host, source: { revision: proposal.source_revision, tree_hash: proposal.source_tree_hash }, planned_writes: prepared.plannedWrites, planned_directories: [], planned_deletes: [], changed_paths: [], blockers: [{ code: "CHANGED_PATHS_REQUIRED", message: `caller must provide exact changed_paths: ${prepared.plannedWrites.join(", ")}` }], warnings: [], authority_domain_candidates: prepared.authorityDomainCandidates, read_back_verified: false, proposal };
    const beforeHash = computeBootstrapPreimageHash(root2, proposal);
    const markerPath = targetPath(root2, BOOTSTRAP_SUPPORT_MARKER_RELATIVE_PATH);
    try {
      fs14.mkdirSync(path15.dirname(markerPath), { recursive: true });
      fs14.writeFileSync(markerPath, markerValue(proposal), "utf8");
      const runtimeResult = applyBootstrapProjectProposal(root2, proposal, { verify: () => verifyBootstrapHealth(root2, proposal, distribution) });
      if (fs14.existsSync(markerPath))
        fs14.rmSync(markerPath, { force: true });
      return { status: "installed", target_root: root2, target_state: classification.state, target_identity: proposal.target_identity, mode: proposal.mode, project: prepared.project, host: prepared.host, source: { revision: proposal.source_revision, tree_hash: proposal.source_tree_hash }, planned_writes: prepared.plannedWrites, planned_directories: [], planned_deletes: [], changed_paths: options.changedPaths, blockers: [], warnings: classification.reasons, authority_domain_candidates: prepared.authorityDomainCandidates, read_back_verified: runtimeResult.read_back_verified, runtime_result: runtimeResult, proposal };
    } catch (error) {
      let rollbackVerified = false;
      try {
        rollbackVerified = computeBootstrapPreimageHash(root2, proposal) === beforeHash;
      } catch {
        rollbackVerified = false;
      }
      if (rollbackVerified && fs14.existsSync(markerPath))
        fs14.rmSync(markerPath, { force: true });
      return { status: "blocked", target_root: root2, target_state: classification.state, target_identity: proposal.target_identity, mode: proposal.mode, project: prepared.project, host: prepared.host, source: { revision: proposal.source_revision, tree_hash: proposal.source_tree_hash }, planned_writes: rollbackVerified ? [] : prepared.plannedWrites, planned_directories: [], planned_deletes: [], changed_paths: options.changedPaths, blockers: [{ code: error instanceof BootstrapSupportError ? error.code : "BOOTSTRAP_SUPPORT_TRANSACTION_FAILED", message: error instanceof Error ? error.message : String(error) }], warnings: [{ code: rollbackVerified ? "ROLLBACK_VERIFIED" : "RECOVERY_REQUIRED", message: rollbackVerified ? "Bootstrap-owned governance scope was restored and its interruption marker was cleared." : "Bootstrap rollback could not be verified; interruption marker retained for explicit recovery." }], authority_domain_candidates: prepared.authorityDomainCandidates, read_back_verified: false, proposal };
    }
  } catch (error) {
    return { status: "blocked", target_root: root2, target_state: classification.state, target_identity: computeBootstrapTargetIdentity(root2), mode: options.mode, project: { name: "unknown", slug: "unknown" }, host: options.host ?? "codex", source: { revision: `distribution-${distribution.manifest_digest.slice(0, 16)}`, tree_hash: distribution.manifest_digest }, planned_writes: [], planned_directories: [], planned_deletes: [], changed_paths: options.changedPaths ?? [], blockers: [{ code: error instanceof BootstrapSupportError ? error.code : "BOOTSTRAP_SUPPORT_PREPARATION_FAILED", message: error instanceof Error ? error.message : String(error) }], warnings: classification.reasons.map((message) => ({ code: "TARGET_STATE", message })), read_back_verified: false };
  }
}
function parseCliFlags(argv) {
  const [command = "prepare", ...rest] = argv;
  const flags = new Map;
  for (let index = 0;index < rest.length; index += 1) {
    const arg = rest[index];
    if (!arg?.startsWith("--"))
      fail5("BOOTSTRAP_SUPPORT_INPUT_INVALID", `unexpected argument: ${arg ?? ""}`);
    const key = arg.slice(2);
    const value = rest[index + 1];
    if (value && !value.startsWith("--")) {
      flags.set(key, value);
      index += 1;
    } else
      flags.set(key, true);
  }
  return { command, flags };
}
function flagString(flags, key, required = false) {
  const value = flags.get(key);
  if (typeof value === "string" && value.trim())
    return value;
  if (value === true)
    fail5("BOOTSTRAP_SUPPORT_INPUT_INVALID", `--${key} requires a value.`);
  if (required)
    fail5("BOOTSTRAP_SUPPORT_INPUT_INVALID", `--${key} requires a value.`);
  return;
}
function readStringListFile(filePath) {
  const content = fs14.readFileSync(path15.resolve(filePath), "utf8");
  if (content.trimStart().startsWith("[")) {
    const parsed = JSON.parse(content);
    if (!Array.isArray(parsed) || parsed.some((value) => typeof value !== "string"))
      fail5("BOOTSTRAP_SUPPORT_INPUT_INVALID", "changed paths file JSON form must be an array of strings.");
    return parsed;
  }
  return content.split(/\r?\n/u).map((line) => line.trim()).filter(Boolean);
}
function readFactsFile(filePath) {
  const value = JSON.parse(fs14.readFileSync(path15.resolve(filePath), "utf8"));
  if (!Array.isArray(value))
    fail5("BOOTSTRAP_SUPPORT_INPUT_INVALID", "facts file must contain an array.");
  return value;
}
function readBaselineFile(filePath) {
  const value = JSON.parse(fs14.readFileSync(path15.resolve(filePath), "utf8"));
  return expectRecord4(value, "design baseline file");
}
function readAuthorizationsFile(filePath) {
  const value = JSON.parse(fs14.readFileSync(path15.resolve(filePath), "utf8"));
  if (!Array.isArray(value))
    fail5("BOOTSTRAP_SUPPORT_INPUT_INVALID", "conditional authorizations file must contain an array.");
  return value;
}
function readAuthorityDomainCandidatesFile(filePath) {
  return normalizeAuthorityDomainCandidates(JSON.parse(fs14.readFileSync(path15.resolve(filePath), "utf8")), "authority_domain_candidates file");
}
function readAuthorityDomainConfirmationFile(filePath) {
  return normalizeAuthorityDomainConfirmation(JSON.parse(fs14.readFileSync(path15.resolve(filePath), "utf8")));
}
async function runBootstrapSupportCli(argv = process.argv.slice(2)) {
  try {
    const { command, flags } = parseCliFlags(argv);
    if (command === "help" || flags.has("help")) {
      console.log([
        "Target-local Vibe Governance Bootstrap support",
        "",
        "Usage:",
        "  node .workflow-system/runtime/dist/cli.js bootstrap-support prepare --root <project> --mode <design|greenfield|inventory|adopt|realign> [--write] [--changed-paths-file <file>] [--authority-domain-candidates-file <file>] [--authority-domain-confirmation-file <file>] [--json]",
        "",
        "The support layer prepares a typed proposal from installed immutable bytes; the project-local Runtime commits governance assets."
      ].join(`
`));
      return 0;
    }
    if (command !== "prepare")
      fail5("BOOTSTRAP_SUPPORT_INPUT_INVALID", `unknown bootstrap-support command: ${command}`);
    const mode = flagString(flags, "mode", true);
    if (!BOOTSTRAP_MODES.includes(mode))
      fail5("BOOTSTRAP_SUPPORT_MODE_INVALID", "mode must be one of the closed Bootstrap modes.");
    const targetRoot = flagString(flags, "root") ?? process.cwd();
    const changedPathsFile = flagString(flags, "changed-paths-file");
    const factsFile = flagString(flags, "facts-file");
    const baselineFile = flagString(flags, "design-baseline-file");
    const authorizationsFile = flagString(flags, "conditional-authorizations-file");
    const authorityDomainCandidatesFile = flagString(flags, "authority-domain-candidates-file");
    const authorityDomainConfirmationFile = flagString(flags, "authority-domain-confirmation-file");
    const result = bootstrapProjectTargetLocal({
      targetRoot,
      mode,
      write: flags.get("write") === true,
      projectName: flagString(flags, "project-name"),
      projectSlug: flagString(flags, "project-slug"),
      host: flagString(flags, "host") ?? "codex",
      designBaseline: baselineFile ? readBaselineFile(baselineFile) : undefined,
      designConfirmed: flags.get("confirm-design") === true || flags.get("confirm") === true,
      confirmedFacts: factsFile ? readFactsFile(factsFile) : undefined,
      inventoryFacts: factsFile ? readFactsFile(factsFile) : undefined,
      adoptionConfirmed: flags.get("confirm-adoption") === true || flags.get("confirm") === true,
      changedPaths: changedPathsFile ? readStringListFile(changedPathsFile) : undefined,
      conditionalAuthorizations: authorizationsFile ? readAuthorizationsFile(authorizationsFile) : undefined,
      authorityDomainCandidates: authorityDomainCandidatesFile ? readAuthorityDomainCandidatesFile(authorityDomainCandidatesFile) : undefined,
      authorityDomainConfirmation: authorityDomainConfirmationFile ? readAuthorityDomainConfirmationFile(authorityDomainConfirmationFile) : undefined
    });
    console.log(JSON.stringify(publicPlan(result), null, 2));
    return ["needs-confirmation", "ready", "installed", "replayed"].includes(result.status) ? 0 : 1;
  } catch (error) {
    console.error(error instanceof Error ? error.message : String(error));
    return 1;
  }
}

// runtime/vnext/src/prepare-task-adapter.ts
import * as crypto14 from "crypto";
import * as fs15 from "fs";
import * as path16 from "path";
var PREPARE_TASK_ADAPTER_COMMANDS = [
  "prepare-draft",
  "prepare-successor",
  "record-human-acceptance",
  "record-evidence-waiver",
  "confirm-draft",
  "clear-resume-review",
  "replan",
  "prepare-replan",
  "confirm-replan",
  "discard-replan",
  "initialize-preservation",
  "suspend-recovery",
  "prepare-scope-amendment",
  "discard-scope-amendment"
];
var SEMANTIC_DRAFT_FIELDS = [
  "task_basis",
  "goal",
  "claim_evidence",
  "out_of_scope",
  "design_decisions",
  "mutation_scope",
  "mutation_authority_version",
  "mutation_authority",
  "test_strategy",
  "implementation_steps",
  "validation_plan",
  "persistent_tests"
];
var STEP_ID_PATTERN3 = /^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/u;
var WINDOWS_ABSOLUTE_PATH = /^[A-Za-z]:[\\/]/u;
var MAX_ITEMS = 256;
function fail6(code, message) {
  throw new VNextRuntimeError(code, message);
}
function record4(value, location) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", `${location} must be an object.`);
  }
  return value;
}
function exactKeys2(value, expected, location) {
  const allowed = new Set(expected);
  const missing = expected.filter((key) => !(key in value));
  const unexpected = Object.keys(value).filter((key) => !allowed.has(key));
  if (missing.length > 0 || unexpected.length > 0) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", `${location} keys mismatch; missing=[${missing.join(", ")}], unexpected=[${unexpected.join(", ")}].`);
  }
}
function text3(value, location, maximumLength = 4096) {
  if (typeof value !== "string")
    fail6("PREPARE_ADAPTER_INPUT_INVALID", `${location} must be a string.`);
  const normalized = value.trim();
  if (!normalized || normalized.length > maximumLength || /[\r\n]/u.test(normalized)) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", `${location} must be one non-empty line of at most ${maximumLength} characters.`);
  }
  return normalized;
}
function verbatim(value, location, maximumLength = 32768) {
  if (typeof value !== "string" || value.trim().length === 0 || value.length > maximumLength || /\0/u.test(value)) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", `${location} must be non-empty verbatim text of at most ${maximumLength} characters.`);
  }
  return value;
}
function normalizeTaskBasisSource(value, location) {
  const source = record4(value, location);
  exactKeys2(source, ["source", "verbatim"], location);
  return {
    source: text3(source.source, `${location}.source`, 1024),
    verbatim: verbatim(source.verbatim, `${location}.verbatim`)
  };
}
function normalizeTaskBasis(value) {
  const basis = record4(value, "task_basis");
  exactKeys2(basis, ["original_request", "user_decisions"], "task_basis");
  if (!Array.isArray(basis.user_decisions) || basis.user_decisions.length > 64) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", "task_basis.user_decisions must be a bounded array.");
  }
  const userDecisions = basis.user_decisions.map((item, index) => normalizeTaskBasisSource(item, `task_basis.user_decisions[${index}]`));
  const keys = userDecisions.map((item) => `${item.source}\x00${item.verbatim}`);
  if (new Set(keys).size !== keys.length) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", "task_basis.user_decisions must not contain duplicate source excerpts.");
  }
  return {
    original_request: normalizeTaskBasisSource(basis.original_request, "task_basis.original_request"),
    user_decisions: userDecisions
  };
}
function textList(value, location, allowEmpty) {
  if (!Array.isArray(value) || value.length > MAX_ITEMS || !allowEmpty && value.length === 0) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", `${location} must be a bounded${allowEmpty ? "" : " non-empty"} array.`);
  }
  const values = value.map((item, index) => text3(item, `${location}[${index}]`));
  if (new Set(values).size !== values.length) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", `${location} must not contain duplicates.`);
  }
  return values;
}
function normalizeScopePath(value, location, allowGlob) {
  const original = text3(value, location, 1024);
  const normalized = original.replace(/\\/gu, "/").replace(/^\.\//u, "");
  if (normalized.startsWith("/") || WINDOWS_ABSOLUTE_PATH.test(original) || normalized.split("/").includes("..") || normalized.includes("\x00") || !allowGlob && normalized.includes("*")) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", `${location} must be a repository-relative ${allowGlob ? "path or glob" : "exact path"} without traversal.`);
  }
  return normalized;
}
function normalizeScopePathList(value, location) {
  if (!Array.isArray(value) || value.length > MAX_ITEMS) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", `${location} must be a bounded array.`);
  }
  const paths = value.map((item, index) => normalizeScopePath(item, `${location}[${index}]`, true));
  if (new Set(paths).size !== paths.length)
    fail6("PREPARE_ADAPTER_INPUT_INVALID", `${location} must not contain duplicates.`);
  return paths;
}
function sha256Revision(value, location) {
  const normalized = text3(value, location, 64);
  if (!/^[a-f0-9]{64}$/u.test(normalized)) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", `${location} must be a SHA-256 revision.`);
  }
  return normalized;
}
function normalizeConfirmationReceipt(input) {
  const source = record4(input, "confirmation_receipt");
  exactKeys2(source, ["kind", "task_id", "document_id", "draft_revision"], "confirmation_receipt");
  if (source.kind !== "prepare-draft-confirmation/v1") {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", "confirmation_receipt.kind must be prepare-draft-confirmation/v1.");
  }
  return {
    kind: source.kind,
    task_id: text3(source.task_id, "confirmation_receipt.task_id", 128),
    document_id: text3(source.document_id, "confirmation_receipt.document_id", 128),
    draft_revision: sha256Revision(source.draft_revision, "confirmation_receipt.draft_revision")
  };
}
function normalizeResumeReadinessReceipt(input) {
  const source = record4(input, "readiness_receipt");
  exactKeys2(source, ["kind", "task_id", "document_id", "source_revision", "reviewed_reasons", "evidence_refs"], "readiness_receipt");
  if (source.kind !== "resume-readiness/v1") {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", "readiness_receipt.kind must be resume-readiness/v1.");
  }
  return {
    kind: source.kind,
    task_id: text3(source.task_id, "readiness_receipt.task_id", 128),
    document_id: text3(source.document_id, "readiness_receipt.document_id", 128),
    source_revision: sha256Revision(source.source_revision, "readiness_receipt.source_revision"),
    reviewed_reasons: textList(source.reviewed_reasons, "readiness_receipt.reviewed_reasons", false),
    evidence_refs: textList(source.evidence_refs, "readiness_receipt.evidence_refs", false)
  };
}
function normalizeStepCommands(value, location) {
  if (!Array.isArray(value) || value.length > MAX_ITEMS) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", `${location} must be a bounded array.`);
  }
  return value.map((item, index) => {
    const itemLocation = `${location}[${index}]`;
    const candidate = record4(item, itemLocation);
    exactKeys2(candidate, ["command", "expected_repo_writes"], itemLocation);
    if (candidate.expected_repo_writes === "none") {
      return {
        command: text3(candidate.command, `${itemLocation}.command`),
        expected_repo_writes: "none"
      };
    }
    const expectedRepoWrites = normalizeScopePathList(candidate.expected_repo_writes, `${itemLocation}.expected_repo_writes`);
    if (expectedRepoWrites.length === 0) {
      fail6("PREPARE_ADAPTER_INPUT_INVALID", `${itemLocation}.expected_repo_writes must be none or a non-empty bounded array.`);
    }
    return {
      command: text3(candidate.command, `${itemLocation}.command`),
      expected_repo_writes: expectedRepoWrites
    };
  });
}
function commandTransformationKind(command) {
  return command.expected_repo_writes !== "none" && command.expected_repo_writes.some((item) => item.includes("*")) ? "inherently-broad" : "localized";
}
function stepScopeAdmitsCommandTarget(target, stepScope) {
  return target.includes("*") ? stepScope.includes(target) : stepScope.some((pattern) => mutationScopePatternMatchesPath(target, pattern));
}
function normalizeSemanticDraft(root2, input) {
  const source = record4(input, "prepare-task semantic draft");
  const v2 = source.mutation_authority !== undefined || source.mutation_authority_version === MUTATION_AUTHORITY_VERSION;
  const allowedDraftFields = SEMANTIC_DRAFT_FIELDS.filter((key) => key !== "mutation_scope" && key !== "mutation_authority_version" && key !== "mutation_authority");
  const scopeFields = v2 ? ["mutation_authority_version", "mutation_authority"] : ["mutation_scope", ...source.mutation_authority_version === undefined ? [] : ["mutation_authority_version"]];
  exactKeys2(source, [...allowedDraftFields, ...scopeFields, ...["project_documents", "affected_contracts"].filter((key) => (key in source))], "prepare-task semantic draft");
  if ("project_documents" in source !== "affected_contracts" in source) {
    fail6("PROJECT_DOCUMENTS_INVALID", "Supply project_documents and affected_contracts together, using [] where applicable.");
  }
  const designDecisions = record4(source.design_decisions, "design_decisions");
  exactKeys2(designDecisions, ["decided", "unresolved"], "design_decisions");
  const decided = textList(designDecisions.decided, "design_decisions.decided", true);
  const unresolved = textList(designDecisions.unresolved, "design_decisions.unresolved", true);
  const duplicatedDecisions = decided.filter((item) => unresolved.includes(item));
  if (duplicatedDecisions.length > 0) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", "a design decision cannot be both decided and unresolved.");
  }
  let authority;
  let allowed = [];
  let conditional = [];
  let forbidden = [];
  if (v2) {
    if (source.mutation_authority_version !== MUTATION_AUTHORITY_VERSION || source.mutation_authority === undefined) {
      fail6("MUTATION_AUTHORITY_VERSION_REQUIRED", "v2 semantic drafts must declare mutation_authority_version=2 together with mutation_authority.");
    }
    try {
      authority = normalizeTaskMutationAuthority(source.mutation_authority ?? { domains: [], exact_exceptions: [], forbidden: [] });
    } catch (error) {
      fail6(error instanceof MutationAuthorityError ? error.code : "MUTATION_AUTHORITY_SCHEMA_INVALID", error instanceof Error ? error.message : String(error));
    }
    forbidden = authority.forbidden;
  } else {
    if (source.mutation_authority_version !== undefined && source.mutation_authority_version !== 1) {
      fail6("MUTATION_AUTHORITY_VERSION_UNSUPPORTED", "legacy semantic drafts may declare only mutation_authority_version=1.");
    }
    const mutationScope = record4(source.mutation_scope, "mutation_scope");
    exactKeys2(mutationScope, ["allowed", "conditional", "forbidden"], "mutation_scope");
    allowed = normalizeScopePathList(mutationScope.allowed, "mutation_scope.allowed");
    if (allowed.length === 0)
      fail6("PREPARE_ADAPTER_INPUT_INVALID", "mutation_scope.allowed must contain at least one executable target.");
    forbidden = normalizeScopePathList(mutationScope.forbidden, "mutation_scope.forbidden");
    if (!Array.isArray(mutationScope.conditional) || mutationScope.conditional.length > MAX_ITEMS) {
      fail6("PREPARE_ADAPTER_INPUT_INVALID", "mutation_scope.conditional must be a bounded array.");
    }
    conditional = mutationScope.conditional.map((item, index) => {
      const candidate = record4(item, `mutation_scope.conditional[${index}]`);
      exactKeys2(candidate, ["path", "condition"], `mutation_scope.conditional[${index}]`);
      return {
        path: normalizeScopePath(candidate.path, `mutation_scope.conditional[${index}].path`, true),
        condition: text3(candidate.condition, `mutation_scope.conditional[${index}].condition`)
      };
    });
    if (new Set(conditional.map((item) => item.path)).size !== conditional.length) {
      fail6("PREPARE_ADAPTER_INPUT_INVALID", "mutation_scope.conditional must not contain duplicate paths.");
    }
  }
  const testStrategySource = record4(source.test_strategy, "test_strategy");
  exactKeys2(testStrategySource, ["mode", "source", "source_ref", "task_classification", "rationale"], "test_strategy");
  if (!TEST_STRATEGY_MODES.includes(testStrategySource.mode)) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", `test_strategy.mode must be one of ${TEST_STRATEGY_MODES.join(", ")}.`);
  }
  if (!TEST_STRATEGY_SOURCES.includes(testStrategySource.source)) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", `test_strategy.source must be one of ${TEST_STRATEGY_SOURCES.join(", ")}.`);
  }
  if (!TEST_STRATEGY_CLASSIFICATIONS.includes(testStrategySource.task_classification)) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", `test_strategy.task_classification must be one of ${TEST_STRATEGY_CLASSIFICATIONS.join(", ")}.`);
  }
  const testStrategy = {
    mode: testStrategySource.mode,
    source: testStrategySource.source,
    source_ref: text3(testStrategySource.source_ref, "test_strategy.source_ref", 1024),
    task_classification: testStrategySource.task_classification,
    rationale: text3(testStrategySource.rationale, "test_strategy.rationale")
  };
  if (!Array.isArray(source.implementation_steps) || source.implementation_steps.length === 0 || source.implementation_steps.length > MAX_ITEMS) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", "implementation_steps must be a bounded non-empty array.");
  }
  const implementationSteps = source.implementation_steps.map((item, index) => {
    const step = record4(item, `implementation_steps[${index}]`);
    const stepTargetKey = v2 ? "planned_mutation_targets" : "mutation_scope";
    exactKeys2(step, ["id", "description", stepTargetKey, "commands", "validation", ...step.review_checkpoint === undefined ? [] : ["review_checkpoint"]], `implementation_steps[${index}]`);
    const checkpoint = step.review_checkpoint === undefined ? { policy: "required", reason: "Review this logical boundary against the confirmed task" } : record4(step.review_checkpoint, "review_checkpoint");
    exactKeys2(checkpoint, ["policy", "reason"], "review_checkpoint");
    if (!["required", "not-required"].includes(String(checkpoint.policy)))
      fail6("PREPARE_ADAPTER_INPUT_INVALID", "review_checkpoint.policy is invalid.");
    const id = text3(step.id, `implementation_steps[${index}].id`, 128);
    if (!STEP_ID_PATTERN3.test(id))
      fail6("PREPARE_ADAPTER_INPUT_INVALID", `implementation_steps[${index}].id is invalid.`);
    return {
      id,
      review_checkpoint: { policy: checkpoint.policy, reason: text3(checkpoint.reason, "review_checkpoint.reason") },
      description: text3(step.description, `implementation_steps[${index}].description`),
      mutation_scope: normalizeScopePathList(step[stepTargetKey], `implementation_steps[${index}].${stepTargetKey}`),
      ...v2 ? { planned_mutation_targets: normalizeScopePathList(step.planned_mutation_targets, `implementation_steps[${index}].planned_mutation_targets`) } : {},
      commands: normalizeStepCommands(step.commands, `implementation_steps[${index}].commands`),
      validation: textList(step.validation, `implementation_steps[${index}].validation`, false)
    };
  });
  if (new Set(implementationSteps.map((step) => step.id)).size !== implementationSteps.length) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", "implementation_steps must not contain duplicate IDs.");
  }
  if (implementationSteps.some((step) => step.mutation_scope.length === 0)) {
    fail6("PREPARE_ADAPTER_INPUT_INVALID", "every implementation step must name at least one mutation-scope target.");
  }
  let persistentTests;
  if (source.persistent_tests === "none") {
    persistentTests = "none";
  } else {
    if (!Array.isArray(source.persistent_tests) || source.persistent_tests.length === 0 || source.persistent_tests.length > MAX_ITEMS) {
      fail6("PREPARE_ADAPTER_INPUT_INVALID", 'persistent_tests must be "none" or a bounded non-empty array.');
    }
    persistentTests = source.persistent_tests.map((item, index) => {
      const test = record4(item, `persistent_tests[${index}]`);
      exactKeys2(test, ["path", "proves", "owner", "owner_source", "source_ref", "basis", "existing_evidence_insufficiency", "assertion_boundary", "failure_disposition"], `persistent_tests[${index}]`);
      return {
        owner: text3(test.owner, "persistent_tests.owner"),
        owner_source: text3(test.owner_source, "persistent_tests.owner_source"),
        source_ref: text3(test.source_ref, "persistent_tests.source_ref"),
        basis: text3(test.basis, "persistent_tests.basis"),
        existing_evidence_insufficiency: text3(test.existing_evidence_insufficiency, "persistent_tests.existing_evidence_insufficiency"),
        assertion_boundary: text3(test.assertion_boundary, "persistent_tests.assertion_boundary"),
        failure_disposition: text3(test.failure_disposition, "persistent_tests.failure_disposition"),
        path: normalizeScopePath(test.path, `persistent_tests[${index}].path`, false),
        proves: textList(test.proves, `persistent_tests[${index}].proves`, false)
      };
    });
    if (new Set(persistentTests.map((test) => test.path)).size !== persistentTests.length) {
      fail6("PREPARE_ADAPTER_INPUT_INVALID", "persistent_tests must not contain duplicate paths.");
    }
  }
  const normalized = {
    ...source.project_documents === undefined ? {} : { project_documents: normalizeProjectDocuments(source.project_documents) },
    ...source.affected_contracts === undefined ? {} : { affected_contracts: textList(source.affected_contracts, "affected_contracts", true) },
    task_basis: normalizeTaskBasis(source.task_basis),
    goal: text3(source.goal, "goal", 512),
    claim_evidence: validateClaimEvidence(source.claim_evidence, "claim_evidence"),
    out_of_scope: textList(source.out_of_scope, "out_of_scope", true),
    design_decisions: { decided, unresolved },
    mutation_scope: {
      allowed: v2 ? [...new Set(implementationSteps.flatMap((step) => step.mutation_scope ?? []))] : allowed,
      conditional,
      forbidden
    },
    ...v2 ? { mutation_authority_version: 2, mutation_authority: authority } : {},
    test_strategy: testStrategy,
    implementation_steps: implementationSteps,
    validation_plan: textList(source.validation_plan, "validation_plan", false),
    persistent_tests: persistentTests
  };
  assertSemanticScopeIsExecutable(normalized);
  assertEvidencePlan(semanticDraftDefinition(normalized), normalized.claim_evidence, true, { root: root2, taskBasis: normalized.task_basis, previous: [] });
  return normalized;
}
function markdownBullets(items, checklist = false) {
  if (items.length === 0)
    return "- none";
  return items.map((item) => checklist ? `- [ ] ${item}` : `- ${item}`).join(`
`);
}
function semanticMutationScope(input) {
  if (!input.mutation_scope)
    fail6("PREPARE_ADAPTER_INPUT_INVALID", "the compatibility mutation_scope projection is missing.");
  return input.mutation_scope;
}
function semanticStepTargets(step) {
  return [...step.planned_mutation_targets ?? step.mutation_scope ?? []];
}
function scopeBody(input) {
  const mutationScope = semanticMutationScope(input);
  const persistentTests = input.persistent_tests === "none" ? ["- none"] : input.persistent_tests.map((test) => `- \`${test.path}\``);
  return [
    "## 允许修改范围",
    "",
    "### Allowed Files",
    "",
    markdownBullets(mutationScope.allowed.map((item) => `\`${item}\``)),
    "",
    "### Conditional Files",
    "",
    markdownBullets(mutationScope.conditional.map((item) => `\`${item.path}\` when ${item.condition}`)),
    "",
    "## 禁止修改范围",
    "",
    "### Forbidden Files",
    "",
    markdownBullets(mutationScope.forbidden.map((item) => `\`${item}\``)),
    "",
    "## 回归检查项",
    "",
    "### Persistent Tests",
    "",
    ...persistentTests,
    ""
  ].join(`
`);
}
function assertSemanticScopeIsExecutable(input) {
  if (input.mutation_authority_version === MUTATION_AUTHORITY_VERSION)
    return;
  const mutationScope = semanticMutationScope(input);
  const scope = parseMutationScope(scopeBody(input));
  const persistentTests = input.persistent_tests === "none" ? [] : input.persistent_tests;
  const persistentTestPaths = new Set(persistentTests.map((test) => test.path));
  const allowedExact = new Set(mutationScope.allowed.filter((item) => !item.includes("*")));
  for (const test of persistentTests) {
    if (!allowedExact.has(test.path)) {
      fail6("PERSISTENT_TEST_SCOPE_INVALID", `persistent test ${test.path} must also appear as an exact mutation_scope.allowed entry.`);
    }
  }
  const testLikeScopeEntries = [
    ...mutationScope.allowed,
    ...mutationScope.conditional.map((item) => item.path)
  ].filter(isLikelyPersistentTestPath);
  for (const entry of testLikeScopeEntries) {
    if (entry.includes("*") || !persistentTestPaths.has(entry)) {
      fail6("PERSISTENT_TEST_SCOPE_INVALID", `test-like mutation scope entry ${entry} is not an exact path in persistent_tests.`);
    }
  }
  if (persistentTests.length > 0) {
    const result = evaluateMutationScope(scope, { changed_paths: persistentTests.map((test) => test.path) });
    if (result.status !== "pass") {
      fail6("PERSISTENT_TEST_SCOPE_INVALID", `persistent test scope is not executable: ${result.blockers.join(" ")}`);
    }
  }
  for (const step of input.implementation_steps) {
    for (const target of semanticStepTargets(step)) {
      const forbidden = mutationScope.forbidden.some((pattern) => target === pattern || !target.includes("*") && mutationScopePatternMatchesPath(target, pattern));
      if (forbidden)
        fail6("STEP_SCOPE_INVALID", `step ${step.id} target ${target} is forbidden.`);
      const allowed = mutationScope.allowed.includes(target);
      const conditional = mutationScope.conditional.some((item) => target === item.path || !target.includes("*") && mutationScopePatternMatchesPath(target, item.path));
      if (!allowed && !conditional) {
        fail6("STEP_SCOPE_INVALID", `step ${step.id} target ${target} is outside Mutation scope.`);
      }
      if (allowed && conditional) {
        fail6("STEP_SCOPE_INVALID", `step ${step.id} target ${target} is ambiguously both Allowed and Conditional.`);
      }
    }
    for (const [commandIndex, command] of step.commands.entries()) {
      if (command.expected_repo_writes === "none")
        continue;
      const outsideStepScope = command.expected_repo_writes.filter((target) => !stepScopeAdmitsCommandTarget(target, semanticStepTargets(step)));
      if (outsideStepScope.length > 0) {
        fail6("COMMAND_FOOTPRINT_BLOCKED", `step ${step.id} command "${command.command}" writes outside the step mutation scope: ${outsideStepScope.join(", ")}.`);
      }
      const result = evaluateCommandWriteFootprint(scope, {
        command: command.command,
        expected_write_footprint: {
          kind: "bounded",
          targets: command.expected_repo_writes,
          evidence_refs: [`adapter:prepare-draft:command:${step.id}:${commandIndex + 1}`]
        },
        transformation_kind: commandTransformationKind(command)
      });
      if (result.status !== "pass") {
        fail6("COMMAND_FOOTPRINT_BLOCKED", `step ${step.id} command "${command.command}" has no executable repository-write plan: ${result.blockers.join(" ")}`);
      }
    }
  }
}
function semanticDigest(input) {
  return crypto14.createHash("sha256").update(JSON.stringify(input)).digest("hex");
}
function adapterIdempotencyKey(prefix, value) {
  const digest = crypto14.createHash("sha256").update(JSON.stringify(value)).digest("hex");
  return `${prefix}-${digest.slice(0, 48)}`;
}
function taskSlug(goal) {
  const ascii = goal.normalize("NFKD").toLowerCase().replace(/[^a-z0-9]+/gu, "-").replace(/^-+|-+$/gu, "").slice(0, 80).replace(/-+$/gu, "");
  if (ascii)
    return ascii;
  return `task-${crypto14.createHash("sha256").update(goal).digest("hex").slice(0, 12)}`;
}
function authority(current, subject, kinds) {
  return kinds.map((kind) => ({ kind, source: current.relativePath, subject }));
}
function claimEvidence(input) {
  return structuredClone(input.claim_evidence);
}
function assertSemanticAuthority(root2, input) {
  if (input.mutation_authority_version !== MUTATION_AUTHORITY_VERSION || !input.mutation_authority)
    return;
  try {
    validateTaskMutationAuthority(root2, input.mutation_authority);
  } catch (error) {
    fail6(error instanceof MutationAuthorityError ? error.code : "MUTATION_AUTHORITY_PROJECT_INVALID", error instanceof Error ? error.message : String(error));
  }
}
function semanticDraftDefinition(input) {
  const mutationScope = semanticMutationScope(input);
  const persistentTests = input.persistent_tests === "none" ? ["- none"] : input.persistent_tests.flatMap((test) => [
    `- \`${test.path}\``,
    ...test.proves.map((proof) => `  - proves: ${proof}`),
    `  - owner: ${test.owner}`,
    `  - owner_source: ${test.owner_source}`,
    `  - source_ref: ${test.source_ref}`,
    `  - basis: ${test.basis}`,
    `  - existing_evidence_insufficiency: ${test.existing_evidence_insufficiency}`,
    `  - assertion_boundary: ${test.assertion_boundary}`,
    `  - failure_disposition: ${test.failure_disposition}`
  ]);
  return {
    background_context: [
      "### Goal",
      "",
      input.goal,
      "",
      "### Out of scope",
      "",
      markdownBullets(input.out_of_scope)
    ].join(`
`) + renderProjectDocuments(input.project_documents),
    acceptance: markdownBullets(input.claim_evidence.filter((claim) => claim.claim_kind === "acceptance").map((claim) => claim.requirement), true),
    allowed_scope: markdownBullets(mutationScope.allowed.map((item) => `\`${item}\``)),
    conditional_scope: markdownBullets(mutationScope.conditional.map((item) => `\`${item.path}\` when ${item.condition}`)),
    forbidden_scope: markdownBullets(mutationScope.forbidden.map((item) => `\`${item}\``)),
    affected_contracts: markdownBullets(input.affected_contracts ?? []),
    confirmed_decisions: markdownBullets(input.design_decisions.decided),
    open_questions: markdownBullets(input.design_decisions.unresolved),
    implementation_plan: input.implementation_steps.map((step) => `- ${step.id}: ${step.description}`).join(`
`),
    implementation_steps: input.implementation_steps.flatMap((step) => [
      `- ${step.id}: ${step.description}`,
      `  - purpose: ${step.description}`,
      `  - ${input.mutation_authority_version === MUTATION_AUTHORITY_VERSION ? "planned_mutation_targets" : "mutation_scope"}: ${semanticStepTargets(step).join(", ")}`,
      `  - required_evidence: ${step.validation.join("; ")}`,
      `  - review_checkpoint: ${step.review_checkpoint?.policy ?? "required"}: ${step.review_checkpoint?.reason ?? "Review this logical boundary against the confirmed task"}`,
      ...step.commands.flatMap((item) => [
        `  - planned_command: ${item.command}`,
        `    - expected_repo_writes: ${item.expected_repo_writes === "none" ? "none" : item.expected_repo_writes.join(", ")}`,
        `    - transformation_kind: ${commandTransformationKind(item)}`
      ])
    ]).join(`
`),
    regression_checks: [
      "### Test Strategy",
      "",
      `- mode: ${input.test_strategy.mode}`,
      `- source: ${input.test_strategy.source}`,
      `- source_ref: ${input.test_strategy.source_ref}`,
      `- task_classification: ${input.test_strategy.task_classification}`,
      `- rationale: ${input.test_strategy.rationale}`,
      "",
      "### Validation Plan",
      "",
      markdownBullets(input.validation_plan, true),
      "",
      "### Persistent Tests",
      "",
      ...persistentTests
    ].join(`
`),
    rollback_points: "- Revert only the current step changes if its required validation cannot pass.",
    design_constraints: null,
    post_release_validation: null,
    propagation_governance: null,
    ...input.mutation_authority_version === MUTATION_AUTHORITY_VERSION && input.mutation_authority ? { mutation_authority_version: MUTATION_AUTHORITY_VERSION, mutation_authority: input.mutation_authority } : {}
  };
}
function verifyAdapterReadBack(root2, result, options) {
  if (options.dryRun || result.status !== "success" && result.status !== "no-op")
    return result;
  const readBack = readCanonicalCurrentTask(root2);
  if (!result.read_back_verified || result.resulting_revision !== readBack.sourceTuple.revision) {
    fail6("PREPARE_ADAPTER_READ_BACK_FAILED", "prepare-task adapter could not verify the committed canonical CURRENT_TASK revision.");
  }
  return result;
}
function sameValue(left, right) {
  return JSON.stringify(left) === JSON.stringify(right);
}
function currentMatchesSemanticDraft(root2, current, semantic) {
  let basisMatches = false;
  try {
    basisMatches = sameValue(readCanonicalTaskBasis(root2, current).basis, semantic.task_basis);
  } catch {
    basisMatches = false;
  }
  return current.runtimeState.active_step_id === semantic.implementation_steps[0].id && basisMatches && sameValue(readDraftDefinitionFromBody(current.body), semanticDraftDefinition(semantic)) && current.runtimeState.business_evidence_version === 1 && !!current.runtimeState.evidence_plan_revision && sameValue(current.runtimeState.claim_evidence, claimEvidence(semantic));
}
function semanticNoOp(current, idempotencyKey, message, options = {}) {
  const state = current.runtimeState;
  return {
    status: "no-op",
    operation_kind: "task-state-transaction",
    idempotency_key: idempotencyKey,
    target_path: current.relativePath,
    dry_run: options.dryRun === true,
    committed: false,
    message,
    previous_revision: current.sourceTuple.revision,
    resulting_revision: current.sourceTuple.revision,
    planned_writes: [],
    governed_mutation_count: 0,
    read_back_verified: true,
    state: {
      task_id: state.task_id,
      workflow_status: state.workflow_status,
      lifecycle_state: state.lifecycle_state,
      resume_requires_review: state.resume_requires_review,
      resume_review_reasons: [...state.resume_review_reasons],
      active_step_id: state.active_step_id,
      active_step_status: state.active_step_status,
      finding_queue_revision: state.finding_queue_revision,
      review_cycle_id: state.review_cycle.id,
      repair_round: state.review_cycle.repair_round
    }
  };
}
function confirmationReceipt(current) {
  return {
    kind: "prepare-draft-confirmation/v1",
    task_id: current.runtimeState.task_id,
    document_id: current.sourceTuple.document_id,
    draft_revision: current.sourceTuple.revision
  };
}
function withConfirmationReceipt(root2, result, options) {
  if (options.dryRun || result.status !== "success" && result.status !== "no-op")
    return result;
  const current = readCanonicalCurrentTask(root2);
  if (current.runtimeState.workflow_status !== "draft" || current.runtimeState.lifecycle_state !== "active")
    return result;
  return { ...result, confirmation_receipt: confirmationReceipt(current) };
}
function hasAppliedProposal(current, idempotencyKey) {
  return current.runtimeState.applied_proposals.some((item) => item.idempotency_key === idempotencyKey);
}
function isCurrentConfirmationReplay(current, idempotencyKey, receipt) {
  const confirmationIndex = current.runtimeState.execution_log.findIndex((item) => ("action" in item) && item.action === "confirm-draft" && item.idempotency_key === idempotencyKey && item.task_id === receipt.task_id && item.document_id === receipt.document_id && item.draft_revision === receipt.draft_revision);
  if (confirmationIndex < 0)
    return false;
  return !current.runtimeState.execution_log.slice(confirmationIndex + 1).some((item) => ("action" in item) && (item.action === "supersede" || item.action === "commit-replan"));
}
function assertDocumentReferencesResubmitted(current, semantic) {
  if (current.runtimeState.lifecycle_state === "archived")
    return;
  const definition = readDraftDefinitionFromBody(current.body);
  if (readProjectDocuments(definition.background_context) !== null && (semantic.project_documents === undefined || semantic.affected_contracts === undefined)) {
    fail6("PROJECT_DOCUMENTS_REQUIRED", "Resubmit recorded project_documents and affected_contracts when refining or replanning; use [] only for an explicit removal.");
  }
}
function prepareDraft(root2, input, options = {}) {
  const semantic = normalizeSemanticDraft(root2, input);
  assertSemanticAuthority(root2, semantic);
  const definition = semanticDraftDefinition(semantic);
  assertV2DraftDefinitionAuthority(root2, definition);
  assertPreparedTestStrategy(root2, definition, semantic.task_basis);
  const current = readCanonicalCurrentTask(root2);
  assertDocumentReferencesResubmitted(current, semantic);
  const creating = current.runtimeState.workflow_status === "closed" && current.runtimeState.lifecycle_state === "archived";
  const updating = current.runtimeState.workflow_status === "draft" && current.runtimeState.lifecycle_state === "active";
  if (!creating && !updating) {
    if (current.runtimeState.workflow_status === "superseded") {
      fail6("REPLACEMENT_OUTCOME_UNSUPPORTED", "A superseded task retains unfinished obligations. This Runtime has no authorized non-completion successor transition; do not close it as completed or overwrite it with a new draft.");
    }
    fail6("PREPARE_DRAFT_STATE_INVALID", "prepare-draft requires closed + archived to create, or draft + active to update. A confirmed task cannot be replaced through the disabled one-call replan route.");
  }
  const identity = creating ? {
    task_id: allocateNextTaskId(root2, current.runtimeState.task_id),
    task_slug: taskSlug(semantic.goal),
    task_title: semantic.goal,
    document_id: undefined
  } : {
    task_id: current.runtimeState.task_id,
    task_slug: current.runtimeState.task_slug,
    task_title: extractTaskIdentityFromCurrentTask(current.body).title,
    document_id: current.sourceTuple.document_id
  };
  const digest = semanticDigest(semantic);
  const retryKey = adapterIdempotencyKey("prepare-draft", { task_id: identity.task_id, semantic });
  if (updating && currentMatchesSemanticDraft(root2, current, semantic)) {
    return withConfirmationReceipt(root2, semanticNoOp(current, retryKey, "The requested semantic draft already matches canonical CURRENT_TASK.", options), options);
  }
  const evidenceRefs = [`adapter:prepare-draft:${digest.slice(0, 16)}`];
  const proposal = createPrepareTaskDraftProposal(current, {
    action: creating ? "create-draft" : "update-draft",
    ...identity,
    task_basis: semantic.task_basis,
    draft_definition: semanticDraftDefinition(semantic),
    active_step_id: semantic.implementation_steps[0].id,
    evidence_refs: evidenceRefs,
    claim_evidence: claimEvidence(semantic),
    idempotency_key: adapterIdempotencyKey("prepare-draft-commit", {
      task_id: identity.task_id,
      source_revision: current.sourceTuple.revision,
      semantic
    }),
    authority_evidence: authority(current, identity.task_id, creating ? ["authorized-caller", "scope-admission", "evidence-admission"] : ["active-task-owner", "scope-admission", "evidence-admission"])
  });
  const result = verifyAdapterReadBack(root2, applyVNextRuntimeProposal(root2, proposal, options), options);
  return withConfirmationReceipt(root2, result, options);
}
function prepareSuccessor(root2, input, options = {}) {
  const request = record4(input, "prepare-successor");
  exactKeys2(request, ["predecessor", "draft"], "prepare-successor");
  const predecessor = validateSuccessorDecision(request.predecessor);
  const semantic = normalizeSemanticDraft(root2, request.draft);
  const current = readCanonicalCurrentTask(root2);
  const prior = current.runtimeState.execution_log.find((event) => ("action" in event) && event.action === "create-draft" && event.predecessor);
  if (prior && "predecessor" in prior && sameValue(prior.predecessor, predecessor) && currentMatchesSemanticDraft(root2, current, semantic) && current.runtimeState.workflow_status === "draft") {
    return withConfirmationReceipt(root2, semanticNoOp(current, prior.idempotency_key, "This exact successor draft is already prepared; the predecessor remains unfinished.", options), options);
  }
  if (current.runtimeState.workflow_status !== "superseded")
    fail6("SUCCESSOR_STATE_INVALID", "Prepare a successor only after an explicit, retained supersede. Do not invalidate an active task for a local correction.");
  assertSemanticAuthority(root2, semantic);
  const taskId = allocateNextTaskId(root2, current.runtimeState.task_id);
  const proposal = createPrepareTaskDraftProposal(current, {
    action: "create-draft",
    predecessor,
    task_id: taskId,
    task_slug: taskSlug(semantic.goal),
    task_title: semantic.goal,
    task_basis: semantic.task_basis,
    draft_definition: semanticDraftDefinition(semantic),
    active_step_id: semantic.implementation_steps[0].id,
    claim_evidence: claimEvidence(semantic),
    evidence_refs: [predecessor.decision_source],
    idempotency_key: adapterIdempotencyKey("prepare-successor", { predecessor, semantic }),
    authority_evidence: authority(current, taskId, ["user-confirmation", "scope-admission", "evidence-admission"])
  });
  return withConfirmationReceipt(root2, verifyAdapterReadBack(root2, applyVNextRuntimeProposal(root2, proposal, options), options), options);
}
function confirmDraft(root2, input, options = {}) {
  const source = record4(input, "confirm-draft input");
  exactKeys2(source, ["confirmation_receipt"], "confirm-draft input");
  const receipt = normalizeConfirmationReceipt(source.confirmation_receipt);
  const current = readCanonicalCurrentTask(root2);
  const idempotencyKey = adapterIdempotencyKey("confirm-draft", receipt);
  if (receipt.task_id !== current.runtimeState.task_id || receipt.document_id !== current.sourceTuple.document_id) {
    fail6("DRAFT_IDENTITY_CONFLICT", "confirmation_receipt does not identify the current draft.");
  }
  if (hasAppliedProposal(current, idempotencyKey)) {
    if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active" || !isCurrentConfirmationReplay(current, idempotencyKey, receipt)) {
      fail6("DRAFT_REVISION_CONFLICT", "confirmation_receipt belongs to an earlier task-definition generation.");
    }
    return semanticNoOp(current, idempotencyKey, "This exact draft confirmation was already committed.", options);
  }
  if (current.runtimeState.workflow_status !== "draft" || current.runtimeState.lifecycle_state !== "active") {
    fail6("DRAFT_CONFIRMATION_BLOCKED", "confirm-draft requires the current task to be draft + active.");
  }
  if (!taskSourceRevisionMatches(root2, current, receipt.draft_revision)) {
    fail6("DRAFT_REVISION_CONFLICT", `confirmation_receipt draft_revision ${receipt.draft_revision} does not match current draft revision ${current.sourceTuple.revision}.`);
  }
  const evidenceRefs = [`adapter:confirm-draft:${receipt.draft_revision.slice(0, 16)}`];
  const proposal = createPrepareTaskConfirmProposal(current, {
    task_id: receipt.task_id,
    task_slug: current.runtimeState.task_slug,
    document_id: receipt.document_id,
    draft_revision: receipt.draft_revision,
    evidence_refs: evidenceRefs,
    idempotency_key: idempotencyKey,
    authority_evidence: [
      {
        kind: "authorized-caller",
        source: current.relativePath,
        subject: receipt.task_id,
        task_id: receipt.task_id,
        document_id: receipt.document_id,
        draft_revision: receipt.draft_revision
      },
      ...authority(current, receipt.task_id, ["evidence-admission"])
    ]
  });
  return verifyAdapterReadBack(root2, applyVNextRuntimeProposal(root2, proposal, options), options);
}
function clearResumeReview(root2, input, options = {}) {
  const source = record4(input, "clear-resume-review input");
  exactKeys2(source, ["readiness_receipt"], "clear-resume-review input");
  const receipt = normalizeResumeReadinessReceipt(source.readiness_receipt);
  const current = readCanonicalCurrentTask(root2);
  const idempotencyKey = adapterIdempotencyKey("clear-resume-review", receipt);
  if (!current.runtimeState.resume_requires_review) {
    if (hasAppliedProposal(current, idempotencyKey)) {
      return semanticNoOp(current, idempotencyKey, "This exact resume-readiness receipt was already committed.", options);
    }
    fail6("RESUME_REVIEW_NOT_REQUIRED", "clear-resume-review requires an active resume-review gate.");
  }
  if (receipt.task_id !== current.runtimeState.task_id || receipt.document_id !== current.sourceTuple.document_id) {
    fail6("RESUME_READINESS_IDENTITY_CONFLICT", "readiness_receipt does not identify the current task document.");
  }
  if (!taskSourceRevisionMatches(root2, current, receipt.source_revision)) {
    fail6("RESUME_READINESS_REVISION_CONFLICT", `readiness_receipt source_revision ${receipt.source_revision} does not match current revision ${current.sourceTuple.revision}.`);
  }
  if (!sameValue(receipt.reviewed_reasons, current.runtimeState.resume_review_reasons)) {
    fail6("RESUME_READINESS_REASON_CONFLICT", "readiness_receipt must cover the exact current resume-review reasons.");
  }
  if (hasAppliedProposal(current, idempotencyKey)) {
    fail6("RESUME_READINESS_REVISION_CONFLICT", "readiness_receipt was committed for an earlier resume-review gate generation.");
  }
  const proposal = createPrepareTaskResumeReviewProposal(current, {
    mode: "default",
    evidence_refs: receipt.evidence_refs,
    idempotency_key: idempotencyKey,
    authority_evidence: [
      {
        kind: "authorized-caller",
        source: current.relativePath,
        subject: receipt.task_id,
        task_id: receipt.task_id,
        document_id: receipt.document_id,
        source_revision: receipt.source_revision
      },
      ...authority(current, receipt.task_id, ["active-task-owner", "resume-review", "evidence-admission"])
    ]
  });
  return verifyAdapterReadBack(root2, applyVNextRuntimeProposal(root2, proposal, options), options);
}
function replan(root2, input, options = {}) {
  fail6("REPLAN_CONFIRMATION_REQUIRED", "The legacy replan command commits immediately and is disabled. A revision-bound candidate and explicit confirmation are required before replacement.");
  const semantic = normalizeSemanticDraft(root2, input);
  assertSemanticAuthority(root2, semantic);
  assertPreparedTestStrategy(root2, semanticDraftDefinition(semantic), semantic.task_basis);
  const current = readCanonicalCurrentTask(root2);
  assertDocumentReferencesResubmitted(current, semantic);
  const digest = semanticDigest(semantic);
  const retryKey = adapterIdempotencyKey("replan", { task_id: current.runtimeState.task_id, semantic });
  if (current.runtimeState.workflow_status === "active" && current.runtimeState.lifecycle_state === "active" && currentMatchesSemanticDraft(root2, current, semantic)) {
    return semanticNoOp(current, retryKey, "The requested replan already matches canonical CURRENT_TASK.", options);
  }
  if (current.runtimeState.workflow_status === "active" && current.runtimeState.lifecycle_state === "active") {
    fail6("REPLAN_INVALIDATION_REQUIRED", "replan cannot replace a confirmed active task until an authorized task-lifecycle supersede invocation has invalidated it.");
  }
  if (current.runtimeState.workflow_status === "blocked_by_replan" && current.runtimeState.lifecycle_state === "active") {
    fail6("REPLAN_BLOCKED", "replan is blocked by unresolved authoritative evidence; the Runtime convergence owner must clear the existing replan block first.");
  }
  if (current.runtimeState.workflow_status !== "superseded" || current.runtimeState.lifecycle_state !== "active") {
    fail6("REPLAN_STATE_INVALID", "replan requires an authorized superseded + active task.");
  }
  const evidenceRefs = [`adapter:replan:${digest.slice(0, 16)}`];
  const proposal = createPrepareTaskReplanProposal(current, {
    delta: {
      kind: "task-state",
      action: "commit-replan",
      task_basis: semantic.task_basis,
      replacement_definition: semanticDraftDefinition(semantic),
      active_step_id: semantic.implementation_steps[0].id,
      evidence_refs: evidenceRefs,
      claim_evidence: claimEvidence(semantic)
    },
    idempotency_key: adapterIdempotencyKey("replan-commit", {
      task_id: current.runtimeState.task_id,
      source_revision: current.sourceTuple.revision,
      semantic
    }),
    authority_evidence: authority(current, current.runtimeState.task_id, ["active-task-owner", "scope-admission", "evidence-admission"]),
    evidence_refs: evidenceRefs
  });
  return verifyAdapterReadBack(root2, applyVNextRuntimeProposal(root2, proposal, options), options);
}
function parsePrepareTaskAdapterCli(argv) {
  const [command, ...rest] = argv;
  if (!PREPARE_TASK_ADAPTER_COMMANDS.includes(command)) {
    throw new Error(`Usage: vnext-runtime <${PREPARE_TASK_ADAPTER_COMMANDS.join("|")}> --root <path> [--dry-run] (semantic JSON on stdin)`);
  }
  let root2 = process.cwd();
  let dryRun = false;
  for (let index = 0;index < rest.length; index += 1) {
    const arg = rest[index];
    if (arg === "--root")
      root2 = rest[++index] ?? "";
    else if (arg === "--dry-run")
      dryRun = true;
    else
      throw new Error(`Unknown prepare-task adapter argument: ${arg}`);
  }
  if (!root2)
    throw new Error("--root requires a path.");
  return { command, root: root2, dryRun };
}
function readSemanticStdin(command) {
  const raw = !process.stdin.isTTY ? fs15.readFileSync(0, "utf8") : "";
  if (!raw.trim()) {
    throw new Error(`${command} requires semantic JSON on stdin.`);
  }
  try {
    return JSON.parse(raw);
  } catch (error) {
    throw new Error(`${command} stdin must be valid JSON: ${error instanceof Error ? error.message : String(error)}`);
  }
}
function validateInstalledRuntime(root2) {
  const runtimeManifest = path16.join(path16.resolve(root2), ...VNEXT_RUNTIME_PACKAGE_RELATIVE_PATH.split("/"), "package.json");
  if (fs15.existsSync(runtimeManifest))
    validateVNextRuntimeContract(root2, true);
}
async function runPrepareTaskAdapterCli(argv = process.argv.slice(2)) {
  try {
    validateRuntimeEnvironment();
    const args = parsePrepareTaskAdapterCli(argv);
    validateInstalledRuntime(args.root);
    const input = readSemanticStdin(args.command);
    const options = { dryRun: args.dryRun };
    let result;
    switch (args.command) {
      case "record-human-acceptance":
        result = recordUserEvidenceDecision(args.root, "human-acceptance", input, options);
        break;
      case "record-evidence-waiver":
        result = recordUserEvidenceDecision(args.root, "waiver", input, options);
        break;
      case "prepare-successor":
        result = prepareSuccessor(args.root, input, options);
        break;
      case "prepare-draft":
        result = prepareDraft(args.root, input, options);
        break;
      case "confirm-draft":
        result = confirmDraft(args.root, input, options);
        break;
      case "clear-resume-review":
        result = clearResumeReview(args.root, input, options);
        break;
      case "replan":
        result = replan(args.root, input, options);
        break;
      case "prepare-replan":
        result = prepareCorrectionReplan(args.root, input, options);
        break;
      case "confirm-replan":
        result = confirmCorrectionReplan(args.root, input, options);
        break;
      case "discard-replan":
        result = discardCorrectionReplan(args.root, input, options);
        break;
      case "prepare-scope-amendment":
        result = prepareScopeAmendment(args.root, input, options);
        break;
      case "discard-scope-amendment":
        result = discardScopeAmendment(args.root, input, options);
        break;
      case "initialize-preservation":
        result = initializeTaskPreservation(args.root, input, options);
        break;
      case "suspend-recovery": {
        const source = record4(input, "suspend-recovery");
        exactKeys2(source, ["source_revision", "reason", "evidence_refs"], "suspend-recovery");
        const current = readCanonicalCurrentTask(args.root);
        if (source.source_revision !== current.sourceTuple.revision)
          fail6("RECOVERY_SOURCE_STALE", "Suspension must bind the current task revision.");
        if (current.runtimeState.findings.some((item) => ["admitted", "in-progress"].includes(item.status)))
          fail6("RECOVERY_OWNER_CONFLICT", "Existing repair owner must converge before recovery.");
        const refs = [...textList(source.evidence_refs, "evidence_refs", false), `caller-reported-recovery-reason:${text3(source.reason, "reason")}`];
        result = applyVNextRuntimeProposal(args.root, createPrepareTaskReplanProposal(current, {
          delta: { kind: "task-state", action: "mark-replan-blocked", evidence_refs: refs },
          idempotency_key: `suspend-recovery-${crypto14.createHash("sha256").update(JSON.stringify(source)).digest("hex").slice(0, 40)}`,
          authority_evidence: authority(current, current.runtimeState.task_id, ["active-task-owner", "scope-admission", "evidence-admission"]),
          evidence_refs: refs
        }), options);
        break;
      }
    }
    console.log(JSON.stringify(result, null, 2));
    return result.status === "blocked" || result.status === "conflict" ? 2 : 0;
  } catch (error) {
    console.error(error instanceof Error ? error.message : String(error));
    return 1;
  }
}

// runtime/vnext/src/execute-step-adapter.ts
import * as crypto15 from "crypto";
import * as fs19 from "fs";
import * as path20 from "path";

// runtime/vnext/src/task-context.ts
import * as fs18 from "node:fs";
import * as path19 from "node:path";
import { createHash as createHash17 } from "node:crypto";

// runtime/vnext/src/file-context.ts
import * as fs17 from "node:fs";
import * as path18 from "node:path";
import { createHash as createHash16 } from "node:crypto";
import { spawn } from "node:child_process";
// node_modules/diff/libesm/diff/base.js
class Diff {
  diff(oldStr, newStr, options = {}) {
    let callback;
    if (typeof options === "function") {
      callback = options;
      options = {};
    } else if ("callback" in options) {
      callback = options.callback;
    }
    const oldString = this.castInput(oldStr, options);
    const newString = this.castInput(newStr, options);
    const oldTokens = this.removeEmpty(this.tokenize(oldString, options));
    const newTokens = this.removeEmpty(this.tokenize(newString, options));
    return this.diffWithOptionsObj(oldTokens, newTokens, options, callback);
  }
  diffWithOptionsObj(oldTokens, newTokens, options, callback) {
    var _a;
    const done = (value) => {
      value = this.postProcess(value, options);
      if (callback) {
        setTimeout(function() {
          callback(value);
        }, 0);
        return;
      } else {
        return value;
      }
    };
    const newLen = newTokens.length, oldLen = oldTokens.length;
    let editLength = 1;
    let maxEditLength = newLen + oldLen;
    if (options.maxEditLength != null) {
      maxEditLength = Math.min(maxEditLength, options.maxEditLength);
    }
    const maxExecutionTime = (_a = options.timeout) !== null && _a !== undefined ? _a : Infinity;
    const abortAfterTimestamp = Date.now() + maxExecutionTime;
    const bestPath = [{ oldPos: -1, lastComponent: undefined }];
    let newPos = this.extractCommon(bestPath[0], newTokens, oldTokens, 0, options);
    if (bestPath[0].oldPos + 1 >= oldLen && newPos + 1 >= newLen) {
      return done(this.buildValues(bestPath[0].lastComponent, newTokens, oldTokens));
    }
    let minDiagonalToConsider = -Infinity, maxDiagonalToConsider = Infinity;
    const execEditLength = () => {
      for (let diagonalPath = Math.max(minDiagonalToConsider, -editLength);diagonalPath <= Math.min(maxDiagonalToConsider, editLength); diagonalPath += 2) {
        let basePath;
        const removePath = bestPath[diagonalPath - 1], addPath = bestPath[diagonalPath + 1];
        if (removePath) {
          bestPath[diagonalPath - 1] = undefined;
        }
        let canAdd = false;
        if (addPath) {
          const addPathNewPos = addPath.oldPos - diagonalPath;
          canAdd = addPath && 0 <= addPathNewPos && addPathNewPos < newLen;
        }
        const canRemove = removePath && removePath.oldPos + 1 < oldLen;
        if (!canAdd && !canRemove) {
          bestPath[diagonalPath] = undefined;
          continue;
        }
        if (!canRemove || canAdd && removePath.oldPos < addPath.oldPos) {
          basePath = this.addToPath(addPath, true, false, 0, options);
        } else {
          basePath = this.addToPath(removePath, false, true, 1, options);
        }
        newPos = this.extractCommon(basePath, newTokens, oldTokens, diagonalPath, options);
        if (basePath.oldPos + 1 >= oldLen && newPos + 1 >= newLen) {
          return done(this.buildValues(basePath.lastComponent, newTokens, oldTokens)) || true;
        } else {
          bestPath[diagonalPath] = basePath;
          if (basePath.oldPos + 1 >= oldLen) {
            maxDiagonalToConsider = Math.min(maxDiagonalToConsider, diagonalPath - 1);
          }
          if (newPos + 1 >= newLen) {
            minDiagonalToConsider = Math.max(minDiagonalToConsider, diagonalPath + 1);
          }
        }
      }
      editLength++;
    };
    if (callback) {
      (function exec() {
        setTimeout(function() {
          if (editLength > maxEditLength || Date.now() > abortAfterTimestamp) {
            return callback(undefined);
          }
          if (!execEditLength()) {
            exec();
          }
        }, 0);
      })();
    } else {
      while (editLength <= maxEditLength && Date.now() <= abortAfterTimestamp) {
        const ret = execEditLength();
        if (ret) {
          return ret;
        }
      }
    }
  }
  addToPath(path, added, removed, oldPosInc, options) {
    const last = path.lastComponent;
    if (last && !options.oneChangePerToken && last.added === added && last.removed === removed) {
      return {
        oldPos: path.oldPos + oldPosInc,
        lastComponent: { count: last.count + 1, added, removed, previousComponent: last.previousComponent }
      };
    } else {
      return {
        oldPos: path.oldPos + oldPosInc,
        lastComponent: { count: 1, added, removed, previousComponent: last }
      };
    }
  }
  extractCommon(basePath, newTokens, oldTokens, diagonalPath, options) {
    const newLen = newTokens.length, oldLen = oldTokens.length;
    let oldPos = basePath.oldPos, newPos = oldPos - diagonalPath, commonCount = 0;
    while (newPos + 1 < newLen && oldPos + 1 < oldLen && this.equals(oldTokens[oldPos + 1], newTokens[newPos + 1], options)) {
      newPos++;
      oldPos++;
      commonCount++;
      if (options.oneChangePerToken) {
        basePath.lastComponent = { count: 1, previousComponent: basePath.lastComponent, added: false, removed: false };
      }
    }
    if (commonCount && !options.oneChangePerToken) {
      basePath.lastComponent = { count: commonCount, previousComponent: basePath.lastComponent, added: false, removed: false };
    }
    basePath.oldPos = oldPos;
    return newPos;
  }
  equals(left, right, options) {
    if (options.comparator) {
      return options.comparator(left, right);
    } else {
      return left === right || !!options.ignoreCase && left.toLowerCase() === right.toLowerCase();
    }
  }
  removeEmpty(array) {
    const ret = [];
    for (let i = 0;i < array.length; i++) {
      if (array[i]) {
        ret.push(array[i]);
      }
    }
    return ret;
  }
  castInput(value, options) {
    return value;
  }
  tokenize(value, options) {
    return Array.from(value);
  }
  join(chars) {
    return chars.join("");
  }
  postProcess(changeObjects, options) {
    return changeObjects;
  }
  get useLongestToken() {
    return false;
  }
  buildValues(lastComponent, newTokens, oldTokens) {
    const components = [];
    let nextComponent;
    while (lastComponent) {
      components.push(lastComponent);
      nextComponent = lastComponent.previousComponent;
      delete lastComponent.previousComponent;
      lastComponent = nextComponent;
    }
    components.reverse();
    const componentLen = components.length;
    let componentPos = 0, newPos = 0, oldPos = 0;
    for (;componentPos < componentLen; componentPos++) {
      const component = components[componentPos];
      if (!component.removed) {
        if (!component.added && this.useLongestToken) {
          let value = newTokens.slice(newPos, newPos + component.count);
          value = value.map(function(value, i) {
            const oldValue = oldTokens[oldPos + i];
            return oldValue.length > value.length ? oldValue : value;
          });
          component.value = this.join(value);
        } else {
          component.value = this.join(newTokens.slice(newPos, newPos + component.count));
        }
        newPos += component.count;
        if (!component.added) {
          oldPos += component.count;
        }
      } else {
        component.value = this.join(oldTokens.slice(oldPos, oldPos + component.count));
        oldPos += component.count;
      }
    }
    return components;
  }
}

// node_modules/diff/libesm/diff/line.js
class LineDiff extends Diff {
  constructor() {
    super(...arguments);
    this.tokenize = tokenize;
  }
  equals(left, right, options) {
    if (options.ignoreWhitespace) {
      if (!options.newlineIsToken || !left.includes(`
`)) {
        left = left.trim();
      }
      if (!options.newlineIsToken || !right.includes(`
`)) {
        right = right.trim();
      }
    } else if (options.ignoreNewlineAtEof && !options.newlineIsToken) {
      if (left.endsWith(`
`)) {
        left = left.slice(0, -1);
      }
      if (right.endsWith(`
`)) {
        right = right.slice(0, -1);
      }
    }
    return super.equals(left, right, options);
  }
}
var lineDiff = new LineDiff;
function diffLines(oldStr, newStr, options) {
  return lineDiff.diff(oldStr, newStr, options);
}
function tokenize(value, options) {
  if (options.stripTrailingCr) {
    value = value.replace(/\r\n/g, `
`);
  }
  const retLines = [], linesAndNewlines = value.split(/(\n|\r\n)/);
  if (!linesAndNewlines[linesAndNewlines.length - 1]) {
    linesAndNewlines.pop();
  }
  for (let i = 0;i < linesAndNewlines.length; i++) {
    const line = linesAndNewlines[i];
    if (i % 2 && !options.newlineIsToken) {
      retLines[retLines.length - 1] += line;
    } else {
      retLines.push(line);
    }
  }
  return retLines;
}

// node_modules/diff/libesm/patch/create.js
function needsQuoting(s) {
  for (let i = 0;i < s.length; i++) {
    if (s[i] < " " || s[i] > "~" || s[i] === '"' || s[i] === "\\") {
      return true;
    }
  }
  return false;
}
function quoteFileNameIfNeeded(s) {
  if (!needsQuoting(s)) {
    return s;
  }
  let result = '"';
  const bytes = new TextEncoder().encode(s);
  let i = 0;
  while (i < bytes.length) {
    const b = bytes[i];
    if (b === 7) {
      result += "\\a";
    } else if (b === 8) {
      result += "\\b";
    } else if (b === 9) {
      result += "\\t";
    } else if (b === 10) {
      result += "\\n";
    } else if (b === 11) {
      result += "\\v";
    } else if (b === 12) {
      result += "\\f";
    } else if (b === 13) {
      result += "\\r";
    } else if (b === 34) {
      result += "\\\"";
    } else if (b === 92) {
      result += "\\\\";
    } else if (b >= 32 && b <= 126) {
      result += String.fromCharCode(b);
    } else {
      result += "\\" + b.toString(8).padStart(3, "0");
    }
    i++;
  }
  result += '"';
  return result;
}
var INCLUDE_HEADERS = {
  includeIndex: true,
  includeUnderline: true,
  includeFileHeaders: true
};
function structuredPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, options) {
  let optionsObj;
  if (!options) {
    optionsObj = {};
  } else if (typeof options === "function") {
    optionsObj = { callback: options };
  } else {
    optionsObj = options;
  }
  if (typeof optionsObj.context === "undefined") {
    optionsObj.context = 4;
  }
  const context = optionsObj.context;
  if (optionsObj.newlineIsToken) {
    throw new Error("newlineIsToken may not be used with patch-generation functions, only with diffing functions");
  }
  if (!optionsObj.callback) {
    return diffLinesResultToPatch(diffLines(oldStr, newStr, optionsObj));
  } else {
    const { callback } = optionsObj;
    diffLines(oldStr, newStr, Object.assign(Object.assign({}, optionsObj), { callback: (diff) => {
      const patch = diffLinesResultToPatch(diff);
      callback(patch);
    } }));
  }
  function diffLinesResultToPatch(diff) {
    if (!diff) {
      return;
    }
    diff.push({ value: "", lines: [] });
    function contextLines(lines) {
      return lines.map(function(entry) {
        return " " + entry;
      });
    }
    const hunks = [];
    let oldRangeStart = 0, newRangeStart = 0, curRange = [], oldLine = 1, newLine = 1;
    for (let i = 0;i < diff.length; i++) {
      const current = diff[i], lines = current.lines || splitLines(current.value);
      current.lines = lines;
      if (current.added || current.removed) {
        if (!oldRangeStart) {
          const prev = diff[i - 1];
          oldRangeStart = oldLine;
          newRangeStart = newLine;
          if (prev) {
            curRange = context > 0 ? contextLines(prev.lines.slice(-context)) : [];
            oldRangeStart -= curRange.length;
            newRangeStart -= curRange.length;
          }
        }
        for (const line of lines) {
          curRange.push((current.added ? "+" : "-") + line);
        }
        if (current.added) {
          newLine += lines.length;
        } else {
          oldLine += lines.length;
        }
      } else {
        if (oldRangeStart) {
          if (lines.length <= context * 2 && i < diff.length - 2) {
            for (const line of contextLines(lines)) {
              curRange.push(line);
            }
          } else {
            const contextSize = Math.min(lines.length, context);
            for (const line of contextLines(lines.slice(0, contextSize))) {
              curRange.push(line);
            }
            const hunk = {
              oldStart: oldRangeStart,
              oldLines: oldLine - oldRangeStart + contextSize,
              newStart: newRangeStart,
              newLines: newLine - newRangeStart + contextSize,
              lines: curRange
            };
            hunks.push(hunk);
            oldRangeStart = 0;
            newRangeStart = 0;
            curRange = [];
          }
        }
        oldLine += lines.length;
        newLine += lines.length;
      }
    }
    for (const hunk of hunks) {
      for (let i = 0;i < hunk.lines.length; i++) {
        if (hunk.lines[i].endsWith(`
`)) {
          hunk.lines[i] = hunk.lines[i].slice(0, -1);
        } else {
          hunk.lines.splice(i + 1, 0, "\\ No newline at end of file");
          i++;
        }
      }
    }
    return {
      oldFileName,
      newFileName,
      oldHeader,
      newHeader,
      hunks
    };
  }
}
function formatPatch(patch, headerOptions) {
  var _a, _b, _c, _d, _e, _f;
  if (!headerOptions) {
    headerOptions = INCLUDE_HEADERS;
  }
  if (Array.isArray(patch)) {
    if (patch.length > 1 && !headerOptions.includeFileHeaders && !patch.every((p) => p.isGit)) {
      throw new Error("Cannot omit file headers on a multi-file patch. " + "(The result would be unparseable; how would a tool trying to apply " + "the patch know which changes are to which file?)");
    }
    return patch.map((p) => formatPatch(p, headerOptions)).join(`
`);
  }
  const ret = [];
  if (patch.isGit) {
    headerOptions = INCLUDE_HEADERS;
    if (!patch.oldFileName) {
      throw new Error("oldFileName must be specified for Git patches");
    }
    if (!patch.newFileName) {
      throw new Error("newFileName must be specified for Git patches");
    }
    let gitOldName = patch.oldFileName;
    let gitNewName = patch.newFileName;
    if (patch.isCreate && gitOldName === "/dev/null") {
      gitOldName = gitNewName.replace(/^b\//, "a/");
    } else if (patch.isDelete && gitNewName === "/dev/null") {
      gitNewName = gitOldName.replace(/^a\//, "b/");
    }
    ret.push("diff --git " + quoteFileNameIfNeeded(gitOldName) + " " + quoteFileNameIfNeeded(gitNewName));
    if (patch.isDelete) {
      ret.push("deleted file mode " + ((_a = patch.oldMode) !== null && _a !== undefined ? _a : "100644"));
    }
    if (patch.isCreate) {
      ret.push("new file mode " + ((_b = patch.newMode) !== null && _b !== undefined ? _b : "100644"));
    }
    if (patch.oldMode && patch.newMode && !patch.isDelete && !patch.isCreate) {
      ret.push("old mode " + patch.oldMode);
      ret.push("new mode " + patch.newMode);
    }
    if (patch.isRename) {
      ret.push("rename from " + quoteFileNameIfNeeded(((_c = patch.oldFileName) !== null && _c !== undefined ? _c : "").replace(/^a\//, "")));
      ret.push("rename to " + quoteFileNameIfNeeded(((_d = patch.newFileName) !== null && _d !== undefined ? _d : "").replace(/^b\//, "")));
    }
    if (patch.isCopy) {
      ret.push("copy from " + quoteFileNameIfNeeded(((_e = patch.oldFileName) !== null && _e !== undefined ? _e : "").replace(/^a\//, "")));
      ret.push("copy to " + quoteFileNameIfNeeded(((_f = patch.newFileName) !== null && _f !== undefined ? _f : "").replace(/^b\//, "")));
    }
  } else {
    if (headerOptions.includeIndex && patch.oldFileName == patch.newFileName && patch.oldFileName !== undefined) {
      ret.push("Index: " + patch.oldFileName);
    }
    if (headerOptions.includeUnderline) {
      ret.push("===================================================================");
    }
  }
  const hasHunks = patch.hunks.length > 0;
  if (headerOptions.includeFileHeaders && patch.oldFileName !== undefined && patch.newFileName !== undefined && (!patch.isGit || hasHunks)) {
    ret.push("--- " + quoteFileNameIfNeeded(patch.oldFileName) + (patch.oldHeader ? "\t" + patch.oldHeader : ""));
    ret.push("+++ " + quoteFileNameIfNeeded(patch.newFileName) + (patch.newHeader ? "\t" + patch.newHeader : ""));
  }
  for (let i = 0;i < patch.hunks.length; i++) {
    const hunk = patch.hunks[i];
    const oldStart = hunk.oldLines === 0 ? hunk.oldStart - 1 : hunk.oldStart;
    const newStart = hunk.newLines === 0 ? hunk.newStart - 1 : hunk.newStart;
    ret.push("@@ -" + oldStart + "," + hunk.oldLines + " +" + newStart + "," + hunk.newLines + " @@");
    for (const line of hunk.lines) {
      ret.push(line);
    }
  }
  return ret.join(`
`) + `
`;
}
function createTwoFilesPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, options) {
  if (typeof options === "function") {
    options = { callback: options };
  }
  if (!(options === null || options === undefined ? undefined : options.callback)) {
    const patchObj = structuredPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, options);
    if (!patchObj) {
      return;
    }
    return formatPatch(patchObj, options === null || options === undefined ? undefined : options.headerOptions);
  } else {
    const { callback } = options;
    structuredPatch(oldFileName, newFileName, oldStr, newStr, oldHeader, newHeader, Object.assign(Object.assign({}, options), { callback: (patchObj) => {
      if (!patchObj) {
        callback(undefined);
      } else {
        callback(formatPatch(patchObj, options.headerOptions));
      }
    } }));
  }
}
function splitLines(text) {
  const hasTrailingNl = text.endsWith(`
`);
  const result = text.split(`
`).map((line) => line + `
`);
  if (hasTrailingNl) {
    result.pop();
  } else {
    result.push(result.pop().slice(0, -1));
  }
  return result;
}
// runtime/vnext/src/rg-tool.ts
import * as fs16 from "node:fs";
import * as path17 from "node:path";
import { execFileSync } from "node:child_process";
import { createHash as createHash15 } from "node:crypto";
var RG_TOOLS_PATH = ".workflow-system/runtime/tools/rg";
var RG_BINARY = process.platform === "win32" ? "rg.exe" : "rg";
function assertRgDirectory(root2) {
  let directory = path17.resolve(root2);
  for (const part of RG_TOOLS_PATH.split("/")) {
    directory = path17.join(directory, part);
    try {
      if (!fs16.lstatSync(directory).isDirectory())
        throw new Error("RG_DEPENDENCY_PATH_INVALID: tool directory must not be a symlink or file.");
    } catch (error) {
      if (error.code !== "ENOENT")
        throw error;
    }
  }
  return directory;
}
function probeRg(command) {
  try {
    const version = execFileSync(command, ["--no-config", "--version"], { encoding: "utf8", windowsHide: true, timeout: 3000, maxBuffer: 4096 });
    const match = /^ripgrep (\d+\.\d+\.\d+)/u.exec(version);
    if (!match)
      return null;
    const [major, minor] = match[1].split(".").map(Number);
    return major === 14 && minor >= 1 || major === 15 ? match[1] : null;
  } catch {
    return null;
  }
}
function resolveRg(root2) {
  const directory = assertRgDirectory(root2);
  const command = path17.join(directory, RG_BINARY);
  try {
    const identity = JSON.parse(fs16.readFileSync(path17.join(directory, "identity.json"), "utf8"));
    if (!fs16.lstatSync(command).isSymbolicLink() && identity.sha256 === createHash15("sha256").update(fs16.readFileSync(command)).digest("hex")) {
      const version = probeRg(command);
      if (version && identity.version === version)
        return { command, version, source: "project" };
    }
  } catch {}
  for (const entry of (process.env.PATH ?? process.env.Path ?? "").split(path17.delimiter).filter(Boolean)) {
    const command = path17.resolve(entry.replace(/^"|"$/gu, ""), RG_BINARY);
    if (!fs16.existsSync(command))
      continue;
    const version = probeRg(command);
    if (version)
      return { command, version, source: "path" };
  }
  throw new Error("RG_DEPENDENCY_MISSING: install or upgrade the Runtime distribution to prepare ripgrep; read-only commands do not download tools.");
}

// runtime/vnext/src/file-context.ts
var DEFAULT_CONTEXT_BYTES = 16 * 1024;
var MAX_CONTEXT_BYTES = 64 * 1024;
var sha2569 = (bytes) => createHash16("sha256").update(bytes).digest("hex");
function contextInput(input, keys) {
  if (!input || typeof input !== "object" || Array.isArray(input))
    throw new Error("CONTEXT_INPUT_INVALID: expected an object.");
  const value = input;
  if (Object.keys(value).some((key) => !keys.includes(key)))
    throw new Error("CONTEXT_INPUT_INVALID: unexpected input field.");
  return value;
}
function integer(value, fallback, min, max) {
  if (value === undefined)
    return fallback;
  if (!Number.isSafeInteger(value) || Number(value) < min || Number(value) > max)
    throw new Error(`CONTEXT_INPUT_INVALID: integer must be ${min}..${max}.`);
  return Number(value);
}
function contextPath(root2, value) {
  if (typeof value !== "string" || !value || value.length > 1024 || /[\0\r\n]/u.test(value))
    throw new Error("CONTEXT_PATH_INVALID: expected a project-relative path.");
  const relative = value.replace(/\\/gu, "/").replace(/^\.\//u, "");
  if (path18.posix.isAbsolute(relative) || /^[A-Za-z]:/u.test(relative) || relative.split("/").includes(".."))
    throw new Error("CONTEXT_PATH_INVALID: path escapes project.");
  const absolute = path18.resolve(root2, relative);
  let parent = path18.resolve(root2);
  for (const part of relative.split("/").filter((part) => part && part !== ".")) {
    parent = path18.join(parent, part);
    try {
      if (fs17.lstatSync(parent).isSymbolicLink())
        throw new Error("CONTEXT_SYMLINK: symbolic links are not followed.");
    } catch (error) {
      if (error.code !== "ENOENT")
        throw error;
    }
  }
  return { relative, absolute };
}
function decodeText(bytes) {
  if (bytes.includes(0))
    return null;
  try {
    return new TextDecoder("utf-8", { fatal: true, ignoreBOM: true }).decode(bytes);
  } catch {
    return null;
  }
}
function textPage(text, input) {
  const startLine = integer(input.start_line, 1, 1, Number.MAX_SAFE_INTEGER);
  const endLine = integer(input.end_line, Number.MAX_SAFE_INTEGER, startLine, Number.MAX_SAFE_INTEGER);
  let start = 0;
  let line = 1;
  while (line < startLine) {
    const newline = text.indexOf(`
`, start);
    if (newline < 0)
      throw new Error("CONTEXT_RANGE_INVALID: start_line is beyond the file.");
    start = newline + 1;
    line++;
  }
  let end = start;
  while (line <= endLine && end < text.length) {
    const newline = text.indexOf(`
`, end);
    end = newline < 0 ? text.length : newline + 1;
    line++;
  }
  const bytes = Buffer.from(text.slice(start, end));
  const offset = integer(input.offset, 0, 0, bytes.length);
  const budget = integer(input.max_bytes, DEFAULT_CONTEXT_BYTES, 4, MAX_CONTEXT_BYTES);
  if (offset < bytes.length && (bytes[offset] & 192) === 128)
    throw new Error("CONTEXT_RANGE_INVALID: offset must be a UTF-8 boundary.");
  let pageEnd = Math.min(bytes.length, offset + budget);
  while (pageEnd < bytes.length && (bytes[pageEnd] & 192) === 128)
    pageEnd--;
  return { text: bytes.subarray(offset, pageEnd).toString("utf8"), start_line: startLine, offset, next_offset: pageEnd < bytes.length ? pageEnd : null, total_bytes: bytes.length, truncated: pageEnd < bytes.length };
}
function textDiff(file, before, after) {
  return createTwoFilesPatch(`before/${file}`, `after/${file}`, before, after, "", "", { context: 3, timeout: 200, maxEditLength: 20000 });
}
function readFileContext(root2, input) {
  const value = contextInput(input, ["operation", "path", "sha256", "offset", "max_bytes", "start_line", "end_line"]);
  const file = contextPath(root2, value.path);
  const bytes = fs17.readFileSync(file.absolute);
  const revision = sha2569(bytes);
  if (value.sha256 !== undefined && value.sha256 !== revision)
    throw new Error("CONTEXT_STALE: file changed; start a fresh read.");
  if (value.offset !== undefined && value.offset !== 0 && value.sha256 === undefined)
    throw new Error("CONTEXT_INPUT_INVALID: continuation requires sha256.");
  const text = decodeText(bytes);
  return {
    status: "pass",
    operation_kind: "file-context",
    committed: false,
    path: file.relative,
    sha256: revision,
    ...text === null ? { content_status: "binary-or-non-utf8", size_bytes: bytes.length } : { content_status: "text", ...textPage(text, value) }
  };
}
async function searchFileContext(root2, input) {
  const value = contextInput(input, ["operation", "roots", "globs", "query", "include_hidden", "limit", "max_bytes"]);
  if (!Array.isArray(value.roots) || !value.roots.length || value.roots.length > 32)
    throw new Error("CONTEXT_INPUT_INVALID: provide 1..32 search roots.");
  const roots = value.roots.map((item) => contextPath(root2, item));
  if (value.query !== undefined && (typeof value.query !== "string" || !value.query || value.query.length > 4096 || /[\0\r\n]/u.test(value.query)))
    throw new Error("CONTEXT_INPUT_INVALID: query must be a non-empty single-line literal.");
  if (value.include_hidden !== undefined && typeof value.include_hidden !== "boolean")
    throw new Error("CONTEXT_INPUT_INVALID: include_hidden must be boolean.");
  const globs = value.globs ?? [];
  if (!Array.isArray(globs) || globs.length > 32 || globs.some((glob) => typeof glob !== "string" || glob.length > 1024 || /[\0\r\n]/u.test(glob)))
    throw new Error("CONTEXT_INPUT_INVALID: invalid globs.");
  const limit = integer(value.limit, 50, 1, 200);
  const budget = integer(value.max_bytes, DEFAULT_CONTEXT_BYTES, 4, MAX_CONTEXT_BYTES);
  const rg = resolveRg(root2);
  const args = [
    "--no-config",
    "--color",
    "never",
    ...value.include_hidden ? ["--hidden"] : [],
    ...globs.flatMap((glob) => ["--glob", glob]),
    ...value.query === undefined ? ["--files", "--null"] : ["--json", "--fixed-strings", "--", value.query],
    ...value.query === undefined ? ["--"] : [],
    ...roots.map((item) => item.absolute)
  ];
  const hits = [];
  let stopReason = null;
  let used = 0;
  let pending = Buffer.alloc(0);
  let stderr = "";
  await new Promise((resolve, reject) => {
    const child = spawn(rg.command, args, { cwd: root2, windowsHide: true, shell: false, stdio: ["ignore", "pipe", "pipe"] });
    const stop = (reason) => {
      stopReason ??= reason;
      child.kill();
    };
    const timer = setTimeout(() => stop("timeout"), 1e4);
    child.on("error", (error) => {
      clearTimeout(timer);
      reject(error);
    });
    child.stderr.on("data", (chunk) => {
      stderr = (stderr + chunk.toString()).slice(0, 2048);
    });
    child.stdout.on("data", (chunk) => {
      if (stopReason)
        return;
      pending = Buffer.concat([pending, chunk]);
      const separator = value.query === undefined ? 0 : 10;
      let at;
      while ((at = pending.indexOf(separator)) >= 0) {
        const line = pending.subarray(0, at);
        pending = pending.subarray(at + 1);
        try {
          let hit;
          if (value.query === undefined)
            hit = { path: path18.relative(root2, line.toString("utf8")).replace(/\\/gu, "/") };
          else {
            const event = JSON.parse(line.toString("utf8"));
            if (event.type !== "match")
              continue;
            if (!event.data.path.text || event.data.lines.text === undefined) {
              stop("non-utf8-result");
              return;
            }
            hit = { path: path18.relative(root2, event.data.path.text).replace(/\\/gu, "/"), line: event.data.line_number, text: event.data.lines.text };
          }
          contextPath(root2, hit.path);
          const size = Buffer.byteLength(JSON.stringify(hit));
          if (hits.length >= limit || used + size > budget) {
            stop("result-limit");
            return;
          }
          used += size;
          hits.push(hit);
        } catch {
          stop("unreadable-result");
          return;
        }
      }
      if (pending.length > MAX_CONTEXT_BYTES)
        stop("oversized-result");
    });
    child.on("close", (code) => {
      clearTimeout(timer);
      if (code !== 0 && code !== 1 && !stopReason)
        stopReason = "search-error";
      resolve();
    });
  });
  return {
    status: stopReason ? "partial" : "pass",
    operation_kind: "file-context",
    committed: false,
    rg: rg.version,
    roots: roots.map((item) => item.relative),
    globs,
    query: value.query ?? null,
    include_hidden: value.include_hidden === true,
    hits,
    complete_within_scope: stopReason === null,
    truncated: stopReason !== null,
    reason: stopReason,
    error: stderr || null
  };
}
async function fileContext(root2, input) {
  const value = contextInput(input, ["operation", "path", "sha256", "offset", "max_bytes", "start_line", "end_line", "roots", "globs", "query", "include_hidden", "limit"]);
  if (value.operation === "read")
    return readFileContext(root2, value);
  if (value.operation === "search")
    return searchFileContext(root2, value);
  throw new Error("CONTEXT_INPUT_INVALID: operation must be search or read.");
}

// runtime/vnext/src/task-context.ts
var TASK_CONTEXT_OPERATION = "task-context";
var TASK_READ_OPERATION = "task-read";
var TASK_CONTEXT_RECEIPT_KIND = "task-context-receipt/v1";
var TASK_READ_RECEIPT_KIND = "task-read-receipt/v1";
function record5(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function asStoreCurrent(current) {
  return current;
}
function stableJson2(value) {
  if (Array.isArray(value))
    return `[${value.map(stableJson2).join(",")}]`;
  if (record5(value))
    return `{${Object.keys(value).sort().map((key) => `${JSON.stringify(key)}:${stableJson2(value[key])}`).join(",")}}`;
  return JSON.stringify(value);
}
function byteLength(value) {
  return Buffer.byteLength(value, "utf8");
}
function sha25610(value) {
  return createHash17("sha256").update(value, "utf8").digest("hex");
}
function parseContinuation(value, expectedKind) {
  if (value === undefined)
    return null;
  let parsed = value;
  if (typeof value === "string") {
    try {
      parsed = JSON.parse(value);
    } catch {
      throw new Error("TASK_CONTEXT_CONTINUATION_INVALID: continuation is not valid JSON.");
    }
  }
  if (!record5(parsed) || parsed.kind !== expectedKind || typeof parsed.source_revision !== "string")
    throw new Error("TASK_CONTEXT_CONTINUATION_INVALID: continuation has an invalid binding.");
  if (!Number.isSafeInteger(parsed.byte_offset) || parsed.byte_offset < 0)
    throw new Error("TASK_CONTEXT_CONTINUATION_INVALID: byte_offset is invalid.");
  if (parsed.content_revision !== undefined && (typeof parsed.content_revision !== "string" || !/^[a-f0-9]{64}$/u.test(parsed.content_revision)))
    throw new Error("TASK_CONTEXT_CONTINUATION_INVALID: content_revision is invalid.");
  if (expectedKind === "task-context-page/v1" && (!Number.isSafeInteger(parsed.block_index) || parsed.block_index < 0 || typeof parsed.definition_revision !== "string" || typeof parsed.state_revision !== "string"))
    throw new Error("TASK_CONTEXT_CONTINUATION_INVALID: task context cursor is invalid.");
  if (expectedKind === "task-read-page/v1" && (typeof parsed.reference !== "string" || typeof parsed.definition_revision !== "string" || !/^[a-f0-9]{64}$/u.test(parsed.definition_revision) || typeof parsed.state_revision !== "string" || !/^[a-f0-9]{64}$/u.test(parsed.state_revision)))
    throw new Error("TASK_CONTEXT_CONTINUATION_INVALID: task read cursor is invalid.");
  return parsed;
}
function currentStore(root2, current) {
  return TaskStore.forCurrent(root2, asStoreCurrent(current));
}
function taskContextReferenceForCurrent(root2, current, entry = "validate", mode = "default") {
  const store = currentStore(root2, current);
  const manifest = store.manifest;
  return {
    kind: "task-context-reference/v1",
    command: TASK_CONTEXT_OPERATION,
    entry,
    mode,
    document_id: current.sourceTuple.document_id,
    source_revision: current.sourceTuple.revision,
    definition_revision: taskStoreDefinitionRevisionForManifest(asStoreCurrent(current), manifest),
    state_revision: taskStoreStateRevision(asStoreCurrent(current)),
    manifest_path: `${store.paths.relativeRoot}/manifest.json`,
    read_only: true
  };
}
function manifestForContext(root2, current) {
  const store = currentStore(root2, current);
  try {
    const manifest = store.manifest;
    if (!manifest)
      return null;
    const validation = store.validateCurrentAggregate(asStoreCurrent(current));
    if (validation.status !== "valid")
      throw new TaskStoreError("TASK_STORE_SOURCE_CONFLICT", `current aggregate is not consistent with CURRENT_TASK: ${validation.errors.join(" | ")}`);
    return manifest;
  } catch (error) {
    if (error instanceof TaskStoreError)
      throw error;
    throw error;
  }
}
function taskTitle(body) {
  return /^-\s*任务标题：(.+)$/mu.exec(body)?.[1]?.trim() ?? /^-\s*Task Title:\s*(.+)$/mu.exec(body)?.[1]?.trim() ?? null;
}
function currentStep(current) {
  const resolved = resolveTaskStep(current.body, current.runtimeState.active_step_id).current;
  return {
    step_id: resolved.id,
    status: current.runtimeState.active_step_status,
    description: resolved.description,
    purpose: resolved.purpose,
    planned_mutation_targets: resolved.planned_mutation_targets,
    mutation_scope: resolved.mutation_scope,
    required_evidence: resolved.required_evidence,
    review_checkpoint: resolved.review_checkpoint,
    checkpoint_boundary: resolved.checkpoint_boundary,
    metadata_complete: resolved.metadata_complete,
    plan_text: resolved.plan_text
  };
}
function slotSummary(claim, slot) {
  const check = record5(slot.check) ? slot.check : null;
  const report = record5(slot.report) ? slot.report : null;
  return {
    claim_id: typeof claim.claim_id === "string" ? claim.claim_id : null,
    claim_kind: typeof claim.claim_kind === "string" ? claim.claim_kind : null,
    requirement: typeof claim.requirement === "string" ? claim.requirement : null,
    slot_id: typeof slot.slot_id === "string" ? slot.slot_id : null,
    check_id: typeof check?.check_id === "string" ? check.check_id : null,
    check_method: typeof check?.method === "string" ? check.method : null,
    check_entry: typeof check?.entry === "string" ? check.entry : null,
    expected_result: typeof check?.expected_result === "string" ? check.expected_result : null,
    subject_paths: Array.isArray(check?.subject_paths) ? check.subject_paths.filter((item) => typeof item === "string") : [],
    minimum_type: typeof slot.minimum_type === "string" ? slot.minimum_type : null,
    disposition: typeof slot.disposition === "string" ? slot.disposition : null,
    user_decision: record5(slot.user_decision) ? { ...slot.user_decision } : null,
    due_step_id: typeof slot.due_step_id === "string" ? slot.due_step_id : null,
    before_step_id: typeof slot.before_step_id === "string" ? slot.before_step_id : null,
    applicability: typeof slot.applicability === "string" ? slot.applicability : null,
    result_id: typeof report?.result_id === "string" ? report.result_id : null,
    report_sha256: report ? sha25610(stableJson2(report)) : null,
    subject_revision: typeof report?.subject_revision === "string" ? report.subject_revision : null,
    evidence_refs: Array.isArray(slot.evidence_refs) ? slot.evidence_refs.filter((item) => typeof item === "string") : []
  };
}
function unfinishedObligations(root2, current) {
  const claims = Array.isArray(current.runtimeState.claim_evidence) ? current.runtimeState.claim_evidence : [];
  const result = [];
  for (const rawClaim of claims) {
    if (!record5(rawClaim) || !Array.isArray(rawClaim.slots))
      continue;
    for (const rawSlot of rawClaim.slots) {
      if (!record5(rawSlot))
        continue;
      const summary = slotSummary(rawClaim, rawSlot);
      const evaluation = evaluateEvidenceSlotForContext(root2, current, rawClaim, rawSlot);
      if (!evaluation.satisfied) {
        result.push({
          ...summary,
          satisfied: false,
          unsatisfied_reason: evaluation.reason
        });
      }
    }
  }
  return result;
}
function dependencyResults(current) {
  const claims = Array.isArray(current.runtimeState.claim_evidence) ? current.runtimeState.claim_evidence : [];
  const result = [];
  for (const rawClaim of claims) {
    if (!record5(rawClaim) || !Array.isArray(rawClaim.slots))
      continue;
    for (const rawSlot of rawClaim.slots) {
      if (!record5(rawSlot) || typeof rawSlot.before_step_id !== "string" && rawSlot.prerequisite_receipt === undefined)
        continue;
      const summary = slotSummary(rawClaim, rawSlot);
      result.push({
        ...summary,
        prerequisite_receipt_present: rawSlot.prerequisite_receipt !== undefined,
        prerequisite_receipt_revision: record5(rawSlot.prerequisite_receipt) && typeof rawSlot.prerequisite_receipt.review_target_revision === "string" ? rawSlot.prerequisite_receipt.review_target_revision : null
      });
    }
  }
  return result;
}
function unresolvedFindings(current) {
  return (Array.isArray(current.runtimeState.findings) ? current.runtimeState.findings : []).filter(record5).filter((finding) => finding.status !== "resolved" && finding.status !== "rejected").map((finding) => ({
    fingerprint: finding.fingerprint ?? null,
    status: finding.status ?? null,
    category: finding.category ?? null,
    file: finding.file ?? null,
    failure_condition: finding.failure_condition ?? null,
    violated_invariant: finding.violated_invariant ?? null,
    required_behavior: finding.violated_invariant ?? null,
    repair_attempts: finding.repair_attempts ?? null,
    max_repair_attempts: finding.max_repair_attempts ?? null,
    evidence_refs: Array.isArray(finding.evidence_refs) ? finding.evidence_refs : []
  }));
}
function latestExecution(current) {
  const entries = Array.isArray(current.runtimeState.execution_log) ? current.runtimeState.execution_log.filter(record5) : [];
  const active = [...entries].reverse().find((entry) => entry.step_id === current.runtimeState.active_step_id || entry.action === "record-review-result");
  if (!active)
    return null;
  return {
    idempotency_key: active.idempotency_key ?? null,
    step_id: active.step_id ?? null,
    action: active.action ?? null,
    status: active.status ?? null,
    recorded_at: active.recorded_at ?? null,
    execution_result_status: record5(active.execution_result) ? active.execution_result.outcome ?? null : null,
    evidence_refs: Array.isArray(active.evidence_refs) ? active.evidence_refs : [],
    result_id: record5(active.execution_result) ? active.execution_result.result_id ?? null : null
  };
}
function pendingReplanCandidateCount(current) {
  const entries = Array.isArray(current.runtimeState.execution_log) ? current.runtimeState.execution_log.filter(record5) : [];
  const lastReplan = entries.findLastIndex((item) => item.action === "commit-replan");
  return entries.slice(lastReplan + 1).filter((item) => item.action === "prepare-replan").length;
}
function pendingScopeAmendmentCandidateCount(current) {
  const directory = path19.join(path19.dirname(current.filePath), "task-candidates", current.sourceTuple.document_id);
  if (!fs18.existsSync(directory))
    return 0;
  const committed = new Set(current.runtimeState.execution_log.filter((item) => ("action" in item) && item.action === "commit-scope-amendment" && item.candidate_digest).map((item) => `${item.candidate_digest}.scope.json`));
  return fs18.readdirSync(directory).filter((name) => name.endsWith(".scope.json") && !committed.has(name) && !fs18.existsSync(path19.join(directory, `${name}.discarded`))).length;
}
function reviewTarget(current) {
  const coverage = record5(current.runtimeState.review_coverage) ? current.runtimeState.review_coverage : null;
  if (!coverage)
    return null;
  const target = record5(coverage.target) ? coverage.target : null;
  return {
    revision: typeof target?.revision === "string" ? target.revision : null,
    paths: Array.isArray(target?.entries) ? target.entries.filter(record5).map((entry) => ({ path: entry.path ?? null, state: entry.state ?? null, sha256: entry.sha256 ?? null })) : [],
    pending_paths: Array.isArray(coverage.pending_paths) ? coverage.pending_paths : [],
    last_clean_revision: coverage.last_clean_revision ?? null
  };
}
function storeNavigation(root2, current, manifest) {
  const paths = taskStorePaths(root2, current.sourceTuple.document_id);
  return {
    manifest_path: `${paths.relativeRoot}/manifest.json`,
    available: manifest !== null,
    event_count: manifest?.counts.events ?? 0,
    object_count: manifest?.counts.objects ?? 0,
    first_event_sequence: manifest?.head.event_range.first ?? null,
    last_event_sequence: manifest?.head.event_range.last ?? null,
    last_event_id: manifest?.head.event_id ?? null,
    last_event_hash: manifest?.head.event_hash ?? null,
    history_query: { kind: "events", reference: `${paths.relativeRoot}/events/` }
  };
}
function contextOverview(root2, current, manifest) {
  const state = current.runtimeState;
  const ledger = record5(state.step_attempts) && record5(state.step_attempts[state.active_step_id]) ? state.step_attempts[state.active_step_id] : null;
  const latest = latestExecution(current);
  const latestIndex = latest === null ? null : {
    idempotency_key: latest.idempotency_key ?? null,
    step_id: latest.step_id ?? null,
    action: latest.action ?? null,
    status: latest.status ?? null,
    recorded_at: latest.recorded_at ?? null,
    execution_result_status: latest.execution_result_status ?? null,
    result_id: latest.result_id ?? null,
    evidence_ref_count: Array.isArray(latest.evidence_refs) ? latest.evidence_refs.length : 0
  };
  const unfinishedCount = unfinishedObligations(root2, current).length;
  const dependencyCount = dependencyResults(current).length;
  const unknownDependencyCount = unfinishedObligations(root2, current).filter((item) => item.before_step_id === null).length;
  const unresolvedFindingCount = unresolvedFindings(current).length;
  const unresolvedChallengeCount = Array.isArray(state.evidence_challenges) ? state.evidence_challenges.filter(record5).filter((item) => item.status !== "resolved").length : 0;
  const pendingReview = record5(state.pending_review_result) ? state.pending_review_result : null;
  const retainedCleanReview = pendingReview?.verdict === "clean" && state.scope_amendment_pending_review_step_id !== undefined;
  const retainedFindingReview = pendingReview?.verdict === "findings" && state.scope_amendment_pending_review_step_id !== undefined;
  const dynamicReviewReady = dynamicReviewRequiredForCurrentExecution(current) && state.active_step_status === "in-progress" && latest?.execution_result_status !== null && latest?.execution_result_status !== undefined;
  const nextEntry = state.resume_requires_review ? "prepare-task:clear-resume-review" : retainedCleanReview ? "execute-step:complete-reviewed-step" : retainedFindingReview ? "execute-step:repair" : state.workflow_status === "blocked_by_replan" ? "prepare-task:amend-scope" : state.active_step_status === "blocked" ? "debug-task" : dynamicReviewReady ? "review-change" : "preflight-step";
  const nextOptions = state.workflow_status === "blocked_by_replan" ? ["prepare-task:amend-scope", "prepare-task:prepare-replan", "debug-task"] : state.active_step_status === "blocked" ? ["debug-task", "execute-step"] : [nextEntry];
  return {
    identity: {
      task_id: state.task_id,
      task_slug: state.task_slug,
      document_id: current.sourceTuple.document_id,
      title: taskTitle(current.body)
    },
    status: {
      workflow_status: state.workflow_status,
      lifecycle_state: state.lifecycle_state,
      active_step_id: state.active_step_id,
      active_step_status: state.active_step_status,
      resume_requires_review: state.resume_requires_review,
      resume_review_reasons: state.resume_review_reasons,
      finding_queue_revision: state.finding_queue_revision,
      review_cycle_id: record5(state.review_cycle) ? state.review_cycle.id ?? null : null,
      repair_round: record5(state.review_cycle) ? state.review_cycle.repair_round ?? 0 : 0
    },
    mutation_authority: current.mutationAuthority === null ? null : {
      version: 2,
      domains: [...current.mutationAuthority.domains],
      exact_exceptions: [...current.mutationAuthority.exact_exceptions],
      forbidden: [...current.mutationAuthority.forbidden]
    },
    dynamic_mutation: {
      review_required: dynamicReviewRequiredForCurrentExecution(current),
      expansions: (state.dynamic_expansions ?? []).slice(0, 64).map((item) => ({
        path: item.path,
        domain: item.domain,
        assessment: item.assessment,
        first_touch_state: item.first_touch_state,
        admitted_at: item.admitted_at,
        ...item.execution_id === undefined ? {} : {
          execution_id: item.execution_id,
          preflight_id: item.preflight_id,
          step_id: item.step_id,
          plan_revision: item.plan_revision,
          change_set_id: item.change_set_id,
          mode: item.mode
        },
        ...item.reviewed_by_review_id === undefined ? {} : {
          reviewed_by_review_id: item.reviewed_by_review_id,
          reviewed_at: item.reviewed_at
        }
      })),
      expansion_count: state.dynamic_expansions?.length ?? 0,
      expansions_truncated: (state.dynamic_expansions?.length ?? 0) > 64
    },
    next_entry: nextEntry,
    next_options: nextOptions,
    obligations: {
      unfinished_count: unfinishedCount,
      unfinished_block: { kind: "task-context-block", reference: "unfinished-obligations" },
      dependencies_recorded_count: dependencyCount,
      dependencies_block: { kind: "task-context-block", reference: "required-dependencies" },
      dependencies_unknown: unknownDependencyCount,
      unknown_dependencies_block: { kind: "task-context-block", reference: "unknown-dependencies" }
    },
    gates: {
      pending_review_verdict: record5(state.pending_review_result) ? state.pending_review_result.verdict ?? null : null,
      pending_review_step_id: record5(state.pending_review_result) ? state.pending_review_result.step_id ?? null : null,
      unresolved_findings_count: unresolvedFindingCount,
      unresolved_findings_block: { kind: "task-context-block", reference: "global-gates" },
      unresolved_evidence_challenges_count: unresolvedChallengeCount,
      unresolved_evidence_challenges_block: { kind: "task-context-block", reference: "global-gates" },
      attempt_count: Array.isArray(ledger?.attempts) ? ledger.attempts.length : 0,
      attempt_budget: ledger?.max_attempts ?? null,
      pending_replan_candidates: pendingReplanCandidateCount(current),
      pending_scope_amendment_candidates: pendingScopeAmendmentCandidateCount(current),
      dynamic_review_required: dynamicReviewRequiredForCurrentExecution(current),
      dynamic_expansion_count: state.dynamic_expansions?.length ?? 0
    },
    latest_execution: latestIndex,
    storage: storeNavigation(root2, current, manifest)
  };
}
function operationBlocks(root2, current, entry, mode, definitionReused, manifest) {
  const definitionAlgorithm = manifest?.definition_revision_algorithm;
  const definition = taskStoreDefinitionPayload(asStoreCurrent(current), definitionAlgorithm);
  const blocks = [];
  const add = (id, required, value) => blocks.push({ id, required, value });
  if (!definitionReused)
    add("current-definition", true, { revision: taskStoreDefinitionRevisionForManifest(asStoreCurrent(current), manifest), ...definition });
  add("current-step", true, currentStep(current));
  add("mutation-authority", true, current.mutationAuthority === null ? { version: null, domains: [], exact_exceptions: [], forbidden: [], legacy_mode: true } : {
    version: 2,
    domains: [...current.mutationAuthority.domains],
    exact_exceptions: [...current.mutationAuthority.exact_exceptions],
    forbidden: [...current.mutationAuthority.forbidden],
    legacy_mode: false
  });
  add("unfinished-obligations", true, unfinishedObligations(root2, current));
  add("required-dependencies", true, dependencyResults(current));
  add("unknown-dependencies", true, {
    status: "unknown",
    obligations: unfinishedObligations(root2, current).filter((item) => item.before_step_id === null).map((item) => ({
      claim_id: item.claim_id,
      slot_id: item.slot_id,
      due_step_id: item.due_step_id,
      result_id: item.result_id
    })),
    required_action: "Read the complete current definition and perform the bounded dependency check before skipping a gate."
  });
  add("global-gates", true, {
    resume_requires_review: current.runtimeState.resume_requires_review,
    resume_review_reasons: current.runtimeState.resume_review_reasons,
    pending_review_result: record5(current.runtimeState.pending_review_result) ? {
      verdict: current.runtimeState.pending_review_result.verdict ?? null,
      step_id: current.runtimeState.pending_review_result.step_id ?? null,
      review_cycle_id: current.runtimeState.pending_review_result.review_cycle_id ?? null,
      latest_execution_id: current.runtimeState.pending_review_result.latest_execution_id ?? null
    } : null,
    unresolved_findings: unresolvedFindings(current),
    unresolved_evidence_challenges: Array.isArray(current.runtimeState.evidence_challenges) ? current.runtimeState.evidence_challenges.filter(record5).filter((item) => item.status !== "resolved").map((item) => ({ challenge_id: item.challenge_id ?? null, status: item.status ?? null, claim_id: item.claim_id ?? null, slot_id: item.slot_id ?? null, result_id: item.result_id ?? null })) : [],
    active_attempt: record5(current.runtimeState.step_attempts) && record5(current.runtimeState.step_attempts[current.runtimeState.active_step_id]) ? current.runtimeState.step_attempts[current.runtimeState.active_step_id] : null,
    dynamic_review_required: dynamicReviewRequiredForCurrentExecution(current),
    dynamic_expansions: current.runtimeState.dynamic_expansions ?? []
  });
  add("latest-execution", true, latestExecution(current));
  if (entry === "review-change" || entry === "review-context" || entry === "review" || entry.includes("review") || mode === "review")
    add("cumulative-review-target", true, reviewTarget(current));
  add("history-navigation", false, storeNavigation(root2, current, manifest));
  return { blocks, required: blocks.filter((block) => block.required).map((block) => block.id), optional: blocks.filter((block) => !block.required).map((block) => block.id) };
}
function fits(base, maxBytes) {
  return byteLength(JSON.stringify(base)) <= maxBytes;
}
function jsonChunk(serialized, offset, base, block, maxBytes, fitsPage) {
  const bytes = Buffer.from(serialized, "utf8");
  const prefix = Array.isArray(base.blocks) ? base.blocks : [];
  if (offset >= bytes.length) {
    return {
      block: { ...block, value: undefined, encoding: "json", text: "", byte_offset: bytes.length, total_bytes: bytes.length, truncated: false },
      nextOffset: bytes.length
    };
  }
  let low = 1;
  let high = Math.max(1, Math.min(bytes.length - offset, maxBytes));
  let best = 0;
  while (low <= high) {
    const middle = Math.floor((low + high) / 2);
    const end = utf8Boundary(bytes, offset + middle);
    const candidateBlock = { ...block, value: undefined, encoding: "json", text: bytes.subarray(offset, end).toString("utf8"), byte_offset: offset, total_bytes: bytes.length, truncated: end < bytes.length };
    const candidate = { ...base, blocks: [...prefix, candidateBlock] };
    if (fits(candidate, maxBytes) && (fitsPage === undefined || fitsPage(candidateBlock, end))) {
      best = middle;
      low = middle + 1;
    } else
      high = middle - 1;
  }
  if (best <= 0)
    throw new Error("TASK_CONTEXT_BUDGET_EXHAUSTED: response metadata leaves no room for the next required content chunk.");
  const end = utf8Boundary(bytes, offset + best);
  const safeText = bytes.subarray(offset, end).toString("utf8");
  const consumed = end - offset;
  return {
    block: { ...block, value: undefined, encoding: "json", text: safeText, byte_offset: offset, total_bytes: bytes.length, truncated: end < bytes.length },
    nextOffset: offset + consumed
  };
}
function utf8Boundary(bytes, requested) {
  let end = Math.max(0, Math.min(requested, bytes.length));
  while (end > 0 && end < bytes.length && (bytes[end] & 192) === 128)
    end--;
  return end;
}
function contextPage(root2, current, input) {
  const entry = typeof input.entry === "string" && input.entry.trim() ? input.entry.trim() : typeof input.operation === "string" && input.operation.trim() ? input.operation.trim() : "validate";
  const mode = typeof input.mode === "string" && input.mode.trim() ? input.mode.trim() : "default";
  const maxBytes = integer(input.max_bytes, 16 * 1024, 256, 64 * 1024);
  const manifest = manifestForContext(root2, current);
  const definitionRevision = taskStoreDefinitionRevisionForManifest(asStoreCurrent(current), manifest);
  const stateRevision = taskStoreStateRevision(asStoreCurrent(current));
  const visibleRevision = input.visible_definition_revision ?? input.known_definition_revision;
  const definitionReused = input.definition_visible === true && visibleRevision === definitionRevision;
  const continuation = parseContinuation(input.continuation, "task-context-page/v1");
  if (continuation && (continuation.source_revision !== current.sourceTuple.revision || continuation.definition_revision !== definitionRevision || continuation.state_revision !== stateRevision)) {
    throw new Error("TASK_CONTEXT_STALE: current definition/state changed; start a fresh task-context read.");
  }
  const aggregate = {
    document_id: current.sourceTuple.document_id,
    task_id: current.runtimeState.task_id,
    task_slug: current.runtimeState.task_slug,
    source_revision: current.sourceTuple.revision,
    definition_revision: definitionRevision,
    state_revision: stateRevision,
    storage_manifest_path: `${taskStorePaths(root2, current.sourceTuple.document_id).relativeRoot}/manifest.json`
  };
  const selection = { entry, mode, required: [], optional: [], definition_reused: definitionReused };
  const built = operationBlocks(root2, current, entry, mode, definitionReused, manifest);
  selection.required = built.required;
  selection.optional = built.optional;
  const overview = contextOverview(root2, current, manifest);
  const base = {
    status: "success",
    operation_kind: TASK_CONTEXT_OPERATION,
    committed: false,
    aggregate,
    selection,
    overview,
    blocks: [],
    returned: { block_count: 0, total_block_count: built.blocks.length, byte_count: 0, required_complete: false },
    required_unexpanded: built.required,
    optional_unexpanded: built.optional,
    complete_for_operation: false,
    continuation: null
  };
  let blockIndex = continuation?.block_index ?? 0;
  let byteOffset = continuation?.byte_offset ?? 0;
  if (blockIndex > built.blocks.length)
    throw new Error("TASK_CONTEXT_CONTINUATION_INVALID: block_index is outside the selected operation.");
  if (blockIndex < built.blocks.length) {
    const blockBytes = Buffer.byteLength(stableJson2(built.blocks[blockIndex].value), "utf8");
    if (byteOffset > blockBytes)
      throw new Error("TASK_CONTEXT_CONTINUATION_INVALID: byte_offset is outside the selected block.");
  } else if (byteOffset !== 0) {
    throw new Error("TASK_CONTEXT_CONTINUATION_INVALID: terminal cursor must have byte_offset 0.");
  }
  const returned = [];
  let next = null;
  const makePage = (pageBlocks, cursor) => {
    const unexpandedIds = built.blocks.slice(cursor ? cursor.block_index : built.blocks.length).map((block) => block.id);
    const requiredUnexpanded = unexpandedIds.filter((id) => built.required.includes(id));
    const optionalUnexpanded = unexpandedIds.filter((id) => built.optional.includes(id));
    const complete = cursor === null && requiredUnexpanded.length === 0 && optionalUnexpanded.length === 0;
    return {
      ...base,
      status: complete ? "success" : "partial",
      blocks: pageBlocks,
      returned: {
        block_count: pageBlocks.length,
        total_block_count: built.blocks.length,
        byte_count: byteLength(JSON.stringify(pageBlocks)),
        required_complete: requiredUnexpanded.length === 0
      },
      required_unexpanded: requiredUnexpanded,
      optional_unexpanded: optionalUnexpanded,
      complete_for_operation: complete,
      continuation: cursor,
      receipt: {
        kind: TASK_CONTEXT_RECEIPT_KIND,
        document_id: current.sourceTuple.document_id,
        source_revision: current.sourceTuple.revision,
        definition_revision: definitionRevision,
        state_revision: stateRevision,
        selection: { entry, mode },
        returned_block_ids: pageBlocks.map((block) => block.id),
        complete_for_operation: complete
      }
    };
  };
  while (blockIndex < built.blocks.length) {
    const original = built.blocks[blockIndex];
    const serialized = stableJson2(original.value);
    const full = { id: original.id, required: original.required, value: original.value };
    const candidate = { ...base, blocks: [...returned, full] };
    if (fits(candidate, maxBytes)) {
      returned.push(full);
      blockIndex++;
      byteOffset = 0;
      continue;
    }
    const serializedBytes = Buffer.byteLength(serialized, "utf8");
    const chunked = jsonChunk(serialized, byteOffset, { ...base, blocks: returned }, original, maxBytes, (candidateBlock, candidateOffset) => {
      const candidateNext = candidateOffset < serializedBytes ? { kind: "task-context-page/v1", source_revision: current.sourceTuple.revision, definition_revision: definitionRevision, state_revision: stateRevision, block_index: blockIndex, byte_offset: candidateOffset } : blockIndex + 1 < built.blocks.length ? { kind: "task-context-page/v1", source_revision: current.sourceTuple.revision, definition_revision: definitionRevision, state_revision: stateRevision, block_index: blockIndex + 1, byte_offset: 0 } : null;
      return fits(makePage([...returned, candidateBlock], candidateNext), maxBytes);
    });
    returned.push(chunked.block);
    const after = chunked.nextOffset;
    if (after < Buffer.byteLength(serialized, "utf8"))
      next = { kind: "task-context-page/v1", source_revision: current.sourceTuple.revision, definition_revision: definitionRevision, state_revision: stateRevision, block_index: blockIndex, byte_offset: after };
    else if (blockIndex + 1 < built.blocks.length)
      next = { kind: "task-context-page/v1", source_revision: current.sourceTuple.revision, definition_revision: definitionRevision, state_revision: stateRevision, block_index: blockIndex + 1, byte_offset: 0 };
    blockIndex = built.blocks.length;
    break;
  }
  const response = makePage(returned, next);
  if (!fits(response, maxBytes)) {
    throw new Error("TASK_CONTEXT_BUDGET_EXHAUSTED: task-context metadata exceeds the requested page budget.");
  }
  return response;
}
function taskContext(root2, input = {}) {
  const value = contextInput(input, ["entry", "mode", "operation", "definition_visible", "visible_definition_revision", "known_definition_revision", "continuation", "max_bytes"]);
  if (value.definition_visible !== undefined && typeof value.definition_visible !== "boolean")
    throw new Error("TASK_CONTEXT_INPUT_INVALID: definition_visible must be boolean.");
  if (value.visible_definition_revision !== undefined && (typeof value.visible_definition_revision !== "string" || !/^[a-f0-9]{64}$/u.test(value.visible_definition_revision)))
    throw new Error("TASK_CONTEXT_INPUT_INVALID: visible_definition_revision must be SHA-256.");
  if (value.known_definition_revision !== undefined && (typeof value.known_definition_revision !== "string" || !/^[a-f0-9]{64}$/u.test(value.known_definition_revision)))
    throw new Error("TASK_CONTEXT_INPUT_INVALID: known_definition_revision must be SHA-256.");
  const current = readCanonicalCurrentTask(root2);
  return contextPage(root2, current, value);
}
function readHistoryMaterial(root2, current, sourceRevision) {
  if (sourceRevision === current.sourceTuple.revision)
    return current.raw;
  const stored = currentStore(root2, current).readHistoryMaterial(sourceRevision);
  if (stored !== null)
    return stored;
  const file = taskStoreHistoryPath(root2, asStoreCurrent(current), sourceRevision);
  if (fs18.existsSync(file)) {
    try {
      const parsed = JSON.parse(fs18.readFileSync(file, "utf8"));
      if (parsed.kind === "vnext-task-definition-history" && parsed.source_revision === sourceRevision && typeof parsed.current_task_base64 === "string") {
        const raw = Buffer.from(parsed.current_task_base64, "base64").toString("utf8");
        if (sha25610(raw) !== sourceRevision)
          throw new Error("history package source digest does not match its base64 preimage.");
        return raw;
      }
    } catch (error) {
      if (error instanceof Error && error.message.includes("source digest"))
        throw new Error(`TASK_READ_HISTORY_INVALID: ${error.message}`);
    }
  }
  const manifest = currentStore(root2, current).manifest;
  const legacyReference = manifest?.object_refs.legacy_source;
  if (legacyReference) {
    const legacy = currentStore(root2, current).readObject(legacyReference);
    if (record5(legacy.payload) && legacy.payload.source_revision === sourceRevision && typeof legacy.payload.raw_base64 === "string") {
      const raw = Buffer.from(legacy.payload.raw_base64, "base64").toString("utf8");
      if (sha25610(raw) !== sourceRevision)
        throw new Error(`TASK_READ_HISTORY_INVALID: retained legacy preimage digest does not match ${sourceRevision}.`);
      return raw;
    }
  }
  throw new Error(`TASK_READ_HISTORY_MISSING: no exact history preimage for ${sourceRevision}.`);
}
function taskStoreReference(value) {
  return record5(value) && typeof value.sha256 === "string" && /^[a-f0-9]{64}$/u.test(value.sha256) && typeof value.object_type === "string";
}
function transactionPayload(store, event, kind) {
  const transaction = event.transaction;
  let raw;
  if (kind === "semantic-delta") {
    raw = transaction?.semantic_delta;
    if (raw === undefined && transaction?.proposal !== undefined) {
      const proposal = taskStoreReference(transaction.proposal) ? store.readTransactionPayload(transaction.proposal, "proposal") : transaction.proposal;
      raw = record5(proposal) ? proposal.semantic_delta : undefined;
    }
  } else {
    raw = transaction?.[kind];
  }
  if (raw === undefined)
    return;
  if (kind === "proposal" && taskStoreReference(raw))
    return store.readTransactionPayload(raw, "proposal");
  if (kind === "result" && taskStoreReference(raw))
    return store.readTransactionPayload(raw, "result");
  if (kind === "semantic-delta" && taskStoreReference(raw)) {
    if (raw.object_type === "proposal") {
      const proposal = store.readTransactionPayload(raw, "proposal");
      return record5(proposal) ? proposal.semantic_delta : undefined;
    }
    if (raw.object_type === "semantic-delta")
      return store.readObject(raw, "semantic-delta").payload;
  }
  return raw;
}
function resolvedRead(root2, current, input, expectedContentRevision) {
  const store = currentStore(root2, current);
  const aggregateManifest = manifestForContext(root2, current);
  const rawRef = input.ref;
  let ref = {};
  if (typeof rawRef === "string")
    ref = { reference: rawRef };
  else if (record5(rawRef))
    ref = rawRef;
  const kind = typeof input.kind === "string" ? input.kind : typeof ref.kind === "string" ? ref.kind : undefined;
  const objectSha = input.object_sha256 ?? input.sha256 ?? (typeof ref.sha256 === "string" ? ref.sha256 : undefined);
  const eventPath = input.event_path ?? (typeof ref.path === "string" ? ref.path : undefined);
  const reference = typeof rawRef === "string" ? rawRef : eventPath ?? objectSha ?? kind ?? "definition";
  if (kind === "manifest") {
    const manifest = aggregateManifest;
    if (!manifest)
      throw new Error("TASK_READ_MANIFEST_MISSING: task store has not been initialized.");
    return { kind, reference: "manifest", required: true, value: manifest, encoding: "json" };
  }
  if (objectSha !== undefined && kind !== undefined && kind !== "object" && !["definition", "state", "current-snapshot"].includes(kind)) {
    if (!/^[a-f0-9]{64}$/u.test(objectSha))
      throw new Error("TASK_READ_OBJECT_INVALID: object_sha256 must be SHA-256.");
    if (kind === "proposal")
      return { kind, reference: objectSha, required: true, value: store.readTransactionPayload({ sha256: objectSha, object_type: "proposal" }, "proposal"), encoding: "json" };
    if (kind === "result")
      return { kind, reference: objectSha, required: true, value: store.readTransactionPayload({ sha256: objectSha, object_type: "result" }, "result"), encoding: "json" };
    if (kind === "semantic-delta") {
      const proposal = store.readTransactionPayload({ sha256: objectSha, object_type: "proposal" }, "proposal");
      if (!record5(proposal) || proposal.semantic_delta === undefined)
        throw new Error("TASK_READ_SEMANTIC-DELTA_MISSING: the selected proposal has no semantic_delta payload.");
      return { kind, reference: objectSha, required: true, value: proposal.semantic_delta, encoding: "json" };
    }
    return { kind, reference: objectSha, required: true, value: store.readObject(objectSha), encoding: "json" };
  }
  if (kind === "definition" || kind === undefined && objectSha === undefined && eventPath === undefined && input.path === undefined) {
    if (objectSha !== undefined)
      return { kind, reference: objectSha, required: true, value: store.readObject(objectSha, "definition"), encoding: "json" };
    const manifest = aggregateManifest;
    if (manifest?.current_representation === ACTIVE_TASK_FORMAT)
      return {
        kind: "definition",
        reference: manifest.object_refs.definition.sha256,
        required: true,
        value: {
          kind: "vnext-task-resolved-view/v1",
          source_object: manifest.object_refs.definition,
          payload: taskStoreDefinitionPayload(asStoreCurrent(current), manifest.definition_revision_algorithm)
        },
        encoding: "json"
      };
    if (manifest)
      return { kind: "definition", reference: manifest.object_refs.definition.sha256, required: true, value: store.readObject(manifest.object_refs.definition, "definition"), encoding: "json" };
    return { kind: "definition", reference: taskStoreDefinitionRevisionForManifest(asStoreCurrent(current), aggregateManifest), required: true, value: taskStoreDefinitionPayload(asStoreCurrent(current), aggregateManifest?.definition_revision_algorithm), encoding: "json" };
  }
  if (kind === "state" || kind === "current-snapshot") {
    const manifest = aggregateManifest;
    if (!manifest)
      throw new Error("TASK_READ_MANIFEST_MISSING: task store has not been initialized.");
    const refValue = objectSha !== undefined ? { sha256: objectSha, object_type: kind } : kind === "state" ? manifest.object_refs.state : manifest.object_refs.current_snapshot;
    if (objectSha === undefined && manifest.current_representation === ACTIVE_TASK_FORMAT)
      return {
        kind,
        reference: refValue.sha256,
        required: true,
        value: {
          kind: "vnext-task-resolved-view/v1",
          source_object: refValue,
          payload: taskStoreStateSnapshotPayload(asStoreCurrent(current))
        },
        encoding: "json"
      };
    return { kind, reference: refValue.sha256, required: true, value: store.readObject(refValue, refValue.object_type), encoding: "json" };
  }
  if (kind === "claim-evidence") {
    return {
      kind,
      reference: "claim-evidence",
      required: true,
      value: Array.isArray(current.runtimeState.claim_evidence) ? current.runtimeState.claim_evidence : [],
      encoding: "json"
    };
  }
  if (kind === "object" || objectSha !== undefined) {
    if (!objectSha || !/^[a-f0-9]{64}$/u.test(objectSha))
      throw new Error("TASK_READ_OBJECT_INVALID: object_sha256 must be SHA-256.");
    return { kind: "object", reference: objectSha, required: true, value: store.readObject(objectSha), encoding: "json" };
  }
  if (kind === "event") {
    const event = eventPath ? store.readEvent(eventPath) : store.readEvent({ sequence: input.event_sequence ?? Number(ref.sequence), event_hash: input.event_hash ?? String(ref.event_hash ?? "") });
    return { kind: "event", reference: `${event.sequence}-${event.event_hash}`, required: true, value: event, encoding: "json" };
  }
  if (kind === "proposal" || kind === "result" || kind === "semantic-delta") {
    const event = eventPath ? store.readEvent(eventPath) : store.readEvent({ sequence: input.event_sequence ?? Number(ref.sequence), event_hash: input.event_hash ?? String(ref.event_hash ?? "") });
    const value = transactionPayload(store, event, kind);
    if (value === undefined)
      throw new Error(`TASK_READ_${kind.toUpperCase()}_MISSING: the selected event has no ${kind} payload.`);
    return { kind, reference: `${event.sequence}-${event.event_hash}:${kind}`, required: true, value, encoding: "json" };
  }
  if (kind === "events" || kind === "history") {
    const events = store.listEvents().map((event) => ({ sequence: event.sequence, event_id: event.event_id, event_hash: event.event_hash, operation_kind: event.operation_kind, idempotency_key: event.idempotency_key, source_revision: event.source_revision, resulting_source_revision: event.resulting_source_revision, object_refs: event.object_refs, recorded_at: event.metadata.recorded_at }));
    return { kind: "events", reference: `${store.paths.relativeRoot}/events`, required: false, value: events, encoding: "json" };
  }
  if (kind === "legacy-current-task") {
    const manifest = aggregateManifest;
    if (!manifest?.object_refs.legacy_source)
      throw new Error("TASK_READ_OBJECT_MISSING: legacy source object is unavailable.");
    return { kind, reference: manifest.object_refs.legacy_source.sha256, required: true, value: store.readObject(manifest.object_refs.legacy_source, "legacy-current-task"), encoding: "json" };
  }
  if (kind === "history-material") {
    const sourceRevision = input.source_revision ?? (typeof ref.source_revision === "string" ? ref.source_revision : undefined);
    if (!sourceRevision || !/^[a-f0-9]{64}$/u.test(sourceRevision))
      throw new Error("TASK_READ_HISTORY_INVALID: source_revision is required.");
    const requestedLine = input.old_line ?? (typeof ref.old_line === "number" ? ref.old_line : undefined);
    if (requestedLine !== undefined) {
      if (!Number.isSafeInteger(requestedLine) || requestedLine < 1)
        throw new Error("TASK_READ_HISTORY_INVALID: old_line must be a positive integer.");
      const locator = store.readHistoryLocator(sourceRevision, requestedLine);
      if (!locator) {
        const raw = readHistoryMaterial(root2, current, sourceRevision);
        const lines = raw.split(/\r\n?|\n/u);
        if (requestedLine > lines.length)
          throw new Error("TASK_READ_HISTORY_INVALID: old_line is outside the retained source preimage.");
        return {
          kind: "history-locator",
          reference: `${sourceRevision}:${requestedLine}`,
          required: true,
          value: { source_revision: sourceRevision, old_line: requestedLine, old_line_count: lines.length, text: lines[requestedLine - 1] ?? "", locator: "exact-preimage" },
          encoding: "json"
        };
      }
      return { kind: "history-locator", reference: `${sourceRevision}:${requestedLine}`, required: true, value: locator, encoding: "json" };
    }
    return { kind, reference: sourceRevision, required: true, value: readHistoryMaterial(root2, current, sourceRevision), encoding: "utf8" };
  }
  if (input.path) {
    const file = contextPath(root2, input.path);
    if (!fs18.existsSync(file.absolute))
      throw new Error(`TASK_READ_PATH_MISSING: ${file.relative}`);
    const result = readFileContext(root2, { operation: "read", path: file.relative, ...input.sha256 ?? expectedContentRevision ? { sha256: input.sha256 ?? expectedContentRevision } : {} });
    const contentRevision = typeof result.sha256 === "string" ? result.sha256 : undefined;
    if (expectedContentRevision !== undefined && contentRevision !== expectedContentRevision)
      throw new Error("TASK_READ_STALE: file content revision changed; start a fresh task-read.");
    return { kind: "file", reference: file.relative, required: true, value: result, encoding: "json", ...contentRevision ? { content_revision: contentRevision } : {} };
  }
  throw new Error("TASK_READ_INPUT_INVALID: provide a precise object, event, history, or file reference.");
}
function taskReadPage(root2, current, input) {
  const maxBytes = integer(input.max_bytes, 16 * 1024, 256, 64 * 1024);
  const continuation = parseContinuation(input.continuation, "task-read-page/v1");
  const manifest = currentStore(root2, current).manifest;
  if (continuation && continuation.source_revision !== current.sourceTuple.revision) {
    throw new Error("TASK_READ_STALE: source revision changed; start a fresh task-read.");
  }
  const definitionRevision = taskStoreDefinitionRevisionForManifest(asStoreCurrent(current), manifest);
  const stateRevision = taskStoreStateRevision(asStoreCurrent(current));
  if (continuation && (continuation.source_revision !== current.sourceTuple.revision || continuation.definition_revision !== definitionRevision || continuation.state_revision !== stateRevision))
    throw new Error("TASK_READ_STALE: source, definition, or state revision changed; start a fresh task-read.");
  const resolved = resolvedRead(root2, current, input, continuation?.content_revision);
  const serialized = resolved.encoding === "utf8" ? String(resolved.value) : stableJson2(resolved.value);
  const serializedBytes = Buffer.from(serialized, "utf8");
  const reference = `${resolved.kind}:${resolved.reference}`;
  if (continuation && continuation.reference !== reference)
    throw new Error("TASK_READ_STALE: exact reference changed; start a fresh task-read.");
  const offset = continuation?.byte_offset ?? integer(input.offset, 0, 0, serializedBytes.length);
  if (offset > serializedBytes.length)
    throw new Error("TASK_READ_CONTINUATION_INVALID: byte_offset is outside the selected value.");
  if (offset < serializedBytes.length && (serializedBytes[offset] & 192) === 128)
    throw new Error("TASK_READ_CONTINUATION_INVALID: byte_offset is not a UTF-8 boundary.");
  const aggregate = {
    document_id: current.sourceTuple.document_id,
    task_id: current.runtimeState.task_id,
    task_slug: current.runtimeState.task_slug,
    source_revision: current.sourceTuple.revision,
    definition_revision: definitionRevision,
    state_revision: stateRevision
  };
  const base = {
    status: "success",
    operation_kind: TASK_READ_OPERATION,
    committed: false,
    aggregate,
    selection: { kind: resolved.kind, reference: resolved.reference, required: resolved.required, total_bytes: serializedBytes.length },
    offset,
    returned_bytes: 0,
    total_bytes: serializedBytes.length,
    complete_for_operation: false,
    continuation: null
  };
  const makePage = (end) => {
    const safeEnd = utf8Boundary(serializedBytes, end);
    const text = serializedBytes.subarray(offset, safeEnd).toString("utf8");
    const complete = safeEnd === serializedBytes.length;
    const includeJsonValue = complete && offset === 0 && serializedBytes.length <= maxBytes;
    return {
      ...base,
      status: complete ? "success" : "partial",
      returned_bytes: byteLength(text),
      complete_for_operation: complete,
      continuation: complete ? null : {
        kind: "task-read-page/v1",
        source_revision: current.sourceTuple.revision,
        definition_revision: definitionRevision,
        state_revision: stateRevision,
        reference,
        byte_offset: safeEnd,
        ...resolved.content_revision ? { content_revision: resolved.content_revision } : {}
      },
      ...includeJsonValue && resolved.encoding === "json" ? { value: resolved.value } : { text, encoding: resolved.encoding },
      receipt: {
        kind: TASK_READ_RECEIPT_KIND,
        document_id: current.sourceTuple.document_id,
        source_revision: current.sourceTuple.revision,
        definition_revision: definitionRevision,
        state_revision: stateRevision,
        reference,
        returned_offset: offset,
        returned_bytes: byteLength(text),
        complete_for_operation: complete,
        ...resolved.content_revision ? { content_revision: resolved.content_revision } : {}
      }
    };
  };
  if (offset === serializedBytes.length) {
    const result = makePage(offset);
    if (!fits(result, maxBytes))
      throw new Error("TASK_READ_BUDGET_EXHAUSTED: read receipt metadata exceeds the requested page budget.");
    return result;
  }
  let low = offset + 1;
  let high = serializedBytes.length;
  let best = offset;
  while (low <= high) {
    const middle = Math.floor((low + high) / 2);
    const boundary = utf8Boundary(serializedBytes, middle);
    if (boundary > offset && fits(makePage(boundary), maxBytes)) {
      best = boundary;
      low = middle + 1;
    } else
      high = boundary - 1;
  }
  if (best === offset)
    throw new Error("TASK_READ_BUDGET_EXHAUSTED: response metadata leaves no room for content.");
  const result = makePage(best);
  if (!fits(result, maxBytes))
    throw new Error("TASK_READ_BUDGET_EXHAUSTED: read receipt metadata exceeds the requested page budget.");
  return result;
}
function parseExportContinuation(value) {
  if (value === undefined)
    return null;
  let parsed = value;
  if (typeof value === "string") {
    try {
      parsed = JSON.parse(value);
    } catch {
      throw new Error("TASK_EXPORT_CONTINUATION_INVALID: continuation is not valid JSON.");
    }
  }
  if (!record5(parsed) || parsed.kind !== "task-export-page/v1" || typeof parsed.source_revision !== "string" || !/^[a-f0-9]{64}$/u.test(parsed.source_revision) || typeof parsed.definition_revision !== "string" || !/^[a-f0-9]{64}$/u.test(parsed.definition_revision) || typeof parsed.state_revision !== "string" || !/^[a-f0-9]{64}$/u.test(parsed.state_revision) || !Number.isSafeInteger(parsed.byte_offset) || parsed.byte_offset < 0) {
    throw new Error("TASK_EXPORT_CONTINUATION_INVALID: continuation has an invalid binding.");
  }
  return parsed;
}
function taskStoreExportPage(root2, input = {}) {
  const value = contextInput(input, ["offset", "max_bytes", "continuation"]);
  const maxBytes = integer(value.max_bytes, 16 * 1024, 256, 64 * 1024);
  const current = readCanonicalCurrentTask(root2);
  const store = currentStore(root2, current);
  const definitionRevision = taskStoreDefinitionRevisionForManifest(asStoreCurrent(current), currentStore(root2, current).manifest);
  const stateRevision = taskStoreStateRevision(asStoreCurrent(current));
  const exported = store.exportAggregate();
  const serialized = Buffer.from(stableJson2(exported), "utf8");
  const continuation = parseExportContinuation(value.continuation);
  if (continuation && (continuation.source_revision !== current.sourceTuple.revision || continuation.definition_revision !== definitionRevision || continuation.state_revision !== stateRevision))
    throw new Error("TASK_EXPORT_STALE: source, definition, or state revision changed; start a fresh task-export.");
  const offset = continuation?.byte_offset ?? integer(value.offset, 0, 0, serialized.length);
  if (offset > serialized.length)
    throw new Error("TASK_EXPORT_CONTINUATION_INVALID: byte_offset is outside the aggregate export.");
  if (offset < serialized.length && (serialized[offset] & 192) === 128)
    throw new Error("TASK_EXPORT_CONTINUATION_INVALID: byte_offset is not a UTF-8 boundary.");
  const aggregate = {
    document_id: current.sourceTuple.document_id,
    task_id: current.runtimeState.task_id,
    task_slug: current.runtimeState.task_slug,
    source_revision: current.sourceTuple.revision,
    definition_revision: definitionRevision,
    state_revision: stateRevision
  };
  const reference = `${store.paths.relativeRoot}/manifest.json`;
  const base = {
    status: "success",
    operation_kind: "task-export",
    committed: false,
    aggregate,
    selection: { kind: "aggregate-export", reference, total_bytes: serialized.length },
    offset,
    returned_bytes: 0,
    total_bytes: serialized.length,
    text: "",
    complete_for_operation: false,
    continuation: null
  };
  const makePage = (end) => {
    const safeEnd = utf8Boundary(serialized, end);
    const text = serialized.subarray(offset, safeEnd).toString("utf8");
    const complete = safeEnd === serialized.length;
    return {
      ...base,
      status: complete ? "success" : "partial",
      returned_bytes: byteLength(text),
      text,
      complete_for_operation: complete,
      continuation: complete ? null : {
        kind: "task-export-page/v1",
        source_revision: current.sourceTuple.revision,
        definition_revision: definitionRevision,
        state_revision: stateRevision,
        byte_offset: safeEnd
      },
      receipt: {
        kind: "task-export-receipt/v1",
        document_id: current.sourceTuple.document_id,
        source_revision: current.sourceTuple.revision,
        definition_revision: definitionRevision,
        state_revision: stateRevision,
        returned_offset: offset,
        returned_bytes: byteLength(text),
        complete_for_operation: complete
      }
    };
  };
  if (offset === serialized.length) {
    const result = makePage(offset);
    if (!fits(result, maxBytes))
      throw new Error("TASK_EXPORT_BUDGET_EXHAUSTED: export receipt metadata exceeds the requested page budget.");
    return result;
  }
  let low = offset + 1;
  let high = serialized.length;
  let best = offset;
  while (low <= high) {
    const middle = Math.floor((low + high) / 2);
    const boundary = utf8Boundary(serialized, middle);
    if (boundary > offset && fits(makePage(boundary), maxBytes)) {
      best = boundary;
      low = middle + 1;
    } else
      high = boundary - 1;
  }
  if (best === offset)
    throw new Error("TASK_EXPORT_BUDGET_EXHAUSTED: export receipt metadata leaves no room for content.");
  const result = makePage(best);
  if (!fits(result, maxBytes))
    throw new Error("TASK_EXPORT_BUDGET_EXHAUSTED: export receipt metadata exceeds the requested page budget.");
  return result;
}
function taskRead(root2, input) {
  const value = contextInput(input, ["kind", "ref", "path", "sha256", "object_sha256", "event_path", "event_sequence", "event_hash", "source_revision", "old_line", "offset", "max_bytes", "continuation", "limit"]);
  const current = readCanonicalCurrentTask(root2);
  return taskReadPage(root2, current, value);
}
function taskContextMigrationPreview(root2) {
  const current = readCanonicalCurrentTask(root2);
  return previewTaskStorageMigration(root2, asStoreCurrent(current));
}
function taskContextMigrationCommit(root2, sourceRevision) {
  return withGovernanceWriteLock(root2, () => {
    recoverPendingTaskStoreCommit(root2);
    const current = readCanonicalCurrentTask(root2);
    return commitTaskStorageMigration(root2, asStoreCurrent(current), sourceRevision);
  });
}
function readCliInput() {
  if (process.stdin.isTTY)
    return {};
  const content = fs18.readFileSync(0, "utf8");
  return content.trim() ? JSON.parse(content) : {};
}
function cliRoot(args) {
  if (args.length !== 2 || args[0] !== "--root" || !args[1])
    throw new Error("Usage: <task-context|task-read|task-storage-migration|task-export> --root <project> (JSON on stdin)");
  return path19.resolve(args[1]);
}
async function runTaskContextCli(command, args = process.argv.slice(2)) {
  try {
    const root2 = cliRoot(args);
    if (command === "task-context") {
      const result = taskContext(root2, readCliInput());
      console.log(JSON.stringify(result, null, 2));
      return result.complete_for_operation ? 0 : 2;
    }
    if (command === "task-read") {
      const result = taskRead(root2, readCliInput());
      console.log(JSON.stringify(result, null, 2));
      return result.complete_for_operation ? 0 : 2;
    }
    if (command === "task-export") {
      const result = taskStoreExportPage(root2, readCliInput());
      console.log(JSON.stringify(result, null, 2));
      return result.complete_for_operation ? 0 : 2;
    }
    const input = readCliInput();
    if (!record5(input) || input.mode !== "preview" && input.mode !== "commit")
      throw new Error("TASK_MIGRATION_INPUT_INVALID: mode must be preview or commit.");
    if (input.mode === "preview") {
      console.log(JSON.stringify(taskContextMigrationPreview(root2), null, 2));
      return 0;
    }
    if (typeof input.source_revision !== "string")
      throw new Error("TASK_MIGRATION_INPUT_INVALID: commit requires source_revision.");
    console.log(JSON.stringify(taskContextMigrationCommit(root2, input.source_revision), null, 2));
    return 0;
  } catch (error) {
    console.error(error instanceof Error ? error.message : String(error));
    return error instanceof TaskStoreError && error.code === "TASK_STORE_SOURCE_CONFLICT" ? 2 : 1;
  }
}

// runtime/vnext/src/execute-step-adapter.ts
var EXECUTE_STEP_ADAPTER_COMMANDS = [
  "preflight-step",
  "extend-preflight",
  "apply-artifact-restore",
  "artifact-checkpoints",
  "evidence-context",
  "retry-step",
  "replace-validation",
  "begin-repair",
  "record-step-result",
  "complete-reviewed-step"
];
var MAX_ITEMS2 = 256;
var WINDOWS_ABSOLUTE_PATH2 = /^[A-Za-z]:[\\/]/u;
var SHA256_PATTERN4 = /^[a-f0-9]{64}$/u;
var SAFE_KEY_PATTERN3 = /^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/u;
function subjectSnapshotSummary(snapshot) {
  return {
    kind: snapshot.kind,
    revision: snapshot.revision,
    entry_count: snapshot.entries.length,
    entries_omitted: true
  };
}
function fail7(code, message) {
  throw new VNextRuntimeError(code, message);
}
function record6(value, location) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", `${location} must be an object.`);
  }
  return value;
}
function exactKeys3(value, expected, location) {
  const allowed = new Set(expected);
  const missing = expected.filter((key) => !(key in value));
  const unexpected = Object.keys(value).filter((key) => !allowed.has(key));
  if (missing.length > 0 || unexpected.length > 0) {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", `${location} keys mismatch; missing=[${missing.join(", ")}], unexpected=[${unexpected.join(", ")}].`);
  }
}
function text4(value, location, maximumLength = 4096) {
  if (typeof value !== "string")
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", `${location} must be a string.`);
  const normalized = value.trim();
  if (!normalized || normalized.length > maximumLength || /[\r\n]/u.test(normalized)) {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", `${location} must be one non-empty line of at most ${maximumLength} characters.`);
  }
  return normalized;
}
function nullableText(value, location, maximumLength = 4096) {
  return value === null ? null : text4(value, location, maximumLength);
}
function textList2(value, location, allowEmpty) {
  if (!Array.isArray(value) || value.length > MAX_ITEMS2 || !allowEmpty && value.length === 0) {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", `${location} must be a bounded${allowEmpty ? "" : " non-empty"} array.`);
  }
  const values = value.map((item, index) => text4(item, `${location}[${index}]`));
  if (new Set(values).size !== values.length) {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", `${location} must not contain duplicates.`);
  }
  return values;
}
function normalizePath(value, location, allowGlob) {
  const original = text4(value, location, 1024);
  const normalized = original.replace(/\\/gu, "/").replace(/^\.\//u, "");
  if (normalized.startsWith("/") || WINDOWS_ABSOLUTE_PATH2.test(original) || normalized.split("/").includes("..") || normalized.includes("\x00") || !allowGlob && normalized.includes("*")) {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", `${location} must be a repository-relative ${allowGlob ? "path or glob" : "exact path"} without traversal.`);
  }
  return normalized;
}
function pathList(value, location, allowEmpty, allowGlob = false) {
  if (!Array.isArray(value) || value.length > MAX_ITEMS2 || !allowEmpty && value.length === 0) {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", `${location} must be a bounded${allowEmpty ? "" : " non-empty"} array.`);
  }
  const values = value.map((item, index) => normalizePath(item, `${location}[${index}]`, allowGlob));
  if (new Set(values).size !== values.length) {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", `${location} must not contain duplicates.`);
  }
  return values;
}
function digest5(value) {
  return crypto15.createHash("sha256").update(JSON.stringify(value)).digest("hex");
}
function idempotencyKey(prefix, value) {
  return `${prefix}-${digest5(value).slice(0, 48)}`;
}
function authority2(current, kinds) {
  return kinds.map((kind) => ({
    kind,
    source: current.relativePath,
    subject: current.runtimeState.task_id,
    task_id: current.runtimeState.task_id,
    document_id: current.sourceTuple.document_id,
    source_revision: current.sourceTuple.revision
  }));
}
function assertExecutableTask(current) {
  if (current.runtimeState.workflow_status === "draft" && current.runtimeState.lifecycle_state === "active") {
    fail7("DRAFT_NOT_EXECUTABLE", "execute-step requires an explicitly confirmed task; the current task is draft + active.");
  }
  if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active") {
    fail7("TASK_STATE_NOT_ACTIVE", "execute-step requires active + active.");
  }
  if (current.runtimeState.resume_requires_review) {
    fail7("RESUME_REVIEW_REQUIRED", "execute-step is blocked by the current resume-review gate.");
  }
}
function parseScopeList(value, location) {
  if (!value)
    fail7("TASK_STEP_METADATA_INCOMPLETE", `${location} is missing.`);
  const items = value.split(",").map((item) => item.replace(/`/gu, "").trim()).filter(Boolean);
  if (items.length === 0 || new Set(items).size !== items.length) {
    fail7("TASK_STEP_METADATA_INCOMPLETE", `${location} must be a non-empty list without duplicates.`);
  }
  return items.map((item, index) => normalizePath(item, `${location}[${index}]`, true));
}
function parseValidationList(value, location) {
  if (!value)
    fail7("TASK_STEP_METADATA_INCOMPLETE", `${location} is missing.`);
  const items = value.split(/;\s+/u).map((item) => item.trim()).filter(Boolean);
  if (items.length === 0 || new Set(items).size !== items.length) {
    fail7("TASK_STEP_METADATA_INCOMPLETE", `${location} must be a non-empty list without duplicates.`);
  }
  return items;
}
function parsePlannedCommands(current, stepId) {
  const definition = readDraftDefinitionFromBody(current.body);
  const lines = definition.implementation_steps.replace(/\r\n?/gu, `
`).split(`
`);
  const commands = [];
  let inCurrentStep = false;
  for (let index = 0;index < lines.length; index += 1) {
    const step = /^-\s+([A-Za-z0-9][A-Za-z0-9._:-]{0,127})\s*[:：]/u.exec(lines[index]);
    if (step) {
      inCurrentStep = step[1] === stepId;
      continue;
    }
    if (!inCurrentStep)
      continue;
    const planned = /^\s{2,}-\s+planned_command:\s*(.+?)\s*$/u.exec(lines[index]);
    if (!planned)
      continue;
    const expected = /^\s{4,}-\s+expected_repo_writes:\s*(.+?)\s*$/u.exec(lines[index + 1] ?? "");
    const transformation = /^\s{4,}-\s+transformation_kind:\s*(localized|inherently-broad)\s*$/u.exec(lines[index + 2] ?? "");
    if (!expected || !transformation) {
      fail7("TASK_STEP_COMMAND_PLAN_INVALID", `planned command "${planned[1].trim()}" is missing canonical write-footprint metadata.`);
    }
    const rawTargets = expected[1].trim();
    const expectedWrites = rawTargets === "none" ? "none" : rawTargets.split(",").map((item, targetIndex) => normalizePath(item.trim(), `planned_command.expected_repo_writes[${targetIndex}]`, true));
    if (expectedWrites !== "none" && expectedWrites.length === 0) {
      fail7("TASK_STEP_COMMAND_PLAN_INVALID", `planned command "${planned[1].trim()}" has an empty write footprint.`);
    }
    commands.push({
      command: planned[1].trim(),
      expected_repo_writes: expectedWrites,
      transformation_kind: transformation[1]
    });
    index += 2;
  }
  if (new Set(commands.map((item) => item.command)).size !== commands.length) {
    fail7("TASK_STEP_COMMAND_PLAN_INVALID", `step ${stepId} contains duplicate planned commands.`);
  }
  return commands;
}
function currentStepPlan(current) {
  const resolution = resolveTaskStep(current.body, current.runtimeState.active_step_id);
  if (!resolution.current.metadata_complete || !resolution.current.purpose || !resolution.current.review_checkpoint) {
    fail7("TASK_STEP_METADATA_INCOMPLETE", `step ${resolution.current.id} is not executable because its canonical metadata is incomplete.`);
  }
  return {
    step: resolution.current,
    planned_mutation_targets: parseScopeList(resolution.current.planned_mutation_targets ?? resolution.current.mutation_scope, `step ${resolution.current.id} planned_mutation_targets`),
    mutation_scope: parseScopeList(resolution.current.mutation_scope, `step ${resolution.current.id} mutation_scope`),
    validation: parseValidationList(resolution.current.required_evidence, `step ${resolution.current.id} required_evidence`),
    commands: parsePlannedCommands(current, resolution.current.id)
  };
}
function stepAdmitsExactPath(file, stepScope) {
  return stepScope.some((pattern) => mutationScopePatternMatchesPath(file, pattern));
}
function stepAdmitsFootprintTarget(target, stepScope) {
  return target.includes("*") ? stepScope.includes(target) : stepAdmitsExactPath(target, stepScope);
}
function assertPathsAdmitted(current, stepPlan, paths, location, root2, assessments = [], mode = "default", phase, plannedTargets = stepPlan.planned_mutation_targets) {
  if (paths.length === 0)
    return;
  assertExecutionTargetAdmissions(root2, current, {
    target_paths: paths,
    planned_targets: plannedTargets,
    step_mutation_scope: stepPlan.mutation_scope,
    assessments,
    mode,
    phase,
    location
  });
}
function assertCommandPlansAdmitted(root2, current, stepPlan, assessments = [], phase, mode = "default") {
  if (current.mutationAuthority) {
    const project = (() => {
      try {
        return readProjectMutationAuthority(root2);
      } catch (error) {
        fail7(error instanceof MutationAuthorityError ? error.code : "MUTATION_AUTHORITY_PROJECT_INVALID", error instanceof Error ? error.message : String(error));
      }
    })();
    if (!project)
      fail7("MUTATION_AUTHORITY_PROJECT_REQUIRED", "v2 command footprints require PROJECT_PROFILE.yaml.mutation_authority.domains.");
    const commandWrites = stepPlan.commands.flatMap((command) => command.expected_repo_writes === "none" ? [] : command.expected_repo_writes);
    const evaluation = evaluateTaskMutationAuthorityPlan({
      project,
      task: current.mutationAuthority,
      planned_targets: [],
      command_write_targets: commandWrites,
      persistent_test_paths: []
    });
    const blocked = evaluation.decisions.find((item) => !item.admitted);
    if (blocked) {
      const command = stepPlan.commands.find((item) => item.expected_repo_writes !== "none" && item.expected_repo_writes.includes(blocked.path));
      fail7(mutationAuthorityPlanBlockerCode(blocked), `planned command${command ? ` "${command.command}"` : ""} is outside the v2 authority envelope: ${evaluation.blockers.join(" ")}`);
    }
    return;
  }
  const scope = parseMutationScope(current.body, current.sourceTuple.revision);
  for (const command of stepPlan.commands) {
    if (command.expected_repo_writes === "none")
      continue;
    const outsideStep = command.expected_repo_writes.filter((target) => !stepAdmitsFootprintTarget(target, stepPlan.mutation_scope));
    if (outsideStep.length > 0) {
      fail7("COMMAND_FOOTPRINT_BLOCKED", `planned command "${command.command}" writes outside current step scope: ${outsideStep.join(", ")}.`);
    }
    const result = evaluateCommandWriteFootprint(scope, {
      command: command.command,
      expected_write_footprint: {
        kind: "bounded",
        targets: command.expected_repo_writes,
        evidence_refs: [`adapter:execute-preflight:${stepPlan.step.id}`]
      },
      transformation_kind: command.transformation_kind
    });
    if (result.status !== "pass") {
      fail7("COMMAND_FOOTPRINT_BLOCKED", `planned command "${command.command}" is not executable: ${result.blockers.join(" ")}`);
    }
  }
}
function assertExactCommandWritesCovered(stepPlan, candidatePaths) {
  const missing = stepPlan.commands.flatMap((command) => command.expected_repo_writes === "none" ? [] : command.expected_repo_writes.filter((target) => !target.includes("*") && !candidatePaths.includes(target)));
  if (missing.length > 0) {
    fail7("COMMAND_FOOTPRINT_BLOCKED", `candidate_paths must include every exact planned command write: ${[...new Set(missing)].join(", ")}.`);
  }
}
function stepPlanRevision(stepPlan) {
  return digest5({
    step: stepPlan.step,
    planned_mutation_targets: stepPlan.planned_mutation_targets,
    mutation_scope: stepPlan.mutation_scope,
    validation: stepPlan.validation,
    commands: stepPlan.commands
  });
}
function changeSetId(current, stepId) {
  if (current.runtimeState.review_coverage)
    return current.runtimeState.review_coverage.change_set_id;
  return `change-set-${digest5({
    task_id: current.runtimeState.task_id,
    document_id: current.sourceTuple.document_id,
    step_id: stepId,
    review_cycle_id: current.runtimeState.review_cycle.id
  }).slice(0, 40)}`;
}
function testStrategyMode(value, location) {
  if (!["flexible", "test-first", "implementation-first", "not-applicable", "legacy"].includes(String(value))) {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", `${location} is not a supported test-strategy mode.`);
  }
  return value;
}
function executionPhase(value, location) {
  if (!["flexible", "test-first", "red", "green", "implementation-first", "not-applicable", "legacy"].includes(String(value))) {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", `${location} is not a supported execution phase.`);
  }
  return value;
}
function normalizePreflightReceipt(value) {
  const source = record6(value, "preflight_receipt");
  if (source.kind === "execute-step-repair-preflight/v1") {
    exactKeys3(source, [
      "kind",
      ...source.preflight_id === undefined ? [] : ["preflight_id"],
      ...source.execution_id === undefined ? [] : ["execution_id"],
      "task_id",
      "document_id",
      "source_revision",
      "step_id",
      "plan_revision",
      "mode",
      "test_strategy_mode",
      "execution_phase",
      "candidate_paths",
      "repair_fingerprints",
      "repair_wave_id",
      "change_set_id",
      "review_target_paths",
      "review_id",
      "review_base"
    ], "preflight_receipt");
    if (source.mode !== "repair")
      fail7("EXECUTE_ADAPTER_INPUT_INVALID", "repair preflight receipt mode must be repair.");
    const sourceRevision = text4(source.source_revision, "preflight_receipt.source_revision", 64);
    const planRevision = text4(source.plan_revision, "preflight_receipt.plan_revision", 64);
    if (!SHA256_PATTERN4.test(sourceRevision) || !SHA256_PATTERN4.test(planRevision))
      fail7("EXECUTE_ADAPTER_INPUT_INVALID", "preflight receipt revisions must be SHA-256 values.");
    return {
      kind: "execute-step-repair-preflight/v1",
      ...source.preflight_id === undefined ? {} : { preflight_id: text4(source.preflight_id, "preflight_receipt.preflight_id", 128) },
      ...source.execution_id === undefined ? {} : { execution_id: text4(source.execution_id, "preflight_receipt.execution_id", 128) },
      task_id: text4(source.task_id, "preflight_receipt.task_id", 128),
      document_id: text4(source.document_id, "preflight_receipt.document_id", 128),
      source_revision: sourceRevision,
      step_id: text4(source.step_id, "preflight_receipt.step_id", 128),
      plan_revision: planRevision,
      mode: "repair",
      test_strategy_mode: testStrategyMode(source.test_strategy_mode, "preflight_receipt.test_strategy_mode"),
      execution_phase: executionPhase(source.execution_phase, "preflight_receipt.execution_phase"),
      candidate_paths: pathList(source.candidate_paths, "preflight_receipt.candidate_paths", true),
      repair_fingerprints: textList2(source.repair_fingerprints, "preflight_receipt.repair_fingerprints", false),
      repair_wave_id: text4(source.repair_wave_id, "preflight_receipt.repair_wave_id", 128),
      change_set_id: text4(source.change_set_id, "preflight_receipt.change_set_id", 128),
      review_target_paths: pathList(source.review_target_paths, "preflight_receipt.review_target_paths", true),
      review_id: text4(source.review_id, "preflight_receipt.review_id", 128),
      review_base: validateRuntimeReviewTarget(source.review_base, "preflight_receipt.review_base")
    };
  }
  exactKeys3(source, [
    "kind",
    ...source.preflight_id === undefined ? [] : ["preflight_id"],
    ...source.execution_id === undefined ? [] : ["execution_id"],
    "task_id",
    "document_id",
    "source_revision",
    "step_id",
    "plan_revision",
    "mode",
    "test_strategy_mode",
    "execution_phase",
    "candidate_paths",
    "repair_fingerprint",
    "change_set_id",
    "review_base",
    ...source.attempt_id === undefined ? [] : ["attempt_id"],
    ...source.mutation_authority_version === undefined ? [] : ["mutation_authority_version"]
  ], "preflight_receipt");
  if (source.kind !== "execute-step-preflight/v1") {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", "preflight_receipt.kind must be execute-step-preflight/v1.");
  }
  const mode = source.mode;
  if (mode !== "default") {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", "execute-step-preflight/v1 is default-only; use begin-repair for repair mode.");
  }
  const sourceRevision = text4(source.source_revision, "preflight_receipt.source_revision", 64);
  const planRevision = text4(source.plan_revision, "preflight_receipt.plan_revision", 64);
  if (!SHA256_PATTERN4.test(sourceRevision) || !SHA256_PATTERN4.test(planRevision)) {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", "preflight receipt revisions must be SHA-256 values.");
  }
  return {
    kind: source.kind,
    ...source.preflight_id === undefined ? {} : { preflight_id: text4(source.preflight_id, "preflight_receipt.preflight_id", 128) },
    ...source.execution_id === undefined ? {} : { execution_id: text4(source.execution_id, "preflight_receipt.execution_id", 128) },
    ...source.attempt_id === undefined ? {} : { attempt_id: text4(source.attempt_id, "preflight_receipt.attempt_id", 128) },
    task_id: text4(source.task_id, "preflight_receipt.task_id", 128),
    document_id: text4(source.document_id, "preflight_receipt.document_id", 128),
    source_revision: sourceRevision,
    step_id: text4(source.step_id, "preflight_receipt.step_id", 128),
    plan_revision: planRevision,
    mode,
    test_strategy_mode: testStrategyMode(source.test_strategy_mode, "preflight_receipt.test_strategy_mode"),
    execution_phase: executionPhase(source.execution_phase, "preflight_receipt.execution_phase"),
    candidate_paths: pathList(source.candidate_paths, "preflight_receipt.candidate_paths", true),
    repair_fingerprint: nullableText(source.repair_fingerprint, "preflight_receipt.repair_fingerprint", 128),
    change_set_id: text4(source.change_set_id, "preflight_receipt.change_set_id", 128),
    review_base: validateRuntimeReviewTarget(source.review_base, "preflight_receipt.review_base"),
    ...source.mutation_authority_version === undefined ? {} : source.mutation_authority_version === 2 ? { mutation_authority_version: 2 } : fail7("EXECUTE_ADAPTER_INPUT_INVALID", "preflight_receipt.mutation_authority_version must be 2.")
  };
}
function assertCurrentReceipt(root2, current, stepPlan, receipt) {
  if (receipt.task_id !== current.runtimeState.task_id || receipt.document_id !== current.sourceTuple.document_id) {
    fail7("EXECUTE_PREFLIGHT_IDENTITY_CONFLICT", "preflight receipt does not identify the current task document.");
  }
  if (!taskSourceRevisionMatches(root2, current, receipt.source_revision)) {
    const expectedRepairBookkeeping = receipt.kind === "execute-step-repair-preflight/v1" && current.runtimeState.pending_review_result?.review_id === receipt.review_id && receipt.repair_fingerprints.every((fingerprint) => {
      const finding = current.runtimeState.findings.find((item) => item.fingerprint === fingerprint);
      return finding?.last_repair_wave_id === receipt.repair_wave_id && finding.status === "in-progress";
    });
    if (!expectedRepairBookkeeping) {
      fail7("EXECUTE_PREFLIGHT_STALE", "CURRENT_TASK changed after preflight; run preflight-step again before editing or committing.");
    }
  }
  if (receipt.step_id !== current.runtimeState.active_step_id || receipt.plan_revision !== stepPlanRevision(stepPlan)) {
    fail7("EXECUTE_PREFLIGHT_STALE", "the active step or its executable plan changed after preflight.");
  }
  const strategy = resolveTestStrategyExecutionContext(current);
  const activePreflight = current.runtimeState.execution_preflight;
  const expectedPhase = activePreflight?.step_id === receipt.step_id ? activePreflight.execution_phase : executionPhaseForCurrentStep(current, strategy);
  if (receipt.test_strategy_mode !== strategy.mode || receipt.execution_phase !== expectedPhase) {
    fail7("EXECUTE_PREFLIGHT_STALE", "the frozen test strategy or current execution phase changed after preflight.");
  }
  if (activePreflight?.step_id === receipt.step_id) {
    if (activePreflight.mode !== receipt.mode || activePreflight.plan_revision !== receipt.plan_revision || (current.mutationAuthority || receipt.mode === "repair") && receipt.execution_id !== activePreflight.execution_id || receipt.execution_id !== undefined && activePreflight.execution_id !== receipt.execution_id || digest5(activePreflight.candidate_paths) !== digest5(receipt.candidate_paths) || receipt.preflight_id !== undefined && activePreflight.preflight_id !== receipt.preflight_id) {
      fail7("EXECUTE_PREFLIGHT_STALE", "the preflight receipt does not bind the current Runtime execution identity.");
    }
    if (receipt.mode === "repair" && activePreflight.review_target_paths !== null && digest5(activePreflight.review_target_paths) !== digest5(receipt.review_target_paths)) {
      fail7("EXECUTE_PREFLIGHT_STALE", "the repair receipt does not bind the Runtime-recorded reviewed target paths.");
    }
    if (receipt.mode === "repair" && (activePreflight.review_id !== receipt.review_id || activePreflight.repair_wave_id !== receipt.repair_wave_id || activePreflight.change_set_id !== receipt.change_set_id)) {
      fail7("EXECUTE_PREFLIGHT_STALE", "the repair receipt does not bind the current Runtime repair identity.");
    }
  }
  if (receipt.kind === "execute-step-repair-preflight/v1") {
    if (current.runtimeState.pending_review_result?.review_id !== receipt.review_id || current.runtimeState.pending_review_result.change_set_id !== receipt.change_set_id) {
      fail7("EXECUTE_PREFLIGHT_STALE", "the repair review or Runtime-owned change set changed after preflight.");
    }
  } else {
    if (current.mutationAuthority) {
      const latestAttempt = current.runtimeState.step_attempts?.[receipt.step_id]?.attempts.at(-1);
      if (!receipt.preflight_id || !latestAttempt || latestAttempt.idempotency_key !== receipt.preflight_id) {
        fail7("EXECUTE_PREFLIGHT_STALE", "the ordinary preflight receipt is no longer the latest receipt; use the replacement receipt returned by extend-preflight.");
      }
    }
    if (receipt.change_set_id !== changeSetId(current, receipt.step_id)) {
      fail7("EXECUTE_PREFLIGHT_STALE", "the Runtime-owned change set identity changed after preflight.");
    }
  }
}
function findingAdmissionWaveId(reviewId) {
  return `finding-wave-${digest5(reviewId).slice(0, 32)}`;
}
function repairWaveId(reviewId, fingerprints) {
  return `repair-wave-${digest5({ review_id: reviewId, fingerprints: [...fingerprints].sort() }).slice(0, 32)}`;
}
function currentStepResult(stepPlan, strategy) {
  return {
    id: stepPlan.step.id,
    description: stepPlan.step.description,
    purpose: stepPlan.step.purpose,
    planned_mutation_targets: stepPlan.planned_mutation_targets,
    mutation_scope: stepPlan.mutation_scope,
    commands: stepPlan.commands,
    validation: stepPlan.validation,
    review_checkpoint: stepPlan.step.review_checkpoint,
    test_strategy_mode: strategy.mode,
    execution_phase: strategy.phase,
    required_outcome: strategy.required_outcome,
    persistent_tests: [...strategy.persistent_tests]
  };
}
function beginRepair(root2, input, options = {}) {
  const source = record6(input, "begin-repair input");
  exactKeys3(source, ["candidate_paths", ...source.blast_radius_assessments === undefined ? [] : ["blast_radius_assessments"]], "begin-repair input");
  const candidatePaths = pathList(source.candidate_paths, "candidate_paths", false);
  let assessments = [];
  if (source.blast_radius_assessments !== undefined) {
    try {
      assessments = normalizeBlastRadiusAssessments(source.blast_radius_assessments);
    } catch (error) {
      fail7(error instanceof MutationAuthorityError ? error.code : "MUTATION_AUTHORITY_ASSESSMENT_INVALID", error instanceof Error ? error.message : String(error));
    }
  }
  let current = readCanonicalCurrentTask(root2);
  assertExecutableTask(current);
  const pending = current.runtimeState.pending_review_result;
  if (!pending || pending.verdict !== "findings") {
    fail7("REVIEW_FINDINGS_REQUIRED", "begin-repair requires the current durable review result to contain findings.");
  }
  const reviewedExecution = current.runtimeState.execution_log.map((item) => ("action" in item) ? item : current.runtimeState.scope_amendment_pending_review_step_id !== undefined && item.idempotency_key === pending.execution_id ? item : cumulativeReviewExecution(current, item)).find((item) => !("action" in item) && item.idempotency_key === pending.execution_id);
  if (!reviewedExecution?.execution_result || reviewedExecution.execution_result.change_set_id !== pending.change_set_id || reviewedExecution.execution_result.review_target.revision !== pending.review_target_revision) {
    fail7("REVIEW_TARGET_CONFLICT", "begin-repair requires the Runtime-recorded reviewed execution target.");
  }
  const stepPlan = currentStepPlan(current);
  const strategy = resolveTestStrategyExecutionContext(current);
  assertTestStrategySequenceReady(current, strategy);
  const phase = executionPhaseForCurrentStep(current, strategy);
  assertPathsAdmitted(current, stepPlan, candidatePaths, "candidate_paths", root2, assessments, "repair", phase);
  assertCommandPlansAdmitted(root2, current, stepPlan, assessments, phase, "repair");
  assertExactCommandWritesCovered(stepPlan, candidatePaths);
  const admissionWaveId = findingAdmissionWaveId(pending.review_id);
  const previousStepCompletion = currentDefinitionExecutionLog(current).findLast((item) => !("action" in item) && item.advancement === "advanced" && item.next_step_id === pending.step_id && item.review_receipt?.cycle_id === current.runtimeState.review_cycle.id);
  const admissionCycleId = pending.cycle_phase === "discovery" && pending.cycle_id === current.runtimeState.review_cycle.id && previousStepCompletion && !("action" in previousStepCompletion) ? reviewCycleForNextStep(current.runtimeState.review_cycle.id, pending.step_id, previousStepCompletion.idempotency_key).id : current.runtimeState.review_cycle.id;
  for (const candidate of pending.findings) {
    if (!candidatePaths.includes(candidate.file)) {
      fail7("EXECUTE_PREFLIGHT_SCOPE_CONFLICT", `candidate_paths must include review finding path ${candidate.file}.`);
    }
    const admissionKey = idempotencyKey("execute-review-admit", { review_id: pending.review_id, fingerprint: candidate.fingerprint });
    if (hasAppliedProposal2(current, admissionKey))
      continue;
    const proposal = createFindingQueueProposal(current, {
      mode: "repair",
      delta: {
        kind: "finding-queue",
        action: "admit",
        cycle_phase: pending.cycle_phase,
        finding_admission_wave_id: admissionWaveId,
        finding: {
          fingerprint: candidate.fingerprint,
          category: candidate.category,
          owner_task_id: current.runtimeState.task_id,
          scope: "admitted",
          decision: "mechanical",
          file: candidate.file,
          failure_condition: candidate.failure_condition,
          violated_invariant: candidate.required_behavior,
          root_cause_status: candidate.root_cause_status,
          max_repair_attempts: MAX_REPAIR_ATTEMPTS,
          evidence_refs: candidate.evidence_refs,
          review_cycle_id: admissionCycleId
        }
      },
      idempotency_key: admissionKey,
      authority_evidence: authority2(current, ["active-task-owner", "scope-admission", "finding-admission", "evidence-admission"]),
      evidence_refs: candidate.evidence_refs
    });
    const result = verifyReadBack(root2, applyVNextRuntimeProposal(root2, proposal, options), options);
    if (result.status !== "success" && result.status !== "no-op") {
      fail7("FINDING_ADMISSION_BLOCKED", result.message);
    }
    if (!options.dryRun)
      current = readCanonicalCurrentTask(root2);
  }
  const fingerprints = [...new Set([
    ...pending.unresolved_fingerprints,
    ...pending.findings.map((item) => item.fingerprint)
  ])].sort();
  for (const fingerprint of fingerprints) {
    const finding = current.runtimeState.findings.find((item) => item.fingerprint === fingerprint);
    if (!options.dryRun && (!finding || !["admitted", "in-progress"].includes(finding.status))) {
      fail7("FINDING_ADMISSION_REQUIRED", `review finding ${fingerprint} is not repairable.`);
    }
    if (!options.dryRun && finding.repair_attempts >= finding.max_repair_attempts) {
      fail7("REPAIR_BUDGET_EXHAUSTED", `finding ${fingerprint} has exhausted its repair budget.`);
    }
  }
  const waveId = repairWaveId(pending.review_id, fingerprints);
  const reviewTargetPaths = [...new Set([
    ...reviewedExecution.execution_result.review_target.entries.map((item) => item.path),
    ...candidatePaths
  ])];
  const preflightProposal = createStepPreflightProposal(current, candidatePaths, assessments, {
    mode: "repair",
    plan_revision: stepPlanRevision(stepPlan),
    execution_phase: phase,
    repair_fingerprints: fingerprints,
    repair_wave_id: waveId,
    review_id: pending.review_id,
    review_target_paths: reviewedExecution.execution_result.review_target.entries.map((item) => item.path),
    change_set_id: pending.change_set_id
  });
  const preflightResult = verifyReadBack(root2, applyVNextRuntimeProposal(root2, preflightProposal, options), options);
  if (preflightResult.status !== "success" && preflightResult.status !== "no-op")
    fail7("PREFLIGHT_BLOCKED", preflightResult.message);
  if (!options.dryRun)
    current = readCanonicalCurrentTask(root2);
  const executionPreflight = current.runtimeState.execution_preflight;
  const receipt = {
    kind: "execute-step-repair-preflight/v1",
    ...executionPreflight?.preflight_id ? { preflight_id: executionPreflight.preflight_id } : { preflight_id: preflightProposal.idempotency_key },
    ...executionPreflight?.execution_id ? { execution_id: executionPreflight.execution_id } : {},
    task_id: current.runtimeState.task_id,
    document_id: current.sourceTuple.document_id,
    source_revision: current.sourceTuple.revision,
    step_id: stepPlan.step.id,
    plan_revision: stepPlanRevision(stepPlan),
    mode: "repair",
    test_strategy_mode: strategy.mode,
    execution_phase: executionPreflight?.execution_phase ?? phase,
    candidate_paths: executionPreflight?.candidate_paths ?? candidatePaths,
    repair_fingerprints: executionPreflight?.repair_fingerprints ?? fingerprints,
    repair_wave_id: executionPreflight?.repair_wave_id ?? waveId,
    change_set_id: executionPreflight?.change_set_id ?? pending.change_set_id,
    review_target_paths: executionPreflight?.review_target_paths ?? reviewedExecution.execution_result.review_target.entries.map((item) => item.path),
    review_id: executionPreflight?.review_id ?? pending.review_id,
    review_base: captureReviewTarget(root2, reviewTargetPaths)
  };
  return {
    status: "pass",
    operation_kind: "execute-step-repair-preflight",
    committed: preflightResult.committed,
    read_back_verified: options.dryRun ? true : preflightResult.read_back_verified,
    current_step: currentStepResult(stepPlan, { ...strategy, phase: receipt.execution_phase }),
    context_projection: taskContextReferenceForCurrent(root2, current, "preflight-step", "repair"),
    receipt
  };
}
function retryStep(root2, input, options = {}) {
  const source = record6(input, "retry-step input");
  exactKeys3(source, ["step_id", "blocked_attempt_id", "blocker_resolution_refs", ...source.repair_diagnosis === undefined ? [] : ["repair_diagnosis"], "idempotency_key"], "retry-step input");
  const current = readCanonicalCurrentTask(root2);
  const proposal = createStepRetryProposal(current, {
    step_id: text4(source.step_id, "step_id", 128),
    blocked_attempt_id: text4(source.blocked_attempt_id, "blocked_attempt_id", 128),
    blocker_resolution_refs: textList2(source.blocker_resolution_refs, "blocker_resolution_refs", false),
    ...source.repair_diagnosis === undefined ? {} : { repair_diagnosis: source.repair_diagnosis },
    idempotency_key: text4(source.idempotency_key, "idempotency_key", 128)
  });
  return verifyReadBack(root2, applyVNextRuntimeProposal(root2, proposal, options), options);
}
function evidenceContext(root2, input) {
  const source = contextInput(input, ["offset", "limit", "continuation"]);
  const current = readCanonicalCurrentTask(root2);
  assertExecutableTask(current);
  assertBusinessEvidenceVersion(current);
  const allChecks = (current.runtimeState.claim_evidence ?? []).flatMap((claim) => claim.slots.map((slot) => {
    if (!slot.check)
      fail7("CLAIM_EVIDENCE_PLAN_REQUIRED", "A frozen check is required.");
    const snapshot = captureReviewTarget(root2, slot.check.subject_paths);
    return {
      claim_id: claim.claim_id,
      slot_id: slot.slot_id,
      check_id: slot.check.check_id,
      boundary: slot.check.boundary ?? null,
      user_decision: slot.user_decision ? { ...slot.user_decision } : null,
      frozen_invocation: slot.check.entry,
      validation_items: [...slot.check.validation_items ?? []],
      minimum_type: slot.minimum_type,
      execution_selection: slot.check.selection ? { ...slot.check.selection } : null,
      subject_revision: snapshot.revision,
      subject_snapshot: subjectSnapshotSummary(snapshot)
    };
  }));
  if (!current.runtimeState.evidence_plan_revision || !allChecks.length)
    fail7("CLAIM_EVIDENCE_PLAN_REQUIRED", "A frozen evidence plan is required.");
  const continuationValue = source.continuation;
  let continuation = null;
  if (continuationValue !== undefined) {
    let parsed = continuationValue;
    if (typeof continuationValue === "string") {
      try {
        parsed = JSON.parse(continuationValue);
      } catch {
        fail7("EVIDENCE_CONTEXT_CONTINUATION_INVALID", "continuation is not valid JSON.");
      }
    }
    if (!parsed || typeof parsed !== "object" || Array.isArray(parsed))
      fail7("EVIDENCE_CONTEXT_CONTINUATION_INVALID", "continuation must be an object.");
    const cursor = parsed;
    if (cursor.kind !== "execute-step-evidence-page/v1" || cursor.source_revision !== current.sourceTuple.revision || cursor.evidence_plan_revision !== current.runtimeState.evidence_plan_revision || !Number.isSafeInteger(cursor.offset) || Number(cursor.offset) < 0) {
      fail7("EVIDENCE_CONTEXT_STALE", "source or evidence-plan revision changed; start a fresh evidence-context read.");
    }
    continuation = {
      kind: "execute-step-evidence-page/v1",
      source_revision: current.sourceTuple.revision,
      evidence_plan_revision: current.runtimeState.evidence_plan_revision,
      offset: Number(cursor.offset)
    };
  }
  const offset = continuation?.offset ?? integer(source.offset, 0, 0, allChecks.length);
  const limit = integer(source.limit, 64, 1, 64);
  if (offset > allChecks.length)
    fail7("EVIDENCE_CONTEXT_CONTINUATION_INVALID", "offset is outside the declared check set.");
  const checks = allChecks.slice(offset, offset + limit);
  const nextOffset = offset + checks.length;
  const complete = nextOffset >= allChecks.length;
  return {
    status: complete ? "pass" : "partial",
    operation_kind: "execute-step-evidence-context",
    committed: false,
    task_id: current.runtimeState.task_id,
    document_id: current.sourceTuple.document_id,
    evidence_plan_revision: current.runtimeState.evidence_plan_revision,
    evidence_assurance: "caller-reported",
    checks,
    returned_check_count: checks.length,
    total_check_count: allChecks.length,
    unexpanded_check_ids: complete ? [] : allChecks.slice(nextOffset, nextOffset + 64).map((item) => item.check_id),
    unexpanded_check_count: Math.max(0, allChecks.length - nextOffset),
    unexpanded_check_ids_truncated: allChecks.length - nextOffset > 64,
    complete_for_operation: complete,
    continuation: complete ? null : {
      kind: "execute-step-evidence-page/v1",
      source_revision: current.sourceTuple.revision,
      evidence_plan_revision: current.runtimeState.evidence_plan_revision,
      offset: nextOffset
    },
    subject_snapshots_read: "Use task-context/task-read for the exact frozen subject entries.",
    context_projection: taskContextReferenceForCurrent(root2, current, "evidence-context", "default")
  };
}
function preflightStep(root2, input) {
  const source = record6(input, "preflight-step input");
  const currentForInput = readCanonicalCurrentTask(root2);
  exactKeys3(source, currentForInput.mutationAuthority ? ["candidate_paths", ...source.blast_radius_assessments === undefined ? [] : ["blast_radius_assessments"]] : ["candidate_paths"], "preflight-step input");
  const candidatePaths = pathList(source.candidate_paths, "candidate_paths", true);
  let assessments = [];
  if (currentForInput.mutationAuthority && source.blast_radius_assessments !== undefined) {
    try {
      assessments = normalizeBlastRadiusAssessments(source.blast_radius_assessments);
    } catch (error) {
      fail7(error instanceof MutationAuthorityError ? error.code : "MUTATION_AUTHORITY_ASSESSMENT_INVALID", error instanceof Error ? error.message : String(error));
    }
  }
  let current = currentForInput;
  assertOrdinaryPreflight(current, root2);
  assertExecutableTask(current);
  const stepPlan = currentStepPlan(current);
  const strategy = resolveTestStrategyExecutionContext(current);
  assertTestStrategySequenceReady(current, strategy);
  const phase = executionPhaseForCurrentStep(current, strategy);
  assertPathsAdmitted(current, stepPlan, candidatePaths, "candidate_paths", root2, assessments, "default", phase);
  assertCommandPlansAdmitted(root2, current, stepPlan, assessments, phase, "default");
  assertExactCommandWritesCovered(stepPlan, candidatePaths);
  let committed = false;
  let preflightId;
  const coverage = current.runtimeState.review_coverage;
  const hasPrerequisites = current.runtimeState.claim_evidence?.some((claim) => claim.slots.some((slot) => slot.before_step_id === stepPlan.step.id && !slot.prerequisite_receipt));
  if (coverage && !hasPrerequisites && captureReviewTarget(root2, coverage.target.entries.map((entry) => entry.path)).revision !== coverage.target.revision)
    fail7("REVIEW_TARGET_STALE", "Unrecorded changes cannot refresh the cumulative baseline.");
  const activePreflightMatchesStep = current.runtimeState.execution_preflight?.step_id === stepPlan.step.id;
  if (current.mutationAuthority && activePreflightMatchesStep && digest5(current.runtimeState.execution_preflight.candidate_paths) !== digest5(candidatePaths)) {
    fail7("EXECUTE_PREFLIGHT_STALE", "the active v2 preflight already owns a different target set; use extend-preflight for additional targets.");
  }
  if (!current.runtimeState.step_attempts?.[stepPlan.step.id] || !coverage || candidatePaths.some((p) => !coverage.base.entries.some((entry) => entry.path === p)) || hasPrerequisites || current.runtimeState.step_attempts?.[stepPlan.step.id]?.attempts.at(-1)?.status === "ready" || current.mutationAuthority && !activePreflightMatchesStep) {
    const proposal = createStepPreflightProposal(current, candidatePaths, assessments, current.mutationAuthority ? {
      plan_revision: stepPlanRevision(stepPlan),
      execution_phase: phase,
      change_set_id: changeSetId(current, stepPlan.step.id)
    } : {});
    preflightId = proposal.idempotency_key;
    const registration = applyVNextRuntimeProposal(root2, proposal);
    if (!["success", "no-op"].includes(registration.status))
      fail7("PREFLIGHT_BLOCKED", registration.message);
    committed = registration.committed;
    current = readCanonicalCurrentTask(root2);
  }
  if (!preflightId)
    preflightId = current.runtimeState.step_attempts?.[stepPlan.step.id]?.attempts.at(-1)?.idempotency_key;
  const executionPreflight = current.runtimeState.execution_preflight;
  const activeStrategy = executionPreflight?.step_id === stepPlan.step.id ? { ...strategy, phase: executionPreflight.execution_phase } : { ...strategy, phase };
  const receipt = {
    kind: "execute-step-preflight/v1",
    ...preflightId ? { preflight_id: preflightId } : {},
    ...executionPreflight ? { execution_id: executionPreflight.execution_id } : {},
    attempt_id: nextStepAttemptId(current),
    task_id: current.runtimeState.task_id,
    document_id: current.sourceTuple.document_id,
    source_revision: current.sourceTuple.revision,
    step_id: stepPlan.step.id,
    plan_revision: stepPlanRevision(stepPlan),
    mode: "default",
    test_strategy_mode: strategy.mode,
    execution_phase: activeStrategy.phase,
    candidate_paths: candidatePaths,
    repair_fingerprint: null,
    change_set_id: changeSetId(current, stepPlan.step.id),
    review_base: captureReviewTarget(root2, candidatePaths),
    ...current.mutationAuthority ? { mutation_authority_version: 2 } : {}
  };
  return {
    status: "pass",
    operation_kind: "execute-step-preflight",
    committed,
    read_back_verified: true,
    current_step: currentStepResult(stepPlan, activeStrategy),
    context_projection: taskContextReferenceForCurrent(root2, current, "preflight-step", "default"),
    receipt
  };
}
function extendPreflight(root2, input, options = {}) {
  const source = record6(input, "extend-preflight input");
  exactKeys3(source, ["current_preflight_receipt", "additional_targets", "blast_radius_assessments", "evidence_refs"], "extend-preflight input");
  const receipt = normalizePreflightReceipt(source.current_preflight_receipt);
  if (receipt.mode !== "default" && receipt.mode !== "repair") {
    fail7("EXECUTE_PREFLIGHT_IDENTITY_CONFLICT", "extend-preflight requires a current ordinary or repair preflight receipt.");
  }
  const current = readCanonicalCurrentTask(root2);
  if (!current.mutationAuthority)
    fail7("MUTATION_AUTHORITY_VERSION_REQUIRED", "extend-preflight is available only for Mutation Authority v2 tasks.");
  if (receipt.mode === "default")
    assertOrdinaryPreflight(current, root2);
  else
    assertExecutableTask(current);
  const stepPlan = currentStepPlan(current);
  assertCurrentReceipt(root2, current, stepPlan, receipt);
  const additionalTargets = pathList(source.additional_targets, "additional_targets", false);
  if (additionalTargets.some((target) => receipt.candidate_paths.includes(target))) {
    fail7("EXECUTE_PREFLIGHT_SCOPE_CONFLICT", "additional_targets must not repeat an already preflighted path.");
  }
  let assessments;
  try {
    assessments = normalizeBlastRadiusAssessments(source.blast_radius_assessments);
  } catch (error) {
    fail7(error instanceof MutationAuthorityError ? error.code : "MUTATION_AUTHORITY_ASSESSMENT_INVALID", error instanceof Error ? error.message : String(error));
  }
  const evidenceRefs = textList2(source.evidence_refs, "evidence_refs", false);
  const candidatePaths = [...receipt.candidate_paths, ...additionalTargets];
  assertTestStrategySequenceReady(current, resolveTestStrategyExecutionContext(current));
  assertPathsAdmitted(current, stepPlan, candidatePaths, "additional_targets", root2, assessments, receipt.mode, receipt.execution_phase);
  assertCommandPlansAdmitted(root2, current, stepPlan, assessments, receipt.execution_phase, receipt.mode);
  const currentPreflightId = receipt.preflight_id ?? receipt.attempt_id;
  if (!currentPreflightId)
    fail7("EXECUTE_PREFLIGHT_IDENTITY_CONFLICT", "current_preflight_receipt must bind a preflight id.");
  const proposal = createStepExtendPreflightProposal(current, {
    step_id: stepPlan.step.id,
    current_preflight_id: currentPreflightId,
    additional_targets: additionalTargets,
    blast_radius_assessments: assessments,
    evidence_refs: evidenceRefs,
    mode: receipt.mode,
    ...receipt.execution_id === undefined ? {} : { execution_id: receipt.execution_id },
    execution_phase: receipt.execution_phase
  });
  const extensionReviewBase = createReviewTargetManifest([
    ...receipt.review_base.entries,
    ...captureReviewTarget(root2, additionalTargets).entries
  ]);
  const result = applyVNextRuntimeProposal(root2, proposal, options);
  if (!["success", "no-op"].includes(result.status))
    fail7("PREFLIGHT_BLOCKED", result.message);
  const next = options.dryRun ? current : readCanonicalCurrentTask(root2);
  const coverage = next.runtimeState.review_coverage;
  const nextBase = coverage?.base ?? captureReviewTarget(root2, candidatePaths);
  const active = next.runtimeState.execution_preflight;
  const nextStrategy = resolveTestStrategyExecutionContext(next);
  const activeStrategy = active ? { ...nextStrategy, phase: active.execution_phase } : { ...nextStrategy, phase: receipt.execution_phase };
  if (receipt.mode === "repair") {
    const repairReceipt = {
      kind: "execute-step-repair-preflight/v1",
      ...active?.preflight_id ? { preflight_id: active.preflight_id } : { preflight_id: proposal.idempotency_key },
      ...active?.execution_id ? { execution_id: active.execution_id } : receipt.execution_id ? { execution_id: receipt.execution_id } : {},
      task_id: next.runtimeState.task_id,
      document_id: next.sourceTuple.document_id,
      source_revision: next.sourceTuple.revision,
      step_id: stepPlan.step.id,
      plan_revision: active?.plan_revision ?? stepPlanRevision(stepPlan),
      mode: "repair",
      test_strategy_mode: activeStrategy.mode,
      execution_phase: active?.execution_phase ?? receipt.execution_phase,
      candidate_paths: active?.candidate_paths ?? candidatePaths,
      repair_fingerprints: active?.repair_fingerprints ?? receipt.repair_fingerprints,
      repair_wave_id: active?.repair_wave_id ?? receipt.repair_wave_id,
      change_set_id: active?.change_set_id ?? receipt.change_set_id,
      review_target_paths: active?.review_target_paths ?? receipt.review_target_paths,
      review_id: active?.review_id ?? receipt.review_id,
      review_base: coverage?.target ?? nextBase
    };
    return {
      status: "pass",
      operation_kind: "execute-step-repair-preflight",
      committed: result.committed,
      read_back_verified: options.dryRun ? true : result.read_back_verified,
      current_step: currentStepResult(stepPlan, activeStrategy),
      context_projection: taskContextReferenceForCurrent(root2, next, "preflight-step", "repair"),
      receipt: repairReceipt
    };
  }
  const ordinaryResult = {
    status: "pass",
    operation_kind: "execute-step-preflight-extension",
    committed: result.committed,
    read_back_verified: options.dryRun ? true : result.read_back_verified,
    current_step: currentStepResult(stepPlan, activeStrategy),
    context_projection: taskContextReferenceForCurrent(root2, next, "preflight-step", "default"),
    receipt: {
      kind: "execute-step-preflight/v1",
      ...active?.preflight_id ? { preflight_id: active.preflight_id } : { preflight_id: proposal.idempotency_key },
      ...active?.execution_id ? { execution_id: active.execution_id } : receipt.execution_id ? { execution_id: receipt.execution_id } : {},
      ...receipt.attempt_id ? { attempt_id: receipt.attempt_id } : {},
      task_id: next.runtimeState.task_id,
      document_id: next.sourceTuple.document_id,
      source_revision: next.sourceTuple.revision,
      step_id: stepPlan.step.id,
      plan_revision: active?.plan_revision ?? stepPlanRevision(stepPlan),
      mode: "default",
      test_strategy_mode: activeStrategy.mode,
      execution_phase: active?.execution_phase ?? receipt.execution_phase,
      candidate_paths: active?.candidate_paths ?? candidatePaths,
      repair_fingerprint: null,
      change_set_id: active?.change_set_id ?? coverage?.change_set_id ?? changeSetId(next, stepPlan.step.id),
      review_base: extensionReviewBase,
      mutation_authority_version: 2
    }
  };
  return ordinaryResult;
}
function resultStatus(value, location) {
  if (value !== "passed" && value !== "expected-failure" && value !== "failed" && value !== "blocked" && value !== "not-run") {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", `${location} must be passed, expected-failure, failed, blocked, or not-run.`);
  }
  return value;
}
function normalizeExpectedFailure(value, location) {
  const source = record6(value, location);
  exactKeys3(source, ["kind", "expected_behavior", "observed_failure_signature"], location);
  if (source.kind !== "behavior-not-implemented") {
    fail7("EXECUTE_EXPECTED_FAILURE_INVALID", `${location}.kind must be behavior-not-implemented; syntax, import, fixture, tool, and environment failures are blocked outcomes.`);
  }
  return {
    kind: "behavior-not-implemented",
    expected_behavior: text4(source.expected_behavior, `${location}.expected_behavior`),
    observed_failure_signature: text4(source.observed_failure_signature, `${location}.observed_failure_signature`)
  };
}
function normalizeCommandResults(value) {
  if (!Array.isArray(value) || value.length > MAX_ITEMS2) {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", "command_results must be a bounded array.");
  }
  const results = value.map((item, index) => {
    const location = `command_results[${index}]`;
    const source = record6(item, location);
    const status = resultStatus(source.status, `${location}.status`);
    exactKeys3(source, status === "expected-failure" ? ["command", "status", "observed_repo_writes", "evidence_refs", "expected_failure"] : ["command", "status", "observed_repo_writes", "evidence_refs", ...source.waiver_decision_id === undefined ? [] : ["waiver_decision_id"]], location);
    const observedRepoWrites = pathList(source.observed_repo_writes, `${location}.observed_repo_writes`, true);
    if (status === "not-run" && observedRepoWrites.length > 0) {
      fail7("EXECUTE_ADAPTER_INPUT_INVALID", `${location}.not-run command must not report repository writes.`);
    }
    return {
      command: text4(source.command, `${location}.command`),
      ...source.waiver_decision_id === undefined ? {} : { waiver_decision_id: text4(source.waiver_decision_id, "waiver_decision_id") },
      status,
      observed_repo_writes: observedRepoWrites,
      evidence_refs: textList2(source.evidence_refs, `${location}.evidence_refs`, status === "not-run"),
      ...status === "expected-failure" ? { expected_failure: normalizeExpectedFailure(source.expected_failure, `${location}.expected_failure`) } : {}
    };
  });
  if (new Set(results.map((item) => item.command)).size !== results.length) {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", "command_results must not contain duplicate commands.");
  }
  return results;
}
function normalizeValidationResults(value) {
  if (!Array.isArray(value) || value.length === 0 || value.length > MAX_ITEMS2) {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", "validation_results must be a bounded non-empty array.");
  }
  const results = value.map((item, index) => {
    const location = `validation_results[${index}]`;
    const source = record6(item, location);
    const status = resultStatus(source.status, `${location}.status`);
    exactKeys3(source, status === "expected-failure" ? ["validation", "status", "evidence_refs", "expected_failure"] : ["validation", "status", "evidence_refs", ...source.waiver_decision_id === undefined ? [] : ["waiver_decision_id"]], location);
    return {
      validation: text4(source.validation, `${location}.validation`),
      ...source.waiver_decision_id === undefined ? {} : { waiver_decision_id: text4(source.waiver_decision_id, "waiver_decision_id") },
      status,
      evidence_refs: textList2(source.evidence_refs, `${location}.evidence_refs`, status === "not-run"),
      ...status === "expected-failure" ? { expected_failure: normalizeExpectedFailure(source.expected_failure, `${location}.expected_failure`) } : {}
    };
  });
  if (new Set(results.map((item) => item.validation)).size !== results.length) {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", "validation_results must not contain duplicate validations.");
  }
  return results;
}
function normalizeAcceptanceEvidence(value) {
  return validateStepAcceptanceEvidence(value, "acceptance_evidence");
}
function updateClaimEvidence(current, evidence) {
  const plan = structuredClone(current.runtimeState.claim_evidence ?? []);
  for (const item of evidence) {
    const slot = plan.find((claim) => claim.claim_id === item.claim_id)?.slots.find((slot) => slot.slot_id === item.slot_id);
    if (!slot || slot.check?.check_id !== item.check_id || slot.minimum_type !== item.minimum_type)
      fail7("CLAIM_EVIDENCE_PLAN_CONFLICT", "result must identify the exact frozen claim/slot/check and type.");
    if (slot.prerequisite_receipt)
      fail7("CLAIM_EVIDENCE_PLAN_CONFLICT", "consumed prerequisite evidence is immutable.");
    slot.disposition = item.disposition;
    slot.evidence_refs = [...item.evidence_refs];
    slot.report = item.report;
  }
  return plan;
}
function assertExactResultSet(actual, expected, location) {
  const missing = expected.filter((item) => !actual.includes(item));
  const unexpected = actual.filter((item) => !expected.includes(item));
  if (missing.length > 0 || unexpected.length > 0) {
    fail7("EXECUTE_RESULT_PLAN_CONFLICT", `${location} must match the current step plan; missing=[${missing.join(", ")}], unexpected=[${unexpected.join(", ")}].`);
  }
}
function assertObservedWithinExpected(command, observed) {
  if (command.expected_repo_writes === "none") {
    if (observed.length > 0) {
      fail7("COMMAND_OBSERVED_WRITE_BLOCKED", `command "${command.command}" was planned as write-free but observed repository writes: ${observed.join(", ")}.`);
    }
    return;
  }
  const expectedWrites = command.expected_repo_writes;
  const outside = observed.filter((file) => !expectedWrites.some((pattern) => mutationScopePatternMatchesPath(file, pattern)));
  if (outside.length > 0) {
    fail7("COMMAND_OBSERVED_WRITE_BLOCKED", `command "${command.command}" wrote outside its prepared footprint: ${outside.join(", ")}.`);
  }
}
function assertCommandResults(root2, current, stepPlan, results, phase, mode) {
  assertExactResultSet(results.map((item) => item.command), stepPlan.commands.map((item) => item.command), "command_results");
  if (current.mutationAuthority) {
    for (const result of results) {
      if (result.status === "not-run")
        continue;
      const planned = stepPlan.commands.find((item) => item.command === result.command);
      assertObservedWithinExpected(planned, result.observed_repo_writes);
      assertPathsAdmitted(current, stepPlan, result.observed_repo_writes, `observed writes for command "${result.command}"`, root2, [], mode, phase);
    }
    return;
  }
  const scope = parseMutationScope(current.body, current.sourceTuple.revision);
  for (const result of results) {
    const planned = stepPlan.commands.find((item) => item.command === result.command);
    if (result.status === "not-run")
      continue;
    assertObservedWithinExpected(planned, result.observed_repo_writes);
    assertPathsAdmitted(current, stepPlan, result.observed_repo_writes, `observed writes for command "${result.command}"`, root2, [], mode, phase);
    if (planned.expected_repo_writes === "none")
      continue;
    const audit = auditCommandMutation(scope, {
      command: planned.command,
      expected_write_footprint: {
        kind: "bounded",
        targets: planned.expected_repo_writes,
        evidence_refs: result.evidence_refs
      },
      transformation_kind: planned.transformation_kind,
      observed_write_paths: result.observed_repo_writes
    });
    if (audit.status !== "pass") {
      fail7("COMMAND_MUTATION_AUDIT_BLOCKED", `command "${result.command}" failed Runtime mutation audit: ${audit.blockers.join(" ")}`);
    }
  }
}
function assertValidationResults(stepPlan, results) {
  assertExactResultSet(results.map((item) => item.validation), stepPlan.validation, "validation_results");
}
function allEvidenceRefs(commands, validations, acceptance) {
  return [...new Set([
    ...commands.flatMap((item) => item.evidence_refs),
    ...validations.flatMap((item) => item.evidence_refs),
    ...acceptance.flatMap((item) => item.evidence_refs)
  ])];
}
function verifyReadBack(root2, result, options) {
  if (options.dryRun || result.status !== "success" && result.status !== "no-op")
    return result;
  const readBack = readCanonicalCurrentTask(root2);
  if (!result.read_back_verified || result.resulting_revision !== readBack.sourceTuple.revision) {
    fail7("EXECUTE_ADAPTER_READ_BACK_FAILED", "execute-step adapter could not verify the committed canonical CURRENT_TASK revision.");
  }
  return result;
}
function semanticNoOp2(current, key, message, options) {
  const state = current.runtimeState;
  return {
    status: "no-op",
    operation_kind: "task-state-transaction",
    idempotency_key: key,
    target_path: current.relativePath,
    dry_run: options.dryRun === true,
    committed: false,
    message,
    previous_revision: current.sourceTuple.revision,
    resulting_revision: current.sourceTuple.revision,
    planned_writes: [],
    governed_mutation_count: 0,
    read_back_verified: true,
    state: {
      task_id: state.task_id,
      workflow_status: state.workflow_status,
      lifecycle_state: state.lifecycle_state,
      resume_requires_review: state.resume_requires_review,
      resume_review_reasons: [...state.resume_review_reasons],
      active_step_id: state.active_step_id,
      active_step_status: state.active_step_status,
      finding_queue_revision: state.finding_queue_revision,
      review_cycle_id: state.review_cycle.id,
      repair_round: state.review_cycle.repair_round
    }
  };
}
function hasAppliedProposal2(current, key) {
  return current.runtimeState.applied_proposals.some((item) => item.idempotency_key === key);
}
function applyRepairAttempts(root2, current, receipt, evidenceRefs, keySeed, options) {
  const fingerprints = receipt.kind === "execute-step-repair-preflight/v1" ? receipt.repair_fingerprints : [receipt.repair_fingerprint];
  const waveId = receipt.kind === "execute-step-repair-preflight/v1" ? receipt.repair_wave_id : `repair-wave-${digest5(keySeed).slice(0, 24)}`;
  let fresh = current;
  for (const fingerprint of fingerprints) {
    const finding = fresh.runtimeState.findings.find((item) => item.fingerprint === fingerprint);
    if (!finding || !["admitted", "in-progress"].includes(finding.status)) {
      fail7("FINDING_ADMISSION_REQUIRED", `repair finding ${fingerprint} is no longer admitted.`);
    }
    if (finding.last_repair_wave_id === waveId)
      continue;
    const key = idempotencyKey("execute-repair-attempt", { keySeed, fingerprint, wave_id: waveId });
    const proposal = createFindingQueueProposal(fresh, {
      mode: "repair",
      delta: {
        kind: "finding-queue",
        action: "record-repair-attempt",
        fingerprint,
        review_cycle_id: fresh.runtimeState.review_cycle.id,
        repair_wave_id: waveId,
        evidence_refs: evidenceRefs,
        note: `execute-step repair for ${receipt.step_id}`
      },
      idempotency_key: key,
      authority_evidence: authority2(fresh, ["active-task-owner", "scope-admission", "finding-admission", "evidence-admission"]),
      evidence_refs: evidenceRefs
    });
    const result = verifyReadBack(root2, applyVNextRuntimeProposal(root2, proposal, options), options);
    if (result.status !== "success" && result.status !== "no-op")
      return result;
    if (options.dryRun)
      continue;
    fresh = readCanonicalCurrentTask(root2);
  }
  return options.dryRun ? current : fresh;
}
function recordStepResult(root2, input, options = {}) {
  const source = record6(input, "record-step-result input");
  exactKeys3(source, [
    "preflight_receipt",
    ...source.blocker_kind === undefined ? [] : ["blocker_kind"],
    "actual_changed_paths",
    "command_results",
    "validation_results",
    "acceptance_evidence",
    "outcome",
    "note"
  ], "record-step-result input");
  if (source.blocker_kind !== undefined && !["environment", "unknown"].includes(String(source.blocker_kind)))
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", "blocker_kind must be environment or unknown.");
  const receipt = normalizePreflightReceipt(source.preflight_receipt);
  const actualChangedPaths = pathList(source.actual_changed_paths, "actual_changed_paths", true);
  const commandResults = normalizeCommandResults(source.command_results);
  const validationResults = normalizeValidationResults(source.validation_results);
  const acceptanceEvidence = normalizeAcceptanceEvidence(source.acceptance_evidence);
  if (source.outcome !== "implemented" && source.outcome !== "test-red" && source.outcome !== "blocked") {
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", "outcome must be implemented, test-red, or blocked.");
  }
  const outcome = source.outcome;
  const note = nullableText(source.note, "note");
  const unplannedActual = actualChangedPaths.filter((file) => !receipt.candidate_paths.includes(file));
  if (unplannedActual.length > 0) {
    fail7("EXECUTE_PREFLIGHT_SCOPE_CONFLICT", `actual_changed_paths were not admitted by preflight: ${unplannedActual.join(", ")}.`);
  }
  const reviewTargetPaths = receipt.kind === "execute-step-repair-preflight/v1" ? [...new Set([...receipt.review_target_paths, ...receipt.candidate_paths])] : [...receipt.candidate_paths];
  const expectedBasePaths = [...new Set(reviewTargetPaths)].sort();
  if (digest5(receipt.review_base.entries.map((item) => item.path)) !== digest5(expectedBasePaths)) {
    fail7("EXECUTE_PREFLIGHT_STALE", "preflight review base does not cover the exact review target path set.");
  }
  const reviewTarget = captureReviewTarget(root2, reviewTargetPaths);
  const changeDelta = createReviewChangeDelta(receipt.review_base, reviewTarget);
  const detectedChangedPaths = changeDelta.entries.map((item) => item.path);
  if (digest5([...actualChangedPaths].sort()) !== digest5(detectedChangedPaths)) {
    fail7("EXECUTE_RESULT_CHANGE_DELTA_CONFLICT", `actual_changed_paths must match Runtime-detected changes: ${detectedChangedPaths.join(", ") || "none"}.`);
  }
  const resultKeySeed = {
    ...source.blocker_kind === undefined ? {} : { blocker_kind: source.blocker_kind },
    receipt,
    actual_changed_paths: detectedChangedPaths,
    command_results: commandResults,
    validation_results: validationResults,
    acceptance_evidence: acceptanceEvidence,
    change_set_id: receipt.change_set_id,
    review_base: receipt.review_base,
    review_target: reviewTarget,
    change_delta: changeDelta,
    outcome,
    note
  };
  const resultKey = idempotencyKey("execute-step-result", resultKeySeed);
  let current = readCanonicalCurrentTask(root2);
  if (hasAppliedProposal2(current, resultKey)) {
    return semanticNoOp2(current, resultKey, "This exact execute-step result was already committed.", options);
  }
  assertExecutableTask(current);
  let stepPlan = currentStepPlan(current);
  assertCurrentReceipt(root2, current, stepPlan, receipt);
  const strategy = resolveTestStrategyExecutionContext(current);
  assertTestStrategySequenceReady(current, strategy);
  assertPathsAdmitted(current, stepPlan, receipt.candidate_paths, "preflight_receipt.candidate_paths", root2, [], receipt.mode, receipt.execution_phase);
  assertPathsAdmitted(current, stepPlan, actualChangedPaths, "actual_changed_paths", root2, [], receipt.mode, receipt.execution_phase);
  assertCommandResults(root2, current, stepPlan, commandResults, receipt.execution_phase, receipt.mode);
  assertValidationResults(stepPlan, validationResults);
  assertExecutionResultWaivers(root2, current, stepPlan.step.id, { command_results: commandResults, validation_results: validationResults });
  if (outcome === "implemented" && commandResults.some((item) => item.status !== "passed" && item.status !== "expected-failure" && !item.waiver_decision_id)) {
    fail7("EXECUTE_RESULT_BLOCKED", "implemented requires passing commands or exact user-waived validation commands.");
  }
  if (outcome === "implemented" && validationResults.some((item) => item.status !== "passed" && item.status !== "expected-failure" && !item.waiver_decision_id)) {
    fail7("EXECUTE_RESULT_BLOCKED", "implemented requires passing validation or exact user-waived validation; never report a waiver as PASS.");
  }
  if (outcome === "test-red") {
    const resultStatuses = [...commandResults, ...validationResults].map((item) => item.status);
    if (strategy.phase !== "red") {
      fail7("TEST_STRATEGY_SEQUENCE_INVALID", `outcome=test-red is not valid during phase=${strategy.phase}.`);
    }
    if (!resultStatuses.some((status) => status === "expected-failure") || resultStatuses.some((status) => status !== "passed" && status !== "expected-failure")) {
      fail7("EXECUTE_EXPECTED_FAILURE_INVALID", "test-red requires at least one expected-failure result and permits only passed companion results.");
    }
    if (acceptanceEvidence.length > 0) {
      fail7("TEST_STRATEGY_RED_ACCEPTANCE_FORBIDDEN", "test-red evidence cannot satisfy final acceptance claims before implementation reaches Green.");
    }
  }
  if (outcome === "blocked" && note === null) {
    fail7("EXECUTE_RESULT_BLOCKED", "blocked requires a concise blocker in note.");
  }
  if (outcome === "blocked" && ![...commandResults, ...validationResults].some((item) => item.status === "failed" || item.status === "blocked")) {
    fail7("EXECUTE_RESULT_BLOCKED", "blocked requires at least one failed or blocked command or validation result.");
  }
  if (receipt.kind === "execute-step-repair-preflight/v1") {
    const pending = current.runtimeState.pending_review_result;
    const priorExecution = pending && currentDefinitionExecutionLog(current).find((item) => !("action" in item) && item.idempotency_key === pending.execution_id);
    const activePreflight = current.runtimeState.execution_preflight;
    const priorTargetPaths = activePreflight?.mode === "repair" ? activePreflight.review_target_paths : priorExecution ? cumulativeReviewExecution(current, priorExecution).execution_result?.review_target.entries.map((item) => item.path) : undefined;
    if (!pending || !priorExecution?.execution_result || pending.review_id !== receipt.review_id || pending.change_set_id !== receipt.change_set_id || priorExecution.execution_result.change_set_id !== receipt.change_set_id || priorTargetPaths !== null && priorTargetPaths !== undefined && digest5(priorTargetPaths) !== digest5(receipt.review_target_paths)) {
      fail7("REVIEW_TARGET_CONFLICT", "repair result no longer binds the Runtime-recorded reviewed change set.");
    }
  }
  const evidenceRefs = allEvidenceRefs(commandResults, validationResults, acceptanceEvidence);
  const claimEvidence = updateClaimEvidence(current, acceptanceEvidence);
  const executionResult = {
    ...receipt.execution_id ? { execution_id: receipt.execution_id } : {},
    ...receipt.kind === "execute-step-preflight/v1" && receipt.attempt_id ? { attempt_id: receipt.attempt_id } : {},
    ...source.blocker_kind === undefined ? {} : { blocker_kind: source.blocker_kind },
    outcome,
    change_set_id: receipt.change_set_id,
    review_base: receipt.review_base,
    review_target: reviewTarget,
    change_delta: changeDelta,
    actual_changed_paths: detectedChangedPaths,
    command_results: commandResults.map((item) => ({
      ...item,
      observed_repo_writes: [...item.observed_repo_writes],
      evidence_refs: [...item.evidence_refs],
      ...item.expected_failure ? { expected_failure: { ...item.expected_failure } } : {}
    })),
    validation_results: validationResults.map((item) => ({
      ...item,
      evidence_refs: [...item.evidence_refs],
      ...item.expected_failure ? { expected_failure: { ...item.expected_failure } } : {}
    })),
    acceptance_evidence: acceptanceEvidence.map((item) => ({ ...item, evidence_refs: [...item.evidence_refs] })),
    blocker: outcome === "blocked" ? note : null
  };
  if (receipt.mode === "repair") {
    const repairState = applyRepairAttempts(root2, current, receipt, evidenceRefs, resultKeySeed, options);
    if ("status" in repairState)
      return repairState;
    current = repairState;
    stepPlan = currentStepPlan(current);
    if (current.runtimeState.active_step_id !== receipt.step_id || stepPlanRevision(stepPlan) !== receipt.plan_revision) {
      fail7("EXECUTE_PREFLIGHT_STALE", "repair bookkeeping changed the active step plan unexpectedly.");
    }
  }
  let status;
  if (outcome === "blocked")
    status = "blocked";
  else if (receipt.mode === "repair" || stepPlan.step.review_checkpoint === "not-required" && !dynamicReviewRequiredForCurrentExecution(current))
    status = "completed";
  else
    status = "in-progress";
  const proposal = createTaskStateProposal(current, {
    mode: receipt.mode,
    status,
    evidence_refs: evidenceRefs,
    idempotency_key: resultKey,
    authority_evidence: authority2(current, ["active-task-owner", "scope-admission", "evidence-admission"]),
    ...note ? { note } : {},
    ...receipt.kind === "execute-step-repair-preflight/v1" ? { repair_fingerprints: receipt.repair_fingerprints, repair_wave_id: receipt.repair_wave_id } : receipt.repair_fingerprint ? { repair_fingerprint: receipt.repair_fingerprint } : {},
    change_set_id: receipt.change_set_id,
    ...claimEvidence === undefined ? {} : { claim_evidence: claimEvidence },
    execution_result: executionResult
  });
  return verifyReadBack(root2, applyVNextRuntimeProposal(root2, proposal, options), options);
}
function resolveVerifiedFindings(root2, current, receipt, options) {
  for (const fingerprint of receipt.admitted_fingerprints) {
    const fresh = readCanonicalCurrentTask(root2);
    const finding = fresh.runtimeState.findings.find((item) => item.fingerprint === fingerprint);
    if (!finding)
      fail7("FINDING_NOT_FOUND", `review receipt finding ${fingerprint} is not in the current queue.`);
    if (finding.status === "resolved")
      continue;
    const key = idempotencyKey("execute-review-resolve", { step_id: current.runtimeState.active_step_id, receipt, fingerprint });
    const proposal = createFindingQueueProposal(fresh, {
      mode: "repair",
      delta: {
        kind: "finding-queue",
        action: "resolve",
        fingerprint,
        evidence_refs: receipt.evidence_refs,
        note: `clean verification for ${current.runtimeState.active_step_id}`
      },
      idempotency_key: key,
      authority_evidence: authority2(fresh, ["active-task-owner", "scope-admission", "finding-admission", "evidence-admission"]),
      evidence_refs: receipt.evidence_refs
    });
    const result = verifyReadBack(root2, applyVNextRuntimeProposal(root2, proposal, options), options);
    if (result.status !== "success" && result.status !== "no-op")
      return result;
  }
  return options.dryRun ? current : readCanonicalCurrentTask(root2);
}
function previewResolvedFindings(current, fingerprints) {
  const pending = new Set(fingerprints.filter((fingerprint) => current.runtimeState.findings.some((item) => item.fingerprint === fingerprint && item.status !== "resolved")));
  const findingQueueRevision = current.runtimeState.finding_queue_revision + pending.size;
  return {
    ...current,
    sourceTuple: { ...current.sourceTuple, finding_queue_revision: findingQueueRevision },
    runtimeState: {
      ...current.runtimeState,
      finding_queue_revision: findingQueueRevision,
      findings: current.runtimeState.findings.map((item) => pending.has(item.fingerprint) ? { ...item, status: "resolved" } : item)
    }
  };
}
function completeReviewedStep(root2, input, options = {}) {
  const source = record6(input, "complete-reviewed-step input");
  exactKeys3(source, ["step_id", "note"], "complete-reviewed-step input");
  const stepId = text4(source.step_id, "step_id", 128);
  if (!SAFE_KEY_PATTERN3.test(stepId))
    fail7("EXECUTE_ADAPTER_INPUT_INVALID", "step_id is invalid.");
  const note = nullableText(source.note, "note");
  let current = readCanonicalCurrentTask(root2);
  const replay = currentDefinitionExecutionLog(current).find((item) => {
    if ("action" in item || item.step_id !== stepId || !item.review_receipt)
      return false;
    const key = idempotencyKey("execute-reviewed-step", {
      step_id: stepId,
      review_receipt: item.review_receipt,
      note
    });
    return item.idempotency_key === key && hasAppliedProposal2(current, key);
  });
  if (replay && !("action" in replay)) {
    return semanticNoOp2(current, replay.idempotency_key, "This exact reviewed-step completion was already committed.", options);
  }
  const pending = current.runtimeState.pending_review_result;
  let reviewReceipt;
  if (pending) {
    if (pending.verdict !== "clean") {
      fail7("CLEAN_REVIEW_REQUIRED", `complete-reviewed-step requires clean; current review verdict is ${pending.verdict}.`);
    }
    const reviewedExecution = currentDefinitionExecutionLog(current).map((item) => ("action" in item) ? item : current.runtimeState.scope_amendment_pending_review_step_id === pending.step_id && item.idempotency_key === pending.execution_id ? item : cumulativeReviewExecution(current, item)).find((item) => !("action" in item) && item.idempotency_key === pending.execution_id);
    if (!reviewedExecution?.execution_result || reviewedExecution.change_set_id !== pending.change_set_id || reviewedExecution.execution_result.review_target.revision !== pending.review_target_revision) {
      fail7("REVIEW_TARGET_CONFLICT", "canonical clean review no longer binds its Runtime-recorded execution target.");
    }
    const currentTarget = captureReviewTarget(root2, reviewedExecution.execution_result.review_target.entries.map((item) => item.path));
    if (currentTarget.revision !== pending.review_target_revision) {
      fail7("REVIEW_TARGET_STALE", "product files changed after the clean review was recorded.");
    }
    const repairLogs = currentDefinitionExecutionLog(current).filter((item) => !("action" in item) && item.step_id === stepId && item.mode === "repair");
    const repaired = [...new Set(repairLogs.flatMap((item) => [
      ...item.repair_fingerprints ?? [],
      ...item.repair_fingerprint ? [item.repair_fingerprint] : []
    ]))];
    reviewReceipt = {
      cycle_id: pending.cycle_id,
      cycle_phase: pending.cycle_phase,
      change_set_id: pending.change_set_id,
      review_target_revision: pending.review_target_revision,
      verdict: "clean",
      admitted_fingerprints: repaired,
      evidence_refs: [...pending.evidence_refs]
    };
  } else {
    fail7("CLEAN_REVIEW_REQUIRED", "complete-reviewed-step requires a canonical pending clean review result.");
  }
  const resultKey = idempotencyKey("execute-reviewed-step", { step_id: stepId, review_receipt: reviewReceipt, note });
  if (hasAppliedProposal2(current, resultKey)) {
    return semanticNoOp2(current, resultKey, "This exact reviewed-step completion was already committed.", options);
  }
  assertExecutableTask(current);
  const priorExecution = currentDefinitionExecutionLog(current).some((item) => !("action" in item) && item.step_id === stepId && item.idempotency_key.startsWith("execute-step-result-"));
  if (!priorExecution)
    fail7("EXECUTE_RESULT_REQUIRED", "complete-reviewed-step requires a prior semantic record-step-result for this step.");
  const retainedReview = current.runtimeState.active_step_id !== stepId && current.runtimeState.scope_amendment_pending_review_step_id === stepId;
  if (current.runtimeState.active_step_id !== stepId && !retainedReview) {
    fail7("ACTIVE_STEP_CONFLICT", `complete-reviewed-step targets ${stepId}, but the current active step is ${current.runtimeState.active_step_id}.`);
  }
  if (retainedReview) {
    assertExecutableTask(current);
    const proposal = createRetainedReviewConsumptionProposal(current, {
      step_id: stepId,
      review_receipt: reviewReceipt,
      evidence_refs: [...reviewReceipt.evidence_refs],
      idempotency_key: resultKey,
      authority_evidence: authority2(current, ["active-task-owner", "scope-admission", "evidence-admission"])
    });
    return verifyReadBack(root2, applyVNextRuntimeProposal(root2, proposal, options), options);
  }
  const stepPlan = currentStepPlan(current);
  if (reviewReceipt.cycle_id !== current.runtimeState.review_cycle.id) {
    fail7("REVIEW_CYCLE_CONFLICT", "review receipt does not belong to the current Runtime review cycle.");
  }
  const repairLogs = currentDefinitionExecutionLog(current).filter((item) => !("action" in item) && item.step_id === stepId && item.mode === "repair");
  const repairedFingerprints = [...new Set(repairLogs.flatMap((item) => [
    ...item.repair_fingerprints ?? [],
    ...item.repair_fingerprint ? [item.repair_fingerprint] : []
  ]))];
  assertExactResultSet(reviewReceipt.admitted_fingerprints, repairedFingerprints, "review_receipt.admitted_fingerprints");
  if (repairLogs.length > 0) {
    if (reviewReceipt.cycle_phase !== "verification") {
      fail7("REVIEW_VERIFICATION_REQUIRED", "a repaired step requires a verification review receipt.");
    }
    const repairTargets = [...new Set(repairLogs.flatMap((item) => item.change_set_id ? [item.change_set_id] : []))];
    if (repairTargets.length !== 1 || repairTargets[0] !== reviewReceipt.change_set_id) {
      fail7("REPAIR_CHANGE_SET_CONFLICT", "verification must cover the exact repaired logical change set.");
    }
  } else if (reviewReceipt.cycle_phase !== "discovery") {
    fail7("REVIEW_PHASE_INVALID", "an unrepaired step requires a discovery review receipt.");
  }
  const openFindings = current.runtimeState.findings.filter((item) => ["admitted", "in-progress"].includes(item.status));
  const unverifiedOpen = openFindings.filter((item) => !reviewReceipt.admitted_fingerprints.includes(item.fingerprint));
  if (unverifiedOpen.length > 0) {
    fail7("REVIEW_CONVERGENCE_REQUIRED", `open findings are not covered by the clean review receipt: ${unverifiedOpen.map((item) => item.fingerprint).join(", ")}.`);
  }
  const claimEvidence = current.runtimeState.claim_evidence;
  const resolution = resolveTaskStep(current.body, stepId);
  const finalEvidenceContext = hasRemainingCorrectionTargets(current, stepId) ? { root: root2, current, due_step_id: stepId } : { root: root2, current };
  if (resolution.next === null && (!claimEvidence || !evaluateClaimEvidence(claimEvidence, finalEvidenceContext).validation_complete)) {
    fail7("CLAIM_EVIDENCE_INCOMPLETE", "the final step cannot complete until every frozen acceptance-evidence slot has evidence.");
  }
  const sourceRevision = current.sourceTuple.revision;
  if (reviewReceipt.admitted_fingerprints.length > 0) {
    const resolved = resolveVerifiedFindings(root2, current, reviewReceipt, options);
    if ("status" in resolved)
      return resolved;
    current = resolved;
  }
  const previewedFindingResolution = options.dryRun === true && reviewReceipt.admitted_fingerprints.some((fingerprint) => current.runtimeState.findings.some((item) => item.fingerprint === fingerprint && item.status !== "resolved"));
  if (previewedFindingResolution)
    current = previewResolvedFindings(current, reviewReceipt.admitted_fingerprints);
  const evidenceRefs = [...reviewReceipt.evidence_refs];
  const proposal = createTaskStateProposal(current, {
    mode: "default",
    status: "completed",
    evidence_refs: evidenceRefs,
    idempotency_key: resultKey,
    authority_evidence: authority2(current, ["active-task-owner", "scope-admission", "evidence-admission"]),
    review_receipt: reviewReceipt,
    change_set_id: reviewReceipt.change_set_id,
    ...note ? { note } : {},
    ...claimEvidence === undefined ? {} : { claim_evidence: claimEvidence }
  });
  if (previewedFindingResolution) {
    if (readCanonicalCurrentTask(root2).sourceTuple.revision !== sourceRevision) {
      fail7("SOURCE_TUPLE_MISMATCH", "CURRENT_TASK changed during reviewed-step dry-run; obtain fresh canonical state.");
    }
    const preview = new GovernanceTransactionKernel(root2, () => current).apply(proposal, options);
    return preview.status === "success" ? { ...preview, resulting_revision: undefined, message: "Finding resolutions and reviewed-step completion validated against an in-memory dry-run state; no files were written." } : preview;
  }
  return verifyReadBack(root2, applyVNextRuntimeProposal(root2, proposal, options), options);
}
function parseExecuteStepAdapterCli(argv) {
  const [command, ...rest] = argv;
  if (!EXECUTE_STEP_ADAPTER_COMMANDS.includes(command)) {
    throw new Error(`Usage: vnext-runtime <${EXECUTE_STEP_ADAPTER_COMMANDS.join("|")}> --root <path> [--dry-run] (semantic JSON on stdin)`);
  }
  let root2 = process.cwd();
  let dryRun = false;
  for (let index = 0;index < rest.length; index += 1) {
    const arg = rest[index];
    if (arg === "--root")
      root2 = rest[++index] ?? "";
    else if (arg === "--dry-run")
      dryRun = true;
    else
      throw new Error(`Unknown execute-step adapter argument: ${arg}`);
  }
  if (!root2)
    throw new Error("--root requires a path.");
  return { command, root: root2, dryRun };
}
function readSemanticStdin2(command) {
  const raw = !process.stdin.isTTY ? fs19.readFileSync(0, "utf8") : "";
  if (!raw.trim())
    throw new Error(`${command} requires semantic JSON on stdin.`);
  try {
    return JSON.parse(raw);
  } catch (error) {
    throw new Error(`${command} stdin must be valid JSON: ${error instanceof Error ? error.message : String(error)}`);
  }
}
function validateInstalledRuntime2(root2) {
  const runtimeManifest = path20.join(path20.resolve(root2), ...VNEXT_RUNTIME_PACKAGE_RELATIVE_PATH.split("/"), "package.json");
  if (fs19.existsSync(runtimeManifest))
    validateVNextRuntimeContract(root2, true);
}
async function runExecuteStepAdapterCli(argv = process.argv.slice(2)) {
  try {
    validateRuntimeEnvironment();
    const args = parseExecuteStepAdapterCli(argv);
    validateInstalledRuntime2(args.root);
    const input = readSemanticStdin2(args.command);
    let result;
    switch (args.command) {
      case "preflight-step":
        result = preflightStep(args.root, input);
        break;
      case "extend-preflight":
        result = extendPreflight(args.root, input, { dryRun: args.dryRun });
        break;
      case "artifact-checkpoints":
        exactKeys3(record6(input, "artifact-checkpoints"), [], "artifact-checkpoints");
        result = listArtifactCheckpoints(args.root);
        break;
      case "apply-artifact-restore": {
        const source = record6(input, "apply-artifact-restore");
        exactKeys3(source, ["preflight_receipt"], "apply-artifact-restore");
        const receipt = normalizePreflightReceipt(source.preflight_receipt);
        const current = readCanonicalCurrentTask(args.root);
        assertCurrentReceipt(root, current, currentStepPlan(current), receipt);
        result = executeConfirmedArtifactRestore(args.root, current.sourceTuple.revision, receipt.step_id, receipt.candidate_paths, args.dryRun);
        break;
      }
      case "evidence-context":
        result = evidenceContext(args.root, input);
        break;
      case "replace-validation":
        result = replaceValidation(args.root, input, { dryRun: args.dryRun });
        break;
      case "retry-step":
        result = retryStep(args.root, input, { dryRun: args.dryRun });
        break;
      case "begin-repair":
        result = beginRepair(args.root, input, { dryRun: args.dryRun });
        break;
      case "record-step-result":
        result = recordStepResult(args.root, input, { dryRun: args.dryRun });
        break;
      case "complete-reviewed-step":
        result = completeReviewedStep(args.root, input, { dryRun: args.dryRun });
        break;
    }
    console.log(JSON.stringify(result, null, 2));
    return result.status === "blocked" || result.status === "conflict" ? 2 : 0;
  } catch (error) {
    console.error(error instanceof Error ? error.message : String(error));
    return 1;
  }
}

// runtime/vnext/src/review-change-adapter.ts
import * as crypto16 from "crypto";
import * as fs20 from "fs";
import * as path21 from "path";
var REVIEW_CHANGE_ADAPTER_COMMANDS = ["review-context", "review-read", "record-review-result", "record-evidence-challenge", "dismiss-evidence-challenge", "ingest-evidence", "route-input"];
var MAX_ITEMS3 = 256;
var SHA256_PATTERN5 = /^[a-f0-9]{64}$/u;
var WINDOWS_ABSOLUTE_PATH3 = /^[A-Za-z]:[\\/]/u;
function fail8(code, message) {
  throw new VNextRuntimeError(code, message);
}
function record7(value, location) {
  if (!value || typeof value !== "object" || Array.isArray(value))
    fail8("REVIEW_ADAPTER_INPUT_INVALID", `${location} must be an object.`);
  return value;
}
function exactKeys4(value, expected, location) {
  const allowed = new Set(expected);
  const missing = expected.filter((key) => !(key in value));
  const unexpected = Object.keys(value).filter((key) => !allowed.has(key));
  if (missing.length > 0 || unexpected.length > 0) {
    fail8("REVIEW_ADAPTER_INPUT_INVALID", `${location} keys mismatch; missing=[${missing.join(", ")}], unexpected=[${unexpected.join(", ")}].`);
  }
}
function text5(value, location, maximumLength = 4096) {
  if (typeof value !== "string")
    fail8("REVIEW_ADAPTER_INPUT_INVALID", `${location} must be a string.`);
  const normalized = value.trim();
  if (!normalized || normalized.length > maximumLength || /[\r\n]/u.test(normalized)) {
    fail8("REVIEW_ADAPTER_INPUT_INVALID", `${location} must be one non-empty line of at most ${maximumLength} characters.`);
  }
  return normalized;
}
function textList3(value, location, allowEmpty) {
  if (!Array.isArray(value) || !allowEmpty && value.length === 0 || value.length > MAX_ITEMS3) {
    fail8("REVIEW_ADAPTER_INPUT_INVALID", `${location} must be a bounded ${allowEmpty ? "" : "non-empty "}array.`);
  }
  const normalized = value.map((item, index) => text5(item, `${location}[${index}]`));
  if (new Set(normalized).size !== normalized.length)
    fail8("REVIEW_ADAPTER_INPUT_INVALID", `${location} must not contain duplicates.`);
  return normalized;
}
function repoPath(value, location) {
  const normalized = text5(value, location, 1024).replace(/\\/gu, "/").replace(/^\.\//u, "");
  if (path21.posix.isAbsolute(normalized) || WINDOWS_ABSOLUTE_PATH3.test(normalized) || normalized.split("/").includes("..")) {
    fail8("REVIEW_ADAPTER_INPUT_INVALID", `${location} must be a project-relative path.`);
  }
  return normalized;
}
function digest6(value) {
  return crypto16.createHash("sha256").update(JSON.stringify(value)).digest("hex");
}
function authority3(current) {
  return ["active-task-owner", "scope-admission", "evidence-admission"].map((kind) => ({
    kind,
    source: current.relativePath,
    subject: current.runtimeState.task_id,
    task_id: current.runtimeState.task_id,
    document_id: current.sourceTuple.document_id,
    source_revision: current.sourceTuple.revision
  }));
}
function recordEvidenceChallenge(root2, input, options = {}) {
  const source = record7(input, "record-evidence-challenge input");
  exactKeys4(source, ["claim_id", "slot_id", "result_id", "evidence_ref", "evidence_sha256", "reason"], "record-evidence-challenge input");
  const current = readCanonicalCurrentTask(root2);
  const challenge = {
    claim_id: text5(source.claim_id, "claim_id", 128),
    slot_id: text5(source.slot_id, "slot_id", 128),
    result_id: text5(source.result_id, "result_id", 128),
    evidence_ref: repoPath(source.evidence_ref, "evidence_ref"),
    evidence_sha256: text5(source.evidence_sha256, "evidence_sha256", 64),
    reason: text5(source.reason, "reason")
  };
  if (!SHA256_PATTERN5.test(challenge.evidence_sha256))
    fail8("REVIEW_ADAPTER_INPUT_INVALID", "evidence_sha256 must be a lowercase SHA-256 digest.");
  const idempotencyKey = `evidence-challenge-${digest6({ document_id: current.sourceTuple.document_id, ...challenge }).slice(0, 40)}`;
  const existing = (current.runtimeState.evidence_challenges ?? []).find((item) => item.claim_id === challenge.claim_id && item.slot_id === challenge.slot_id && item.result_id === challenge.result_id && item.evidence_sha256 === challenge.evidence_sha256);
  if (existing) {
    if (existing.evidence_ref !== challenge.evidence_ref || existing.reason !== challenge.reason)
      fail8("EVIDENCE_CHALLENGE_DUPLICATE", "The same result and material are already bound to a different challenge description.");
    return semanticNoOp3(current, idempotencyKey, "The exact challenge is already recorded.", options);
  }
  const proposal = createEvidenceChallengeProposal(current, {
    ...challenge,
    idempotency_key: idempotencyKey,
    authority_evidence: authority3(current)
  });
  return applyVNextRuntimeProposal(root2, proposal, options);
}
function ingestEvidence(root2, input, options = {}) {
  return withGovernanceWriteLock(root2, () => {
    const source = record7(input, "ingest-evidence input");
    exactKeys4(source, ["source_revision", "source_locator", "body"], "ingest-evidence input");
    const current = readCanonicalCurrentTask(root2);
    if (source.source_revision !== current.sourceTuple.revision)
      fail8("EVIDENCE_SOURCE_STALE", "Evidence ingestion must bind the exact current task revision.");
    return { status: "success", evidence_assurance: "caller-reported", ...ingestEvidenceText(root2, current.filePath, {
      source_revision: current.sourceTuple.revision,
      task_id: current.runtimeState.task_id,
      document_id: current.sourceTuple.document_id,
      source_locator: text5(source.source_locator, "source_locator", 2048),
      body: text5(source.body, "body", 1048576)
    }, options.dryRun === true) };
  });
}
function routeTaskInput(root2, input) {
  const source = record7(input, "route-input");
  exactKeys4(source, ["source_revision", "input_ref", "input_sha256", "relation", "operation", "reason"], "route-input");
  const current = readCanonicalCurrentTask(root2);
  if (source.source_revision !== current.sourceTuple.revision)
    fail8("INPUT_SOURCE_STALE", "Input routing must bind the current task.");
  const ref = repoPath(source.input_ref, "input_ref");
  if (describeEvidenceObjects(root2, [ref])[0]?.sha256 !== source.input_sha256)
    fail8("INPUT_EVIDENCE_STALE", "Input material changed.");
  if (!["unrelated", "current-task"].includes(String(source.relation)) || !["review-conclusion", "recover-execution", "change-goal", "change-acceptance", "expand-authority", "other"].includes(String(source.operation)))
    fail8("INPUT_ROUTE_INVALID", "Unknown relation or requested operation.");
  const reason = text5(source.reason, "reason");
  const authorityChange = ["change-goal", "change-acceptance", "expand-authority"].includes(String(source.operation));
  const route = source.relation === "unrelated" ? "capture-work-item" : authorityChange || source.operation === "other" ? "user" : source.operation === "review-conclusion" ? "review-change" : "debug-task";
  return {
    status: authorityChange ? "user-decision-required" : "routed",
    kind: "task-input-routing/v1",
    source_tuple: current.sourceTuple,
    input_ref: ref,
    input_sha256: source.input_sha256,
    relation: source.relation,
    operation: source.operation,
    reason,
    next_route: route,
    evidence_assurance: "caller-reported",
    permission_change: "none",
    receipt_digest: digest6({ source_tuple: current.sourceTuple, input: source, route })
  };
}
function dismissEvidenceChallenge(root2, input, options = {}) {
  const source = record7(input, "dismiss-evidence-challenge input");
  exactKeys4(source, ["challenge_id", "evidence_ref", "evidence_sha256", "reason"], "dismiss-evidence-challenge input");
  const current = readCanonicalCurrentTask(root2);
  const assessment = {
    challenge_id: text5(source.challenge_id, "challenge_id", 128),
    evidence_ref: repoPath(source.evidence_ref, "evidence_ref"),
    evidence_sha256: text5(source.evidence_sha256, "evidence_sha256", 64),
    reason: text5(source.reason, "reason")
  };
  if (!SHA256_PATTERN5.test(assessment.evidence_sha256))
    fail8("REVIEW_ADAPTER_INPUT_INVALID", "evidence_sha256 must be a lowercase SHA-256 digest.");
  const existing = current.runtimeState.evidence_challenges?.find((item) => item.challenge_id === assessment.challenge_id);
  const idempotencyKey = `evidence-challenge-dismissal-${digest6({ document_id: current.sourceTuple.document_id, ...assessment }).slice(0, 40)}`;
  if (existing?.resolution) {
    if (existing.resolution.evidence_ref !== assessment.evidence_ref || existing.resolution.evidence_sha256 !== assessment.evidence_sha256 || existing.resolution.reason !== assessment.reason)
      fail8("EVIDENCE_CHALLENGE_ALREADY_RESOLVED", "Challenge was resolved by a different assessment.");
    return semanticNoOp3(current, idempotencyKey, "The exact challenge assessment is already recorded.", options);
  }
  const proposal = createEvidenceChallengeDismissalProposal(current, {
    ...assessment,
    idempotency_key: idempotencyKey,
    authority_evidence: authority3(current)
  });
  return applyVNextRuntimeProposal(root2, proposal, options);
}
function latestRecordedExecution(current) {
  const records = currentDefinitionExecutionLog(current).filter((item) => !("action" in item) && item.step_id === current.runtimeState.active_step_id && item.idempotency_key.startsWith("execute-step-result-") && item.review_receipt === undefined);
  const latest = records[records.length - 1];
  if (!latest)
    fail8("REVIEW_EXECUTION_REQUIRED", "review-change requires a recorded execute-step result for the active step.");
  if (!latest.change_set_id || !latest.execution_result?.review_target) {
    fail8("REVIEW_TARGET_REQUIRED", "the latest execute-step result does not contain a Runtime-owned review target.");
  }
  if (latest.execution_result.change_set_id !== latest.change_set_id) {
    fail8("REVIEW_TARGET_CONFLICT", "the latest execution change-set identity is inconsistent.");
  }
  assertReviewExecutionEligible(current, latest);
  return cumulativeReviewExecution(current, latest);
}
function stepScope(value, location) {
  if (!value)
    fail8("TASK_STEP_METADATA_INCOMPLETE", `${location} is missing.`);
  const items = value.split(",").map((item) => item.replace(/`/gu, "").trim()).filter(Boolean);
  if (items.length === 0 || new Set(items).size !== items.length)
    fail8("TASK_STEP_METADATA_INCOMPLETE", `${location} is invalid.`);
  return items;
}
function validationList(value, location) {
  if (!value)
    fail8("TASK_STEP_METADATA_INCOMPLETE", `${location} is missing.`);
  return value.split(/;\s+/u).map((item) => item.trim()).filter(Boolean);
}
function assertReviewableTask(current) {
  if (current.runtimeState.workflow_status !== "active" || current.runtimeState.lifecycle_state !== "active") {
    fail8("TASK_STATE_NOT_ACTIVE", "review-change requires active + active.");
  }
  if (current.runtimeState.resume_requires_review)
    fail8("RESUME_REVIEW_REQUIRED", "review-change is blocked by the current resume-review gate.");
}
var MAX_CONTEXT_ENTRIES = 64;
function boundedList(values, limit = MAX_CONTEXT_ENTRIES) {
  return { values: values.slice(0, limit), total: values.length, truncated: values.length > limit };
}
function boundedValues(values, limit = MAX_CONTEXT_ENTRIES) {
  return { values: values.slice(0, limit), total: values.length, truncated: values.length > limit };
}
function containsTruncation(value) {
  if (Array.isArray(value))
    return value.some(containsTruncation);
  if (!value || typeof value !== "object")
    return false;
  return Object.entries(value).some(([key, item]) => key.endsWith("_truncated") && item === true || containsTruncation(item));
}
function reviewTargetSummary(value) {
  const entries = value.entries.slice(0, MAX_CONTEXT_ENTRIES).map((item) => ({ ...item }));
  return {
    kind: value.kind,
    revision: value.revision,
    entries,
    entry_count: value.entries.length,
    entries_truncated: value.entries.length > MAX_CONTEXT_ENTRIES
  };
}
function evidenceReportSummary(value) {
  if (!value || typeof value !== "object" || Array.isArray(value))
    return null;
  const report = value;
  return {
    result_id: report.result_id ?? null,
    status: report.status ?? null,
    evidence_plan_revision: report.evidence_plan_revision ?? null,
    subject_revision: report.subject_revision ?? null,
    actual_method: report.actual_method ?? null,
    assurance: report.assurance ?? null
  };
}
function evidenceRefSummary(values) {
  const refs = boundedList(values);
  return { evidence_refs: refs.values, evidence_ref_count: refs.total, evidence_refs_truncated: refs.truncated };
}
function observedWritesSummary(values) {
  const writes = boundedList(values);
  return {
    observed_repo_writes: writes.values,
    observed_repo_write_count: writes.total,
    observed_repo_writes_truncated: writes.truncated
  };
}
function claimEvidenceSummary(value) {
  const slots = boundedValues(value.slots).values.map((slot) => ({
    slot_id: slot.slot_id,
    minimum_type: slot.minimum_type,
    disposition: slot.disposition,
    user_decision: slot.user_decision ? { ...slot.user_decision } : null,
    obligation_resolution: slot.user_decision?.kind === "waiver" ? "explicit-risk-decision-not-PASS" : "evidence-required",
    applicability: slot.applicability ?? null,
    due_step_id: slot.due_step_id ?? null,
    before_step_id: slot.before_step_id ?? null,
    check_id: slot.check?.check_id ?? null,
    frozen_invocation: slot.check?.entry ?? null,
    validation_items: [...slot.check?.validation_items ?? []],
    boundary: slot.check?.boundary ?? null,
    required_observation: slot.check?.expected_observation ?? null,
    required_boundaries: slot.check?.required_boundaries ?? [],
    execution_selection: slot.check?.selection ? { ...slot.check.selection } : null,
    result_id: slot.report?.result_id ?? null,
    report: evidenceReportSummary(slot.report),
    ...evidenceRefSummary(slot.evidence_refs)
  }));
  return {
    claim_id: value.claim_id,
    claim_kind: value.claim_kind,
    requirement: value.requirement,
    source_ref: value.source_ref,
    slots,
    slot_count: value.slots.length,
    slots_truncated: value.slots.length > MAX_CONTEXT_ENTRIES
  };
}
function eventReference(root2, current, idempotencyKey) {
  const store = TaskStore.forCurrent(root2, current);
  if (!store.manifest)
    return null;
  const match = store.lookupIdempotency(idempotencyKey);
  return match ? { kind: "event", event_path: match.event_path } : null;
}
function copyExecutionResult(value) {
  if (!value)
    return null;
  const actualPaths = boundedList(value.actual_changed_paths);
  const commandResults = value.command_results.slice(0, MAX_CONTEXT_ENTRIES).map((item) => ({
    command: item.command,
    status: item.status,
    ...item.waiver_decision_id ? { waiver_decision_id: item.waiver_decision_id } : {},
    ...observedWritesSummary(item.observed_repo_writes),
    ...evidenceRefSummary(item.evidence_refs),
    ...item.expected_failure === undefined ? {} : { expected_failure: { ...item.expected_failure } }
  }));
  const validationResults = value.validation_results.slice(0, MAX_CONTEXT_ENTRIES).map((item) => ({
    validation: item.validation,
    status: item.status,
    ...item.waiver_decision_id ? { waiver_decision_id: item.waiver_decision_id } : {},
    ...evidenceRefSummary(item.evidence_refs),
    ...item.expected_failure === undefined ? {} : { expected_failure: { ...item.expected_failure } }
  }));
  const acceptanceEvidence = value.acceptance_evidence.slice(0, MAX_CONTEXT_ENTRIES).map((item) => {
    if ("acceptance" in item)
      return { acceptance: item.acceptance, ...evidenceRefSummary(item.evidence_refs) };
    return {
      claim_id: item.claim_id,
      slot_id: item.slot_id,
      check_id: item.check_id,
      minimum_type: item.minimum_type,
      disposition: item.disposition,
      ...evidenceRefSummary(item.evidence_refs),
      report: evidenceReportSummary(item.report)
    };
  });
  return {
    ...value.execution_id === undefined ? {} : { execution_id: value.execution_id },
    ...value.attempt_id === undefined ? {} : { attempt_id: value.attempt_id },
    ...value.blocker_kind === undefined ? {} : { blocker_kind: value.blocker_kind },
    outcome: value.outcome,
    change_set_id: value.change_set_id,
    review_base: reviewTargetSummary(value.review_base),
    review_target: reviewTargetSummary(value.review_target),
    change_delta: {
      kind: value.change_delta.kind,
      base_revision: value.change_delta.base_revision,
      target_revision: value.change_delta.target_revision,
      entries: value.change_delta.entries.slice(0, MAX_CONTEXT_ENTRIES).map((item) => ({ ...item })),
      entry_count: value.change_delta.entries.length,
      entries_truncated: value.change_delta.entries.length > MAX_CONTEXT_ENTRIES
    },
    actual_changed_paths: actualPaths.values,
    actual_changed_path_count: actualPaths.total,
    actual_changed_paths_truncated: actualPaths.truncated,
    command_results: commandResults,
    command_result_count: value.command_results.length,
    command_results_truncated: value.command_results.length > MAX_CONTEXT_ENTRIES,
    validation_results: validationResults,
    validation_result_count: value.validation_results.length,
    validation_results_truncated: value.validation_results.length > MAX_CONTEXT_ENTRIES,
    acceptance_evidence: acceptanceEvidence,
    acceptance_evidence_count: value.acceptance_evidence.length,
    acceptance_evidence_truncated: value.acceptance_evidence.length > MAX_CONTEXT_ENTRIES,
    blocker: value.blocker
  };
}
function reviewContext(root2, input) {
  const source = record7(input, "review-context input");
  exactKeys4(source, [], "review-context input");
  const current = readCanonicalCurrentTask(root2);
  assertReviewableTask(current);
  const execution = latestRecordedExecution(current);
  const currentTarget = captureReviewTarget(root2, execution.execution_result.review_target.entries.map((item) => item.path));
  if (currentTarget.revision !== execution.execution_result.review_target.revision) {
    fail8("REVIEW_TARGET_STALE", "product files changed after the latest execution result was recorded.");
  }
  if (current.runtimeState.pending_review_result?.execution_id === execution.idempotency_key && current.runtimeState.pending_review_result.verdict !== "blocked") {
    fail8("REVIEW_ALREADY_RECORDED", "the latest execution already has a durable clean or findings review result.");
  }
  const phase = execution.mode === "repair" ? "verification" : "discovery";
  const resolution = resolveTaskStep(current.body, current.runtimeState.active_step_id);
  if (!resolution.current.metadata_complete || !resolution.current.purpose) {
    fail8("TASK_STEP_METADATA_INCOMPLETE", `step ${resolution.current.id} is not reviewable because its metadata is incomplete.`);
  }
  const scope = parseMutationScope(current.body, current.sourceTuple.revision);
  const admitted = phase === "verification" ? current.runtimeState.findings.filter((item) => item.review_cycle_id === current.runtimeState.review_cycle.id && ["admitted", "in-progress"].includes(item.status)) : [];
  const definition = readDraftDefinitionFromBody(current.body);
  const plannedMutationTargets = stepScope(resolution.current.planned_mutation_targets ?? resolution.current.mutation_scope, `step ${resolution.current.id} planned_mutation_targets`);
  const receipt = {
    kind: "review-context/v1",
    task_id: current.runtimeState.task_id,
    document_id: current.sourceTuple.document_id,
    source_revision: current.sourceTuple.revision,
    step_id: resolution.current.id,
    execution_id: execution.idempotency_key,
    cycle_id: current.runtimeState.review_cycle.id,
    cycle_phase: phase,
    admitted_fingerprints: admitted.map((item) => item.fingerprint)
  };
  const executionEvidenceRefs = boundedList(execution.evidence_refs);
  const unexpandedPaths = boundedList(execution.execution_result.change_delta.entries.slice(1).map((item) => item.path));
  const persistentTests = scope.persistent_tests === null ? null : boundedList(scope.persistent_tests);
  const expandedMutationTargets = boundedValues(currentExecutionDynamicExpansions(current));
  const claimEvidence = boundedValues(current.runtimeState.claim_evidence ?? []);
  const claimSummaries = claimEvidence.values.map(claimEvidenceSummary);
  const admittedFindings = boundedValues(admitted);
  const nestedSlotsTruncated = claimEvidence.values.some((item) => item.slots.length > MAX_CONTEXT_ENTRIES);
  const executionResult = copyExecutionResult(execution.execution_result);
  const executionResultTruncated = containsTruncation(executionResult);
  const requiredUnexpanded = [
    ...unexpandedPaths.truncated ? ["cumulative-review-target"] : [],
    ...executionResultTruncated ? ["recorded-execution"] : [],
    ...persistentTests?.truncated ? ["persistent-tests"] : [],
    ...claimEvidence.truncated || nestedSlotsTruncated ? ["claim-evidence"] : [],
    ...admittedFindings.truncated ? ["admitted-findings"] : [],
    ...expandedMutationTargets.truncated ? ["dynamic-expansions"] : []
  ];
  return {
    status: "pass",
    operation_kind: "review-context",
    committed: false,
    read_back_verified: true,
    recorded_execution: {
      id: execution.idempotency_key,
      mode: execution.mode,
      status: execution.status,
      change_set_id: execution.change_set_id,
      review_target_revision: execution.execution_result.review_target.revision,
      evidence_refs: executionEvidenceRefs.values,
      evidence_ref_count: executionEvidenceRefs.total,
      evidence_refs_truncated: executionEvidenceRefs.truncated,
      execution_result: executionResult,
      event_reference: eventReference(root2, current, execution.idempotency_key)
    },
    current_step: {
      id: resolution.current.id,
      description: resolution.current.description,
      purpose: resolution.current.purpose,
      planned_mutation_targets: plannedMutationTargets,
      mutation_scope: stepScope(resolution.current.mutation_scope, `step ${resolution.current.id} mutation_scope`),
      validation: validationList(resolution.current.required_evidence, `step ${resolution.current.id} required_evidence`)
    },
    project_documents: readProjectDocuments(definition.background_context),
    affected_contracts: definition.affected_contracts,
    acceptance: definition.acceptance,
    regression_checks: definition.regression_checks,
    mutation_scope: {
      allowed: scope.allowed.map((item) => item.pattern),
      conditional: scope.conditional.map((item) => item.pattern),
      forbidden: scope.forbidden.map((item) => item.pattern)
    },
    mutation_authority: current.mutationAuthority === null ? null : {
      version: 2,
      domains: [...current.mutationAuthority.domains],
      exact_exceptions: [...current.mutationAuthority.exact_exceptions],
      forbidden: [...current.mutationAuthority.forbidden]
    },
    planned_mutation_targets: plannedMutationTargets,
    expanded_mutation_targets: expandedMutationTargets.values,
    dynamic_review_required: dynamicReviewRequiredForCurrentExecution(current),
    text_diff: execution.execution_result.change_delta.entries.length ? reviewFilePage(root2, current, execution, execution.execution_result.change_delta.entries[0].path, "diff", {}) : null,
    unexpanded_paths: unexpandedPaths.values,
    unexpanded_path_count: unexpandedPaths.total,
    unexpanded_paths_truncated: unexpandedPaths.truncated,
    persistent_tests: persistentTests?.values ?? null,
    persistent_tests_count: persistentTests?.total ?? 0,
    persistent_tests_truncated: persistentTests?.truncated ?? false,
    claim_evidence: claimSummaries,
    claim_evidence_count: claimEvidence.total,
    claim_evidence_truncated: claimEvidence.truncated || nestedSlotsTruncated,
    claim_evidence_read_reference: { command: "task-read", kind: "claim-evidence", required: true },
    admitted_findings: admittedFindings.values.map((item) => ({
      fingerprint: item.fingerprint,
      file: item.file,
      failure_condition: item.failure_condition,
      required_behavior: item.violated_invariant,
      repair_attempts: item.repair_attempts,
      max_repair_attempts: item.max_repair_attempts
    })),
    admitted_finding_count: admittedFindings.total,
    admitted_findings_truncated: admittedFindings.truncated,
    complete_for_operation: requiredUnexpanded.length === 0,
    required_unexpanded: requiredUnexpanded,
    context_projection: taskContextReferenceForCurrent(root2, current, "review-context", "review"),
    receipt
  };
}
function reviewFilePage(root2, current, execution, file, view, input) {
  if (!["before", "after", "diff"].includes(view))
    fail8("CONTEXT_INPUT_INVALID", "view must be before, after or diff.");
  const target = execution.execution_result.review_target.entries.find((item) => item.path === file);
  if (!target)
    fail8("REVIEW_PATH_OUTSIDE_TARGET", "path is not part of this cumulative review target.");
  const preimage = current.runtimeState.review_coverage?.preimages.find((item) => item.path === file);
  const base = { path: file, view, target_revision: execution.execution_result.review_target.revision };
  if (!preimage && view !== "after")
    return { ...base, content_status: "baseline-unavailable" };
  if (target.state === "symlink" || preimage?.state === "symlink")
    return { ...base, content_status: "symlink-not-followed" };
  let before = Buffer.alloc(0);
  if (preimage?.state === "file") {
    try {
      const legacy = Object.prototype.hasOwnProperty.call(preimage, "content_base64") ? decodeLegacyReviewPreimage(preimage) : null;
      before = legacy?.content ?? readReviewPreimageBlob(root2, preimage.sha256);
    } catch (error) {
      if (error instanceof ReviewPreimageStoreError)
        fail8(error.code, error.message);
      throw error;
    }
  }
  const after = target.state === "file" ? fs20.readFileSync(contextPath(root2, file).absolute) : Buffer.alloc(0);
  if (target.state === "file" && sha2569(after) !== target.sha256)
    fail8("REVIEW_TARGET_STALE", "file changed while reading review context.");
  if (preimage?.state === "file" && sha2569(before) !== preimage.sha256)
    fail8("REVIEW_BASE_INVALID", "first-touch baseline hash mismatch.");
  const left = decodeText(before);
  const right = decodeText(after);
  if (view !== "after" && left === null || view !== "before" && right === null)
    return { ...base, content_status: "binary-or-non-utf8" };
  let text;
  if (view === "before")
    text = left;
  else if (view === "after")
    text = right;
  else
    text = textDiff(file, left, right);
  if (text === undefined)
    return { ...base, content_status: "diff-budget-exceeded", next_read: ["before", "after"] };
  return { ...base, content_status: "text", before_state: preimage?.state ?? null, after_state: target.state, ...textPage(text, input) };
}
function reviewRead(root2, input) {
  const value = contextInput(input, ["context_receipt", "path", "view", "offset", "max_bytes", "start_line", "end_line"]);
  const current = readCanonicalCurrentTask(root2);
  assertReviewableTask(current);
  const receipt = normalizeContextReceipt(value.context_receipt);
  const execution = assertCurrentContext(root2, current, receipt);
  const file = repoPath(value.path, "path");
  return {
    status: "pass",
    operation_kind: "review-read",
    committed: false,
    ...reviewFilePage(root2, current, execution, file, String(value.view ?? "diff"), value)
  };
}
function normalizeContextReceipt(value) {
  const source = record7(value, "context_receipt");
  exactKeys4(source, ["kind", "task_id", "document_id", "source_revision", "step_id", "execution_id", "cycle_id", "cycle_phase", "admitted_fingerprints"], "context_receipt");
  if (source.kind !== "review-context/v1")
    fail8("REVIEW_ADAPTER_INPUT_INVALID", "context_receipt.kind must be review-context/v1.");
  if (source.cycle_phase !== "discovery" && source.cycle_phase !== "verification")
    fail8("REVIEW_ADAPTER_INPUT_INVALID", "context_receipt.cycle_phase is invalid.");
  const sourceRevision = text5(source.source_revision, "context_receipt.source_revision", 64);
  if (!SHA256_PATTERN5.test(sourceRevision))
    fail8("REVIEW_ADAPTER_INPUT_INVALID", "context_receipt.source_revision must be SHA-256.");
  return {
    kind: "review-context/v1",
    task_id: text5(source.task_id, "context_receipt.task_id", 128),
    document_id: text5(source.document_id, "context_receipt.document_id", 128),
    source_revision: sourceRevision,
    step_id: text5(source.step_id, "context_receipt.step_id", 128),
    execution_id: text5(source.execution_id, "context_receipt.execution_id", 128),
    cycle_id: text5(source.cycle_id, "context_receipt.cycle_id", 128),
    cycle_phase: source.cycle_phase,
    admitted_fingerprints: textList3(source.admitted_fingerprints, "context_receipt.admitted_fingerprints", true)
  };
}
function assertCurrentContext(root2, current, receipt) {
  if (receipt.task_id !== current.runtimeState.task_id || receipt.document_id !== current.sourceTuple.document_id)
    fail8("REVIEW_CONTEXT_STALE", "review context belongs to a different task document.");
  if (!taskSourceRevisionMatches(root2, current, receipt.source_revision))
    fail8("REVIEW_CONTEXT_STALE", "CURRENT_TASK changed after review context was issued.");
  if (receipt.step_id !== current.runtimeState.active_step_id || receipt.cycle_id !== current.runtimeState.review_cycle.id)
    fail8("REVIEW_CONTEXT_STALE", "active step or review cycle changed after review context was issued.");
  const latest = latestRecordedExecution(current);
  if (latest.idempotency_key !== receipt.execution_id)
    fail8("REVIEW_CONTEXT_STALE", "recorded execution changed after review context was issued.");
  const currentTarget = captureReviewTarget(root2, latest.execution_result.review_target.entries.map((item) => item.path));
  if (currentTarget.revision !== latest.execution_result.review_target.revision) {
    fail8("REVIEW_TARGET_STALE", "product files changed after review context was issued.");
  }
  const expectedPhase = latest.mode === "repair" ? "verification" : "discovery";
  if (receipt.cycle_phase !== expectedPhase)
    fail8("REVIEW_CONTEXT_STALE", "review phase changed after review context was issued.");
  const open = expectedPhase === "verification" ? current.runtimeState.findings.filter((item) => item.review_cycle_id === receipt.cycle_id && ["admitted", "in-progress"].includes(item.status)).map((item) => item.fingerprint) : [];
  if (open.length !== receipt.admitted_fingerprints.length || open.some((item) => !receipt.admitted_fingerprints.includes(item))) {
    fail8("REVIEW_CONTEXT_STALE", "admitted finding set changed after review context was issued.");
  }
  return cumulativeReviewExecution(current, latest);
}
function assertRecordedTargetCurrent(root2, current, receipt) {
  const execution = current.runtimeState.execution_log.map((item) => ("action" in item) ? item : cumulativeReviewExecution(current, item)).find((item) => !("action" in item) && item.idempotency_key === receipt.execution_id);
  if (!execution?.execution_result || !execution.change_set_id || execution.execution_result.change_set_id !== execution.change_set_id) {
    fail8("REVIEW_TARGET_CONFLICT", "review result no longer binds its Runtime-recorded execution target.");
  }
  const currentTarget = captureReviewTarget(root2, execution.execution_result.review_target.entries.map((item) => item.path));
  if (currentTarget.revision !== execution.execution_result.review_target.revision) {
    fail8("REVIEW_TARGET_STALE", "product files changed after the recorded execution target.");
  }
  return execution;
}
function normalizeFinding(value, index, current, root2) {
  const source = record7(value, `findings[${index}]`);
  exactKeys4(source, ["category", "file", "failure_condition", "required_behavior", "root_cause_status", "evidence_refs"], `findings[${index}]`);
  if (source.root_cause_status !== "confirmed" && source.root_cause_status !== "bounded")
    fail8("REVIEW_ADAPTER_INPUT_INVALID", `findings[${index}].root_cause_status is invalid.`);
  const candidate = {
    category: text5(source.category, `findings[${index}].category`, 256),
    file: repoPath(source.file, `findings[${index}].file`),
    failure_condition: text5(source.failure_condition, `findings[${index}].failure_condition`),
    required_behavior: text5(source.required_behavior, `findings[${index}].required_behavior`, 512),
    root_cause_status: source.root_cause_status,
    evidence_refs: textList3(source.evidence_refs, `findings[${index}].evidence_refs`, false)
  };
  const step = resolveTaskStep(current.body, current.runtimeState.active_step_id).current;
  if (current.mutationAuthority) {
    let project;
    try {
      project = readProjectMutationAuthority(root2);
    } catch (error) {
      fail8(error instanceof MutationAuthorityError ? error.code : "MUTATION_AUTHORITY_PROJECT_INVALID", error instanceof Error ? error.message : String(error));
    }
    if (!project)
      fail8("MUTATION_AUTHORITY_PROJECT_REQUIRED", "v2 review requires PROJECT_PROFILE.yaml.mutation_authority.domains.");
    const authorityDecision = evaluateMutationAuthority({
      root: root2,
      project,
      task: current.mutationAuthority,
      candidate_paths: [candidate.file],
      planned_targets: stepScope(step.planned_mutation_targets ?? step.mutation_scope, `step ${step.id} planned_mutation_targets`),
      assessments: currentExecutionDynamicExpansions(current).map((item) => item.assessment),
      persistent_test_paths: resolveTestStrategyExecutionContext(current).persistent_tests
    });
    if (authorityDecision.status !== "pass") {
      fail8("REVIEW_FINDING_SCOPE_BLOCKED", `finding path ${candidate.file} cannot be repaired within the v2 task authority: ${authorityDecision.blockers.join(" ")}`);
    }
  } else {
    const scope = parseMutationScope(current.body, current.sourceTuple.revision);
    const decision = evaluateMutationScope(scope, { changed_paths: [candidate.file] });
    const admittedByStep = stepScope(step.mutation_scope, `step ${step.id} mutation_scope`).some((pattern) => mutationScopePatternMatchesPath(candidate.file, pattern));
    if (decision.status !== "pass" || !admittedByStep) {
      fail8("REVIEW_FINDING_SCOPE_BLOCKED", `finding path ${candidate.file} cannot be repaired within the confirmed current-step scope.`);
    }
  }
  return {
    fingerprint: `finding-${digest6({
      task_id: current.runtimeState.task_id,
      file: candidate.file,
      failure_condition: candidate.failure_condition,
      required_behavior: candidate.required_behavior
    }).slice(0, 32)}`,
    ...candidate
  };
}
function convergenceBlocker(current, receipt, findings, unresolved) {
  if (receipt.cycle_phase !== "verification")
    return null;
  if (findings.length > 0 && current.runtimeState.review_cycle.verification_new_finding_wave_used) {
    return { code: "NEW_FINDING_WAVE_BUDGET_EXHAUSTED", summary: "Verification found another new-finding wave after the one allowed wave was already used.", next_route: "debug-task" };
  }
  if (current.runtimeState.review_cycle.repair_round >= MAX_REPAIR_ROUNDS && (findings.length > 0 || unresolved.length > 0)) {
    return { code: "REPAIR_BUDGET_EXHAUSTED", summary: "The current review cycle has exhausted its repair-wave budget.", next_route: "debug-task" };
  }
  const exhausted = unresolved.filter((fingerprint) => {
    const finding = current.runtimeState.findings.find((item) => item.fingerprint === fingerprint);
    return finding !== undefined && finding.repair_attempts >= finding.max_repair_attempts;
  });
  if (exhausted.length > 0) {
    return { code: "REPAIR_BUDGET_EXHAUSTED", summary: `Findings exhausted their repair-attempt budget: ${exhausted.join(", ")}.`, next_route: "debug-task" };
  }
  return null;
}
function verifyReadBack2(root2, result, reviewId, options) {
  if (options.dryRun || result.status !== "success" && result.status !== "no-op")
    return result;
  const current = readCanonicalCurrentTask(root2);
  if (!result.read_back_verified || current.runtimeState.pending_review_result?.review_id !== reviewId || result.resulting_revision !== current.sourceTuple.revision) {
    fail8("REVIEW_ADAPTER_READ_BACK_FAILED", "review-change adapter could not verify the durable pending review result.");
  }
  return result;
}
function semanticNoOp3(current, key, message, options) {
  const state = current.runtimeState;
  return {
    status: "no-op",
    operation_kind: "task-state-transaction",
    idempotency_key: key,
    target_path: current.relativePath,
    dry_run: options.dryRun === true,
    committed: false,
    message,
    previous_revision: current.sourceTuple.revision,
    resulting_revision: current.sourceTuple.revision,
    planned_writes: [],
    governed_mutation_count: 0,
    read_back_verified: true,
    state: {
      task_id: state.task_id,
      workflow_status: state.workflow_status,
      lifecycle_state: state.lifecycle_state,
      resume_requires_review: state.resume_requires_review,
      resume_review_reasons: [...state.resume_review_reasons],
      active_step_id: state.active_step_id,
      active_step_status: state.active_step_status,
      finding_queue_revision: state.finding_queue_revision,
      review_cycle_id: state.review_cycle.id,
      repair_round: state.review_cycle.repair_round
    }
  };
}
function recordReviewResult(root2, input, options = {}) {
  const source = record7(input, "record-review-result input");
  exactKeys4(source, ["context_receipt", "verdict", "findings", "unresolved_fingerprints", "evidence_refs", "blocker", ...source.test_assessment === undefined ? [] : ["test_assessment"]], "record-review-result input");
  const receipt = normalizeContextReceipt(source.context_receipt);
  if (!["clean", "findings", "blocked"].includes(String(source.verdict)))
    fail8("REVIEW_ADAPTER_INPUT_INVALID", "verdict must be clean, findings, or blocked.");
  const verdict = source.verdict;
  const current = readCanonicalCurrentTask(root2);
  assertReviewableTask(current);
  const recordedExecution = assertRecordedTargetCurrent(root2, current, receipt);
  if (!Array.isArray(source.findings) || source.findings.length > MAX_ITEMS3)
    fail8("REVIEW_ADAPTER_INPUT_INVALID", "findings must be a bounded array.");
  const findings = source.findings.map((item, index) => normalizeFinding(item, index, current, root2));
  if (new Set(findings.map((item) => item.fingerprint)).size !== findings.length)
    fail8("REVIEW_ADAPTER_INPUT_INVALID", "findings must not contain duplicates.");
  const unresolved = textList3(source.unresolved_fingerprints, "unresolved_fingerprints", true);
  if (unresolved.some((item) => !receipt.admitted_fingerprints.includes(item)))
    fail8("REVIEW_ADAPTER_INPUT_INVALID", "unresolved_fingerprints must be drawn from the Runtime review context.");
  const evidenceRefs = textList3(source.evidence_refs, "evidence_refs", false);
  let blocker;
  if (source.blocker === null)
    blocker = null;
  else {
    const raw = record7(source.blocker, "blocker");
    exactKeys4(raw, ["code", "summary", "next_route"], "blocker");
    if (!["review-change", "debug-task", "prepare-task:replan", "prepare-task:amend-scope", "user"].includes(String(raw.next_route)))
      fail8("REVIEW_ADAPTER_INPUT_INVALID", "blocker.next_route is invalid.");
    blocker = { code: text5(raw.code, "blocker.code", 128), summary: text5(raw.summary, "blocker.summary"), next_route: raw.next_route };
  }
  if (verdict === "clean" && (findings.length > 0 || unresolved.length > 0 || blocker !== null))
    fail8("REVIEW_ADAPTER_INPUT_INVALID", "clean must not contain findings or a blocker.");
  if (verdict === "findings" && (findings.length === 0 && unresolved.length === 0 || blocker !== null))
    fail8("REVIEW_ADAPTER_INPUT_INVALID", "findings verdict requires a finding and no blocker.");
  if (verdict === "blocked" && (findings.length > 0 || unresolved.length > 0 || blocker === null))
    fail8("REVIEW_ADAPTER_INPUT_INVALID", "blocked requires only a blocker.");
  const runtimeBlocker = verdict === "findings" ? convergenceBlocker(current, receipt, findings, unresolved) : null;
  const finalVerdict = runtimeBlocker ? "blocked" : verdict;
  const finalFindings = runtimeBlocker ? [] : findings;
  const finalUnresolved = runtimeBlocker ? [] : unresolved;
  const finalBlocker = runtimeBlocker ?? blocker;
  const reviewId = `review-${digest6({ receipt, verdict: finalVerdict, findings: finalFindings, unresolved: finalUnresolved, evidenceRefs, blocker: finalBlocker }).slice(0, 40)}`;
  const reviewResult = {
    ...source.test_assessment === undefined ? {} : { test_assessment: validateTestAssessment(source.test_assessment) },
    kind: "review-result/v1",
    review_id: reviewId,
    execution_id: receipt.execution_id,
    step_id: receipt.step_id,
    cycle_id: receipt.cycle_id,
    cycle_phase: receipt.cycle_phase,
    change_set_id: recordedExecution.change_set_id,
    review_target_revision: recordedExecution.execution_result.review_target.revision,
    verdict: finalVerdict,
    findings: finalFindings,
    unresolved_fingerprints: finalUnresolved,
    evidence_refs: evidenceRefs,
    blocker: finalBlocker
  };
  const resultKey = `review-result-${digest6(reviewResult).slice(0, 48)}`;
  if (current.runtimeState.pending_review_result?.review_id === reviewId) {
    const { recorded_at: _recordedAt, ...durable } = current.runtimeState.pending_review_result;
    if (digest6(durable) !== digest6(reviewResult))
      fail8("REVIEW_REPLAY_CONFLICT", "the durable review id is bound to different review semantics.");
    return semanticNoOp3(current, resultKey, "This exact review result was already recorded.", options);
  }
  assertCurrentContext(root2, current, receipt);
  const proposal = createReviewResultProposal(current, {
    review_result: reviewResult,
    evidence_refs: evidenceRefs,
    idempotency_key: resultKey,
    authority_evidence: authority3(current)
  });
  return verifyReadBack2(root2, applyVNextRuntimeProposal(root2, proposal, options), reviewId, options);
}
function parseCli2(argv) {
  const [command, ...rest] = argv;
  if (!REVIEW_CHANGE_ADAPTER_COMMANDS.includes(command))
    throw new Error(`Usage: vnext-runtime <${REVIEW_CHANGE_ADAPTER_COMMANDS.join("|")}> --root <path> [--dry-run] (semantic JSON on stdin)`);
  let root2 = process.cwd();
  let dryRun = false;
  for (let index = 0;index < rest.length; index += 1) {
    const arg = rest[index];
    if (arg === "--root")
      root2 = rest[++index] ?? "";
    else if (arg === "--dry-run")
      dryRun = true;
    else
      throw new Error(`Unknown review-change adapter argument: ${arg}`);
  }
  if (!root2)
    throw new Error("--root requires a path.");
  return { command, root: root2, dryRun };
}
function readSemanticStdin3(command) {
  const raw = !process.stdin.isTTY ? fs20.readFileSync(0, "utf8") : "";
  if (!raw.trim())
    throw new Error(`${command} requires semantic JSON on stdin.`);
  try {
    return JSON.parse(raw);
  } catch (error) {
    throw new Error(`${command} stdin must be valid JSON: ${error instanceof Error ? error.message : String(error)}`);
  }
}
function validateInstalledRuntime3(root2) {
  const runtimeManifest = path21.join(path21.resolve(root2), ...VNEXT_RUNTIME_PACKAGE_RELATIVE_PATH.split("/"), "package.json");
  if (fs20.existsSync(runtimeManifest))
    validateVNextRuntimeContract(root2, true);
}
async function runReviewChangeAdapterCli(argv = process.argv.slice(2)) {
  try {
    validateRuntimeEnvironment();
    const args = parseCli2(argv);
    validateInstalledRuntime3(args.root);
    const input = readSemanticStdin3(args.command);
    let result;
    if (args.command === "review-context")
      result = reviewContext(args.root, input);
    else if (args.command === "review-read")
      result = reviewRead(args.root, input);
    else if (args.command === "record-review-result")
      result = recordReviewResult(args.root, input, { dryRun: args.dryRun });
    else if (args.command === "record-evidence-challenge")
      result = recordEvidenceChallenge(args.root, input, { dryRun: args.dryRun });
    else if (args.command === "ingest-evidence")
      result = ingestEvidence(args.root, input, { dryRun: args.dryRun });
    else if (args.command === "route-input")
      result = routeTaskInput(args.root, input);
    else
      result = dismissEvidenceChallenge(args.root, input, { dryRun: args.dryRun });
    console.log(JSON.stringify(result, null, 2));
    return "status" in result && (result.status === "blocked" || result.status === "conflict") ? 2 : 0;
  } catch (error) {
    console.error(error instanceof Error ? error.message : String(error));
    return 1;
  }
}

// runtime/vnext/src/file-context-cli.ts
import * as fs21 from "node:fs";
import * as path22 from "node:path";
async function runFileContextCli(args) {
  try {
    validateRuntimeEnvironment();
    if (args.length !== 2 || args[0] !== "--root" || !args[1])
      throw new Error("Usage: file-context --root <project> (JSON on stdin)");
    const root2 = path22.resolve(args[1]);
    if (fs21.existsSync(path22.join(root2, VNEXT_RUNTIME_PACKAGE_RELATIVE_PATH, "package.json")))
      validateVNextRuntimeContract(root2, true);
    const result = await fileContext(root2, JSON.parse(fs21.readFileSync(0, "utf8")));
    console.log(JSON.stringify(result, null, 2));
    return result.status === "partial" ? 2 : 0;
  } catch (error) {
    console.error(error instanceof Error ? error.message : String(error));
    return 1;
  }
}

// runtime/vnext/src/cli.ts
var args = process.argv.slice(2);
var runner;
if (args[0] === "file-context")
  runner = runFileContextCli(args.slice(1));
else if (args[0] === "task-context" || args[0] === "task-read" || args[0] === "task-storage-migration" || args[0] === "task-migrate" || args[0] === "task-export") {
  runner = runTaskContextCli(args[0] === "task-migrate" ? "task-storage-migration" : args[0], args.slice(1));
} else if (args[0] === "bootstrap-project")
  runner = runBootstrapCli(args.slice(1));
else if (args[0] === "bootstrap-support")
  runner = runBootstrapSupportCli(args.slice(1));
else if (PREPARE_TASK_ADAPTER_COMMANDS.includes(args[0])) {
  runner = runPrepareTaskAdapterCli(args);
} else if (EXECUTE_STEP_ADAPTER_COMMANDS.includes(args[0])) {
  runner = runExecuteStepAdapterCli(args);
} else if (REVIEW_CHANGE_ADAPTER_COMMANDS.includes(args[0])) {
  runner = runReviewChangeAdapterCli(args);
} else
  runner = runCli(args);
runner.then((exitCode) => {
  process.exitCode = exitCode;
});
export {
  runBootstrapCli,
  runBootstrapSupportCli,
  runCli,
  runExecuteStepAdapterCli,
  runPrepareTaskAdapterCli,
  runReviewChangeAdapterCli,
  runTaskContextCli
};
