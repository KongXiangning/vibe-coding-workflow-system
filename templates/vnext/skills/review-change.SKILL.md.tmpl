---
name: review-change
description: Read the recorded step diff, persist one review result, then stop.
entry_contract:
  entry: review-change
  mode:
    - default
  intent: Review the current recorded step without changing the implementation.
  input_contract:
    required:
      - recorded_step_execution
    optional: []
  authority_owner: none
  mutation_boundary:
    product_files: []
    governance_sources: []
    forbidden_targets:
      - product files
      - test files
      - configuration files
      - direct governance writes
      - proposal or helper files
  internal_capabilities:
    - project-context-resolver
    - read-only-review-guard
    - diff-target-resolver
    - scope-guard
  runtime_operations:
    - task-state-transaction
  stop_conditions:
    - Runtime cannot provide one current executed step and exact diff target
    - the review cannot be completed read-only
    - Runtime rejects the review result
  output_kind: review-result
---

# vNext Skill: review-change

## Intent

Review the current executed step. Do not modify code, tests, configuration, or governance sources.

## Operating rules

1. Ask Runtime `review-context` for the current executed step, exact recorded diff target, acceptance, validation evidence, mutation scope, Persistent Tests, and any admitted findings. Review only that target and context.
2. Check step compliance, scope, implementation correctness, acceptance coverage, validation evidence, unauthorized persistent-test changes, and regression risk.
3. In discovery, report all clear repairable findings together. In verification, verify the admitted findings and affected regression surface; report any newly discovered issue without widening the repair yourself.
4. Return exactly one verdict:
   - `clean`: the recorded change is acceptable.
   - `findings`: include specific file, failure condition, required behavior, root-cause confidence, and evidence for each repairable issue.
   - `blocked`: include the blocker and recommended route when review or convergence cannot proceed safely.
5. Send the verdict to Runtime `record-review-result` on stdin. This canonical review result is the only durable effect and is the handoff to a later execute session. Do not create proposal or helper files in the project.
6. Do not repair code or advance the step. A recommendation must not invoke another public Skill.

## Required result

Return the Runtime-recorded `review-result`, inspected diff target, findings or blocker, evidence, and at most one `next_route` recommendation for a later caller invocation. Then stop.

terminal_boundary: public-entry-terminal/v1
