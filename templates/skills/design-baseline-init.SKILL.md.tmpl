---
name: design-baseline-init
preamble-tier: 1
version: 0.1.0
description: |
  Produce the first design baseline for a brand-new project before governance is frozen.
purpose: |
  为新项目完成首版设计基线，包括路线图、架构、领域模型、数据库、接口边界和详细设计草案。
stage: 初始化
trigger: |
  当新项目只有产品想法或原始需求，尚未形成架构设计、数据库设计、详细设计或接口边界时。
inputs:
  - user_request
  - product_goals
  - target_users_and_scenarios
  - non_goals
  - stack_preferences
  - delivery_constraints
reads:
  - .workflow-system/WORKFLOW_PROTOCOL.md
  - .workflow-system/FILE_SCHEMAS.md
  - templates/docs/
  - README.md
  - package.json
writes:
  - ROADMAP.md
  - ARCHITECTURE.md
  - DATABASE.md
  - docs/designs/database-design.md
  - docs/designs/detailed-design.md
  - docs/designs/api-contracts.md
  - docs/designs/domain-model.md
  - BASELINES.md
  - DECISIONS.md
forbidden_writes:
  - CURRENT_TASK.md
  - CONTRACTS.md
  - src/**
  - test/**
  - scripts/**
must_check:
  - 目标用户、核心场景、非目标和成功标准是否明确
  - 总体架构、数据模型、接口边界和关键流程是否能互相支撑
  - 未确认的产品、架构、数据库或发布选择是否显式列出
  - 哪些内容只是设计草案，哪些内容可以交给 greenfield-init 固化
stop_conditions:
  - 需求目标或目标用户不明确
  - 架构、数据库或交付方式存在多个高影响备选方案且未确认
  - 关键设计取舍无法从用户输入或现有约束中判断
output:
  - ROADMAP.md 初稿
  - ARCHITECTURE.md 初稿
  - DATABASE.md 与 docs/designs/database-design.md 草案
  - docs/designs/detailed-design.md
  - docs/designs/api-contracts.md
  - docs/designs/domain-model.md
  - BASELINES.md / DECISIONS.md 草案
handoff:
  success: greenfield-init
  failure: ask-user
decision_policy:
  mechanical: 可以整理需求、补齐设计章节、统一文档结构。
  taste: 对产品口味、UI 方向、命名偏好和工程风格必须显式暴露。
  user_challenge: 不得把未确认的产品定位、核心架构、数据模型或发布策略写成既定事实。
verification:
  - 设计文档能解释第一轮实现应从哪里开始
  - 未确认项保留为待确认问题，没有写入 CONTRACTS.md
  - 输出足以让 greenfield-init 固化已确认治理基线
allowed-tools:
  - Read
  - Grep
  - Glob
  - Write
  - Edit
  - AskUserQuestion
notes:
  - 这是新项目设计基线 skill，不直接开始实现功能。
  - 设计草案不能替代 CONTRACTS.md；只有经确认的内容才交给 greenfield-init 固化。
  - 完成后下一步通常是 greenfield-init，而不是 create-current-task。
---

# Skill: design-baseline-init

## Purpose

为新项目完成首版设计基线，包括路线图、架构、领域模型、数据库、接口边界和详细设计草案。

## Trigger

当新项目只有产品想法或原始需求，尚未形成架构设计、数据库设计、详细设计或接口边界时。

## Execution Rules

1. 先澄清目标用户、核心场景、非目标、成功标准、技术栈偏好和交付约束。
2. 先产出设计基线，不创建 `CURRENT_TASK.md`，不写业务代码。
3. `ROADMAP.md` 记录版本窗口、里程碑、当前阶段目标和明确不做的范围。
4. `ARCHITECTURE.md` 描述目标架构、模块职责、依赖方向、运行时边界和关键数据流。
5. `DATABASE.md` 作为数据库设计 home；必要时在 `docs/designs/database-design.md` 补充更细的数据库设计草案，描述领域对象、表结构、迁移约束、数据生命周期和不可破坏字段。
6. `docs/designs/api-contracts.md` 描述 API / DTO / Event / Module contracts 草案；不要把草案写入 `CONTRACTS.md`。
7. `docs/designs/detailed-design.md` 描述关键流程、状态机、错误路径、权限、异步任务和边界条件。
8. `docs/designs/domain-model.md` 描述核心实体、关系、状态和业务不变量。
9. `BASELINES.md` 和 `DECISIONS.md` 如被创建，只能写草案或待确认项；正式固化由 `greenfield-init` 完成。
10. 所有未确认设计取舍必须标为待确认，不得写成稳定事实。

## Hard Boundaries

- 不写业务代码。
- 不创建 `CURRENT_TASK.md`。
- 不修改 `CONTRACTS.md`。
- 不把设计草案伪装成稳定契约。

## Handoff

- 成功：`greenfield-init`
- 失败：`ask-user`
